# 05-telemetry-steering.md
- **Steering**: ON-TRACK. This doc *is* the fleet driver's cockpit — telemetry, monitoring, interrupt, and steer are exactly the user's three named features, and it treats them as the point ("steering without it is guessing"). No verification-as-product inversion here.
- **Residual inversion / drift**: none found. §6 ("steering exists to correct pathology, not to co-author") demotes *mid-turn micromanagement*, not the driver — that's a correct design stance, not an inversion. §4's "every control op is enforced by the non-LLM supervisor, not by model good-will" correctly keeps the plumbing under the driver.
- **Jargon**: (1) "effect-receipt Ack / `work_preservation: partial` / `applied_after_seq`" → "the reply tells you what your steer actually changed and what already ran." (2) "turn-scoped fencing / `turn_epoch` / stale-epoch ops" → "tag each command with the turn it's meant for; drop commands aimed at a turn that already ended." (3) "northbound/southbound" (used throughout the cluster) → "toward the orchestrator / toward the workers."
- **Soundness for the driver**: Strong and the most complete control spec in the cluster. Real gap: the plain two-way *messaging* flow the user asked for — "send a worker a message, get its reply/question back" — is scattered across `nudge`/`steer`/approval-routing and the worker's `ask` verb actually lives in doc 09 (E1), not here. A reader can't find "how do I just talk to a worker" in the messaging/steering doc.
- **One improvement**: Add a short §0 "the three things you do to a running worker — watch it, message it, stop/redirect it" in plain words, with the worker's `ask`→`fleet_respond` loop pulled in from doc 09, *before* the dense control-verb table. Lead with the driver experience; keep the race semantics as the appendix they are.

# 06-critiques-and-quibbles.md
- **Steering**: DRIFTING. This is where the cluster prefigures the later inversion: it twice declares a *support* element to be "the actual product" and openly demotes a named feature.
- **Residual inversion / drift**:
  - Q1: "the *telemetry, approvals, and session durability* are the actual product, **not the messaging**." — messaging is one of the user's three named driver features; calling it not-the-product is the inversion pattern in miniature.
  - Q6 title: "**Context engineering is the actual product** (the pitch says 'protocol', the value says 'briefs')." — elevates a craft-inside-the-driver to "the product," exactly what doc 19 retires.
  - Q2: "Steering is the most over-requested, least-used feature… Don't let the protocol's coolest verb distort the product." — the empirical prediction is fair, but framed as demotion of another named feature.
- **Jargon**: (1) "model diversity as error-decorrelation" → "different models make different mistakes, so one catches the other's." (2) "comparative advantage routing / vibes-based dispatch" → "send each job to the model that's actually best at it." (3) "the Foreman posture" (cross-doc) → "one hub on a big box you reach over SSH."
- **Soundness for the driver**: The critiques themselves are sound and valuable (prompt-injection Q4, orchestrator-is-the-weak-link Q3, failure taxonomy Q8 are all real driver concerns). The problem is rhetorical altitude, not technical error.
- **One improvement**: Reconcile every "X is the actual product" line with doc 19: rewrite them as "X is what makes the driver good" (telemetry, briefs, and durability are *how the driver drives well*), and keep messaging/steer as first-class driver features whose *usage frequency* is low — low-use is not low-value for the moments that matter.

# 07-roadmap.md
- **Steering**: DRIFTING toward the inversion. The build order leads with eval + verification + cross-review as the justification for existing, and pushes most of the actual fleet-driving surface (steer, nudge-at-tool-boundary, ask/respond, freeze, pause, the `baton top` dashboard, takeover) to M2 "earned by M1's eval."
- **Residual inversion / drift**: M1 is titled "The differentiating demo + cross-review + the supervisor," and M2 is where "Full control surface + human seat" lands. So the human-watches-and-steers-a-fleet experience — the user's headline product — is gated behind proving value via eval and cross-review. "Eval gates M2, not the reverse" and "the honest MVP is cut hard" make trust-proof the spine and the driving the reward.
- **Jargon**: (1) "each milestone… falsifies something measurable" → "each milestone proves or kills one assumption." (2) "differentiating exit demo (name it, gate on it)" → "the one demo a for-loop can't do; ship first." (3) "app-server / `codex mcp-server` / `codex app-server proxy`" — unavoidable, but flag on first use with one plain line ("the background process each vendor's CLI exposes for programmatic control").
- **Soundness for the driver**: The M0 empirical spikes (does `turn/steer` splice or queue; interrupt latency; `fleet_wait` under host timeouts) are genuinely load-bearing and correctly front-loaded — good. But the sequencing risks hitting the M1 exit gate having proven "cross-review works + restart recovers" while never having demonstrated the plain loop the user asked for: spawn two workers, watch a live feed, interrupt and steer one, get an answer back. That loop is diluted across M0/M1/M2.
- **One improvement**: Re-title M1 around the driver: "Drive two workers — spawn, watch a live feed, interrupt-and-steer, worker can ask you a question — and trust the result." Keep the restart-recovery demo and cross-review as *proof points inside that*, not as the headline that displaces it. Match doc 19's "plain next step" 1-5 list exactly.

# 08-shared-memory-and-pm.md
- **Steering**: ON-TRACK on hierarchy. §5 explicitly subordinates memory: "baton ships the operational + coordinative layers; the epistemic layer is an optional export, not a runtime dependency… Everything richer… is PM's job, not baton's." That is memory-as-support done right, and it resists building a "second brain."
- **Residual inversion / drift**: none on hierarchy. Minor over-bill: the whole doc is framed through the research-KG lens (PM), which imports research-project vocabulary into what is a coding-fleet bookkeeping problem — an altitude drift, not a goal inversion.
- **Jargon**: (1) "three memory tempos: operational / coordinative / epistemic" → "three kinds of memory: what's happening now, who's doing what, and what we learned." (2) "enforced causal backbone / temporal-coherence integrity" → "every decision must point at the evidence behind it, and can't cite evidence that came later." (3) "epistemic substrate / graph promotion" → "long-term memory / copying the important bits into long-term storage."
- **Soundness for the driver**: Sound. The task-DAG + artifact-registry design (§3) is real coordination the driver needs, the concurrency rules (§4: ledger is the only append point, no shared `state.json`) are correct and important, and "the repo is the shared memory" is the right call. Nothing missing for driving; if anything it's *more* rigorous than the driver strictly needs yet.
- **One improvement**: Open with a two-sentence plain framing — "the fleet needs to remember three things: the live event stream (doc 05), who's working on what (this doc), and a lightweight end-of-run scorecard; deep research memory is out of scope, exported to PM." Then translate the PM-derived lessons into fleet terms once, and stop leaning on the KG vocabulary.

# 09-revision-log.md
- **Steering**: ON-TRACK, with the *seed* of the later drift. Section A (control-plane liveness: interrupt, steer, `fleet_wait`, kill) is the most driver-focused material in the whole cluster — it hardens exactly the user's named features against real red-team attacks. But the doc's headline elevates verification.
- **Residual inversion / drift**: The headline names "**workers can forge their own verification**" first among the three load-bearing lessons, and C1's disposition elevates it to a doc-04 principle: "worker prose is non-authoritative; only independently re-run evidence counts… This is the deepest lesson of the round." That's the exact seed doc 19 says later rounds over-grew into "the Referee is the product." Here it's still correctly scoped as a *trust feature* (the hub re-runs the check), so it's a seed, not a full inversion.
- **Jargon**: (1) "`still_queued` necromancy" → "a stale queued message coming back to life after an interrupt and firing the thing you just stopped." (2) "at-least-once cursor / implicit ack / dedup the overlap" → "never drop an event page just because the waiter timed out." (3) "fencing tokens / worktree leases" (again) → "a version stamp so a stale command can't act on a worktree someone else now owns."
- **Soundness for the driver**: Excellent — this is where the driver's control loop actually gets made reliable. A1-A7 (two-phase interrupt, flush the nudge queue on interrupt, drop-don't-splice a stale steer, drain approvals before kill, verify process death) are precisely the bugs that would make "interrupt and steer worker 3" untrustworthy. Nothing missing; this doc earns the driver's dependability.
- **One improvement**: Reframe the headline so the driver is the subject: "these fixes make interrupt/steer/messaging actually reliable; independent verification is one of them — a trust feature, not the point." Move C1 from "deepest lesson of the round" to "one of the trust features that let the driver believe 'done'," matching doc 19's re-slotting table.

---
**Cluster summary:** Steered mostly right — this control-ops cluster is the beating heart of the fleet driver (05 and 09 are the cockpit and its reliability fixes; 08 correctly keeps memory as support). The drift is real but concentrated: doc 06 twice crowns a support element "the actual product" and demotes messaging, and doc 07's build order gates the plain drive-watch-steer experience behind eval/verification/cross-review. **Top fix:** re-title M1 in doc 07 around doc 19's "plain next step" (drive two workers, watch, interrupt-and-steer, worker can ask back, then trust the result) and demote the "X is the actual product" lines in doc 06 to "X is what makes the driver good" — and everywhere, cut the jargon (effect-receipt, necromancy, epistemic substrate, fencing tokens) down to plain English.