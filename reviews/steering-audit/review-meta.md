Read all six cluster docs plus the authoritative doc 19. Verdicts below.

### 13-revision-log-r2.md
- **Steering**: INVERTED
- **Residual inversion / drift**: This doc is the *origin* of the inversion doc 19 exists to reverse. "baton's defensible product is the *Referee* … and the *Conductor* (driving and steering a live fleet) is an optional branch, not the headline." And: "The supervisor's liveness apparatus (I1 fencing, I6 two-phase stop, the steering channel) moves from *headline* to *only-if-you-use-the-driving-branch*." That last sentence demotes the user's three named features (interrupt, steer, the control channel) out of the core. Also "Declare the **control/verification plane THE product**; make the capability/knowledge/stigmergy planes *optional consumers*" — verification is elevated to "the product."
- **Jargon**: "Referee / Conductor" (codewords the user dislikes — say "the trust check" and "the fleet driver"); "stigmergy / AIAI / O(N) — the bet" (say "shared-state coordination"); "bitter-lesson-proof" / "paradigm-vendor-obviation verdict" (say "won't be obsoleted by a stronger model" / "hard for a vendor to copy").
- **Soundness for the driver**: The mechanisms it validates (fencing, two-phase stop, at-least-once cursors, single-consumer approvals) are exactly the plumbing the driver needs and are technically sound. The defect is purely framing: it labels those mechanisms "the Conductor branch's real content" as if that branch were optional.
- **One improvement**: Re-title the "Referee, not Conductor" meta-finding to "the driver, with independent verification as its trust feature," and restore fencing/two-phase-stop/steering to the core rather than an optional branch.

### 14-practitioner-addenda.md
- **Steering**: DRIFTING (but by far the most *useful* doc in the cluster for the actual product)
- **Residual inversion / drift**: The craft is driver-positive, but the closing frame demotes driving: "build deep only the few things — independent cross-vendor verification, the fleet's earned wariness, the discipline of knowing which tasks *not* to orchestrate — that a better base model makes *more* valuable" and "orchestration a natively-multi-agent harness subsumes" listed under **ephemeral/rentals**. So it files fleet-driving itself as the perishable part and verification as the moat — the same inversion, softer.
- **Jargon**: "own the moat, rent the frontier" (say "build the durable parts deep, keep the throwaway parts thin"); "negative capability" (Keats reference — just say "knowing when to stop cleanly"); "context choreography" / "epistemic contagion" (say "reshaping the window as the task moves" / "one bad shared fact infecting every worker").
- **Soundness for the driver**: This is the strongest doc for the user's named features. #2 the interruption "violence spectrum" (gentle nudge-at-tool-boundary → hard kill) is exactly right for *interrupt/steer* and correctly notes the specs only built the loud end. #4 telemetry-derived-from-artifacts-not-narration and #16 "narrative, not a dashboard" nail *monitoring*. #3 partial-delivery result contract, #5 workers that can decline/counter-propose, and #17 decision-rationale capture are all real gaps in *messaging*. Nothing technically wrong; this is the doc to mine for the driver's feature list.
- **One improvement**: Re-label its own "durable vs rental" list so the fleet driver is the trunk that these features improve — not a rental — then promote #2/#3/#4/#16/#17 into the driver's MVP spec.

### 15-representation-and-computation.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: §0 explicitly re-inverts back the right way and matches doc 19: "the Conductor IS the ask; the Referee is what makes it trustworthy… A Conductor without a Referee is an unstoppable fleet you can't trust; a Referee without a Conductor is a grading service that doesn't orchestrate anything." Minor residual: the body still leans "if baton's Referee is the defensible core, the CPG is the representation the Referee should think in" — treats the Referee as *the* core in passing, but the doc's own §0 already corrects it.
- **Jargon**: "representation ladder / CPG (Code Property Graph)" (fine if defined, but heavy — say "different views of the code: text, syntax tree, data-flow graph"); "e-graph / equality saturation" (say "a compact set of all equivalent forms of the code"); "attestation-annotated representation" (say "code that shows which parts have been verified").
- **Soundness for the driver**: This is worker-*tooling* support, not messaging/telemetry/interrupt, and it's honest about that (§5: earned by demand, gated on the control plane proving out). The one genuinely load-bearing item for the driver is 4a: serve the verification/review step a **semantic (AST/data-flow) diff instead of a raw text diff** — cheaper to check, sharper signal. That directly makes "trust the result" (doc 19 step 4) faster.
- **One improvement**: Cut everything except the semantic-diff (4a) to an appendix and state plainly that its only near-term job is to make the driver's result-verification cheaper — the CPG/e-graph/fingerprint ladder is research, not product.

### 16-framing-critique-and-pivots.md
- **Steering**: INVERTED (partially redeemed — this is the doc doc 19 both borrows from and explicitly overrides)
- **Residual inversion / drift**: "build the durable core (the Referee) as a small measured thing before building anything else"; the value table's only "High and durable" rows are verification + routing, tagged "this is the Referee, the one real moat"; and "the corpus is a **research artifact**, not a product." Pivot 3 ("Ship a measurement, not a system") and "refuse to build until it returns a number" demote the driver to a hypothesis. Doc 19 keeps this doc's Pivot 1 (a plain-code layer under the AI) but explicitly *rejects* its move to strip the CLI agent out of the conductor's seat ("you lose 'your normal CLI agent is the conductor'").
- **Jargon**: "Referee / Conductor" again; "the four-plane cathedral" (cute but a codeword — say "the full multi-layer design"); "manufactures nearly every hard problem" is fine/plain. This doc is actually the *least* jargon-heavy — its problem is framing, not vocabulary.
- **Soundness for the driver**: Pivot 1 is technically sound and doc 19 adopts a reconciled version: a deterministic program underneath makes "interrupt worker 3" reliable. The *unsound* leap is concluding that because the plumbing should be a program, the *driver* (the AI decision-maker) should be too — which deletes the user's actual ask. Its enumeration of the six "LLM-orchestrator problems" (event loop, context poisoning, orchestrator-death) is a useful real list the driver's plumbing layer must solve.
- **One improvement**: Keep Pivot 1 as "a reliable program executes the AI's decisions" (doc 19's reconciliation) and delete Pivots 3/4's "don't build the system, just measure" — the build/no-build gate is now decided.

### 17-language-and-runtime.md
- **Steering**: ON-TRACK (mild drift via eval-gating)
- **Residual inversion / drift**: No framing inversion — it's about how to *build* the reliable core, which doc 19 endorses as plumbing under the driver. Only drift is the closing deference to the eval-first hedge doc 19 stood down: "if the number says 'one strong agent wins'… the best language for the product is *no product*." That's the doc-16/18 "measure before building" posture leaking in, now overtaken.
- **Jargon**: Mostly legitimate technical terms (OTP, BEAM, GenServer, NIFs). "OTP ceremony" and "the borrow checker is a fencing checker" are fine for an implementer. No user-losing codewords — this doc is clear.
- **Soundness for the driver**: Genuinely useful and correct. The core insight — the reliable coordination core the driver needs (supervision, restart, mailbox-ordered messages, backpressure) is almost exactly Erlang/OTP, and the user already runs Elixir/Phoenix — is a strong, concrete recommendation for making "interrupt/steer always lands" dependable. Go as the pragmatic alternative is well-judged. Missing: it discusses the daemon core but says little about the *messaging/telemetry surfaces* themselves (the northbound feed the operator watches) — those are where the driver's named features actually live.
- **One improvement**: Drop the "eval might say no product" caveat (decision is made) and add a short section on the messaging/telemetry surface (the live event feed + steering channel), since that's the driver-facing half the language choice also has to serve.

### 18-campaign-synthesis.md
- **Steering**: INVERTED (the hardest case — this is the doc that went furthest off, and doc 19 names it explicitly)
- **Residual inversion / drift**: The full inversion plus the retired "institution" jargon: "baton's durable identity may not be 'a thing that drives a fleet' but 'a neutral, cross-vendor verification-and-memory institution'… it makes the Conductor an *optional consumer* of its trust, not the product." Also "**#1 baton as a neutral trust institution vendors route through**" and "**#12 the reproducible run corpus *is* the moat**" promoted "from feature to thesis," and "**refuse to write another line of Conductor spec** until its pre-committed decorrelation threshold survives." This is exactly the "neutral trust institution" and "Referee-is-the-product" framing doc 19 retired by name.
- **Jargon**: "neutral trust institution" (doc 19 explicitly kills this — say nothing, it's a distraction); "E1 / E2 / decorrelation rate" (say "does a second vendor's check actually catch bugs the first missed, often enough to be worth it"); "the moat / the rental / bitter-lesson-proof" (say "the durable part / the throwaway part"); "CaMeL / dual-LLM / PIVOT-TO-REFEREE" (unexplained codewords).
- **Soundness for the driver**: Actively steers *away* from the product: "E2… minus the orchestrator entirely" and "needs **zero baton infrastructure**." It builds none of messaging, telemetry, or interrupt/steer — it argues for not building them until a verification-decorrelation number licenses it. That said, two technical nuggets survive and are useful: the honest downgrade that Claude/GLM workers are in-hub stdio children so they *don't* survive hub death (a real constraint on the driver's crash-recovery story), and the Darwin `boot_id`→`pid_alive ∧ process_start_time` liveness fix.
- **One improvement**: Reframe E2 from "the experiment that decides whether baton exists" to "optional de-risking of *one* driver feature (verification)," exactly as doc 19 re-slots it — and delete the "neutral trust institution / Conductor-as-optional-consumer" reframe outright.

---

**Cluster summary**: This cluster is the epicenter of the inversion — docs 13, 16, and 18 still treat independent verification (the "Referee") and even a "neutral trust institution" as the product and demote fleet-driving to an optional/rental branch; 18 is the worst and 13 is its source. Docs 15 and 17 are on-track (15's §0 already self-corrects; 17's OTP insight is a real gift for the driver's reliable core), and 14 is the hidden treasure — its interruption-spectrum, artifact-derived telemetry, narrative monitoring, and partial-delivery/decline primitives are the best concrete spec anywhere for the user's actual named features. Top fix: apply doc 19's re-inversion as an editing pass across 13/16/18 (fleet driver = product, verification = its trust feature, kill the "Referee/Conductor/institution" codewords), then promote doc 14's driver features into the MVP.