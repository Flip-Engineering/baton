Read all seven. Verdicts below.

### docs/10-interaction-model.md
- **Steering**: INVERTED
- **Residual inversion / drift**: The round-2 caveat block ends: *"The project's honest center of gravity is the **Referee** (doc 13 T5), not this doc's Conductor paradigm."* That is the exact inversion doc 19 retired — it names the fleet-driver ("Conductor paradigm") as *not* the center and hands the crown to verification. Also §0 bets the whole doc on *"stigmergic coordination… is where the leverage is,"* elevating a coordination substrate over the driver.
- **Jargon**: "stigmergy / AIAI (agent-infrastructure-agent interaction)" → *workers coordinate by reading/writing shared files (the log, git, a shared index), not by messaging each other*; "beyond HCI / three interaction topologies" → *three kinds of interaction: agent-uses-tools, agent-directs-agent, agents-share-a-workspace*; "the Conductor paradigm / the Referee" → drop the codewords, say *the fleet driver* and *the result-checker*.
- **Soundness for the driver**: The load-bearing content is actually sound and directly serves the driver: the two-channels split (communication vs steering, §1) is exactly the user's "bidirectional messaging + unidirectional interrupt/steer," and the "steering is SIGINT for agents, own out-of-band channel" argument is correct and important. Nothing missing mechanically; the problem is packaging, not engineering.
- **One improvement**: Delete the "center of gravity is the Referee" caveat and re-title the doc around the two channels (the driver's messaging + steering model). Keep §1 and §5 as the spine; demote the "three topologies / stigmergy is the bet" vocabulary to a short appendix so the driver's control model isn't buried under a coined paradigm.

### docs/11-capability-plane.md
- **Steering**: DRIFTING
- **Residual inversion / drift**: Not inverted — it correctly frames these as tools the fleet hands workers. But it over-bills the trust angle: *"the capability plane serving as the evidence layer the control plane's trust model stands on"* and *"The trust spine: every module's re-runnable output feeds supervisor I7"* lean back toward "verification is the point." The MVP line is honest ("The framework is the deliverable; the modules are earned by demand").
- **Jargon**: The self-referential codewords the user dislikes are thick here — *atlas, Vantage, Scratch, Cairn, Cartographer, Quartermaster, Skill Forge, the Evidence Ladder* → just call them *code search, debugger, shared scratchpad, run-memory, repo-map, dependency-checker, skill store, the test/proof escalation ladder*; "ACI envelope" → *one standard result shape (summary + data + a handle to the full output)*; "stigmergy" (again) → *shared-workspace coordination*.
- **Soundness for the driver**: Fine as support — none of these are on the messaging/telemetry/interrupt critical path, and the doc says so (they're later increments). The one genuinely driver-relevant hook — every long capability op is interruptible via the control plane — is present and correct.
- **One improvement**: Strip the seven codenames to plain names and lead with one sentence tying the plane back to the driver: "these are tools the driver gives workers so they do better work *and* so the driver can cheaply re-check it." That reframes the "evidence layer" language as a driver feature, not a rival product.

### docs/12-context-harness-engineering.md
- **Steering**: DRIFTING
- **Residual inversion / drift**: The caveat closes: *"See doc 13 and the honest project center of gravity, the **Referee** (doc 13 T5)."* Same demoting refrain as doc 10 — should be struck per doc 19. Otherwise the body is legitimately support ("how the driver presents work to each worker in that worker's style").
- **Jargon**: "the compaction firewall" → *when a worker's context gets summarized, baton re-sends the original goal so it isn't lost*; "the emergence engine / scaffold what-and-verify not how" → *because every tool returns the same shape, workers can chain them into steps we didn't script*; "semantics invariant, syntax projected" → *define a task once, render it in each vendor's prompt style*.
- **Soundness for the driver**: This is the single most driver-relevant support doc after the specs — "engineer context for windows it does not own" and the per-harness brief translation (Codex vs Claude vs GLM) is exactly what a cross-vendor driver needs, and §1c (re-inject the goal after the worker compacts) is a real, correct mechanism. §3's cross-vendor best-of-N ensemble is already self-corrected as *not* the asset — good.
- **One improvement**: Remove the "center of gravity is the Referee" pointer and rename the coined terms; the content already serves the driver, it just keeps genuflecting to the retired framing.

### spec/adapter-contract.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: none found. This is the driver's southbound plumbing, framed as such: *"the single most load-bearing practical artifact… if the mapping table has a lie in it, the adapter is a lie."*
- **Jargon**: clear — it's concrete API-mapping prose (`turn/steer`, `interrupt()`, `canUseTool`), the good kind of specific.
- **Soundness for the driver**: Strong and directly on the user's named features. Steer/interrupt/approve/kill are mapped per vendor with honesty about gaps (Codex `steer` behavioral semantics unverified → M0 must test; Claude steer is emulated-but-arguably-superior via PreToolUse hook; `kill` is a verified *sequence*, not one call). The "no silent emulation — card declares native|emulated|unsupported" rule is exactly right for a cross-vendor driver. Real cross-client-interrupt and `still_queued` reconciliation details show this was checked against actual APIs.
- **One improvement**: Add a small worked example of the full drive loop across two vendors (spawn Codex + spawn Claude/GLM, stream both event sets, land an interrupt on one) so the contract visibly delivers doc 19's "drive two workers" step, not just per-verb rows.

### spec/supervisor-state-machine.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: none found. Correctly slotted as plumbing *under* the driver: *"The LLM drives; the supervisor guarantees."* Verification (I7) appears as one invariant among seven, not as the product — exactly doc 19's ordering. The pilot/fly-by-wire analogy matches doc 19's "AI drives, plumbing makes it safe."
- **Jargon**: mostly clear; "fencing / turn_epoch fence" is worth a one-line gloss (*a version number on each worker-turn so a stale command from an old turn is rejected*), and "two-phase stop, drain-first" → *stop = ask-then-confirm, and first answer any pending approval so the worker doesn't hang*.
- **Soundness for the driver**: This is the backbone that makes "interrupt worker 3 always works" true, and it's the most rigorous doc in the cluster. The interrupt path (working→stopping→confirmed-idle, danger-window as first-class state, lease not released until diff re-read), the bounded-poll fix for `fleet_wait`, out-of-band human path (I5), and priority-vs-bulk lanes so an interrupt never queues behind a delta flood — all three of the user's named features (messaging, telemetry, interrupt/steer) have real mechanisms here. The honest MVP cut (I1/I3/I4/I6, single adapter, no steer/pause) is right-sized.
- **One improvement**: Nothing missing technically. Add one framing line at the top tying it to doc 19 explicitly ("this is the reliable coordination core that makes the driver's commands dependable") so a reader doesn't mistake this rigor for "the supervisor is the real system" — the very over-billing doc 19 called out.

### spec/capability-plane.md
- **Steering**: ON-TRACK (mild drift)
- **Residual inversion / drift**: One over-bill: *"The capability plane is thus not just tools — it's the evidence layer the control plane's trust model stands on."* True but rhetorically inflated toward "verification is the point"; it's cleaner as "cheap re-checks the driver uses to trust a worker's 'done.'"
- **Jargon**: "ACI surface / Agent-Computer Interface" → *tool outputs shaped for an agent (short summary + a handle to the full result)*; "stigmergy" (recurring) → *shared-state coordination*; "reverify / the evidence layer" → *the hub re-runs the check itself*.
- **Soundness for the driver**: Sound as support and not on the critical driver path. Good detail that a capability op is fenced like a control op and interruptible via the control plane, so the driver's steer/interrupt reaches a running tool, not just a model turn — that's the right integration. Result envelope + at-least-once cursor mirror the comms spec cleanly.
- **One improvement**: Reword the "evidence layer the trust model stands on" sentence to frame re-verification as a driver convenience ("so the driver can trust 'done' and move on") rather than a foundation the system "stands on."

### spec/communication-channel.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: none found. This is literally the user's named "bidirectional communication" feature, specified as such, with the correct separation from steering.
- **Jargon**: clear — plain message-kind tables (`brief`, `nudge`, `ask`, `answer`, `result`, `digest`). Only "stigmergically through the knowledge plane" in §8 needs the same plain gloss (*coordinate via shared files, not messages*).
- **Soundness for the driver**: Strong on two named features. The `ask`/`question` primitive (worker's voice, blocking vs non-blocking, single-consumer answer via `fleet_respond`, timeout that never strands or forces a guess on safety-critical) fills the exact gap the user cares about for two-way steering. The `digest` as the `fleet_wait` return with attention-items-first and facts-vs-untrusted-prose typing is the telemetry surface the driver reads. `result` correctly marked non-authoritative and re-run by the hub. Fencing on `answer`/`nudge` so a stale reply doesn't misfire is the right echo of the control plane.
- **One improvement**: The `digest` schema here is the driver's primary monitoring feed but telemetry/live-event-stream is only implied — cross-link explicitly to the supervisor's priority/bulk lanes and spell out the "live event feed of what each worker is doing" (doc 19 step 2) as a first-class digest mode, so the user's "watch them" feature has one obvious home.

---
**Cluster summary:** The two specs the user actually named features against — communication-channel and supervisor-state-machine — plus adapter-contract are steered right and genuinely sound: messaging, interrupt/steer, and the reliable coordination core all have real, vendor-checked mechanisms. The drift is in the three narrative docs (10, 11, 12), which still carry the retired "center of gravity is the Referee" refrain and a wall of coined codewords the user dislikes. Top fix: delete every "the Referee is the center of gravity" line from docs 10/12 and strip the self-referential codenames (atlas/Vantage/Scratch/Cairn/stigmergy/ACI) to plain English — the engineering underneath already serves the fleet driver; only the packaging still inverts it.