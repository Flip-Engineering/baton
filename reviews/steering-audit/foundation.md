### 00-brief.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: None found. This is the pre-inversion brief and it states the goal cleanly: "an orchestrator agent (Claude Code CLI or Codex CLI) directing full-session/full-harness subagents." Messaging, telemetry/monitoring, and interruption/steering are named up front (lines 9-11) as the core ask. Verification is not even elevated here — it appears only as D7 "trust" and D9 "evaluation," correctly subordinate.
- **Jargon**: (1) "capability matrix" — fine, but "capability plane" language doesn't appear here, good. (2) "subscription arbitrage" (line 20) — plain enough but borderline; could say "flat-rate plans instead of per-token billing." (3) The D1–D9 dimension codes are navigable, not jargon. Mostly clear.
- **Soundness for the driver**: Strong. D1 (control surface: spawn/send/stream/interrupt/steer/resume/usage), D3 (the event-loop problem — how a synchronous CLI agent gets "woken up" by a child), D4 (telemetry), D5 (interruption/steering granularities) are exactly the driver's hard parts and all present. The event-loop framing in D3 is the single most important technical insight for actually driving a fleet and it's here.
- **One improvement**: Add a one-line "definition of done for the MVP" mirroring doc 19's 5-step plain next step (drive two workers, watch, interrupt/steer, trust result, grow). The brief lists dimensions exhaustively but never says what the smallest shippable driver looks like — which is what keeps the reader anchored to the product vs. the survey.

### 01-landscape.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: None found. Verification/"institution" framing is absent entirely — this is a factual prior-art and protocol-state survey. The design takeaways (lines 29, 35, 65) all serve driving: state machines for pause/resume, task-augmentable spawn/send, graceful degradation when a vendor tightens seats.
- **Jargon**: (1) "[V]/[L]" and "3-vote panels / adversarially verified" (line 3) — internal research-process jargon that will glaze the user's eyes; keep the confidence marks but drop the panel-voting vocabulary or move it to a footnote. (2) "V-adjacent" (line 19) — invented hedge, say "partly verified." (3) "flat-rate-fleet economics" (line 57) — fine, plain.
- **Soundness for the driver**: Very strong and current. §4–§5 nail the control surfaces the driver needs (Claude SDK interrupt/hooks, Codex app-server, GLM-via-Claude-Code). §7 (ToS/rate-limit fragility, per-vendor concurrency) is load-bearing for a real fleet and honestly flagged — the driver must degrade to fewer seats mid-run. Nothing technically missing for messaging/telemetry/interrupt.
- **One improvement**: The doc buries the single most decision-relevant fact — Codex has native `turn/steer` and goal-pinning while Claude Code must emulate steering — inside §4 leads. Surface a one-line "what this means for interrupt/steer, the user's named feature" so the landscape connects to the product's headline capability.

### 02-harness-control-surfaces.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: None found. This is the doc the driver most depends on and it is squarely about driving: the 10-primitive vocabulary (spawn/prompt/stream/interrupt/steer/inject/approve/resume/usage/health) is exactly the fleet-driver's control surface.
- **Jargon**: (1) "northbound/southbound" doesn't appear here (good — it's in 03/04). (2) "harness card" (line 111) — reasonable metaphor but define it once in plain terms: "each adapter declares which controls it supports." (3) "capability-negotiated surface" (line 111) — say "the hub asks each worker what it can do and adjusts." Otherwise notably concrete and clear.
- **Soundness for the driver**: The strongest doc in the cluster for actual driving. The capability matrix (lines 95-107) is precisely what you need to build interrupt/steer/telemetry across vendors, and the key asymmetry (line 76: Claude Code control is per-process, Codex is a multi-client daemon → a Claude adapter must itself be a small daemon owning N children) is a real, correct, build-shaping insight. Steer emulation for Claude (line 69: interrupt → inject → continue, or hook at tool-boundary) is sound. Telemetry primitives (usage push notifications, OTel) are covered. Nothing missing.
- **One improvement**: Add a "health/liveness" row of substance — the matrix has a `health` primitive but the driver's hardest operational job is distinguishing crash vs. hang vs. quota-exhaustion vs. infinite-loop (doc 00's D8), and this doc only lists raw signals. A short mapping of "which signal means which failure, per harness" would make the driver actually safe to leave running.

### 03-protocol-analysis.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: None found. The verdict (lines 44-56) keeps the orchestrator agent driving and protocols in service: "app-server's steer/inject/goal primitives as the bar every adapter emulates toward." No institution/referee framing.
- **Jargon**: (1) "L1/L2/L3 layer model" and "northbound is L2, southbound is L1" (lines 6-13) — this is the cluster's densest jargon knot. It's genuinely useful shorthand but the user will bounce off it; add a one-line plain gloss: "the driver talks to the hub the way an agent calls a tool (MCP); the hub talks to workers the way a program drives a session." (2) "universal socket" (line 31) — actually a good plain metaphor, keep. (3) "push-shaped UX inside a pull-shaped protocol" (line 32) — clever but opaque; say "streaming updates while the driver waits."
- **Soundness for the driver**: Sound. The core decision — MCP northbound because it's the only client all three orchestrators already have, native L1 southbound so you don't waste Codex's steer/Claude's hooks — is correct and directly enables the driver. The `input-required` state as the guaranteed steering/approval path (line 23) is the right technical anchor for interrupt/steer. The ASCII diagram (lines 45-54) is the clearest single picture of the driver in the cluster.
- **One improvement**: State plainly, once, that A2A/L3 is deferred and MCP-tasks is aspirational — so the reader doesn't think three protocols must be built. The doc says "composes them" which risks reading as "implement all three"; the truth is "build MCP-northbound + native adapters now, borrow A2A's vocabulary, ship nothing L3."

### 04-architecture-options.md
- **Steering**: ON-TRACK
- **Residual inversion / drift**: None found. Options are all fleet-driving shapes; verification isn't treated as the product anywhere. "Works in both directions today (Claude→GPT+GLM and GPT→Claude+GLM are the same hub with a different MCP client attached)" (line 44) is exactly the north-star's bidirectional driver.
- **Jargon**: (1) "northbound/southbound" (line 5) — defined inline here, acceptable. (2) "long-poll-as-push" / "push semantics inside pull mechanics" (line 14) — the key idea but jargon-dense; the plain version is "one `fleet_wait` call parks the driver for minutes and returns only when a worker needs it." (3) "sidecar/foreman/mesh" deployment names (lines 73-75) — fine, and "mesh" is honestly flagged as "later, if ever."
- **Soundness for the driver**: Sound and this is the doc that actually specifies the driver. The `fleet_*` tool surface (lines 26-32) IS the fleet-driver API: spawn, send with turn|nudge|steer modes, wait, events, approve, interrupt-then-message, result, kill, list. That directly delivers the user's three named features (messaging = send/approve, telemetry = events/wait digests, interrupt/steer = interrupt/send steer). The event-loop analysis (lines 10-16) correctly identifies the crux and picks the pragmatic answer (long-poll now, own-the-loop later). Doc 19's reconciliation — AI drives, plumbing makes it safe — is implicitly this Option A hub, though this doc predates and doesn't name it.
- **One improvement**: Reconcile explicitly with doc 19's "you still drive from your CLI agent." Option D ("own the loop / conductor app") is described as "the natural end-state" and "strongest runtime," which slightly over-sells replacing the user's CLI — the exact thing doc 19 says NOT to do. Add a sentence: Option D is an optional power mode, not the destination; the product is the stock CLI agent driving through Option A's hub.

---

**Cluster summary:** This foundation cluster (00-04) is steered correctly — it predates the 13/16/18 inversion and never treats verification or a "trust institution" as the product; the fleet driver, its `fleet_*` control API, and the user's three named features (messaging, telemetry, interrupt/steer) are front and center throughout. The main liability is not drift but jargon density — worst offenders are the L1/L2/L3 "northbound/southbound" layer vocabulary (doc 03) and "long-poll-as-push / push-shaped inside pull-shaped" (docs 03/04). Top fix: add a plain one-line gloss beside each jargon knot and, in doc 04, demote Option D ("own the loop") from "natural end-state" to "optional power mode" so it stops implying the product replaces the user's own CLI agent — the one thing doc 19 forbids.