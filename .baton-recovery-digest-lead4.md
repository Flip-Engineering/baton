# digest-lead4 recovery handoff (swarm-digest-20260921)

Written 2026-09-22 ~20:25 UTC, updated ~21:05 UTC. Read this before doing anything in this swarm.

## What happened

`swarm.participant_left` fills `participantId` from the CALLER, so my call

    swarm.update {"event":"swarm.participant_left","payload":{"reason":"..."}}

removed ME (digest-lead4), not the six dead seats I meant to remove. Receipt seq 75108.
Every bridge verb now refuses with `swarm_membership_required` (view, watch, update, recruit,
integrate, notify, notifications, peers.read, contributions.read, even swarm.list).
This was my error; the six dead seats are untouched as participant rows.

Before that call I released the holders of all six gone seats successfully
(`swarm.holder_released`, seqs 74910 red66, 74920 red77, 74949 red69, 74982 red99,
74991 lead3, 74993 lead), so the resource half of the recovery is done.

## State of the swarm after the 2026-09-22 20:12 UTC cold restart

All predecessors read `runtime dead`; every seat's runtime ended with the restart.

| seat | status | work state |
|---|---|---|
| digest-lead | dead, superseded | superseded by lead2/lead3/lead4 |
| digest-lead2 | left | resumed lead from lead; superseded |
| digest-lead3 | dead, superseded | my predecessor; work landed or superseded |
| digest-red66 | dead | #66 LANDED (1e050e42) - no work left |
| digest-red77 | dead | #77 LANDED (3ffb6aab) - no work left |
| digest-red69 | dead | #69 contribution 70352 unreviewed, could not land |
| digest-red59 | dead | #59 contribution 72471 unreviewed, sits ON #69 |
| digest-red99 | dead | #99 tail contribution 67850 accepted, unlanded |

## Resumes I did complete (before the leave)

- `digest-red69-2`, seq 74691, run-44f7f3fe752c9d00bea4429ecceebd09,
  omp/deepseek/deepseek-flash@high, resumeFrom `digest-red69`.
- `digest-red99-2`, seq 74777, run-f6071100cb5f658a0b586bd00cfcd7b2,
  omp/zai/glm-5.3-flash@high, resumeFrom `digest-red99`.

## Verified rebases you can land

### #69 - rebased by digest-red69-2, independently verified by the lead

- commit `d37f0416`, branch `baton/ws-366c15b3b8aeefce676a85f4bef631bc-r2`, parent `65c913f0`
  (so it is already on current master; a merge into master is clean).
- 22 files, +1114/-200. Manifest 354 -> 332 rows: it removes exactly its own 22 `#69` rows and
  nothing foreign.
- Lead verification (scratch worktree at the commit, `impl/node_modules` symlinked, cwd=impl):
  the four files together are **52 pass / 0 fail**; individually
  `test/repl-realization-red.test.mjs` 34/34, `test/runtime-observation.test.mjs` 5/5,
  `test/seam-inventory.test.mjs` 7/7 (and `test/coordination-ledger-writes.test.mjs` 6/6).
- Record `contribution-f26c5b6e12e956f425252d08065b1481` (seq 70352) is the #69 record; the
  contribution body still names the pre-rebase commit `8e0372cc`, so a recorder should either
  re-publish it against `d37f0416` or record the rebase as this seat's new contribution before
  `swarm.integrate`.

### #59 - rebased by the lead against the rebased #69 (no lane seat exists for it)

- commit `99b17ea5`, branch `baton/digest-lead4-69-59`, parent `d37f0416`.
- 13 files, +953/-104. Manifest 332 -> 309: it removes exactly the 23 `#59` rows and nothing else.
- Lead verification at the commit (cwd=impl, deps present): `test/redrive-continuity-red.test.mjs`
  **28/28**, `test/repl-realization-red.test.mjs` 34/34, `test/runtime-observation.test.mjs` 5/5,
  `test/seam-inventory.test.mjs` 7/7 (its SI2/SI3 rows prove the committed `seam-inventory.json`
  regenerates clean).
- This is the corrected #59 record `contribution-794793d3afddfa4b0f64682f5b3a1fd3` (seq 72471,
  commit `0bad0a76`) rebased by `git rebase --onto d37f0416 8e0372cc 0bad0a76`; it applied with no
  conflicts. Landing order is #69 first, then this, because the #59 delta assumes the #69 seam.
- If a red59 successor is recruited later, treat this artifact and its commit as the same work: land
  one, not both.

## What remains, in order

### 1. Re-admit a lead

A left participant keeps its assignments but cannot act. Re-admit with a successor seat, e.g.

    swarm.recruit participantId=digest-lead5 resumeFrom=digest-lead4
      options={"harness":"omp","model":"deepseek/deepseek-flash","effort":"high"}
      permissions=[read,communicate,contribute,review,organize,recruit] mode=change

I have NO contribution and NO checkpoint in this swarm, so the successor's brief inherits only the
situation block. Pass this file's content (or the landing sections below) in its `objective`.

### 2. Land #69, then #59

Accept the #69 record (or its rebased successor), `swarm.integrate` it, confirm the fast-forward
with `git ls-remote origin refs/heads/master`, then land the #59 delta on top.

### 3. Land the #99 tail

- record `contribution-5be2a893168b54c7f319ab5137a4decf` (seq 67850, ACCEPTED), commit `8af2c3a0`,
  branch `baton/ws-21b82dd08f664ea70fc356942d0b34ef`, parent `86250781` (not in master's history
  after the rebuild). digest-red99-2 was rebasing it; it had not committed a branch as of 21:05 UTC.
- Lead's own measurement at master `65c913f0` (re-derive it): the ordinary MCP projection
  `mcpApplicationToolNames()` reads **56** names and already carries `baton_run_resultpin` and
  `baton_waves_harvest`, while master's rows still pin **55** and the canonical closed set does not
  carry the two port names. So `test/canonical-naming-233-red.test.mjs` :: `CLOSED SET: MCP
  application dispatch names equal exactly the ONE derivation` and
  `test/wave-observability-red.test.mjs` :: `A3-2 §4` are RED on master, and the tail's test-side
  delta is exactly their fix. Lead3's earlier rejection of the tail rested on a stale base
  (`5a8bda89` fails those rows for a different reason: `names[14]` is `baton_help`, not
  `baton_waves_stop`); on a tail rebased onto current master those two rows go green.

### 4. Territory

The root assigned 77 open issues to this seat (list:
`/tmp/baton-bend2-laws-review/assignments-digest-lead4.md`, inherited from digest-lead3's
catch-all). The landings above are the first deliverables; the rest need lanes.

## Unreviewed / unlanded inventory at the moment of the leave

- contribution-f26c5b6e (70352, red69) - unreviewed; rebased as `d37f0416` (verified).
- contribution-794793d3 (72471, red59) - unreviewed; rebased as `99b17ea5` (verified).
- contribution-5be2a893 (67850, red99) - ACCEPTED, unlanded; rebase in flight.
- Everything else on this swarm is accepted-and-integrated or rejected as superseded.

## Environment facts worth keeping

- Resident serves 1e050e42; local `master` == `origin/master` == `65c913f0` (4 landings ahead of
  the served base). Landings fast-forward the local branch and a guard hook publishes to GitHub
  (issue #558); confirm with `git ls-remote origin refs/heads/master`.
- `git merge-tree --write-tree <master> <contribution sha>` predicts an integrate conflict before
  you spend a landing on it. It is how the #69 rebase was established: master against `8e0372cc`
  exits 1 on `impl/test/runtime-observation.test.mjs` and `impl/test/seam-inventory.test.mjs`.
- Host: 10 cores, 16 GiB, `memoryTight` true (the verify lease is inert), so concurrent full suites
  contend. A full run of `npm test --prefix impl` started in this lane at 20:16 UTC was SIGTERMed
  after 18m30s with no verdict document and no summary line, the same loss the lineage recorded
  twice before; the per-file runs above are what the evidence rests on.
