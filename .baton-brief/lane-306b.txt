Deliver lane B of GitHub issue #306 (priority:high) in this Baton repository: the doctor shows the served commit and how far behind the target branch it is; a recruit on a resident whose served commit is behind is admitted with a typed `baseBehind` advisory; the wake feed announces an incarnation change. The worker runtime has no gh credential; the issue is transcribed here. Lane A (ds-306a) builds the reincarnate verb itself in application-host.mjs / application-deployment.mjs's host and publication regions — never touch those; your sub-orchestrator (kimi-306) writes docs/48 in parallel (this brief wins for this wave; name disagreements in notes).

REQUIRED:
1. Doctor: `deployment.doctor` (and `baton doctor --check`) `served` block already names `commit` and `branch` (impl/src/application-deployment.mjs — grep `"served"`); add `target: {ref, sha}` (the deployment's configured landing target — the branch the residents serve, from the deployment options, default the repository's default branch) and `behind: {count, commits: [{sha, subject}]}` bounded by a registry row, derived through the git authority (#301) with `git rev-list --count <served>..<target>` — computed at a bounded cadence or on demand, NEVER on every doctor read if the read path is a no-spawn path (docs/46 §7: read how #438 keeps git off the view; a doctor is a diagnostic read and may spawn once per call — say which rule you followed). `behind.count: 0` reads `upToDate: true`.
2. Recruit advisory: `swarm.recruit` on a resident whose served commit is behind the target is ADMITTED and its receipt carries `advisory: {kind: 'base_behind', served: sha, target: {ref, sha}, count, next: 'baton deployment reincarnate <target>'}`; the recruit brief's inheritance/base section names it in one line ("This resident serves <sha>, <n> commits behind <ref>; your base is the served commit."), so the seat knows its base is stale. The advisory is one derivation with the doctor's `behind` (one exported function on the deployment; the runtime asks the deployment, never git).
3. Wake class `incarnation_changed`: the `host.reincarnated` row (lane A records it) maps to a new closed wake class on the deployment watch (`baton deployment watch --follow --wake-class incarnation_changed`) and every bounded swarm watch sees it as a `dead`-class sibling with `terminal: false` and `next: re-read the view` — extend the closed class map where #442 added `dead` (impl/src/swarm-wake*.mjs / wake-stream mapping), never a side list. Until lane A's row exists, register the kind name in the map from the string lane A's brief fixes: `host.reincarnated`.
4. Red-before rows in a NEW file impl/test/issue306b-served-behind.test.mjs on the deployment fixtures (a temporary repository whose target branch is two commits ahead of the served commit): (a) doctor `served.target` and `behind {count: 2, commits}` and `upToDate: false`; (b) a recruit receipt carries the base_behind advisory and the composed brief names it; (c) served == target → `behind.count 0`, `upToDate: true`, no advisory; (d) a `host.reincarnated` row on the ledger wakes a `--wake-class incarnation_changed` watch; (e) the doctor and the advisory derive from one function (spy pin). Observe every row red at HEAD before editing.
5. Keep green: impl/test/application-deployment*.test.mjs, impl/test/doctor*.test.mjs, impl/test/issue335-*.test.mjs, impl/test/issue341*.test.mjs, impl/test/issue331-*.test.mjs, impl/test/issue339-*.test.mjs, impl/test/swarm-wake*.test.mjs, impl/test/wake-stream*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/issue438-*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/swarm-family-doc.test.mjs.

OWNED FILES (yours alone): impl/src/application-deployment.mjs (the doctor `served`/`behind` derivation ONLY — one exported function; not the host/publication regions), impl/src/swarm-runtime.mjs (the recruit receipt advisory and the brief's one base line ONLY), impl/src/swarm-wake*.mjs / the wake-stream class map, impl/src/limits.mjs (the behind-list bound with its derivation), impl/test/issue306b-*.test.mjs, plus regenerated artifacts. Do NOT edit application-host.mjs, coordinator.mjs, coordination-store.mjs, application-cli.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
