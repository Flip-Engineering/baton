Deliver lane B of GitHub issue #441 (priority:high, the operator's top-ranked gap) in this Baton repository: the seat-side READ verbs. The worker runtime has no gh credential, so the issue is transcribed here.

#441 — Seats cannot read the world they work in: not the issue their brief names, not the docs it cites, not the work their peers landed, not what peers are doing now. Lane A (a sibling seat, running now) makes the root's CLI admit the issue + cited docs as ONE ContextPackage (docs/32 REFLEX-3; impl/src/application.mjs admitContextPackage/attachContextPackage/contextPackageBranch ~10737–10790; coordination-store.mjs ~9131–9400) attached to the seat's run with scope worker:<seat>, and renders its branch digests in the brief. THIS lane gives the seat the verbs to read MORE than the brief carries, through the ONE bridge a seat already has.

THE BRIDGE (read first): impl/src/swarm-native-bridge.mjs (identity is minted on the bridge side; every verb runs contract admission BEFORE any runtime effect — the #318 knowledge verbs `run.knowledge.seed`, `run.scratchpad.append/read/elevate`, `run.board.post/read` are the pattern) and the dispatch table in impl/src/swarm-runtime.mjs ~236–350 (the ONLY region of that file you own — two other lanes own its recruit path and its inspect/brief composer). impl/src/swarm-native-access.mjs advertises what a seat may call; `swarm.guide` / the brief's Swarm section render that closed set (#372) — extend the set, never a side list.

REQUIRED — three read verbs, all read-only, all bounded by a registry row (impl/src/limits.mjs, derived never literal), all refusing typed:
1. `run.package.read {packageDigest, branch?}` → the package's branch list (name, digest, bytes) or ONE branch's text, resolved through `application.contextPackageBranch` exactly as MCP's `baton_package_read` does (impl/src/mcp-northbound.mjs ~1623/~2760 — reuse its projection, do not fork it). Refuses `context_package_not_found`, `context_package_branch_not_found`, and `package_not_attached_to_run` when the digest is not attached to the caller's run or its swarm (scope check on the attach rows).
2. `swarm.contributions.read {since?: seq}` → the accepted contributions on the caller's swarm since a seq: {participantId, contributionId, seq, summary, files, decision} — derived from the durable `swarm.contribution_recorded` + `swarm.contribution_reviewed` rows through the swarm-state fold (docs/46 §5 / #433 names this the contributions projection; if `swarm.view --projection contributions` does not exist yet, this verb reads the fold directly and the projection lane will reuse your derivation — put it in ONE exported function). No ledger scan on the read path beyond the fold (docs/46 §7).
3. `swarm.peers.read` → peers-now for the caller (docs/46 §6): for each other seat that can act: participantId, status, route, scope paths, last checkpoint summary and its age, current holder rows — derived from the participants fold the view already composes (call the runtime's existing participant composer for the fold-only fields; NEVER the workspace live reads — #438).
4. Advertise: the three verbs join the closed seat verb set (swarm-native-access.mjs) and the brief's Swarm section (the `swarm.guide` derivation) with one usage row each; the contract example the brief renders (#371) is untouched.
5. Red-before rows in a NEW file impl/test/issue441b-seat-read-verbs.test.mjs on the light SwarmRuntime + bridge harness (grep swarm-native-bridge*.test.mjs, issue318*, issue372* for how a seat calls the bridge): (a) a package attached to the seat's run is readable by digest and by branch through the bridge; an unattached digest refuses typed; (b) contributions.read returns the accepted rows since a seq in order and nothing before it; (c) peers.read lists the other live seat with its checkpoint and never the caller, and a spy on the runtime's git authority counts zero spawns; (d) the closed verb set in swarm.guide/brief contains exactly the three new names; (e) each verb refuses an unknown argument key typed (contract admission first). Observe every row red at HEAD before editing.
6. Keep green: impl/test/swarm-native-bridge*.test.mjs, impl/test/issue318-*.test.mjs, impl/test/issue372-*.test.mjs, impl/test/issue344-*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue430-*.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-native-bridge.mjs, impl/src/swarm-native-access.mjs, impl/src/swarm-runtime.mjs (the dispatch table region ~236–350 and one new exported contributions derivation ONLY — keep away from recruit, inspect and _composeRecruitBrief), impl/src/limits.mjs (new rows only), impl/test/issue441b-*.test.mjs. Do NOT edit application-cli.mjs, swarm-contract.mjs, swarm-state.mjs, mcp-northbound.mjs, application.mjs, coordination-store.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 6 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
