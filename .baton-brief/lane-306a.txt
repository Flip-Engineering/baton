Deliver lane A of GitHub issue #306 (priority:high) in this Baton repository: `baton serve --reincarnate <commit-ish>` / `deployment.reincarnate` — the running resident starts a successor over the same deployment, hands the publication over, and exits. The worker runtime has no gh credential; the issue is transcribed here. Your sub-orchestrator (kimi-306) writes docs/48-reincarnation-in-place.md in parallel; where this brief and docs/48 disagree, this brief wins for this wave and you name the disagreement in your contribution's notes.

THE FACT: seat workers are child processes of the resident on stdio pipes (impl/src/omp-rpc.mjs, coordinator.mjs) — a successor cannot inherit them. Part 1 is therefore a DRAIN-RESTART IN PLACE (#204): the old incarnation admits no new turns, in-flight one-shot turns complete on it, the successor adopts the deployment state directory, replays/opens, publishes, and only then the old incarnation withdraws its publication and exits through the #351 stop path (#288: never two publications, never none). Participant rows, workspaces (#428 custody on disk), contracts, parked guidance (#337) and claims survive unchanged; the participants' NEXT turns run under the successor (their runtime rows read `state: dead` until resumed — #364's participant_runtime_lost is the row that already says this; keep it, do not invent a second).

REQUIRED:
1. `deployment.reincarnate {target}` (application command, permission organize/owner; CLI `baton deployment reincarnate <commit-ish>` and `baton serve --reincarnate <commit-ish>` as the same verb sent to the running resident): resolves `target` through the git authority (#301; `git fetch` of the deployment's configured remote if the commit is unreachable, then `rev-parse`), records `host.reincarnation_requested {target: {sha, ref}, from: {incarnation, commit}}`, closes new-turn admission (the existing #351 admission gate), waits for in-flight turns with the #351 `host.stop_waiting` rows, then spawns the successor: `node scripts/baton.mjs serve` in a checkout at the target sha — the deployment's own checkout is `git checkout --detach <sha>` when the checkout is the resident's serving directory and no worker holds it (read how the residents are launched: cwd = <repo>/impl, the served commit is the checkout's HEAD; the successor serves the same directory after the checkout moves), with `BATON_PREDECESSOR_INCARNATION` naming the old incarnation. Records `host.successor_started {pid, incarnation}`.
2. Writer lease and publication handoff: the successor opens the same state directory; the old incarnation releases the coordination writer lease (impl/src/coordination-store.mjs `releaseWriterLease`) BEFORE the successor takes it and after its last row (the `host.successor_started` row is the last row the old incarnation writes until `host.publication_withdrawn`) — a successor that finds the lease held waits on the lease's own bound (limits registry), never refuses `coordination_writer_busy` at once. The successor publishes (connection.json, the local socket) with its own incarnation; the old incarnation observes the successor's publication (poll connection.json's incarnation field on the deployment's existing publication read; bound from the registry), records `host.publication_withdrawn` (it can no longer write rows: record it via the successor — the successor records `host.reincarnated {from, to}` when it sees the old process exit), withdraws its own listeners and exits 0 through the #351 stop path. A successor that dies before publishing: the old incarnation records `host.reincarnation_failed {step, cause: {exit, stderrTail}}` (the #326 bounded tail), reopens admission and keeps serving.
3. Refusals, typed and pre-effect, in the deployment's closed refusal set (find where `application_host_shutdown_failed`/deployment codes live): `reincarnation_target_unreachable {target}`, `reincarnation_in_flight {since, successorPid}`, `reincarnation_checkout_held {holders}` (a worker holds the serving checkout), `reincarnation_same_commit`.
4. Red-before rows in a NEW file impl/test/issue306a-reincarnate.test.mjs on the deployment fixtures the issue351/issue434/issue383 tests use (a real temporary repository with two commits; the successor may be a stub process the test provides through an injected spawner — inject, never spawn a real second resident in the unit rows): (a) the request records the row, closes admission and waits for one in-flight turn (stop_waiting row) before spawning; (b) the successor takes the lease only after the release (the lease row order pins it); (c) publication handoff: the old publication is withdrawn only after the successor's publication is observed, and the deployment is never unpublished in between (a read of connection.json at every step answers exactly one incarnation); (d) a successor that exits before publishing → reincarnation_failed with the tail, admission reopened; (e) the four refusals; (f) replay parity of the new rows. Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue351-*.test.mjs, impl/test/issue434-*.test.mjs, impl/test/issue383-*.test.mjs, impl/test/issue276-*.test.mjs, impl/test/issue288-*.test.mjs, impl/test/application-host*.test.mjs, impl/test/application-deployment*.test.mjs, impl/test/coordination-store*.test.mjs, impl/test/issue330-*.test.mjs, impl/test/issue364-*.test.mjs, impl/test/issue360-*.test.mjs, impl/test/frame-economics-red.test.mjs, every impl/test/application-cli*.test.mjs.

OWNED FILES (yours alone): impl/src/application-host.mjs, impl/src/application-deployment.mjs (the host/publication/reincarnation regions only — the doctor rows belong to lane B), impl/src/coordination-store.mjs (the writer-lease wait ONLY), impl/src/application-cli.mjs (the two spellings and their help rows only), impl/src/application.mjs (the command registration only), impl/src/limits.mjs (the new bounds with derivation comments), impl/test/issue306a-*.test.mjs, plus regenerated artifacts. Do NOT edit coordinator.mjs, swarm-*.mjs, worktree.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
