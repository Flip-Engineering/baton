Deliver lane A of GitHub issue #441 (priority:high, the operator's top-ranked gap) in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

#441 — Seats cannot read the world they work in. Every lane brief today is hand-typed text because a seat cannot read the GitHub issue its brief names, the design docs the issue cites, or the work its peers landed. Owed by THIS lane (item 1 + item 4 of the issue): the root's CLI pulls the issue and its cited docs at recruit time into ONE durable object using Baton's EXISTING context-package primitive, the recruit brief renders it, and the seat can cite it by digest. Never a worker-side gh call, never a parallel channel.

THE PRIMITIVE YOU EXTEND (read it first): docs/32-reflexive-orchestration.md §3.3 REFLEX-3 ContextPackage — typed immutable hand-off objects with named branches, admitted through `application.admitContextPackage(fields, principal)` (impl/src/application.mjs ~10737) into coordination-store.mjs `admitContextPackage` (~9252; branches need a content reference — see `_normalizeContextPackageBranch` ~9131 and `package_branch_empty` ~9093 for what a branch must carry; artifacts live in the store's content-addressed artifact CAS — find the admit path for a text artifact that the package branch can reference), attached with `attachContextPackage({runId, packageDigest, scope})` (~9337), resolved with `resolveContextPackageBranch(digest, name)` (~9171). MCP already exposes `baton_package_read` (impl/src/mcp-northbound.mjs ~1623, ~2760) — reuse its projection shape.

REQUIRED:
1. CLI: `baton swarm recruit <swarm> <seat> <objective> --issue N [--doc PATH ...]` in impl/src/application-cli.mjs `parseSwarmCli` (the closed flag set — #431 refuses unknown flags; add the two to the recruit spelling and its usage/help row). The ROOT process reads the issue with `gh issue view N --json number,title,body,labels,url` (the CLI runs on the root's host, which has gh; inject the reader for tests as `issueReader`) and each --doc path from the repo (bounded by the registry row you declare in impl/src/limits.mjs: `context_package.brief_bytes` for the rendered slice and `context_package.source_bytes` per branch — derive, never a literal). A doc path the issue body cites as `docs/NN-…md` is added automatically (the citation regex is ONE exported function).
2. Admission: the CLI admits ONE ContextPackage {name: `issue-N`, branches: [{name:`issue:N`, …}, {name:`doc:<path>@<sha>`, …}]} through the served application (the web transport's existing command for context packages — if the web layer has no route for admit/attach, add the minimal typed route pair in impl/src/web-northbound.mjs mirroring the MCP handlers; name it in needsFromOthers if you cannot), then recruits with `options.contextPackage = {digest}`; the runtime (impl/src/swarm-runtime.mjs recruit path — ONLY the args→run seam; another lane owns the bridge dispatch table at ~236–350 and another owns inspect()) attaches the package to the seat's run with scope `worker:<seat>` right after the run starts, and records nothing new — the attach row IS the durable fact.
3. Brief: `_composeRecruitBrief` (~2403) renders a `## Context package` section after `## Swarm situation`: package digest, then per branch its name, digest, byte size and the first `context_package.brief_bytes` of its text (issue title + body first). The root's objective stays the judgment paragraph. Pre-#441 recruits (no --issue) render no section — today's hand-typed briefs keep working.
4. Refusals typed pre-effect: `gh` missing or unauthenticated → `issue_reader_unavailable {issue, reason}`; the issue not found → `issue_not_found {issue}`; a --doc path outside the repo or unreadable → `context_doc_unreadable {path}`; oversize → the registry row's refusal. All added to the closed CLI refusal set the same way #430/#431 did.
5. Red-before rows in a NEW file impl/test/issue441a-recruit-issue-package.test.mjs on the light SwarmRuntime harness (grep swarm-runtime.test.mjs) and the CLI parse fixtures (grep issue431 / issue338 tests for the real-transport CLI rows): (a) parse admits --issue/--doc and refuses --issue without a number; (b) with an injected issueReader the recruit admits ONE package whose branches are `issue:N` and the cited doc, attached to the seat's run with scope worker:<seat>; (c) the brief renders the section with the issue title and each branch digest, bounded by the registry row; (d) a recruit without --issue renders no section (today's briefs unchanged); (e) reader unavailable → typed refusal naming N, no package admitted, no seat joined; (f) `baton_package_read` / the application's branch resolver returns the issue body for the digest the brief printed. Observe every row red at HEAD before editing.
6. Keep green: impl/test/swarm-runtime.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue431-*.test.mjs, impl/test/issue430-*.test.mjs, impl/test/issue373-*.test.mjs, impl/test/issue371-*.test.mjs, impl/test/issue341-*.test.mjs, impl/test/application-cli*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue292-coupling-truth.test.mjs, any test naming context_package / attach_package (grep impl/test).

OWNED FILES (yours alone): impl/src/application-cli.mjs (recruit parse + help + the issue/doc reader), impl/src/swarm-runtime.mjs (recruit args→attach seam and the brief's new section ONLY), impl/src/swarm-contract.mjs (the recruit options field), impl/src/limits.mjs (two new rows), impl/src/web-northbound.mjs (only if the admit/attach route pair is missing), impl/CLI.md (the recruit usage line), impl/test/issue441a-*.test.mjs. Do NOT edit swarm-native-bridge.mjs, swarm-native-access.mjs, swarm-state.mjs, coordination-store.mjs, application.mjs, coordinator.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 6 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
