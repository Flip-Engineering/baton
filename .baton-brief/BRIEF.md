Deliver lane C of GitHub issue #441 (bug, priority:high — the operator's P0) in this Baton repository: the `## Swarm situation` section and the seat's peer/contribution reads derive from ONE contributions derivation. Your brief renders #441 as a context package (## Context package, `run.package.read`) and the design is docs/47-the-reading-half.md (read §3 and §6 in full: "No second contributions derivation. The #433 view projection and run.contributions.read share ONE exported function; a second spelling is a bug"; "No per-view scans or spawns"). Lanes A (recruit --issue → ContextPackage, brief `## Context package`) and B (seat read verbs `run.package.read`, `run.contributions.read`, `run.peers.read`) are LANDED; #433's contributions projection is LANDED (the `contributions` projection in impl/src/swarm-runtime.mjs and the bounded watch's `events`).

REQUIRED:
1. ONE exported contributions derivation: find the function the #433 `contributions` projection renders from and the function lane B's `run.contributions.read` answers from; if they are two spellings, collapse them into one exported function in impl/src/swarm-runtime.mjs (or a sibling module the projection already imports) that both call — the reviewState rule (#350/#433: unrevoked accept review), per-row `{contributionId, participantId, subject, items, commit, reviewState, integration}` and the cursor — and pin the parity: the projection and the read answer byte-identical rows for the same swarm.
2. The `## Swarm situation` brief section (docs/47 §3 row 3, `_composeRecruitBrief` in swarm-runtime.mjs) renders peers-now and the contribution counts FROM the participants fold and that ONE contributions derivation — no ledger walk, no git spawn at compose time (docs/46 §7 binds it): each peer row `{participantId, status, route, holds: claims/work, lastContributionAt}`, the settled count, contributions by reviewState. Bound by the existing registry rows (never a literal; read impl/src/limits.mjs / frame-limits for the brief section bounds).
3. The seat-side read `run.peers.read` (lane B) answers the same peer rows the brief section renders (one derivation; pin the parity).
4. Red-before rows in a NEW file impl/test/issue441c-situation-one-derivation.test.mjs on the light SwarmRuntime harness (grep issue441b-seat-read-verbs.test.mjs and issue433-*.test.mjs for the fixtures): (a) the `contributions` projection and `run.contributions.read` answer identical rows (deep-equal) for a swarm with reviewed, unreviewed and integrated contributions; (b) the `## Swarm situation` section of a composed brief names the peers and counts from the folds and composes without spawning git (the #438 no-spawn guard the view tests use); (c) `run.peers.read` and the brief's peer rows are identical; (d) a brief for a swarm with no contributions renders the section byte-identically to today's (migration rule §7). Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue441*.test.mjs, impl/test/issue433-*.test.mjs, impl/test/issue438-*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-view-slices.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/swarm-projections.test.mjs, impl/test/issue318-*.test.mjs, impl/test/issue385-*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/swarm-family-doc.test.mjs, impl/test/surface-truth*.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the contributions derivation, the `## Swarm situation` section of _composeRecruitBrief, and lane B's contributions/peers read handlers ONLY — no other region: recruit, integrate, the fault fold, the drain and the bridge dispatch table belong to other lanes), impl/test/issue441c-*.test.mjs, docs/47-the-reading-half.md (only the §3 row-3 line, to name the landed derivation), plus regenerated artifacts. Do NOT edit swarm-state.mjs, swarm-contract.mjs, application-*.mjs, web-northbound.mjs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
