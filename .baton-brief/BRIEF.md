Deliver IMPLEMENTER LANE 3 of GitHub issue #314 in this Baton repository: a bridge session survives a resident reincarnation. Your brief renders #314 as a context package (## Context package, `run.package.read`); this file is the lane contract. FIRST ACT: in your worktree run `git rebase master` (master carries the design c22bf697, lane 4 d05ef90c and lane 1's core table), then read docs/49-mcp-primary-surface.md §6 (reincarnation survival) and §10, the red rows 314-e1 and 314-e2 in impl/test/issue314-mcp-core-surface-red.test.mjs, impl/src/mcp-web-bridge.mjs (discovery, the attestation to the resident incarnation digest, the client the wake plane opens through, the once-retry), impl/src/mcp-northbound.mjs (`WakeSubscriptions` invariants ~186+: the plane outlives the client it opens through; the notification method beside `WAKE_NOTIFICATION_METHOD` ~926), impl/src/mcp-bridge*.mjs (admission #270, attestation), docs/48 §2 and §11 (the handoff rows: `host.successor_started`, `host.stop_waiting {on: successor_publication}`, `host.successor_published`, `host.publication_withdrawn`, `host.reincarnated`; the successor publishes connection.json naming a DIFFERENT incarnation), and impl/test/issue306*.test.mjs (the injected-successor fixture you can reuse to stage a reincarnation under a bridge).

YOU OWN red rows 314-e1 (a bridge session re-binds to the successor: rediscover the publication, re-attest against the successor's digest scoped to the rebind, swap the client atomically, swap the wake-plane client, and emit ONE `notifications/baton/resident_reincarnated {from, to, at}` to the client) and 314-e2 (a call in flight during the handoff window is retried ONCE against the successor under the SAME idempotency key, so a mutation is never doubled and a read never fails with a dead transport). Turn both green on their own assertions; do not touch rows d1/d2 (lane 2, running beside you).

REQUIRED:
1. The rebind is driven by the same facts docs/48 names: the connection selector naming a new incarnation (a discovery re-read on a transport failure or on the wake `incarnation_changed` class), never a timer. The old incarnation's re-publish arm (#306r) is respected: a `host.reincarnation_failed` row means NO rebind (the publication is intact).
2. The re-attestation is scoped to the rebind (the session principal is unchanged; the digest is the successor's), one notification per handoff, and `WakeSubscriptions` survive (subscription ids unchanged; the plane re-opens through the new client).
3. The once-retry: the bridge's dispatch keeps the idempotency key of the in-flight call and replays it once after the rebind; a second failure crosses typed (the #430 set), never `command_outcome_unknown`.
4. docs/49 gets a "landed" note per law you closed; docs/48 §11 gains one item naming the bridge's rebind beside the handoff rows; regenerate seam-inventory and surface-gate.

RED-BEFORE rows in impl/test/issue314-lane3-reincarnation-rebind.test.mjs over the real bridge and the issue306 injected-successor fixture: (a) a served bridge session across a staged handoff answers the next call from the successor and the client received exactly one `resident_reincarnated`; (b) an in-flight mutation during the window is applied once (the ledger holds one row under the key); (c) a wake subscription opened before the handoff still delivers after it; (d) a handoff that fails (306r fixture) produces no rebind and no notification. Bound every await (docs/42 §8); end every resident/successor you spawn (#471 helper).

VERIFY ONLY with `node --test` on impl/test/issue314-*.test.mjs, impl/test/mcp-*.test.mjs, impl/test/issue270-*.test.mjs, impl/test/issue294-*.test.mjs, impl/test/issue306a-reincarnate.test.mjs, impl/test/issue306r-republish-arm.test.mjs, then `node scripts/run-suite.mjs test/issue314-mcp-core-surface-red.test.mjs` (BATON_HOST_CAPACITY_DISABLED=1). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #314 lane 3: …'. Plain technical English.
