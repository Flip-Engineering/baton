Deliver GitHub issue #477 (bug, priority:high) in this Baton repository: route liveness never verifies on a real deployment because the coordinator's deferred startup reconstruction replaces the adapter listener the liveness wrapper installed at open. Your brief renders #477 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read the issue (it is the measured hand-back of lane ds-460, e2af4fa1), then impl/src/route-liveness.mjs (`_wrapAdapters`, `ensure()`, the probe deadline and the `unknown` settle), impl/src/coordinator.mjs (where the coordinator registers its adapter listener when `completeDeferredStartup` / the startup reconstruction finishes — #351 lane 3 / #434 made the open async: `coordinationAsyncOpen: true` in impl/src/application-deployment.mjs), every real adapter's single callback slot (`_cb` / `_onEvent` in impl/src/harness-*.mjs, omp-*.mjs, claude-session, codex-appserver, cli-adapters, grok-acp, kimi-acp), and impl/test/readiness-honesty-red.test.mjs (A1a–A6 stage rows and the fixture the #460 lane repaired by delivering to every observer — the fixture-side workaround this lane makes unnecessary on the served path).

THE FACT: the deployment installs the liveness observer while it opens; the coordinator registers its own listener later, when its deferred reconstruction completes; adapters keep ONE slot, so the later registration replaces the wrapper. At probe time the adapter's listener is the coordinator's callback, the probe's lifecycle.spawned / resource.provider_call / lifecycle.turn_completed fire, and the liveness controller sees none of them: `ensure()` waits out its (unref'd) probe deadline and settles `unknown` every time. The tier never verifies and never blocks, silently.

REQUIRED (root-side, one owner, one order):
1. Decide and implement the ownership: EITHER route-liveness re-installs its wrapper after the coordinator's registration (subscribe to the completion of the deferred startup, the same signal #434 exposes) OR the coordinator forwards the listener it replaces (wrap, never overwrite) — pick the one with a single derivation of "who observes the adapter" and say why in the contribution. No second listener slot invented on adapters unless every adapter gets it from one shared base.
2. A real `openBatonDeployment` (the fixture the readiness rows use, over a temporary repository, with the async open) sees a probe's turn events reach the controller and `ensure()` settle `verified`; the doctor row reads `liveness.state: 'verified'` with `probedAt`.
3. The A1a–A6 stage rows in impl/test/readiness-honesty-red.test.mjs that this seam was blocking: re-run them and report which now pass on their own assertion (retire their manifest rows by the rule: a -red file is in `rows` or in `converged`, never both/neither; re-render with `manifestRows` from impl/scripts/suite-verdict.mjs) and which still fail on a genuinely missing design (keep those with their reason).

RED-BEFORE rows in impl/test/issue477-liveness-observer-survives-startup.test.mjs: (a) on a real openBatonDeployment with the async open, a probe's events reach the controller and ensure() settles verified (red at master: settles unknown); (b) the order pin: the observer is installed AFTER (or forwarded through) the coordinator's registration, asserted on the adapter's listener identity; (c) a scriptable-adapter fixture with a single `_cb` slot (the shape the #460 hand-back named) no longer goes deaf on the served path. Bound every await (#460 / docs/42 §8).

VERIFY ONLY with `node --test` on the file you add, impl/test/readiness-honesty-red.test.mjs, impl/test/readiness-credentials-red.test.mjs, impl/test/issue434-deferred-open-reconstruction.test.mjs, impl/test/issue351-open-liveness.test.mjs, impl/test/issue375-*.test.mjs and `node scripts/run-suite.mjs` on the two readiness files (BATON_HOST_CAPACITY_DISABLED=1). Never the full suite; never run-suite --changed (#424). Regenerate seam-inventory and surface-gate if a seam changed.

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #477: …'. Plain technical English.
