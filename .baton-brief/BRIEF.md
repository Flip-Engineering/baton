You are a PROBE seat in this Baton repository. Two facts to observe and report; do not edit any source or test file.

1. (#462) Your OWN process environment: list every environment variable whose name starts with `BATON_` (names only, never values — print `name=<set>`), and state explicitly whether any of BATON_INCARNATION, BATON_PREDECESSOR_INCARNATION, BATON_PREDECESSOR_PID, BATON_PREDECESSOR_COMMIT, BATON_REINCARNATION_TARGET is present. Use `node -e 'console.log(Object.keys(process.env).filter(k=>k.startsWith("BATON_")).sort().join("\n"))'`.
2. (#464) `swarm.view` with `{"projection":"participants"}` through the swarm client your brief's Swarm section names (`node "$BATON_SWARM_CLIENT" swarm.view '{"projection":"participants"}'`): record the number of participant rows returned, the `page` field verbatim (total / served / next), whether any row carries a `workspace.commits` array, one row's `commitsTotal`, one row's `role` / `roleBytes` / `roleRef`, and the total byte length of the answer. Then `swarm.view` with no arguments: record `projection`, `narrowed` if present, and the byte length.

WRITE both records into a NEW file PROBE-462.md at the repository root (the only path you may touch), commit it on your lane branch (git add PROBE-462.md && git commit -m "probe-462: seat env and participants page as observed"), and record ONE contribution with `node "$BATON_SWARM_CLIENT" swarm.update` following the admitted payload example your brief's Swarm section renders — two items, each `evidence` carrying the observed values (names only for env). Then end your turn. NEVER run git stash. Never print credential values. Never run the suite.
