Deliver GitHub issue #464 (bug) in this Baton repository. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. Read docs/46 (frame economics), impl/src/limits.mjs (the VIEW block and how a row's bound is derived), impl/src/swarm-projections.mjs / swarm-state.mjs (where the participant row is projected: `role`, `scope`, `route`, `options`) and impl/src/swarm-native-bridge.mjs (#343/#457 paging: `swarm-page:full:6`) before editing.

WHAT HAPPENS LIVE (primary, 36-seat swarm, 2026-09-18 12:17Z): a participants page serves 6 of 36 rows in 664 761 bytes (~110 KB per row) because each row carries `role` = the recruit's whole objective text (the brief pointer plus the lane-contract summary) and the same text again where the row renders attention/activity; the explicit full view is 3.3 MB. A seat walking its peers pages six times; the root's CLI view of one swarm is 3.3 MB.

REQUIRED:
1. ONE derivation of the participant row's budget: `role` is rendered as its FIRST LINE bounded by a limits row (read whether `view.role.head` or a similar row exists; if not, add ONE row in the VIEW block with its derivation — from `wire.frame` and the roster size the frame must hold, e.g. a 36-seat roster fits one frame — and name it in docs/43 or docs/46 where the VIEW rows are listed). The row carries `roleBytes` (the full length) so a reader knows what it did not get; the full text stays reachable through the ledger row it came from (`swarm.participant_joined`) and `run.member.view` — name the exact read in the row (`roleRef: {seq}`), never a second copy.
2. Every other duplicate on the row goes: attention/activity rows reference the participant, never re-render its objective; `scope` stays (it is the claim); `route`/`options` stay bounded as they are.
3. The measured participants page must hold a 36-seat roster inside `wire.frame` (36 rows × the bounded row): prove it in a fixture with 36 seats whose objectives are 3 KB each — the page reports `served: 36`.
4. Red-before rows in a NEW file impl/test/issue464-participant-row-budget.test.mjs: (a) a row's `role` is its first line + `roleBytes` + `roleRef`; (b) the 36-seat fixture's participants projection fits one frame (served 36, no page cursor); (c) the CLI full view of that fixture stays under the frame; (d) the attention row for a seat carries no copy of the objective. Observe every row red at HEAD before editing. Keep the bridge's paging contract (#343/#457 rows) green — the page shape does not change, only the row size.
5. Keep green: impl/test/swarm-projections.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue457-*.test.mjs, impl/test/issue433-*.test.mjs, impl/test/issue268-*.test.mjs, impl/test/issue349-*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/limits*.test.mjs, impl/test/issue292-coupling-truth.test.mjs.

OWNED FILES (yours alone): the participant-row projection (impl/src/swarm-projections.mjs or the swarm-state.mjs projection region ONLY — name which), impl/src/limits.mjs (ONE new VIEW row, if needed), the attention-row renderer's objective copy ONLY, impl/test/issue464-*.test.mjs, plus regenerated artifacts. Do NOT edit swarm-runtime.mjs, swarm-native-bridge.mjs, application-cli.mjs or the docs beyond the one VIEW-row line (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
