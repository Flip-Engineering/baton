Deliver the remaining arm of GitHub issue #306 (part 1) in this Baton repository: docs/48-reincarnation-in-place.md §11 item 7 — the crash table's RE-PUBLISH arm. Your brief renders #306 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read docs/48 §2 (the nine-step protocol and its crash table), §11 items 7, 13 and 14, and impl/src/application-deployment.mjs (the reincarnation region: `#successorSpec`, `#awaitSuccessorReady`, the readiness marker, the publication wait `host.stop_waiting {on: 'successor_publication'}`, the withdrawal, the three `host.reincarnation_failed` sites at ~4302/4422/4577) before editing. Four live reincarnations of the primary today (the last at 11acf339) went through the happy path; this lane closes the one window part 1 accepted narrower.

WHAT IS MISSING (docs/48 item 7 verbatim): a successor that dies AFTER writing its readiness marker and BEFORE publishing leaves the old incarnation's bounded publication wait to expire; the old then WITHDRAWS its own publication and exits narrating `residentState: 'reconciliation_required'` — no `host.reincarnation_failed {step: 'publication_handoff'}` row and NO re-publish of the old authority. The deployment is left with no publisher until an operator restarts something.

REQUIRED:
1. When the publication wait expires (or the successor process exits) after the marker and before `host.successor_published`, the old incarnation records `host.reincarnation_failed {step: 'publication_handoff', cause: {exit, signal, stderrTail, waitedMs}}` (the existing row kind; the new `step` value joins its closed set) and RE-PUBLISHES its own authority: reopens turn admission (the `reincarnation_in_flight` refusal lifts), keeps its writer lease (it never released it — verify; if the release already happened, re-take it through the same lease path the open uses and record that), and keeps serving. The receipt the verb answered earlier is the reincarnation's promise; the durable rows are its outcome — a caller following `deployment reincarnate --follow` or the `incarnation_changed` wake class must see the failure row, never silence.
2. The successor's marker/publication race is closed by ONE state read: the old decides `published` vs `failed` from the ledger rows and the child's exit, never from a timer alone; a successor that publishes AFTER the old already re-published is refused by the old's held writer lease (the successor's open must fail its lease take and exit with its own `host.reincarnation_failed {step: 'lease_held_by_predecessor'}` narration) — pin that too.
3. Red-before rows in a NEW file impl/test/issue306r-republish-arm.test.mjs on the issue306a fixture (the injected successor stub): (a) a stub that writes its marker then exits without publishing → the old records `host.reincarnation_failed {step: 'publication_handoff'}`, admission reopens (a recruit is admitted after), the publication is still the old's, and `host.publication_withdrawn` is NOT recorded; (b) a stub that writes its marker and stalls past the wait bound → the same outcome with `waitedMs` on the row and the stub killed; (c) a stub that publishes after the old re-published → the stub's lease take fails typed and the old keeps serving; (d) the `incarnation_changed` wake class / `deployment reincarnate --follow` surfaces the failure row. Observe every row red at HEAD before editing.
4. Keep green: impl/test/issue306*.test.mjs, impl/test/issue461-*.test.mjs, impl/test/issue462-*.test.mjs, impl/test/issue351-*.test.mjs, impl/test/issue434-*.test.mjs, impl/test/issue450-*.test.mjs, impl/test/application-deployment*.test.mjs, impl/test/application-host*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/application-deployment.mjs (the reincarnation/handoff region ONLY), impl/src/application-host.mjs (only if the withdrawal/re-publish needs a host hunk — name it), docs/48-reincarnation-in-place.md (§11 item 7 rewritten as landed truth ONLY), impl/test/issue306r-*.test.mjs, plus regenerated artifacts. Do NOT edit coordinator.mjs, coordination-store.mjs, swarm-*.mjs, application-cli.mjs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 4 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
