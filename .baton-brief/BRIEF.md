Deliver the SSE follow-up of GitHub issue #316 in this Baton repository (the lane that landed #316 handed this over; docs and gh are outside the worker runtime, so the finding is transcribed here).

WHAT LANDED (d29e2b13, #316 item b): impl/src/wake-stream.mjs declares ONE closed attachment-end reason set {error, restart, transport_closed} and the typed final frame `baton.wake_attachment_closed {reason, at, resumeFrom}`; the WS binding writes it when an attachment ends, and the CLI follow (impl/src/application-cli.mjs followWakes / the watch legs) rides it on its ended row and delivers it as the only page when the attachment delivered nothing.

WHAT REMAINS: impl/src/web-northbound.mjs `_handleWakes` — the SSE leg never names the resident's own end: no `event: ended` (and no attachment_closed frame) is written on shutdown, so a follow over SSE can only infer transport_closed. Owed: in the SSE leg's finally/close path, write the SAME `baton.wake_attachment_closed` frame through the ONE mapping wake-stream.mjs exports (reason `restart` when the resident is stopping for a restart/shutdown, `transport_closed` when the client went away, `error` on a stream failure), with `resumeFrom` = the last seq the leg delivered; never a second reason set and never a bare stream end.

Red-before rows appended to impl/test/issue316-provider-degraded.test.mjs (or a NEW impl/test/issue316-sse-attachment-closed.test.mjs on the same served-transport fixture): (a) a follow over the SSE leg receives baton.wake_attachment_closed {reason: 'restart', resumeFrom} when the resident shuts down mid-follow (drive the real served host the way issue356/issue339 rows do and stop it); (b) the reason set the SSE leg uses is the wake-stream export (identity or grep row); (c) the CLI follow over SSE renders the same ended row it renders over WS. Observe red at HEAD before editing.

Keep green: impl/test/issue316-provider-degraded.test.mjs, impl/test/wake-stream*.test.mjs, impl/test/swarm-wake*.test.mjs, impl/test/issue339-*.test.mjs, impl/test/issue356-*.test.mjs, impl/test/issue365-run-follow-legs.test.mjs, every impl/test/web-northbound*.test.mjs, impl/test/issue394-web-wait-ceiling.test.mjs, impl/test/issue430-swarm-refusal-set.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/web-northbound.mjs (the _handleWakes SSE leg ONLY), impl/src/wake-stream.mjs (only if the mapping needs an export), impl/test/issue316-*.test.mjs. Do NOT edit application-cli.mjs, application-host.mjs, swarm-*.mjs or the docs. NEVER run git stash. Never print credential values.

VERIFY: node --test on the test file(s) plus the keep-green files — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`; include the regenerated artifact if it changed.

FINISH (muse is one-shot — there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
