Deliver the WIRING half of GitHub issue #306 (priority:high) in this Baton repository: make the landed `deployment.reincarnate` verb reachable end-to-end. Lanes A (809341b3: the verb in impl/src/application.mjs `reincarnate()`, the handoff rows, refusals, the `host.reincarnation.wait_ms` bound, both CLI spellings in impl/src/application-cli.mjs) and B (6bc66bcb: doctor served/behind, the base_behind advisory, the `incarnation_changed` wake class) are LANDED on your base. What is missing is exactly what lane A handed back — without it the resident answers 'unsupported command' and the CLI refuses `cli_command_unavailable` locally:

REQUIRED:
1. impl/src/web-northbound.mjs: admit `deployment.reincarnate` on the web lane the way `deployment.doctor` is admitted (~136: the DEPLOYMENT_WEB_ENTRIES-style row on both spellings via `canonicalAndTransportNames`, the COMMAND_CAPABILITY entry with the right capability (a lifecycle act: read what `deployment.doctor` declares and what the verb's own authorization in application.mjs ~13734 requires — owner/lifecycle), ARG_FIELDS `{target}` (~304: doctor's argument authority is the closed empty set; reincarnate's is exactly `target`), and WEB_DIRECT_PORT_COMMANDS so the port dispatches it to `application.command('deployment.reincarnate')`).
2. impl/src/application-semantics.mjs: the canonical operation row for `deployment.reincarnate` beside `deployment.doctor` (~1752) with its field union `{target}`, and the `application.commands` surface alias, so `cliDispatchTransports` admits it and `validateApplicationCommandArgs` (application.mjs ~255–275) finds a canonical operation for it.
3. Pins that move with the vocabulary (read the failing pin's message, move the ONE pinned set, never a second table): the surface-gate ledger / CLI card ledger (impl/scripts/surface-gate.mjs, surface-divergence-ledger.json — regenerate with `node impl/scripts/surface-gate.mjs --write`), `render-surface-docs.mjs --write` for CLI.md, and any test that pins the web command-table key set or the canonical operation count (grep `COMMAND_CAPABILITY`, `DEPLOYMENT_WEB_ENTRIES`, `canonicalOperation` in impl/test).
4. Red-before rows in a NEW file impl/test/issue306w-reincarnate-reachable.test.mjs through the REAL served transport (grep issue338/issue365/issue431 tests for the fixture that serves a resident over the local socket and drives the CLI): (a) `baton deployment reincarnate <sha>` against a served resident reaches the verb — with an unreachable target it answers the typed `reincarnation_target_unreachable` refusal FROM THE RESIDENT (not 'unsupported command', not cli_command_unavailable); (b) `baton serve --reincarnate <sha>` takes the same path; (c) the web card / `application.commands` lists the verb once with `target` as its only argument. Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue306a-*.test.mjs, impl/test/issue306b-*.test.mjs, impl/test/web-northbound*.test.mjs, impl/test/issue233-*.test.mjs, impl/test/issue338-*.test.mjs, impl/test/issue365-*.test.mjs, impl/test/issue431-*.test.mjs, impl/test/issue340-*.test.mjs, impl/test/grammar-m3-red.test.mjs (if present), impl/test/surface-truth*.test.mjs, impl/test/application-semantics*.test.mjs, every impl/test/application-cli*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/web-northbound.mjs (the admission rows only), impl/src/application-semantics.mjs (the one canonical row + alias), impl/scripts/surface-gate.mjs and its ledger (regenerated), impl/CLI.md (regenerated), impl/test/issue306w-*.test.mjs, plus the one pinned set that moves. Do NOT edit application.mjs, application-deployment.mjs, application-host.mjs, application-cli.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
