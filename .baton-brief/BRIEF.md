Deliver IMPLEMENTER LANE 2 of GitHub issue #314 in this Baton repository: receipts and wakes — never a blocking call over MCP. Your brief renders #314 as a context package (## Context package, `run.package.read`); this file is the lane contract. FIRST ACT: in your worktree run `git rebase master` (master carries the design c22bf697, lane 4 d05ef90c and lane 1 — the core table `impl/src/mcp-core-tools.mjs`), then read docs/49-mcp-primary-surface.md §5 (receipts and wakes) and §10 (closed-set owners), the red rows 314-d1 and 314-d2 in impl/test/issue314-mcp-core-surface-red.test.mjs, impl/src/mcp-core-tools.mjs (`coreVerbFacts(tool, verb)` answers `{mutation, long, wake: {kinds, settleOn}}` from the core rows — the four long verbs are baton_run:start, baton_swarm:recruit, baton_swarm:check, baton_waves:start; do NOT add a second table), impl/src/mcp-web-bridge.mjs (`_inspectOutline` answer at ~638-665 — the blocking read this lane retires; grep `_inspectOutline` in impl/test before touching it), impl/src/mcp-northbound.mjs (run/waves receipt derivation beside `swarmChangedRow` / `swarmReceiptNext`, the wake notification method `WAKE_NOTIFICATION_METHOD` ~926, `WakeSubscriptions`), #302 (mutations return receipts) and #294 (the wake subscription) and their rows, and #479 (the MCP doctor projection lacks the `stopping` section the client's `doctor()` already carries — fold that one-field change in here since you touch the same files).

YOU OWN red rows 314-d1 (no blocking reads: a core mutation never answers a whole view; every mutation answers the #302 receipt) and 314-d2 (a long operation answers receipt + wake subscription id; the wake arrives as the notification, never as a held call). Turn both green on their own assertions; do not touch rows e1/e2 (lane 3) — they stay listed in impl/scripts/expected-red-tests.json under reason "#314".

REQUIRED:
1. Every core mutation verb answers the receipt shape #302 defines (`{receipt: {command, event: {kind, seq, ts, actor}, changed: [...]}, next}`), derived from the same receipt derivation the CLI uses (one derivation; a bridge-only shape is a red row). The `_inspectOutline` blocking answer is retired; its consumers (grep) are migrated.
2. The four long verbs answer `{receipt, wake: {subscriptionId, kinds, settleOn}}` composed from `coreVerbFacts`, and the settle arrives on the existing wake plane (`notifications/baton/wake`, #294) — the call itself returns as soon as the receipt exists. A client that wants the old blocking behaviour gets it by waiting on the wake, never by a flag.
3. #479: `baton_deployment {verb: doctor}` projects the client's `doctor()` result including its `stopping` section.
4. docs/49 gets a "landed" note per law you closed; MCP.md's verb table (rendered by render-surface-docs.mjs from the core table — lane 4's renderer) shows receipt/wake per verb if the renderer reads `coreVerbFacts`; regenerate seam-inventory and surface-gate.

RED-BEFORE rows: extend impl/test/issue314-mcp-core-surface-red.test.mjs's d1/d2 only as far as the design says; add impl/test/issue314-lane2-receipts-wakes.test.mjs over the real McpFleetServer fixture the red file uses: (a) each core mutation verb's answer is byte-identical to the CLI receipt for the same operation; (b) each long verb answers within the receipt bound with a wake id and the settle notification carries the row; (c) #479's doctor projection during a drain. Bound every await (docs/42 §8).

VERIFY ONLY with `node --test` on impl/test/issue314-*.test.mjs, impl/test/mcp-*.test.mjs, impl/test/unified-mcp-surface.test.mjs, impl/test/issue302-*.test.mjs, impl/test/issue294-*.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue344-*.test.mjs, then `node scripts/run-suite.mjs test/issue314-mcp-core-surface-red.test.mjs` (BATON_HOST_CAPACITY_DISABLED=1). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #314 lane 2: …'. Plain technical English.

TWO HAND-BACKS FROM LANES 1 AND 4 ARE YOURS TOO (same files, one landing): (i) impl/scripts/render-surface-docs.mjs `renderMcpToolInventory()` renders from the SHIPPED core table (`CORE_TOOL_NAMES`, `CORE_TOOL_VERBS`, `coreToolDefinitions()` in impl/src/mcp-core-tools.mjs), the block's third column keeps the bare advertised name (surface-conformance `namesFromMcpInventoryBlock` reads it), and MCP.md's inventory block is regenerated (`node impl/scripts/render-surface-docs.mjs`); lane 4's ES-B row goes green with it. (ii) impl/test/unified-mcp-surface.test.mjs row U-E2 pins the six `baton_surface_*` meta tools as ADVERTISED on the ordinary surface — docs/49 §2 replaced them with the single `baton_surface {verb}` tool; route the META block and the three meta calls through `baton_surface {verb}` (the row's own law — advertised == dispatchable, no kernel merge, the ghost refused — is already satisfied) and retire its manifest row under "#314".
