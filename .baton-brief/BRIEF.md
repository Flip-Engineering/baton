Deliver GitHub issue #444 in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: Model profiles gain Design Arena rankings beside Artificial Analysis: live API by openRouterId, design Elo on doctor rows and the brief's route table, prefer: design on recruit route comparison.

CONTEXT: #429 landed impl/src/model-profile.mjs — ONE live source (Artificial Analysis, `fetchModelProfiles({fetchImpl, key, now})`, `profileFor(catalog, slug, {billing})`, cache file `model-profiles.json` in the state dir, staleness from the registry row `model_profile.catalog_staleness_ms` in impl/src/limits.mjs, key at `BATON_AA_KEY` else `~/.config/baton/aa_key` declared ONCE in impl/src/adapter.mjs (`AA_CREDENTIAL_ENV`, `AA_CREDENTIAL_FILE`), absent key = degraded row never a refusal, subscription routes carry `price: null, priceReason: 'subscription'`). Route rows in impl/src/application-deployment.mjs carry `aaSlug` and `billing`; the doctor renders profile rows; `advanced.modelProfiles` injects a catalog for tests. Read all of that first: the Design Arena source must ride the SAME shapes, never a second profile system.

DESIGN ARENA API (confirmed 2026-09-18): base `https://www.designarena.ai/api/v1`, Bearer key (`Authorization: Bearer …`) obtained by application; `GET /arenas` (arenas and categories: Website, 3D Design, Data Visualization, Game Dev, UI Component, plus the Code Categories aggregate), `GET /models` (every model with rankings across arenas), `GET /leaderboard/{arena}/{category}`; every entry carries `openRouterId` (a string like "anthropic/claude-opus-4.6", or null) to join on; rankings are Bradley-Terry Elo from blind pairwise votes; CDN cache 5 minutes (stale-while-revalidate 10) for models/leaderboards, 1 hour for arenas; errors are JSON `{success:false, error:{code, message}}` with 400/401/403/500.

REQUIRED:
1. impl/src/model-profile.mjs gains a second live source: `fetchDesignRankings({fetchImpl, key, now, timeoutMs})` calling `GET /models` ONCE (the models answer carries every arena) and a joiner that attaches `design: {observedAt, arenas: [{arena, category, elo, rank, votes}]}` to a profile by the route's `openRouterId`. The cache rides the same file with a `design` section and the same staleness row; a stale or absent design section degrades to `design: null, designReason: 'stale' | 'key_absent' | 'unavailable' | 'no_openrouter_id'`, never a refusal of the route. `fetchImpl` is injected exactly like the AA reader; no real network in tests.
2. The credential: `BATON_DESIGNARENA_KEY`, else `~/.config/baton/designarena_key` (0600; the same config-root resolver adapter.mjs uses for aa_key — declare `DESIGNARENA_CREDENTIAL_ENV` / `DESIGNARENA_CREDENTIAL_FILE` beside the AA pair, read at the deployment root only; impl/src/runtime-isolation.mjs already sweeps every environment name matching its `SECRET_NAME` regex (any name containing KEY) out of worker environments, so BATON_DESIGNARENA_KEY needs NO new list entry — pin that with one test row instead). Add `designarena_key` to the deployment snapshot's credential exclusions (`SNAPSHOT_CREDENTIAL_PATHS`, application-deployment.mjs ~300) and to .gitignore beside aa_key.
3. Route rows: add `openRouterId` to the route normalization beside `aaSlug` for every served route (zai/glm-5.3-flash, kimi-code/k3, deepseek/deepseek-flash, deepseek/deepseek-v4-pro[1m], gpt-5.6-sol, grok-4.5, claude-opus-4-6 / sonnet, muse → null); the doctor's profile rows render the design arenas beside the AA quality score; the brief's route table (#341, swarm-runtime.mjs `_composeRecruitBrief` — ONLY the route-table row rendering, another lane owns the rest of that function) shows `design: <best arena> <elo>` when present.
4. `swarm.recruit` route comparison (#341, the `routes.considered` field) accepts `options.prefer: 'quality' | 'design'` (closed set; default quality); `design` orders candidates by the best design Elo among ready routes; the comparison row names the axis it ordered on. Refuse an unknown prefer value typed with the closed set.
5. Billing stance unchanged: Design Arena carries no price; subscription rows keep price null.
6. Red-before rows in a NEW file impl/test/issue444-design-arena-profiles.test.mjs (grep impl/test/issue429-model-profile.test.mjs for the injected-fetch fixtures and the deployment doctor rows): (a) a fixture /models answer joins by openRouterId → the profile row carries design arenas; (b) key absent → design null with reason key_absent, the route still ready; (c) staleness from the registry row, never a literal; (d) the doctor row and the brief's route table render the design field; (e) recruit with prefer: design orders on design Elo and names the axis; unknown prefer refuses with the closed set; (f) a snapshot with a stray designarena_key at the repository root is refused as credential material like aa_key. Observe every row red at HEAD before editing.
7. Keep green: impl/test/issue429-model-profile.test.mjs, impl/test/issue341-*.test.mjs, impl/test/issue324-*.test.mjs, impl/test/application-deployment*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/runtime-isolation*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue292-coupling-truth.test.mjs.

OWNED FILES (yours alone): impl/src/model-profile.mjs, impl/src/adapter.mjs (the credential pair ONLY), impl/src/runtime-isolation.mjs (the secret-name list ONLY), impl/src/application-deployment.mjs (route normalization openRouterId + doctor profile rows + snapshot exclusion ONLY — two other lanes own its close path/startup report and its route degrade on provider fault), impl/src/swarm-runtime.mjs (the route-table row rendering in the brief and the `prefer` axis of the recruit route comparison ONLY — keep away from recruit's args→attach seam, inspect, the dispatch table and the worker-fault observation, all owned by other lanes), impl/src/limits.mjs (a new row only if one is needed), .gitignore, impl/test/issue444-*.test.mjs. Do NOT edit application-cli.mjs, swarm-contract.mjs, swarm-state.mjs, coordinator.mjs, web-northbound.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 7 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl .gitignore && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
