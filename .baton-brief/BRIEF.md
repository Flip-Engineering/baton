You are kimi-vis, a DESIGN seat (read + contribute; you do not edit impl/src). Deliver the swarm VISIBILITY model for this Baton repository as ONE design document plus red-before test skeletons, covering GitHub issues #433, #364 (its view half), #268 (remainder) and the root's standing visibility gaps. The worker runtime has no gh credential, so the issues are transcribed here.

#433 — The bounded swarm watch returns on its first wake, so a contribution recorded between that return and the next --after-seq re-arm is invisible unless the root scans the ledger by hand (2026-09-18: omp-373's contribution at seq 14840 went unseen for 25 minutes; the seat read `paused` with a worktree_foreign_changes attention row and nothing said "unreviewed contribution"). Owed: (1) `swarm.view S --projection contributions` (and the bridge) lists every recorded contribution with its review state `unreviewed | accepted | rejected` from the swarm.contribution_reviewed rows — ONE derivation, cursorable in the #312 vocabulary; (2) the bounded watch's wake frame carries EVERY wake row at or after --after-seq up to the frame bound, or names `pendingSince: <seq>` so nothing is lost between wakes; (3) an `unreviewed_contribution` attention row on the participant when a contribution sits unreviewed past one recruit-brief cadence.

#364 (view half) — after a resident restart a seat whose worker died reads runtime {state: idle, live: true, turn: paused}; on 2026-09-18 the clone listed 42 seats active of which 2 had a working runtime; the other 40 rode into every new recruit brief's Peers section. The runtime half (a swarm.participant_runtime_lost row and a worker_lost_on_restart attention) is being landed by another lane; the VIEW half is yours: how liveness, activity and "can this seat act" are ONE derivation the outline, the participants projection, the Peers section and scopeOverlap all read.

#268 remainder — participant activity and usage are invisible on swarm.view / run show; only the raw worker log knows. Owed: the participant row carries `activity {lastEventKind, lastEventAt, turnsCompleted, contributions}` and `usage {tokens|unavailable, providerCalls|unavailable}` derived from the rows the store already folds (the #305 progress checkpoints, resource.tokens rows) — never a per-view scan of every worker log (P9; see #438: swarm.view must not spawn git or scan ledgers per seat).

STANDING GAPS (root, 2026-09-17/18): (a) brief exposure by relationship — what a seat's brief shows about its peers derives from its relationship to them (same group / same checkout / same swarm / delegating parent), not from a flat list of everyone; (b) the ROOT is not a participant row, so its reviews, guides and stops have no actor row the view can render (a root review lands with reviewerId: null — task #20); (c) attention rows carry no coverage — the view cannot say which seats' attention it has examined and which it has not, so an empty attention list is indistinguishable from an unexamined one; (d) the view's cost: the outline for a 45-seat swarm must answer within the transport's request bound with no per-seat process spawn.

READ FIRST (in your worktree): docs/39-swarm-runtime.md (Loose and tight orchestration; Communication; Delegated organization), docs/45-open-coordination.md (§6 peers-now brief section, §8 what the view renders — align with it, do not duplicate), docs/36-unified-control-grammar.md §5 laws and §7, impl/src/swarm-contract.mjs SWARM_VIEW_PROJECTIONS (the one projection table; #427 added `context`), impl/src/swarm-runtime.mjs inspect() and _composeRecruitBrief (how projections and the brief are composed today; note the #428/#425 live git reads the design must remove per #438), impl/src/wake-stream.mjs (wake classes, frame bound), impl/src/swarm-state.mjs (participant status settlement #350, group departed rows #395).

DELIVER:
1. docs/46-swarm-visibility.md — in docs/39's house style (rules, not prose): (a) the ONE liveness/activity derivation and the closed participant `runtime` state set every surface reads; (b) the `contributions` projection with review state and cursor, the `unreviewed_contribution` attention row and its cadence derivation; (c) the multi-wake frame contract for the bounded watch (every row from --after-seq to the frame bound, `pendingSince` when cut) and what the CLI/bridge render; (d) brief exposure by relationship — which relationship classes exist and what each exposes; (e) the root as a participant row (`root`, its permissions, how its reviews/guides/stops are attributed); (f) attention coverage (`attention: {rows, coverage: {examined: [...], unexamined: [...]}}`); (g) the cost rule: every projection derives from folded rows and caches the runtime maintains on change; NO process spawn and NO per-seat ledger scan on a read (cite #438's sites); (h) what stays OUT; (i) migration: every existing projection keeps its meaning; (j) closed-set owners and the seam map by file and function; (k) open questions.
2. Red-before skeletons in the -red convention (docs/44; header stage `design-not-landed`, expected red, each row's message names what the implementer must land) that run today on the light SwarmRuntime harness (grep swarm-runtime.test.mjs): impl/test/issue433-contributions-projection-red.test.mjs (projection with review state; multi-wake frame; unreviewed attention) and impl/test/issue268-visibility-red.test.mjs (activity/usage on the row; liveness derivation; root row; attention coverage; a spy row that swarm.view --projection participants spawns no process).
3. Your contribution: items = the doc and the two files (delivered), plus one item per OPEN QUESTION you could not settle (status not_delivered, the question in `change`); carriedForward = the implementation lanes you recommend in order, each with its files; notes = your honest assessment of the cost rule (g) against what inspect() does today.

RULES: edit only docs/46-swarm-visibility.md and the two new test files. NEVER run git stash. NEVER run the test suite beyond `node --test` on your two files. Never print credential values.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A docs impl/test && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
