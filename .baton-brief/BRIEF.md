Deliver GitHub issue #489 (bug, priority:high) in this Baton repository: on a long-lived swarm every Run view exceeds the 512 KiB projection ceiling, so `run show` refuses at every depth and every `recruit --issue` refuses `application_run_view_oversize` at package attach. Read the issue and its first comment (`gh issue view 489 --comments` works from your worktree; the comment carries the measurements: a seat's rendered brief is 171 KB on the primary, 158 KB of it the `## Swarm situation` section; 21 KB / 13 KB on a 12-seat swarm), then impl/src/swarm-brief.mjs (the situation section: which seats it renders and how), impl/src/application.mjs (`_buildView` ~8437–8893 and `_planningView` ~6646–6706: where the objective is copied into the view — goal, plan nodes, planPreview — and where `MAX_RUN_VIEW_BYTES` is checked; the depth/section/item narrowing that #335 added and where it applies), impl/src/swarm-runtime.mjs (~7257: the recruit's `attachContextPackage` call), impl/src/limits.mjs (`view.run.bytes`), docs/39-swarm-runtime.md (the brief), and the #464 landing (participant-row budget: the ONE row-budget derivation projections use — reuse it, never a second one).

REQUIRED:
1. The brief's `## Swarm situation` section is bounded by #464's row budget: seats that can act (live, joined, queued) render in full; settled seats (completed, stopped, left) render as one line with a count per status and the pointer `swarm view <swarm> --projection participants`; the section's bytes derive from that budget, not from the swarm's age. Red-before: a 45-seat swarm with 40 settled seats renders a situation section under the budget and names the count.
2. The Run view carries the objective ONCE (`goal.objective`); plan nodes and planPreview reference it (a digest or `{ref: 'goal.objective'}`), never a copy. Red-before: a run whose objective is 200 KB projects under the ceiling.
3. Narrowing runs BEFORE the ceiling: `run show <run> --depth outline` (and index/section/item) answers for a run whose full content is over the ceiling; the oversize refusal names the section and its bytes and prescribes a narrowing that works. Red-before through the real transport.
4. The recruit's package attach never reads the full Run view: `attachContextPackage` binds the package through the coordination store and answers the attach receipt. Red-before: with `MAX_RUN_VIEW_BYTES` forced small in the fixture, `recruit --issue` still admits and attaches.
Every await bounded (docs/42 §8); no new numeric constants (derive from the existing budget and ceiling).

VERIFY ONLY with `node --test` on the file you add (impl/test/issue489-run-view-bounded.test.mjs), impl/test/issue464-*.test.mjs, impl/test/issue335-*.test.mjs, impl/test/issue343-*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue441-reading-half-red.test.mjs. Never the full suite; never run-suite --changed (#424). Regenerate seam-inventory and surface-gate if a seam changed. docs/39 gets one paragraph on the bounded situation section; docs/47 §9 one sentence on the attach.

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #489: …'. Plain technical English.
