Deliver the second half of GitHub issue #464 in this Baton repository. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. The first half (lane ds-464, commit 3aebda2e: the participant row's `role` is the objective's first line bounded by the new `view.role.head` registry row, with `roleBytes` + `roleRef`) is landing on master; you own the OTHER driver of the live page size.

WHAT ds-464 MEASURED: on the live 36-seat swarm a participants page served 6 of 36 rows in 664 761 bytes. The text budget is now fixed, but the measured driver of the LIVE page is `workspace.commits` on the participant row — impl/src/swarm-runtime.mjs builds `commitsByParticipant` (~lines 2735–2750, one entry per wrapper-attributed commit) and the row field at ~2821 (`commits: Object.freeze([...(commitsByParticipant.get(participant.participantId) ?? [])])`) crosses every page whole, unbounded, for every seat.

REQUIRED:
1. ONE registry row `view.workspace.commits` in impl/src/limits.mjs (VIEW block) derived the way `view.served_behind.commits` is (read that row's derivation; LIST_PAGE_ITEMS is the shape), bounding the commits a participant row carries: the row carries the bounded TAIL (newest first) plus `commitsTotal` (what it did not carry) and the reach for the rest (`run.member.view` / the ledger `workspace.committed` rows — name the exact read). No new numeric ceiling typed by hand; the value derives from the existing row.
2. A PAGE drops `commits` the way it already drops `lastToolRows` / `native`: add 'commits' to `SWARM_VIEW_PAGE_HEAVY_PARTICIPANT_FIELDS` in impl/src/web-northbound.mjs (read how the page arm applies that set) so a bridge page carries the count and the reach only.
3. Red-before rows in a NEW file impl/test/issue464b-workspace-commits-budget.test.mjs: (a) a participant with 200 wrapper-attributed commits answers a row whose `commits` length equals the registry row's value, newest first, with `commitsTotal: 200` and the reach; (b) the bridge's participants PAGE for that fixture carries no `commits` array but keeps `commitsTotal`; (c) a 36-seat fixture whose seats each carry 60 commits answers its participants projection in ONE frame (served 36, no page cursor) — the acceptance ds-464 could not reproduce-fix. Observe every row red at HEAD before editing.
4. Keep green: impl/test/issue464-*.test.mjs (ds-464's file, once it lands — if it is not on your base yet, name that in your contribution), impl/test/issue438-*.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue457-*.test.mjs, impl/test/swarm-projections.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/limits*.test.mjs, impl/test/phase12-web-northbound.test.mjs, impl/test/issue292-coupling-truth.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the commitsByParticipant map and the participant row's `commits` field ONLY), impl/src/web-northbound.mjs (the SWARM_VIEW_PAGE_HEAVY_PARTICIPANT_FIELDS set ONLY), impl/src/limits.mjs (ONE new VIEW row), docs/43 (the one VIEW-row line beside `view.served_behind.commits`), impl/test/issue464b-*.test.mjs, plus regenerated artifacts. Do NOT edit swarm-state.mjs (ds-464's file), swarm-native-bridge.mjs, application-cli.mjs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 4 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
