Deliver GitHub issue #443 (bug) in this Baton repository. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. #442 (the fold, the dead wake, the route degrade — impl/src/swarm-runtime.mjs `swarm.participant_faulted`, impl/src/application-deployment.mjs route degrade, impl/src/provider-faults.mjs) and #385 (resume-from carries the workspace — `workspace.carried_from`, `_predecessorWorkspace` in swarm-runtime.mjs) are LANDED on your base; build on them, never beside them.

TITLE: Runs ended by depleted usage re-route to another model: reroute_proposed wake for the root or sub-orchestrator, per-swarm manual|auto policy, subscription-aware candidates, workspace carried.

OPERATOR ASK: "detect and notify of runs ended by depleted usage — these should maybe implicitly re-route to another model through the orchestrator or suborchestrator". Six GLM seats died at once when the zai 5-hour window closed; each left a snapshot commit (#428) and a checkpoint (#305); the only way back was the root typing a resume by hand.

REQUIRED:
1. A re-route decision row, not a silent spawn: on the `swarm.participant_faulted` fold (#442) the runtime records `swarm.reroute_proposed {swarmId, participantId, workerId, from: {harness, model, effort}, code, resetAt|null, candidates: [{harness, model, effort, billing, reason}], carry: {snapshotSha|null, checkpoint|null}}`. Candidates derive from the deployment's route rows — served routes whose readiness is ready (#324), not degraded/faulted (#442/#316), ranked by the #429 profile comparison the recruit already performs (#341 route comparison; reuse that function, never a second ranking) — and the row wakes the root/sub-orchestrator under a new closed wake class `reroute_proposed` (extend the class map where #442 added `dead`). `swarm.view` projects the proposal on the participant row (`reroute: {proposedAt, candidates, decision}`) and the attention projection renders it with `next: recruit <successor> --resume-from <seat> --options {exact: <candidate>}`.
2. Swarm policy `rerouteOnProviderFault: 'manual' | 'auto'` set at `swarm.create`/`swarm.update` (`swarm.policy_updated` or the existing swarm-level update kind — read swarm-contract.mjs and swarm-state.mjs for where swarm-level policy lives; one place), rendered on the view; default `manual` stops at the proposal; `auto` performs the resume itself onto the first candidate through the SAME recruit path the root would use (`--resume-from` semantics: the successor inherits the workspace through #385's `workspace.carried_from` and the parked guidance through #337), recording `swarm.rerouted {from, to, successor, carriedFrom, proposalSeq}`; the successor's brief carries a `## Re-routed` section naming the fault, the route it came from, what was carried and the predecessor's last checkpoint (ONE derivation with #385's resume section in _composeRecruitBrief — extend it, never a second renderer).
3. Subscription awareness: `billing: subscription` routes (#429) with a known closed window (`resetAt` in the future, or a faulted route row) are excluded; a subscription route with headroom ranks above an API route unless the policy names `preferApi`; the candidate row's `reason` says why it ranked (`subscription_headroom`, `api_fallback`, `excluded_window_closed`).
4. No candidate: the proposal records `candidates: []` and an attention row `reroute_no_candidate {next: wait until <resetAt> or add a route}`.
5. Red-before rows in a NEW file impl/test/issue443-reroute-on-provider-fault.test.mjs on the light SwarmRuntime harness the issue442 tests use (grep issue442-provider-fault-fold.test.mjs for the bound seat whose worker ends with the provider_fault pair, and issue385-resume-carries-workspace.test.mjs for the carry fixture): (a) a fault on seat X with two ready candidate routes → exactly one reroute_proposed row naming both, the wake class fires, the view projects it; (b) policy auto → a successor is bound on the first candidate with `workspace.carried_from` and `swarm.rerouted` recorded, the brief carries `## Re-routed`; (c) a subscription route whose window is closed is excluded with its reason; (d) no candidates → `candidates: []` + attention row; (e) replay parity of the new rows. Observe every row red at HEAD before editing.
6. Keep green: impl/test/issue442-*.test.mjs, impl/test/issue385-*.test.mjs, impl/test/issue318-*.test.mjs, impl/test/issue337-*.test.mjs, impl/test/issue341*.test.mjs, impl/test/issue429-*.test.mjs, impl/test/issue324-*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-state*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/swarm-wake*.test.mjs, impl/test/swarm-family-doc.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/swarm-projections.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the fault-observation follow-on, the auto resume call, the `## Re-routed` brief section — no other region), impl/src/swarm-state.mjs (the new fold kinds + policy field), impl/src/swarm-contract.mjs, impl/src/swarm-event-schemas.mjs, impl/src/swarm-wake*.mjs (the class map), impl/src/swarm-refusals.mjs (if a new code is needed), impl/test/issue443-*.test.mjs, plus regenerated artifacts. Do NOT edit application-deployment.mjs, coordinator.mjs, provider-faults.mjs, application-cli.mjs, web-northbound.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 6 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
