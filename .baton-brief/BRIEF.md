Deliver the remaining families of GitHub issue #465 (bug, priority:high) in this Baton repository: the projection checkpoint still serializes to ~274 MB for a 56k-row / 112 MB ledger, and a resident holding that parsed window is what the OS kills first on a busy 16 GB host (the 15:24Z incident on #465). Your brief renders #465 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read #465 and its comments in full (the bytesByFamily measurements: `_events` 181 MB dominates, then the second-copy families), then #469 (item 1 landed as 9379d805 — the objective referenced, never copied three times; items 2 and 3 are this lane: `_webCommands` and `_spills` hold whole request/response bodies, and task/plan/goal rows keep second copies of text already in the ledger), impl/src/coordination-store.mjs (the checkpoint writer and reader, `bytesByFamily`, the release-checkpoint bound `release_checkpoint_unbounded`, the projection families), impl/src/projection-checkpoint*.mjs if present, and the rows in impl/test/issue465-*.test.mjs and impl/test/issue469-*.test.mjs.

REQUIRED (each family measured before and after on the fixture the #465 rows use, numbers in the contribution):
1. `_webCommands`: a completed web command's projection row keeps the receipt's IDENTITY and OUTCOME (commandId, seq, httpStatus, the row it minted) and references the request/response bodies by the ledger row (`bodyRef {seq}`), never a second copy of the body; a reader that needs the body reads the ledger row. Bounded by the existing #464 reference pattern (`objectiveRef` + bytes), one derivation.
2. `_spills`: the same — a spill row references its spilled bytes, never re-embeds them in the projection.
3. task/plan/goal second copies: every text field the projection keeps that is byte-identical to a ledger row's field becomes a reference (`{kind, seq}` + bytes) with ONE reader helper used by every projection consumer; the fold still answers every existing read (the parity rows for run show, swarm view, plan render must stay green).
4. `_events`: measure what the 181 MB is (which kinds, which fields) and report it with a proposal; implement the reference conversion only if it is the same mechanism as 1–3 (no new mechanism in this lane).
5. The checkpoint's `bytesByFamily` is re-measured on the #465 fixture and on a real clone-shaped ledger (impl/test/fixtures/ledgers if one is large enough; otherwise the fixture) and the numbers ride the contribution; the release write must fall under `costBound` (16 MiB) or the contribution says by how much it still misses and why.

RED-BEFORE rows in impl/test/issue465-second-copies.test.mjs: (a) a completed web command with a 200 KB body adds under 1 KB to the projection and reads back through the reference; (b) the same for a spill; (c) a task/plan/goal row's text appears once on the ledger and the projection references it; (d) the parity rows (existing readers unchanged). Bound every await (docs/42 §8). Old-shape checkpoints must still load (a fold over an older checkpoint file is a red-before row too).

VERIFY ONLY with `node --test` on the file you add, impl/test/issue465-*.test.mjs, impl/test/issue469-*.test.mjs, impl/test/issue449-*.test.mjs, impl/test/coordination-store*.test.mjs, impl/test/issue434-deferred-open-reconstruction.test.mjs and the run show / swarm view parity files you touch. Never the full suite; never run-suite --changed (#424). Regenerate seam-inventory and surface-gate if a seam changed.

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #465: …'. Plain technical English.
