Deliver GitHub issues #404, #405 and #413 in this Baton repository (three audit drift findings on refusal detail and bounds; consolidated report items C31, C32, C40; umbrella #382). The worker runtime has no gh credential, so the findings are transcribed here.

#404 — writeScratchpad masks unknown CoordinationRefusal codes as worker_not_active (P3). Site: impl/src/coordinator.mjs, the writeScratchpad handler (grep `writeScratchpad` and the catch that maps a CoordinationRefusal to `worker_not_active`, cited at ~12477–12484). An active worker hitting a novel store refusal is told the worker is not active, so the named remedy is wrong. Owed: the store code crosses verbatim beside the closed allowlist the handler translates — a code outside the allowlist is surfaced as itself with the store's message, never re-labelled.

#405 — writeQuarantineEntry's conflict refusal declares a {seq} detail the refusal class silently discards (P3). Sites: impl/src/coordination-store.mjs ~172 (`CoordinationRefusal` constructor takes (message, code) only) and impl/src/coordination-internals.mjs ~44–46 (the quarantine conflict refusal built with a {seq} detail). The quarantined seq is lost. Owed: the detail travels — the refusal class carries `detail` the way the neighbouring quarantine refusals attach it (find the Object.assign path they use and make it the ONE path; the constructor accepts (message, code, detail = null)), and the conflict refusal names the seq on the wire.

#413 — export staging reconciliation re-derives completed-row archives with an unbounded byte ceiling (P9). Site: impl/src/result-export.mjs ~695–699 (`maxArchiveBytes: Number.MAX_SAFE_INTEGER` over a large completed stage allocates the full archive with no deployment bound). Owed: the bound is the deployment's export policy (grep the export policy rows — `export`/`archive` bytes in the deployment's policy or limits.mjs) read at that site; a stage above the bound is reconciled in bounded pieces or refused typed naming the bound, never allocated whole.

REQUIRED: one red-before row per finding at its site in NEW files impl/test/issue404-scratchpad-refusal-verbatim.test.mjs, impl/test/issue405-quarantine-refusal-detail.test.mjs, impl/test/issue413-export-archive-bound.test.mjs (build on the fixtures the nearest existing tests use: grep `writeScratchpad` in impl/test, `quarantine` in impl/test, `result-export` in impl/test). Observe each row red at HEAD before editing. The fix converges on the sibling seam that already honours the principle (the issues name it); no new numeric literal anywhere (frame-economics F1).

Keep green: every impl/test/*scratchpad*.test.mjs, impl/test/issue290-*.test.mjs (quarantine), impl/test/coordination-internals*.test.mjs, impl/test/issue304-*.test.mjs, every impl/test/*export*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/coordinator.mjs (the writeScratchpad handler ONLY — other lanes own its startup, drain and provider-fault regions), impl/src/coordination-store.mjs (the CoordinationRefusal class and the quarantine write ONLY — another lane owns the release/close region), impl/src/coordination-internals.mjs (quarantine refusal only), impl/src/result-export.mjs, impl/test/issue404-*.test.mjs, impl/test/issue405-*.test.mjs, impl/test/issue413-*.test.mjs. Do NOT edit application.mjs, the swarm files, application-host.mjs or the docs. NEVER run git stash. Never print credential values.

VERIFY: node --test on your new files plus the keep-green files — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`; include the regenerated artifact if it changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
