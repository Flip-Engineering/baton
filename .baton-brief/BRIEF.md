Deliver the second half of GitHub issue #446 (bug, flaky) in this Baton repository: the served-transport SSE rows race under a parallel gate run. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. The first half (316-b2, test-only) landed as 070016cd; do not touch impl/test/issue316-provider-degraded.test.mjs.

EVIDENCE (root's gate run at the suite's lane width, master 070016cd, 2026-09-18 10:5xZ): with impl/test/issue316-sse-attachment-closed.test.mjs and impl/test/issue316-provider-degraded.test.mjs in the same parallel gate set (BATON_SUITE_PARALLELISM=2, 7 files), 316-sse-a / 316-sse-b / 316-sse-c ALL failed with the same message: `Web host configuration is invalid`; the file passes 3/3 alone at the same commit and at 5b177cc6. So two served-host fixtures running side by side invalidate each other's web host configuration — a shared, process-external fact (an env-derived socket path, a port, a token file, a published connection.json path, a lease) that one test process changes under the other.

REQUIRED:
1. Find exactly what `Web host configuration is invalid` reads (grep the message in impl/src/web-northbound.mjs / application-host.mjs / application-deployment.mjs; read the validator) and which of its inputs the two fixtures share (HOME, XDG dirs, the repository's .git/baton publication, a fixed socket name, an env var the fixture sets on process.env). Record the exact shared input in your contribution's items.
2. Make the served-host fixture hermetic per process at the seam that matters: derive the shared input per fixture (a tmpdir-confined HOME/publication root, a per-process socket name) the way the route-truth and readiness fixtures already do (`fixtureHome`), or — if the PRODUCTION validator reads a process-global that two residents on one host would also fight over — fix the production side so two residents on one host never read each other's configuration (that is the real bug: the operator runs two residents on one host all day). One derivation, no new numeric ceilings.
3. Prove it: run the two 316 files together under `BATON_SUITE_PARALLELISM=2` (node --test on both files at once) 20 times in a loop; record the pass count in your items; a single failure means the race is not fixed.
4. Red-before: if the cause is production-side, add a row in a NEW file impl/test/issue446-sse-host-config.test.mjs that starts two served hosts in one process/tmp root and asserts both validate; observe it red at HEAD. If test-only, change only the fixture and say so.
5. Keep green: impl/test/issue316-*.test.mjs, impl/test/issue445-*.test.mjs, impl/test/issue356-*.test.mjs, impl/test/issue338-*.test.mjs, impl/test/issue365-*.test.mjs, impl/test/web-northbound*.test.mjs, impl/test/application-host*.test.mjs, impl/test/issue383-*.test.mjs.

OWNED FILES (yours alone): impl/test/issue316-sse-attachment-closed.test.mjs, impl/test/issue446-*.test.mjs, the shared served-host fixture module those tests import (find it), impl/src/web-northbound.mjs / impl/src/application-host.mjs (the host configuration validator ONLY, if the cause is production). Do NOT edit swarm-*.mjs, coordinator.mjs, application-deployment.mjs's reincarnation region (lane ds-461 owns it) or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on the files in item 5 plus the 20-run loop of item 3 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` if you touched impl/src; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
