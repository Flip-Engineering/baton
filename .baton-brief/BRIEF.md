Deliver lane D of GitHub issue #441 (bug, priority:high — the operator's P0) in this Baton repository: claims instead of handovers (docs/47-the-reading-half.md §5 — read it in full, plus docs/45 §2 on claims). Your brief renders #441 as a context package (## Context package, `run.package.read`). The claim primitive is LANDED (#422/#423: `swarm.claim_updated` with `claimId, participantId, workId|paths, status active|released, handoffTo`, the `claims` view slice, the `claim_holder_gone` attention row) and lanes A/B/C of #441 are landed or in flight (C owns the `## Swarm situation` section — do not touch it).

REQUIRED (docs/47 §5, activated now that #423's claims exist):
1. A seat that needs a file outside its path scope files a path claim (`swarm.claim_updated {claimId, participantId: self, paths, status: active}`) on its recorded checkout instead of finishing with a `needsFromOthers` handover: the brief's contribution-contract section (#310/#371 example) and the `## Swarm situation` peers block's sibling — a NEW short `## Claims` block that lane D owns — teach the spelling with the admitted example derived from the validator's schema (impl/src/swarm-event-schemas.mjs — never a hand-typed example), and list the seat's own active claims and every peer's active claims that overlap its scope.
2. `swarm_claim_conflict`: a claim refuses (typed, pre-effect, in impl/src/swarm-refusals.mjs' closed set) when another seat's ACTIVE claim overlaps on the same checkout/paths, naming the holder, the holding `claimId` and the overlapping paths; the holder releases or hands off in one row (`handoffTo`). Read how the landed claim fold validates today (swarm-state.mjs `swarm.claim_updated` arm, swarm-runtime.mjs `_composeClaim*`/the update path) and add the overlap rule there — one rule, the fold and the pre-effect check derive from the same function.
3. The path scope a seat was recruited with (`options.scope`, #441's "path scope becomes a claim") is recorded as the seat's initial claim at bind time (`claimId: scope:<participantId>`, `paths: scope`) so overlaps between seats are visible as claims from the first minute; the brief names it. A recruit whose scope overlaps an ACTIVE claim of another seat is admitted (scopes overlap legally, #423) but the brief's `## Claims` block names the overlap and the holder.
4. Red-before rows in a NEW file impl/test/issue441d-claims-instead-of-handovers.test.mjs on the light harness (grep issue422-*/issue423-* tests for the claim fixtures): (a) a seat's recruit scope becomes its initial claim row; (b) an overlapping active claim by another seat refuses `swarm_claim_conflict` naming holder, claimId, paths; (c) a handoff moves the hold and the former conflict is admitted; (d) the composed brief's `## Claims` block lists own and overlapping peer claims and the admitted example matches the validator's schema example (docs/47 red skeleton row (e) — find it in impl/test/issue441-*.test.mjs and turn it green); (e) replay parity. Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue441*.test.mjs, impl/test/issue422-*.test.mjs, impl/test/issue423-*.test.mjs, impl/test/swarm-coupling.test.mjs (its #448 row is an expected red — leave it), impl/test/swarm-brief*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-state*.test.mjs, impl/test/swarm-view-slices.test.mjs, impl/test/issue430-*.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/swarm-family-doc.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the claim update/conflict path, the bind-time scope claim, and a NEW `## Claims` block in _composeRecruitBrief ONLY — never the `## Swarm situation` section, recruit's route comparison, integrate, the fault fold or the bridge table), impl/src/swarm-state.mjs (the claim arm's overlap rule only), impl/src/swarm-refusals.mjs (the new code), impl/src/swarm-event-schemas.mjs (only if the example needs a field), impl/test/issue441d-*.test.mjs, docs/47-the-reading-half.md (§5 item 4 only: mark it activated), plus regenerated artifacts. Do NOT edit swarm-contract.mjs, application-*.mjs, web-northbound.mjs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
