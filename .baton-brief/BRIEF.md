Deliver IMPLEMENTER LANE 4 of GitHub issue #314 in this Baton repository: the one entry story. Your brief renders #314 as a context package (## Context package, `run.package.read`); this file is the lane contract. The design is docs/49-mcp-primary-surface.md and the red skeleton is impl/test/issue314-mcp-core-surface-red.test.mjs, both landed on master at c22bf697 — NOT in your worktree's base commit. FIRST ACT: in your worktree run `git rebase master` (master carries c22bf697), then read docs/49 in full (its §entry story and §migration table) and the red file, then impl/MCP.md (the 58-tool table and the two entry points), impl/scripts/mcp-web.mjs (the resident bridge; its descriptor fallback at ~25-27 has consumers: `grep -rn mcp-web impl/test impl/scripts` before you change it), impl/scripts/mcp-stdio.mjs (the descriptor-launched headless mode), impl/scripts/render-surface-docs.mjs (the generated inventory), impl/test/mcp-bridge-admission.test.mjs (~390-405: the U-G3 pin reads MCP.md's headed sections — keep those headings).

YOU OWN red rows 314-f (migration completeness: every one of the 58 flat tools maps to a core verb or a surface in docs/49's table, and MCP.md's inventory agrees) and 314-h (the entry story: MCP.md leads with the resident bridge, the descriptor is documented as the headless mode for a host without a resident, both share the same principal and the same tool table, gate-checked against #289's operation table; `mcp-web.mjs` refuses a descriptor argument with a typed refusal naming `mcp-stdio.mjs` as the headless entry). Turn both green on their own assertions; do not touch rows a/b/c/g (lane 1, running beside you) or d1/d2/e1/e2 (lanes 2–3, later); they stay listed in impl/scripts/expected-red-tests.json under reason "#314".

REQUIRED:
1. impl/MCP.md rewritten to the docs/49 entry story: the resident bridge first (what it needs — a served resident, the connection selector, the principal), the descriptor as the headless mode second, one tool table shared by both (lane 1 lands the seven core tools; until then the table is the current flat one rendered by render-surface-docs.mjs — do not hand-write a table the renderer owns), the migration table from docs/49 rendered as its own headed section. Keep the headings the U-G3 pin reads.
2. impl/scripts/mcp-web.mjs refuses a descriptor argument typed (code from the closed refusal family the bridge already uses, message naming the headless entry); every consumer of the old fallback is migrated (the grep above) with its rows green.
3. The parity pin: MCP.md's rendered inventory equals the tool table the server advertises (derived from the same source, gate-checked); a stale MCP.md is a red row, not a doc drift.
4. docs/49 gets a "landed" note per law you closed (one line each).

VERIFY ONLY with `node --test` on impl/test/issue314-mcp-core-surface-red.test.mjs, impl/test/mcp-bridge-admission.test.mjs, impl/test/mcp-web-local-resident-red.test.mjs and every test that names mcp-web or mcp-stdio (grep them), `node impl/scripts/render-surface-docs.mjs --check`, then `node scripts/run-suite.mjs test/issue314-mcp-core-surface-red.test.mjs` (BATON_HOST_CAPACITY_DISABLED=1; expected: your two rows pass, 0 cancelled). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #314 lane 4: …'. Plain technical English.
