Deliver GitHub issue #471 (bug, priority:high) in this Baton repository: fixture residents outlive their test. Your brief renders #471 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read the issue in full, then impl/scripts/baton.mjs (serveDeployment and the SignalLifecycleOwner it builds), impl/src/application-deployment.mjs (`reincarnationProcessAlive`, `#watchPredecessorExit` — the ONE process-liveness primitive the successor already uses for its predecessor), and the nine test files that spawn a real `baton serve` child: impl/test/issue276-resident-drain, issue306w-reincarnate-reachable, issue351-idle-stop, issue351-open-liveness, issue351-reconstruction-liveness, issue351-resident-shutdown, issue351-startup-answer, issue468-stream-errors, phase89-resident-cli-e2e (each has a `spawn(process.execPath, [SCRIPT, 'serve', …])` and a `test.after` that kills the children).

THE FACT: ten `baton serve` processes aged 26–42 minutes were found at 15:00Z today, every one a fixture resident over a temporary repository (bt468-c-* worlds), left by runners that `timeout`, a seat's suite lease or Ctrl-C had ended before their after-hooks ran. Each held a listener, a writer lease and host capacity (load 9.9 on 10 cores).

REQUIRED, two halves, both root-side (no test-only band-aids):
1. RESIDENT SIDE: `baton serve` under a declared parent — the environment variable `BATON_SERVE_PARENT_PID`, set by the fixture helper below and by nothing else — watches that pid with `reincarnationProcessAlive` (the same primitive, never a new one) and, when the parent is gone, stops through its ORDINARY stop path (the same `deployment.close()` a signal takes; `host.stop_requested {trigger: 'parent_exited', parentPid}`), so the ledger says why the resident ended. The watch is bounded and cheap (a poll interval derived from the existing reincarnation wait bound, or the same 100 ms poll `#watchPredecessorExit` uses); a resident started without the variable behaves exactly as today. Document the variable in docs/48 or docs/39 wherever the resident's environment is described, in one paragraph.
2. FIXTURE SIDE: ONE shared helper module (impl/test/fixtures/fixture-resident.mjs or the existing fixtures directory's convention) exporting `spawnFixtureResident(t, {script, args, env, ...})` that spawns the child in its own process group with `BATON_SERVE_PARENT_PID=process.pid`, registers it once, and ends it (SIGTERM then SIGKILL after a short bound, by process GROUP) on `t.after`, on the process `exit` event and on SIGTERM/SIGINT/SIGHUP (kill the children, then re-raise the signal's exit code). Migrate all nine files onto the helper; keep every existing row's assertions intact.

RED-BEFORE rows (new file impl/test/issue471-fixture-residents-die-with-runner.test.mjs): (a) a resident started with BATON_SERVE_PARENT_PID naming a child runner process ends by itself within its stop bound after that runner is SIGKILLed, and its ledger carries `host.stop_requested {trigger: 'parent_exited'}` then `host.stopped`; (b) a resident started WITHOUT the variable is untouched by a stranger's exit (negative row); (c) the helper's own cleanup: a runner that spawns a fixture resident through the helper and is then SIGTERMed leaves no resident alive (spawn a small runner script as a child, kill it, assert the grandchild is gone). Hermetic: temp dirs under os.tmpdir(), no network, no provider.

VERIFY ONLY with `node --test` on the files you touch plus impl/test/issue461-old-incarnation-exits.test.mjs and impl/test/issue306a-reincarnate.test.mjs. Never run the full suite; never run-suite --changed (#424). After your run, `pgrep -fl 'baton.mjs serve'` must show no fixture residents of yours.

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #471: …'. Plain technical English; no marketing words.
