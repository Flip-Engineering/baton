You are a PROBE seat on a LARGE swarm in this Baton repository (dozens of seats, many contributions). Your job is to exercise the seat's DEFAULT swarm view over the native bridge and report exactly what it answered (GitHub issue #457: an over-bound default read answers narrowed, never refused). Do not edit any source or test file.

DO, in order, using the swarm client your brief's Swarm section names (`node "$BATON_SWARM_CLIENT" <verb> …`):
1. `swarm.view` with NO arguments (the seat default). Record: the exit code; whether the answer is a refusal (its `code`) or an answer; the answer's top-level keys; the value of `projection`; the value of the `narrowed` field if present (verbatim: from, to, reason); the number of participant rows and contribution rows returned; the total byte length of the answer.
2. `swarm.view` with `{"projection":"participants"}`. Record: exit code, refusal code if any, `projection`, `narrowed` if present, row count, whether a page cursor / `page` field is present (verbatim), byte length.
3. `swarm.view` with `{"projection":"contributions"}`. Record the same fields as step 2.
4. `swarm.view` with `{"projection":"full"}` (an EXPLICIT over-size request). Record: exit code, refusal code and the refusal's `detail` (verbatim, trimmed to 600 characters — it should name the projection that fits), or the answer's byte length if it was answered.

WRITE the four records (the JSON you observed, trimmed to the fields above) into a NEW file PROBE-457.md at the repository root (the only path you may touch), commit it on your lane branch (git add PROBE-457.md && git commit -m "probe-457: seat default view on a large swarm as observed"), and record ONE contribution with `node "$BATON_SWARM_CLIENT" swarm.update` following the admitted payload example your brief's Swarm section renders — items: one per step above, each `evidence` carrying the observed values. Then end your turn. NEVER run git stash. Never print credential values. Never run the suite.
