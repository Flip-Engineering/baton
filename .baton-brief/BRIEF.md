DESIGN lane (you are the designer, not the implementer): GitHub issue #314 in this Baton repository — MCP as the primary agent surface: a small core tool set by default, one entry story, receipts + wake subscriptions instead of blocking calls, a bridge session that survives a resident reincarnation. Your brief renders #314 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read #314 in full, then docs/36 (the agentic tool grammar: few verb-driven tools, closed verb-discriminated schemas), docs/39 §1–3 (swarm runtime verbs), docs/46 (wakes) and docs/48 §11 (reincarnation and the bridge attestation), MCP.md (the 54-tool table and the two entry points), impl/src/mcp-web.mjs and impl/src/mcp-stdio.mjs (the two entries), impl/src/mcp-bridge*.mjs (admission, attestation to the resident incarnation), impl/src/swarm-contract.mjs (the closed swarm verb table the bridge derives from), and the issues that already shaped this: #289 (one operation table), #302 (mutations return receipts), #294 (wake subscription), #306 (reincarnation), #343/#349 (bridge view paging and narrowing), #430/#436 (typed refusals across the web layer).

DELIVER exactly two things, both root-landable without implementation:
1. docs/49-mcp-primary-surface.md — the design, in the house style of docs/36 and docs/48: (a) the CORE tool set for an orchestrator (name every tool, its verb-discriminated closed schema, the receipt it returns, and which wake class a long operation hands back), derived from the docs/36 grammar so the core is ONE tool per family (run, swarm, waves, surface, knowledge, deployment) with a `verb` discriminator and a closed per-verb argument schema — not 54 flat tools; (b) progressive disclosure: how `baton_surface_*` opens the rest, what tools/list carries by default, and the byte budget the test will measure (derive it from the largest core schema × the core count; no arbitrary ceiling — docs/43); (c) one entry story: MCP.md leads with the resident bridge, the descriptor is the headless mode, both share the principal and the tool table gate-checked against #289's operation table; (d) receipts + wakes: every mutation returns the #302 receipt; every long operation returns receipt + a wake subscription id (#294), never a blocking call; (e) reincarnation survival: how a bridge session re-attests against the successor's digest transparently (read docs/48's handoff rows and the successor's publication) with ONE typed notification to the client, and what an in-flight call sees during the handoff window; (f) a migration table: each of the 54 tools → its core verb or its surface, and which are retired; (g) the refusal set the bridge surfaces (derived from #430's closed set, nothing new); (h) an implementation plan cut into 3–4 lanes for DeepSeek implementers with the files each touches and the red rows each owns.
2. Red skeleton tests: impl/test/issue314-mcp-core-surface-red.test.mjs — one `test()` per design law above, each asserting the shape (tools/list size and bytes, the verb discriminator, the receipt shape, the wake id, the re-attestation notification, the migration table completeness against MCP.md), each RED today with an assertion that names the missing law, none cancelled (bound every await; a red row asserts, never dangles — #460), plus the manifest rows for impl/scripts/expected-red-tests.json under reason "#314" following the manifest rule (a -red file is in `rows` or in `converged`, never both/neither; re-render with `manifestRows` from impl/scripts/suite-verdict.mjs).

DO NOT implement the surface. DO NOT edit impl/src. Verify only with `node --test impl/test/issue314-mcp-core-surface-red.test.mjs` (every row must fail on its assertion, zero cancelled) and `node impl/scripts/run-suite.mjs impl/test/issue314-mcp-core-surface-red.test.mjs` (BATON_HOST_CAPACITY_DISABLED=1). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers naming the implementer lanes you cut), commit on your lane branch, subject 'Issue #314: design — …'. Plain technical English; no marketing words; every claim about current behaviour cites the file and line you read.
