Deliver GitHub issue #472 (bug) in this Baton repository: `host.stopped` names abandoned workers as a first-class list, and a stop that abandons a worker still mints its outcome row. Your brief renders #472 as a context package (## Context package, `run.package.read`); this file is the lane contract. This is the hand-back of lane ds-467 (landed 93f675d5, issue #467): read #467's landing (impl/src/coordinator.mjs STOP_DEADLINE_ATTEMPT_BOUND, `control.stop_abandoned`, `drain.worker_abandoned`, the `{how: 'abandoned'}` outcome row; impl/src/application-host.mjs and impl/src/application-deployment.mjs stop wait rows), impl/src/coordination-store.mjs `armHostStopOutcome` (the armed #351 outcome the writer-lease release mints as `host.stopped`), and impl/src/index.mjs `createDriver` + the coordinator's `closeAuthority` / capacity quiescence, and the existing rows in impl/test/issue467-stop-converges.test.mjs (467b is the fixture: a worker the kill cannot reach).

REQUIRED:
1. `armHostStopOutcome` accepts an optional `abandoned` reader and mints `abandoned: [{workerId, attempt, alive}]` BESIDE `released` on `host.stopped`. An abandoned worker never rides inside `released` as `{how: 'abandoned'}` — a release it was not. Every reader of `host.stopped` (docs/39 and docs/48 narration lines, `_say` tails, the wake table, doctor rows) reads the new list; the old shape is not kept as an alias.
2. A stop that abandons a worker STILL mints `host.stopped`. Today the drain cannot converge on a worker that keeps its holds, the writer lease stays held, the resident exits `closed_degraded` and no outcome row exists — the ledger is silent about how the stop ended. Release the coordinator's authority over ABANDONED workers (their resources are already named durably by `drain.worker_abandoned` and `control.stop_abandoned`) so the writer-lease release runs and mints the outcome, with `abandoned` filled and the resident's exit state saying `closed_degraded` only when something ELSE stayed unreleased. No new numeric bounds (docs/43): the attempt bound is #467's, derived once.

RED-BEFORE rows (extend impl/test/issue467-stop-converges.test.mjs or a new impl/test/issue472-abandoned-on-host-stopped.test.mjs): (a) after the bounded attempts on 467b's unreachable worker, `host.stopped` EXISTS with `abandoned: [{workerId: 'w-1', attempt: 2, ...}]` and `released` carries no abandoned rows; (b) the narration line names the abandoned list (`host.stopped stopped_after_deadline (abandoned 1: w-1 attempt 2)`); (c) an ordinary stop with no abandoned worker carries `abandoned: []` (or omits the key — pick one, say which in the row, and make every reader agree). Hermetic fixtures only.

VERIFY ONLY with `node --test` on impl/test/issue467-stop-converges.test.mjs, impl/test/coordinator.test.mjs, impl/test/issue351-resident-shutdown.test.mjs, impl/test/issue450-stop-releases-gone-reservations.test.mjs, impl/test/issue360-drain-releases-settled.test.mjs and the file you add. Never the full suite; never run-suite --changed (#424). Regenerate impl/scripts/seam-inventory.json and the surface gate (`node impl/scripts/seam-inventory.mjs --write; node impl/scripts/surface-gate.mjs --write`) if a seam changed.

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #472: …'. Plain technical English.
