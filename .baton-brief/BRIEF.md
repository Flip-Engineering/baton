Deliver the third half of GitHub issue #464 in this Baton repository. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. Halves 1 and 2 are landed on your base: `role` is the objective's first line under `view.role.head` with `roleBytes` + `roleRef` (swarm-state.mjs participantRole, 75bc9a76), and `workspace.commits` is a bounded tail under `view.workspace.commits` with `commitsTotal`, dropped by a bridge page (swarm-runtime.mjs inspect + web-northbound.mjs SWARM_VIEW_PAGE_HEAVY_PARTICIPANT_FIELDS, bb5640bd). Read both landings and docs/46-swarm-visibility.md §4 (brief exposure by relationship) before editing.

WHAT THE LIVE PROBE MEASURED (primary at bb5640bd, 39-seat swarm, 13:05Z): the participants page serves 24 of 39 rows in 494 693 bytes; the rows' `brief` fields sum to 421 920 of the 471 164-byte participants array (~17.5 KB per row). `brief` on the participant row is a STRING — the composed brief text (5 169 bytes on omp-429 from the root's own CLI view, more on a seat's read where the exposure class renders the full text). The roster still does not fit one frame.

REQUIRED:
1. The same rule the role and the commit tail follow: a ROSTER row (any `participants` projection, the full view's participants array, a bridge page) carries the brief's REACH, never its text — `brief: {bytes, seq, exposure}` where `bytes` is the composed text's byte length, `seq` the ledger row / read that holds it, and `exposure` the docs/46 §4 relationship class that decided what this caller may see (the existing `briefWithheld` arm's vocabulary — read swarm-runtime.mjs ~3475 and reuse its class names, never a second set). The participantId-SCOPED read (`swarm.view {swarmId, participantId}`) carries the text whole under the same exposure rule it applies today — one derivation of "what may this caller see", two projections of it (reach on the roster, text on the scoped read).
2. `run.member.view` and the recruit brief's `## Swarm situation` peers section keep working from the reach (they must not read the text off roster rows — find every reader of `participant.brief` and route it to the scoped read or the ledger row).
3. Red-before rows in a NEW file impl/test/issue464c-brief-reach.test.mjs: (a) a roster row's `brief` is `{bytes, seq, exposure}` and the scoped read's `brief` is the text with the same `bytes`; (b) the exposure class on the roster row equals the class the scoped read applies for the same caller (root vs peer vs the seat itself — three callers); (c) a 39-seat fixture whose briefs are 17 KB each (the live shape) answers its participants projection in ONE frame through the bridge (served 39, page.next null) — the acceptance halves 1 and 2 could not reach; (d) `run.member.view` still renders the brief text for the seat's own read. Observe every row red at HEAD before editing. Keep #268's four expected-red rows as they are.
4. Keep green: impl/test/issue464-*.test.mjs, impl/test/issue464b-*.test.mjs, impl/test/issue268-*.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue349-*.test.mjs, impl/test/issue457-*.test.mjs, impl/test/swarm-projections.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/swarm-replay-corpus.test.mjs (recompute the sidecar digests the way ds-464 did if the fold's projection changes).

OWNED FILES (yours alone): the participant row's `brief` projection (swarm-runtime.mjs inspect / the briefWithheld arm ONLY, or swarm-state.mjs's row if the text is folded there — name which), impl/src/swarm-brief*.mjs readers of `participant.brief` ONLY, impl/src/web-northbound.mjs (the heavy-field set ONLY, if `brief` text must ride the scoped read as a heavy field), impl/test/issue464c-*.test.mjs, impl/test/fixtures/ledgers/*.digest (recomputed only), plus regenerated artifacts. Do NOT edit limits.mjs (no new numeric row is needed — the reach is a fixed small shape), swarm-native-bridge.mjs, application-cli.mjs or the docs beyond docs/43 §4's one sentence (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 4 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
