Deliver GitHub issue #429 in this Baton repository (an operator feature, with two operator constraints that override the issue's first draft). The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: The route table carries each model's MEASURED profile (Artificial Analysis intelligence / coding index, output speed, time-to-first-token, price) so recruit's route comparison (#341) and the doctor rank routes on measured facts, not on the operator's memory.

THE SOURCE: Artificial Analysis publishes a per-model catalog over its API: `GET https://artificialanalysis.ai/api/v2/data/llms/models` with header `x-api-key: <key>`; the answer is `{data: [{slug, name, model_creator: {slug,name}, release_date, evaluations: {artificial_analysis_intelligence_index, artificial_analysis_coding_index, …}, median_output_tokens_per_second, median_time_to_first_token_seconds, pricing: {price_1m_blended_3_to_1, price_1m_input_tokens, price_1m_output_tokens}, …}], …}`. (Do NOT print the key. The root has the key at `~/.config/baton/aa_key` (0600); tests must never need it.)

OPERATOR CONSTRAINTS (2026-09-17, binding):
(1) LIVE, not static: the integration queries the API at use time (the doctor's route-table read, or a `baton doctor --refresh-profiles` verb — choose the ONE seam the doctor already uses for route readiness, #341/#342) with a bounded cache in the deployment's state dir keyed by the response's own timestamps; NEVER a committed table of numbers, never a network call on the resident's request loop (P9 — the refresh is an explicit read the doctor performs, with the fetch injected so tests use a fake).
(2) Pricing is IGNORED for subscription-routed harnesses: muse, kimi-code/k3 and zai/glm here are flat subscriptions, not per-token API billing. Every fleet route declares a billing basis `billing: 'subscription' | 'api'` ONCE beside the route (the same declaration that names the route's AA model slug mapping); price fields enter the profile ONLY when billing is 'api' and read `null` (with reason `subscription`) otherwise. Intelligence/coding index, tokens-per-second and time-to-first-token apply to both.

REQUIRED:
1. A new module impl/src/model-profile.mjs: `fetchModelProfiles({fetchImpl, key, now})` (one request, typed refusals: key absent → `model_profile_credential_absent`; HTTP/shape failure → `model_profile_unavailable {status|cause}`), `profileFor(catalog, slug, {billing})` → `{slug, intelligence, coding, tps, ttftS, price: {blended31, input, output} | null, priceReason: null | 'subscription', measuredAt}` (closed shape, declared once), and the cache: `readCachedCatalog(stateDir)` / `writeCachedCatalog(stateDir, catalog)` with freshness derived from the catalog's own `updated_at` / the response's date — a registry row in limits.mjs names the staleness bound with its derivation (NEVER an arbitrary literal; if the API answers a cache-control/age, derive from it).
2. The credential: `BATON_AA_KEY` env or the file `~/.config/baton/aa_key` (declare the path ONCE beside the other credential files the readiness reads, e.g. where `aa_key.json`/kimi credential files are named — grep credential readiness in impl/src/adapter.mjs / route readiness); git-ignored, snapshot-excluded, projected into no worker. Absence is a degraded profile row on the doctor (`profile: {state: 'unavailable', reason: 'credential_absent', next: '…'}`), never a refusal of the doctor.
3. The fleet route declaration gains `aaSlug` and `billing` (closed set) beside harness/model/effort — ONE declaration in the deployment's route normalization (grep normalizeRoutes in impl/src/application-deployment.mjs); the doctor's route table and `swarm.recruit`'s `routes.considered[]` (#341) carry `profile` per route from the cache (never fetching inline); a route without an aaSlug carries `profile: null`.
4. Red-before rows in a NEW file impl/test/issue429-model-profile.test.mjs with a fake fetch: (a) a fetch with a key answers a catalog and the cache is written; without a key the typed credential refusal; (b) profileFor on a subscription route nulls price with reason 'subscription' and keeps intelligence/coding/tps/ttft; on an api route the price is present; (c) the doctor route table carries the profile for a route with an aaSlug and null for one without, and reads 'credential_absent' degraded when no key exists (drive openBatonDeployment with a fake fetch injected through the deployment's advanced options — find how other injected authorities are passed); (d) recruit's routes.considered carries the profile; (e) the cache freshness derives from the registry row (no literal). Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue341-*.test.mjs, impl/test/issue342-*.test.mjs, impl/test/issue324-*.test.mjs, every impl/test/*doctor*.test.mjs and *route*.test.mjs, impl/test/adapter-card-contract.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/model-profile.mjs (new), impl/src/limits.mjs (one row), impl/src/application-deployment.mjs (route normalization + doctor route table ONLY — two other lanes own its close path and its startup-refusal row; keep to the routes/doctor region), impl/src/swarm-runtime.mjs (routes.considered profile field ONLY), impl/src/adapter.mjs (credential path declaration only), .gitignore (the key file), impl/test/issue429-*.test.mjs. Do NOT edit application.mjs, coordinator.mjs, coordination-store.mjs, application-host.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs .gitignore && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
