Deliver GitHub issue #442 (bug, priority:high) in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: A provider-quota kill of a seat leaves no swarm row and no wake: six GLM seats died silently (participants still 'active'), two more recruited onto the exhausted route.

EVIDENCE (2026-09-18 07:20–07:30Z, both residents): the zai GLM 5-hour window closed. Every omp zai/glm-5.3-flash seat ended the same way: `lifecycle.turn_completed status=failed failure.code=provider_quota_exhausted` ("429 Usage limit reached for 5 hour. Your limit will reset at 2026-09-18 18:07:32 (type=1308)", detail.resetAt), then `kill.requested {rule: provider_fault, actor: policy}` (impl/src/coordinator.mjs ~15649 `provider_fault_death`), `control.stop_requested mode=kill`, process closed 143, `worktree.progress_checkpointed`, `worktree.snapshotted`, `worktree.removed` (#428 custody kept), `task.transitioned working→failed`. The worker half is right. The swarm half never learned:
1. No `swarm.*` row follows any kill. `swarm.view --projection participants` still says `status: active` with `runtime: {state: dead, live: false}` twenty minutes later. `swarm.participant_runtime_lost` (impl/src/swarm-runtime.mjs ~427–470) covers only a restart-time fleet mismatch.
2. No wake: the root's bounded watch `--wake-class contribution_recorded,dead` sat across two of the kills and returned `reason: timeout, matchedSeq: null`.
3. No route truth: the fault is a deployment-level fact about the route (#316 made a provider STALL one deployment fact: `provider_degraded` on the route row, impl/src/application-deployment.mjs ~2861), yet `swarm.recruit` admitted two more seats onto the exhausted route 4–10 minutes later; each died in 20 s. #324 already refuses a blocked route pre-effect from the readiness row.
4. `resetAt` recorded as `2026-09-18T18:07:32.000Z` from a provider string with no zone (impl/src/omp-rpc.mjs ~825, impl/src/provider-faults.mjs). zai reports Beijing time; the honest value is a zone-qualified parse or `resetAtText` with `resetAt: null`.

REQUIRED:
1. ONE fold: the runtime records `swarm.participant_faulted {swarmId, participantId, workerId, code, route: {harness, model, effort}, resetAt|null, resetAtText, snapshotSha|null}` when a bound seat's worker ends with a provider-fault kill (observe the `lifecycle.turn_completed status=failed` + `kill.requested rule=provider_fault` pair the coordinator already emits — hook the coordinator's provider_fault_death seam or the runtime's worker observation, not a ledger scan). Status settles per the #350 rule (left/stopped with reason `provider_fault`), the participant row reads `status: stopped, runtime.state: dead, fault: {code, resetAt}`, an attention row `provider_fault {code, resetAt, next: recruit --resume-from}` pages the root, and the `dead` wake class fires on the row (the watch's class derivation in impl/src/swarm-wake*.mjs / the wake-stream mapping — extend the closed class map, never a side list). Register the kind where `swarm.participant_runtime_lost` is registered (swarm-state.mjs fold + event schema) with replay parity.
2. Route truth: the deployment's route row for that (harness, model) becomes `state: degraded, reason: provider_quota_exhausted, resetAt` on the fault (the same ONE deployment-level fact #316 landed for stalls; clear it at resetAt or on the next successful turn on that route); `swarm.recruit` onto it refuses typed pre-effect `route_provider_faulted {route, code, resetAt}` through #324's readiness consult, and the brief's route table (#341) marks it.
3. resetAt honesty: parse a zone-qualified instant only; otherwise keep the text and `resetAt: null`. One place (provider-faults.mjs).
4. Red-before rows in a NEW file impl/test/issue442-provider-fault-fold.test.mjs on the light SwarmRuntime harness (grep swarm-runtime.test.mjs, issue364-runtime-lost-on-restart.test.mjs, issue350-*.test.mjs for a bound seat whose worker ends): (a) a seat whose worker ends with the provider_fault pair → exactly one swarm.participant_faulted row, status stopped, fault on the participant row, attention row; (b) the bounded watch with wake-class dead returns on that row; (c) the route row degrades with the reset and a recruit onto it refuses typed naming resetAt; (d) a zone-less reset string keeps text and null; (e) replay parity of the new row. Observe every row red at HEAD before editing.
5. Keep green: impl/test/swarm-runtime.test.mjs, impl/test/issue364-*.test.mjs, impl/test/issue350-*.test.mjs, impl/test/issue316-*.test.mjs, impl/test/issue324-*.test.mjs, impl/test/issue339-*.test.mjs, impl/test/swarm-wake*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/provider-faults*.test.mjs, impl/test/issue430-*.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the worker-fault observation and the participant row's fault field ONLY — other lanes own recruit, the bridge dispatch table ~236–350, inspect's workspace composer and _composeRecruitBrief), impl/src/swarm-state.mjs (the new fold kind), impl/src/swarm-event-schemas.mjs, impl/src/swarm-wake*.mjs (the class map), impl/src/application-deployment.mjs (the route degrade on fault ONLY), impl/src/provider-faults.mjs, impl/src/coordinator.mjs (only a hook at the provider_fault_death seam if the runtime cannot observe the pair otherwise), impl/test/issue442-*.test.mjs. Do NOT edit application-cli.mjs, web-northbound.mjs, swarm-native-bridge.mjs, swarm-contract.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
