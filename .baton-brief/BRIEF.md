Deliver GitHub issue #438 (priority:high) in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: swarm.view spawns synchronous git per seat since #428/#425 (live branch, HEAD and status per workspace, plus the writer-state settle before every inspect): the participants view outlives a 180 s request bound on a 45-seat swarm and the resident loop is held for minutes.

EVIDENCE (2026-09-18 06:40–06:47Z, clone resident at master 009e686e, 26 K-row ledger, swarm-backlog-20260916 with ~45 seats): `baton swarm view swarm-backlog-20260916 --projection participants` → after 120 s an empty answer; after 185.9 s `cli_command_pending: Baton Web command swarm.view outlived this caller's request bound while the deployment still answers`. The same view answered in ~2 s before the wave 10 landings. The resident sat at 16 % CPU the whole wait; host load rose from 2 to 5.6. The bounded watch (which answers the outline projection) is wedged the same way, so the root's wake primitive is dead on that swarm.

CAUSE in impl/src/swarm-runtime.mjs:
- c93ffa72 (#428): the participants composer inside inspect() builds every seat's `workspace` row with LIVE reads — `gitRead(['branch','--show-current'], checkout.worktree)`, `gitRead(['rev-parse','HEAD'], …)`, `gitRead(['status','--porcelain'], …)` — three synchronous git spawns per seat with a checkout; `git status` on a full checkout of this repository costs seconds.
- f2fb2d30 (#425): inspect() first calls `this._settleCheckoutWriterState()`, which drains the projected wrapper's commit observations and re-reads every checkout's writer state, on every view.
On a swarm whose seats still have worktrees on disk that is >100 synchronous spawns per read, on the resident's request loop (P9), for every caller — every seat's brief refresh, the root's watch, the doctor.

REQUIRED:
1. swarm.view NEVER spawns a process on the read path. The workspace row derives from the DURABLE rows (#428's worktree.snapshotted / worktree.removed / worktree.custody_retained rows and #425's worktree.commit_recorded rows) plus a change-driven cache the runtime maintains: the live branch/HEAD/dirty triple is refreshed (a) when the seat's projected wrapper reports a commit (#425's observation spool), (b) at the seat's turn boundaries (the #305 checkpoint seam), and (c) on an explicit `--participant-id` read of ONE seat (the only place a live read is allowed, bounded to that seat). Declare the cache ONCE (a `workspaceObservations` map keyed by workspaceId with {branch, headSha, dirty, observedAt, source}) and have the row say where its truth came from (`source: 'rows' | 'wrapper' | 'turn' | 'live'`).
2. `_settleCheckoutWriterState()` runs when the wrapper's spool has new observations (event-driven on append, or at most once per spool change), never unconditionally on every inspect; its per-checkout reads move behind the same cache.
3. Red-before rows in a NEW file impl/test/issue438-view-no-spawn.test.mjs on the light SwarmRuntime harness (grep swarm-runtime.test.mjs / issue428 / issue425 tests for how seats with checkouts are seeded): (a) a swarm of N seats with checkouts is viewed with a spy on the runtime's git spawn authority (find the ONE function the runtime spawns git through — the #301 read-only git authority — and count calls) asserting ZERO spawns on `--projection participants` and on the outline; (b) `--participant-id X` performs at most one seat's worth of live reads; (c) after the wrapper reports a commit for seat X, the next view shows the new headSha with source 'wrapper' without a spawn; (d) a timing row: the participants view over N=40 seats with checkouts answers within the transport's request bound (read the bound from the web wait ceiling row, #394, never a literal); (e) issue428's workspace rows and issue425's bypass rows stay green (their truths now come from rows/cache). Observe every row red at HEAD before editing.
4. Keep green: impl/test/swarm-runtime.test.mjs, impl/test/issue428-worktree-custody-on-stop.test.mjs, impl/test/issue425-writer-coupling-honest.test.mjs, impl/test/issue364-runtime-lost-on-restart.test.mjs, impl/test/issue427-context-projection.test.mjs, impl/test/issue350-*.test.mjs, impl/test/swarm-view*.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue349-*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (inspect()'s participants/workspace composer, the observation cache and _settleCheckoutWriterState ONLY — two other lanes own its recruit route refusal and its routes.considered field; keep away from recruit), impl/src/runtime-isolation.mjs (only if the wrapper's spool needs a change hook), impl/test/issue438-*.test.mjs. Do NOT edit swarm-state.mjs, swarm-contract.mjs, worktree.mjs, coordinator.mjs, application-cli.mjs, web-northbound.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 4 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`; include the regenerated artifact if it changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
