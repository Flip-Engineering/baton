Deliver IMPLEMENTER LANE 1 of GitHub issue #314 in this Baton repository: the MCP core tool table and its verb grammar. Your brief renders #314 as a context package (## Context package, `run.package.read`); this file is the lane contract. The design is docs/49-mcp-primary-surface.md and the red skeleton is impl/test/issue314-mcp-core-surface-red.test.mjs, both landed on master at c22bf697 — NOT in your worktree's base commit. FIRST ACT: in your worktree run `git rebase master` (master carries c22bf697; a clean rebase is expected), then read docs/49 in full and the red file, then docs/36 (the agentic tool grammar), impl/src/mcp-northbound.mjs, impl/src/surface-capability-catalog.mjs, impl/src/surface-capability-resolution.mjs, impl/src/production-mcp-convergence.mjs (the merge seam at ~668+ and the instruction suffix at ~517), impl/src/swarm-contract.mjs (the closed swarm verb table the bridge derives from), and impl/MCP.md.

YOU OWN red rows 314-a (the seven-tool core set on tools/list by default), 314-b (the byte budget — derived and measured, never a constant), 314-c (the verb discriminator: closed per-verb oneOf schemas derived from the docs/36 grammar and the family verb tables, one tool per family: baton_deployment, baton_run, baton_swarm, baton_waves, baton_knowledge, baton_wakes, baton_surface) and 314-g (the refusal set the bridge surfaces is #430's closed set, nothing new). Turn each of them green on its own assertion; do not touch rows d1/d2/e1/e2/f/h (lanes 2–4 own them; they stay red and listed in impl/scripts/expected-red-tests.json under reason "#314").

REQUIRED:
1. impl/src/mcp-core-tools.mjs (new): the seven core tools, each schema a closed `oneOf` over its verbs with per-verb argument schemas DERIVED from the existing operation table / contract tables (#289 one operation table; no hand-copied duplicate of a schema that exists elsewhere) — a pin proves the derivation (a verb added to the family table appears in the tool, a verb removed disappears).
2. mcp-northbound.mjs advertises the core set by default and dispatches a core tool call to the same operation the flat tool reached (byte-identical results on a parity fixture over the real McpFleetServer for every verb that has a flat counterpart today); the 51 non-core flat tools stay reachable through baton_surface exactly as docs/49 §progressive disclosure says. production-mcp-convergence.mjs merges ONE baton_surface tool instead of six.
3. The byte budget: tools/list of the core measured on the wire in the red file's fixture, bounded by the derivation docs/49 states (largest core schema × core count); the pin fails when growth exceeds it. No arbitrary ceiling (docs/43).
4. impl/MCP.md's generated inventory (render-surface-docs.mjs) renders the seven core tools; regenerate seam-inventory and surface-gate; docs/49 gets a "landed" note per law you closed (one line each).

VERIFY ONLY with `node --test` on impl/test/issue314-mcp-core-surface-red.test.mjs, impl/test/mcp-bridge-admission.test.mjs, impl/test/mcp-web-local-resident-red.test.mjs, impl/test/issue343-*.test.mjs, impl/test/issue344-*.test.mjs, impl/test/issue349-*.test.mjs and every mcp-* test file, then `node scripts/run-suite.mjs test/issue314-mcp-core-surface-red.test.mjs` (BATON_HOST_CAPACITY_DISABLED=1; expected: your four rows pass, six remain expected red, 0 cancelled). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers naming exactly what lanes 2–4 must know about your table), commit on your lane branch, subject 'Issue #314 lane 1: …'. Plain technical English.
