Deliver GitHub issue #316 (priority:high) in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: Provider stall killed nine lanes in forty minutes with no wake reaching the root.

EVIDENCE (2026-09-14): every running participant on two residents ended a turn with `omp_error: OpenAI completions stream timed out while waiting for the first event` (a DeepSeek first-token stall), was killed by policy within 50 ms, and became a dead runtime — nine lanes, one provider fault class, forty minutes. The root learned of the deaths only by asking `swarm view` an hour later; the per-swarm watch attachments emitted nothing. Since then: #295 typed provider faults and retry-before-kill landed; #341 route readiness reads provider refusals; #356/#339 the bounded watch and wake classes work; #433 asks for a multi-wake frame (another lane).

REQUIRED (the three items beyond #294/#295/#306):
(a) A provider stall affecting N participants in one window is ONE deployment-level attention row `provider_degraded {route: {harness, model}, participants: [ids], window: {from, to}, faultClass}` with `next` = pause recruits on that route until a probe succeeds — derived from the provider-fault rows the coordinator already folds (#295: grep the typed provider fault taxonomy / `provider_fault` kinds and where readiness reads them), never N anonymous deaths. The window bound derives from the retry-before-kill policy row that already exists (never a new literal). `swarm.recruit` on a degraded route refuses typed (`route_degraded {route, since, next}`) until a readiness probe on that route succeeds (#341's probe seam); the doctor's route table shows the degraded state.
(b) A watcher attachment that ends — resident restart, transport close, its own crash — delivers a typed final frame `attachment_closed {reason: restart|transport_closed|error, at, resumeFrom: <seq>}` to the consumer (the wake stream's follow and the bounded watch), never silence: grep impl/src/wake-stream.mjs and the CLI's followWakes / watch legs for how a stream ends today (#356: 'a stream that ends names its reason') and make the reason set closed and the same on the CLI and the bridge.
(c) A resident whose served commit is behind master says so on every wake frame header: the frame carries `served: {commit, behind: <n> | null}` derived from the repository the resident serves (git rev-list count against the branch it was started from — read it ONCE at publish and refresh on the doctor's cadence, never per frame), so an operator sees the drift where the deaths appear.

Red-before rows in a NEW file impl/test/issue316-provider-degraded.test.mjs: (a) three seats on one route dying with the same first-token fault inside the window fold to ONE provider_degraded attention row with the three ids and next; a recruit on that route refuses route_degraded until a probe succeeds; (b) a bounded watch whose resident closes the transport receives attachment_closed with reason and resumeFrom (drive the real web transport the way issue356/issue339 rows do); (c) the wake frame header carries served.commit and behind (seed a repo where master is ahead of the served commit). Observe every row red at HEAD before editing.

Keep green: impl/test/issue295-*.test.mjs, impl/test/issue341-*.test.mjs, impl/test/issue324-*.test.mjs, impl/test/issue339-*.test.mjs, impl/test/issue356-*.test.mjs, impl/test/swarm-wake*.test.mjs, impl/test/wake-stream*.test.mjs, impl/test/issue427-context-projection.test.mjs, impl/test/issue357-*.test.mjs (attention rows), impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/wake-stream.mjs, impl/src/coordinator.mjs (the provider-fault fold / attention derivation region ONLY — other lanes own its startup, drain and scratchpad regions), impl/src/application-deployment.mjs (doctor route degraded state and the served-commit fact ONLY), impl/src/swarm-runtime.mjs (recruit route_degraded refusal ONLY), impl/src/swarm-refusals.mjs (the new code), impl/src/application-cli.mjs (the ended-frame rendering in followWakes / watch legs ONLY), impl/test/issue316-*.test.mjs. Do NOT edit application-host.mjs, worktree.mjs, coordination-store.mjs, swarm-state.mjs, swarm-contract.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the keep-green files — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
