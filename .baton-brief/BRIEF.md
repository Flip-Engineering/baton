Deliver the remainder of GitHub issue #268 in this Baton repository: activity and usage on the participant row. Your brief renders #268 as a context package (## Context package, `run.package.read`); this file is the lane contract. The design is landed (docs/46-swarm-visibility.md §1 — the ONE liveness/activity derivation and §1.2 `activity` and `usage` on the participant row) and the red skeleton exists: impl/test/issue268-visibility-red.test.mjs (rows listed in impl/scripts/expected-red-tests.json under #268 — read them: the activity/usage rows, the closed SWARM_PARTICIPANT_RUNTIME_STATES set, the root participant row with reviewerId 'root', the attention coverage envelope, the no-spawn spy). Read docs/46 §1 and the skeleton before editing; #364 (runtime.state on the row), #438 (no git on the read path) and #464 (the row's budget: role head, commit tail, brief reach) are landed on your base and bound what a row may carry.

REQUIRED:
1. `activity` on the participant row — docs/46 §1.2's shape exactly (the current turn's phase, the last tool row's name + at, the last contribution/guidance seq, `idleSince`), derived from the rows the ledger already holds (turn_started/turn_completed, tool rows the worker adapter records, contribution/guidance rows) — the ONE derivation `run.member.view` and the brief's `## Swarm situation` peers-now line also read; never a second scan of the ledger on the read path (#438: the fold keeps it current).
2. `usage` on the participant row — the seat's turns, tokens/cost if the harness reports them (the #429 profile knows the billing class; a subscription route reports `null` cost with `billing: 'subscription'`), wall time, derived from the same rows; bounded shape (no arrays).
3. The rows in the skeleton that these two deliver turn GREEN and leave the expected-red manifest (re-render it canonically through `manifestRows` in impl/scripts/suite-verdict.mjs; a row that pins something still unlanded — the root participant row, the coverage envelope — stays red with its stage named); the `docs/44 rule 5` reason on each remaining row is accurate.
4. Red-before is the skeleton itself: run it at HEAD and record which rows are red for which reason before editing; after, record which turned green.
5. Keep green: impl/test/issue464*.test.mjs, impl/test/issue364-*.test.mjs, impl/test/issue438-*.test.mjs, impl/test/issue433-*.test.mjs, impl/test/swarm-projections.test.mjs, impl/test/swarm-projection-pins.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/swarm-replay-corpus.test.mjs (recompute the sidecar digests the way ds-464 did if the fold's projection changes), impl/test/issue290-suite-manifest-guard.test.mjs, impl/test/suite-manifest-reasons.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-state.mjs (the participant row's activity/usage fold ONLY), impl/src/swarm-runtime.mjs (the row projection's activity/usage ONLY), impl/src/swarm-brief.mjs (the peers-now line reading activity ONLY), impl/scripts/expected-red-tests.json (the #268 rows ONLY, re-rendered), impl/test/issue268-visibility-red.test.mjs (stage text of rows that stay red ONLY — never delete a row), impl/test/fixtures/ledgers/*.digest (recomputed only), plus regenerated artifacts. Do NOT edit web-northbound.mjs, swarm-native-bridge.mjs, application-cli.mjs or the docs beyond docs/43 §4's one sentence (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on the skeleton plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
