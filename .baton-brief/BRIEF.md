Deliver GitHub issue #385 in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: swarm.recruit --resume-from a dead seat binds a fresh worktree: the predecessor's uncommitted edits and new files are not carried, so a resumed seat starts from the base commit.

EVIDENCE (2026-09-18 02:44Z, clone at 3e06ad64, after the #383 crash): `omp-356` died with 1 modified file (impl/src/wake-stream.mjs) and 1 new test file uncommitted in ws-f35ccfdcccbca7404047df38aa6b46bd. `swarm recruit omp-356-b … --resume-from omp-356` joined, recorded resumeFrom, and bound a NEW worktree ws-3cfdf35910487fdb788179e5b37671dd at the base commit with 0 changed paths. The root copied the diff and the new file across by hand. docs/39 (#263 item 3) says a resume-from successor inherits the predecessor's session ownership and its parked guidance; the workspace is the one thing a dead seat leaves behind that matters most, and it is the one thing not carried. Since then #428 landed worktree custody (ensureLaneBranchAtHead, worktree.snapshotted / removed / custody_retained rows; a dead seat's checkout is retained when its lane branch does not contain HEAD) and #364 landed swarm.participant_runtime_lost + worker_lost_on_restart, so a dead predecessor's checkout and its custody truth are now durable facts the recruit path can read.

REQUIRED:
1. A resume-from successor BINDS THE PREDECESSOR'S WORKTREE when no live worker owns it (the #357 ownership record and #364's lost row prove it): the successor's seat takes the same physical workspace (same `.baton/wt/<id>`, same lane branch), the runtime records `workspace.carried_from {workspaceId, predecessor, paths: <changed paths>, snapshotSha|null}` on the join (a runtime-recorded fold kind, registered like swarm.participant_runtime_lost), and the predecessor's parked guidance rides along as today.
2. When a new worktree IS required (the predecessor's checkout is gone, or another live worker holds it), the runtime applies the predecessor's uncommitted change set from its retained checkout or its snapshot commit (#428's worktree.snapshotted sha) into the new worktree and records the same workspace.carried_from row; a dirty predecessor checkout that cannot be carried refuses typed `resume_workspace_uncarriable {paths, reason}` naming the paths — never a silent base-commit start.
3. The successor's brief names what was carried: paths and the predecessor's last progress checkpoint (#305), so it starts from `git status`, not from zero (the brief renderer reads the carried_from row — ONE derivation; the brief section is the existing resume section).
4. Red-before rows in a NEW file impl/test/issue385-resume-carries-workspace.test.mjs on the recruit/resume fixtures (grep resumeFrom in impl/test — issue337, issue350, issue364 rows build resumed seats over real worktrees): (a) a predecessor with one modified tracked file and one new untracked file dies (its worker closed, its runtime lost); recruit --resume-from binds the SAME workspace id and the carried_from row names both paths; the successor's brief names them and the predecessor's last checkpoint; (b) the predecessor's worktree removed but its snapshot commit present → a new worktree carries the change set from the snapshot; (c) a predecessor checkout held by a live worker → typed refusal naming the holder; (d) replay parity of the new row. Observe every row red at HEAD before editing.
5. Keep green: impl/test/issue337-*.test.mjs, impl/test/issue350-*.test.mjs, impl/test/issue364-runtime-lost-on-restart.test.mjs, impl/test/issue428-worktree-custody-on-stop.test.mjs, impl/test/issue435-* if present, impl/test/worktree*.test.mjs, impl/test/phase70*.test.mjs, impl/test/phase71*.test.mjs, impl/test/phase72*.test.mjs, impl/test/phase75*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/swarm-brief*.test.mjs, impl/test/issue292-coupling-truth.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-runtime.mjs (the recruit/resume path and the brief's resume section ONLY — another lane owns inspect()'s workspace composer and the writer-state settle in the same file; keep away from inspect), impl/src/worktree.mjs (a carry helper beside ensureLaneBranchAtHead), impl/src/coordinator.mjs (only the workspace binding on resume, if the recruit path binds through it — keep away from the drain and startup regions other lanes own), impl/src/swarm-state.mjs (the new fold kind ONLY — another lane owns the coupling/claim folds), impl/test/issue385-*.test.mjs. Do NOT edit swarm-contract.mjs, swarm-event-schemas.mjs, application-cli.mjs, web-northbound.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
