Deliver GitHub issue #360 in this Baton repository. The worker runtime has no gh credential, so the issue is transcribed here.

TITLE: `baton serve` exits SIGTERM with application_host_shutdown_failed after every seat was stopped: the fleet drain waits its whole deadline on resources a dead worker still holds, then fails the shutdown.

TWO INSTANCES:
(1) 2026-09-18 01:57Z, clone at 9ef491d5: one seat stopped through swarm.stop a minute earlier (participant_left recorded); SIGTERM → exit non-zero 'driver capacity reservations remained after fleet drain'.
(2) 2026-09-18 05:35Z, audit resident at 6438ea8d: one seat (omp-390) stopped via swarm.stop seconds before SIGTERM → 'drain did not converge; waiting on w-29 (local_resources:localAuthority+local_resources:worktree+local_resources:cleanupPending), reason deadline, deadline 90000ms' → host.stop_waiting on w-29 → after ~5 min exit non-zero application_host_shutdown_failed with the wire receipt {"reason":"deadline","stage":"convergence","timeoutMs":90000,"waitingOn":[{"workerId":"w-29","status":"dead","disposition":null,"processState":"closed","waiting":["local_resources:localAuthority","local_resources:worktree","local_resources:cleanupPending"]}]}. A worker that is dead with its process closed still held localAuthority / worktree / cleanupPending, and the drain waited its full deadline on resources nobody would release.

REQUIRED:
1. The fleet drain (impl/src/coordinator.mjs — the drain/convergence path that produces `waitingOn` rows and the `driver capacity reservations remained` check; grep 'remained after fleet drain', 'waitingOn', 'cleanupPending') RELEASES what a settled holder cannot: a worker whose process is closed and whose seat has left/stopped has its local resources reaped by the drain itself (the reap must go through the #428/#435 custody boundary — capture-or-retain, never a bare delete — and the reservation release through the capacity authority), each release recorded on the host.stop_waiting / host.stopped row as `released: [{workerId, resource, how}]`. The drain then converges; the shutdown exits zero.
2. The deadline row names the reaper it waits on: `waitingOn[].waiting` entries carry `{resource, reaper, since}` so an operator reads which release is pending, and a resource with NO reaper (nothing will ever release it) is released immediately with reason `orphaned`, never waited on.
3. The drain's deadline stays the deployment's own policy row (never a new literal); a drain with nothing to do converges without waiting (the idle case is another lane's — do not touch the host's stop path in application-host.mjs).
4. Red-before rows in a NEW file impl/test/issue360-drain-releases-settled.test.mjs on the drain fixtures (grep impl/test/phase56*.test.mjs 'drain', impl/test/issue276-*.test.mjs, impl/test/issue353-*.test.mjs): (a) a seat stopped through swarm.stop whose worker process is closed, then drain → converges within the policy bound, exit zero, host.stopped carries the released rows; (b) the same with a capacity reservation the holder left behind → released and named; (c) waitingOn rows carry reaper/since while a live worker is still draining; (d) replay parity of the new fields. Observe every row red at HEAD before editing.
5. Keep green: impl/test/phase56*.test.mjs, impl/test/phase70*.test.mjs, impl/test/phase71*.test.mjs, impl/test/phase72*.test.mjs, impl/test/phase75*.test.mjs, impl/test/issue276-*.test.mjs, impl/test/issue353-*.test.mjs, impl/test/issue428-*.test.mjs, impl/test/issue435-* if present, impl/test/worktree*.test.mjs, impl/test/issue332-*.test.mjs, impl/test/issue350-*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/coordinator.mjs (the fleet drain / convergence / reservation region ONLY — two other lanes own its startup-reconciliation region and its writeScratchpad region; keep your hunks in the drain), impl/src/host-capacity.mjs (release-by-holder only if needed), impl/test/issue360-*.test.mjs. Do NOT edit application-host.mjs, application-deployment.mjs, worktree.mjs, swarm-*.mjs, coordination-store.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 5 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`; include the regenerated artifact if it changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
