Deliver the hand-backs of GitHub issue #443 (landed 11c6dfba) in this Baton repository, plus the #455 web-port hand-back. Your brief renders #443 as a context package (## Context package, `run.package.read`); this file is the lane contract.

WHAT LANE ds-443 HANDED BACK (its contribution 6a18d762, verbatim intent):
1. `swarm.create` cannot yet OPEN a swarm with its re-route policy: adding a `policy` argument to SWARM_COMMAND_ARGUMENTS (impl/src/swarm-contract.mjs) needs the canonical `swarm.create` operation's field union in impl/src/application-semantics.mjs, because impl/src/application.mjs ~255–275 (`validateApplicationCommandArgs`) throws at load on any declared argument outside the canonical operation. Until then the policy is declared with one `swarm.update {event: 'swarm.policy_updated'}` (the same kind, the same fold). Deliver: `baton swarm create <purpose> --policy '{"rerouteOnProviderFault":"auto"}'` → the create records the policy row (the same `swarm.policy_updated` fold, ONE derivation), the canonical operation row gains the `policy` field, the CLI spelling and its help row exist (impl/src/application-cli.mjs parseSwarmCli, the way #306w added a field), and `swarm.view` shows the policy from the first row. Read how #306w (ef85d724) added a canonical row + field for `deployment.reincarnate` — same shape.
2. `reroute_proposal_mismatch` is admission-guarded (re-derived from the projection the ledger reconstructs); pin it in `SWARM_FOLD_ADMISSION_PINS` (impl/scripts/surface-gate.mjs) so the replay check is explicit, and regenerate with `node impl/scripts/surface-gate.mjs --write`.
3. docs/43 (the policy section) names `swarm.policy_updated` with `rerouteOnProviderFault: manual | auto` and `reroutePreferApi`; docs/39 already names the rows.
4. (#455's hand-back) impl/src/web-northbound.mjs, the package.admit port's return (~2311–2318): forward the store receipt's reuse half — `reused: admitted.reused === true`, `admittedEvent: admitted.package?.admittedEvent ?? null` — beside packageDigest/name/branches; impl/src/application-cli.mjs `admitRecruitContextPackage` (~2383–2420) passes `reused` through so the recruit's printed receipt says `contextPackage: {digest, reused}`.

RED-BEFORE rows in a NEW file impl/test/issue443-handbacks.test.mjs (reuse the issue443 and issue455 fixtures — a real WebNorthbound + SwarmRuntime + the CLI parser): (a) `swarm create … --policy` records the policy and the view shows it; (b) the canonical operation for swarm.create names `policy` and `validateApplicationCommandArgs` admits it; (c) the admission pin exists for `reroute_proposal_mismatch` (read how surface-gate pins are tested); (d) a second `--issue` recruit's CLI receipt carries `contextPackage.reused: true`. Observe every row red at HEAD before editing.

Keep green: impl/test/issue443-*.test.mjs, impl/test/issue455-*.test.mjs, impl/test/issue441*.test.mjs, impl/test/swarm-contract*.test.mjs, impl/test/swarm-surface.test.mjs, impl/test/surface-truth*.test.mjs, impl/test/canonical-naming-233-red.test.mjs, impl/test/application-semantics*.test.mjs, every impl/test/application-cli*.test.mjs, impl/test/swarm-runtime.test.mjs, impl/test/frame-economics-red.test.mjs, impl/test/issue292-coupling-truth.test.mjs.

OWNED FILES (yours alone): impl/src/swarm-contract.mjs (the create args row ONLY), impl/src/application-semantics.mjs (the swarm.create canonical row's field ONLY), impl/src/swarm-runtime.mjs (create's policy write ONLY, reusing the #443 policy fold), impl/src/application-cli.mjs (the --policy flag/help row and the reused pass-through ONLY), impl/src/web-northbound.mjs (the package.admit return ONLY), impl/scripts/surface-gate.mjs + its ledger (regenerated), docs/43 (the policy section), impl/CLI.md (regenerated), impl/test/issue443-handbacks.test.mjs, plus the one pinned set that moves. Do NOT edit swarm-state.mjs, coordinator.mjs, application-deployment.mjs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the keep-green files — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write`, `node impl/scripts/surface-gate.mjs --write`, `node impl/scripts/render-surface-docs.mjs --write`; include regenerated artifacts.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl docs && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
