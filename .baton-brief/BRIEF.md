Deliver GitHub issue #461 (bug, priority:high) in this Baton repository. Your brief renders the issue as a context package (## Context package, `run.package.read`); this file is the lane contract. #306 part 1 (lanes A 809341b3, B 6bc66bcb, W ef85d724, docs/48) is landed on your base; read docs/48-reincarnation-in-place.md §2 (the nine-step protocol) and impl/src/application-deployment.mjs (the reincarnation region: #awaitInFlightTurns, #awaitSuccessorReady, the publication withdrawal, the successor spawn) before editing.

TITLE: Reincarnation live: the handoff works (successor published, old withdrew, doctor serves the new commit) but the old incarnation lingers after host.publication_withdrawn instead of exiting — host.reincarnated and the incarnation_changed wake never fire until an operator signals it.

EVIDENCE (primary, 173 k-row ledger, 2026-09-18 11:31–11:38Z): `baton deployment reincarnate 08780160` → receipt in 3 s (state reincarnating, successor pid 39634) → rows host.reincarnation_requested (173005) → host.successor_started (173006) → host.stop_requested/host.stopped on the old (173008/173013) → host.successor_published (173017, 11:32:40, after the successor's ~50 s open) → the old's narration `host.publication_withdrawn instance-45cc… → instance-4413…; tail host_closed 49804ms`. connection.json switched to the successor; doctor served the target; swarm.list answered. Then the OLD process (pid 88715) stayed alive four minutes at 0 % CPU with nothing but an idle loop (a kqueue and pipes open) and wrote no further row; only after the root's SIGTERM ('signal received; draining participants (count unavailable: coordinator_closed …)', a second drain of an already-closed coordinator taking ~55 s) did it exit, and only then did the successor record host.reincarnated (173019). Also: the successor is spawned with an argv shape `pgrep -f "baton.mjs serve"` does not match, so an operator cannot find it by the served spelling.

REQUIRED:
1. After `host.publication_withdrawn` the old incarnation ends every listener and handle it still holds (the owner socket server, the publication lease, the wake attachments, the host lease, any timer — find what keeps the loop alive: `process.getActiveResourcesInfo()` at that point names them) and exits 0 without any external signal, within the #351 stop stages (record the exit as the last stage on the tail).
2. A SIGTERM to an incarnation that has already withdrawn exits at once — no second drain of a closed coordinator; the signal path reads the incarnation's state first.
3. The wait for the successor's publication is a durable `host.stop_waiting {resource: 'successor_publication', reaper, since}` row (the #351 shape), not only a narration tail number.
4. `host.successor_started` carries the successor's `argv` (as spawned) so a reader can find the process; the successor's own narration goes to a named log the row points at (or the same stdout), never nowhere.
5. Red-before rows in a NEW file impl/test/issue461-old-incarnation-exits.test.mjs on the issue306a fixture (the injected successor stub): (a) after the stub publishes, the old incarnation's `close()` resolves and its process handles are released (assert the active-resources set is empty of the host's listeners) within `host.reincarnation.wait_ms` with no signal; (b) `host.reincarnated` is recorded by the successor as soon as the old exits; (c) SIGTERM after withdrawal returns without a drain; (d) the stop_waiting row for the successor publication; (e) argv on host.successor_started. Observe every row red at HEAD before editing.
6. Keep green: impl/test/issue306*.test.mjs, impl/test/issue351-*.test.mjs, impl/test/issue434-*.test.mjs, impl/test/issue383-*.test.mjs, impl/test/issue450-*.test.mjs, impl/test/application-host*.test.mjs, impl/test/application-deployment*.test.mjs, impl/test/frame-economics-red.test.mjs.

OWNED FILES (yours alone): impl/src/application-deployment.mjs (the reincarnation/handoff region and the stop tail ONLY), impl/src/application-host.mjs (the signal path's already-withdrawn branch ONLY), impl/test/issue461-*.test.mjs, plus regenerated artifacts. Do NOT edit coordinator.mjs, coordination-store.mjs, swarm-*.mjs, application-cli.mjs or the docs (name needed hunks in needsFromOthers). NEVER run git stash. Never print credential values.

VERIFY: node --test on your new file plus the files in item 6 — NEVER run-suite --changed and NEVER the full suite. Then `node impl/scripts/seam-inventory.mjs --write` and `node impl/scripts/surface-gate.mjs --write`; include regenerated artifacts if they changed.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A impl && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
