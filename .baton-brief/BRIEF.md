Deliver GitHub issue #487 (bug, priority:high) in this Baton repository: the resident session ledger (`.git/baton/application-v3/resident/sessions/sessions.jsonl`) corrupts on every clean reincarnation handoff because the old and the new incarnation both append to it with a seq taken from their own memory; the next successor then fails replay with `sequence_gap` and the deployment can no longer reincarnate. Read the issue (`gh issue view 487` works from your worktree; it quotes both live ledgers), then impl/src/web-auth.mjs (`WebSessionStore`: `_load`, `_append`, `_apply`, the fsync discipline `_syncPath`), impl/src/application-deployment.mjs ONLY TO READ where the store is constructed (~3617) and where the old incarnation revokes its session on close with `deployment_closed` (~5452) — do not edit that file, another lane holds it — docs/48-reincarnation-in-place.md §11 (the handoff order: the successor publishes and issues its own resident session BEFORE the old incarnation closes), and impl/test/web-auth*.test.mjs for the store's existing rows.

THE FACT: `_append` numbers a row `this._events.length + 1` and `_load` refuses any line whose seq is not its line index. Two processes hold the file across a handoff: the successor loads N rows and issues (row N+1); the old incarnation, which still counts N, then revokes with seq N+1 too. The running processes never notice; the next process to open the file refuses line N+2.

REQUIRED:
1. `_append` derives the seq from the ledger on disk: before numbering it reads the rows another process appended since its last load (bytes beyond what it has consumed), applies them through `_apply` (so a revoke of a session the other process issued still references an active session), then appends `seq = rows on disk + 1`. One derivation for load and append (the same line reader). Keep the integrity rule (`seq == line index`) and the fsync discipline. No lock files, no retries with numeric ceilings.
2. `WebSessionIntegrityError` for `sequence_gap` names the line, the seq found, and both rows' `actor` and `ts` (the row at that line and the row that already holds that seq), so a successor's log and `host.reincarnation_failed.cause.stderrTail` say who wrote what — the message is the only change needed for that; application-deployment.mjs stays untouched.
3. RED-BEFORE rows in impl/test/issue487-session-ledger-dual-writer.test.mjs: (a) two `WebSessionStore` instances over one file: A issues, B issues, A revokes A's session, B revokes B's session; a third store loads cleanly, its rows are numbered 1..4 and both sessions read revoked; (b) B's in-memory view after its own append includes A's rows (B can validate a bearer A issued); (c) a ledger that already carries the duplicate (write two rows with seq 2 by hand) refuses on load with the typed `sequence_gap` whose message names line 3, seq 2, and both actors. Bound every await (docs/42 §8). The real two-process handoff row (#461b through the #471 helper) is welcome as (d) only if it runs under 60 s; otherwise say so in needsFromOthers.

VERIFY ONLY with `node --test` on the file you add and impl/test/web-auth*.test.mjs, impl/test/issue306a-reincarnate.test.mjs, impl/test/issue461-old-incarnation-exits.test.mjs. Never the full suite; never run-suite --changed (#424). docs/48 §11 gets one sentence (the session ledger is shared by both incarnations during the window; appends number from disk).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #487: …'. Plain technical English.
