Deliver GitHub issue #460 in this Baton repository: expected-red rows that are CANCELLED by a dangling promise instead of failing an assertion. Your brief renders #460 as a context package (## Context package, `run.package.read`); this file is the lane contract. Read the issue and its comments (the row lists), then impl/test/readiness-honesty-red.test.mjs (the six pin rows A4p, A5p, A6p, P-stale, A-L, A-Lcap), impl/test/readiness-credentials-red.test.mjs (26 rows RT-10a … listed in impl/scripts/expected-red-tests.json under reason "#460"), the suite verdict's cancellation classification in impl/scripts/suite-verdict.mjs / run-suite.mjs ("cancelled by a dangling await earlier in their file"), and docs/42 (the suite laws: an expected-red row asserts and fails; it never leaves the loop empty).

THE FACT (measured today at fed18071): `node --test --test-name-pattern=A4p test/readiness-honesty-red.test.mjs` cancels A4p ALONE in 1 s with "Promise resolution is still pending but the event loop has already resolved" — no earlier row is involved. The fixture opens (`openFixture` resolves), then `await spawnWorker(fixture.deployment, ROUTE_SIBLING, 'a4p-sibling')` never resolves: the fixture's `deployment.run(...)`/`run.approve()`/`settle()` chain waits on a signal the LivenessAdapter fixture (mode 'complete') never emits under the current code, and the loop empties. The same shape cancels the 26 readiness-credentials-red rows. A cancelled row is not a red row: it asserts nothing, so the design it pins is neither proven missing nor present, and the verdict has to carry a special class for it.

REQUIRED:
1. Every await in those two files' fixtures is BOUNDED and ASSERTED: a `spawnWorker`/`gateOutcome`/probe wait that does not settle inside a declared bound (derived from the fixture's own probeTimeoutMs/failureWindowMs — no new magic number) fails the row with an assertion that names the wait (`spawnWorker(a4p-sibling): run.approve never settled within <bound>`), never leaves a pending promise. Prefer fixing the FIXTURE ADAPTER so its spawn/turn path always emits what the deployment waits on (the honest fix: find what `deployment.run` awaits on this adapter and why the fixture never provides it), and add the bound as the safety net.
2. After the change every row in both files ends `ok` or `not ok` — zero `cancelled` — and each `not ok` fails on the assertion that names the missing design (the stage rows) or passes (the pin rows, which pin LANDED behaviour and must be green: A4p's F-1 fan-out, P-stale's staleness law, A-L/A-Lcap's fixture laws are all landed code).
3. Update impl/scripts/expected-red-tests.json by the manifest rule: a `-red` file is either in `rows` or in `converged`, never both/neither; retire the "#460" reason on rows that now pass, keep (with their own design reason) the rows that fail on their design assertion; re-render with `manifestRows` from impl/scripts/suite-verdict.mjs; run `node scripts/run-suite.mjs test/readiness-honesty-red.test.mjs test/readiness-credentials-red.test.mjs` and paste the verdict line in the contribution (expected: 0 cancelled, 0 unexpected).

VERIFY ONLY with `node --test` on the two files and `node scripts/run-suite.mjs` on the two files (with BATON_HOST_CAPACITY_DISABLED=1). Never the full suite; never run-suite --changed (#424).

CONTRIBUTION: the standard contract (items with id/status/change/files/evidence, verification {targeted, gates, fullSuite, environmentRed}, needsFromOthers), commit on your lane branch, subject 'Issue #460: …'. Plain technical English.
