You are kimi-read, a DESIGN seat (read + contribute; you do not edit impl/src). Deliver the design for GitHub issue #441 ("the reading half") in this Baton repository as ONE document plus red-before test skeletons. The operator ranked this the highest-impact gap in Baton. The worker runtime has no gh credential, so the issue is transcribed here.

#441 — Seats cannot read the world they work in. Every lane brief today is 2–6 KB of root-typed text because a seat cannot read (a) the GitHub issue its brief names (#347: no gh credential in the worker runtime); (b) the design docs and sibling contributions that decide its work (#311 knowledge does not travel; #433 contributions are not a projection; docs/45 and docs/46 had to be pointed at by path); (c) what its peers are doing now (docs/46 peers-now, #423). A third of today's lanes handed something back in needsFromOthers because their path scope stopped at a file they needed; two lanes rediscovered facts the root already held. Owed: (1) a seat reads its issue — pulled by the ROOT's credential at recruit time into a durable object, never a worker-side gh call; (2) a seat reads landed work — the brief's Situation derives from durable rows (accepted contributions since the seat's base with their files); (3) a seat reads its peers (docs/46 §6 peers-now); (4) brief authoring DERIVES: `swarm.recruit --issue N --files <globs>` composes the brief from the issue body, the docs the issue cites, the gate set the landing table derives for those files (#296), the contract example (#371); the root adds only the judgment paragraph; (5) path scope is a negotiation: a seat that needs a file outside its scope files a claim (docs/45 §2) instead of finishing with a handover.

THE POINT OF THIS DESIGN: Baton ALREADY designed and largely landed the context-passing substrate; the reading half must ACTIVATE it for a recruited seat, never invent a parallel channel. Read these first, in full, and map every owed item onto them:
- docs/32-reflexive-orchestration.md §1.3 (shared state: Scratch, Cairn, ContextManifest/cells/artifact CAS), §1.4 (the Bench), §3.3 REFLEX-3 (ContextPackage: typed immutable hand-off objects, `run.attach_package(run, packageDigest, {scope: run | worker:<role> | board:<b>}`, "worker briefs render branch digests + schemas") — LANDED: grep `attach_package`, `packageDigest`, `context_package` in impl/src/application.mjs, coordination-store.mjs, mcp-northbound.mjs; §3.4 REFLEX-4 (REPL objects as hand-offs), §3.5 (the MCP surface: baton_package_{admit,attach,read}, baton_board_*).
- docs/33-shared-objects-repl-layer.md (cells, named bindings REPL-2, cell-as-source REPL-3 — what a seat can cite by digest).
- docs/34-knowledge-horizons.md KG-2 (promotion) and KG-3 (activation: `recallPreview` brief-time injection at the _providerBrief/spawn seam — LANDED: coordination-store.mjs recallPreview, messages.mjs renders it; decision-time surfacing; board sidebars).
- docs/39-swarm-runtime.md §Communication and shared context (swarm.context_updated = the whiteboard; #427 gave it a projection) and §The knowledge verbs reach the loop (#318: run.knowledge.seed, run.scratchpad.append/read/elevate through the bridge — grep swarm-runtime.mjs lines ~243–345).
- docs/45-open-coordination.md §2 (claims) and docs/46-swarm-visibility.md §6 (peers-now) and §7 (the no-spawn cost rule) — align, do not duplicate.
- impl/src/swarm-runtime.mjs `_composeRecruitBrief` (the ONLY seat-facing composition today: a `## Swarm situation` section plus the Swarm/bridge guidance) and impl/src/swarm-native-access.mjs / swarm-native-bridge.mjs (what a seat can call), impl/src/application-cli.mjs `parseSwarmCli` recruit (the flags a root types).

DELIVER:
1. docs/47-the-reading-half.md, in docs/39's house style (rules, not prose): (a) the objects a recruited seat can read and how each arrives — the issue and cited docs as ONE ContextPackage admitted by the root's CLI at recruit time (branches `issue:<n>` and `doc:<path>@<sha>`, provenance from the admission event, attached with scope worker:<seat>), landed work as the #433 contributions projection filtered since the seat's base, peers as docs/46 peers-now, project knowledge as the KG-3 recallPreview over the issue text; (b) the recruit brief's derived sections, in order, each naming the ONE row/projection it renders from and its byte bound from the registry (never a literal); (c) the bridge verbs a seat uses to read more than the brief carries (package read by digest, contributions projection, context projection, knowledge recall) and what each refuses; (d) `swarm.recruit --issue N --files <globs> [--doc PATH…]`: the composition rule, what the root still types, and the refusal when the root's credential cannot read the issue; (e) claims instead of handovers: a seat naming a file outside its scope files a docs/45 §2 claim the root accepts or refuses in one row; (f) what stays OUT (no worker-side gh, no free scripting, no per-view scans or spawns — docs/46 §7); (g) migration: today's hand-typed briefs keep working; (h) closed-set owners and the seam map by file and function; (i) open questions.
2. Red-before skeletons (docs/44 -red convention, header stage `design-not-landed`, expected red, each row's message names what the implementer must land) that run today on the light SwarmRuntime harness (grep swarm-runtime.test.mjs) and the CLI parse: impl/test/issue441-reading-half-red.test.mjs — rows: (a) recruit --issue N with an injected issue reader admits ONE package with an issue branch and attaches it to the seat; (b) the brief renders the package's branches with digests and the issue title; (c) a seat reads the package back through the bridge by digest; (d) the brief's Situation lists accepted contributions since the seat's base from the projection (no ledger scan — spy row); (e) a seat's claim on an out-of-scope file is a claim row, not a handover; (f) the root's reader refusing (no credential) refuses the recruit naming the issue number.
3. Your contribution: items = the doc and the test file (delivered), plus one item per OPEN QUESTION you could not settle (status not_delivered, the question in `change`); carriedForward = the implementation lanes you recommend in order, each with its files and which landed primitive it extends; notes = your honest assessment of how much of REFLEX-3/KG-3 is truly usable from a seat today versus advertised.

RULES: edit only docs/47-the-reading-half.md and the one new test file. NEVER run git stash. NEVER run the test suite beyond `node --test` on your file. Never print credential values.

FINISH (there is no "later"; never wait on a background job): COMMIT on your lane branch (git add -A docs impl/test && git commit -m "<descriptive message>") and record ONE contribution with node "$BATON_SWARM_CLIENT" swarm.update — your brief's Swarm section renders the exact admitted payload example; follow it. Do not end your turn without the swarm.contribution_recorded receipt.
