#!/usr/bin/env node
import { execFileSync } from 'node:child_process';import { existsSync,mkdirSync,writeFileSync } from 'node:fs';import { homedir,tmpdir } from 'node:os';import { dirname,join,resolve } from 'node:path';import { fileURLToPath } from 'node:url';import { CodexAppServerCli,createBrief,createDriver } from '../../../../impl/src/index.mjs';
const HERE=dirname(fileURLToPath(import.meta.url)),REPO=resolve(HERE,'../../../..'),AUTH=join(homedir(),'.codex','auth.json'),LOG_DIR=join(tmpdir(),`baton-web-session-review-${Date.now()}`),TASK_ID='codex-web-session-lifecycle-review',MODEL='gpt-5.6-sol',EFFORT='low',REPORT='docs/reference/evidence/phase12-web-session-lifecycle-codex-review-2026-07-11/review.md';
const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)),git=(args)=>execFileSync('git',args,{cwd:REPO,encoding:'utf8'}).trim(),alive=(pid)=>{try{process.kill(pid,0);return true;}catch{return false;}};async function until(fn,label,timeout=600000){const end=Date.now()+timeout;while(Date.now()<end){const v=await fn();if(v)return v;await sleep(100);}throw new Error(`timeout waiting for ${label}`);}
if(!existsSync(AUTH))throw new Error('PENDING-LIVE-no-codex-auth-file');const adapter=new CodexAppServerCli({requestTimeoutMs:30000,ceiling:1});const {coordinator,log}=createDriver({repoRoot:REPO,logDir:LOG_DIR,adapters:{codex:adapter},runtimeIsolation:{credentialFiles:{codex:[AUTH]}},workerDependencyDirs:['impl/node_modules'],verifyDependencyDirs:['impl/node_modules'],approvalTimeoutMs:60000,stopDeadlineMs:15000,budgetPolicy:{terminalGraceMs:2000},watchdog:{stallMs:240000}});
const brief=createBrief({goal:'Independently and adversarially review the IL1-IL8 authenticated web session lifecycle. Trace injected-provider login, cookie/Bearer issuance, refresh atomicity, predecessor refusal, restart integrity, logout, origin/TLS/content-type/body/CSRF controls, CORS, audit ordering and crash windows, stream liveness, credential leakage, confused-deputy/claim expansion, replay, and zero fleet side effects. Write precise severity/source/failure-sequence/missing-regression findings, or explicitly report none.',constraints:[`Write only ${REPORT}; do not edit source/tests/specs.`,'Review actual current spec, web-auth/web-northbound/web-stream source, and Phase 12 tests.','Do not inspect prior evidence logs.','Separate implementation defects from deferred WN5/WN8/WN9 rate/proxy/WebSocket/browser scope.','Do not add homelab integration, commit, push, or use network tools.'],pathScope:[REPORT],definitionOfDone:'Report covers IL1-IL8 trust seams and lists every actionable defect precisely or explicitly reports none',verification:{command:`test -s ${REPORT} && node --test impl/test/phase12-web-session-lifecycle.test.mjs impl/test/phase12-web-auth.test.mjs impl/test/phase12-web-northbound.test.mjs impl/test/phase12-web-stream.test.mjs`,expectExit:0,timeoutMs:180000},budget:{tokens:450000,usd:4,wallMin:9}});
let workerId=null,pid=null,result=null,integration=null,fatal=null,pumping=true;const approvals=[];const pump=(async()=>{const seen=new Set();while(pumping){for(const w of coordinator.list()){const id=w.pendingApprovalId??w.pendingQuestionId;if(!id||seen.has(id))continue;seen.add(id);approvals.push(await coordinator.respond(id,w.pendingApprovalId?{decision:'allow'}:{text:'Complete the report-only review.'},'human'));}await sleep(100);}})();
try{const h=await coordinator.spawn('codex',brief,{taskId:TASK_ID,taskType:'review',model:MODEL,effort:EFFORT,modelPolicy:{allow:[MODEL],allowFamilies:['openai']}});workerId=h.id;const spawned=await until(()=>log.read(workerId).find(e=>e.kind==='lifecycle.spawned'&&e.actor==='worker'),'native spawn');pid=spawned.payload?.pid??null;await until(async()=>(await coordinator.result(workerId)).ready,'verified review');result=await coordinator.result(workerId);if(result.status!=='completed')throw new Error(`review failed: ${JSON.stringify(result)}`);integration=await coordinator.integrate(workerId,{strategy:'ff-only',actor:'orchestrator'});}catch(error){fatal=String(error?.stack??error);}finally{pumping=false;await pump.catch(()=>{});if(workerId)await Promise.resolve(coordinator.kill(workerId,'policy')).catch(()=>{});if(workerId)try{await until(()=>(!pid||!alive(pid))&&!existsSync(join(REPO,'.baton','wt',TASK_ID))&&!existsSync(join(REPO,'.baton','runtime',workerId))&&git(['branch','--list',`baton/${TASK_ID}`])==='', 'full reap',30000);}catch(error){fatal=`${fatal??''}\ncleanup:${error?.stack??error}`.trim();}}
const events=workerId?log.read(workerId):[],handle=workerId?coordinator.list().find(w=>w.id===workerId):null,card=`${adapter.card().harness}@${adapter.card().version}`;const checks={noHarnessError:fatal===null,exactTupleHonest:handle?.harnessResolved===card&&handle?.modelRequested===MODEL&&handle?.modelResolved===MODEL&&handle?.modelObserved===MODEL&&handle?.effortRequested===EFFORT&&handle?.effortResolved===EFFORT&&(handle?.effortObserved==null||handle?.effortObserved===EFFORT),freshVerified:result?.status==='completed'&&events.some(e=>e.kind==='verify.reverified'&&e.payload?.accept===true),integrated:integration?.ok===true,integrationIntent:events.some(e=>e.kind==='integration.completed'),killConfirmed:events.some(e=>e.kind==='kill.confirmed'),processGone:!!pid&&!alive(pid),worktreeGone:!existsSync(join(REPO,'.baton','wt',TASK_ID)),runtimeGone:workerId?!existsSync(join(REPO,'.baton','runtime',workerId)):false,branchGone:git(['branch','--list',`baton/${TASK_ID}`])===''};const summary={at:new Date().toISOString(),repoHead:git(['rev-parse','HEAD']),workerId,pid,model:MODEL,effort:EFFORT,result,integration,approvals,checks,fatal,pass:Object.values(checks).every(Boolean)};mkdirSync(HERE,{recursive:true});writeFileSync(join(HERE,'events.jsonl'),events.map(e=>JSON.stringify(e)).join('\n')+(events.length?'\n':''));writeFileSync(join(HERE,'summary.json'),`${JSON.stringify(summary,null,2)}\n`);console.log(JSON.stringify({pass:summary.pass,checks,pid,fatal},null,2));if(!summary.pass)process.exitCode=1;
