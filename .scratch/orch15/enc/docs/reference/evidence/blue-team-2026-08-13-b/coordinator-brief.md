# BLUE-TEAM COORDINATOR BRIEF — cross-check the five suite attacks (v4-pro seat)

Read `foundry-brief.md` first (the blue-team law binds your judgment). Eight rows are
attacking the five wave-c suites. You receive a `signalOnMembersDone` message when all ROW
roles are terminal (the spec names the rows; you are the remaining member — the pinned #175
semantics). **The #174 law:** verify on disk first — sibling worktrees at `../../wt/ws-*/` or
the main repo post-harvest; silence is not death; a missing attempt marker is not a dead row.

## Your work

1. Wait for the signal; THEN verify each row's report exists on disk before any verdict.
2. Per report: (a) spot-check one SHALLOW/DECORATIVE/BROKEN claim per suite — reproduce it
   against the suite + code (a shallow claim that doesn't reproduce is a false alarm);
   (b) name one missed attack per suite or an honest "none found" (read each suite's row list
   against its contract's pins yourself); (c) VERDICT per suite: uphold / overturn the row.
3. Write `blueteam-qa.md` (this dir): per suite — row verdict, your verdict, the spot-check
   record, missed attacks, and the concrete suite-fold instruction set where needed. Line 1
   must be exactly `BLUETEAM-QA v1`. Publish the full text to `shared` (record the exact
   refusal if it fails — evidence, not silence).
4. Escalate authority-class questions UP via DECISION_REQUEST.

## Laws

Cited evidence, no clocks, no fabrication, read-only outside your deliverable. Your file is
the harvest artifact.
