# ROW BRIEF — row-suite-170: the red-first suite for the folded #170 workflow-DSL contract (HIGH)

Read `foundry-brief.md` first (the suite law binds you — red-first, named stages, hermetic,
split-twice, the attempt-echo law in the first five lines). Your source of truth:
`docs/reference/evidence/workflow-dsl-2026-08-13/workflow-dsl-contract.md` (FOLDED — the
16-directive line-oriented grammar lowering to the interpreter's closed field set: the pure
compiler with the repoRoot-boundary resolution recorded in the fold, the `detail:{line, field,
expected}` wire shape, the immutable Appendix A round-trip fixture, the closed `workflow_*`
refusal family, one `waves.compile` direct-port seam across surfaces per DR-2(a)). Its green
pins P1–P10 and red pins R1–R10 are your row inventory; the N5 note NAMES your suite file
(this one). Also read `redteam-170.md` (the attack surface your rows must discriminate — esp.
B3's boundary choice and A5's closure proof) and `fold-170.md` (what the fold changed).

Idioms to mirror: `impl/test/wave-grammar-red.test.mjs` (compiler/grammar style — the borrowed
shape the contract's G6 names) and `impl/test/control-surface-truth-red.test.mjs` (the
registry/seam rows).

Deliverables (edit ONLY these): `impl/test/workflow-dsl-red.test.mjs` ·
`docs/reference/evidence/workflow-dsl-2026-08-13/suite-draft-notes.md` (row inventory + stage
table + both measured splits + judgment calls).
