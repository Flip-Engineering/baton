# #72 FOLD BRIEF — fold the red-team report into the prescriptive-doctor contract (v1.0 → v1.1)

You are folding an adversarial red-team report into the prescriptive-doctor contract. Read fully,
in order: (1) `contract-redteam.md` (NOT FOLD-READY — 9 numbered blockers, each with its
concrete fix); (2) `prescriptive-doctor-contract.md` (v1.0 — your edit target).

## The blockers, headlined (fold ALL 9 + apply the open-question verdicts)

Per the report's §D list — the precision-law holes (warning noise that teaches operators to
ignore the catalog), the detection-cost holes (expensive census scans per call), the
false-positive/false-negative threshold cases, the stale-remediation classes, and every
citation fix. The catalog's shape is right; the fold is about the detection honesty per warning.

## Laws + deliverables

No clocks; every citation verified (`grep -an`/`sed -n` on the two NUL files); sorted-key
literals ACTUAL order; `localeCompare` banned. Header to **v1.1** with the fold note. Edit ONLY:
`prescriptive-doctor-contract.md` (v1.1) + `contract-fold.md` (blocker → change map, all 9 +
open questions) — this directory.
