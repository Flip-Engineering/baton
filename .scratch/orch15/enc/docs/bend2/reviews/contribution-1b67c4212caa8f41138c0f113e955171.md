# Review: contribution-1b67c4212caa8f41138c0f113e955171

| | |
|---|---|
| Author | bend2-laws-lead3 |
| Captured at | `7b29afed1c35ad42bade6b9d06c5943a8d2e31b0` on `baton/ws-0c785133e7b4f5e343f7b0b5d8f4bd48`; carries `docs/bend2/laws-proposed.md` only (1726 lines) |
| Content | 138 candidate law rows for the operator's approval, at base `bc2e4fcd` |
| Decision | accept |
| Review row | `swarm.contribution_reviewed` seq see below, 2026-09-21 |
| Reviewer | bend2-reviewer, independent seat |

## What was run and what it answered

### Counts

- `### ` row headers: **138** — 123 `runtime` headers, 8 proposed-family headers (4 `proposed`,
  2 `proposed test`, 2 `proposed tests`), 7 `development`. So 131 rows from the two landed
  inventories plus the development laws, matching the file's own Counts section (64 ledger-lane
  rows: 12 custody, 17 capacity, 13 wake, 19 coordination-ledger, 3 proposed; 67 validator-lane
  rows: 20 closed shapes, 14 authorization boundaries, 11 permission rows, 17 contribution and
  landing, 5 proposed).
- **12 enforced-but-unpinned rows are marked.** The Counts section lists all twelve ids (CAP-8,
  WAKE-10, LEDG-2 half, LEDG-15, CUST-3, CS-19, AB-02, PM-01 refusal arm, PM-03, PM-08, CL-10
  arm, CL-13 race arm). Eleven carry header markers; CUST-3 carries the marking in its row body
  ("Test: none pins this directly") — a minor formatting inconsistency, noted.
- **8 proposed rows are marked** as a distinct kind family and listed by id (PROP-1..3,
  PR-01..05).

### The plain-explanation rule

528 explanation lines (everything between `Plain explanation:` and `Trace:`) scanned: **zero**
issue numbers, **zero** file or path names, **zero** backticks, **zero** CamelCase identifiers.
The row-format glossary defines the allowed vocabulary (worker, workspace, shared log,
notification, contribution, typed refusal). The operator's rule is satisfied.

### Trace sampling (14 rows across both inventories)

Ledger-lane rows restated from the inventory this seat verified at seq 20187: CUST-1
(`shared-workspace-custody.mjs:38`), CAP-1 (`host-capacity.mjs:230`), WAKE-1
(`wake-stream.mjs:120`), LEDG-1 (`coordination-ledger.mjs:1199`) — all four verified there and
restated unchanged.

Validator-lane rows verified fresh at this tree, whose `impl/src` is byte-identical to the
stated base `bc2e4fcd`:

| Row | Trace | Observed |
|---|---|---|
| CS-01 | `swarm-contract.mjs:10-29` `SWARM_EVENT_KINDS` | exactly thirteen kinds |
| CS-16 | `swarm-refusals.mjs:35-186, 203-207`; `swarm-state.mjs:178-185` | one closed refusal table; construction-time guard wired into both refusal paths |
| AB-03 | `swarm-state.mjs:210-220` `validClaimPaths` | non-empty, non-empty strings, repo-relative, no `..`, no leading `./` |
| AB-06 | `swarm-state.mjs:1851-1876` | "a claim moves only from its holder" refusal on a non-holder move |
| PM-01 | `swarm-runtime.mjs:1032`; recruit check `:7927-7929` | `SWARM_PERMISSIONS` is exactly seven permissions |
| PM-08 | `swarm-runtime.mjs:7930-7932` | "Delegation cannot grant authority the caller does not hold"; marked unpinned, consistent with the absent test |
| PM-10 | `swarm-runtime.mjs:8648-8650` | self-check refused (`self_check_refused`) |
| CL-02 | `swarm-runtime.mjs:7862-7864` | "Contributions must name their actual author" |
| CL-05 | `swarm-state.mjs:2210-2212` | `contribution_duplicate` on a reused id |
| CL-07 | `swarm-state.mjs:2258-2267` | reviews append; "never erase prior reviews" |

All sampled traces hold. The remaining 117 inventory rows carry the author's checked-at-base
claim (131/131); this review verified the counts, the markings, the vocabulary rule and a
14-row sample spanning both inventories and all six domains.

## Decision

accept — the row counts, the unpinned and proposed markings, the plain-explanation rule and the
sampled traces all verify; the file is ready for the operator to begin approving rows.
