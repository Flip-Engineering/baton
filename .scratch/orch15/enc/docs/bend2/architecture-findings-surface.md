# Surface lane architecture findings

Provenance: bend2-arch-surface-lane, contribution-d31675f128fc5af02a973ee72f16014a.

Revision read: `bc2e4fcd072e9edce21f94ee2ea8bb938c409f9b`. The lane was read only; this file preserves its accepted contribution on the branch.

## F1. attention redaction duplicate

**Deletion or merge.** Merge the two shipped credential-redaction implementations into one. impl/src/messages.mjs:546-556 and impl/src/application-observation.mjs:336-343 are two bodies behind the same exported name, and the application copy is the one the client-visible run surface reads. Delete the application copy and keep one module owning the pattern set, the vocabulary and the truncation marker.

**Evidence.** messages.mjs:546-556 replaces each matched secret inline with '[redacted]' (patterns at :521-528, six of them, including AKIA at :526 and the JWT form at :527) and reports a cap through ATTENTION_TRUNCATION_MARKER '[truncated]' reserved inside the byte ceiling (:518-519, :553-555). application-observation.mjs:336-343 replaces the WHOLE string with '[credential-shaped content redacted]' on any match (patterns at :330-335, four of them: no AKIA, no JWT), emits no truncation marker at all (it appends an ellipsis at :342), coerces a non-string to empty string, and trims. Consumers measured: application.mjs:92,140,161 and the attention projection at application-observation.mjs:554,3802 take the application copy; runtime-admission.mjs:25, runtime-api.mjs:23 and runtime-recovery.mjs:11 take the messages.mjs copy. The pattern set is declared five times with three sensitivities: messages.mjs:521-528 (6 patterns), application-observation.mjs:330-335 (4), goal-plan.mjs:89-96 (6, used as a predicate), context-runtime.mjs:41-46 (5, AKIA|ASIA, predicate), verifier-diagnostics.mjs:6-13 (6, JWT threshold {8,} where messages.mjs uses {16,}). Byte-capping is duplicated too: messages.mjs:531-541 capBytes returns {text,truncated} against application-observation.mjs:319-329 capBytesToScalar returning a bare string. Test pins on both vocabularies: 11 test files assert '[credential-shaped content redacted]' (phase64-integrated-run-application.test.mjs:515 on view.attention[0].question, reflex2-boards-red.test.mjs:399, issue10-blocked-interaction-red.test.mjs:174, issue451-integrate-dependency-link.test.mjs:244 'the redaction is the one vocabulary'), 3 assert '[redacted]'; frame-economics-red.test.mjs:1947-1953 pins the marker end to end through the coordinator context read only ('stage: truncation-marker-missing').

**Source files.** [impl/src/messages.mjs](../../impl/src/messages.mjs), [impl/src/application-observation.mjs](../../impl/src/application-observation.mjs), [impl/src/application.mjs](../../impl/src/application.mjs), [impl/src/verifier-diagnostics.mjs](../../impl/src/verifier-diagnostics.mjs), [impl/src/goal-plan.mjs](../../impl/src/goal-plan.mjs), [impl/src/context-runtime.mjs](../../impl/src/context-runtime.mjs).

**Tests.** node --test impl/test/frame-economics-red.test.mjs impl/test/phase64-integrated-run-application.test.mjs impl/test/issue299-tool-row-digests.test.mjs impl/test/issue451-integrate-dependency-link.test.mjs.

**Loss if wrong.** Unify on the messages.mjs copy and every client-visible assertion above changes text (inline '[redacted]' where the surfaces now show the whole-string marker), and a long text carrying one secret stops collapsing to a bare marker. Unify on the application copy and the coordinator context-read renderer loses AKIA access-key-id and JWT detection (an AWS key id or a bearer JWT reaches the renderer unredacted) and loses the truncation marker that frame-economics-red.test.mjs:1949 names as the pinned mutation class. A merge has to carry the union pattern set and re-pin both test families; until then the application surface runs the weaker policy while every test that reads the coordinator path stays green.

## F2. seam inventory shadow type

**Deletion or merge.** Delete the ast-grep seam classifier and every shadow copy of the partition it produces: impl/scripts/seam-inventory.mjs, impl/scripts/seam-inventory.json, the inventory half of impl/src/landing-table.mjs:121-141, the impl/scripts/surface-gate.mjs:23 consumer, and the five tests that re-derive the partition from source. Replace it with the authority seam carried on each split method's declared type.

**Evidence.** The artifact is 26,008 lines covering 22 files, 2,670 members and 5 seam classes, produced by parsing source with @ast-grep/napi (impl/scripts/seam-inventory.mjs:2-48, 60-69, 102-108). The split modules keep the partner's declarations verbatim: runtime-recovery.mjs:26-31 records 28 module-scope declarations relocated and re-exported so the coordinator export surface is unchanged, and defines its own deepFreeze (:297) and canonicalDigest (:309) instead of importing coordination-internals copies. A duplicate-name census over impl/src/*.mjs finds 42 export names declared in two files each; canonical, canonicalDigest, deepFreeze, bindingFence and boardFence are five of the coordinator-to-runtime pairs. coordinator.mjs carries 323 single-line delegates of the form 'return runtime<Mod>.<name>(this, this._recorder, ...)' into the six partials. Five tests re-derive the partition from source against hard-coded tables: application-observation.test.mjs:43-45 (ARITIES, PARAMS, ASYNC) and :156-158 (member counts 174 and 237), coordination-admission.test.mjs:193, coordination-internals.test.mjs:187, coordination-ledger.test.mjs:199, coordination-ledger-writes.test.mjs:157 - each asserting 'one bijection'. Measured at this checkout: node --test impl/test/application-observation.test.mjs FAILS AO5, assert.equal(target.members.length, 174) at test/application-observation.test.mjs:156 with observed 175, while node impl/scripts/seam-inventory.mjs answers 'seam-inventory: ok' with exit 0. The committed artifact is current and the hard-coded count is stale; impl/scripts/expected-red-tests.json (451 rows) has no row naming it and impl/scripts/run-suite.mjs:305 runs every test/*.test.mjs file. The four sibling suites pass (coordination-internals + coordination-ledger-writes 12 pass 0 fail; coordination-admission + coordination-ledger 12 pass 0 fail): the copies that read the map as data stayed green, the copy that hard-codes numbers went red. landing-table.mjs:121-132 additionally makes a missing or unreadable inventory a silent narrowing - 'A missing or unreadable inventory is NOT a refusal - the gate set falls back to the region table alone, and inventoried reads empty rather than inventing coverage'.

**Source files.** [impl/scripts/seam-inventory.mjs](../../impl/scripts/seam-inventory.mjs), [impl/scripts/seam-inventory.json](../../impl/scripts/seam-inventory.json), [impl/src/landing-table.mjs](../../impl/src/landing-table.mjs), [impl/scripts/surface-gate.mjs](../../impl/scripts/surface-gate.mjs), [impl/test/application-observation.test.mjs](../../impl/test/application-observation.test.mjs), [impl/test/coordination-admission.test.mjs](../../impl/test/coordination-admission.test.mjs), [impl/test/coordination-internals.test.mjs](../../impl/test/coordination-internals.test.mjs), [impl/test/coordination-ledger.test.mjs](../../impl/test/coordination-ledger.test.mjs), [impl/test/coordination-ledger-writes.test.mjs](../../impl/test/coordination-ledger-writes.test.mjs).

**Tests.** node impl/scripts/seam-inventory.mjs; node --test impl/test/application-observation.test.mjs impl/test/coordination-admission.test.mjs impl/test/coordination-internals.test.mjs impl/test/coordination-ledger.test.mjs impl/test/coordination-ledger-writes.test.mjs.

**Loss if wrong.** Delete the map and the landing gate set loses the only written link from a changed path to a seam class: landing-table.mjs:8-12 declares the inventory the ONLY source of which modules are inventoried seams, so with it absent every changed seam module falls back to the region table's coarse test-name tokens and a landed change to a split bucket is verified only by whichever tests that table happens to name. Replacing it with a declared type removes the second textual copy of the partition that is already stale at HEAD; if the type is not carried on the split methods themselves, nothing records that a bucket's members were moved, and the five re-derivation tests can only compare one copy against another.

## F3. provider supervisor merge

**Deletion or merge.** Merge ProviderPollSupervisor and ProviderProcessingSupervisor into one interval supervisor over {action, backoff policy, fan-out}, and fold SessionRecoverySupervisor's start/_run/status/close shape onto the same base. Delete one of the two class bodies and the duplicated timer plumbing.

**Evidence.** The two classes are one machine with two receivers: typed() at :3 versus :1, positive() at :4 versus :2, _emit at :31-33 versus :30, _schedule with the same 'this._closed || timer || promise' guard, the same 'if (row.timer !== handle) return' latch and the same handle.unref() at :35-39 versus :32-35, _run with the same single-flight guard and AbortController at :41-43 versus :38-40, and start/status/close in the same order at :66-70/:74-79/:81-91 versus :67-70/:73-75/:77-85. SessionRecoverySupervisor is the same start/_run/status/close skeleton one-shot (session-recovery-supervisor.mjs:24-74). All three are constructed from the same factory block at index.mjs:1710, 1718, 1727 and re-exported at :202-204, and both provider supervisors are exercised by the same test file, test/phase43-provider-poll-lifecycle.test.mjs.

**Source files.** [impl/src/provider-poll-supervisor.mjs](../../impl/src/provider-poll-supervisor.mjs), [impl/src/provider-processing-supervisor.mjs](../../impl/src/provider-processing-supervisor.mjs), [impl/src/session-recovery-supervisor.mjs](../../impl/src/session-recovery-supervisor.mjs), [impl/src/index.mjs](../../impl/src/index.mjs).

**Tests.** node --test impl/test/phase43-provider-poll-lifecycle.test.mjs impl/test/phase43-provider-reconciliation.test.mjs impl/test/phase45-session-auto-rejoin.test.mjs.

**Loss if wrong.** The two are not one policy: poll enforces each card's own ceiling (provider-poll-supervisor.mjs:25 'provider poll timing exceeds card ceiling', against card.poll.maxBackoffMs validated at :24) while processing deliberately leaves durable per-root backoff in the store and owns only scan timing (provider-processing-supervisor.mjs:5-7). A merge that flattens the policies loses the poll card's per-provider ceiling refusal; a merge that gives processing the poll's row state loses the processing supervisor's documented statelessness. The merged machine has to keep the ceiling as a per-card value and the durable backoff as the store's.

## F4. archive ceiling single

**Deletion or merge.** Delete application-observation.mjs:275-281 (resultExportArchiveCeiling(policy)) and import the result-export.mjs:894 body at both of its call sites (application.mjs:4410 and application-observation.mjs:2184).

**Evidence.** Two exported functions of the same name derive the same archive bound: result-export.mjs:894 (signature {maxFiles,maxBytes}, refusal code result_export_invalid, used by the export pipeline at :714 and :717, documented at :890-893 as the ONE deployment-derived archive ceiling) and application-observation.mjs:275 (signature policy, refusal code application_export_policy_stale at :278, used at :2184 and by application.mjs:4410 through the import at application.mjs:140; its MAX_RUN_VIEW_BYTES at :50 is the same registry row result-export reads). Neither imports the other, and result-export.mjs imports only canonical-order.mjs and limits.mjs, so no import cycle forces the copy. The uniqueness pin is issue413-export-archive-bound.test.mjs:158-169 ('I413-4 RED: the ONE deployment-derived archive ceiling is exported and registry-sourced'), measured passing 4/4 here. No test asserts application_export_policy_stale, so the copy is untested at both of its call sites.

**Source files.** [impl/src/application-observation.mjs](../../impl/src/application-observation.mjs), [impl/src/application.mjs](../../impl/src/application.mjs), [impl/src/result-export.mjs](../../impl/src/result-export.mjs).

**Tests.** node --test impl/test/issue413-export-archive-bound.test.mjs impl/test/phase66-result-export-adversarial.test.mjs.

**Loss if wrong.** Merging changes the refusal code the profile-export path raises today (application_export_policy_stale becomes result_export_invalid), which is the one observable difference between the copies, and it puts an application-lane module on the export lane's module as a dependency. Keeping the copy instead means the bound is derived twice in one process and the untested copy is the one the application facade uses for its own profile archive, so a change to the export pipeline's derivation leaves the facade deriving the old bound with nothing to fail.

## F5. lineage validator merge

**Deletion or merge.** Merge impl/src/context-result-lineage.mjs and impl/src/context-effect-result-lineage.mjs into one validator parameterised by the call normaliser and the source-evidence row shape. The two files share their whole constant table and differ only in the normaliser and two field sets.

**Evidence.** Measured: 312 lines versus 519 lines, diff 451 lines. Lines 1-6 are the same imports except the normaliser (normalizeContextMapCall from context-map.mjs at :4 versus normalizeContextEffectCall from context-call.mjs at :4); DIGEST and GIT_SHA are byte-identical at :9-10, BUILD_FIELDS at :11-14, VALIDATE_FIELDS at :15-18, OUTPUT_FIELDS at :19-22, ARTIFACT_FIELDS at :29 versus :40. The real differences are the SOURCE_EVIDENCE_FIELDS row (the effect copy adds callDigest, callId, generation, partitions, providerResults, source) and the accepted-child row plus a GENERIC_SOURCE_EVIDENCE_FIELDS row the map copy does not carry.

**Source files.** [impl/src/context-result-lineage.mjs](../../impl/src/context-result-lineage.mjs), [impl/src/context-effect-result-lineage.mjs](../../impl/src/context-effect-result-lineage.mjs).

**Tests.** the context lineage suite that drives both modules (the map path and the effect path).

**Loss if wrong.** Those two field sets are how a reader tells a context-map result from a context-effect result, so a merge that unifies them accepts an effect record on the map path (or the reverse) and the acceptance is invisible downstream. Leaving them split carries the opposite risk: a field added to one row shape is not mirrored in the other, and the two validators then disagree about the same digest.

## F6. northbound capability value

**Deletion or merge.** Delete the exported string-token capability in impl/src/northbound-capability-authority.mjs and the six duck-typed ternaries that carry it optionally; give the northbound verbs one home and make the capability a value the surface constructor produces, consumed once per call. Move resumeCapabilityNorthbound out of coordinator.mjs:5295-5298 beside its two siblings.

**Evidence.** The proof of authority is an interned string: northbound-capability-authority.mjs:6 fixes TOKENS to 'northbound:web' and 'northbound:mcp', and :9-11 exports northboundCapabilityToken, so any in-process caller mints what the check at :14-16 accepts. Mint sites: mcp-northbound.mjs:2318, 2489, 2860-2862 and 3016; web-northbound.mjs:1968, 2118, 2230, 2304-2306 and 2754. Check sites: application-observation.mjs:971, mcp-web-bridge.mjs:892, coordinator.mjs:5296, runtime-admission.mjs:2092 and :2097. The three verbs are one authority triple split by the classification: invokeCapabilityNorthbound (runtime-admission.mjs:2091) and reverifyCapabilityNorthbound (:2096) moved to the partial, while resumeCapabilityNorthbound stayed inline in coordinator.mjs:5295-5298 with its body verbatim against the other two's delegate. Each surface call site wraps the call in a typeof ternary that drops the token entirely when the verb is absent (mcp-northbound.mjs:2860-2862, web-northbound.mjs:2304-2306), and tests build stub coordinators that make that false branch reachable (coordinator: {} at test/bridge-idempotency-344.test.mjs:186 and test/error-actionability-red.test.mjs:207).

**Source files.** [impl/src/northbound-capability-authority.mjs](../../impl/src/northbound-capability-authority.mjs), [impl/src/mcp-northbound.mjs](../../impl/src/mcp-northbound.mjs), [impl/src/web-northbound.mjs](../../impl/src/web-northbound.mjs), [impl/src/runtime-admission.mjs](../../impl/src/runtime-admission.mjs), [impl/src/coordinator.mjs](../../impl/src/coordinator.mjs), [impl/src/mcp-web-bridge.mjs](../../impl/src/mcp-web-bridge.mjs), [impl/src/application-observation.mjs](../../impl/src/application-observation.mjs).

**Tests.** node --test impl/test/bridge-idempotency-344.test.mjs impl/test/error-actionability-red.test.mjs impl/test/mcp-descriptor-auth.test.mjs.

**Loss if wrong.** The token is a bearer string exported from its own module, so the check is a convention any in-process caller satisfies by calling the exported mint; an affine value produced by the surface constructor and consumed once is what would make the refusal structural. Delete the token without a replacement and the two copies of the package (the MP18 stdio factory resolves the installed package while the host runs the worktree, per northbound-capability-authority.mjs:1-5) fail object-identity comparison and refuse every context. Delete the ternaries without moving resumeCapabilityNorthbound and the MCP and web surfaces lose the ability to speak to a Coordinator whose three verbs are not all present, which the stub-coordinator tests currently rely on.

## F7. atlas substrate single

**Deletion or merge.** Add one atlas-substrate module owning the native version, the extension-to-Lang map and the underlying string, and let atlas-cpg.mjs, atlas-index.mjs, atlas-rewrite.mjs and atlas-structural.mjs import it. Delete the four createRequire/require version reads and the three language maps.

**Evidence.** Four modules each call createRequire(import.meta.url) and then require('@ast-grep/napi/package.json').version: atlas-cpg.mjs:8-9, atlas-index.mjs:9-10, atlas-rewrite.mjs:7-8, atlas-structural.mjs:8-9. Each republishes it in its capability card's underlying field: atlas-cpg.mjs:98, atlas-index.mjs:275, atlas-rewrite.mjs:109, atlas-structural.mjs:169, and again in per-result cost.underlying at atlas-cpg.mjs:326, atlas-rewrite.mjs:190 and atlas-structural.mjs:213. Three differently shaped language maps exist: atlas-cpg.mjs:10 LANG keyed by extension, atlas-index.mjs:12 LANGUAGE, atlas-structural.mjs:10 LANGUAGE with both language-name and extension keys, atlas-rewrite.mjs:9 LANGUAGE. All four also carry the same static import of Lang and parse from @ast-grep/napi at line 5.

**Source files.** [impl/src/atlas-cpg.mjs](../../impl/src/atlas-cpg.mjs), [impl/src/atlas-index.mjs](../../impl/src/atlas-index.mjs), [impl/src/atlas-rewrite.mjs](../../impl/src/atlas-rewrite.mjs), [impl/src/atlas-structural.mjs](../../impl/src/atlas-structural.mjs).

**Tests.** the atlas suite that asserts each capability card's underlying field.

**Loss if wrong.** The cards are the surface a caller reads to decide whether a capability is available and what it runs on, so the version string is part of the contract: four derivations mean an @ast-grep/napi bump can leave three cards naming the old version while the fourth moves. The map drift has a behavioural edge too - the same file can be admitted by one atlas capability and refused by another. Merging the maps too eagerly is the opposite error: the extension-keyed and language-name-keyed shapes are two lookups over one table, not one flattened map.

## F8. production wrapper succession

**Deletion or merge.** Collapse each duplicated production wrapper to one definition: wrapProductionDeployment (production-convergence.mjs:321 versus production-deployment-convergence.mjs:221) and wrapProductionMcpServer (production-mcp-convergence.mjs:750 versus production-mcp-complete.mjs:11). Keep the decorator's behaviour in one place and name the two entry paths explicitly.

**Evidence.** Two live definitions of each name. wrapProductionDeployment: production-convergence.mjs:321 (ProductionConvergenceRuntime) is what the CLI uses (scripts/baton.mjs:26 imports it, :137 wraps), and production-deployment-convergence.mjs:221 (DurableProductionConvergenceRuntime) is what index-converged.mjs:13,27 uses, with package.json exporting the latter as './production-convergence'. wrapProductionMcpServer: production-mcp-convergence.mjs:750 is the base; production-mcp-complete.mjs:2 imports it as wrapBaseProductionMcpServer and :11 defines its own decorator, whose header says it changes 'only the previously broken attention authorization seam' and which delegates at :18. The decorator is what every live entry uses (configured-mcp-client.mjs:7,29; index-converged.mjs:16; scripts/mcp-stdio.mjs:8); the base is used directly only by test/unified-mcp-surface.test.mjs:5 and by the decorator itself.

**Source files.** [impl/src/production-convergence.mjs](../../impl/src/production-convergence.mjs), [impl/src/production-deployment-convergence.mjs](../../impl/src/production-deployment-convergence.mjs), [impl/src/production-mcp-convergence.mjs](../../impl/src/production-mcp-convergence.mjs), [impl/src/production-mcp-complete.mjs](../../impl/src/production-mcp-complete.mjs), [impl/src/index-converged.mjs](../../impl/src/index-converged.mjs), [impl/scripts/baton.mjs](../../impl/scripts/baton.mjs).

**Tests.** node --test impl/test/production-convergence.test.mjs impl/test/production-deployment-convergence.test.mjs impl/test/production-mcp-convergence.test.mjs impl/test/production-attention-authorization.test.mjs impl/test/converged-export-surface.test.mjs.

**Loss if wrong.** The two deployment wrappers are two recovery behaviours, not two spellings, and the difference is observable only on a deployment that recovers, so collapsing them silently picks one path's startup recovery for the other entry. For the MCP pair, deleting the decorator leaves the attention authorization seam undecorated, which is exactly the broken state its own header records; deleting the base leaves the decorator with nothing to delegate the tool catalogue, admission and meta authority to.

## F9. rpc client close latch

**Deletion or merge.** Merge OmpRpcProcess onto AcpJsonRpcProcess, or extract one JSON-RPC child-process base, so the spawn options, the close latch, kill() and closePromise are defined once.

**Evidence.** omp-rpc.mjs:135 states it: 'Surface mirrors AcpJsonRpcProcess (start/closePromise/kill/child) so adapters read alike.' The identical close latch is stated twice as a fix: acp-json-rpc-process.mjs:56-64 creates the latch at construction so kill() can record its stop authority before the exit lands, and omp-rpc.mjs:192-196 restates the same reasoning for the same latch ('The exact-close latch is created at START (the AcpJsonRpcProcess pattern) ... Creating it lazily at exit (the old shape) meant no stop record existed when kill() asked'). Both spawn with detached:true and stdio pipe/pipe/pipe (omp-rpc.mjs:189-191), and both terminate through the same ProcessCloseReapLatch consumer set (the adapter dispatch path); acp-json-rpc-process.mjs is imported by kimi-acp.mjs:303, grok-acp.mjs:26 and test/acp-json-rpc-process.test.mjs.

**Source files.** [impl/src/omp-rpc.mjs](../../impl/src/omp-rpc.mjs), [impl/src/acp-json-rpc-process.mjs](../../impl/src/acp-json-rpc-process.mjs), [impl/src/adapter.mjs](../../impl/src/adapter.mjs).

**Tests.** node --test impl/test/acp-json-rpc-process.test.mjs impl/test/omp-process-truth.test.mjs impl/test/omp-rpc-red.test.mjs.

**Loss if wrong.** The two transports are not the same protocol: omp's RPC has a ready frame and agent session events (omp-rpc.mjs:132-136) while ACP carries sessions and permission requests, so the frame-level differences have to survive as hooks on one base. The latch itself is the part with no divergence, and leaving two copies means the next close-lifecycle fix is written twice - and the second copy is where a stop degrades to unconfirmed crash noise plus a forced reap, which is what the note at omp-rpc.mjs:192-196 records.

## F10. esm shim modules

**Deletion or merge.** Delete impl/src/workflow-lane.mjs (a 9-line re-export whose only consumer is a test) and impl/src/native-modules.mjs (a 10-line re-export whose stated premise is false at this revision and which provides no laziness), and delete the inlined resolveResultPin copy at workflow-interpreter.mjs:406-427 in favour of the exported wave.mjs:231 implementation.

**Evidence.** workflow-lane.mjs is 9 lines: a comment plus 'export { runWorkflow } from ./workflow-interpreter.mjs'. Consumers measured: test/workflow-as-data-red.test.mjs:1467 (import), :1469 (existence), :1478 (source text), :1488 (walkImportGraph) and :1492 (idempotent import), plus a comment at workflow-interpreter.mjs:13; no production module imports it. native-modules.mjs is 10 lines and its header claims '@ast-grep/napi is an optionalDependency', which impl/package.json:52-58 contradicts: @ast-grep/napi 0.44.1 sits under dependencies and @mozilla/readability 0.5.0 is the single optionalDependencies entry (pinned by test/browser-use-red.test.mjs:392-401, which also asserts the engine never leaks into dependencies). It provides no laziness either: index.mjs:26-29 imports it statically, atlas-index.mjs:5-6 imports @ast-grep/napi statically, and index.mjs:193-194 re-exports two of the same classes from their own modules. Its only importer is index.mjs:29. resolveResultPin: the exported original is wave.mjs:231, async, taking {repoRoot,report,startedAtMs,excludeShas,signal} and honouring the signal (wave.mjs:236-240 notes that a synchronous read cannot honour a budget or a cancellation), pinned by test/wave-driver-red.test.mjs:346-355; the copy at workflow-interpreter.mjs:406-427 is synchronous execFileSync with the same git for-each-ref and cat-file logic and the comment 'inlined from wave.mjs:134-155'.

**Source files.** [impl/src/workflow-lane.mjs](../../impl/src/workflow-lane.mjs), [impl/src/native-modules.mjs](../../impl/src/native-modules.mjs), [impl/src/index.mjs](../../impl/src/index.mjs), [impl/src/workflow-interpreter.mjs](../../impl/src/workflow-interpreter.mjs), [impl/src/wave.mjs](../../impl/src/wave.mjs).

**Tests.** node --test impl/test/workflow-as-data-red.test.mjs impl/test/wave-driver-red.test.mjs impl/test/browser-use-red.test.mjs.

**Loss if wrong.** Deleting workflow-lane.mjs removes the object the suite's import-graph law is asserted over - the law (importing the lane starts nothing) is checked by graph-walking workflow-lane.mjs:1488, so the test has to be re-expressed over workflow-interpreter.mjs, which is the module the law is about. Deleting native-modules.mjs removes a claimed laziness boundary that does not exist today; a real optional-dependency posture for the atlas family has to be built at the capability seam, because index.mjs currently reaches the native module eagerly through atlas-index.mjs. Collapsing resolveResultPin to the async original changes the lane's evaluation from synchronous to abortable and adds a promisified node:child_process import to the graph that workflow-lane.mjs exists to keep to node built-ins, so the three deletions are one decision, not three.

## F11. wave join seam

**Deletion or merge.** Express wave member supervision as one join over first-class member values plus a supervision policy, instead of a poll loop in one layer and a Promise.all fan-out in the other. Delete the per-member timer and nudge bookkeeping from wave-driver.mjs and keep the policy as a value.

**Evidence.** Two representations of the same join live in the wave family. workflow-interpreter.mjs:924-925 is the fan-out form: 'while (pending.size > 0) { await Promise.all([...pending].map((role) => processMember(role))); ... }'. wave-driver.mjs is the polling form: the L4-L6 loop at :613 (for (;;)), one status read per member per poll at :629, decision delivery at :710, nudge-on-checkpoint at :756, the settle count at :831, per-member stall accounting at :845, and a three-attempt claim recovery loop at :887-893. The policy the loop implements is already a declared value at :40-47 (pollIntervalMs 20_000, stallTimeoutMs 20*60_000, settleTimeoutMs 5_000, finalization, unproductiveNudgeBudget 1, refusalNudgeBudget 2), with :34-38 recording the ruling that no wall-clock kill cap exists. Per-member mutable state (memberState, memberProgress, claims, decisionFired) is what stands in for member values.

**Source files.** [impl/src/wave-driver.mjs](../../impl/src/wave-driver.mjs), [impl/src/wave.mjs](../../impl/src/wave.mjs), [impl/src/workflow-interpreter.mjs](../../impl/src/workflow-interpreter.mjs).

**Tests.** node --test impl/test/workflow-surface-red.test.mjs impl/test/wave-driver-red.test.mjs.

**Loss if wrong.** What the loop carries and a naive join drops: the per-member stall clock and the L6 unproductivity cycle (park, nudge once, re-park on an unchanged changedPathsDigest), the claim-turn resolution on a paused member (:756-770, :876-885), the refusal-nudge budget drawn per delivered acknowledgment, and the settle window that decides completion from terminality rather than from process exit. Those are the supervision policy's semantics; a rewrite that keeps only Promise.all loses exactly the stall and unproductivity machinery the suite's red cases pin.

## F12. adapter card contract

**Deletion or merge.** Make adapter-contract.mjs the single card check and delete the duck-type check at adapter.mjs:316-325, or fold the axis set into ADAPTER_CONTRACT_DEFINITION so the load-time self-check has one subject.

**Evidence.** adapter-contract.mjs:1-7 records the incident in its own words: 'Two checks disagreed about the same card. assertIsAdapter (adapter.mjs) duck-types the D1 method set and accepts any card(); the dispatch path normalizes the card's workerPolicy and the legacy SubprocessAdapterBase tier publishes none, so a live export on the module's public surface is refused LATE, with a generic worker_policy_invalid that never names the axis the card is missing.' Both checks are live: ADAPTER_CONTRACT_DEFINITION at adapter.mjs:310-314 with assertIsAdapter at :316-325 verifying only that the eight methods exist, while adapter-contract.mjs:69-125 declares the axes, :201-213 self-checks them at load time and :161-190 completes a legacy subprocess card. The fix added a third module rather than replacing the first check.

**Source files.** [impl/src/adapter-contract.mjs](../../impl/src/adapter-contract.mjs), [impl/src/adapter.mjs](../../impl/src/adapter.mjs).

**Tests.** node --test impl/test/adapter.test.mjs impl/test/omp-card-contract.test.mjs.

**Loss if wrong.** assertIsAdapter is what refuses a non-adapter at registration and adapter-contract is what names the axis a card is missing, so deleting the wrong one either admits a card-shaped object without the method set into dispatch or removes the named-axis refusal and returns to the late generic worker_policy_invalid the module was written to end. A merge has to hold both parts: the method set is one axis of the same contract.

## Lane verification

- node scripts/seam-inventory.mjs -> 'seam-inventory: ok', exit 0 (the committed impl/scripts/seam-inventory.json is current with the source)
- node --test impl/test/application-observation.test.mjs -> FAIL: AO5 at test/application-observation.test.mjs:156, assert.equal(target.members.length, 174), observed 175
- node --test impl/test/coordination-internals.test.mjs impl/test/coordination-ledger-writes.test.mjs -> pass 12, fail 0
- node --test impl/test/coordination-admission.test.mjs impl/test/coordination-ledger.test.mjs -> pass 12, fail 0
- node --test impl/test/issue413-export-archive-bound.test.mjs -> pass 4, fail 0

## Carried facts

- Measured red at the pinned base: 'node --test impl/test/application-observation.test.mjs' fails AO5 at test/application-observation.test.mjs:156 (assert.equal(target.members.length, 174); observed 175) while 'node impl/scripts/seam-inventory.mjs' answers 'seam-inventory: ok' exit 0, so the committed artifact is current and the hard-coded count is stale. impl/scripts/expected-red-tests.json (451 rows) carries no row naming AO5 and impl/scripts/run-suite.mjs:305 runs every test/*.test.mjs file, so the failure is unabsorbed. Reproducible on a clean checkout at this head; recording it on the branch is the orchestrator's call, not this lane's.
- The duplicate-name census over impl/src/*.mjs finds 42 export names declared in two files each. The surface-lane pairs are itemized here (resultExportArchiveCeiling, wrapProductionDeployment, wrapProductionMcpServer, and the coordinator-to-runtime helpers as seen from application-observation.mjs). The rest sit on the coordinator-to-runtime partial split (coordination-ledger, coordination-internals, coordination-admission and coordination-replay against runtime-observation, runtime-admission and runtime-recovery) and belong to the coordination lane.
- Subsystems checked and found to own one implementation each, so this lane proposes no change to them: route-quota.mjs (derived expiry, no timer or poller), route-tuple.mjs, router.mjs, provider-faults.mjs and provider-governance.mjs (distinct owners), route-liveness.mjs (its own issue-375 module), concurrency-policy.mjs (one ceiling representation), limits.mjs (single registry), canonical-order.mjs, web-result-export-delivery.mjs (consumes result-export.mjs), control-surface-unification.mjs (derives from the registry), and acp-json-rpc-process.mjs as a single client reused by kimi-acp.mjs and grok-acp.mjs.
