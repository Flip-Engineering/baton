# Handoff — 2026-08-14, post-v20 flood (orchestrator: kimi → glm-5.3 seat)

State at handoff. Everything below is committed; master HEAD is the source of truth.

## Landed today (all pushed)

- `8ec52a6c` — **#163 the no-clock law** (operator ruling): hardCap ripped out everywhere
  (interpreter / wave-driver / recipes / production default; a numeric cap refuses
  `workflow_spec_invalid`). Replacement: quiescence-derived completion — exits
  `pending_empty` / `terminalized_unrecoverable` / `stuck_handled` / `quiesced`; quiet window
  `max(2×maxObservedGapMs, 8×poll)`; phase-stuck terminalization window `max(4×gap, 96×poll)`;
  ACTIVE_TURN_PHASES gate; WAVE-QUIESCED verdict + per-member declaration snapshot.
  Suite quiescence-completion-red 15/15.
- `8551955` WLS-1 single-pass index (waves.list/progress: 162→3 log reads/page).
- `cda6355` async base-commit + skip-if-clean (the launch blockade fix).
- `3794b583` **worktree-availability tri-state** — errored read = UNKNOWN, never fatal; kills on
  positive-false or 3 consecutive unknown sweeps. Pin RED-verified; coordinator 58/58.
- `852700a5` **phantom surfacing** — `wave_member_start_failed` evidence at drive start +
  settle outcomes carry `terminalCause:'start'` + typed error. Pin W2-phantom RED-verified.
- `47ac21c7` #144 LSP pool landed (23/23). `5dc5e128` #161 plan-object store-fold leg (8/47 =
  declared split; the surface leg is plan-object-c's partition).
- `ed655c3e` #220 versioned committer identity (`baton <version>` from impl/package.json).
- `6a40ebc8` the update report (`reviews/baton-update-2026-08-14.html`, README cross-refs it).

## Fleet state

- v19 resident is UP (incarnation 258b01fa) running master minus `3794b583`/`852700a5`
  (booted 15:11Z; the authority tri-state + phantom surfacing landed after). **Restart to pick
  them up when the fleet is idle — it is idle now.**
- v19 flood: 15 waves, all settled INCOMPLETE — 14 GLM member deaths (provider event under
  15-wave load; glm alive-after by direct probe) + the phantom class.
- v20 re-drive (14 packs, deepseek seats): all settled — 12 INCOMPLETE + 2 QUIESCED. Causes:
  7 members murdered by an ORCHESTRATOR-SIDE `git worktree remove` racing the re-attachment of
  same-content re-drives (my error, owned; the tri-state fix + the OPS law now cover it);
  phantom start losses (now surfaced by `852700a5`); an 18:07Z cluster of 8 `lifecycle.crashed`
  with NO cause detail (the death-cert gap — crashes carry no exit code/stderr).
- **v21 re-drive is the pending fleet action**: bump the 12 failed packs rd-next and fire on a
  resident that carries `3794b583` + `852700a5`. Packs live under
  `docs/reference/evidence/*2026-08-14*/` (latest redriveN dir per pack); the patient launcher is
  `docs/reference/evidence/v20-redrive-2026-08-14.sh` (clone it for v21; keys must bump again).

## Open hot items (priority order)

1. **C2 durability** — flood-proven: durable member retry/session-restore (members drop instead
   of resuming), death certs (crashes carry no cause — every death is a forensic dig), member
   stderr/stdout harvest, phantom ROOT cause (the `852700a5` surfacing now names it per wave),
   #199/#200/#204/#207, #222 (starts head-of-line-block the bus; launcher curls need ≥240s).
2. **C5 honesty package** — on a green honesty wave: reconcile → full gate → close
   #157/#158/#159/#160 + #191.
3. **#163 follow-ons** — wave-driver stall-break is the last fate clock; leg-b catatonia
   refinement (`lastActivityAt` so tool-call-only members don't read as silent); R5's 300ms
   fixture is load-flaky under concurrent suite runs (green standalone).
4. The campaign frontier map: `reviews/baton-campaign-state-2026-08-14.html` (C1/C2/C6/K/P/D/G/E
   tiers, all issue-cross-referenced).

## OPS laws (binding)

- No direct dev by the orchestrator (waves do it; bootstrap exceptions need pins).
- Never edit a running script. NO clock/turn controls over agentic work (operator law).
- **No manual git/worktree mutations while any wave is in flight** (the re-attachment race).
- Briefs ≤ 4036 bytes; bump key AND path on every re-drive; watcher per launch; MEASURED limits
  only; resident launches only as background tasks.
