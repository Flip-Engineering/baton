# #59 SUITE BRIEF — red-first suite for the folded re-drive-continuity contract v1.1

You are drafting the **red-first acceptance suite** for the folded re-drive-continuity contract.
Read fully, in order: (1) `redrive-continuity-contract.md` (**v1.1** — source of truth); (2)
`contract-fold.md` (the blocker resolutions: per-item framing + neutralization, both renderers,
the one total render order across the #59/#69/#79 carried-content sections, the amended R3);
(3) `contract-redteam.md` (the attack surface — the poisoned-successor axis); (4) idioms:
`impl/test/bidirectional-v3-red.test.mjs` (the collaboration lane) and
`impl/test/worker-delivery-push-red.test.mjs` (the carried-content framing discipline).

## Coverage (from the v1.1 acceptance pins)

- **The closed content set + framing** — a re-drive with `carryForward` renders the named
  continuity section with the provenance line FIRST; every carried item is per-item
  `[carried/untrusted]` framed; a carried note containing `## Pending attention` or a fake frame
  header renders INERT (the neutralization row — the poisoned-successor kill).
- **Opt-in + refusals (D3)** — default-off (a plain re-drive carries nothing, byte-identical to
  today); a cross-role or unrelated-wave source refuses with the named typed code; surface-
  constant.
- **The trust posture (D4)** — carried content NEVER re-arms a gate, satisfies a verification,
  answers a steering cycle, or counts as the new attempt's digest (the TG2 distinct-digest class
  counts only THIS attempt's — the row must stage a carried digest that would qualify if counted
  and prove it isn't).
- **The pin digest list** — bound to the dead attempt's actual checkpoint history (an attacker
  pin never in that history refuses/is absent per the contract's rule).
- **The composition order** — the one total render order across the three carried-content
  sections is pinned and holds when two or more are present.
- **Refusals** — every code the contract names, typed, surface-constant.

## Suite law

Red-first (every capability row fails at a NAMED stage at HEAD); namespace imports for invented
surfaces; hermetic (mock adapters, mkdtemp, test.after, no network); run TWICE from the repo
root, record the stable split; header carries the row inventory + stages + invented signatures +
verified split; sorted-key literals ACTUAL order; `localeCompare` banned; no clocks; NUL
discipline.

## Deliverables (edit ONLY these)

`impl/test/redrive-continuity-red.test.mjs` ·
`docs/reference/evidence/redrive-continuity-2026-08-07/suite-draft-notes.md`.
