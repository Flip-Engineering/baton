# RESEARCH TASK (glm) — project-manager knowledge-graph cross-check

You are a RESEARCH worker. Deliverable: ONE markdown report at
`docs/reference/evidence/kg-cross-check-2026-08-12/project-manager-kg-review.md`.

## The task

Baton's knowledge plane uses tiered promotion: task-ephemeral (scratchpad) → workflow-ephemeral
(shared across a wave's members) → project-persistent (the KG, via the settlement ritual's
orchestrator review). A sibling project — **project-manager** (a separate repo by the same
operator, likely at a path like ~/Development/Experiments/project-manager or
~/Development/project-manager — FIND it; if it isn't on this machine, say so in the report and
work from what's available) — employs its own knowledge-graph approach for project management.

1. **Locate and read** the project-manager repo's knowledge-graph design (its docs, schema,
   and any graph/memory modules). If the repo isn't present locally, check for design docs or
   notes elsewhere under ~/Development (read-only; never write outside this repo).
2. **Compare**, concretely, against baton's tiered model (the scratchpad write/elevate/settle
   machinery, the KG settlement ritual, the promotion taxonomy, the contradiction workspace):
   - What does project-manager's KG capture that baton's does not (entity kinds, relations,
     lifecycle states, queries)?
   - What does baton's settlement-review promotion do that project-manager's does not?
   - Which of project-manager's patterns would genuinely improve baton's tiered KGs (be
     skeptical — some patterns fit a PM tool and not an orchestration kernel)?
3. **Name the top-3 actionable borrowings**, each with the baton seam it would ride (the
   settlement ritual, the recall surface, the context-package lane, or the REPL objects lane
   from the #69 contract at docs/reference/evidence/repl-realization-2026-08-07/).

## Laws

Read-only outside this repo; never write outside the deliverable path. Every claim about either
system cites the file you read it from. If the project-manager repo is not found, the report
says so plainly and delivers the best-available analysis from whatever docs exist, marked as
such.
