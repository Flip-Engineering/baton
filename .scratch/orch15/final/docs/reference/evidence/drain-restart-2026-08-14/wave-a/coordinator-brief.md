# COORDINATOR BRIEF — drain-restart-2026-08-14 wave-a (#204)

One implementation row: the first-class drain-restart verb (option (a) of the issue — no
module hot-reload). You verify the row's deliverable and write verify-notes.md carrying
DRAIN-RESTART-VERIFY v1. Acceptance: the row's red-first pin green at HEAD; deployment
batteries unchanged. Issue #204 is the contract. Cross-refs: #184 (trap class), #173
(detached launches — the drain waits on them), #59 (a drained wave resumes cleanly),
#168 (deployment snapshot family).
