# IMPL BRIEF — row-cli: #157 CLI wave fidelity + the CLI admission half of #158

You are the CLI-seam impl row of the honesty-package wave. **Your acceptance rows:**
`impl/test/cli-wave-fidelity-red.test.mjs` ALL green at named stages, and the
`scratchpad-write-red.test.mjs` rows whose stage names name the CLI parser/admission
(A10-1's CLI leg and its siblings) green. Never edit suites. PINs stay green; adjacents
unmoved.

**Read first:** `docs/reference/evidence/cli-wave-fidelity-2026-08-13/contract-fold.md` (v1.1 —
your authority), then `impl/test/cli-wave-fidelity-red.test.mjs` (your rows), then
`docs/reference/evidence/scratchpad-write-2026-08-13/contract-fold.md` §D4 (the CLI-admission
half of the append verb — the parser must SERVE `run.scratchpad.append`; the kernel half is
row-kernel's, not yours).

**You OWN:** `impl/src/application-cli.mjs` · `impl/src/application.mjs` (the interpreter-wave
registry projection hydration — the phase/progressClass/attentionCount string-branch fix at
the seam the suite names). Do not touch other files (the wave's partition law — another row
owns the store/semantics/northbounds/scripts).

**Craft laws:** no clocks · localeCompare banned · sorted-key literals ACTUAL order · byte
literals only in limits.mjs · additive-only on closed vocabularies · NUL discipline:
application.mjs + coordination-store.mjs are NUL-bearing (`grep -an`/`sed -n` only; don't
disturb NUL bytes) · generated docs regenerate via the shipped generator, never hand-edit ·
work only in your worktree · your `[attempt: <salt> <role>]` line VERBATIM in your notes'
first five lines.

**Verify before you finish:** `node --test impl/test/cli-wave-fidelity-red.test.mjs` green ·
`impl/test/workflow-dsl-red.test.mjs` 35/35 · `impl/test/workflow-as-data-red.test.mjs` 30/30
· `impl/test/wave-observability-red.test.mjs` 30/30 · `impl/test/control-surface-truth-red.test.mjs`
7/7. Write `docs/reference/evidence/honesty-package-2026-08-14/notes-row-cli.md`: decisions,
splits, anything not green and why.
