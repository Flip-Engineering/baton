# ROW BRIEF — row-seeds: the three contract seeds + the law-farm spec (#150 #151 #152 #184)

Four contract seeds to draft, each in the Ring-2 form (ground truths → decisions → closed
refusal vocabulary → red-first acceptance pins → open questions), each citing its origin:

1. **#150 coaching-payload passthrough on both northbounds** — the contract for carrying
   coaching payloads through web + MCP unchanged (closed shapes, refusals on mutation).
2. **#151 spill query kind run-horizon authorization** — the spill query kind's auth contract
   (which principals, which horizons, the refusal taxonomy).
3. **#152 workflow-surface docs disclosure + evidenceRef schema** — what the workflow surface
   discloses to whom, and the evidenceRef schema as data.
4. **#184 the law-list bug farm** — the campaign's operating laws that are currently
   agent-discipline (briefs ≤4096−60B, re-drive key+path bumps, watcher-per-launch, the
   file-partition discipline…). For EACH: the machinery that would make the trap impossible
   (admission-time validation, automatic namespacing, …), and the priority order. Ground in
   `gh issue view 184` if available, else the campaign's commit messages and the evidence dirs.

**Your file partition:** `docs/reference/evidence/contract-seeds-2026-08-14/**` ONLY.
Read-and-run only outside it. One file per seed: `seed-150.md`, `seed-151.md`, `seed-152.md`,
`seed-184.md` — each with your `[attempt: <salt> row-seeds]` line verbatim in its first five
lines. Judgment calls recorded; authority-class ambiguity → DECISION_REQUEST with options.
