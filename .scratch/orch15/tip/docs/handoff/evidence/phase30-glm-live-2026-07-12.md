# Phase 30 credentialed GLM live evidence — 2026-07-12

## Outcome

The previously pending GLM boundary is live-proven through Baton's real public driver. A clean
disposable clone ran `GlmSessionCli` through Coordinator with exact model `glm-4.7` and native
effort `low`. Requested and resolved identity matched, the Claude Code/Z.ai wire independently
reported `glm-4.7`, a real PID and provider usage were observed, a generated review artifact passed
fresh-worktree verification, normal kill was confirmed, and process, worktree, metadata, private
runtime scope, and task branch were all gone.

## Credential and routing boundary

- The ignored local credential stayed owner-only and was never copied into source, worker
  worktree, evidence, or Git.
- The loader accepts a raw one-line token or the documented
  `/env/ANTHROPIC_AUTH_TOKEN` JSON shape by default. A deployment may explicitly select another
  bounded JSON Pointer; Baton never scans guessed `key`/`token` names.
- Errors and evidence omit the token. The successful committed event stream is a whitelist of
  lifecycle, route, usage, verification, and stop fields.
- The card identifies `claude-code-2.1.206+zai-anthropic` as harness transport and reports
  `authPosture: api_key`; the model remains an independent exact route dimension.
- The live route is requested/resolved `glm-4.7`, observed `glm-4.7`, requested/resolved effort
  `low`, and honestly nullable observed effort because the wire does not report it.

## Validation

- Focused GLM/model/runtime tests passed 26/26.
- Canonical `npm test` passed 795/795 before the live run.
- Live summary checks passed 11/11.
- Provider usage was 37,000 reported tokens and $0.254129.
- Fresh verification emitted `verify.reverified` with `accept:true`.
- Normal kill emitted `kill.confirmed`; PID, worktree, metadata, runtime scope, and branch cleanup
  all passed.

Sanitized executable evidence is in
`docs/reference/evidence/phase30-glm-live-2026-07-12/`.

## Dogfooding findings

The failed attempts are retained in `attempts.md`: public-entrypoint dependency availability,
dirty symlink projection, explicit local credential schema, pre-init failure classification, and a
too-low initial provider ceiling. None is rewritten as success. The accepted run uses copied
ignored dependencies in the clean clone and a $0.40 ceiling at native low effort.

## Honest boundary

This proves one credentialed GLM session, exact model/effort routing, fresh verification, and reap.
It does not prove concurrent GLM seats, automatic quota discovery, OpenCode-as-GLM parity, or a
general Claude/Z.ai network-denial policy. The full-system goal remains active, with Phase 29 ACI
contract drift and the Cairn sealed-run scorecard next. No homelab integration was added.
