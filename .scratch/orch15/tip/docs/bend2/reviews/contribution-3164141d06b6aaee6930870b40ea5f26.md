# Review: contribution-3164141d06b6aaee6930870b40ea5f26

| | |
|---|---|
| Author | bend2-arch-lead |
| Captured at | `f21a70d318c61fe96c4b43390b2d5b0ce4b9c552` on `baton/ws-34158f3ac9fbb1ffecf2af728fd753ff`; carries `docs/bend2/target-architecture.md` only |
| Content | the mandate part-2 target architecture (BATON2 ownership model) |
| Decision | accept |
| Review row | `swarm.contribution_reviewed` seq see below, 2026-09-21 |
| Reviewer | bend2-reviewer, independent seat |

## What was run and what it answered

Requested properties, verified mechanically against the file:

- **Eight subsystem ownership and prohibition contracts.** Eight numbered sections — Domain
  Kernel, Journal and Projectors, Scheduler, Worker Gateway, Workspace and Artifacts,
  Verification and Landing, Northbound Gateway, Capability Services — each with one **Owns**
  and one **Forbidden from owning** list (8 of each counted).
- **All 28 unordered pair synchronization seams.** The pair table has exactly 28 rows, and each
  of the eight subsystem names appears in exactly 7 pairs — the complete-graph signature, so no
  pair is missing and none is duplicated. 16 pairs carry concrete typed seams
  (`AppendTransaction`, `SubscriptionCursor<ViewDelta>`, affine `WorkerSession`,
  `ResourceLease`/`WorkspaceLease`, `VerificationRequest`, `CapabilityCall ->
  CapabilityResult`, artifact and evidence references); the other 12 name `None` plus the
  intermediary that owns the exchange. The Kernel pairs are runtime-`None` with compile-time
  dependencies, consistent with the mermaid diagram's dotted edges.
- **All-Bend2 / no-Node end state.** Explicit in the design constraints: "BATON2 is written
  entirely in Bend2 and runs on HVM. Its final deployment contains no JavaScript, Node runtime,
  or JavaScript host for processes, sockets, files, Git, or transports", with a missing host
  effect classed as a rewrite prerequisite or go/no-go reason. Enforced downstream by the
  disposition rows (Node timer loops, hand-written WebSocket server, `v8.serialize` repair,
  `/bin/ps` lease probe, yield choreography deleted) and the migration-deletions checklist.
- **Explicit disposition of current subsystems.** A thirteen-row table maps every current
  subsystem and layer named by `architecture-review.md` — facade partials, both store families,
  application and swarm classes, plan/scheduling engines, adapters and provider supervisors,
  authority emulation, capacity protocols, parallel runtimes, seam machinery, ESM and Node
  machinery, optional capabilities — to a target owner or deletion, cross-referenced to the
  review and both findings files.

Consistency with the verified corpus: the ownership and prohibition lists absorb the
architecture review's findings (F1-F24) and cite the same pinned test anchors
(`wave-driver-red.test.mjs`, `acp-json-rpc-process.test.mjs`, `shared-workspace-custody.test.mjs`,
`docs/41-verification-recovery-review.md`), all previously verified by this seat; the #541
worker-admission law is carried as an explicit Scheduler policy.

## Decision

accept — the target architecture defines eight complete ownership/prohibition contracts, a
complete and duplicate-free 28-pair synchronization matrix, an explicit all-Bend2 end state,
and an explicit disposition for every current subsystem.
