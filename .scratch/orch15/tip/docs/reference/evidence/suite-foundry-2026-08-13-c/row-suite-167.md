# ROW BRIEF — row-suite-167: the red-first suite for the folded #167 readiness-honesty contract

Read `foundry-brief.md` first (the suite law binds you). Your source of truth:
`docs/reference/evidence/contract-foundry-2026-08-13/contract-167.md` (FOLDED — readiness
honesty: the bounded actual-inference probe tier, the `probe-verified` verdict vocabulary (the
`alive` overclaim is renamed), the `{static, probedAt, verdict}` projection with the staleness
law, the refuse-or-inform-never-reroute admission, the OPERATOR doctor path pinned end-to-end
(`impl/scripts/baton.mjs:79-98` → `BatonWebClient.doctor()` → web-northbound forced-probe
parameter), the enumerable `verdict`/`probedAt` wire law (a JSON read must carry them)). Also
read `redteam-167.md` (the wire-invisibility attack — non-enumerable fields vanish under JSON)
and `fold-167.md`.

Idioms to mirror: `impl/test/prescriptive-doctor-red.test.mjs` (doctor/readiness fixtures) —
rows assert the wire shape (serialized JSON carries the fields), the probe gating, and the
classification separation; provider probes are faked at the adapter seam, never real network.

Deliverables (edit ONLY these): `impl/test/readiness-honesty-red.test.mjs` ·
`docs/reference/evidence/contract-foundry-2026-08-13/suite-notes-167.md` (row inventory +
stage table + both measured splits + judgment calls).
