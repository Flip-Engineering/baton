# #66 FOLD BRIEF — fold the red-team report into the doubt-review contract (v1.0 → v1.1)

You are folding an adversarial red-team report into the doubt-review contract. Read fully, in
order: (1) `contract-redteam.md` (NOT FOLD-READY — 7 numbered blockers in §E, each with its
concrete fix); (2) `doubt-review-contract.md` (v1.0 — your edit target).

## The blockers, headlined (fold ALL 7 + apply the open-question verdicts)

Per the report's §E list — the shape decision (composition of (b) + durable doubt record)
survives; the fold is in the seams: the elevation/lifecycle seams, the queryable-surface
authority boundary, the promote_doubt resolution rules (double-resolution races, worker
self-resolution receipts, forged-resolution posture), the settle composition's error path, and
the answer-push delivery discipline. Plus every citation fix (automatic class).

## Laws + deliverables

No clocks; every citation verified (`grep -an`/`sed -n` on the two NUL files); sorted-key
literals ACTUAL order; `localeCompare` banned. Header to **v1.1** with the fold note. Edit ONLY:
`doubt-review-contract.md` (v1.1) + `contract-fold.md` (blocker → change map, all 7 + open
questions) — this directory.
