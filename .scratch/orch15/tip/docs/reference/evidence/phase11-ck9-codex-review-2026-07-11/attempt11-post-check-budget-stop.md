# Attempt 11 — repaired review passed its check but exceeded the terminal budget

The exact `gpt-5.4` low-reasoning worker wrote the repaired-head review and its pinned artifact
verification passed. Cumulative usage then crossed the 300,000-token ceiling at 315,732 before a
terminal frame arrived, so Baton correctly rejected the unverified task result and fully reaped
the process, runtime, worktree, and branch.

Rejected file-edit telemetry raised broader integration-authority and refinement-cleanup leads.
Direct source tracing separated already-proved replay assertions from real gaps. The real gaps are
now covered by atomic integration decision/driver/artifact authority, strict integration replay
verification, post-merge authority-failure and asymmetric-stream tests, and runtime-scope cleanup
assertions for follow-up/recovery refinement faults. The final rerun uses an earlier synthesis
trigger and a larger measured terminal tail; all trust and reap gates remain unchanged.
