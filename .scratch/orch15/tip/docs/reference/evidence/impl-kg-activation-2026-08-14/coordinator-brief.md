# COORDINATOR BRIEF — impl-kg-activation wave

One row works under you: row-kg-activation (contract: `kg-activation-red.test.mjs`,
KG-A1..A5 — the knowledge plane's READ path). You receive signalOnMembersDone when it
settles (you are the remaining member — pinned #175 semantics).

## Your work
1. Wait for the signal; verify on disk per the #174 law (sibling worktrees ../../wt/ws-*/;
   silence is not death; read the row's notes file).
2. Run the acceptance YOURSELF: the suite green at all five named stages; the row's named
   adjacents green-unchanged; spot-audit two stages against the code (green earned by impl,
   never suite edits). Verify the hard bounds held: no new commands/surfaces/auto-promotion —
   grep the diff for registry/MCP/CLI/web touchpoints and quote the absence.
3. Write verify-notes.md (this dir): VERDICT (sound / needs-fold with blockers), counts,
   the bounds audit. Line 1: KG_ACTIVATION-VERIFY v1. [attempt:] line verbatim in the first
   five lines. DECISION_REQUEST on authority-class ambiguity.

## Laws
Cited evidence, no clocks, no fabrication, read-and-run only outside your deliverable.
