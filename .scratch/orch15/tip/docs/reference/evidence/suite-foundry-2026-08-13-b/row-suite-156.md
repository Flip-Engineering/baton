# ROW BRIEF — row-suite-156: the red-first suite for the folded #156 contract (v1.1)

Read `foundry-brief.md` first (the suite law binds you — red-first, named stages, hermetic,
split-twice, the attempt-echo law in the first five lines). Your source of truth:
`docs/reference/evidence/mcp-profile-parity-2026-08-13/mcp-profile-parity-contract.md` (v1.1
FOLDED — the MCP default profile as a bus superset: the `uncoveredCommands()` derivation with
the construction-order law (gap snapshot BEFORE the sibling spread; the 14 hand-rows
forbidden), the renderer fallback to `alias.canonical`, the RG pins, the corrected citation
anchors). Also read `redteam-156.md` (the attack surface) and `fold-156.md` (what the fold
changed, incl. the judgment call between fix 1 and fix 3 — your rows must pin what the
contract ACTUALLY says now).

Idioms to mirror: `impl/test/control-surface-truth-red.test.mjs` (conformance/derivation
style) — this suite is mostly registry/projection conformance: derivation-not-literal pins,
coverage set-difference pins, and the fallback render rows.

Deliverables (edit ONLY these): `impl/test/mcp-profile-parity-red.test.mjs` ·
`docs/reference/evidence/mcp-profile-parity-2026-08-13/suite-draft-notes.md` (row inventory +
stage table + both measured splits + judgment calls).
