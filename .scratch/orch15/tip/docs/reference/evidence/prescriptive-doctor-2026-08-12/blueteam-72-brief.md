# #72 BLUE-TEAM BRIEF — attack the prescriptive-doctor red-first suite

You are the **blue team** for the prescriptive-doctor suite. Target: NOT the contract — the
SUITE's red-keeping power. Read fully, in order: (1) `prescriptive-doctor-contract.md` (v1.1);
(2) `contract-fold.md` (the 9 detection-honesty resolutions); (3)
`impl/test/prescriptive-doctor-red.test.mjs` (16 tests: 3 green PINs, 13 red at named stages);
(4) `suite-draft-notes.md`.

## Attack axes (per row)

- **Green-side blockers FIRST** — can every red row go green under a CORRECT v1.1
  implementation? Fixtures that can't plant the needed condition (a ghost-worktree census, a
  stale lease, a TTL window) or oracles contradicting the fold.
- **Shallow-greenability** — sharpened for this lane: could the precision-law rows pass with a
  detection that fires on BOTH the planted condition and the healthy state (a noisy warning
  greening the row)? Could the never-block rows pass with a warning that DOES block a command?
  Could the metadata-only row pass with a fixture containing NO token-shaped content (a fixture
  without adversarial content greenwashes the law — the fixture must include planted
  token-shaped values)? Could the action-link rows pass with a remediation that names a verb
  that doesn't exist?
- **Missing-row gaps** — every v1.1 warning in the catalog with no row; the CLI/MCP parity row;
  the briefing-field composition row (byte-stable for non-reading consumers).
- **Stage honesty + hermeticity** — named stages at HEAD; planted fixtures only (no real
  credential reads, no real census of the host); no order-dependence.

## Output + laws

`docs/reference/evidence/prescriptive-doctor-2026-08-12/suite-blueteam.md`: BLUE-CLEAN or
NEEDS-FOLD with numbered findings (row/gap + attack + concrete fix). Edit ONLY that file. No
clocks; citations verified (`grep -an`/`sed -n` on the two NUL files); run the suite twice from
the repo root and record both splits.
