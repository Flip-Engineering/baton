{
  "name": "baton",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fleet driver — one orchestrator directs cross-vendor coding-tool workers through AI/AX and an authenticated web command surface, with reliable control and a hard-to-fool trust gate. Runnable ESM JS, zero dependencies.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "demo": "node demo.mjs"
  }
}
