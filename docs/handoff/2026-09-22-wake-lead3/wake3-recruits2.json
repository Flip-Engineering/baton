[{"participantId":"sse-attach2","objective":"Fix issue #320: the ordinary/embedded host's SSE wake attach fails cross-process. The failing row — impl/test/swarm-wake.test.mjs :: a real resident wakes a real `baton swarm watch --follow` child on guidance, contribution, and close — is pinned expected-red (reason #320) in impl/scripts/expected-red-tests.json. The resident is hosted through the embedded API (openBaton({repo, advanced}).host(), application-deployment.mjs #startOrdinaryHost) and the first long-lived GET /v1/wakes from a genuinely separate `baton swarm watch --follow` child is closed by the server before any response is written (WAKE_DEBUG outcome={\"status\":\"error\"} cause=Error: aborted). Reproduce from impl/ with: node scripts/run-suite.mjs test/swarm-wake.test.mjs. Fix the root cause in src/ — the issue names WebNorthbound._handleWakes and the owner-socket auth path as suspects (a check a same-process caller satisfies implicitly). Keep the documented `baton serve` path working. When the row passes, remove its expected-red entry from impl/scripts/expected-red-tests.json and re-run the runner over test/swarm-wake.test.mjs plus the wake/bridge files the fix touches. Do not run formatters, lints, or the whole suite. Publish a contribution naming your commit, the files, and the runner verdict. Notes: this host's unauthenticated GitHub API limit is exhausted (403 until about 2026-09-22T21:16Z) — issue titles are in /tmp/baton-bend2-laws-review/assignments-wake-lead3.md and the #320 body can be read with curl after the reset; the codex routes are out of credits, so stay on the route you were recruited with.","options":{"exact":{"harness":"omp","model":"deepseek/deepseek-flash","effort":"max"}},"permissions":["read","communicate","contribute"],"mode":"change","workId":"issue-320"},
 {"participantId":"attention-ledger2","objective":"Own the attention plane's three dead surfaces, in this order. #255: attention events never reach the coordination ledger — interaction.requested lands only in worker logs (the #243 last mile, measured dead); a recorded attention row is the precondition for the rest. #249: run.attention.list is advertised with applicationCommand but has no dispatch site on any surface (a GHOST verb). #108: attention.watch is silently dead for the documented MCP principal — it answers an empty page and rewinds the cursor, never an error. Start from the repository: locate each surface and its pins, reproduce, fix with a red-first row that fails before the change and passes after, and remove any expected-red pin the fix retires. Verify with the deployment runner over the files you touch (node scripts/run-suite.mjs <files> from impl/); do not run formatters, lints, or the whole suite. Publish one contribution per issue naming the commit, files, and runner verdict; if an issue is already fixed on the served base, say so with the citation instead of re-implementing it. Notes: this host's unauthenticated GitHub API limit is exhausted (403 until about 2026-09-22T21:16Z) — well-formed issue URLs are in /tmp/baton-bend2-laws-review/assignments-wake-lead3.md and the bodies can be read with curl after the reset; the codex routes are out of credits.","options":{"exact":{"harness":"omp","model":"zai/glm-5.3-flash","effort":"max"}},"permissions":["read","communicate","contribute"],"mode":"change","workId":"attention-ledger"}]
