{"participantId":"failure-stall-188","objective":"Implement the accepted design docs/55-failure-stall.md, red-first, in the order the document lays out. Read docs/55 in full first - it is the contract and it names every seam. Then: (1) the fact - the failure-stall count is the number of CONSECUTIVE failure verdicts of a run in ledger order, folded in the same derivation that already carries progressClass and the terminal cause (impl/src/application-observation.mjs:518), a success verdict (completed) resetting it, never a clock; (2) the park - one durable run.parked {reason: 'failure_stall', count, threshold, atSeq} row at the deployment-declared threshold, stopping NEW spawns (the spawn gate is impl/src/runtime-admission.mjs), never stopping, killing or interrupting a live member and never writing a terminal phase, clearing by the orchestrator's answer or a later success verdict as run.unparked; (3) the attention item - exactly ONE failure_stall row per park carrying {runId, count, threshold, verdicts:[{memberId, attemptId, disposition, atSeq, certificate}]} with each attempt's death certificate (impl/src/holistic-runtime.mjs:393, impl/src/production-convergence-state.mjs:193) - the attention kind joins the closed CANONICAL_ATTENTION_KINDS set (impl/src/application-semantics.mjs:33-36) as its ninth value; (4) the threshold - operator policy stallPolicy.failureCount in impl/src/limits.mjs, unset means the park never fires; (5) the wake reason failure_stall joins the closed WAKE_REASONS set AFTER issue #71's set is on the tree (impl/test/orchestrator-wake-red.test.mjs:267 pins it) - if #71's rows are still red on your base, land items 1-4 and leave the wake-reason row for the lane that follows, saying so. Write the seven pins FS-1..FS-7 from docs/55 section 7 as red-first rows (a new test file is right, e.g. impl/test/issue188-failure-stall.test.mjs) and pin them in impl/scripts/expected-red-tests.json with reason #188 until they pass, removing each pin as its row goes green. Regenerate the generated vocabulary with node impl/scripts/surface-gate.mjs --write in the same change that adds the attention kind and the run view's failureStall field. Verify with the deployment runner over the files you touch (node scripts/run-suite.mjs <files> from impl/); do not run formatters, lints, or the whole suite. Publish a contribution naming each pin's state, the commit, and the runner verdict. Notes: codex routes are out of credits; the store has been refusing writes (ENOSPC) so publish as soon as it answers; the issue title and URL are in /tmp/baton-bend2-laws-review/assignments-wake-lead3.md.","options":{"exact":{"harness":"omp","model":"deepseek/deepseek-flash","effort":"max"}},"permissions":["read","communicate","contribute"],"mode":"change","workId":"issue-188"}
