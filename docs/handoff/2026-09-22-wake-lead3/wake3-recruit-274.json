{"participantId":"visibility-274","objective":"Issue #274 is already designed: docs/46-swarm-visibility.md is the accepted spec, and three of its sections are unimplemented in the served tree - implement them, red-first, with pins. Section 5 (root as a participant row): every view's participants array carries ONE synthesized row {participantId:'root', role:'root', status:'active', permissions: the full SWARM_PERMISSIONS set, runtime:{workerId:null, state:'root', live:true, turn:null}}, derived per view, never folded; 'root' joins the reserved names so swarm.recruit with participantId 'root' refuses; durable rows keep reviewerId:null and the VIEW renders attribution as reviewerId/actor 'root'; the root row carries no runId/workspace/activity/usage; it appears in every scope. Section 6 (attention coverage): attention becomes attention:{rows:[...], coverage:{examined:[...], unexamined:[{participantId, checks, reason}]}} on every projection that carries it, rows byte-identical to today's array; coverage.examined names every participant the derivation evaluated this read, coverage.unexamined names seats a check skipped because a fact was unreadable (today worktreeChangedPaths returning null silently skips checks - name the seat instead, reason 'worktree_unreadable'); an empty rows with empty unexamined reads 'looked, nothing found', with non-empty unexamined reads 'not fully looked'; scoped views scope coverage as they scope rows. docs/46 calls section 6 'the one intentional SHAPE change' and its section 9 carries the migration - read section 9 before changing the shape. Section 4 (brief exposure by relationship): one derivation _peerExposure(swarm, seat, peer) consumed by _composeRecruitBrief, with the closed relationship classes self/subtree/checkout/group/swarm/repository and the per-class exposure table in section 4.2; a peer a class would list but _canAct rejects is never listed as able. Every closed set this adds must be regenerated into docs/36 section 7.4 with node impl/scripts/surface-gate.mjs --write in the same change. Work red-first: extend or add the pin rows that fail before the change and pass after; existing pins live in test/issue268-visibility-red.test.mjs, test/issue433-contributions-projection.test.mjs and test/issue286-attention-index.test.mjs - read them first, they may already state part of the contract. Verify with the deployment runner over the files you touch (node scripts/run-suite.mjs <files> from impl/); do not run formatters, lints, or the whole suite. Publish a contribution per section or one contribution naming each item's status, with the commit, the files, and the runner verdict. Notes: the codex routes are out of credits and this host's unauthenticated GitHub API limit resets hourly, so work from docs/46 in the tree; the issue title and URL are in /tmp/baton-bend2-laws-review/assignments-wake-lead3.md.","options":{"exact":{"harness":"omp","model":"zai/glm-5.3-flash","effort":"max"}},"permissions":["read","communicate","contribute"],"mode":"change","workId":"issue-274"}
