## High — Successful retry drops the predecessor's settlement evidence

`run.mjs:200-219` correctly captures the failed generation-1 outline and `mapped.evidence()` in `progress`, including the data needed to explain why generation 2 ran. On success, however, `run.mjs:247-262` replaces `record` with a summary that retains only `predecessorCallId`, `generation`, final state, and output count; the final write at `run.mjs:284-285` serializes `{ record, lifecycle, cleanup }`, not `progress`. Consequently a successful retry permanently loses the predecessor's terminal result and exact failed unit/node/task/route/capsule evidence, so replay or recovery cannot prove that the retry was selective or that unaffected provider work was not re-executed. Carry the predecessor outline/evidence and retried-unit lineage into the successful record (or persist `progress` beside it).
