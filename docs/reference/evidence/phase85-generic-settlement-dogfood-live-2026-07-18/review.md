# Adversarial review — context unit `12fa32b4342eac9972b28691b41fb1d125def377ae2eb79c8c55c9f7013fec04`

- **High — Generic retry settlement silently aliases current and inherited children for one unit.** In `_contextSettlementChildren`, current-generation rows are accumulated before inherited rows, then `new Map(rows.map(row => [row.unitId, row]))` is validated only with `byUnit.size === call.units.length`. If a logical unit has both a current child and an inherited binding while all units are covered, the check passes and the later inherited row silently replaces the current row. The returned settlement consequently loses the current child’s route/result and task/resource-release identity. `_normalizeContextCleanupReceipt` then excludes the surviving inherited row from generic cleanup targets, so the displaced current task can also escape cleanup accounting. Reject any duplicate `unitId` across current and inherited rows (and require exactly one row per `call.units` entry) before constructing the result.
