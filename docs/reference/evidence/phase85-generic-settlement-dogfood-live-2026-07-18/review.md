# Phase 85 generic-settlement verdict

The live run `run-5b02e8f27dc7ca05dcc1eaaf6ee35761` proves the intended generic-settlement path. Generation 1 (`context-call:d929ec78382fcd9c3f597f44d5ef70f3eebed89b724cba4326aa797e7a1a2e59`) executed five exact `codex/gpt-5.6-sol/xhigh` units, deliberately cancelled only unit `ad62d0dc…`, accepted the other four, and settled failed with exact zero-remaining cleanup. Generation 2 (`context-call:a049b431cd5647db13409faba463cf5a2f25c7cd1eb247d9d1244f10b036fc0d`) then records `executionUnitCount: 1`, `inheritedUnitCount: 4`, and `providerEffects: 1`, while returning all five ordered capsule/result references; this is affirmative replay-without-provider-re-execution proof, not merely a completed outline. Reduce call `context-call:7383b920101326a9fb10dfd1460373d48609801895a23edadb8101e9e0a5f45c` completed with one result. Final stop covered all ten observed processes and all three calls, left zero processes/sessions/calls/workers, closed dispatch, interactions, and Run authority, and `baton.close()` reported `closed: true`.

One adversarial finding is a real route-truth defect: `_settleContextCall` chooses `codePrefix` from the looked-up call before handling an absent call or a typed-wrapper kind mismatch. Thus `settleContextEffectCall` can report `context_map_call_not_found` for an unknown ID, and can also namespace a known map/effect mismatch by the stored kind instead of the requested route. Settlement, result, and cleanup identities remain protected, but the public refusal misstates the exact route; the prefix must come from `expectedKind` for typed wrappers.

The other four hypotheses do not survive the complete contracts. A current and inherited retry row cannot alias: call normalization requires unique inherited and retry unit IDs, disjoint sets, canonical order, and exact coverage, and derives `executionUnitIds` only from the retry set. A failed provider child is intentionally projected as retry-eligible under the existing provider-failure policy; its exact cause remains on the child, while aggregate `context_child_failed/retryable: true` authorizes a separately approved selective generation and does not re-enter accepted siblings. Completed legacy map evidence cannot carry a contradictory `state`: exact-key validation excludes `state` from the completed schema-v2/v3 field set, so such an artifact fails integrity; accepting schema v2 only during replay is deliberate map compatibility, whereas generic effect evidence is natively schema v4 and fail-closed. Finally, the claimed dogfood assertion gap is disproved for this record by the generation counts, inherited-child/result derivations, capsule bindings, and cleanup receipts above, although retaining explicit regression assertions would make that proof easier to audit.

A separate interrupted attempt, `run-a1abbe5e9efc4d9952498a89efe3612c`, must not be conflated with this successful run. Its generic reduce call `context-call:8136f414067bacdf70a84d3db05e48705c087cc769e977fe2b4787606ffc31b6` was admitted at sequence 116 before stop admission/completion at 117/118, but the stored stop target contained only map call `context-call:63cfe1aaced2835bfea5f55bbdc3aad5b73d6c338a52ef8792866bcc909dfa89`. That stale, cryptographically bound target snapshot is a confirmed `run_stop_integrity` defect in stop capture. Strict replay correctly refused it—intentional Phase 85 closure—and cleanup still closed all seven observed processes, independently left zero live PIDs, preserved the append-only ledger, and removed the quarantined deployment.

Technical verdict: generic map/reduce result lineage, selective retry, provider-result inheritance, and physical cleanup are verified for the live run. Phase 85 remains fail-closed as designed. The actionable defects are the effect-wrapper error namespace and the independently proven stale stop-target capture; neither should be recast as a result-lineage or cleanup failure in the successful run.
