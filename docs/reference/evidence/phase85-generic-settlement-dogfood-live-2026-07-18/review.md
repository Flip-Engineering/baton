## Finding: selective retry is accepted without unit-lineage or replay proof

After deliberately stopping `settlement-adversary:0001`, the harness retries only when the first map has call-level state `failed`, then accepts generation 2 solely because `completedMap.item.state === 'completed'`. Although it records each generation's outline and evidence, it never asserts that the retry retains the same source units, node/task/route/capsule identities, replays the successful generation-1 siblings without provider execution, and reruns only the cancelled unit. Consequently, an implementation that re-executes the entire map or changes a unit's route/result lineage can pass this dogfood and be reduced as verified evidence. Compare per-unit settlement and provider-execution identities across the two generations, and reject any generation-2 work beyond the failed unit or any drift in inherited lineage/resource-release bindings.
