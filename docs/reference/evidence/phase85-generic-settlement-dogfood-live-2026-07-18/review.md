# Phase 85 generic-settlement verdict

The evidence describes two distinct attempts, not contradictory outcomes. In the preserved interrupted run `run-a1abbe5e9efc4d9952498a89efe3612c`, generic reduce call `context-call:8136f414067bacdf70a84d3db05e48705c087cc769e977fe2b4787606ffc31b6` was admitted at sequence 116 before stop admission/completion at 117/118, but the stored stop target contained only the completed map call and omitted that reduce call. This is a real `run_stop_integrity` defect in the stop writer or its creation-time projection: it bound a stale target snapshot. Strict replay's refusal to accept that incomplete cryptographically bound snapshot is intentional Phase 85 fail-closed behavior, not the defect. Cleanup for that attempt is proven: all seven observed receipt processes were closed, independent observation found zero live PIDs, the append-only ledger was not rewritten, and the quarantined disposable deployment was removed.

The current deployment `/var/folders/p5/v13h2kf1227ffch19r_83kkr0000gn/T/baton-phase85-generic-settlement-zTWUKK` never produced a run or Context result. Exact-route admission failed with `application_profile_route_unavailable` because profile `default` contained an unavailable route; consequently `runId`, progress, lifecycle, stop, and close are all null. That fail-fast route rejection does not establish another generic-settlement defect, but neither does it verify map/reduce settlement, provider-failure terminality, replay without provider execution, or resource-release binding. Its surviving recovery descriptor also has a null `runId`, so it is only residue for a pre-run deployment and cannot prove cleanup. The immutable dispatch names call `context-call:7383b920101326a9fb10dfd1460373d48609801895a23edadb8101e9e0a5f45c` and unit `context-unit:b056893c0b2c7e630e90c1b6477a1ffd45a7992a3658dec5b76cafd7095f17fc`, but supplies no serialized finding value; no additional finding can be truthfully attributed to them.

Technical verdict: Phase 85's strict replay closure behaved correctly, while interrupted-stop target capture has a verified stale-snapshot defect. The earlier run's descendant cleanup is exact and complete; the current dogfood attempt is inconclusive and has no cleanup proof, so it must not be represented as a successful generic-settlement verification.
