# ROW BRIEF — row-dsh-redteam: the scope-creep red-team (the rejection rubric, dsh edition)

Read `foundry-brief.md` first (the shared laws bind you). You are the wave's conscience. The
three comparison rows will produce proposals; your job is to make rejection CHEAP and
CONFIDENT — and dsh's shine makes this wave's creep risk the highest of any comparison wave.

## Your work

1. **The rubric** — the standing vetoes operationalized for dsh-shaped proposals, plus the
   dsh-specific traps: (a) **the single-agent trap** — dsh's primitives serve ONE live agent;
   baton's serve a swarm with fenced worktrees and content-addressed results. A proposal that
   only makes sense for one agent (per-agent tool scoping for a human's pet session, chat-UI
   chrome, session titles) is OUT-OF-SCOPE unless its swarm reading is named. (b) **the
   plugin-everything trap** — "make baton a plugin system" is a rewrite, not a rung; only a
   proposal naming a specific Cordis-shaped mechanism with a specific baton pain survives.
   (c) **the framework-envy trap** — dsh's elegance is boot-composition elegance; baton's
   elegance is evidence/pin/harvest honesty. Proposals trading the second for the first:
   reject with the reason named. (d) the standard vetoes (clocks, lying surfaces, closed-
   vocabulary mutation, per-worker heaviness, methodology bypass, imagined-vs-observed cost).
2. **The trap list, pre-computed** — read the digest yourself and name the specific dsh
   mechanisms that are traps for baton: the web UI as a center of gravity (baton's surfaces
   are machine-first; a UI-first proposal is a rejection with the #115/#133/docs-38 framing
   as the honest home for visual work); profiles/bundles as config sprawl (baton's deployment
   files are already the thin version); event-mode proliferation (four dispatch modes vs
   baton's write-mostly log — where does each mode actually pay?); the session-fork romance
   (forking live model state vs baton's content-addressed re-drive #59 — which is honest
   about what carries over?).
3. **The pre-registered rejections** — the proposals you EXPECT the rows to make that you
   would reject, pre-written. If any row reports exist on disk by the time you finish
   (`../../wt/ws-*/docs/reference/evidence/dsh-comparison-2026-08-13/` per the #174 law),
   apply the rubric to them directly; otherwise the pre-registered list stands.

Deliverable: `docs/reference/evidence/dsh-comparison-2026-08-13/dsh-redteam.md` — the rubric,
the trap list (cited), the pre-registered rejections. Attempt line verbatim in the first five
lines.
