# DSH-COMPARISON COORDINATOR BRIEF — merge + apply the rubric (v4-pro seat)

Read `foundry-brief.md` first (the shared laws bind you). Three comparison rows (arch,
lifecycle, seams) + one scope-creep red-team row are writing in parallel. You are the
remaining member of the `signalOnMembersDone` (the spec watches the four rows — pinned #175
semantics).

## Your work

1. **Wait for the signal, then verify on disk** (the #174 law: sibling worktrees at
   `../../wt/ws-*/`, or the main repo post-harvest — silence is not death; a missing attempt
   marker is not a dead row).
2. Read the four reports (shared partition if reachable — record the refusal if not; durable
   files as truth).
3. **Merge** the three comparison reports' candidates into ONE adoption table (candidate →
   verdict → baton landing zone → sizing class).
4. **Apply the red-team row's rubric** to every ADOPT/ADAPT: each gets SURVIVES or
   REJECTED-BY-RUBRIC (the question it fails) — disagreements recorded as judgment calls.
5. Write `dsh-qa.md` (this dir): the merged table, the rubric application, the final
   prioritized adoption list, and the honest "what dsh does better than baton" paragraph —
   plain, no defensiveness (the expected headline candidates: model-visible-means-logged as an
   asserted invariant; inject-into-next-request; the seam triple discipline; subagent-provider
   breadth — verify or overturn each against the row evidence). Line 1 must be exactly
   `DSH-QA v1`. Attempt line verbatim in the first five lines. Publish to `shared` — or record
   the refusal.
6. Escalate authority-class questions UP via DECISION_REQUEST.

## Laws

Cited evidence (both sides), no clocks, no fabrication, read-only outside your deliverable.
Your file is the harvest artifact.
