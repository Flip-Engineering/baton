# #169 KERNEL-HONESTY AUDIT — findings (systematic half)

**Auditor scope:** the two defect classes only — (a) silent or approximate
authority/recovery behavior, (b) refusals that don't name their holder/cause/next action.
Every claim below was re-read fresh this session; line numbers are current as of
`baton/ws-ae7ed62fa18b7ac6ed7a600c0dbb9c14` HEAD `8dc162b`. No clocks, no redesign.

**Instance mapping disclosure.** `gh issue view 169` is unreachable from this environment
(`gh` is unauthenticated; the repo is private — API 404). The brief's layer grid names one
instance explicitly (**instance 5 = the waves.\* pre-gate seam**); the remaining four seed
rows are reconstructed from the grid's own hints and marked as such in the table below.
Instance 5 was verified fresh against the code and is a confirmed finding. Fixed instances
are called out as non-findings.

## Findings table

| # | Layer | Finding | Evidence (`file:line`) | Severity | Concrete fix | Instance |
|---|-------|---------|------------------------|----------|--------------|----------|
| F-10 | Dispatch/authority seams | **#169 instance 5, VERIFIED.** The seam comment claims a coordinator-seat principal "reaching a wave/steering authority verb" draws `coordinator_authority_forbidden`, but the refusal set covers only `['waves.start','waves.run','waves.stop']`. `waves.send` is a steering verb (same comment), dispatches outside the set and before the recursive gate, and `sendWaveMember` has no coordinator-seat check on its plain-send path — a `worker:<id>` principal can steer a wave member unrefused. | `impl/src/application.mjs:12555-12560` (comment vs. set); `:12564` (`waves.send` dispatch); `:12551` ("steer ONE member"); `:11840-11887` (`sendWaveMember`, no seat check on non-claimGrant path; only `:11853` requires `sessionAuthority` for `claimGrant`); `:12589` (recursive gate, waves.\* already returned) | **HIGH** | Add `'waves.send'` to the refusal set at `application.mjs:12560`, or give `sendWaveMember`'s plain-send path the same `_refuseCoordinatorAuthority` gate its three siblings draw. | **#169 instance 5** (verified) |
| F-12 | Kernel API contracts | **Pin-exists-but-section-empty collapses to a verified-adjacent headline.** When the accepted commit pin exists but the accepted verification artifact is gone (e.g. a failed reverify superseded it), the run view reports `result: { state: 'accepted', sha, verificationArtifact: null }`; `verification.state` then reads `'mechanically_verified'` off the *live* `result?.verificationStability` rather than a durable accepted artifact; `_semanticTarget` silently returns null (review skipped with no named cause); and the episode result capsule defaults `state` to `'verified'` whenever the source lacks a `state` field — which every workstream candidate does. Section emptiness is visible only by drilling into `result.verificationArtifact`; every headline state word hides it. | `impl/src/application.mjs:7765-7772` (`publicResult`); `:7577` (`resultStability = acceptedVerification?.stability ?? result?.verificationStability`); `:7870-7872` (`verificationState` ⇒ `'mechanically_verified'` for any `work_completed` phase); `:3873-3878` (`_semanticTarget` null gate); `:10332` (`state: authoritativeResult.state ?? 'verified'`); `:6302-6314` (workstream candidates carry no `state`) | **MED-HIGH** | (a) Derive `verification.state` from the accepted verification artifact's presence — pin with null `acceptedVerification` ⇒ distinct state (e.g. `accepted_verification_missing`), never `mechanically_verified` on a live-only source; (b) `_semanticTarget` returns a named reason when it nulls on this shape; (c) `_episodeItem` propagates a missing `state` as null/`unknown` — never defaults to `'verified'`. | **NEW** (matches brief Layer-7 seed "run.result() materialization — the pin-exists-but-section-empty report") |
| F-01 | Coordination store + writer lease | **Busy refusal names neither holder nor next action.** `coordination_writer_busy` is thrown with no `{pid, pidStart}` of the current holder and no next step. A driver refused the writer lease cannot tell who holds it, how long it has been held, or what to do (retry vs. inspect `writer.lease`). | `impl/src/coordination-store.mjs:1290` (`'coordination writer is already active'`); `:1322-1323` (claim/ownership refusal); `:1326` (EEXIST refusal) | MED | Attach holder `{pid, pidStart, acquiredAt}` + next action (`retry` / `inspect writer.lease`) to the typed `CoordinationRefusal`. | **#169 instance 1** (reconstructed — grid seed "busy refusal's payload") |
| F-06 | Capacity authority | **Reservation refusal names no numbers.** `worktree_capacity_exceeded` is thrown with no current free/floor/reserved/wave figures though all are computed in the same scope. Contrast: the deployment doctor *does* name `freeBytes/freeInodes/minFreeBytes/minFreeInodes`. A refused client cannot tell which bound failed (reserved ceiling vs. free floor; bytes vs. inodes) or by how much. | `impl/src/worktree-capacity.mjs:402` (refusal); in-scope at `:388-401` (`totals`, `outstanding`, `wave`, `observation`); contrast `impl/src/application-deployment.mjs:537-575` (doctor names numbers) | MED | Attach `{freeBytes, freeInodes, minFreeBytes, minFreeInodes, reservedBytes, reservedInodes, waveBytes, waveInodes}` to the typed refusal payload. | **NEW** (matches brief Layer-4 seed "do the reservation refusals name the current free/floor/reservations?") |
| F-11 | Dispatch/authority seams | **`_authorize` collapse.** Every authorization denial flattens to `'application command is not authorized'` / `application_unauthorized`: the allowlist returns a bare boolean, so the specific failing authority (missing capability vs. wrong seat vs. closed session vs. non-owned run) and any next action are erased. Contrast `_refuseCoordinatorAuthority`, which names `{attempted, gracefulPath}`. | `impl/src/application.mjs:3214-3219` (`_authorize`); contrast `:3232-3237` (`_refuseCoordinatorAuthority`) | MED | Thread the specific authority-gate failure (code + next action) out of `authorize()`/`_authorize` instead of collapsing to one code. | **NEW** (matches brief Layer-6 seed "the `_authorize` collapse") |
| F-08 | Worktree + snapshot/commit machinery | **Stale `index.lock` class: raw git error, no lock named, no next action.** `sh`/`gitFile` run `execFileSync('git', …)` with no stale-lock detection or recovery; `captureCommit`'s catch even runs `git reset`, which fails on the same lock, then rethrows raw. A crashed prior git leaves a worker snapshot permanently blocked behind an unactionable message. | `impl/src/worktree.mjs:67-83` (`sh`/`gitFile`); `:1187-1245` (`captureCommit` — `git add -A`, `git commit`, `git rev-parse`, `git reset` in catch at `:1221-1224`) | LOW-MED | On a git failure, detect `index.lock`, name the lock path, and (after owner-liveness check per the capacity `_lock` pattern) remove it or name the recovery verb — never surface a bare git stderr. | **NEW** (matches brief Layer-5 seed "the stale index.lock class") |
| F-04 | Fencing/incarnation | **Stale-fence refusal strips the fence stamps at the northbound.** The fence table's check returns `{result:'stale_fence', current, currentTurnEpoch}`, and the coordinator logs the rich form, but both operator-facing surfaces drop the stamps: MCP throws `'stale fence'` with only `mcpCode`; web returns HTTP 409 `stale_fence`. A fenced driver cannot see the fence event that displaced it or the re-sync next action. | `impl/src/fence.mjs:34-39` (rich check return); `impl/src/coordinator.mjs:7269-7276` (rich log); `impl/src/mcp-northbound.mjs:2072` (stamps dropped); `impl/src/web-northbound.mjs:1118` (`error(409, 'stale_fence')`) | LOW-MED | Carry `current`/`currentTurnEpoch` + a re-sync next action in the northbound error payloads. | **#169 instance 3** (reconstructed — grid seed "a fenced principal's refusal — does it name the fence event?") |
| F-02 | Coordination store + writer lease | **Stale-lease recovery is automatic but unrecorded.** A dead writer's claim and lease are `unlinkSync`'d with no surfaced record — the record doesn't say that a previous writer was discovered dead and its authority reclaimed. Contrast: the resident-authority stale-prior-publication recovery *is* surfaced (`recoveredStaleAuthority` in the outline). | `impl/src/coordination-store.mjs:1310` (stale claim removal); `:1324` (stale lease removal); contrast `impl/src/resident-authority.mjs:347,384,398,412` (recovery surfaced) | LOW | Emit a durable recovery event (or attach the reclaimed owner `{pid, pidStart}` to the claim's result) so the reaped-writer fact is observable. | **#169 instance 2** (reconstructed — grid seed "`recoveredStaleAuthority` — when is it automatic vs. manual?") |
| F-03 | Coordination store + writer lease | **`coordination_writer_lost` names the cause but not the replacer or next action.** `'coordination writer lease was replaced'` tells the loser its lease was superseded but not *which* pid replaced it or what to do next (restart/restore). | `impl/src/coordination-store.mjs:1350,1353,1356` | LOW | Attach the observed lease's `{pid, pidStart}` + next action to the refusal. | NEW |
| F-05 | Fencing/incarnation | **Resident credential lifetime (#148) is not part of the resident authority outline.** The deployment default is `sessionTtlMs ?? 24*60*60*1000` and the session endpoint exposes `expiresAt`, but the serve-time publication outline an operator reads carries only `ownerPid`/`ownerPidStart` — the ~24h death is not written where an operator naturally looks. | `impl/src/resident-authority.mjs:331-337` (profile has ownerPid/ownerPidStart, no TTL); `:412` (`publicOutline`); `impl/src/application-deployment.mjs:1758` (`sessionTtlMs` default); `impl/src/web-northbound.mjs:1780` (`expiresAt` on session endpoint) | LOW | Include `sessionTtlMs`/`expiresAt` in the resident profile and `publicOutline`. | **NEW** (extends #148 — the kernel-facing half) |
| F-07 | Capacity authority | **Stale capacity-lock reaper is silent.** A dead-owner `.lock` is renamed to a tombstone and removed inside `_lock` with no surfaced record. (Reservation reaps in `reconcile` *are* reported via the `{removed, adopted, active}` return — that half is honest.) | `impl/src/worktree-capacity.mjs:322-323` (silent reaper); `:535-558` (reconcile reports reaps) | LOW | Surface the reclaimed stale lock (return field or event). | NEW |
| F-13 | Kernel API contracts | **`complete()`'s quiescence contract is client-honor; the no-progress exit can return a still-`running` view.** The loop's exit conditions (terminal / attention-required / answer_* / unchanged digest) can fire while the run is mid-turn, returning a `running` view to a caller expecting "settled". The data is honest (the view says `running`); the contract name over-promises and nothing server-side enforces quiescence. | `impl/src/application-client.mjs:1086-1098` (`complete()` loop); `impl/src/wave.mjs:338` (pump drives `entry.run.complete()`) | LOW | Document the boundary semantics (returns at a quiescent-or-terminal boundary; caller checks `phase`), or add an explicit `settled: boolean` on the returned view. | NEW |
| — | Replay paths | **Verified clean (non-finding).** Coordination replay is exact and fails closed: `_load` validates schemaVersion/seq-gaps/duplicate-keys/exact-UTF-8/truncated-tail; batch validators and the `_apply` final `else` throw rather than skip. The nearest replay-adjacent approximation is the `_semanticTarget` silent null, captured under F-12. | `impl/src/coordination-store.mjs:1410-1497` (`_load`); `:2134`, `:2196` (batch validators); `:8864-8865` (`unsupported_event_kind` fail-closed) | — | None. | **#169 instance 4** (reconstructed — grid seed "any spot where replay is approximate") → non-finding |
| — | Worktree + snapshot pins | **#168 base-commit pinning: verified present (non-finding).** The worktree branch is created *at* `baseSha` (`git worktree add -b branch dir baseSha`) — on-branch pinning — and `validateOwnedWorktree` enforces branch identity plus `merge-base --is-ancestor baseSha HEAD` (sideband metadata check). No gap. | `impl/src/worktree.mjs:1113` (`createFromBase`); `:1005-1014` (`validateOwnedWorktree` merge-base + branch checks) | — | None. | **#168** → non-finding |

## Ranked fix list

1. **F-10 — add `waves.send` to the `coordinator_authority_forbidden` refusal set**
   (`application.mjs:12560`). HIGH. Verified live authorization gap: the one steering verb
   whose seat gate the seam comment claims but the code omits, dispatched before the
   recursive gate with no compensating check in `sendWaveMember`'s plain-send path.
2. **F-12 — make the pin-exists-but-section-empty shape distinct in every headline state**
   (`application.mjs:7765-7772`, `7870-7872`, `3873-3878`, `10332`). MED-HIGH. A run whose
   accepted verification evidence is gone must not read `accepted`/`mechanically_verified`/
   `completed`/`verified`; report `accepted_verification_missing` and a named
   `_semanticTarget` skip reason. Also stop defaulting the capsule state to `'verified'` on a
   missing field.
3. **F-01 — name the lease holder and next action in the `coordination_writer_busy`
   refusal** (`coordination-store.mjs:1290`). MED. The #41-pattern: field/class + next
   action.
4. **F-06 — name the failing capacity numbers in `worktree_capacity_exceeded`**
   (`worktree-capacity.mjs:402`). MED. Match the doctor's own naming.
5. **F-11 — stop collapsing the `_authorize` denial to one bare code**
   (`application.mjs:3214-3219`). MED. Thread the specific authority-failure code + next
   action through, mirroring `_refuseCoordinatorAuthority`'s `{attempted, gracefulPath}`.
6. **F-08 — detect/name `index.lock` and give a recovery verb on snapshot git failures**
   (`worktree.mjs:67-83`, `1187-1245`). LOW-MED.
7. **F-04 — carry the fence stamps + re-sync next action in the northbound stale-fence
   refusals** (`mcp-northbound.mjs:2072`, `web-northbound.mjs:1118`). LOW-MED.
8. **F-02 — surface stale-writer-lease reaping** (`coordination-store.mjs:1310`,
   `1323-1324`). LOW.
9. **F-03 — name the replacing holder in `coordination_writer_lost`**
   (`coordination-store.mjs:1356`). LOW.
10. **F-05 — include the resident credential lifetime in `publicOutline`**
   (`resident-authority.mjs:331-337`, `412`). LOW.
11. **F-07 — surface the capacity stale-lock reclaim** (`worktree-capacity.mjs:322-323`).
    LOW.
12. **F-13 — document/normalize `complete()`'s quiescence boundary**
    (`application-client.mjs:1086-1098`). LOW.

**Non-findings (verified clean):** coordination replay fails closed (`coordination-store.mjs:
1410-1497`, `2134`, `2196`, `8864-8865`); #168 base-commit pinning is on-branch and sideband
(`worktree.mjs:1113`, `1005-1014`); capacity `reconcile` reports dead-owner reaps
(`worktree-capacity.mjs:535-558`); resident-authority stale-prior-publication recovery is
automatic *and* surfaced (`resident-authority.mjs:347,384,398,412`).
