#!/usr/bin/env node
import { execFileSync } from 'node:child_process';import { existsSync,mkdirSync,writeFileSync } from 'node:fs';import { homedir,tmpdir } from 'node:os';import { dirname,join,resolve } from 'node:path';import { fileURLToPath } from 'node:url';import { CodexAppServerCli,createBrief,createDriver } from '../../../../impl/src/index.mjs';
const HERE=dirname(fileURLToPath(import.meta.url)),REPO=resolve(HERE,'../../../..'),AUTH=join(homedir(),'.codex','auth.json'),LOG_DIR=join(tmpdir(),`baton-web-edge-build-${Date.now()}`),TASK_ID='codex-web-edge-policy',MODEL='gpt-5.6-sol',EFFORT='low';
const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)),git=(args)=>execFileSync('git',args,{cwd:REPO,encoding:'utf8'}).trim(),alive=(pid)=>{try{process.kill(pid,0);return true;}catch{return false;}};async function until(fn,label,timeout=1200000){const end=Date.now()+timeout;while(Date.now()<end){const v=await fn();if(v)return v;await sleep(100);}throw new Error(`timeout waiting for ${label}`);}
if(!existsSync(AUTH))throw new Error('PENDING-LIVE-no-codex-auth-file');const adapter=new CodexAppServerCli({requestTimeoutMs:30000,ceiling:1});const {coordinator,log}=createDriver({repoRoot:REPO,logDir:LOG_DIR,adapters:{codex:adapter},runtimeIsolation:{credentialFiles:{codex:[AUTH]}},workerDependencyDirs:['impl/node_modules'],verifyDependencyDirs:['impl/node_modules'],approvalTimeoutMs:60000,stopDeadlineMs:15000,budgetPolicy:{terminalGraceMs:2000},watchdog:{stallMs:360000}});
const TARGETS=['impl/src/web-edge.mjs','impl/src/web-northbound.mjs','impl/src/web-stream.mjs','impl/src/index.mjs','impl/test/phase12-web-edge.test.mjs','impl/test/phase12-web-session-lifecycle.test.mjs','impl/test/phase12-web-auth.test.mjs','impl/test/phase12-web-northbound.test.mjs','impl/test/phase12-web-stream.test.mjs'];
const brief=createBrief({goal:'Implement spec/phase12/web-edge-policy.md EP1-EP9 as one complete vertical over the existing authenticated command/session/SSE authority. Start red-first. Add strict canonical direct/trusted-proxy address and forwarded-HTTPS resolution; deterministic bounded fixed-window address/login/principal/weighted-command quotas; refusal before provider/body/admission/dispatch; Retry-After and fail-closed audit; non-disclosing health/readiness; and bounded idempotent graceful shutdown that closes admission/streams/listener without touching worker lifecycle.',constraints:[`Edit only: ${TARGETS.join(', ')}.`,'Use Node built-ins only; no dependencies.','Do not trust any forwarding header unless the immediate socket peer exactly matches configured trusted proxies.','Never audit/store raw client addresses; use a keyed digest and coarse class only.','Address quota must run before body/provider work; principal/cost quota before durable command admission.','Health/readiness return no fleet, repo, path, credential, provider, or dependency details.','Shutdown must never call coordinator interrupt/kill or claim worker death.','Preserve all existing Phase 12 behavior and tests.','Do not add OIDC, WebSocket, UI/browser automation, MCP, or homelab integration.','Do not commit, push, or use network tools.'],pathScope:TARGETS,definitionOfDone:'EP8 tests prove proxy spoof resistance, deterministic quotas and bounded keys, refusal ordering/no mutation, Retry-After, readiness non-disclosure, shutdown drain/idempotency/no fleet effects, and strict configuration',verification:{command:'test -s impl/test/phase12-web-edge.test.mjs && node --test impl/test/phase12-web-edge.test.mjs impl/test/phase12-web-session-lifecycle.test.mjs impl/test/phase12-web-auth.test.mjs impl/test/phase12-web-northbound.test.mjs impl/test/phase12-web-stream.test.mjs',expectExit:0,timeoutMs:240000},budget:{tokens:950000,usd:7,wallMin:17}});
let workerId=null,pid=null,result=null,integration=null,fatal=null,pumping=true,steer=null;const approvals=[];const pump=(async()=>{const seen=new Set();while(pumping){for(const w of coordinator.list()){const id=w.pendingApprovalId??w.pendingQuestionId;if(!id||seen.has(id))continue;seen.add(id);approvals.push(await coordinator.respond(id,w.pendingApprovalId?{decision:'allow'}:{text:'Proceed within EP1-EP9 and pinned files.'},'human'));}if(workerId&&!steer&&log.read(workerId).some(e=>e.kind==='resource.budget_threshold'&&e.payload?.threshold>=0.8))steer=await coordinator.send(workerId,'Budget steer: finish the smallest complete EP1-EP9 vertical and pinned tests, then return.','steer',{actor:'orchestrator'});await sleep(100);}})();
try{const h=await coordinator.spawn('codex',brief,{taskId:TASK_ID,taskType:'implementation',model:MODEL,effort:EFFORT,modelPolicy:{allow:[MODEL],allowFamilies:['openai']}});workerId=h.id;const spawned=await until(()=>log.read(workerId).find(e=>e.kind==='lifecycle.spawned'&&e.actor==='worker'),'native spawn');pid=spawned.payload?.pid??null;await until(async()=>(await coordinator.result(workerId)).ready,'verified edge policy');result=await coordinator.result(workerId);if(result.status!=='completed')throw new Error(`edge policy failed: ${JSON.stringify(result)}`);integration=await coordinator.integrate(workerId,{strategy:'ff-only',actor:'orchestrator'});}catch(error){fatal=String(error?.stack??error);}finally{pumping=false;await pump.catch(()=>{});if(workerId)await Promise.resolve(coordinator.kill(workerId,'policy')).catch(()=>{});if(workerId)try{await until(()=>(!pid||!alive(pid))&&!existsSync(join(REPO,'.baton','wt',TASK_ID))&&!existsSync(join(REPO,'.baton','runtime',workerId))&&git(['branch','--list',`baton/${TASK_ID}`])==='', 'full reap',30000);}catch(error){fatal=`${fatal??''}\ncleanup:${error?.stack??error}`.trim();}}
const events=workerId?log.read(workerId):[],handle=workerId?coordinator.list().find(w=>w.id===workerId):null,card=`${adapter.card().harness}@${adapter.card().version}`;const checks={noHarnessError:fatal===null,exactTupleHonest:handle?.harnessResolved===card&&handle?.modelRequested===MODEL&&handle?.modelResolved===MODEL&&handle?.modelObserved===MODEL&&handle?.effortRequested===EFFORT&&handle?.effortResolved===EFFORT&&(handle?.effortObserved==null||handle?.effortObserved===EFFORT),freshVerified:result?.status==='completed'&&events.some(e=>e.kind==='verify.reverified'&&e.payload?.accept===true),integrated:integration?.ok===true,integrationIntent:events.some(e=>e.kind==='integration.completed'),killConfirmed:events.some(e=>e.kind==='kill.confirmed'),processGone:!!pid&&!alive(pid),worktreeGone:!existsSync(join(REPO,'.baton','wt',TASK_ID)),runtimeGone:workerId?!existsSync(join(REPO,'.baton','runtime',workerId)):false,branchGone:git(['branch','--list',`baton/${TASK_ID}`])===''};const summary={at:new Date().toISOString(),repoHead:git(['rev-parse','HEAD']),workerId,pid,model:MODEL,effort:EFFORT,result,integration,approvals,steer,checks,fatal,pass:Object.values(checks).every(Boolean)};mkdirSync(HERE,{recursive:true});writeFileSync(join(HERE,'events.jsonl'),events.map(e=>JSON.stringify(e)).join('\n')+(events.length?'\n':''));writeFileSync(join(HERE,'summary.json'),`${JSON.stringify(summary,null,2)}\n`);console.log(JSON.stringify({pass:summary.pass,checks,pid,fatal},null,2));if(!summary.pass)process.exitCode=1;
