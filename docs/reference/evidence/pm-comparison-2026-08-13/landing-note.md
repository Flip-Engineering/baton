# PM-comparison foundry LANDING NOTE (top orchestrator, 2026-08-13)

## The synthesis correction (rows govern over the blind coordinator)

The wave settled WAVE-OK with all five deliverables harvested — but the coordinator wrote
`pm-qa.md` BEFORE the rows' work was visible to it (its §0/§7 record this honestly; the rows
were seat-deferred, it found nothing on disk, and it synthesized from the briefs + digest
rather than fabricate attribution — the correct fallback, and the #181 lifecycle gap's fifth
instance). The coordinator's table is directionally sound, but where it conflicts with the row
reports it never saw, **the rows govern**:

- **The headline shifts to ACTIVATION, not representation** (pm-kg.md's own framing, endorsed
  by the red-team's §4.2 push-back): baton's knowledge plane is write-only in practice (13
  worker-scoped entries, zero promotions — channel-audit `knowledge.md`). Shipping typed
  edges/contradiction detection onto an empty graph is machinery with no consumer. The first
  contract is the one that makes the plane USED: computed, honesty-pinned member briefings +
  elevation ergonomics.
- **Contradiction detection splits:** Layer-1 (deterministic candidate-surfacing) SURVIVES
  with the red-team's three conditions (deployment-owned weights — no arbitrary limits;
  candidate-only, never auto-assert; after activation). Layer-2 (the LLM NLI cascade) is
  REJECTED — non-deterministic classification has no place feeding a byte-exact store.
- **Impact propagation** SURVIVES conditioned: derived purely from blockedBy-DAG structure
  (downstream-blocked count), read-side advisory only, never a focus throttle, no operator
  scalars.
- **The confidence scalar stays out** (fabricated-precision surface); `belief_status`
  (believed/suspended/retracted + dependent suspension) is the honest half.
- **Every pm clock is REJECTED or ADAPTED to event-derived** (staleness-by-days, review-after-
  T-hours, idle-on-clock — the veto held everywhere; both rows carried a time-mechanism
  census).

## The adoption list as filed (issues, this date)

Per the rows+redteam (the corrected order): ① knowledge-plane ACTIVATION (computed briefings +
elevation ergonomics) · ② typed edges + belief_status + contradiction candidates (Layer-1
only) · ③ failure-stall → forced review (event-derived, #67's sibling) · ④ auto-scaffold on
wave harvest · ⑤ delta-aware re-briefing (cry-wolf-guarded) · ⑥ CLI↔MCP parity test shape
(the one clean ADOPT) · ⑦ impact-propagation read-side projection (conditioned).

Deferred with reasons recorded: staleness-by-unreferenced-count, auto-link-on-elevate,
portfolio/opportunity-cost projection, session handoff docs, multi-signal atlas retrieval.
REJECTED: the confidence scalar, the NLI cascade, every wall-clock gate, SQLite-locality as a
model (baton is content-addressed/git-anchored), per-worker heaviness, and UI-first chrome.

## What pm does better, kept plain (the coordinator's §6 stands)

pm is a research journal with a query surface; baton is a work orchestrator with a control
law. For long-horizon single-researcher R&D — stable finding IDs, typed traversal, resumption
briefings — pm's shape is genuinely better. The adoption list takes that half's shape and
refuses its clocks, its locality, and its precision theater.
