# ROW BRIEF — row-bt164: blue-team the blind-waits-red suite

Read `foundry-brief.md` first (the blue-team law binds you — per-row cheapest-wrong-impl attacks, pin-bite attacks, split-twice, the attempt-echo law in the first five lines). Your target: `impl/test/blind-waits-red.test.mjs`. Its authority contract is named in the suite's header comment (Authority line) under `docs/reference/evidence/` — read the contract AND its fold/red-team companions in the same dir; your job is to attack the SUITE against that intent, not to re-review the contract.

Per capability row: name the cheapest wrong implementation that turns it green (or "none found" + one line of reasoning). Per PIN row: name the plausible wrong impl it kills (or "decorative"). Re-run the split twice; both must match the row's declared notes. Law re-check per the frame.

Deliverable: `docs/reference/evidence/blue-team-2026-08-13-a/blueteam-164.md` ONLY (per-row verdicts SHALLOW/DECORATIVE/BROKEN/SOUND + final ACCEPT/NEEDS-FOLD), plus the `shared` publish (title "#164") — or the exact refusal recorded.
