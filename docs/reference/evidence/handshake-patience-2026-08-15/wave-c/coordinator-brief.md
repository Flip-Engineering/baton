# COORDINATOR BRIEF — handshake-patience-2026-08-14 wave-a (#226)

One implementation row: the web client's GET floor stops silently capping caller patience.
You verify the row's deliverable and write verify-notes.md carrying HANDSHAKE-PATIENCE-VERIFY v1.
Acceptance: the row's red-first pin green at HEAD; cli-adapters + adapter batteries unchanged.
Issue #226 is the contract (measured: doctor handshake cli_transport_failed under 3-wave load
with commandTimeoutMs=240s configured — the 45s floor ate it).
