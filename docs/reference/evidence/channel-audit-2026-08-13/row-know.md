# ROW BRIEF — row-know: knowledge generation + tiered elevation, as actually exercised

Read `foundry-brief.md` first — it binds you (verdict scale, evidence law, publish rule,
attempt-echo law). Your target: **the knowledge-graph / scratchpad tier system as the campaign
actually used it** — task-ephemeral → workflow-ephemeral → project-persistent, with
orchestrator-gated elevation.

Questions, each PROVEN/GAPPED/UNEXERCISED with citations:
1. **What did members actually WRITE?** Enumerate the scratchpad entries this campaign's waves
   produced (from the store row-chan locates — coordinate via `shared` if reachable, else cite
   the same store directly): kinds, partitions, titles. Is the volume honest signal or noise?
2. **Did elevation ever happen?** Any task→workflow or workflow→project elevation event in the
   store? If zero: is that because no orchestrator elevated (behavior gap — me), or because no
   member surface can REQUEST elevation and no orchestrator surface LISTS what awaits elevation
   (surface gap — cite the code)?
3. **The orchestrator-side ergonomics:** given a settled wave, what command shows me "entries
   awaiting elevation review"? If the answer is "none", that's the gap that makes the tier law
   unenforceable — name it precisely with the command inventory citation.
4. **Content quality spot-check:** read 3–5 actual member notes from this campaign. Are they
   knowledge (durable, reusable facts) or status chatter? The tier system's value depends on
   members writing knowledge-shaped entries — if they write chatter, say so with quotes.
5. **The `run.scratchpad.elevate` surface** (`application-semantics.mjs` — the facade-projection
   epic): who exercised it, ever? UNEXERCISED is a finding.

Deliverable: `docs/reference/evidence/channel-audit-2026-08-13/knowledge.md` ONLY, plus the
`shared` publish (title "row-know") — or the exact refusal if the publish fails.
