[attempt: 31545279-5f3c-49ad-809b-2492a09b0efc row-fold170]
# fold-170 — blocker → resolution map (#170 workflow-DSL contract, wavefile)

Date: 2026-08-13 · Target: `workflow-dsl-contract.md` (folded v1 → **v2**) · Red-team:
`redteam-170.md` (governs) · QA: `review-foundry-2026-08-13-b/review-qa.md` §1 (blind; row report
governs on conflict).

## Top-orchestrator decision (law)

- **DR-2 (OQ1, `waves.compile` seam home) → option (a), FOLDED.** `waves.compile` lands as a new
  direct-port command beside `waves.run` at `application.mjs:12560-12573` (same seam family, one
  authorization story) PLUS a new read-only MCP tool `baton_waves_compile`. All four surfaces
  (CLI/bus/MCP/facade) funnel through that one port. Applied in D4 + OQ1.

## Blockers (red-team)

| # | Blocker | Resolution | Contract anchor |
|---|---|---|---|
| B1 | stale `workflow.json:12,25,38,51,64` citation (actual lines 8/15/22/29/36, and the file is a live wave artifact) | **FOLDED** — re-cited semantically, dropped the volatile line list | G3 |
| B2 | Appendix A does not lower to `workflow.json` (4 byte-mismatch classes: key `-wave-b` vs `-wave-a`; hyphen vs em-dash; `->`/`-`/no-backticks vs `→`/`—`/backticks) | **FOLDED** — Appendix A regenerated byte-for-byte from the actual JSON bytes; P2 pins the immutable committed fixture `impl/test/fixtures/workflow-dsl-foundry-roundtrip.json` instead of the live doc | Appendix A, P2 |
| B3 | round-trip law self-contradictory (pure-function compiler vs realpath containment) | **FOLDED** — chose: compiler does realpath harvest containment when `repoRoot` is provided; "pure function" scoped to "given `repoRoot`"; pin passes `repoRoot`; S1 allows gated `realpathSync` | D1, D2, P1, S1 |
| B4 | `{line,field,expected}` triple cannot ride the wire (D2 never sets `detail`; `LANE_CRAFTED` forwards only `cause?.detail` at `mcp-northbound.mjs:1651-1654`) | **FOLDED** — D2 sets `detail: {line, field, expected}`; §3/P9 pin `error.detail.line/field/expected` | D2, §3, P9 |

## Amendments (red-team)

| # | Amendment | Resolution | Contract anchor |
|---|---|---|---|
| A2 | repeated top-level `scope` accumulation rule unstated | **FOLDED** — accumulation sentence added | D1 placement rules |
| A3 | R1's `expected: '<closed directive list>'` missing from the vocabulary table | **FOLDED** — added to `workflow_spec_invalid` expected examples | §3 |
| A4 | S1 grep too narrow (`readFile`/`openSync`/`realpathSync` uncaught) | **FOLDED** — denylist broadened; `realpathSync` gated behind `repoRoot` | S1 |
| A5 | closure asserted, never proved | **FOLDED** — D1 closure paragraph + S4 negative pin (salt→`randomUUID` `workflow-interpreter.mjs:506`, runId/waveId→dispatch, driver→`application.mjs:11645`, no projection) | D1, S4 |
| A6 | resolve OQ2/B3 in-contract | **FOLDED** — OQ2 resolved (mirror incl. realpath containment; constant duplication closed by S5) | OQ2 |

## Notes (red-team)

| # | Note | Resolution |
|---|---|---|
| N1 | P10 depends on #160 R3 | **FOLDED** — P10 gated on #160 R3 landing |
| N2 | `render-surface-docs.mjs` is under `impl/scripts/` | **FOLDED** — path corrected (D4, P8) |
| N3 | `waves.compile` registry row + `waves.run` `web`-addition sequencing | **FOLDED** — both registry rows pinned in D4 (`['embedded','mcp','cli','web']`) |
| N4 | shared-publish not performable from a worker surface | **FOLDED as audit note** — no contract change (write/append verb absent per #158) |
| N5 | DSL's own suite file never named | **FOLDED** — `impl/test/workflow-dsl-red.test.mjs` named in G6 |

## QA §1.4 fold set

| # | Item | Resolution | Contract anchor |
|---|---|---|---|
| H1 | Appendix A byte-identity precondition | **FOLDED** — subsumed by B2 | Appendix A, P2 |
| H2 | compiler≡interpreter constants source-scan pin / shared module | **FOLDED** — S5 pin; OQ2 names the constant-duplication risk | S5, OQ2 |
| H3 | `signalOnMembersDone` roles / `answerDecisions` keys not cross-validated | **FOLDED** — one-line residual note (silent no-op, not a refusal) | §3 |
| §1.4.4 | ship sound remainder; fix OQ6 `waves.run` surface set (add `web`) | **FOLDED** — grammar/D2/D3/D4/vocabulary/pins shipped; `waves.run` row gains `web`, no ghost row | D1–D4, OQ6 |

## Citation-audit + body items (exhaustive — nothing dropped silently)

| Item | Disposition | Contract anchor |
|---|---|---|
| C-1 — stale `workflow.json` citation | **FOLDED** — same as B1 (semantic re-cite, line list dropped) | G3 |
| C-2 — first-pass `:146` off-by-one claim | **STRUCK** — withdrawn in full by the red-team's own second pass (the contract's `:146` citation is exact); no contract change | — |
| C-3 — verification-HEAD identity (`7661b1f` vs fold HEAD `e371f70`) | **FOLDED** — header re-verification note updated; anchors hold against the working tree | header |
| R2 — unspecified compile-validation ordering (which missing field does `field:` name?) | **FOLDED** — validation order fixed (`harness`→`model`→`effort`→`objectiveRef`, first-missing-field-wins); R2 pin asserts that order | §4 red pins |

## Conflict resolutions (blind-QA law — row report governs)

- QA §1.4.4 lists **P9** among "ship as written"; **B4** (row blocker) proves P9 is false as written
  (no `detail` leg). **B4 governs** → D2 sets `detail` and P9 pins `error.detail.line/field/expected`.
- QA §1.4.4 lists **P1** "as written"; **B3** (row blocker) requires the pin to always pass
  `repoRoot`. **B3 governs** → P1 passes `{ repoRoot }`.
- Every QA instruction is still disposed (FOLDED/STRUCK/ESCALATED) — none dropped; conflicts resolve
  to the row report.

## Escalations

- None. DR-2 was the only authority-class item for #170 and was already decided by the top
  orchestrator (option (a)) — applied, not escalated.

## Verification

Every citation touched this fold was re-verified against the working tree with `grep -an`/`sed -n`:
`mcp-northbound.mjs:1651-1654` (LANE_CRAFTED → `toolError(code, cause?.message, cause?.detail)`),
`:198-199` (`toolError` wire `{code, message?, detail?}`), `workflow-interpreter.mjs:26-33`
(`workflowError` sets only `{code}` today), `:506` (`randomUUID()` salt), `application.mjs:12573`
(`waves.run` dispatch), `application-semantics.mjs:1637-1647` (`waves.run` surfaces
`['embedded','mcp','cli']`), and the current `contract-foundry-2026-08-13/workflow.json` (key
`-wave-a`, Unicode bodies at steering/message/signal). All four blocker claims and both QA
drift-hazard claims confirmed.
