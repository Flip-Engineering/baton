# #157 RED-TEAM REPORT — adversarial review of `cli-wave-fidelity-contract.md` v1

- **Target:** `docs/reference/evidence/cli-wave-fidelity-2026-08-13/cli-wave-fidelity-contract.md` (issue #157 — CLI wave ghosts + interpreter-wave registry fidelity)
- **Red-team brief:** `redteam-157-brief.md` (same dir) — all five attack axes addressed below
- **Review HEAD (this report):** `f2acbf8f7a0fef8f9acf9e9f1640315263a9fcca` (branch `baton/ws-d00b3f54f79829c93d77f39b69e94498`)
- **Contract's stated base:** the header names `master` @ worktree `baton/ws-9486fb64ea7454173244299de3fe4cfd` (HEAD `ada515d`). That worktree/HEAD does not exist in this deployment tree — it is stale drafting metadata. Every working-tree citation below was re-verified at the review HEAD and holds, so the base-line staleness changes no citation, only the header's provenance note.
- **Method:** every `file:line` re-verified with `grep -a`/`sed -n` at the review HEAD; the two NUL-bearing files (`application.mjs`, `coordination-store.mjs` — 3 NUL bytes each, byte-counted) were read with `grep -a`/the Read tool. Runtime claims were executed, not inferred: `servedCliOrdinaryKeys()`, `render-surface-docs.mjs --check`, and a live `parseBatonCli` probe.

---

## 1. Citation verification

### 1.1 Working-tree citations — all verified except one

Every citation in D1/D2/D3, the refusal vocabulary, the acceptance pins, and the open questions was re-read at the review HEAD. Notable confirmations:

- **D1 registry schemas.** `waves.send` (`application-semantics.mjs:1599-1613`) — `{runId, message, delivery ∈ ['nudge','now','turn'], claimGrant}`, required `['runId','message']`, example `baton waves send RUN_ID --message TEXT`; `waves.stop` (`:1614-1621`) — `{runId, reason}`, required `['runId']`, `destructive: true`. Both carry `surfaces: ['embedded','mcp','cli','web']`.
- **D1 parse side.** The `waves` branch handles `run|list|progress|start|attach` only (`application-cli.mjs:1323-1416`); the closed-set refusal `if (action !== 'attach') throw cliError('expected waves list, progress, start, attach, or run', 'cli_command_unavailable')` sits at `:1383-1384`. `CLI_WEB_COMMANDS` (`:16-32`) carries `waves.attach/start/list/progress/run` and **not** `waves.send`/`waves.stop`. The web-client admit gate is `application-cli.mjs:2013` and the transport split `name.replaceAll('.', '_')` is `:2015`. `runBatonCli` dispatches the command shape at `:2211-2228` → `client.command(parsed.name, parsed.args, parsed.idempotencyKey)` at `:2228`. `waves.start` branch idiom at `:1372-1376`. The `run send` delivery-mode idiom at `:1733-1738`. The singular `wave` corrective at `:1314-1322` with `pluralCorrective` naming send/stop at `:1316`.
- **Live parse probe (executed).** `['waves','send','run-a','--message','hi']` and `['waves','stop','run-a','--reason','done']` both throw `cli_command_unavailable | expected waves list, progress, start, attach, or run` — byte-identical to the contract's red baseline.
- **D1 web/MCP wire.** `WAVE_WEB_ENTRIES` (`web-northbound.mjs:37-47`) contains `waves_send` (`:40`) and `waves_stop` (`:41`); `WAVE_ARG_FIELDS` (`:54-61`) carries `waves_send {claimGrant, delivery, message, runId}` and `waves_stop {reason, runId}`. MCP tools `baton_waves_send`/`baton_waves_stop` at `mcp-northbound.mjs:100-101`.
- **D1 application dispatch.** `sendWaveMember` (`application.mjs:11840-11893`), `stopWaveMember` (`:11895-11902`, `_normalizeWaveMemberAction(rawRequest,'wave stop',{reason:true})` at `:11900`), dispatch table `waves.send→sendWaveMember` / `waves.stop→stopWaveMember` at `:12565-12566` (table `:12560-12569`). `_normalizeWaveMemberAction` (`:11963-11983`) closes the field set to `{runId, message, delivery, reason, claimGrant}` (`:11965`), guards `validId(value.runId)` (`:11966`), the reason/message pair (`:11967-11968`), the delivery enum (`:11969`), and the claimGrant closed shape (`:11974-11981`).
- **D1 doc regeneration.** `servedCliOrdinaryKeys()` (`render-surface-docs.mjs:34-75`) iterates `CLI_WEB_COMMANDS` (`:42-52`) and filters by registry cli surface (`:71-74`); the `--check` flag is pinned at `:8`; `renderCliVerbInventory()` (`:77-89`) emits `| \`key\` | \`profile\` | \`cli\` | \`example\` |` from the registry's `operation.example`. **Executed:** the emitted waves subset is exactly `attach, list, progress, run, start`; `render-surface-docs.mjs --check` exits 0 at HEAD; the CLI.md generated block (`cli-verb-inventory`, CLI.md:52-56) lacks both ghost rows.
- **D2 interpreter lane.** `runWorkflow` (`workflow-interpreter.mjs:483-618`) calls `baton.waves.start({members: rendered, …})` at `:534`. The facade `start` is `createWave` (`application-client.mjs:1555`); `createWave` mints the role-only string roster `const roster = members.map((member) => member.role);` (`wave.mjs:180`) and each member's `runs.start` carries `waveId, waveRole, waveStart` (`wave.mjs:202-206`). The facade's `prepareRunStart` maps `exact`/`harness/model/effort` into `intent.route` (`application-client.mjs:162-168`); `_resolveIntent` (`application.mjs:3166-3206`) resolves it to the concrete route; the steering record mints at `:4654-4667` with `{runId, driverKind, waveId, waveRole, route}` (`APPLICATION_STEERING_REGISTERED_KIND` at `:135`). The pre-loop `wave.started` string-roster payload is accepted as the legacy `idempotencyKey,roster` pair at `application.mjs:1525-1532`.
- **D2 registry fold.** `coordination-store.mjs:8099-8123` — the `wave.started` fold accepts a well-formed string-array or object-array roster (`:8108-8114`), keeps `[...roster]` (`:8118`), and reserves `wave_registry_invalid` (`:8113`) for malformed NEW-shape records only.
- **D2 projection.** `waveList` (`application.mjs:11759-11822`): string-member branch `:11776-11788` hardcodes `phase: null, progressClass: null, attentionCount: null` (`:11785`) while recovering the seat-map route via `_runWaveRoute(_runIdForWaveMember(row.waveId, member))` (`:11782`, #74 comment `:11779-11781`); object-member branch `:11789-11809` inspects the live run (`:11794`), renders live phase/progress/attention (`:11802-11808`), and refuses typed `wave_not_found` on a vanished registered run (`:11796-11799`). `_runIdForWaveMember` at `:11826-11836`; `_runWaveRoute` at `:11610-11619`. `startWave` object-roster mint `[{role, route, scope}]` at `:11661-11665` with `waveStart: {deploymentId, roster, idempotencyKey}` at `:11679`.
- **D3 conformance.** `checkProfileDocParity` (`surface-conformance.mjs:495-545`) compares the CLI.md generated block against `cliOrdinaryKeys()` (`:374-376`), which returns `servedCliOrdinaryKeys()` — the SAME served inventory the docs render from. Both sides derive from `CLI_WEB_COMMANDS`, so a registry cli claim absent from the whitelist is invisible to both (confirmed by the green `--check` at HEAD).
- **Acceptance pins.** A2-4 F6/F13 (`wave-observability-red.test.mjs:667-706`), A2-5 (`:708-723`), A5-1..A5-5 (`:852-904`), A6-6 (`:1036-1067`) all read exactly as cited. A2-4's members have no steering registration, so the D2.3 no-run branch fires for them.
- **Open-question cross-refs.** `surface-audit-cli.md:292` carries the audit's `baton waves send WAVE_ID|RUN_ID --message TEXT` fix wording. `wave-observability-contract.md:354` carries the #132 "keep their current parse" claim OQ4 calls stale.

### 1.2 The one wrong item

The refusal vocabulary table's `cli_config_unavailable` row is a **wrong citation** (see §2, Refusal vocabulary). `application-cli.mjs:2013` throws `cli_command_unavailable` (message `unsupported Run command ${name}`), and the code `cli_config_unavailable` appears **nowhere** in the repo (repo-wide grep over `.mjs`/`.md`/`.json`). Every other citation verified.

---

## 2. Per-decision verdicts

### D1 — the CLI wave verbs, complete: **SOUND** (two non-blocking notes)

The four claims the brief asks me to attack all hold.

- **The parse branches use the EXISTING input schemas.** Verified field-for-field: the proposed `waves.send` shape (`runId, message, delivery, claimGrant`) matches `application-semantics.mjs:1599-1613`, and `waves.stop` (`runId, reason`) matches `:1614-1621`. The proposed command shape keeps `idempotencyKey` OUT of `args` (top-level only) — this is exactly right, because `_normalizeWaveMemberAction` rejects any key outside `{runId, message, delivery, reason, claimGrant}` (`application.mjs:11965`), whereas the `waves.start` branch puts the key inside `args` because `_normalizeWaveStart` accepts it. The two verbs are not copies of the `start` idiom — they follow the member-lane field set, which is the correct closed shape.
- **The admit seam is correctly identified.** `BatonWebClient.command` (`application-cli.mjs:2013`) refuses any name outside `CLI_WEB_COMMANDS` before the transport split (`:2015`), so D1.2(2) (whitelist admission) is genuinely mandatory, not belt-and-suspenders. The dispatch leg is real end-to-end: `runBatonCli` (`:2228`) → `client.command` → `waves_send`/`waves_stop` ∈ `WAVE_WEB_ENTRIES` (`web-northbound.mjs:40-41`) → `sendWaveMember`/`stopWaveMember` (`application.mjs:12565-12566`). A7-7 is satisfiable.
- **Closed-set refusal byte-consistency.** The proposed `'expected waves list, progress, start, send, stop, attach, or run'` is a strict superset of the current message with `send, stop` inserted. No test in `impl/test/` pins the exact current string (repo-wide grep for `expected waves` finds none outside `application-cli.mjs`); A5-3's `/waves list/` pin exercises the singular corrective (`:1314-1322`), which is untouched and already names send/stop (`:1316`). The message update is free.
- **Generated-docs row regenerates, never hand-edits (#142).** `renderCliVerbInventory` emits the row from `operation.example` (registry-driven), so once the whitelist admits `waves.send`/`waves.stop`, the regenerated block yields exactly the two rows the contract predicts (`baton waves send RUN_ID --message TEXT`, `baton waves stop RUN_ID --reason TEXT`). The `--check` drift gate is real (green at HEAD, flips red if the whitelist admits without doc regeneration). The `deriveSurfaceNames('waves.send').cli === 'baton waves send'` claim is verified (`application-semantics.mjs:1146`).
- **Note N1 (minor citation imprecision).** D1.2(1) cites `:1346-1348, :1391-1393` as "the same `id()` helper the other branches use," but those lines are the direct `/^wave:[a-f0-9]{32}$/u` checks; the `id()` helper lives at `application-cli.mjs:100-103` (used at `:1386`). The runId validation intent is nonetheless correct — `id()` matches `validId` (`application.mjs:316`), and the schema's `runId: id` is a plain id, not the wave pattern — so this is wording, not substance.
- **Note N2 (header provenance).** The contract's base line names a worktree/HEAD absent from this deployment tree. Metadata only; no citation depends on it.

### D2 — the registry fidelity law: **SOUND** (three notes)

The brief's three probes all resolve.

- **The hydration seam is real and correctly placed.** The interpreter's members register through the SAME store path as driver members: `runWorkflow` → `baton.waves.start` (`workflow-interpreter.mjs:534`) → facade `start` → `createWave` (`application-client.mjs:1555`) → role-only roster (`wave.mjs:180`) → each member's `runs.start` carries `driverKind:'wave', waveId, waveRole` (`wave.mjs:203-204`) → `steering.registered` minted at `application.mjs:4654-4667` (route resolved via `_resolveIntent`, `application.mjs:3166`). Driver members take the identical path (`startWave` → `this.start(..., driverKind:'wave', waveId, waveRole)` at `:11672-11678`). Both read back through `_runIdForWaveMember` (`:11826-11836`) over the same event log. The seam is the string-member branch (`:11776-11788`), which already proves the read works for `route` — extending it to `inspect` is the object branch's proven move.
- **No residual null path for a healthy interpreter wave.** After the fix, the only members that keep null phase/progress/attention are (a) genuinely run-less legacy string members with no steering record (no-run branch, A2-4 pinned), and (b) transiently, members whose runs are still mid-start — honest, and identical to driver waves in the same state. `_runIdForWaveMember` scans the full event log, so the steering record is found even if minted after the registry fold; there is no ordering hole. The D2.5 "honest remainder" claim holds.
- **#74 seat-map consistency.** The recovered `route` (`:11782`) and the proposed phase/progress hydration both key off the same `_runIdForWaveMember` resolution: a resolved runId yields both the seat map and the live read; a null runId yields the pinned no-run render (route null + nulls, no `error` key). Perfectly consistent — the D3 seat-map recovery just landed is the exact neighbor D2 extends.
- **Note N3 (behavior change to state explicitly).** Hydration makes the string branch draw `run.inspect` — including its authorization — and inherit the D5.2 seam: a steering-registered string member whose run vanished now refuses `wave_not_found` (as the object branch already does at `:11796-11799`) instead of rendering nulls. This is deliberate parity and breaks no existing pin (A2-4's members have no steering record), but the contract should say in one sentence that the string branch now carries the D5.2 refusal, so the implementer does not "safeguard" it back to silent nulls.
- **Note N4 (A7-8 pin phrasing).** The pin asserts "renders **non-null** phase/progressClass/attentionCount," but the law renders `view?.phase ?? view?.outline?.phase ?? null` — legitimately null for a freshly-started run before any outline exists. The pin's comparative clause ("identical to a driver-wave member") is the real law; the test must drive both members to a phase-bearing state rather than assert absolute non-nullness.

### D3 — the ghost-prevention pin: **SOUND** (one fix item + two notes)

- **The verb set is derived mechanically, never a hand list.** The pin iterates `APPLICATION_SEMANTIC_REGISTRY.canonicalOperations` (`application-semantics.mjs:2018`, entries carry `key` + `surfaces`) filtered by `surfaces.includes('cli') && key.startsWith('waves.')`. No hand enumeration.
- **It would have caught the ghosts at their introduction.** At HEAD the ghosts fail all three legs: `CLI_WEB_COMMANDS.has('waves.send')` is false, `parseBatonCli(['waves','send',...])` throws `cli_command_unavailable`, and `servedCliOrdinaryKeys()`/CLI.md lack both rows. The three-way invariant is RED at HEAD and GREEN only after D1 lands — exactly the pin's contract.
- **The D3.1 diagnosis is confirmed by execution.** `servedCliOrdinaryKeys()` derives from `CLI_WEB_COMMANDS` (`render-surface-docs.mjs:42-52`) and `cliOrdinaryKeys()` is the same function (`surface-conformance.mjs:374-376`), so docs and served set share a source and the ghost is invisible to both — `render-surface-docs.mjs --check` exits 0 at HEAD despite the registry claiming `cli` for two unwired verbs.
- **Fix item F1 — same wrong code as the refusal-vocabulary blocker.** D3.2 invariant leg (3) and the D3.3 "pre-existing web-client refusal" both point at `application-cli.mjs:2013`; the code there is `cli_command_unavailable`, not `cli_config_unavailable` (see Refusal vocabulary). The D3 admission assertion itself is sound; the code label it carries is wrong.
- **Note N5 (directionality).** The pin is lane-scoped and one-directional: a registry cli claim must be whitelisted+parsed+documented. The reverse ghost (a `CLI_WEB_COMMANDS` entry with no registry row or parse branch) is not covered — a whitelist-only `waves.xyz` would pass. That is a deliberate closed-set scope per D3.2, not a #157 gap, but a two-way pin would be the fuller #159 doctrine.
- **Note N6 (parse-leg construction).** The parse-acceptance leg needs per-verb minimal valid args (`waves.progress` a waveId, `waves.run` a specPath, `waves.send` runId+`--message`, `waves.stop` runId+`--reason`), since `parseBatonCli` rejects arg-less invocations for value-taking verbs. The contract should pin how each verb's minimal invocation is built (or assert the closed-set refusal no longer names the verb) so the implementer does not hardcode a per-key arg table by hand.

### Refusal vocabulary: **HOLE** (one blocker)

Three of four rows verify; one is wrong.

- `cli_command_unavailable` — correct: `application-cli.mjs:1383-1384`, and the singular `wave` corrective (`:1314-1322`) is unchanged and already names send/stop.
- `cli_action_inputs_invalid` — a real, existing code (used at `application-cli.mjs:1696-1697`), appropriate for the new branches.
- `cli_invalid` — correct: the `id()` helper at `:100-103` throws the default code.
- **`cli_config_unavailable` | `application-cli.mjs:2013` — WRONG.** Line 2013 is `throw cliError(\`unsupported Run command ${name}\`, 'cli_command_unavailable')`. The code is `cli_command_unavailable`; `cli_config_unavailable` does not exist anywhere in the repo (repo-wide grep). An implementer asserting the table (or the A7-6 D3 admission leg) would write a test for a code that can never fire. This is a wrong citation and, per the campaign law, an automatic blocker. **Fix:** change the row's code to `cli_command_unavailable` and note the message `unsupported Run command ${name}`.

### Acceptance pins: **SOUND** (one note)

- A2-4 (`:667-706`), A2-5 (`:708-723`), A5-1..A5-5 (`:852-904`), A6-6 (`:1036-1067`) verified at the cited lines; all stay green under D1/D2. The D2 boundary is correctly drawn: A2-4's members have no steering record, so the D2.3 no-run branch preserves the pinned render verbatim and A2-4 cannot flip red.
- A7-1..A7-8 are each RED at HEAD and GREEN after the fix, with one phrasal caveat: A7-8's "non-null" (see N4) should be read as "identical to a driver-wave member driven to the same state."
- A7-7's dispatch leg is real (`waves_send`/`waves_stop` in `WAVE_WEB_ENTRIES` at `web-northbound.mjs:40-41`; dispatch at `application.mjs:12565-12566`).
- **Note N7 (gate sequencing).** A7-5 couples to the `--check` drift gate: it is green only after D1.2(2) whitelist admission AND D1.2(4) doc regeneration land together. The contract sequences this correctly, but the implementer must land both in one change set or the new pin red-flags mid-flight.

---

## 3. Open-question verdicts

| OQ | Verdict | Note |
|---|---|---|
| OQ1 — `waves.stop` reason required-vs-optional | **Recommendation sound** | Schema required-set is `['runId']` only (`application-semantics.mjs:1620`); the dispatcher requires `reason` (`application.mjs:11900`, guard `:11967-11968`). Requiring `--reason` at the CLI (the stricter seam) and flagging the schema `required` set as a doc-truth follow-up is correct — an early `cli_action_inputs_invalid` beats a server `application_wave_member_action_invalid`. |
| OQ2 — runId vs WAVE_ID | **Deferral sound** | The audit fix wording (`surface-audit-cli.md:292`) says `WAVE_ID|RUN_ID`, but `sendWaveMember`/`stopWaveMember` are runId-validated (`_normalizeWaveMemberAction`, `validId` at `application.mjs:11966`) and the web port is runId-keyed (`WAVE_ARG_FIELDS` carries `runId`, `web-northbound.mjs:57-58`). Pinning `RUN_ID`-only and deferring WAVE_ID (which needs a wave→member-run resolve step) is honest. |
| OQ3 — `--claim-grant` on the CLI | **Recommendation sound** | The schema accepts `claimGrant {boardRunId, board}` (`application-semantics.mjs:1608-1611`), `_normalizeWaveMemberAction` honors it (`:11974-11981`), and the web wire carries it (`web-northbound.mjs:57`). Parsing `--claim-grant JSON` (mirroring `--members` at `application-cli.mjs:1363-1371`) keeps the CLI from dropping a closed shape the wire already accepts. The "do not silently drop it" clause is the right enforcement. |
| OQ4 — #132 D4.5 stale premise | **Correction accurate** | `wave-observability-contract.md:354` says send/stop "keep their current parse," but the live probe at HEAD throws `cli_command_unavailable` — there was never a parse. The verbs were ghosts. OQ4's supersede-note is the correct treatment; no #132 code change is owed. |

---

## 4. Consequential pinned-test / gate drift the contract should own

- The D3 pin is NEW conformance logic. Its natural home is `surface-conformance.mjs` (the main gate at `:734-745` already iterates profiles and calls `checkProfileDocParity` at `:738`). Landing the pin changes the conformance gate's verdict at HEAD (RED until D1 lands), so any CI that runs surface-conformance as a required gate blocks until D1+D3 land in the same change set. The contract should state this coupling explicitly so the implementer does not split the pin into a separate green-only change.
- The A7 rows are new test rows in the wave-observability suite (or a sibling `cli-wave-fidelity-red.test.mjs`). No existing pinned tool list is touched by D1/D2/D3 (no new MCP tool, no new `APPLICATION_COMMAND_DEFINITIONS` entry, no new registry row — `waves.send`/`waves.stop` already exist), so the surface-inventory drifts observed in the #132 review do not recur here.

---

## 5. Final verdict: **NOT FOLD-READY**

Numbered blocker (what · why · concrete fix):

1. **The refusal vocabulary table's `cli_config_unavailable` row is a wrong citation.** It claims `application-cli.mjs:2013` throws `cli_config_unavailable`; the line throws `cli_command_unavailable` (message `unsupported Run command ${name}`), and the code `cli_config_unavailable` does not exist anywhere in the repo. An implementer following the table — or writing the A7-6 D3 admission leg against it — asserts a code that can never fire, and the error-code vocabulary the contract promises is false. Per the campaign law, a wrong citation is an automatic blocker. **Fix:** change the row's code to `cli_command_unavailable` and note the actual message; re-run the conformance + wave-observability gates (A7-1..A7-8 green, A2-4/A2-5/A5-*/A6-6 green, `render-surface-docs.mjs --check` green).

Non-blocking items to fold into the same revision: N1 (cite the `id()` helper at `:100-103` rather than the wave-ID regex lines), N3 (state explicitly that the hydrated string branch now carries the D5.2 `wave_not_found` seam), N4/N7 (phrase A7-8 as "identical to a driver-wave member driven to the same state," and note A7-5's dependency on whitelist-admission + doc-regeneration landing together), N6 (pin the per-verb minimal-args construction for the D3 parse leg), N5 (record the reverse-ghost direction as a deliberate out-of-scope), and the header's stale base-worktree/HEAD provenance line.

With blocker 1 fixed, the contract is **FOLD-READY**: D1 and D2 are sound, D3 is sound, the acceptance pins are correctly RED/GREEN, and the open questions are resolved honestly.
