# ROW BRIEF — row-death: the death contract (package ④ lifecycle-honesty)

Read `foundry-brief.md` first (the frame + laws bind you — Ring-2 contract form: ground truths (cited fresh, NUL discipline on application.mjs + coordination-store.mjs) → decisions → closed refusal vocabulary → red-first acceptance pins at named stages → open questions; no clocks; sorted-key literals ACTUAL order; your [attempt: <salt> <role>] line VERBATIM in the first five lines). Your issue set + the campaign evidence:

- #182 death certificates: every terminal run carries a derived suspicionClass + evidence (provider_refusal/capacity_reap/watchdog_stall/wave_close_teardown/credential_death/explicit_stop/clean_terminal) — today's manual recoveries: the suite-fold member, the trust-gate-killed impl member (worker_path_scope_violation at completion), the #169 drives. The derivation inputs exist (#67 watchdog states, #79 delivery receipts, adapter refusals, the wave-close path)

**Deliverable:** `docs/reference/evidence/lch-contracts-2026-08-14/contract-death.md` ONLY (plus the shared publish — or the recorded refusal). Every acceptance pin RED at HEAD at a named stage, green only for a correct impl (shallow-greenability is a defect). Judgment calls recorded; authority-class ambiguity → DECISION_REQUEST with options.
