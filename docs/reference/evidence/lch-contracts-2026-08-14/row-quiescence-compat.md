# ROW BRIEF — row-quiescence-compat: the quiescence-compat contract (package ④ lifecycle-honesty)

Read `foundry-brief.md` first (the frame + laws bind you — Ring-2 contract form: ground truths (cited fresh, NUL discipline on application.mjs + coordination-store.mjs) → decisions → closed refusal vocabulary → red-first acceptance pins at named stages → open questions; no clocks; sorted-key literals ACTUAL order; your [attempt: <salt> <role>] line VERBATIM in the first five lines). Your issue set + the campaign evidence:

- #163 is FOLDED (contract-163.md v1.1+fold) — your contract is the WAKE/QUIESCENCE COHERENCE contract: how parked-for-signal members (#181), retrying members (#201), and silent-but-working members interact with the quiescence candidate predicate (progressClass/liveness gating per the folded #163) — the composition contract that keeps the three honest about each other. Evidence: the folded contract + the day's six coordinator-lifecycle instances

**Deliverable:** `docs/reference/evidence/lch-contracts-2026-08-14/contract-quiescence-compat.md` ONLY (plus the shared publish — or the recorded refusal). Every acceptance pin RED at HEAD at a named stage, green only for a correct impl (shallow-greenability is a defect). Judgment calls recorded; authority-class ambiguity → DECISION_REQUEST with options.
