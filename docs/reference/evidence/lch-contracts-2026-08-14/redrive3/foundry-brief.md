# LIFECYCLE-CONTRACT FOUNDRY — shared frame (2026-08-14)

Every member reads this first. This wave CONTRACTS the lifecycle-HONESTY package (④) — the
waves.run operator surface — as four parallel contract rows. Methodology: Ring-2 contract form
(ground truths → decisions → refusal vocabulary → red-first acceptance pins → open questions),
every citation re-verified this session (`grep -an`/`sed -n` on application.mjs +
coordination-store.mjs — NUL discipline; plain grep elsewhere), no clocks anywhere,
sorted-key literals ACTUAL order.

**The campaign evidence is your seed:** today's incidents are the requirement source — four
base-commit captures of dirty master (#168), two index.lock reaps (#172), two client timeouts
on synchronous launches (#173), two member scope escapes (#185), the phantom-failure saga
(#199/#200) whose root cause was the objective-cap mismatch (#207), the bare-text bus response
(#202), the restart dance (#204), and the logged-invariant gap (#194).

## The laws

- Verdict-grade contracts: acceptance pins must be RED at HEAD and green only for a correct
  impl (shallow-greenability is a defect).
- **THE ATTEMPT-ECHO LAW (#171):** your `[attempt: <salt> <role>]` line VERBATIM in your
  contract's first five lines.
- Judgment calls recorded; authority-class ambiguity → DECISION_REQUEST with options.
- Publish to `shared` when complete — or record the exact refusal (evidence, #158).

## Row assignments (each writes its contract + a fold-record-ready pin list)

- `row-lc-fs` → the FILESYSTEM-lifecycle contract (#168 sideband refs + #172 own-index + #185
  member confinement/settle sweep) → `contract-filesystem.md`
- `row-lc-launch` → the LAUNCH/RECEIPT honesty contract (#173 detach/acceptance-receipt +
  #202 response shapes + #207 objective-cap admission alignment + the startError on the wire +
  the spill-digest graceful path) → `contract-launch.md`
- `row-lc-members` → the MEMBER-CREATION honesty contract (#199 creation failures emit typed
  events, never phantom + #200 task-id namespacing + #204 drain-restart semantics) →
  `contract-members.md`
- `row-lc-ledger` → the LOGGED-INVARIANT contract (#194 model-visible-means-logged + the
  durable no-step turn; #205 decision ledgering rides here) → `contract-ledger.md`
