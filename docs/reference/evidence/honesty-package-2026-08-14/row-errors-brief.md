# IMPL BRIEF — row-errors: #160 error actionability

You are the error-surface impl row of the honesty-package wave. **Your acceptance rows:**
`impl/test/error-actionability-red.test.mjs` ALL green at named stages (the W/M/C/X matrix +
the S static pins). Never edit suites. PINs stay green; adjacents unmoved.

**Read first:** `docs/reference/evidence/error-actionability-2026-08-13/contract-fold.md`
(your authority — the actionability triple {typed code, field/cause, next action} across
web/MCP/CLI + the sanitization negatives/carve-outs), then
`impl/test/error-actionability-red.test.mjs` (your rows), then the dir's redteam/fold notes.

**You OWN:** the error/refusal RENDERING paths in `impl/src/mcp-northbound.mjs` +
`impl/src/web-northbound.mjs` + `impl/src/application-cli.mjs`'s error line (the
`baton: <code>: …` shape) — NOT the command/admission tables in those files (row-kernel owns
those, row-cli owns the CLI parser). Touch only the error paths.

**Craft laws:** no clocks · localeCompare banned · sorted-key literals ACTUAL order · byte
literals only in limits.mjs · additive-only on closed vocabularies (new refusal codes APPEND
to the ledger, never amend) · NUL discipline · generated docs regenerate, never hand-edit ·
work only in your worktree · your `[attempt: <salt> <role>]` line VERBATIM in your notes'
first five lines.

**Verify before you finish:** `node --test impl/test/error-actionability-red.test.mjs` green ·
`impl/test/mcp-reflex-surface-red.test.mjs` 21/21 · `impl/test/phase16-mcp-northbound.test.mjs`
29/29. Write `docs/reference/evidence/honesty-package-2026-08-14/notes-row-errors.md`.
