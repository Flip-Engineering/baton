# RE-DRIVE BRIEF — row-cli-b: #157 CLI wave fidelity + the CLI admission half of #158

You are the CLI-seam impl row (re-driven; the first attempt died mid-flight with its work
checkpointed). **Your acceptance rows:** `impl/test/cli-wave-fidelity-red.test.mjs` ALL green
at named stages, plus the `scratchpad-write-red.test.mjs` CLI-parser/admission leg (A10-1 and
siblings). Never edit suites. PINs stay green; adjacents unmoved.

**Read first:** `docs/reference/evidence/cli-wave-fidelity-2026-08-13/contract-fold.md` (your
authority) · `impl/test/cli-wave-fidelity-red.test.mjs` (your rows) ·
`docs/reference/evidence/scratchpad-write-2026-08-13/contract-fold.md` §D4 (the CLI-admission
half of the append verb — the parser must SERVE `run.scratchpad.append`).

**You OWN:** `impl/src/application-cli.mjs` · the interpreter-wave registry-projection
hydration work (the phase/progressClass/attentionCount string-branch fix). NOTE: a parallel
remediation wave owns the waves_list roster-projection region of `application.mjs` — make your
application.mjs edits only in the registry-hydration region the suite names; if the file's
current text differs from your base, re-base onto it.

**Craft laws:** no clocks · localeCompare banned · sorted-key literals ACTUAL order · byte
literals only in limits.mjs · additive-only on closed vocabularies · NUL discipline
(`grep -an`/`sed -n` on application.mjs + coordination-store.mjs; don't disturb NUL bytes) ·
generated docs regenerate, never hand-edit · work only in your worktree · your
`[attempt: <salt> <role>]` line VERBATIM in your notes' first five lines.

**Verify before you finish:** `node --test impl/test/cli-wave-fidelity-red.test.mjs` green ·
`workflow-dsl-red` 35/35 · `workflow-as-data-red` 30/30 · `wave-observability-red` 30/30 ·
`control-surface-truth-red` 7/7. Write
`docs/reference/evidence/honesty-package-2026-08-14/redrive/notes-row-cli.md`.
