# ROW BRIEF — row-cli2: the CLI parser legs (append branch + run.watch + teaching)

The honesty package's four rows landed their recovered work; the drain truncated the CLI
legs. The acceptance suites are RED at exactly your stages — turn them green, correctly.

**Read first:** `impl/test/scratchpad-write-red.test.mjs` stages A1-1, A1-2, A9-1 (the append
CLI legs + bare-scratchpad teaching) and `impl/test/doc-truth-conformance-red.test.mjs`
stages R1, R4, R5 (run.watch served, no silent reinterpretation, example shapes honest);
the recovered row-cli notes `docs/reference/evidence/honesty-package-2026-08-14/notes-row-cli.md`
(the cross-seam map — it names the F8 silent-reinterpretation defect anchor and the PT-7
39→40 cross-wave pin constraint, which you MUST honor: coordinate the detection-set
re-pin, don't break the suite silently); the parser `impl/src/application-cli.mjs`.

**Your file partition:** `impl/src/application-cli.mjs` + `impl/CLI.md` +
`docs/reference/evidence/honesty-package-2026-08-14/**`. Never touch the northbounds or
application.mjs (row-web2's). Never edit the acceptance suites.

**Implement:** the closed `run.scratchpad.append` CLI parse (per A1-1/A1-2's pinned shapes);
bare `baton run scratchpad` refusing with the closed teaching set read|elevate|append (A9-1);
`run watch RUN_ID` → run.watch and bare `run watch` refusing value-required (R1/R4); CLI.md
taught forms matching served reality (R5). Additive; closed refusal vocabulary only.

**Acceptance:** A1-1, A1-2, A9-1, R1, R4, R5 green; `cli-wave-fidelity-red` and
`cli-silent-start-red` green (paste counts — the PT-7 re-pin per the notes' constraint).
Notes: `docs/reference/evidence/honesty-package-2026-08-14/notes-row-cli2.md` with
`[attempt: <salt> row-cli2]` verbatim in the first five lines.
