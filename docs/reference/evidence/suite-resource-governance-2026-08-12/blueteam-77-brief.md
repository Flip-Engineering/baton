# #77 BLUE-TEAM BRIEF — attack the suite-resource-governance red-first suite

You are the **blue team** for the suite-resource-governance suite. Target: NOT the contract —
the SUITE's red-keeping power. Read fully, in order: (1) `suite-resource-governance-contract.md`
(v1.1); (2) `contract-fold.md` (the 6 calibration-seam resolutions); (3)
`impl/test/suite-resource-governance-red.test.mjs` (26 tests: 2 green PINs, 24 red at named
stages); (4) `suite-draft-notes.md`.

## Attack axes (per row)

- **Green-side blockers FIRST** — can every red row go green under a CORRECT v1.1
  implementation? The fake-load doubles must be able to mint every needed load state; no row may
  require REAL host load (that would be both un-greenable and a flake factory).
- **Shallow-greenability** — sharpened for this lane: could the calibration rows pass with a
  deadline derived from a HARDCODED baseline wearing a measurement costume (no real sample)?
  Could the floor row pass with a calibrated deadline BELOW the floor (the inverse violation)?
  Could the flake-taxonomy rows pass with a correctness failure recalibrated away (the exact
  thing D2 exists to kill)? Could the parallelism rows pass with concurrency exceeding the host
  bound?
- **Missing-row gaps** — every v1.1 refusal code; the load-context receipt row (a flake report
  must carry the calibration); the per-file vs whole-run budget separation row.
- **Stage honesty + hermeticity** — named stages at HEAD; mkdtemp only; no real timers, no real
  load reads (the #7 class is the whole point of this lane — a flaky row here is the deepest
  irony; catch it).

## Output + laws

`docs/reference/evidence/suite-resource-governance-2026-08-12/suite-blueteam.md`: BLUE-CLEAN or
NEEDS-FOLD with numbered findings (row/gap + attack + concrete fix). Edit ONLY that file. No
clocks; citations verified (`grep -an`/`sed -n` on the two NUL files); run the suite twice from
the repo root and record both splits.
