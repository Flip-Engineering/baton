# #77 FOLD BRIEF — fold the red-team report into the suite-resource-governance contract (v1.0 → v1.1)

You are folding an adversarial red-team report into the suite-resource-governance contract.
Read fully, in order: (1) `contract-redteam.md` (NOT FOLD-READY — 6 numbered blockers in §D,
each with its concrete fix); (2) `suite-resource-governance-contract.md` (v1.0 — your edit
target).

## The blockers, headlined (fold ALL 6 + apply the open-question verdicts)

Per the report's §D list — the measured-load shape is right; the fold is in the calibration
seams: the load-sample honesty (the floor on calibrated deadlines, the load-context receipt),
the flake-vs-correctness cause-class discipline, the adaptive-concurrency host bound, and every
citation fix (automatic class).

## Laws + deliverables

No clocks; every citation verified (`grep -an`/`sed -n` on the two NUL files); sorted-key
literals ACTUAL order; `localeCompare` banned. Header to **v1.1** with the fold note. Edit ONLY:
`suite-resource-governance-contract.md` (v1.1) + `contract-fold.md` (blocker → change map, all
6 + open questions) — this directory.
