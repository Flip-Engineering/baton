# ROW BRIEF — row-rt170: adversarial red-team of the #170 contract (HIGH PRIORITY)

Read `foundry-brief.md` first (the shared frame binds you: the axes, the citation law, the
verdict scale, the publish-as-you-go rule). Your target:
`docs/reference/evidence/workflow-dsl-2026-08-13/workflow-dsl-contract.md` — read it FULLY
(523 lines; this is the campaign's next serialized impl, so your blockers are cheap now and
expensive after impl), plus its brief (same dir) and the issue WITH comments
(`gh issue view 170 --comments` — the operator fold-in on baton-attached dispatch fields is
part of the requirement set).

Attack per the shared axes, plus one DSL-specific axis: **grammar expressiveness vs
hand-written JSON escape hatches** — if an orchestrator can still be forced to hand-write
machinery fields (attempt salts, lane ids, harvest projections, idempotency keys), the
contract has failed its purpose; name every such gap. Citation audit first (every anchor
re-verified; a wrong citation is an automatic blocker), then per-decision attacks against
the REAL code, the refusal vocabulary, the acceptance pins (shallow-greenability), the open
questions. Verdict per decision SOUND/HOLE with the fix; final FOLD-READY or NOT with
numbered blockers.

Deliverable: `docs/reference/evidence/review-foundry-2026-08-13-b/redteam-170.md` ONLY, plus
the full text published to the `shared` scratchpad partition (kind `note`, title "#170").
