#!/usr/bin/env node
import { execFileSync } from 'node:child_process';
import { existsSync, mkdirSync, mkdtempSync, rmSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

import { CairnRunScorecard, GlmSessionCli, createBrief, createDriver } from '../../../../impl/src/index.mjs';

const HERE = dirname(fileURLToPath(import.meta.url));
const REPO = resolve(process.env.BATON_REPO ?? resolve(HERE, '../../../..'));
const OUTPUT = resolve(process.env.BATON_EVIDENCE_DIR ?? HERE);
const GLM_AUTH = resolve(process.env.BATON_GLM_AUTH_FILE ?? resolve(HERE, '../../../..', 'glm_key.json'));
const LOG_DIR = mkdtempSync(join(tmpdir(), 'baton-phase44-route-review-')); const ARTIFACT_ROOT = join(LOG_DIR, 'artifacts');
const git = (args) => execFileSync('git', args, { cwd: REPO, encoding: 'utf8' }).trim();
const TASK_ID = process.env.BATON_TASK_ID ?? 'phase44-route-stats-glm-review'; const RUN_ID = process.env.BATON_RUN_ID ?? 'phase44-route-stats-review'; const TARGET = `reviews/dogfood/${TASK_ID}.md`;
const MAX_TOKENS = Number(process.env.BATON_REVIEW_MAX_TOKENS ?? 150_000); const MAX_USD = Number(process.env.BATON_GLM_MAX_USD ?? 1.25);
const routeLearningPolicy = { mode: 'auto', halfLifeMs: 604_800_000, explorationConstant: 0.5, seedDiscount: 0.5, minSamplesForAdaptive: 1, defaultPriorSuccessRate: 0.5 };
const sleep = (ms) => new Promise((done) => setTimeout(done, ms)); const alive = (pid) => { try { process.kill(pid, 0); return true; } catch { return false; } };
async function until(fn, label, timeoutMs = 480_000) { const deadline = Date.now() + timeoutMs; while (Date.now() < deadline) { const value = await fn(); if (value) return value; await sleep(100); } throw new Error(`timeout waiting for ${label}`); }
function adapter() { return new GlmSessionCli({ authTokenFile: GLM_AUTH, authTokenJsonPointer: process.env.BATON_GLM_AUTH_JSON_POINTER ?? '/glm_key', model: 'glm-4.7', approvals: false, permissionMode: 'acceptEdits', args: ['--safe-mode', '--no-session-persistence', '--max-budget-usd', MAX_USD.toFixed(2)], ceiling: 1, killGraceMs: 5_000 }); }
function options() { return { repoRoot: REPO, repoId: 'baton-phase44-review', logDir: LOG_DIR, adapters: { glm: adapter() }, approvalTimeoutMs: 60_000, stopDeadlineMs: 15_000, watchdog: { stallMs: 420_000 }, routeLearningPolicy, capabilityFactories: { cairn: ({ coordination, readOperational, router }) => new CairnRunScorecard({ coordination, readOperational, artifactRoot: ARTIFACT_ROOT, routeAdvisor: router, routeAdvice: { maxCandidates: 4, maxTaskTypeBytes: 128, maxRows: 4, maxBytes: 16_384 } }) }, maxCapabilityBudgetTokens: 10_000, maxCapabilityEnvelopeBytes: 128 * 1024 }; }
function brief() {
  return createBrief({
    goal: `Adversarially review committed Phase 44 durable RouteStats and Cairn route advice at ${git(['rev-parse', '--short', 'HEAD'])}. Read spec/phase44/cairn-route-stats.md and the relevant implementation/tests. Write ${TARGET} with exactly "## Verdict", "## P0-P1 findings", and "## Required red tests".`,
    constraints: [
      `Edit only ${TARGET}.`,
      'Use at most ten repository or tool calls and keep the report under 1,000 words.',
      'Ground every finding in an exact source seam and reproducible event, mutation, retry, or restart.',
      'Review atomic terminal observation, exact retry conflicts, policy pinning, replay hydration, tuple isolation, verified-only learning, advice purity/bounds, and northbound authority.',
      'Do not inspect credentials or environment, use network tools, commit, push, deploy, access homelab/project-manager, or edit product files.',
    ],
    pathScope: [TARGET], definitionOfDone: 'The three exact headings exist and the verdict is explicit.',
    verification: { command: `test -s ${TARGET} && grep -Fq '## P0-P1 findings' ${TARGET} && grep -Fq '## Required red tests' ${TARGET}`, expectExit: 0, timeoutMs: 30_000 },
    budget: { tokens: MAX_TOKENS, usd: MAX_USD, wallMin: 9 },
  });
}
function bounded(events) { const allowed = new Set(['runtime.scope_created', 'lifecycle.spawned', 'lifecycle.turn_started', 'lifecycle.turn_completed', 'lifecycle.crashed', 'resource.tokens', 'resource.budget_threshold', 'verify.reverified', 'kill.requested', 'kill.confirmed']); return events.filter((event) => allowed.has(event.kind)).map((event) => ({ seq: event.seq, ts: event.ts, actor: event.actor, kind: event.kind, harnessRequested: event.harnessRequested ?? null, harnessResolved: event.harnessResolved ?? null, modelRequested: event.modelRequested ?? event.payload?.modelRequested ?? null, modelResolved: event.modelResolved ?? null, modelObserved: event.modelObserved ?? event.payload?.modelObserved ?? null, effortRequested: event.effortRequested ?? null, effortResolved: event.effortResolved ?? null, effortObserved: event.effortObserved ?? null, payload: event.kind === 'lifecycle.spawned' ? { pid: event.payload?.pid ?? null, threadId: event.payload?.threadId ?? event.payload?.sessionId ?? null } : event.kind === 'resource.tokens' ? { tokens: event.payload?.tokens ?? null, usd: event.payload?.usd ?? null, accounting: event.payload?.accounting ?? null } : event.kind === 'verify.reverified' ? { accept: event.payload?.accept ?? false, observedExit: event.payload?.verdict?.observedExit ?? null, captureSha: event.payload?.capture?.sha ?? null } : ['lifecycle.turn_completed', 'lifecycle.crashed'].includes(event.kind) ? { status: event.payload?.status ?? event.payload?.result?.status ?? null, reason: String(event.payload?.error ?? event.payload?.reason ?? '').slice(0, 256) } : {} })); }

if (!existsSync(GLM_AUTH)) throw new Error('PENDING-LIVE-no-project-glm-key'); mkdirSync(OUTPUT, { recursive: true });
let driver; let replay; let handle; let result; let report = null; let pid = null; let verify = null; let kill = null; let route = null; let routeObservation = null; let advice = null; let adviceReadOnly = false; let replayRowsCount = 0; let appliedTaskIds = 0; let workerEvents = []; let firstClosed = false; let finalClosed = false; let fatal = null; let pumping = true;
try {
  driver = createDriver(options()); const coordinator = driver.coordinator; const pump = (async () => { const consumed = new Set(); while (pumping) { for (const worker of coordinator.list()) { const id = worker.pendingApprovalId ?? worker.pendingQuestionId; if (!id || consumed.has(id)) continue; consumed.add(id); await coordinator.respond(id, worker.pendingApprovalId ? { decision: 'allow' } : { text: 'Finish the bounded review.' }, 'orchestrator'); } await sleep(100); } })();
  try {
    handle = await coordinator.spawn('glm', brief(), { taskId: TASK_ID, taskType: 'phase44-route-review', runId: RUN_ID, model: 'glm-4.7', effort: 'low', modelPolicy: { allow: ['glm-4.7'], allowFamilies: ['glm'], reasoningEffort: 'low' } });
    await until(async () => (await coordinator.result(handle.id)).ready, 'GLM terminal result'); result = await coordinator.result(handle.id); workerEvents = driver.log.read(handle.id); pid = workerEvents.find((event) => event.kind === 'lifecycle.spawned' && event.actor === 'worker')?.payload?.pid ?? null; verify = workerEvents.find((event) => event.kind === 'verify.reverified') ?? null; route = coordinator._publicHandle(coordinator._workers.get(handle.id)); routeObservation = driver.coordination.routeObservations().find((row) => row.taskId === TASK_ID) ?? null; const sha = verify?.payload?.capture?.sha; if (result.status === 'completed' && verify?.payload?.accept === true && sha) report = git(['show', `${sha}:${TARGET}`]);
  } finally { pumping = false; await pump.catch(() => {}); if (handle) { kill = await coordinator.kill(handle.id, 'policy'); workerEvents = driver.log.read(handle.id); } }
  await until(() => (!pid || !alive(pid)) && !existsSync(join(REPO, '.baton', 'wt', TASK_ID)) && !existsSync(join(REPO, '.baton', 'runtime', handle?.id ?? '')) && git(['branch', '--list', `baton/${TASK_ID}`]) === '', 'GLM resources reaped', 30_000);
  firstClosed = driver.close(); driver = null; replay = createDriver(options()); const replayRows = replay.coordination.routeObservations(); const candidate = { routeKey: routeObservation.routeKey, modelFamily: routeObservation.modelFamily, concurrencyCeiling: 1, inFlight: 0 }; const beforeAdvice = replay.router.snapshot(); advice = await replay.coordinator.invokeCapability('cairn', 'route.advice', { taskType: routeObservation.taskType, observedAt: routeObservation.observedAt, candidates: [candidate] }, { actor: 'orchestrator', budgetTokens: 4000 }); const afterAdvice = replay.router.snapshot(); adviceReadOnly = JSON.stringify(beforeAdvice) === JSON.stringify(afterAdvice); replayRowsCount = replayRows.length; appliedTaskIds = afterAdvice.appliedTaskIds.length; finalClosed = replay.close(); replay = null;
} catch (error) { fatal = String(error?.stack ?? error); try { if (handle && driver) kill ??= await driver.coordinator.kill(handle.id, 'policy'); } catch {} try { driver?.close(); } catch {} try { replay?.close(); } catch {} }
const payload = advice?.payload?.[0] ?? null;
const checks = { runnerHealthy: fatal === null, exactRoute: route?.harnessRequested === 'glm' && route?.modelRequested === 'glm-4.7' && route?.modelResolved === 'glm-4.7' && route?.modelObserved === 'glm-4.7' && route?.effortRequested === 'low' && route?.effortResolved === 'low', credentialedNativePid: Number.isSafeInteger(pid), freshVerified: result?.status === 'completed' && verify?.payload?.accept === true && report?.includes('## Required red tests'), durableObservation: routeObservation?.verifiedWin === true && routeObservation?.routeKey?.includes('glm-4.7'), restartHydratedOnce: replayRowsCount === 1 && appliedTaskIds === 1 && payload?.rows?.[0]?.evidenceCount === 1, cairnSelectedExactRoute: payload?.effectiveMode === 'adaptive' && payload?.selectedRouteKey === routeObservation?.routeKey, adviceReadOnly, killSafe: ['confirmed', 'forced', 'already_dead'].includes(kill?.result), processGone: !pid || !alive(pid), worktreeGone: !existsSync(join(REPO, '.baton', 'wt', TASK_ID)), runtimeGone: !existsSync(join(REPO, '.baton', 'runtime', handle?.id ?? '')), branchGone: git(['branch', '--list', `baton/${TASK_ID}`]) === '', writerAuthorityReleased: firstClosed === true && finalClosed === true && !existsSync(join(LOG_DIR, 'coordination', 'writer.lease')) };
const summary = { at: new Date().toISOString(), repoHead: git(['rev-parse', 'HEAD']), runId: RUN_ID, taskId: TASK_ID, workerId: handle?.id ?? null, pid, result: result ? { status: result.status, ready: result.ready } : null, route: route ? { harnessRequested: route.harnessRequested, harnessResolved: route.harnessResolved, modelRequested: route.modelRequested, modelResolved: route.modelResolved, modelObserved: route.modelObserved, effortRequested: route.effortRequested, effortResolved: route.effortResolved, effortObserved: route.effortObserved } : null, budgetUsed: route?.budgetUsed ?? null, verifyAccept: verify?.payload?.accept ?? false, reportCaptured: Boolean(report), routeObservation: routeObservation ? { taskId: routeObservation.taskId, routeKey: routeObservation.routeKey, modelFamily: routeObservation.modelFamily, verifiedWin: routeObservation.verifiedWin, eventSeq: routeObservation.eventSeq } : null, advice: payload ? { effectiveMode: payload.effectiveMode, selectedRouteKey: payload.selectedRouteKey, evidenceCount: payload.rows?.[0]?.evidenceCount } : null, kill, checks, fatal, pass: Object.values(checks).every(Boolean) };
writeFileSync(join(OUTPUT, 'events.jsonl'), `${bounded(workerEvents).map((event) => JSON.stringify({ taskId: TASK_ID, requestedHarness: 'glm', requestedModel: 'glm-4.7', requestedEffort: 'low', ...event })).join('\n')}\n`); writeFileSync(join(OUTPUT, 'summary.json'), `${JSON.stringify(summary, null, 2)}\n`); if (report) writeFileSync(join(OUTPUT, `${TASK_ID}.md`), report); rmSync(LOG_DIR, { recursive: true, force: true }); console.log(JSON.stringify({ pass: summary.pass, summary }, null, 2)); if (!summary.pass) process.exitCode = 1;
