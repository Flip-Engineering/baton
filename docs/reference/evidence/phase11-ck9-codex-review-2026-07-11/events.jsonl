{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":1,"kind":"runtime.scope_created","actor":"policy","payload":{"root":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1","home":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/home","tmp":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/tmp","config":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/config/codex","family":"codex","strippedEnvKeys":["ANTHROPIC_DEFAULT_HAIKU_MODEL","CLAUDE_CODE_MAX_OUTPUT_TOKENS","GEMINI_API_KEY","OPENROUTER_API_KEY","SECURITYSESSIONID","SSH_AUTH_SOCK"],"projectedEnvKeys":[],"projectedFiles":["auth.json"],"permissions":{"directories":"0700","credentialFiles":"0600"},"sandboxPolicy":"wire-workspaceWrite-network-deny","active":true},"seq":1,"ts":"2026-07-11T20:05:51.598Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":1,"kind":"lifecycle.spawned","actor":"orchestrator","payload":{"taskId":"codex-ck9-crash-window-review","brief":{"goal":"Adversarially review commit c8a272e's coordination crash-window changes against spec/phase11/coordination-knowledge.md and docs/26-full-system-goal.md. Inspect impl/src/coordinator.mjs plus phase11 coordination, persistent-session, and acceptance/integration tests. Write reviews/dogfood/codex-ck9-crash-window-review.md with exact headings \"## Verdict\", \"## Crash-window matrix\", \"## Remaining major findings\", and \"## Required next actions\". Try to falsify pre-effect intent ordering, bounded post-effect ambiguity, restart closure, single-consumer behavior, adapter/PID cleanup, integration Git safety, publication authorization, and claims that CK9 is green. Distinguish this deterministic gate from still-missing product features.","constraints":["Edit only reviews/dogfood/codex-ck9-crash-window-review.md.","Do not modify implementation, tests, specs, task state, or evidence files.","Do not commit, push, deploy, or use network tools.","Ground every finding in exact repository paths and classify critical, major, minor, or no finding.","Keep the review under 1800 words; do not accept green tests as sufficient evidence.","Use at most 18 repository-read/tool calls. Once enough evidence is available, stop exploring and write the review."],"pathScope":["reviews/dogfood/codex-ck9-crash-window-review.md"],"definitionOfDone":"The four exact headings exist and the review explicitly evaluates CK9 crash windows","verification":{"command":"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Verdict$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Crash-window matrix$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Remaining major findings$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Required next actions$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md","expectExit":0,"timeoutMs":10000},"budget":{"tokens":350000,"usd":3,"wallMin":6},"tools":[],"outputFormat":""},"vendorRequested":"codex","vendorResolved":"codex","modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelPolicy":{"allow":["gpt-5.4"],"allowFamilies":["openai"],"reasoningEffort":"high"},"sessionRequest":{"mode":"new"},"lineage":null,"review":null},"seq":2,"ts":"2026-07-11T20:05:51.607Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"lifecycle.turn_started","actor":"orchestrator","payload":{},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":null,"seq":3,"ts":"2026-07-11T20:05:51.607Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"worktree.ready","actor":"orchestrator","payload":{"worktree":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","repoRoot":"/Users/wahargis/Development/Experiments/baton","baseSha":"d7ef59bce1599066602ada311c66cc0c3a71ad4d","branch":"baton/codex-ck9-crash-window-review","ownerTaskId":"codex-ck9-crash-window-review"},"seq":4,"ts":"2026-07-11T20:05:51.744Z"}
{"worker":"w-1","harness":"codex","turnEpoch":0,"kind":"lifecycle.spawned","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","pid":42759,"modelRequested":"gpt-5.4","modelObserved":"gpt-5.4","reasoningEffort":"high","serviceTier":null},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":5,"ts":"2026-07-11T20:05:52.259Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"lifecycle.turn_started","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":6,"ts":"2026-07-11T20:05:52.268Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"I’m reviewing commit `c8a272e` against the phase 11 coordination specs and the targeted tests first, then I’ll write the required gate review into `reviews/dogfood/codex-ck9-crash-window-review.md` only."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":7,"ts":"2026-07-11T20:06:09.239Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_rgtp45ol87aCHmchuTbUGZSh","command":"/bin/zsh -lc 'git show --stat --oneline --name-only c8a272e'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"80555","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"git show --stat --oneline --name-only c8a272e"}],"aggregatedOutput":"c8a272e Close coordination effect crash windows\ndocs/handoff/evidence/phase11-coordination-foundation-2026-07-11.md\nimpl/VALIDATION.md\nimpl/src/coordinator.mjs\nimpl/test/phase11-acceptance-integration.test.mjs\nimpl/test/phase11-coordination-store.test.mjs\nimpl/test/phase11-persistent-sessions.test.mjs\nspec/phase11/coordination-knowledge.md\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'git show --stat --oneline --name-only c8a272e'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":8,"ts":"2026-07-11T20:06:09.296Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_C2KQ8lMGQq0mAQ0ODprjMLVY","command":"/bin/zsh -lc \"rg --files . | rg 'spec/phase11/coordination-knowledge\\\\.md|docs/26-full-system-goal\\\\.md|impl/src/coordinator\\\\.mjs|phase11|persistent-session|acceptance|integration|reviews/dogfood/codex-ck9-crash-window-review\\\\.md'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"58981","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"listFiles","command":"rg --files .","path":"."},{"type":"search","command":"rg \"spec/phase11/coordination-knowledge\\\\.md|docs/26-full-system-goal\\\\.md|impl/src/coordinator\\\\.mjs|phase11|persistent-session|acceptance|integration|reviews/dogfood/codex-ck9-crash-window-review\\\\.md\"","query":"spec/phase11/coordination-knowledge\\.md|docs/26-full-system-goal\\.md|impl/src/coordinator\\.mjs|phase11|persistent-session|acceptance|integration|reviews/dogfood/codex-ck9-crash-window-review\\.md","path":null}],"aggregatedOutput":"./reviews/build/integration-red.md\n./docs/reference/evidence/phase11-grok-governance-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-grok-governance-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-grok-governance-2026-07-11/summary.json\n./docs/reference/evidence/phase11-grok-persistent-session-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-grok-persistent-session-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-grok-persistent-session-2026-07-11/summary.json\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/attempt1-budget-stop.md\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/attempt1-summary.json\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/attempt1-events.jsonl\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-coordination-spec-review-2026-07-11/summary.json\n./docs/reference/evidence/phase11-grok-model-selection-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-grok-model-selection-2026-07-11/attempt-2026-07-11-auth-expired.md\n./docs/reference/evidence/phase11-grok-model-selection-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-grok-model-selection-2026-07-11/summary.json\n./docs/reference/evidence/phase11-coordination-implementation-review-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-coordination-implementation-review-2026-07-11/summary.json\n./docs/reference/evidence/phase11-grok-timeout-reap-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-grok-timeout-reap-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-grok-timeout-reap-2026-07-11/summary.json\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/attempt1-budget-stop.md\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/attempt2-budget-stop.md\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/attempt3-budget-stop.md\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/events.jsonl\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/run.mjs\n./docs/reference/evidence/phase11-ck9-codex-review-2026-07-11/summary.json\n./docs/26-full-system-goal.md\n./docs/handoff/evidence/phase11-acceptance-integration-2026-07-11.md\n./docs/handoff/evidence/phase11-control-integrity-2026-07-11.md\n./docs/handoff/evidence/phase11-coordination-foundation-2026-07-11.md\n./docs/handoff/evidence/phase11-governance-2026-07-11.md\n./docs/handoff/evidence/phase11-persistent-sessions-2026-07-11.md\n./docs/handoff/evidence/phase11-model-selection-2026-07-11.md\n./impl/src/coordinator.mjs\n./impl/test/phase11-concurrent-grok-reap.test.mjs\n./impl/test/phase11-control-integrity.test.mjs\n./impl/test/phase11-model-selection.test.mjs\n./impl/test/phase11-coordination-store.test.mjs\n./impl/test/phase11-governance.test.mjs\n./impl/test/phase11-acceptance-integration.test.mjs\n./impl/test/phase11-persistent-sessions.test.mjs\n./spec/phase11/control-integrity-and-model-selection.md\n./spec/phase11/persistent-sessions.md\n./spec/phase11/coordination-knowledge.md\n./spec/phase11/governance.md\n./spec/phase11/acceptance-integration.md\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"rg --files . | rg 'spec/phase11/coordination-knowledge\\\\.md|docs/26-full-system-goal\\\\.md|impl/src/coordinator\\\\.mjs|phase11|persistent-session|acceptance|integration|reviews/dogfood/codex-ck9-crash-window-review\\\\.md'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":9,"ts":"2026-07-11T20:06:09.310Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":11270,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":11270,"inputTokens":10475,"cachedInputTokens":4480,"outputTokens":795,"reasoningOutputTokens":516},"last":{"totalTokens":11270,"inputTokens":10475,"cachedInputTokens":4480,"outputTokens":795,"reasoningOutputTokens":516},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":11270,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":10,"ts":"2026-07-11T20:06:09.311Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":11,"ts":"2026-07-11T20:06:09.311Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"I have the commit footprint. Next I’m pulling the specific spec clauses, coordinator code paths, and test assertions that speak to the crash-window guarantees the brief calls out."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":12,"ts":"2026-07-11T20:06:14.301Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_8NemuIMg0NIHt3cwSAQapHGR","command":"/bin/zsh -lc 'rg -n \"intent|effect|ambigu|restart|single-consumer|single consumer|adapter|PID|pid|cleanup|publish|authorization|authorized|green|CK9|coordination|claim|git safety|git|crash|window|replay|resume|consumer|lease|heartbeat\" spec/phase11/coordination-knowledge.md spec/phase11/persistent-sessions.md spec/phase11/acceptance-integration.md docs/26-full-system-goal.md impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"43161","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n 'intent|effect|ambigu|restart|single-consumer|single consumer|adapter|PID|pid|cleanup|publish|authorization|authorized|green|CK9|coordination|claim|git safety|git|crash|window|replay|resume|consumer|lease|heartbeat' spec/phase11/coordination-knowledge.md spec/phase11/persistent-sessions.md spec/phase11/acceptance-integration.md docs/26-full-system-goal.md impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs","query":"intent|effect|ambigu|restart|single-consumer|single consumer|adapter|PID|pid|cleanup|publish|authorization|authorized|green|CK9|coordination|claim|git safety|git|crash|window|replay|resume|consumer|lease|heartbeat","path":"coordination-knowledge.md"}],"aggregatedOutput":"spec/phase11/coordination-knowledge.md:1:# Phase 11.4 — durable coordination and knowledge substrate\nspec/phase11/coordination-knowledge.md:9:task, artifact, Scratch, and knowledge mutations are typed coordination events. The append-only\nspec/phase11/coordination-knowledge.md:10:event file is truth; every query result is a replayable projection.\nspec/phase11/coordination-knowledge.md:12:## CK1 — one durable coordination stream\nspec/phase11/coordination-knowledge.md:27:first appends its typed coordination mutation; only then may the in-memory task/artifact/Scratch/KG\nspec/phase11/coordination-knowledge.md:29:cross-worker comparable only through an `evidence.mapped` coordination event whose global sequence\nspec/phase11/coordination-knowledge.md:30:is the observation order. Knowledge temporal checks use the global coordination sequence, never a\nspec/phase11/coordination-knowledge.md:34:operational evidence references at startup: an operational event with no coordination mutation is\nspec/phase11/coordination-knowledge.md:35:telemetry only and cannot affect task/KG truth; a coordination event pointing to missing or\nspec/phase11/coordination-knowledge.md:40:Construction validates the complete coordination stream immediately: JSON parse, schema version,\nspec/phase11/coordination-knowledge.md:45:## CK2 — typed durable task DAG and claims\nspec/phase11/coordination-knowledge.md:51:createdEvent, claimedEvent, terminalEvent, artifactIds[]\nspec/phase11/coordination-knowledge.md:57:`claimTask(id, worker, expectedVersion, key)` is compare-and-swap: exactly one concurrent caller\nspec/phase11/coordination-knowledge.md:58:wins; stale, blocked, already-assigned, and terminal claims are typed refusals and do not append.\nspec/phase11/coordination-knowledge.md:63:the durable task `assignee` remains null until dispatch wins `claimTask`; reservation is not claim.\nspec/phase11/coordination-knowledge.md:64:Dispatch durably claims it. Input waits, recovery, cancellation, verification\nspec/phase11/coordination-knowledge.md:69:Every adapter- or outside-world-reaching action has a durable intent before the effect: stop,\nspec/phase11/coordination-knowledge.md:71:request, and publication authorization. If intent append fails, no adapter, worktree/Git merge, or\nspec/phase11/coordination-knowledge.md:72:publisher is called. If an adapter has already confirmed stop/attach/turn advancement when a later\nspec/phase11/coordination-knowledge.md:74:typed coordination-unavailable result, kills or quarantines the ambiguous transport, and restart\nspec/phase11/coordination-knowledge.md:75:closes the still-nonterminal durable task. Asynchronous adapter callbacks may not turn an already\nspec/phase11/coordination-knowledge.md:76:recorded fatal coordination fault into an uncaught process exception.\nspec/phase11/coordination-knowledge.md:79:policy, and the reserved public handle ID. `task.claimed` persists assignee, resolved vendor/model,\nspec/phase11/coordination-knowledge.md:109:- advisory claims with resource glob, owner worker/task, intent, `envRef`, supervisor fence,\nspec/phase11/coordination-knowledge.md:110:  version, and lease deadline;\nspec/phase11/coordination-knowledge.md:112:  claim was observed on another tree; and\nspec/phase11/coordination-knowledge.md:113:- explicit `scratch.claim_expired` / `scratch.fact_expired` events.\nspec/phase11/coordination-knowledge.md:115:There is no reader-clock expiry, heartbeat verb, free-text fleet chat, mutable world-state blob, or\nspec/phase11/coordination-knowledge.md:117:lease/terminal state, so replay at different wall times is identical. Path-resource conflicts use\nspec/phase11/coordination-knowledge.md:119:OS resources remain the sandbox/allocator's job, not an advisory Scratch claim.\nspec/phase11/coordination-knowledge.md:123:Live worktree paths are invalid environment references; callers snapshot `git write-tree` (or an\nspec/phase11/coordination-knowledge.md:127:Normative path-claim overlap is conservative: normalize repository-relative POSIX paths; exact\nspec/phase11/coordination-knowledge.md:135:The graph is a materialized view over coordination events. It supports typed nodes:\nspec/phase11/coordination-knowledge.md:150:Every node and edge stores observation time (coordination seq/time), event time (referenced source\nspec/phase11/coordination-knowledge.md:176:- consequential human/orchestrator spawn, reroute, accept, integrate, publish, abort → Decision;\nspec/phase11/coordination-knowledge.md:187:recall utility, and read-contamination blast radius separately—never one unexplained green bit.\nspec/phase11/coordination-knowledge.md:203:coordination}`. The coordinator receives it in its constructor and invokes one non-optional\nspec/phase11/coordination-knowledge.md:204:state-changing integration point for spawn/create, dispatch/claim, input wait/resume, terminal,\nspec/phase11/coordination-knowledge.md:206:coordination event is a contract failure, not an optional sink failure. Direct mutation methods\nspec/phase11/coordination-knowledge.md:212:## CK9 — zero-quota safety gate\nspec/phase11/coordination-knowledge.md:217:2. queued/unassigned tasks and dependency readiness survive restart;\nspec/phase11/coordination-knowledge.md:218:3. claim CAS has exactly one winner and terminal states cannot reopen;\nspec/phase11/coordination-knowledge.md:221:6. Scratch conflicts, explicit lease expiry, replay determinism, and cross-tree warnings;\nspec/phase11/coordination-knowledge.md:226:9. a full restart has identical task/artifact/Scratch/KG projections before any new dispatch.\nspec/phase11/coordination-knowledge.md:227:10. an unsatisfied-dependency task crashes before dispatch and replays pending, unassigned, with\nspec/phase11/coordination-knowledge.md:229:11. a multi-task DAG replays with the same ready set and cannot dispatch a dependent early;\nspec/phase11/coordination-knowledge.md:232:13. every public spawn/claim/input/terminal/capture/review/integration/publication state change has\nspec/phase11/coordination-knowledge.md:233:    a coordination event, and the test fails if coordinator state is nonempty while the substrate\nspec/phase11/coordination-knowledge.md:239:16. injected append failures cover create, claim, input wait/resume, stop intent, cancel terminal,\nspec/phase11/coordination-knowledge.md:241:    integration intent, publication authorization, and knowledge/Scratch reads. Pre-effect\nspec/phase11/coordination-knowledge.md:242:    failures call no adapter/Git/publisher; post-effect failures are bounded, poison authority,\nspec/phase11/coordination-knowledge.md:243:    preserve the earlier intent, and replay to a non-success terminal state.\nspec/phase11/coordination-knowledge.md:245:After CK9 is green and committed, Baton may recursively run a real provider review against this\nspec/phase11/coordination-knowledge.md:248:kill/process/worktree/branch cleanup.\nimpl/test/phase11-persistent-sessions.test.mjs:16:import { CoordinationStore } from '../src/coordination-store.mjs';\nimpl/test/phase11-persistent-sessions.test.mjs:36:    sessions: { multiTurn: 'native', resume: 'native', fork: 'native' },\nimpl/test/phase11-persistent-sessions.test.mjs:40:function adapter(over = {}) {\nimpl/test/phase11-persistent-sessions.test.mjs:59:  const coordination = new CoordinationStore(mkdtempSync(join(tmpdir(), 'baton-ps-coordination-')), {\nimpl/test/phase11-persistent-sessions.test.mjs:64:    log, fences: new FenceTable(), adapters: { session: ad }, coordination,\nimpl/test/phase11-persistent-sessions.test.mjs:75:  return { c, log, verifyCalls, coordination };\nimpl/test/phase11-persistent-sessions.test.mjs:89:  return { status: 'completed', summary, artifacts: { files: [] }, verification: { command: 'true', claimedExit: 0 } };\nimpl/test/phase11-persistent-sessions.test.mjs:93:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:94:  const { c, log, verifyCalls, coordination } = harness(ad);\nimpl/test/phase11-persistent-sessions.test.mjs:96:  ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'session-1', pid: 123 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:111:  assert.equal(coordination.snapshot().tasks.length, 2);\nimpl/test/phase11-persistent-sessions.test.mjs:112:  assert.deepEqual(coordination.snapshot().tasks.map((task) => task.status), ['completed', 'completed']);\nimpl/test/phase11-persistent-sessions.test.mjs:113:  assert.equal(coordination.snapshot().tasks[1].refines, 'follow-up');\nimpl/test/phase11-persistent-sessions.test.mjs:115:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:116:    log, fences: new FenceTable(), adapters: { session: adapter() }, coordination,\nimpl/test/phase11-persistent-sessions.test.mjs:120:  assert.equal(replay.list()[0].taskId, coordination.snapshot().tasks[1].id);\nimpl/test/phase11-persistent-sessions.test.mjs:121:  assert.equal((await replay.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:122:  assert.equal((await replay.result(h.id)).ready, true);\nimpl/test/phase11-persistent-sessions.test.mjs:126:  const ad = adapter({ prompt: async () => ({ ok: false, reason: 'busy' }) });\nimpl/test/phase11-persistent-sessions.test.mjs:141:  const ad = adapter({ prompt: async () => { throw new Error('wire write failed'); } });\nimpl/test/phase11-persistent-sessions.test.mjs:152:test('CK8/CK9: follow-up refinement failure kills an already-advanced native session', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:153:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:154:  const { c, coordination } = harness(ad);\nimpl/test/phase11-persistent-sessions.test.mjs:158:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-persistent-sessions.test.mjs:159:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-persistent-sessions.test.mjs:163:  await assert.rejects(c.send(h.id, 'advance native state', 'turn'), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:167:  assert.equal(coordination.snapshot().tasks.length, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:168:  assert.equal(coordination.snapshot().tasks[0].status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:169:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'follow_up.requested'), true);\nimpl/test/phase11-persistent-sessions.test.mjs:172:test('PS2: emitted turn facts followed by refusal are a protocol violation and kill the ambiguous session', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:174:  ad = adapter({\nimpl/test/phase11-persistent-sessions.test.mjs:191:test('PS3: interrupt-with-follow-up reopens coordinator state before the adapter follow-up terminal', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:192:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:208:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:222:test('PS5: wire sessionRef reaches handle/result and survives terminal replay', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:223:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:224:  const { c, log, coordination } = harness(ad);\nimpl/test/phase11-persistent-sessions.test.mjs:226:  ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'native-session-7', pid: 123 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:233:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:234:    log, coordination, fences: new FenceTable(), adapters: { session: adapter() },\nimpl/test/phase11-persistent-sessions.test.mjs:238:  assert.deepEqual(replay.list()[0].sessionRef, expected);\nimpl/test/phase11-persistent-sessions.test.mjs:239:  assert.deepEqual(replay.list()[0].sessionRequest, { mode: 'new' });\nimpl/test/phase11-persistent-sessions.test.mjs:240:  assert.equal(replay.list()[0].status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:241:  assert.deepEqual(await replay.send(h.id, 'must not reach an unattached adapter', 'turn'), { ok: false, result: 'worker_not_active' });\nimpl/test/phase11-persistent-sessions.test.mjs:242:  assert.equal((await replay.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:255:    const coordination = new CoordinationStore(mkdtempSync(join(tmpdir(), `baton-ps-${name}-coordination-`)), {\nimpl/test/phase11-persistent-sessions.test.mjs:259:      log, coordination, fences: new FenceTable(), adapters: { [name]: ad },\nimpl/test/phase11-persistent-sessions.test.mjs:276:    const pid = firstSpawn?.payload?.pid;\nimpl/test/phase11-persistent-sessions.test.mjs:278:    assert.ok(pid && sessionRef, `${name}: first turn must expose native PID/session identity`);\nimpl/test/phase11-persistent-sessions.test.mjs:284:    const pids = new Set(log.read(h.id).filter((e) => e.kind === 'lifecycle.spawned' && e.actor === 'worker').map((e) => e.payload?.pid).filter(Boolean));\nimpl/test/phase11-persistent-sessions.test.mjs:285:    assert.deepEqual([...pids], [pid], `${name}: second turn must not respawn`);\nimpl/test/phase11-persistent-sessions.test.mjs:293:test('PS6: unsupported resume/fork policy fails typed before worker allocation', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:294:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:295:  ad.card = () => ({ ...card(), sessions: { multiTurn: 'native', resume: 'native', fork: 'planned' } });\nimpl/test/phase11-persistent-sessions.test.mjs:304:test('PS6: Claude maps per-task resume and fork to native session identities', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:305:  for (const mode of ['resume', 'fork']) {\nimpl/test/phase11-persistent-sessions.test.mjs:318:test('PS6: Codex maps resume and fork to thread/resume and thread/fork', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:319:  for (const mode of ['resume', 'fork']) {\nimpl/test/phase11-persistent-sessions.test.mjs:335:test('PS6: Grok maps resume to ACP session/load', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:339:  t.after(() => cli.kill('grok-resume').catch(() => {}));\nimpl/test/phase11-persistent-sessions.test.mjs:340:  const ack = await cli.spawn('grok-resume', brief(), { worktree: tmpdir(), session: { mode: 'resume', id: 'grok-parent' } });\nimpl/test/phase11-persistent-sessions.test.mjs:346:test('PS8: coordinator resume requires and reuses a validated worktree without creating another', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:347:  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-resume-wt-'));\nimpl/test/phase11-persistent-sessions.test.mjs:348:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:353:    create: async () => { creates += 1; throw new Error('resume must not create'); },\nimpl/test/phase11-persistent-sessions.test.mjs:358:    () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session' } }),\nimpl/test/phase11-persistent-sessions.test.mjs:363:    () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session', context: { worktree: wt } } }),\nimpl/test/phase11-persistent-sessions.test.mjs:370:    taskId: 'resumed-task', refines: 'original-task', session: { mode: 'resume', id: 'external-session', context },\nimpl/test/phase11-persistent-sessions.test.mjs:375:  assert.deepEqual(h.lineage, { relation: 'resume', parentSessionId: 'external-session', parentTaskId: 'original-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:382:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:399:test('PS7: replayed session is reattached only after bounded handshake proves the same native identity', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:401:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:402:  const { c, log, coordination } = harness(original, undefined, {\nimpl/test/phase11-persistent-sessions.test.mjs:407:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:408:  original.emit(h.id, 'lifecycle.turn_completed', completed('before restart'), 1);\nimpl/test/phase11-persistent-sessions.test.mjs:411:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:412:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:413:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 222 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:414:    resumed.emit(worker, 'lifecycle.turn_started', { sessionId: 'recover-native' }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:417:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:418:    log, coordination, fences: new FenceTable(), adapters: { session: resumed }, repoRoot: null,\nimpl/test/phase11-persistent-sessions.test.mjs:427:  assert.equal(replay.list()[0].status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:428:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:431:  assert.equal(replay.list()[0].status, 'working');\nimpl/test/phase11-persistent-sessions.test.mjs:432:  assert.equal(replay.list()[0].sessionRef.id, 'recover-native');\nimpl/test/phase11-persistent-sessions.test.mjs:436:test('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:437:  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\nimpl/test/phase11-persistent-sessions.test.mjs:438:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:439:  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\nimpl/test/phase11-persistent-sessions.test.mjs:440:  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:442:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:446:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:448:  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\nimpl/test/phase11-persistent-sessions.test.mjs:449:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:450:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:454:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-persistent-sessions.test.mjs:455:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-persistent-sessions.test.mjs:456:    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\nimpl/test/phase11-persistent-sessions.test.mjs:459:  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:461:  assert.equal(replay._workers.get(h.id).status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:464:test('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:466:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:467:  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\nimpl/test/phase11-persistent-sessions.test.mjs:470:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:474:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:475:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:476:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:479:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:480:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:484:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-persistent-sessions.test.mjs:485:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-persistent-sessions.test.mjs:489:  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:490:  await until(() => resumed.calls.kill.length === 1);\nimpl/test/phase11-persistent-sessions.test.mjs:491:  assert.equal(replay._workers.get(h.id).status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:496:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:497:  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\nimpl/test/phase11-persistent-sessions.test.mjs:500:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'expected-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:504:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:505:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:506:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'wrong-native', pid: 333 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:509:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:510:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:514:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:517:  assert.equal(replay.list()[0].status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:518:  assert.equal(resumed.calls.kill.length, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:522:test('PS7: a hung reattachment is bounded, remains orphaned, and invokes adapter cleanup', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:524:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:525:  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\nimpl/test/phase11-persistent-sessions.test.mjs:528:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'timeout-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:532:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:533:  resumed.spawn = async () => new Promise(() => {});\nimpl/test/phase11-persistent-sessions.test.mjs:534:  const replay = new Coordinator({\nimpl/test/phase11-persistent-sessions.test.mjs:535:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:539:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:542:  assert.equal(replay.list()[0].status, 'orphaned');\nimpl/test/phase11-persistent-sessions.test.mjs:543:  assert.equal(resumed.calls.kill.length, 1);\nspec/phase11/acceptance-integration.md:3:## AC1 — red then green is measured at pinned commits\nspec/phase11/acceptance-integration.md:35:SHAs and marks the task integrated. Before branch cleanup Baton pins the captured SHA under\nspec/phase11/acceptance-integration.md:36:`refs/baton/results/<sha>`; success releases the pin because main retains it, while refusal keeps\nspec/phase11/acceptance-integration.md:42:single-consumer approval containing remote/ref/SHA; only an explicit allow response may invoke the\nspec/phase11/acceptance-integration.md:43:side effect. Deny, timeout, replay, stale fence, or missing approval performs no push. Secret\nspec/phase11/acceptance-integration.md:49:Temp-repo tests prove base/result freshness, coverage, mutation parsing, sandbox cleanup,\nimpl/test/phase11-acceptance-integration.test.mjs:9:import { MockAdapter } from '../src/adapter.mjs';\nimpl/test/phase11-acceptance-integration.test.mjs:11:function git(args, cwd) { return execFileSync('git', args, { cwd, encoding: 'utf8' }).trim(); }\nimpl/test/phase11-acceptance-integration.test.mjs:14:  git(['init', '-q'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:15:  git(['config', 'user.email', 'baton-test@example.com'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:16:  git(['config', 'user.name', 'Baton Test'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:25:  git(['add', '-A'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:26:  git(['commit', '--allow-empty', '-q', '-m', 'base'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:30:    goal: 'make the pinned check newly pass', constraints: [], pathScope: ['src/**'], definitionOfDone: 'check is red then green',\nimpl/test/phase11-acceptance-integration.test.mjs:45:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/integrated.txt', content: 'accepted\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:47:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:57:  const adapter = new MockAdapter({ scenario });\nimpl/test/phase11-acceptance-integration.test.mjs:58:  const baseCard = adapter.card.bind(adapter);\nimpl/test/phase11-acceptance-integration.test.mjs:59:  adapter.card = () => ({\nimpl/test/phase11-acceptance-integration.test.mjs:66:  return adapter;\nimpl/test/phase11-acceptance-integration.test.mjs:74:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/publish.txt', content: 'publish me\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:76:    repoRoot: root, logDir, adapters: { mock: adapter }, now, approvalTimeoutMs,\nimpl/test/phase11-acceptance-integration.test.mjs:77:    publisher: async (target) => { calls.push(target); return { transport: 'test-publisher' }; },\nimpl/test/phase11-acceptance-integration.test.mjs:80:  const handle = await driver.coordinator.spawn('mock', brief({ command: 'test -f src/publish.txt', expectExit: 0 }), { taskId: 'publish-task' });\nimpl/test/phase11-acceptance-integration.test.mjs:86:test('AC1: createDriver requireRedGreen proves base red and result green', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:89:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/new.txt', content: 'ok\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:91:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac1-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:94:  const h = await coordinator.spawn('mock', brief({ command: 'test -f src/new.txt', expectExit: 0 }), { taskId: 'red-green' });\nimpl/test/phase11-acceptance-integration.test.mjs:108:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:110:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:128:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:130:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2b-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:146:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:148:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:164:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:166:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3b-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:179:test('AC4: independent oracle receives immutable spec/git evidence and unlocks required integration', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:189:    adapters: { implementer, oracle }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:226:    adapters: { implementer, fallback }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:242:test('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:250:  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-acceptance-integration.test.mjs:253:  const rawAppend = driver.coordination._appendFile;\nimpl/test/phase11-acceptance-integration.test.mjs:254:  driver.coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-acceptance-integration.test.mjs:260:  }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-acceptance-integration.test.mjs:262:  assert.deepEqual(driver.coordination.snapshot().tasks.map((task) => [task.id, task.status]), [['review-fault-parent', 'completed']]);\nimpl/test/phase11-acceptance-integration.test.mjs:268:  const beforeSha = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:269:  const { coordinator, coordination, log, handle } = await completedTask(root);\nimpl/test/phase11-acceptance-integration.test.mjs:278:  assert.equal(git(['rev-parse', 'HEAD'], root), response.integration.resultSha);\nimpl/test/phase11-acceptance-integration.test.mjs:282:  assert.equal(git(['branch', '--list', 'baton/integrate-me'], root), '');\nimpl/test/phase11-acceptance-integration.test.mjs:283:  assert.equal(git(['for-each-ref', '--format=%(refname)', `refs/baton/results/${response.integration.resultSha}`], root), '');\nimpl/test/phase11-acceptance-integration.test.mjs:288:  assert.equal(coordination.snapshot().artifacts.some((artifact) => artifact.mediaType === 'application/vnd.baton.integration+json'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:289:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.completed'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:290:  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:integrate:')), true);\nimpl/test/phase11-acceptance-integration.test.mjs:291:  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'integration'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:297:  const { coordinator, coordination, log, handle } = await completedTask(root, 'diverged-task');\nimpl/test/phase11-acceptance-integration.test.mjs:299:  git(['add', 'main-only.txt'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:300:  git(['commit', '-q', '-m', 'advance main independently'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:301:  const mainBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:309:  assert.equal(git(['rev-parse', 'HEAD'], root), mainBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:311:  assert.equal(git(['show', '-s', '--format=%s', 'HEAD'], root), 'advance main independently');\nimpl/test/phase11-acceptance-integration.test.mjs:316:  assert.equal(git(['show-ref', '--verify', refusedResult.retainedResultRef], root).split(' ')[0], taskSha);\nimpl/test/phase11-acceptance-integration.test.mjs:317:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.refused'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:325:  const headBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:326:  const statusBefore = git(['status', '--porcelain=v1'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:329:  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:330:  assert.equal(git(['status', '--porcelain=v1'], root), statusBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:333:  assert.equal(git(['show-ref', '--verify', retained], root).endsWith(` ${retained}`), true);\nimpl/test/phase11-acceptance-integration.test.mjs:339:  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\nimpl/test/phase11-acceptance-integration.test.mjs:341:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-reject-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:353:test('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:356:  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\nimpl/test/phase11-acceptance-integration.test.mjs:357:  const headBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:359:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-acceptance-integration.test.mjs:360:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-acceptance-integration.test.mjs:361:    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:364:  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-acceptance-integration.test.mjs:365:  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:368:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:371:test('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:372:  const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:388:  assert.deepEqual(new Set([a.result, b.result]), new Set(['published', 'already_resolved']));\nimpl/test/phase11-acceptance-integration.test.mjs:391:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'publication.completed'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:392:  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:publish:')), true);\nimpl/test/phase11-acceptance-integration.test.mjs:393:  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'publication'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:396:test('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:397:  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:399:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-acceptance-integration.test.mjs:400:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-acceptance-integration.test.mjs:401:    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:406:    (error) => error.code === 'coordination_write_unavailable',\nimpl/test/phase11-acceptance-integration.test.mjs:410:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:413:test('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:414:  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:416:  const rawAppend = coordination._appendFile;\nimpl/test/phase11-acceptance-integration.test.mjs:417:  coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-acceptance-integration.test.mjs:423:    (error) => error.code === 'coordination_write_unavailable',\nimpl/test/phase11-acceptance-integration.test.mjs:427:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:428:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:431:test('AC6: deny and timeout are fail-closed and never call the publisher', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:461:test('AC6: restart drops a pending approval and cannot publish it by replay', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:464:  const replayCalls = [];\nimpl/test/phase11-acceptance-integration.test.mjs:465:  const replay = createDriver({\nimpl/test/phase11-acceptance-integration.test.mjs:467:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-acceptance-integration.test.mjs:468:    publisher: async (target) => { replayCalls.push(target); }, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:470:  const response = await replay.coordinator.respond(request.requestId, { decision: 'allow', fence: request.fence }, 'test-user');\nimpl/test/phase11-acceptance-integration.test.mjs:472:  assert.equal(replayCalls.length, 0);\nimpl/test/phase11-acceptance-integration.test.mjs:479:    () => coordinator.requestPublication(handle.id, { remote: 'https://token@example.com/repo.git', ref: 'refs/heads/main' }),\nspec/phase11/persistent-sessions.md:1:# Phase 11.1 — persistent sessions, follow-up turns, resume, and fork\nspec/phase11/persistent-sessions.md:3:The session adapters already survive more than one turn. These contracts make that capability\nspec/phase11/persistent-sessions.md:13:failed verification and after a confirmed soft interrupt, but never after kill, crash, timeout,\nspec/phase11/persistent-sessions.md:14:or unattached replay.\nspec/phase11/persistent-sessions.md:18:The coordinator changes idle/terminal state to working only after the adapter returns\nspec/phase11/persistent-sessions.md:26:confirmation both arrive, the coordinator reopens the task before the adapter's automatic\nspec/phase11/persistent-sessions.md:32:Coordinator turn generations are authoritative. Each adapter wire epoch is mapped to the current\nspec/phase11/persistent-sessions.md:41:on handle/result/events/replay. The reference includes vendor, kind, id, persistence posture, and\nspec/phase11/persistent-sessions.md:44:## PS6 — resume and fork are explicit spawn policies\nspec/phase11/persistent-sessions.md:46:`spawn(..., {session:{mode,id,lastTurnId?}})` supports `new`, `resume`, and `fork` when the card\nspec/phase11/persistent-sessions.md:47:declares them. Claude maps resume/fork to `--resume ID [--fork-session]`; Codex maps to\nspec/phase11/persistent-sessions.md:48:`thread/resume`/`thread/fork`; Grok maps resume to `session/load`. Grok's documented\nspec/phase11/persistent-sessions.md:54:## PS7 — restart recovery is honest before it is automatic\nspec/phase11/persistent-sessions.md:56:Replay restores session references but does not claim control until an adapter proves reattachment.\nspec/phase11/persistent-sessions.md:57:`recover(worker, {timeoutMs?,context?})` attempts bounded native resume/rejoin when explicitly\nspec/phase11/persistent-sessions.md:60:or timeout kills the untrusted transport and leaves the handle orphaned. PID identity requires a\nspec/phase11/persistent-sessions.md:61:birth token or native session handshake; a replayed numeric PID alone is never signalled because\nspec/phase11/persistent-sessions.md:62:of PID reuse.\nspec/phase11/persistent-sessions.md:68:and explicit parent-session/refinement edge. Vendor history resume never\nspec/phase11/persistent-sessions.md:76:follow-up on the same native session and PID, a second independent verification, then confirmed\nspec/phase11/persistent-sessions.md:77:kill/reap. Native resume/fork probes follow only after persisted references and worktree-coherence\nspec/phase11/persistent-sessions.md:78:checks are green.\ndocs/26-full-system-goal.md:24:knowledge, and give workers the complete capability and representation planes below. Every claim\ndocs/26-full-system-goal.md:39:Recursive dogfooding resumes only after the lifecycle, provenance, and credential gates relevant\ndocs/26-full-system-goal.md:40:to the recursive run are green.\ndocs/26-full-system-goal.md:47:- Ordered delivery, human-over-policy fencing, single-consumer interactions, two-phase stop,\ndocs/26-full-system-goal.md:49:- Persistent multi-turn workers; interrupt-follow-up; resume, fork, rejoin, checkpoint, rewind, and\ndocs/26-full-system-goal.md:50:  crash recovery without fabricating uniform semantics across vendors.\ndocs/26-full-system-goal.md:51:- Restart-safe task/worker identity, pending interactions, fences, adapter/session references,\ndocs/26-full-system-goal.md:53:- Dependency DAG, refinement links, idempotent claims, artifact registry, path leases, and\ndocs/26-full-system-goal.md:66:  verification, routing statistics, replay, scorecards, and commit attribution; and\ndocs/26-full-system-goal.md:70:`--model`/ACP model state, and future adapters’ native mechanism.\ndocs/26-full-system-goal.md:76:  resume/fork/rewind, config-home isolation, and live capability discovery.\ndocs/26-full-system-goal.md:77:- Codex: app-server sessions, steer/interrupt, approvals/questions, thread inject/resume/fork,\ndocs/26-full-system-goal.md:84:- Honest one-shot tier and future ACP adapters remain separately carded; no reduced tier may pose\ndocs/26-full-system-goal.md:90:  isolated harness homes, and approval-gated outside-world side effects.\ndocs/26-full-system-goal.md:92:- Wall, token, USD, rate-limit, account-seat, and quota-window budgets folded into authoritative\ndocs/26-full-system-goal.md:96:- Correct provenance: hub facts, worker prose, external evidence, and derived claims never share a\ndocs/26-full-system-goal.md:102:- Fresh-result verification plus base red→green, coverage-of-change, mutation strength,\ndocs/26-full-system-goal.md:106:- Verified branch integration, textual then structured/semantic merge, conflict handling, effect\ndocs/26-full-system-goal.md:112:  version; only verified outcomes learn; idempotent replay preserves them.\ndocs/26-full-system-goal.md:120:- **Operational:** append-only event ledger, resumable cursors, replay, artifacts, and telemetry.\ndocs/26-full-system-goal.md:121:- **Coordinative:** transactional task DAG, artifact manifest, claims/leases, refinements, and\ndocs/26-full-system-goal.md:124:- **Scratch:** TTL/CAS/take/notify blackboard and memoized Bench for fleet-live facts and leases.\ndocs/26-full-system-goal.md:138:- run scorecards expose graph and fleet health with metric breakdowns rather than one green bit.\ndocs/26-full-system-goal.md:147:2. **Vantage:** DAP observation plans, causal debugging results, record/replay assets, exclusive\ndocs/26-full-system-goal.md:148:   debuggee leases, and reproducible postmortems.\ndocs/26-full-system-goal.md:150:   trusted kernels, full dependency-closure hashes, and no “proven” claim over worker-weakened\ndocs/26-full-system-goal.md:152:4. **Scratch:** operational blackboard/coordination REPL and memoized experiment Bench.\ndocs/26-full-system-goal.md:169:- R5 behavioral fingerprints from differential/property/fuzz execution and effect signatures;\ndocs/26-full-system-goal.md:190:  Authentication binds a stable user/session identity; authorization scopes commands and repo/run\ndocs/26-full-system-goal.md:192:  TLS, secure session/token storage, expiry/revocation, origin and CSRF checks, replay protection,\ndocs/26-full-system-goal.md:205:1. Repair P0 control integrity: immutable briefs, truthful responses, crash-safe idempotent kill,\ndocs/26-full-system-goal.md:206:   provenance, story identity, replay identity, and complete cleanup.\ndocs/26-full-system-goal.md:208:3. Make persistent sessions genuinely usable through the driver; add resume/fork/rejoin.\nimpl/test/phase11-coordination-store.test.mjs:8:import { CoordinationIntegrityError, CoordinationRefusal, CoordinationStore } from '../src/coordination-store.mjs';\nimpl/test/phase11-coordination-store.test.mjs:14:const dir = () => mkdtempSync(join(tmpdir(), 'baton-coordination-'));\nimpl/test/phase11-coordination-store.test.mjs:35:test('CK8: direct Coordinator construction refuses an optional coordination sidecar', () => {\nimpl/test/phase11-coordination-store.test.mjs:36:  assert.throws(() => new Coordinator({}), /requires a durable coordination store/);\nimpl/test/phase11-coordination-store.test.mjs:37:  assert.throws(() => new Coordinator({ coordination: {} }), /missing snapshot/);\nimpl/test/phase11-coordination-store.test.mjs:43:  assert.deepEqual(store.snapshot(), { tasks: [], artifacts: [], evidence: [], scratch: { facts: [], claims: [], reads: [] }, knowledge: { nodes: [], edges: [], reads: [], contamination: [] }, lastSeq: 0 });\nimpl/test/phase11-coordination-store.test.mjs:46:test('CK8/CK9: public spawn poisons before publishing any handle when task creation cannot append', async () => {\nimpl/test/phase11-coordination-store.test.mjs:48:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:49:  const coordination = new CoordinationStore(dir(), { appendFile: () => { throw new Error('coordination disk full'); } });\nimpl/test/phase11-coordination-store.test.mjs:50:  const driver = createDriver({ repoRoot: repo, logDir: dir(), coordination, adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:51:  const brief = { goal: 'must not publish', constraints: [], pathScope: [], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:52:  await assert.rejects(driver.coordinator.spawn('mock', brief, { taskId: 'create-write-failure' }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:53:  assert.deepEqual(coordination.snapshot().tasks, []);\nimpl/test/phase11-coordination-store.test.mjs:54:  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:57:test('CK8/CK9: claim append failure poisons before adapter/worktree dispatch and replays pending', async () => {\nimpl/test/phase11-coordination-store.test.mjs:59:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:60:  const coordination = new CoordinationStore(dir(), {\nimpl/test/phase11-coordination-store.test.mjs:62:      if (body.includes('\"task.claimed\"')) throw new Error('claim disk full');\nimpl/test/phase11-coordination-store.test.mjs:66:  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\nimpl/test/phase11-coordination-store.test.mjs:69:    log: new Log(dir()), fences: new FenceTable(), adapters: { mock: adapter }, coordination,\nimpl/test/phase11-coordination-store.test.mjs:74:  await assert.rejects(coordinator.spawn('mock', brief, { taskId: 'claim-write-failure' }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:75:  assert.equal(coordination.task('claim-write-failure').status, 'pending');\nimpl/test/phase11-coordination-store.test.mjs:76:  assert.equal(coordination.task('claim-write-failure').assignee, null);\nimpl/test/phase11-coordination-store.test.mjs:78:  assert.throws(() => coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:81:test('CK1/CK9: operational append failure poisons the coordinator and restart closes the claimed task', async () => {\nimpl/test/phase11-coordination-store.test.mjs:83:  const coordination = new CoordinationStore(dir());\nimpl/test/phase11-coordination-store.test.mjs:89:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-coordination-store.test.mjs:90:    coordination,\nimpl/test/phase11-coordination-store.test.mjs:99:  assert.equal(coordination.task('append-failure').status, 'working', 'the durable claim records the exact crash window');\nimpl/test/phase11-coordination-store.test.mjs:102:  const replay = make(new Log(logRoot));\nimpl/test/phase11-coordination-store.test.mjs:103:  assert.equal(coordination.task('append-failure').status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:104:  assert.equal(replay.list()[0].status, 'exited');\nimpl/test/phase11-coordination-store.test.mjs:107:test('CK1/CK9: terminal artifact-batch failure poisons the driver and restarts as durable failed', async () => {\nimpl/test/phase11-coordination-store.test.mjs:109:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:110:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:111:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:113:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:114:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:116:  const make = (adapters) => createDriver({ repoRoot: repo, logDir, adapters, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:118:  const rawAppend = driver.coordination._appendFile;\nimpl/test/phase11-coordination-store.test.mjs:120:  driver.coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-coordination-store.test.mjs:129:  await until(() => driver.coordination.task('terminal-batch-failure')?.status === 'failed');\nimpl/test/phase11-coordination-store.test.mjs:131:  assert.equal(driver.coordination.snapshot().artifacts.length, 0);\nimpl/test/phase11-coordination-store.test.mjs:132:  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:134:  const replay = make({ mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) });\nimpl/test/phase11-coordination-store.test.mjs:135:  assert.equal(replay.coordination.task('terminal-batch-failure').status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:136:  assert.equal((await replay.coordinator.result(handle.id)).status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:155:test('CK2: queued DAG and ready set replay with exact deps and reserved handle', () => {\nimpl/test/phase11-coordination-store.test.mjs:161:  const replay = new CoordinationStore(root);\nimpl/test/phase11-coordination-store.test.mjs:162:  assert.deepEqual(replay.task('child').deps, ['base']);\nimpl/test/phase11-coordination-store.test.mjs:163:  assert.equal(replay.task('child').reservedWorkerId, 'w-child');\nimpl/test/phase11-coordination-store.test.mjs:164:  assert.equal(replay.task('child').assignee, null);\nimpl/test/phase11-coordination-store.test.mjs:165:  assert.deepEqual(replay.readyTasks().map((task) => task.id), ['base']);\nimpl/test/phase11-coordination-store.test.mjs:168:test('CK2: claim CAS has one winner; stale/blocked refusals append nothing', () => {\nimpl/test/phase11-coordination-store.test.mjs:171:  const winner = store.claimTask('a', 'w1', 1, { actor: 'orchestrator', key: 'claim-a' });\nimpl/test/phase11-coordination-store.test.mjs:174:  assert.throws(() => store.claimTask('a', 'w2', 1, { actor: 'orchestrator', key: 'claim-b' }), (error) => error instanceof CoordinationRefusal && error.code === 'stale_version');\nimpl/test/phase11-coordination-store.test.mjs:178:test('CK2: terminal state is immutable across replay', () => {\nimpl/test/phase11-coordination-store.test.mjs:182:  store.claimTask('a', 'w1', 1, { actor: 'orchestrator', key: 'claim' });\nimpl/test/phase11-coordination-store.test.mjs:188:test('CK8/CK9: public driver exposes coordination and queued DAG survives restart before dispatch', async () => {\nimpl/test/phase11-coordination-store.test.mjs:190:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:191:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:192:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:194:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:195:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:199:    adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 }, scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-coordination-store.test.mjs:206:  assert.ok(first.coordination instanceof CoordinationStore);\nimpl/test/phase11-coordination-store.test.mjs:207:  assert.deepEqual(first.coordination.readyTasks().map((task) => task.id), ['base']);\nimpl/test/phase11-coordination-store.test.mjs:210:  const replay = make();\nimpl/test/phase11-coordination-store.test.mjs:211:  assert.deepEqual(replay.coordinator.list().map((worker) => worker.taskId), ['base', 'child']);\nimpl/test/phase11-coordination-store.test.mjs:212:  assert.equal(replay.coordinator.list().find((worker) => worker.id === base.id)?.status, 'pending');\nimpl/test/phase11-coordination-store.test.mjs:213:  assert.equal(replay.coordinator.list().find((worker) => worker.id === child.id)?.status, 'pending');\nimpl/test/phase11-coordination-store.test.mjs:214:  assert.deepEqual(replay.coordination.task('child').deps, ['base']);\nimpl/test/phase11-coordination-store.test.mjs:215:  assert.equal(replay.coordination.task('child').assignee, null);\nimpl/test/phase11-coordination-store.test.mjs:216:  assert.deepEqual(replay.coordination.readyTasks().map((task) => task.id), ['base']);\nimpl/test/phase11-coordination-store.test.mjs:219:test('CK2/CK9: restart terminalizes a durable claim that crashed before operational spawn', () => {\nimpl/test/phase11-coordination-store.test.mjs:221:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:222:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:223:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:225:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:226:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:229:  const coordination = new CoordinationStore(join(logDir, 'coordination'));\nimpl/test/phase11-coordination-store.test.mjs:230:  const task = fields('claimed-before-spawn');\nimpl/test/phase11-coordination-store.test.mjs:231:  coordination.createTask(task, { actor: 'orchestrator', key: 'create-crash-task' });\nimpl/test/phase11-coordination-store.test.mjs:232:  coordination.claimTask(task.id, task.reservedWorkerId, 1, { actor: 'orchestrator', key: 'claim-crash-task' });\nimpl/test/phase11-coordination-store.test.mjs:234:  const replay = createDriver({\nimpl/test/phase11-coordination-store.test.mjs:237:    coordination,\nimpl/test/phase11-coordination-store.test.mjs:238:    adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) },\nimpl/test/phase11-coordination-store.test.mjs:242:  assert.equal(replay.coordination.task(task.id).status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:243:  assert.equal(replay.coordinator.list().find((worker) => worker.taskId === task.id)?.status, 'exited');\nimpl/test/phase11-coordination-store.test.mjs:244:  assert.equal(replay.log.workers().length, 0, 'recovery must not invent a lifecycle.spawned event');\nimpl/test/phase11-coordination-store.test.mjs:245:  assert.equal(replay.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'recovery.claimed_without_spawn'), true);\nimpl/test/phase11-coordination-store.test.mjs:246:  assert.equal(replay.coordination.events().some((event) => event.kind === 'task.transitioned' && event.payload.to === 'failed'), true);\nimpl/test/phase11-coordination-store.test.mjs:249:test('CK1a: operational evidence gets a global coordination order and digest validation', () => {\nimpl/test/phase11-coordination-store.test.mjs:257:  assert.ok(ma.evidence.coordinationSeq < mb.evidence.coordinationSeq);\nimpl/test/phase11-coordination-store.test.mjs:265:  const claimed = store.claimTask('a', 'w-a', 1, { actor: 'orchestrator', key: 'claim-a-for-artifacts' });\nimpl/test/phase11-coordination-store.test.mjs:271:  store.transitionTask('a', 'completed', claimed.task.version, { actor: 'policy', key: 'complete-a-for-artifacts' }, mapped.evidence);\nimpl/test/phase11-coordination-store.test.mjs:272:  assert.throws(() => store.registerArtifact({ taskId: 'a', kind: 'commit', refs: { sha: 'abc' }, accepted: true, provenance: [{ coordinationSeq: 1 }] }, { actor: 'policy', key: 'wrong-provenance' }), (error) => error.code === 'unverified_provenance');\nimpl/test/phase11-coordination-store.test.mjs:283:  const replay = new CoordinationStore(store.root, { operationalRead: (worker, seq) => events.get(`${worker}:${seq}`) });\nimpl/test/phase11-coordination-store.test.mjs:284:  assert.deepEqual(replay.artifact(registered.artifact.id), store.artifact(registered.artifact.id));\nimpl/test/phase11-coordination-store.test.mjs:287:test('CK3/CK9: terminal task and accepted manifests commit in one append batch', () => {\nimpl/test/phase11-coordination-store.test.mjs:299:  const claimed = store.claimTask('atomic', 'w-atomic', 1, { actor: 'orchestrator', key: 'claim-atomic' });\nimpl/test/phase11-coordination-store.test.mjs:306:  assert.throws(() => store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence), /batch disk full/);\nimpl/test/phase11-coordination-store.test.mjs:311:  const completed = store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence);\nimpl/test/phase11-coordination-store.test.mjs:317:test('CK8/CK9: completed public task maps verification evidence, terminal state, and manifests', async () => {\nimpl/test/phase11-coordination-store.test.mjs:319:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:320:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:321:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:323:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:324:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:327:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'done.txt', content: 'done\\n' }] } }) },\nimpl/test/phase11-coordination-store.test.mjs:333:  assert.equal(driver.coordination.task('durable-complete').status, 'completed');\nimpl/test/phase11-coordination-store.test.mjs:334:  const snapshot = driver.coordination.snapshot();\nimpl/test/phase11-coordination-store.test.mjs:338:  assert.equal(snapshot.artifacts.find((artifact) => artifact.kind === 'report').accepted, false, 'worker artifact claims stay explicitly untrusted');\nimpl/test/phase11-coordination-store.test.mjs:341:  assert.equal(driver.coordination.events().some((event) => event.kind === 'knowledge.promoted' && event.payload.promotion?.trigger === 'verified_task_outcome'), true);\nimpl/test/phase11-coordination-store.test.mjs:345:  assert.equal(driver.coordination.traceKnowledge(recalled.nodes[0].id).edges.some((edge) => edge.type === 'ReadBy' && edge.to === 'task:durable-complete'), true);\nimpl/test/phase11-coordination-store.test.mjs:346:  const replay = new CoordinationStore(driver.coordination.root, {\nimpl/test/phase11-coordination-store.test.mjs:349:  assert.deepEqual(replay.snapshot(), driver.coordination.snapshot());\nimpl/test/phase11-coordination-store.test.mjs:354:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:355:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:356:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:358:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:359:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:362:    adapters: { mock: new MockAdapter({ scenario: {\nimpl/test/phase11-coordination-store.test.mjs:370:  assert.equal(driver.coordination.task('durable-input').status, 'input_required');\nimpl/test/phase11-coordination-store.test.mjs:373:  assert.equal(driver.coordination.events().some((event) => event.kind === 'task.transitioned' && event.payload.to === 'working'), true);\nimpl/test/phase11-coordination-store.test.mjs:375:  assert.equal(driver.coordination.task('durable-input').status, 'completed');\nimpl/test/phase11-coordination-store.test.mjs:378:test('CK8/CK9: input transition append failure poisons before pending/blocked state becomes visible', async () => {\nimpl/test/phase11-coordination-store.test.mjs:380:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:381:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:382:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:383:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:384:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:385:  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:386:  const brief = { goal: 'input crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:388:  const rawAppend = driver.coordination._appendFile;\nimpl/test/phase11-coordination-store.test.mjs:389:  driver.coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-coordination-store.test.mjs:395:    kind: 'question.asked', actor: 'worker', payload: { requestId: 'q-crash', question: 'continue?', blocking: true },\nimpl/test/phase11-coordination-store.test.mjs:396:  }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:397:  assert.equal(driver.coordination.task('input-write-failure').status, 'working');\nimpl/test/phase11-coordination-store.test.mjs:400:  await adapter.kill(handle.id);\nimpl/test/phase11-coordination-store.test.mjs:403:test('CK8/CK9: stop-intent append failure calls no adapter and leaves durable task working', async () => {\nimpl/test/phase11-coordination-store.test.mjs:405:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:406:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:407:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:408:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:409:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:410:  let adapterKills = 0;\nimpl/test/phase11-coordination-store.test.mjs:411:  const rawKill = adapter.kill.bind(adapter);\nimpl/test/phase11-coordination-store.test.mjs:412:  adapter.kill = async (...args) => { adapterKills += 1; return rawKill(...args); };\nimpl/test/phase11-coordination-store.test.mjs:413:  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:414:  const brief = { goal: 'stop crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:416:  const rawAppend = driver.coordination._appendFile;\nimpl/test/phase11-coordination-store.test.mjs:417:  driver.coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-coordination-store.test.mjs:418:    if (body.includes('\"kind\":\"control.stop_requested\"')) throw new Error('stop intent disk full');\nimpl/test/phase11-coordination-store.test.mjs:421:  await assert.rejects(driver.coordinator.kill(handle.id, 'human'), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:422:  assert.equal(adapterKills, 0);\nimpl/test/phase11-coordination-store.test.mjs:423:  assert.equal(driver.coordination.task('stop-write-failure').status, 'working');\nimpl/test/phase11-coordination-store.test.mjs:425:  driver.coordination._appendFile = rawAppend;\nimpl/test/phase11-coordination-store.test.mjs:429:test('CK8/CK9: cancellation completion failure resolves bounded, keeps stop intent, and restart closes task', async () => {\nimpl/test/phase11-coordination-store.test.mjs:431:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:432:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:433:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:434:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:436:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:437:  const driver = createDriver({ repoRoot: repo, logDir, adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:438:  const brief = { goal: 'cancel completion crash', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:440:  const rawAppend = driver.coordination._appendFile;\nimpl/test/phase11-coordination-store.test.mjs:441:  driver.coordination._appendFile = (file, body, encoding) => {\nimpl/test/phase11-coordination-store.test.mjs:446:  assert.deepEqual(stopped, { ok: false, result: 'coordination_unavailable' });\nimpl/test/phase11-coordination-store.test.mjs:447:  assert.equal(driver.coordination.task('cancel-completion-failure').status, 'working');\nimpl/test/phase11-coordination-store.test.mjs:448:  assert.equal(driver.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'control.stop_requested'), true);\nimpl/test/phase11-coordination-store.test.mjs:450:  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:451:  driver.coordination._appendFile = rawAppend;\nimpl/test/phase11-coordination-store.test.mjs:452:  const replay = createDriver({ repoRoot: repo, logDir, coordination: driver.coordination, adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:453:  assert.equal(replay.coordination.task('cancel-completion-failure').status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:456:test('CK4: Scratch claims conservatively conflict, warn cross-tree, and expire only by event', () => {\nimpl/test/phase11-coordination-store.test.mjs:461:  const first = store.claimScratch({ resource: 'path:payments/**', ownerWorker: 'w1', ownerTask: 't1', intent: 'edit', envRef: envA, fence: 3, leaseDeadline: 'later' }, { actor: 'w1', key: 'claim-a' });\nimpl/test/phase11-coordination-store.test.mjs:462:  assert.equal(store.claimScratch({ resource: 'changed', envRef: envB }, { actor: 'w1', key: 'claim-a' }).result, 'idempotent');\nimpl/test/phase11-coordination-store.test.mjs:463:  const conflict = store.claimScratch({ resource: 'path:payments/stripe.js', ownerWorker: 'w2', ownerTask: 't2', intent: 'edit', envRef: envB, fence: 4, leaseDeadline: 'later' }, { actor: 'w2', key: 'claim-b' });\nimpl/test/phase11-coordination-store.test.mjs:465:  assert.equal(conflict.conflict.id, first.claim.id);\nimpl/test/phase11-coordination-store.test.mjs:466:  const globWitness = store.claimScratch({ resource: '*stripe.js', ownerWorker: 'w3', ownerTask: 't3', intent: 'edit', envRef: envB, fence: 5, leaseDeadline: 'later' }, { actor: 'w3', key: 'claim-glob-witness' });\nimpl/test/phase11-coordination-store.test.mjs:470:  assert.equal(check.claims[0].warning, 'observed on aaaa1111 — not your tree');\nimpl/test/phase11-coordination-store.test.mjs:471:  assert.equal(new CoordinationStore(root).checkScratch('path:payments/stripe.js', envB).clear, false, 'wall-clock replay never expires a lease');\nimpl/test/phase11-coordination-store.test.mjs:472:  store.expireScratchClaim(first.claim.id, 1, { actor: 'policy', key: 'expire-a' });\nimpl/test/phase11-coordination-store.test.mjs:480:  const fact = store.postScratchFact({ namespace: 'tests', key: 'test:flaky', value: { seed: 42 }, grounding: 'observed', envRef: { repoId: 'repo', treeSha: 'cafe1234' }, evidence: [{ coordinationSeq: 1 }] }, { actor: 'w1', key: 'fact-a' });\nimpl/test/phase11-coordination-store.test.mjs:492:  assert.throws(() => store.addKnowledgeNode({ type: 'Finding', id: 'Ffuture', body: 'future', grounding: 'verified', evidence: [{ coordinationSeq: 99 }] }, { actor: 'policy', key: 'future' }), (error) => error.code === 'temporal_incoherence');\nimpl/test/phase11-coordination-store.test.mjs:493:  const finding = store.addKnowledgeNode({ type: 'Finding', id: 'F1', body: 'verified outcome', grounding: 'verified', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-01-01T00:00:00Z' }, { actor: 'policy', key: 'f1' });\nimpl/test/phase11-coordination-store.test.mjs:494:  const decision = store.addKnowledgeNode({ type: 'Decision', id: 'D1', body: 'choose A', grounding: 'asserted', informedBy: [finding.node.id], evidence: [{ coordinationSeq: finding.event.seq }], validFrom: '2026-01-02T00:00:00Z' }, { actor: 'human', key: 'd1-ok' });\nimpl/test/phase11-coordination-store.test.mjs:498:test('CK5/CK7: bitemporal query, supersession, logged reads, and contamination survive replay', () => {\nimpl/test/phase11-coordination-store.test.mjs:502:  store.addKnowledgeNode({ type: 'Finding', id: 'old', body: 'old belief', grounding: 'observed', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-01-01T00:00:00Z' }, { actor: 'policy', key: 'old' });\nimpl/test/phase11-coordination-store.test.mjs:503:  store.addKnowledgeNode({ type: 'Finding', id: 'new', body: 'new belief', grounding: 'verified', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-02-01T00:00:00Z' }, { actor: 'policy', key: 'new' });\nimpl/test/phase11-coordination-store.test.mjs:514:  const replay = new CoordinationStore(root);\nimpl/test/phase11-coordination-store.test.mjs:515:  assert.deepEqual(replay.snapshot(), store.snapshot());\nimpl/test/phase11-coordination-store.test.mjs:516:  assert.equal(replay.affectedReaders('old')[0].taskId, 'a');\nimpl/test/phase11-coordination-store.test.mjs:517:  assert.equal(replay.affectedReaders('old')[0].taskStatus, 'pending');\nimpl/test/phase11-coordination-store.test.mjs:518:  assert.equal(replay.traceKnowledge('old').edges.some((edge) => edge.type === 'ReadBy' && edge.to === 'task:a'), true);\nimpl/test/phase11-coordination-store.test.mjs:519:  const audit = replay.auditKnowledge();\nimpl/test/phase11-coordination-store.test.mjs:525:test('CK7/CK9: invalidation and contamination commit atomically', () => {\nimpl/test/phase11-coordination-store.test.mjs:533:  store.addKnowledgeNode({ type: 'Finding', id: 'fragile', body: 'fragile belief', grounding: 'observed', evidence: [{ coordinationSeq: 1 }] }, { actor: 'policy', key: 'fragile' });\nimpl/test/phase11-coordination-store.test.mjs:555:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:556:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:557:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:558:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:561:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } }) },\nimpl/test/phase11-coordination-store.test.mjs:569:  const lease = driver.coordinator.claimScratch(handle.id, {\nimpl/test/phase11-coordination-store.test.mjs:570:    resource: 'path:slow.txt', intent: 'edit', envRef, leaseDeadline: 'terminal',\nimpl/test/phase11-coordination-store.test.mjs:571:  }, { actor: 'orchestrator', expectedFence: active.fence, idempotencyKey: 'claim-slow-file' });\nimpl/test/phase11-coordination-store.test.mjs:572:  assert.equal(driver.coordinator.claimScratch(handle.id, { resource: 'path:other.txt', intent: 'edit', envRef }, { expectedFence: active.fence - 1, idempotencyKey: 'stale-claim' }).result, 'stale_fence');\nimpl/test/phase11-coordination-store.test.mjs:577:  const scratchRead = driver.coordinator.readScratch(handle.id, 'path:slow.txt', envRef, { idempotencyKey: 'read-slow-claim', runId: 'cancel-run' });\nimpl/test/phase11-coordination-store.test.mjs:579:  assert.equal(scratchRead.result.claims[0].ownerWorker, handle.id);\nimpl/test/phase11-coordination-store.test.mjs:580:  assert.equal(driver.coordination.events().some((event) => event.kind === 'scratch.read' && event.payload.taskId === 'durable-cancel'), true);\nimpl/test/phase11-coordination-store.test.mjs:583:  assert.equal(driver.coordination.task('durable-cancel').status, 'cancelled');\nimpl/test/phase11-coordination-store.test.mjs:584:  assert.equal(driver.coordination.snapshot().evidence.some((item) => item.kind === 'kill.confirmed'), true);\nimpl/test/phase11-coordination-store.test.mjs:585:  assert.equal(driver.coordination.activeScratchClaims({ workerId: handle.id }).length, 0);\nimpl/test/phase11-coordination-store.test.mjs:586:  assert.equal(driver.coordination.snapshot().scratch.claims.find((claim) => claim.id === lease.claim.id).active, false);\nimpl/src/coordinator.mjs:3:// reliably (fence-checked), enforces two-phase stop, single-consumer approvals, and the\nimpl/src/coordinator.mjs:87:  'createTask', 'claimTask', 'transitionTask', 'transitionTaskWithArtifacts', 'mapOperationalEvent',\nimpl/src/coordinator.mjs:88:  'recordDriver', 'registerArtifact', 'supersedeArtifact', 'claimScratch', 'postScratchFact',\nimpl/src/coordinator.mjs:206:  if (!['new', 'resume', 'fork'].includes(mode)) {\nimpl/src/coordinator.mjs:259:    if (!opts?.coordination) throw new TypeError('Coordinator requires a durable coordination store');\nimpl/src/coordinator.mjs:260:    for (const method of ['snapshot', 'task', 'createTask', 'claimTask', 'transitionTask', 'transitionTaskWithArtifacts', 'mapOperationalEvent', 'recordDriver', 'registerArtifact', 'artifact', 'claimScratch', 'postScratchFact', 'readScratch', 'activeScratchClaims', 'expireScratchClaim', 'addKnowledgeNode', 'promoteKnowledgeNode', 'readKnowledge']) {\nimpl/src/coordinator.mjs:261:      if (typeof opts.coordination[method] !== 'function') throw new TypeError(`Coordinator coordination store is missing ${method}()`);\nimpl/src/coordinator.mjs:265:    this._adapters = opts.adapters;\nimpl/src/coordinator.mjs:268:    const rawCoordination = opts.coordination;\nimpl/src/coordinator.mjs:269:    this._coordination = new Proxy(rawCoordination, {\nimpl/src/coordinator.mjs:305:    this._publisher = opts.publisher ?? null;\nimpl/src/coordinator.mjs:312:    // fails closed until process restart/replay, and any not-yet-entered spawn is aborted. A\nimpl/src/coordinator.mjs:367:    for (const adapter of Object.values(this._adapters)) {\nimpl/src/coordinator.mjs:368:      adapter.onEvent((e) => {\nimpl/src/coordinator.mjs:385:    this._replay();\nimpl/src/coordinator.mjs:406:      if (!vendor || !this._adapters[vendor]) continue;\nimpl/src/coordinator.mjs:407:      const card = this._adapters[vendor].card();\nimpl/src/coordinator.mjs:430:      if (!cardSupportsSession(this._adapters[vendor]?.card(), task.sessionRequest)) return null;\nimpl/src/coordinator.mjs:431:      const resolved = resolveCardModel(this._adapters[vendor]?.card(), task.modelRequested, task.modelPolicy, { explicit: true });\nimpl/src/coordinator.mjs:436:    for (const [name, ad] of Object.entries(this._adapters)) {\nimpl/src/coordinator.mjs:449:    for (const name of Object.keys(this._adapters)) inFlight[name] = this._inFlightCount(name);\nimpl/src/coordinator.mjs:451:    if (!chosen || !this._adapters[chosen] || !Object.hasOwn(resolvedModels, chosen)) return null;\nimpl/src/coordinator.mjs:464:    const card = this._adapters[vendor]?.card();\nimpl/src/coordinator.mjs:471:    if (this._coordination) {\nimpl/src/coordinator.mjs:472:      const claim = this._coordination.claimTask(task.id, workerId, task.coordinationVersion, {\nimpl/src/coordinator.mjs:473:        actor: 'orchestrator', key: `task.claimed:${task.id}:${task.coordinationVersion}`,\nimpl/src/coordinator.mjs:475:      task.coordinationVersion = claim.task.version;\nimpl/src/coordinator.mjs:487:      const crashEvent = this._log.append({\nimpl/src/coordinator.mjs:488:        worker: workerId, harness, turnEpoch: this._safeTurnEpoch(handle), kind: 'lifecycle.crashed', actor: 'policy',\nimpl/src/coordinator.mjs:491:      const evidence = this._coordMapEvent(crashEvent);\nimpl/src/coordinator.mjs:499:    // Create the worktree; the returned readiness promise is handed to the adapter so the\nimpl/src/coordinator.mjs:501:    // 'working' and the adapter is invoked synchronously below (so a bare tick() dispatches\nimpl/src/coordinator.mjs:503:    const worktreeSource = task.sessionRequest?.mode === 'resume'\nimpl/src/coordinator.mjs:531:        // late worktree exists, reap again while the adapter's cancelled reservation prevents\nimpl/src/coordinator.mjs:557:    // SC12: adapters receive an explicit cancellation signal in addition to their verb call.\nimpl/src/coordinator.mjs:558:    // Session adapters own the stronger pending-spawn reservation, while this signal makes the\nimpl/src/coordinator.mjs:564:    Promise.resolve(this._adapters[vendor].spawn(workerId, task.brief, {\nimpl/src/coordinator.mjs:593:  /** SC1d: a refused spawn Ack may never strand its task in 'working'. `lifecycle.crashed` is\nimpl/src/coordinator.mjs:594:   * the honest kind — replay already folds it to 'failed' and the story compiler\nimpl/src/coordinator.mjs:596:   * if an adapter event already ended the worker (both paths racing is benign). */\nimpl/src/coordinator.mjs:599:    // to fail only a still-live spawn; it may never clobber cancellation or duplicate a crash.\nimpl/src/coordinator.mjs:602:    const crashEvent = this._log.append({\nimpl/src/coordinator.mjs:606:      kind: 'lifecycle.crashed',\nimpl/src/coordinator.mjs:610:    const evidence = this._coordMapEvent(crashEvent);\nimpl/src/coordinator.mjs:633:    if (vendor !== 'auto' && !this._adapters[vendor]) throw new UnknownVendorError(`unknown vendor \"${vendor}\"`);\nimpl/src/coordinator.mjs:634:    if (vendor !== 'auto' && !cardSupportsSession(this._adapters[vendor].card(), sessionRequest)) {\nimpl/src/coordinator.mjs:638:      const resolved = resolveCardModel(this._adapters[vendor].card(), opts.model, modelPolicy, { explicit: true });\nimpl/src/coordinator.mjs:643:      const anyCapable = Object.values(this._adapters).some((ad) => resolveCardModel(ad.card(), opts.model, modelPolicy, { explicit: false }).ok);\nimpl/src/coordinator.mjs:647:      const anySessionCapable = Object.values(this._adapters).some((ad) => cardSupportsSession(ad.card(), sessionRequest));\nimpl/src/coordinator.mjs:654:    if (sessionRequest.mode === 'resume') {\nimpl/src/coordinator.mjs:660:        throw new SessionSelectionError('resume requires session.context.worktree and ownerTaskId', 'session_context_required');\nimpl/src/coordinator.mjs:672:    let coordinationVersion = null;\nimpl/src/coordinator.mjs:673:    if (this._coordination) {\nimpl/src/coordinator.mjs:674:      const created = this._coordination.createTask({\nimpl/src/coordinator.mjs:680:      coordinationVersion = created.task.version;\nimpl/src/coordinator.mjs:692:      sessionContext: sessionRequest.mode === 'resume' ? sessionRequest.context : null,\nimpl/src/coordinator.mjs:709:      coordinationVersion,\nimpl/src/coordinator.mjs:750:    if (!this._coordination) return;\nimpl/src/coordinator.mjs:751:    for (const durable of this._coordination.snapshot().tasks) {\nimpl/src/coordinator.mjs:763:        review: null, taskType: durable.taskType ?? 'general', coordinationVersion: durable.version,\nimpl/src/coordinator.mjs:794:    if (vendor === 'auto' || !this._adapters[vendor]) {\nimpl/src/coordinator.mjs:801:    const parentFamily = this._adapters[parentHandle.vendor]?.card()?.modelSelection?.family ?? parentHandle.vendor;\nimpl/src/coordinator.mjs:802:    const reviewerFamily = this._adapters[vendor].card()?.modelSelection?.family ?? vendor;\nimpl/src/coordinator.mjs:821:        'Treat worker prose and claimed verification as untrusted; inspect the captured git objects directly.',\nimpl/src/coordinator.mjs:881:  /** PS7: explicitly reattach a replayed native session. Recovery never trusts a stale PID;\nimpl/src/coordinator.mjs:882:   * authority comes only from a fresh adapter handshake that reports the expected native ID. */\nimpl/src/coordinator.mjs:891:    const adapter = this._adapters[handle.vendor];\nimpl/src/coordinator.mjs:892:    if (!adapter || !cardSupportsSession(adapter.card(), { mode: 'resume' })) {\nimpl/src/coordinator.mjs:897:      ? normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context: rawContext }).context\nimpl/src/coordinator.mjs:910:    const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\nimpl/src/coordinator.mjs:928:    const attempt = Promise.resolve(adapter.spawn(workerId, task.brief, {\nimpl/src/coordinator.mjs:955:          ? { result: 'recovery_refused', reason: outcome?.ack?.reason ?? 'adapter refused recovery' }\nimpl/src/coordinator.mjs:967:      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\nimpl/src/coordinator.mjs:978:      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\nimpl/src/coordinator.mjs:1036:      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\nimpl/src/coordinator.mjs:1069:    if (task.retainedResultRef && typeof this._worktrees.releaseResult === 'function') {\nimpl/src/coordinator.mjs:1070:      try { await this._worktrees.releaseResult(task.retainedResultRef); } catch { /* merged HEAD now retains the result */ }\nimpl/src/coordinator.mjs:1079:    if (this._coordination) {\nimpl/src/coordinator.mjs:1080:      const acceptingEvidence = this._coordination.task(task.id).artifactIds\nimpl/src/coordinator.mjs:1081:        .map((artifactId) => this._coordination.artifact(artifactId))\nimpl/src/coordinator.mjs:1084:      this._coordination.registerArtifact({\nimpl/src/coordinator.mjs:1088:      this._coordination.promoteKnowledgeNode({\nimpl/src/coordinator.mjs:1092:        evidence: [{ coordinationSeq: integrationEvidence.coordinationSeq }],\nimpl/src/coordinator.mjs:1099:  /** AC6: create an approval-gated exact-SHA publication request. No side effect occurs here. */\nimpl/src/coordinator.mjs:1111:      throw new PublicationError('remote must be a credential-free git remote name', 'invalid_remote');\nimpl/src/coordinator.mjs:1124:      kind: 'publication', worker: workerId, state: 'pending', resolution: null, consumer: null,\nimpl/src/coordinator.mjs:1217:    // SC4a: per-worker delivery serialization — deliveries reach the adapter strictly in\nimpl/src/coordinator.mjs:1234:    const card = this._adapters[handle.vendor]?.card();\nimpl/src/coordinator.mjs:1267:    const ack = await this._adapters[handle.vendor].prompt(workerId, message, mode);\nimpl/src/coordinator.mjs:1320:    // A native adapter can emit turn_started synchronously inside prompt(), before returning its\nimpl/src/coordinator.mjs:1326:      ack = await this._adapters[handle.vendor].prompt(workerId, message, 'turn');\nimpl/src/coordinator.mjs:1332:    // A crash/exit is intentionally processed immediately instead of queued. It wins over an Ack\nimpl/src/coordinator.mjs:1351:      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\nimpl/src/coordinator.mjs:1377:        op: 'follow_up_admission', reason: String(reason ?? 'adapter refused after emitting turn events'),\nimpl/src/coordinator.mjs:1382:    // advanced despite refusing admission. Confirmed two-phase kill owns transport cleanup.\nimpl/src/coordinator.mjs:1401:      return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\nimpl/src/coordinator.mjs:1415:      return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\nimpl/src/coordinator.mjs:1417:    // CI3: a crashed/exited child cannot emit another kill.confirmed. Treat its authoritative\nimpl/src/coordinator.mjs:1418:    // terminal event as the confirmation, finish cleanup now, and never arm an unfulfillable wait.\nimpl/src/coordinator.mjs:1439:        const call = Promise.resolve(this._adapters[handle.vendor].kill(handle.id));\nimpl/src/coordinator.mjs:1488:        ? Promise.resolve(this._adapters[handle.vendor].kill(handle.id))\nimpl/src/coordinator.mjs:1489:        : Promise.resolve(this._adapters[handle.vendor].interrupt(handle.id, then));\nimpl/src/coordinator.mjs:1504:    if (!this._coordination || !task) return null;\nimpl/src/coordinator.mjs:1505:    const durable = this._coordination.task(task.id);\nimpl/src/coordinator.mjs:1507:    const result = this._coordination.transitionTask(task.id, to, task.coordinationVersion ?? durable.version, { actor, key }, evidence);\nimpl/src/coordinator.mjs:1508:    task.coordinationVersion = result.task.version;\nimpl/src/coordinator.mjs:1517:    if (!this._coordination || !event) return null;\nimpl/src/coordinator.mjs:1518:    return this._coordination.mapOperationalEvent(event, { actor: 'policy', key }).evidence;\nimpl/src/coordinator.mjs:1527:    if (!this._coordination) return null;\nimpl/src/coordinator.mjs:1528:    return this._coordination.recordDriver(kind, payload, { actor, key }).event;\nimpl/src/coordinator.mjs:1533:      const fatal = new Error(`authoritative coordination mutation failed: ${err?.message ?? err}`, { cause: err });\nimpl/src/coordinator.mjs:1535:      fatal.code = 'coordination_write_unavailable';\nimpl/src/coordinator.mjs:1538:        if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) handle.spawnAbort.abort({ reason: 'coordination_write_unavailable' });\nimpl/src/coordinator.mjs:1545:    if (!this._coordination) return prior;\nimpl/src/coordinator.mjs:1547:    const created = this._coordination.createTask({\nimpl/src/coordinator.mjs:1553:    const claimed = this._coordination.claimTask(id, handle.id, created.task.version, {\nimpl/src/coordinator.mjs:1554:      actor: 'orchestrator', key: `task.claimed:${id}:${created.task.version}`,\nimpl/src/coordinator.mjs:1559:      coordinationVersion: claimed.task.version,\nimpl/src/coordinator.mjs:1568:    if (!this._coordination || !task) return;\nimpl/src/coordinator.mjs:1570:    for (const claim of this._coordination.activeScratchClaims({ workerId, taskId: task.id })) {\nimpl/src/coordinator.mjs:1571:      this._coordination.expireScratchClaim(claim.id, claim.version, {\nimpl/src/coordinator.mjs:1572:        actor: 'policy', key: `scratch.claim_expired:${claim.id}:${claim.version}:${reason}`,\nimpl/src/coordinator.mjs:1586:    const lease = this._runtimeScopes.create(handle.id, handle.vendor);\nimpl/src/coordinator.mjs:1587:    handle.runtimeLease = lease;\nimpl/src/coordinator.mjs:1588:    handle.runtimeScope = { ...lease.posture, active: true };\nimpl/src/coordinator.mjs:1593:    return lease;\nimpl/src/coordinator.mjs:1598:    try { this._runtimeScopes.remove(handle.id); } catch { /* best-effort cleanup continues */ }\nimpl/src/coordinator.mjs:1835:        Promise.resolve(this._adapters[handle.vendor]?.kill(handle.id)).catch(noop);\nimpl/src/coordinator.mjs:1837:      for (const resolve of waiter.resolvers) resolve({ ok: false, result: 'coordination_unavailable' });\nimpl/src/coordinator.mjs:1856:    let coordinationFailure = null;\nimpl/src/coordinator.mjs:1864:          coordinationFailure = err;\nimpl/src/coordinator.mjs:1869:    if (handle && this._adapters[handle.vendor]) {\nimpl/src/coordinator.mjs:1870:      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\nimpl/src/coordinator.mjs:1877:      if (!coordinationFailure && task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\nimpl/src/coordinator.mjs:1881:    const result = coordinationFailure ? { ok: false, result: 'coordination_unavailable' } : { ok: true, result: 'forced' };\nimpl/src/coordinator.mjs:1909:    // CI2: reserve the single-consumer slot, but do not COMMIT resolution until the adapter\nimpl/src/coordinator.mjs:1912:    let releaseResolving;\nimpl/src/coordinator.mjs:1913:    record.resolvingDone = new Promise((resolve) => { releaseResolving = resolve; });\nimpl/src/coordinator.mjs:1915:      releaseResolving();\nimpl/src/coordinator.mjs:1941:        record.consumer = actor;\nimpl/src/coordinator.mjs:1955:        record.consumer = actor;\nimpl/src/coordinator.mjs:1960:      if (typeof this._publisher !== 'function') {\nimpl/src/coordinator.mjs:1965:      let authorizedEvent;\nimpl/src/coordinator.mjs:1967:        authorizedEvent = this._log.append({\nimpl/src/coordinator.mjs:1969:          kind: 'publication.authorized', actor,\nimpl/src/coordinator.mjs:1972:        const evidence = this._coordMapEvent(authorizedEvent);\nimpl/src/coordinator.mjs:1973:        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\nimpl/src/coordinator.mjs:1979:      let published;\nimpl/src/coordinator.mjs:1981:        published = await this._publisher(record.publication);\nimpl/src/coordinator.mjs:1985:        throw new PublicationError(String(err?.message ?? err), 'publisher_failed');\nimpl/src/coordinator.mjs:1988:      record.consumer = actor;\nimpl/src/coordinator.mjs:1991:      void published;\nimpl/src/coordinator.mjs:1999:        this._coordination?.promoteKnowledgeNode({\nimpl/src/coordinator.mjs:2000:          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\nimpl/src/coordinator.mjs:2003:          evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\nimpl/src/coordinator.mjs:2011:      return { ok: true, result: 'published', publication: task.publication };\nimpl/src/coordinator.mjs:2027:      record.consumer = actor;\nimpl/src/coordinator.mjs:2041:      if (task && this._coordination?.task(task.id)?.status === 'input_required') {\nimpl/src/coordinator.mjs:2046:      record.consumer = actor;\nimpl/src/coordinator.mjs:2056:        ack = await this._adapters[handle.vendor].answer(handle.id, requestId, answer);\nimpl/src/coordinator.mjs:2059:        ack = await this._adapters[handle.vendor].approve(handle.id, requestId, decision, answer && answer.payload);\nimpl/src/coordinator.mjs:2063:      record.consumer = null;\nimpl/src/coordinator.mjs:2071:      record.consumer = null;\nimpl/src/coordinator.mjs:2074:      return { ok: false, result: 'delivery_refused', reason: ack?.reason ?? 'adapter did not affirm response delivery' };\nimpl/src/coordinator.mjs:2089:    if (task && this._coordination?.task(task.id)?.status === 'input_required') {\nimpl/src/coordinator.mjs:2095:        record.consumer = actor;\nimpl/src/coordinator.mjs:2103:    record.consumer = actor;\nimpl/src/coordinator.mjs:2138:  /** Pull-only causal recall. The coordination append is the authority boundary: if the read\nimpl/src/coordinator.mjs:2142:    if (!this._coordination) throw new Error('coordination store is required for knowledge recall');\nimpl/src/coordinator.mjs:2153:    return this._coordination.readKnowledge(query, {\nimpl/src/coordinator.mjs:2161:  claimScratch(workerId, fields, opts = {}) {\nimpl/src/coordinator.mjs:2166:    if (opts.expectedFence === undefined) throw new TypeError('Scratch claim requires expectedFence');\nimpl/src/coordinator.mjs:2169:    if (typeof opts.idempotencyKey !== 'string' || opts.idempotencyKey.length === 0) throw new TypeError('Scratch claim requires idempotencyKey');\nimpl/src/coordinator.mjs:2170:    return this._coordination.claimScratch({\nimpl/src/coordinator.mjs:2187:    return this._coordination.postScratchFact({\nimpl/src/coordinator.mjs:2199:    return this._coordination.readScratch(resource, envRef, {\nimpl/src/coordinator.mjs:2327:    if (handle.turnAdmission && actor === 'worker' && !['lifecycle.crashed', 'lifecycle.exited', 'kill.confirmed'].includes(kind)) {\nimpl/src/coordinator.mjs:2358:          persistence: this._adapters[handle.vendor]?.card()?.sessions?.resume === 'native' ? 'native' : 'process',\nimpl/src/coordinator.mjs:2371:      const selection = this._adapters[handle.vendor]?.card()?.modelSelection;\nimpl/src/coordinator.mjs:2390:        // until the adapter proves the mismatched session is gone.\nimpl/src/coordinator.mjs:2407:        // (coordinator.test). Normalize so the logged claim and the gate both see the WorkerResult.\nimpl/src/coordinator.mjs:2416:      case 'lifecycle.crashed':\nimpl/src/coordinator.mjs:2422:          if (evidence) this._coordTransition(task, 'failed', `task.failed:${task.id}:${evidence.coordinationSeq}`, evidence);\nimpl/src/coordinator.mjs:2440:          consumer: null,\nimpl/src/coordinator.mjs:2470:          consumer: null,\nimpl/src/coordinator.mjs:2494:        if (task && this._coordination?.task(task.id)?.status === 'input_required') {\nimpl/src/coordinator.mjs:2587:          taskId: task.id, kind: 'commit', refs: { sha: captured.sha }, mediaType: 'application/vnd.git.commit',\nimpl/src/coordinator.mjs:2604:        const claimEvent = this._log.read(handle.id).filter((event) => event.kind === 'lifecycle.turn_completed').at(-1);\nimpl/src/coordinator.mjs:2605:        const claimEvidence = this._coordMapEvent(claimEvent);\nimpl/src/coordinator.mjs:2607:          taskId: task.id, kind: 'report', refs: { claimedArtifacts: workerResult.artifacts },\nimpl/src/coordinator.mjs:2608:          mediaType: 'application/vnd.baton.worker-artifact-claim+json', accepted: false,\nimpl/src/coordinator.mjs:2609:          provenance: claimEvidence ? [claimEvidence] : [], grounding: 'worker_prose',\nimpl/src/coordinator.mjs:2614:      const terminal = this._coordination.transitionTaskWithArtifacts(\nimpl/src/coordinator.mjs:2615:        task.id, terminalStatus, task.coordinationVersion,\nimpl/src/coordinator.mjs:2618:      task.coordinationVersion = terminal.task.version;\nimpl/src/coordinator.mjs:2622:      this._coordination.promoteKnowledgeNode({\nimpl/src/coordinator.mjs:2626:        grounding: 'verified', evidence: [{ coordinationSeq: evidence.coordinationSeq }, ...artifactEvidence],\nimpl/src/coordinator.mjs:2653:        const card = this._adapters[handle.vendor]?.card();\nimpl/src/coordinator.mjs:2669:      let durable = this._coordination.task(task.id);\nimpl/src/coordinator.mjs:2673:          const transitioned = this._coordination.transitionTask(task.id, 'failed', durable.version, {\nimpl/src/coordinator.mjs:2676:          task.coordinationVersion = transitioned.task.version;\nimpl/src/coordinator.mjs:2678:        } catch (coordinationError) {\nimpl/src/coordinator.mjs:2679:          this._poisonCoordination(coordinationError);\nimpl/src/coordinator.mjs:2680:          durable = this._coordination.task(task.id);\nimpl/src/coordinator.mjs:2696:  // Construction replay (D10) — rebuild ALL state purely from the log.\nimpl/src/coordinator.mjs:2699:  _replay() {\nimpl/src/coordinator.mjs:2760:                  persistence: this._adapters[vendorResolved]?.card()?.sessions?.resume === 'native' ? 'native' : 'process',\nimpl/src/coordinator.mjs:2787:              ? { mode: 'resume', id: sessionRef.id, ...(sessionContext ? { context: sessionContext } : {}) }\nimpl/src/coordinator.mjs:2816:          case 'lifecycle.crashed':\nimpl/src/coordinator.mjs:2841:      // and wire session. The coordination stream is authoritative for which refinement is\nimpl/src/coordinator.mjs:2842:      // current after restart, so associate the replayed terminal/result state with the newest\nimpl/src/coordinator.mjs:2844:      const durableWorkerTasks = this._coordination?.snapshot().tasks\nimpl/src/coordinator.mjs:2850:      // Operational completion without its authoritative coordination terminal batch is a crash\nimpl/src/coordinator.mjs:2851:      // gap, never permission to infer success from telemetry. Fail the claimed task durably so a\nimpl/src/coordinator.mjs:2852:      // restart cannot leave it working forever or fabricate its missing accepted manifests.\nimpl/src/coordinator.mjs:2861:          payload: { reason: 'coordination_terminal_batch_missing', priorStatus: priorOperationalStatus },\nimpl/src/coordinator.mjs:2864:        const transitioned = this._coordination.transitionTask(currentDurableTask.id, 'failed', currentDurableTask.version, {\nimpl/src/coordinator.mjs:2865:          actor: 'policy', key: `task.failed:${currentDurableTask.id}:coordination_gap:${gapEvent.seq}`,\nimpl/src/coordinator.mjs:2866:        }, gapEvidence ?? { reason: 'coordination_terminal_batch_missing' });\nimpl/src/coordinator.mjs:2868:        if (seeded) seeded.coordinationVersion = transitioned.task.version;\nimpl/src/coordinator.mjs:2871:      // CI6: replay cannot resurrect an adapter session. Until the persistent-session phase can\nimpl/src/coordinator.mjs:2885:        const durable = taskId ? this._coordination?.task(taskId) : null;\nimpl/src/coordinator.mjs:2888:          const transitioned = this._coordination.transitionTask(taskId, 'failed', durable.version, {\nimpl/src/coordinator.mjs:2889:            actor: 'policy', key: `task.failed:${taskId}:replay:${recoveryEvent?.seq ?? maxTurnEpoch}`,\nimpl/src/coordinator.mjs:2893:            seeded.coordinationVersion = transitioned.task.version;\nimpl/src/coordinator.mjs:2894:            this._expireScratchClaims(this._workers.get(workerId), seeded, 'replay_failed');\nimpl/src/coordinator.mjs:2929:        const durable = this._coordination?.task(taskId);\nimpl/src/coordinator.mjs:2932:        task.coordinationVersion = durable?.version ?? task.coordinationVersion ?? null;\nimpl/src/coordinator.mjs:2965:        // reusable before restart must remain uncontrollable until PS7 proves reattachment.\nimpl/src/coordinator.mjs:2982:      // CI6: replayed auto identifiers reserve their numeric slots. A subsequent allocation may\nimpl/src/coordinator.mjs:3005:    if (!this._coordination) return;\nimpl/src/coordinator.mjs:3006:    for (const durable of this._coordination.snapshot().tasks) {\nimpl/src/coordinator.mjs:3011:      const recorded = this._coordRecord('recovery.claimed_without_spawn', { taskId: durable.id, workerId }, `driver.recovery:${durable.id}:claimed_without_spawn`);\nimpl/src/coordinator.mjs:3012:      const transitioned = this._coordination.transitionTask(durable.id, 'failed', durable.version, {\nimpl/src/coordinator.mjs:3013:        actor: 'policy', key: `task.failed:${durable.id}:claimed_without_spawn`,\nimpl/src/coordinator.mjs:3014:      }, { coordinationSeq: recorded?.seq ?? null, reason: 'claimed_without_operational_spawn' });\nimpl/src/coordinator.mjs:3017:        task.status = 'failed'; task.coordinationVersion = transitioned.task.version;\nimpl/src/coordinator.mjs:3018:        this._expireScratchClaims(this._workers.get(workerId), task, 'claimed_without_spawn');\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'rg -n \"intent|effect|ambigu|restart|single-consumer|single consumer|adapter|PID|pid|cleanup|publish|authorization|authorized|green|CK9|coordination|claim|git safety|git|crash|window|replay|resume|consumer|lease|heartbeat\" spec/phase11/coordination-knowledge.md spec/phase11/persistent-sessions.md spec/phase11/acceptance-integration.md docs/26-full-system-goal.md impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":13,"ts":"2026-07-11T20:06:17.941Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":12873,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":24143,"inputTokens":22919,"cachedInputTokens":14592,"outputTokens":1224,"reasoningOutputTokens":696},"last":{"totalTokens":12873,"inputTokens":12444,"cachedInputTokens":10112,"outputTokens":429,"reasoningOutputTokens":180},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":24143,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":14,"ts":"2026-07-11T20:06:17.942Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":15,"ts":"2026-07-11T20:06:17.942Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_BHm9uKoBKpKYlJ5rQTWYosev","command":"/bin/zsh -lc 'git show --unified=80 c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs spec/phase11/coordination-knowledge.md'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"61720","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"git show --unified=80 c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs spec/phase11/coordination-knowledge.md"}],"aggregatedOutput":"commit c8a272eaaa95c85f9d6a83cacf4463509623b988\nAuthor: wahargis <will.a.hargis@gmail.com>\nDate:   Sat Jul 11 12:57:18 2026 -0700\n\n    Close coordination effect crash windows\n\ndiff --git a/impl/src/coordinator.mjs b/impl/src/coordinator.mjs\nindex 5b26737..5eafbed 100644\n--- a/impl/src/coordinator.mjs\n+++ b/impl/src/coordinator.mjs\n@@ -288,161 +288,168 @@ export class Coordinator {\n     this._approvalTimeoutMs = opts.approvalTimeoutMs ?? 60000;\n     this._stopDeadlineMs = opts.stopDeadlineMs ?? 15000;\n     this._recoveryTimeoutMs = opts.recoveryTimeoutMs ?? 15000;\n     this._budgetThresholds = Object.freeze([...(opts.budgetPolicy?.thresholds ?? [0.5, 0.8, 1])].sort((a, b) => a - b));\n     this._budgetHardStopAt = opts.budgetPolicy?.hardStopAt ?? 1;\n     this._watchdog = Object.freeze({\n       stallMs: opts.watchdog?.stallMs ?? 120000,\n       loopThreshold: opts.watchdog?.loopThreshold ?? 3,\n       scopeAction: opts.watchdog?.scopeAction ?? 'kill',\n       loopAction: opts.watchdog?.loopAction ?? 'interrupt',\n       stallAction: opts.watchdog?.stallAction ?? 'interrupt',\n     });\n     this._waitPollMs = opts.waitPollMs ?? 25;\n     // C1: the sole done-gate, and the driver-level policy passed to every accept() call.\n     this._accept = opts.accept ?? defaultAccept;\n     this._acceptOpts = opts.acceptOpts ?? {};\n     this._requireIndependentOracle = opts.requireIndependentOracle ?? false;\n     this._publisher = opts.publisher ?? null;\n     // C4: injectable timer primitives for a real, unref'd stop-deadline timer.\n     this._setTimeout = opts.setTimeout ?? globalThis.setTimeout;\n     this._clearTimeout = opts.clearTimeout ?? globalThis.clearTimeout;\n \n     // D8/CK1: feed the optional story sink, but never turn an authoritative-log failure into a\n     // warning-and-drop. Once an append fails the coordinator is poisoned: every public command\n     // fails closed until process restart/replay, and any not-yet-entered spawn is aborted. A\n     // caller may tear storage down only after it has quiesced the coordinator; racing teardown\n     // is an integrity failure, not a benign sink failure.\n     {\n       const rawAppend = this._log.append.bind(this._log);\n       this._appendFailures = 0;\n       this._fatalError = null;\n       this._log.append = (partial) => {\n         let e;\n         try {\n           e = rawAppend(partial);\n         } catch (err) {\n           this._appendFailures += 1;\n           if (!this._fatalError) {\n             const fatal = new Error(`authoritative operational log append failed: ${err?.message ?? err}`, { cause: err });\n             fatal.name = 'OperationalLogIntegrityError';\n             fatal.code = 'operational_log_unavailable';\n             this._fatalError = fatal;\n             for (const handle of this._workers?.values?.() ?? []) {\n               if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) {\n                 handle.spawnAbort.abort({ reason: 'operational_log_unavailable' });\n               }\n             }\n           }\n           throw this._fatalError;\n         }\n         if (this._story && typeof this._story.record === 'function') {\n           try { this._story.record(e); } catch { /* a broken story sink never affects correctness */ }\n         }\n         return e;\n       };\n     }\n \n     /** @type {Map<string, object>} taskId -> DriverTask */\n     this._tasks = new Map();\n     /** @type {string[]} creation order, for FIFO dispatch */\n     this._taskOrder = [];\n     /** @type {Map<string, object>} workerId -> WorkerHandle (internal) */\n     this._workers = new Map();\n     /** @type {Map<string, object>} requestId -> pending question/approval record */\n     this._pending = new Map();\n     /** @type {Map<string, object>} workerId -> stop-waiter bookkeeping */\n     this._stopWaiters = new Map();\n     /** @type {Map<string, Cursor>} */\n     this._cursors = new Map();\n     /** @type {Map<string, number>} workerId -> highest seq served but not yet acked */\n     this._pendingAck = new Map();\n \n     this._workerSeq = 0;\n     this._taskSeq = 0;\n     this._publicationSeq = 0;\n     this._refinementSeq = 0;\n \n     this._seedCoordinationTasks();\n \n     for (const adapter of Object.values(this._adapters)) {\n-      adapter.onEvent((e) => this._handleEvent(e));\n+      adapter.onEvent((e) => {\n+        try { this._handleEvent(e); } catch (err) {\n+          // Adapter callbacks are an asynchronous trust boundary. A fatal authoritative-write\n+          // failure has already poisoned this coordinator; do not let it become an uncaught\n+          // process exception. The next public command observes the fatal error.\n+          if (!this._fatalError) throw err;\n+        }\n+      });\n     }\n \n     // D10: reconcile first, then rebuild all state purely from the log.\n     if (this._worktrees && typeof this._worktrees.reconcile === 'function') {\n       Promise.resolve(this._worktrees.reconcile()).catch(noop);\n     }\n     if (this._runtimeScopes && typeof this._runtimeScopes.reconcile === 'function') {\n       Promise.resolve(this._runtimeScopes.reconcile([])).catch(noop);\n     }\n     this._replay();\n     this._terminalizeUnattachedCoordinationTasks();\n   }\n \n   // =========================================================================\n   // tick() — dispatch + deadline sweep. Called implicitly by every public command.\n   // =========================================================================\n \n   tick() {\n     if (this._fatalError) throw this._fatalError;\n     this._sweepDeadlines();\n     this._dispatchPass();\n   }\n \n   _dispatchPass() {\n     for (const taskId of this._taskOrder) {\n       const task = this._tasks.get(taskId);\n       if (!task || task.status !== 'pending') continue;\n       if (task.deps.some((d) => this._tasks.get(d)?.status !== 'completed')) continue;\n       const selection = this._resolveVendor(task);\n       const vendor = selection?.vendor;\n       if (!vendor || !this._adapters[vendor]) continue;\n       const card = this._adapters[vendor].card();\n       if (this._inFlightCount(vendor) >= card.concurrencyCeiling) continue;\n       this._dispatch(task, vendor, selection.model);\n     }\n   }\n \n   _sweepDeadlines() {\n     const now = this._now();\n     for (const [requestId, record] of [...this._pending]) {\n       if ((record.kind === 'approval' || record.kind === 'publication') && record.state === 'pending' && record.deadlineAt != null && now >= record.deadlineAt) {\n         this._resolveRecord(requestId, { decision: 'deny' }, 'policy').catch(noop);\n       }\n     }\n     for (const [workerId, waiter] of [...this._stopWaiters]) {\n       if (!waiter.finalized && waiter.deadlineAt != null && now >= waiter.deadlineAt) {\n         this._forceStop(workerId, waiter);\n       }\n     }\n   }\n \n   _resolveVendor(task) {\n     if (task.vendorRequested !== 'auto') {\n       const vendor = task.vendorRequested;\n       if (!cardSupportsSession(this._adapters[vendor]?.card(), task.sessionRequest)) return null;\n       const resolved = resolveCardModel(this._adapters[vendor]?.card(), task.modelRequested, task.modelPolicy, { explicit: true });\n       return resolved.ok ? { vendor, model: resolved.model } : null;\n     }\n     const cards = {};\n     const resolvedModels = {};\n     for (const [name, ad] of Object.entries(this._adapters)) {\n       const card = ad.card();\n       if (!cardSupportsSession(card, task.sessionRequest)) continue;\n       const resolved = resolveCardModel(card, task.modelRequested, task.modelPolicy, { explicit: false });\n       if (resolved.ok) {\n         cards[name] = {\n           ...card,\n           modelSelection: { ...(card.modelSelection ?? {}), resolved: resolved.model ?? null },\n         };\n         resolvedModels[name] = resolved.model;\n       }\n     }\n     const inFlight = {};\n     for (const name of Object.keys(this._adapters)) inFlight[name] = this._inFlightCount(name);\n     const chosen = this._route(task, cards, inFlight);\n     if (!chosen || !this._adapters[chosen] || !Object.hasOwn(resolvedModels, chosen)) return null;\n     return { vendor: chosen, model: resolvedModels[chosen] };\n   }\n \n   _inFlightCount(vendor) {\n@@ -824,365 +831,387 @@ export class Coordinator {\n         baseSha: review.baseSha,\n         resultSha: review.resultSha,\n         diffRange: review.baseSha ? `${review.baseSha}..${review.resultSha}` : null,\n       },\n     };\n     const child = await this.spawn(vendor, reviewBrief, {\n       taskId: opts.taskId,\n       model: opts.model,\n       modelPolicy: opts.modelPolicy,\n       taskType: kind,\n       refines: parent.id,\n       review,\n     });\n     this._log.append({\n       worker: workerId, harness: this._harnessOf(parentHandle.vendor), turnEpoch: this._safeTurnEpoch(parentHandle),\n       kind: 'review.requested', actor: opts.actor ?? 'orchestrator',\n       payload: { ...review, reviewerWorkerId: child.id, reviewerModelRequested: opts.model ?? null },\n     });\n     return child;\n   }\n \n   _autoTaskId() {\n     return `task-${++this._taskSeq}`;\n   }\n \n   _knownSessionContext(sessionId, vendor) {\n     for (const handle of this._workers.values()) {\n       if (handle.sessionRef?.id !== sessionId) continue;\n       if (vendor !== 'auto' && handle.vendor !== vendor) continue;\n       return { handle, context: handle.sessionContext ?? null };\n     }\n     return null;\n   }\n \n   async _validateSessionContext(context) {\n     if (this._repoRoot && context.repoRoot && context.repoRoot !== this._repoRoot) {\n       throw new SessionSelectionError('session context belongs to a different repository', 'session_context_mismatch');\n     }\n     if (typeof this._worktrees?.validateSessionContext === 'function') {\n       const verdict = await this._worktrees.validateSessionContext(context);\n       if (!verdict?.ok) {\n         throw new SessionSelectionError(verdict?.reason ?? 'session worktree is not reusable', 'session_context_mismatch');\n       }\n       return;\n     }\n     if (!existsSync(context.worktree)) {\n       throw new SessionSelectionError(`session worktree does not exist: ${context.worktree}`, 'session_context_missing');\n     }\n   }\n \n   /** PS7: explicitly reattach a replayed native session. Recovery never trusts a stale PID;\n    * authority comes only from a fresh adapter handshake that reports the expected native ID. */\n   async recover(workerId, opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     if (handle.status !== 'orphaned') return { ok: false, result: 'worker_not_orphaned' };\n     if (!task || !handle.sessionRef || handle.sessionRef.persistence !== 'native') {\n       return { ok: false, result: 'session_not_resumable' };\n     }\n     const adapter = this._adapters[handle.vendor];\n     if (!adapter || !cardSupportsSession(adapter.card(), { mode: 'resume' })) {\n       return { ok: false, result: 'session_not_resumable' };\n     }\n     const rawContext = opts.context ?? handle.sessionContext;\n     const context = rawContext\n       ? normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context: rawContext }).context\n       : null;\n     if (!context) return { ok: false, result: 'session_context_required' };\n     try {\n       await this._validateSessionContext(context);\n     } catch (err) {\n       return { ok: false, result: err.code ?? 'session_context_mismatch', reason: err.message };\n     }\n \n     const timeoutMs = opts.timeoutMs ?? this._recoveryTimeoutMs;\n     const admission = { events: [] };\n     admission.spawned = new Promise((resolve) => { admission.resolveSpawned = resolve; });\n     handle.turnAdmission = admission;\n     const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\n-    const runtime = this._ensureRuntimeScope(handle);\n-    this._log.append({\n+    const recoveryRequested = this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n       kind: 'control.recovery_requested', actor: opts.actor ?? 'orchestrator',\n       payload: { sessionRef: handle.sessionRef, context },\n     });\n+    const recoveryEvidence = this._coordMapEvent(recoveryRequested);\n+    this._coordRecord('recovery.requested', {\n+      taskId: task.id, workerId, sessionId: handle.sessionRef.id, context, evidence: recoveryEvidence,\n+    }, `driver.recovery.requested:${task.id}:${recoveryRequested.seq}`, opts.actor ?? 'orchestrator');\n+    const runtime = this._ensureRuntimeScope(handle);\n \n     let timerHandle;\n     let timedOut = false;\n     const timeout = new Promise((resolve) => {\n       timerHandle = this._setTimeout(() => { timedOut = true; resolve({ timeout: true }); }, timeoutMs);\n       if (timerHandle && typeof timerHandle.unref === 'function') timerHandle.unref();\n     });\n     const attempt = Promise.resolve(adapter.spawn(workerId, task.brief, {\n       worktree: context.worktree,\n       timeoutMs: task.brief?.budget?.wallMin ? task.brief.budget.wallMin * 60000 : undefined,\n       model: handle.modelResolved ?? undefined,\n       reasoningEffort: handle.modelPolicy?.reasoningEffort,\n       serviceTier: handle.modelPolicy?.serviceTier,\n       session,\n       env: runtime?.env,\n       replaceEnv: runtime?.replaceEnv === true,\n     })).then((ack) => ({ ack }), (error) => ({ error }));\n \n     let outcome = await Promise.race([attempt, timeout]);\n     if (outcome?.ack?.ok === true && !timedOut) {\n       outcome = await Promise.race([\n         admission.spawned.then((event) => ({ ack: outcome.ack, spawned: event })),\n         timeout,\n       ]);\n     }\n     if (timerHandle != null) this._clearTimeout(timerHandle);\n \n     const expectedId = handle.sessionRef.id;\n     const observedId = outcome?.spawned?.payload?.threadId ?? outcome?.spawned?.payload?.sessionId;\n     const failed = outcome?.timeout\n       ? { result: 'recovery_timeout', reason: `native reattachment exceeded ${timeoutMs}ms` }\n       : outcome?.error\n         ? { result: 'recovery_exception', reason: String(outcome.error?.message ?? outcome.error) }\n         : outcome?.ack?.ok !== true\n           ? { result: 'recovery_refused', reason: outcome?.ack?.reason ?? 'adapter refused recovery' }\n           : observedId !== expectedId\n             ? { result: 'session_identity_mismatch', reason: `expected ${expectedId}, observed ${observedId ?? '(none)'}` }\n             : null;\n \n     if (failed) {\n       if (handle.turnAdmission === admission) handle.turnAdmission = null;\n       handle.status = 'orphaned';\n       this._log.append({\n         worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n         kind: 'control.recovery_failed', actor: 'policy', payload: { ...failed, action: 'kill_untrusted_transport' },\n       });\n       Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n       return { ok: false, ...failed };\n     }\n \n     const stamp = this._fences.bumpTurn(workerId);\n-    const activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n+    let activeTask;\n+    try {\n+      activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n+    } catch (err) {\n+      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n+      handle.status = 'orphaned';\n+      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n+      throw err;\n+    }\n     activeTask.status = 'working';\n     activeTask.result = null;\n     activeTask.verdict = null;\n     activeTask.sessionRequest = session;\n     activeTask.sessionContext = context;\n     handle.status = 'working';\n     handle.sessionRequest = session;\n     handle.sessionContext = context;\n     handle.turnAdmission = null;\n     this._resetWatchdogTurn(handle);\n     this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n       kind: 'control.recovery_attached', actor: 'orchestrator',\n       payload: { sessionRef: handle.sessionRef, context },\n     });\n     this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n       kind: 'lifecycle.turn_started', actor: 'orchestrator', payload: { recovery: true },\n       modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n     });\n     for (const event of admission.events) this._handleEvent(event);\n     return { ok: true, result: 'attached', handle: this._publicHandle(handle) };\n   }\n \n   /** AC5: explicitly integrate an accepted captured commit. This never pushes. */\n   async integrate(workerId, opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     if (!task || task.status !== 'completed' || !task.capturedSha) {\n       throw new IntegrationError('integration requires an accepted captured task result', 'result_not_accepted');\n     }\n     if (this._requireIndependentOracle) {\n       const oracle = [...this._tasks.values()].find((candidate) =>\n         candidate.review?.parentTaskId === task.id\n         && candidate.review.kind === 'oracle'\n         && candidate.review.independent === true\n         && candidate.status === 'completed');\n       if (!oracle) {\n         throw new IntegrationError('integration requires a completed independent oracle from a different model family', 'independent_oracle_required');\n       }\n     }\n     const strategy = opts.strategy ?? 'ff-only';\n     if (strategy !== 'ff-only') {\n       throw new IntegrationError(`unsupported integration strategy: ${strategy}`, 'unsupported_strategy');\n     }\n     if (!this._worktrees || typeof this._worktrees.integrate !== 'function') {\n       throw new IntegrationError('worktree manager does not implement integration', 'integration_unavailable');\n     }\n     if (handle.status === 'working' || handle.status === 'blocked' || handle.status === 'stopping' || handle.status === 'pending') {\n       throw new IntegrationError('worker must be idle, dead, exited, or orphaned before integration', 'worker_not_quiescent');\n     }\n \n+    this._coordRecord('integration.requested', {\n+      taskId: task.id, workerId, strategy, sha: task.capturedSha,\n+      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\n+    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\n+\n     if (typeof this._worktrees.retainResult === 'function') {\n       task.retainedResultRef = await this._worktrees.retainResult(task.capturedSha);\n     }\n \n     if (handle.status === 'idle') {\n       const stopped = await this.kill(workerId, opts.actor ?? 'orchestrator');\n       if (!['confirmed', 'already_dead', 'already_stopped'].includes(stopped.result)) {\n         throw new IntegrationError('worker could not be safely stopped before integration', 'worker_stop_failed');\n       }\n     } else if (handle.status === 'exited') {\n       await this.kill(workerId, opts.actor ?? 'orchestrator');\n     }\n     await this._removeTaskWorktree(task);\n \n     let integrated;\n     try {\n       integrated = await this._worktrees.integrate(task.capturedSha, { strategy });\n     } catch (err) {\n       const refusedEvent = this._log.append({\n         worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n         kind: 'integration.refused', actor: 'policy',\n         payload: { strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef, reason: String(err?.message ?? err) },\n       });\n       const refusedEvidence = this._coordMapEvent(refusedEvent);\n       this._coordRecord('integration.refused', {\n         taskId: task.id, strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef,\n         reason: String(err?.message ?? err), evidence: refusedEvidence,\n       }, `driver.integration.refused:${task.id}:${refusedEvent.seq}`, 'policy');\n       throw new IntegrationError(String(err?.message ?? err), 'non_fast_forward_or_dirty');\n     }\n     if (task.retainedResultRef && typeof this._worktrees.releaseResult === 'function') {\n       try { await this._worktrees.releaseResult(task.retainedResultRef); } catch { /* merged HEAD now retains the result */ }\n       task.retainedResultRef = null;\n     }\n     task.integration = Object.freeze({ ...integrated, strategy, actor: opts.actor ?? 'orchestrator' });\n     const integrationEvent = this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n       kind: 'integration.completed', actor: opts.actor ?? 'orchestrator', payload: task.integration,\n     });\n     const integrationEvidence = this._coordMapEvent(integrationEvent);\n     if (this._coordination) {\n       const acceptingEvidence = this._coordination.task(task.id).artifactIds\n         .map((artifactId) => this._coordination.artifact(artifactId))\n         .filter((artifact) => artifact?.accepted === true)\n         .flatMap((artifact) => artifact.provenance ?? []);\n       this._coordination.registerArtifact({\n         taskId: task.id, kind: 'report', refs: { beforeSha: task.integration.beforeSha, resultSha: task.integration.resultSha, afterSha: task.integration.afterSha },\n         mediaType: 'application/vnd.baton.integration+json', accepted: true, provenance: [integrationEvidence, ...acceptingEvidence],\n       }, { actor: opts.actor ?? 'orchestrator', key: `artifact.integration:${task.id}:${task.integration.afterSha}` });\n       this._coordination.promoteKnowledgeNode({\n         id: `decision:integrate:${task.id}:${integrationEvent.seq}`, type: 'Decision',\n         body: `Integrated task ${task.id} at ${task.integration.afterSha}`, grounding: 'observed',\n         informedBy: [`task:${task.id}`],\n         evidence: [{ coordinationSeq: integrationEvidence.coordinationSeq }],\n       }, { kind: 'Decision', trigger: 'integration' }, { actor: opts.actor ?? 'orchestrator', key: `knowledge.integration:${task.id}:${integrationEvent.seq}` });\n       this._coordRecord('integration.completed', { taskId: task.id, integration: task.integration, evidence: integrationEvidence }, `driver.integration:${task.id}:${integrationEvent.seq}`, opts.actor ?? 'orchestrator');\n     }\n     return { ok: true, result: 'integrated', integration: task.integration };\n   }\n \n   /** AC6: create an approval-gated exact-SHA publication request. No side effect occurs here. */\n   requestPublication(workerId, target = {}, actor = 'orchestrator') {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     if (!task?.integration?.afterSha) {\n       throw new PublicationError('publication requires a locally integrated result', 'result_not_integrated');\n     }\n     const remote = target.remote;\n     const ref = target.ref;\n     const sha = target.sha ?? task.integration.afterSha;\n     if (typeof remote !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9._-]*$/.test(remote)) {\n       throw new PublicationError('remote must be a credential-free git remote name', 'invalid_remote');\n     }\n     if (typeof ref !== 'string' || !/^refs\\/heads\\/[A-Za-z0-9._\\/-]+$/.test(ref) || ref.includes('..')) {\n       throw new PublicationError('ref must be a full, safe refs/heads/* name', 'invalid_ref');\n     }\n     if (sha !== task.integration.afterSha) {\n       throw new PublicationError('publication SHA must equal the integrated result SHA', 'sha_mismatch');\n     }\n     const stamp = this._fences.bumpHuman(workerId);\n     const requestId = `publication-${workerId}-${++this._publicationSeq}`;\n     const publication = Object.freeze({ remote, ref, sha });\n     const deadlineAt = this._now() + this._approvalTimeoutMs;\n-    this._pending.set(requestId, {\n+    const record = {\n       kind: 'publication', worker: workerId, state: 'pending', resolution: null, consumer: null,\n       turnEpochAtAsk: stamp.turnEpoch, fenceAtAsk: stamp.fence,\n       deadlineAt, publication,\n-    });\n-    this._log.append({\n+    };\n+    const requestedEvent = this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n       kind: 'publication.requested', actor,\n       payload: { requestId, ...publication, fence: stamp.fence, deadlineAt },\n     });\n+    const evidence = this._coordMapEvent(requestedEvent);\n+    this._coordRecord('publication.requested', {\n+      taskId: task.id, workerId, requestId, publication, fence: stamp.fence, deadlineAt, evidence,\n+    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\n+    this._pending.set(requestId, record);\n     return { ok: true, requestId, fence: stamp.fence, target: publication };\n   }\n \n   _allocWorkerId() {\n     return `w-${++this._workerSeq}`;\n   }\n \n   _assertNoCycle(taskId, deps) {\n     const graph = new Map();\n     for (const [id, t] of this._tasks) graph.set(id, t.deps);\n     graph.set(taskId, deps);\n \n     const visiting = new Set();\n     const visited = new Set();\n     const dfs = (node) => {\n       if (visited.has(node)) return false;\n       if (visiting.has(node)) return true;\n       visiting.add(node);\n       for (const dep of graph.get(node) ?? []) {\n         if (dfs(dep)) return true;\n       }\n       visiting.delete(node);\n       visited.add(node);\n       return false;\n     };\n     if (dfs(taskId)) throw new DependencyCycleError(`spawn() would create a dependency cycle at \"${taskId}\"`);\n   }\n \n   _publicHandle(handle) {\n     let fence = null;\n     let turnEpoch = null;\n     if (handle.status !== 'pending') {\n       try {\n         const s = this._fences.current(handle.id);\n         fence = s.fence;\n         turnEpoch = s.turnEpoch;\n       } catch {\n         // not yet registered — leave null\n       }\n     }\n     return {\n       id: handle.id,\n       vendor: handle.vendor,\n       modelRequested: handle.modelRequested ?? null,\n       modelResolved: handle.modelResolved ?? null,\n       modelObserved: handle.modelObserved ?? null,\n       modelMismatch: handle.modelMismatch ?? null,\n       modelPolicy: handle.modelPolicy ?? null,\n       sessionRequest: handle.sessionRequest ?? { mode: 'new' },\n       sessionRef: handle.sessionRef ?? null,\n       sessionContext: handle.sessionContext ?? null,\n       lineage: handle.lineage ?? null,\n       runtimeScope: handle.runtimeScope ?? null,\n       review: this._tasks.get(handle.taskId)?.review ?? null,\n       taskId: handle.taskId,\n       worktree: handle.worktree,\n       fence,\n       turnEpoch,\n       status: handle.status,\n       pendingApprovalId: handle.pendingApprovalId,\n       pendingQuestionId: handle.pendingQuestionId,\n       budgetUsed: { ...handle.budgetUsed },\n       createdAt: handle.createdAt,\n     };\n   }\n \n   _getWorker(workerId) {\n     const h = this._workers.get(workerId);\n     if (!h) throw new WorkerNotFoundError(`unknown worker \"${workerId}\"`);\n     return h;\n   }\n \n   // =========================================================================\n   // Command: send()\n   // =========================================================================\n \n   async send(workerId, message, mode, opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     // SC4a: per-worker delivery serialization — deliveries reach the adapter strictly in\n@@ -1201,294 +1230,317 @@ export class Coordinator {\n     const task = this._tasks.get(handle.taskId);\n     // SC14: delivery-slot acquisition is the authority boundary. A queued continuation cannot\n     // cross a finalized stop, and a terminal task cannot be resurrected by a surviving session.\n     if (handle.status === 'stopping') return { ok: false, result: 'worker_stopping' };\n     const card = this._adapters[handle.vendor]?.card();\n     const reusableFollowUp = mode === 'turn'\n       && handle.status === 'idle'\n       && task && TERMINAL_TASK_STATUSES.has(task.status)\n       && ['native', 'emulated'].includes(card?.sessions?.multiTurn);\n     if (handle.status === 'idle' && !reusableFollowUp) return { ok: false, result: 'worker_not_active' };\n     if (handle.status === 'dead' || handle.status === 'exited' || handle.status === 'orphaned' || handle.status === 'pending') {\n       return { ok: false, result: 'worker_not_active' };\n     }\n     if (!task || (TERMINAL_TASK_STATUSES.has(task.status) && !reusableFollowUp)) return { ok: false, result: 'task_terminal' };\n \n     if (reusableFollowUp) return this._deliverFollowUp(handle, task, message, opts);\n \n     // C3: pre-check against an externally-supplied fence, BEFORE any delivery attempt —\n     // re-evaluated HERE at delivery-slot acquisition, not at send() entry (SC4b).\n     if (opts.expectedFence !== undefined) {\n       const preCheck = this._fences.check(workerId, { fence: opts.expectedFence });\n       if (!preCheck.ok) {\n         const harness = this._harnessOf(handle.vendor);\n         const recoveryEvent = this._log.append({\n           worker: workerId,\n           harness,\n           turnEpoch: this._fences.current(workerId).turnEpoch,\n           kind: 'control.stale_rejected',\n           actor: 'orchestrator',\n           payload: { op: 'send', mode, attempted: opts.expectedFence, current: preCheck.current, phase: 'pre_delivery' },\n         });\n         return { ok: false, result: 'stale_fence', current: preCheck.current };\n       }\n     }\n \n     const stamp = this._fences.issue(workerId);\n     const harness = this._harnessOf(handle.vendor);\n     const ack = await this._adapters[handle.vendor].prompt(workerId, message, mode);\n     const check = this._fences.check(workerId, stamp);\n     const currentTurnEpoch = this._fences.current(workerId).turnEpoch;\n \n     if (!check.ok) {\n       this._log.append({\n         worker: workerId,\n         harness,\n         turnEpoch: currentTurnEpoch,\n         kind: 'control.stale_rejected',\n         actor: 'orchestrator',\n         payload: { op: 'send', mode, attempted: stamp, current: check.current, phase: 'post_delivery' },\n       });\n       // C3: delivery already happened despite the staleness — say so, loudly.\n       this._log.append({\n         worker: workerId,\n         harness,\n         turnEpoch: currentTurnEpoch,\n         kind: 'control.delivery_amended',\n         actor: 'policy',\n         payload: { op: 'send', mode, message, deliveredDespiteStale: true, attempted: stamp, current: check.current },\n       });\n       return { ok: false, result: 'stale_fence', current: check.current };\n     }\n \n     if (ack && ack.ok === false) {\n       return { ok: false, result: ack.reason ?? 'delivery_refused', reason: ack.reason };\n     }\n \n     const kind = mode === 'nudge' ? 'control.nudge' : mode === 'steer' ? 'control.steer' : 'control.send';\n     const ev = { worker: workerId, harness, turnEpoch: currentTurnEpoch, kind, actor: 'orchestrator', payload: { message } };\n     if (ack && ack.emulated === true) ev.emulated = true;\n     this._log.append(ev);\n     return { ok: true, result: 'ok', emulated: ack && ack.emulated === true };\n   }\n \n   async _deliverFollowUp(handle, task, message, opts) {\n     const workerId = handle.id;\n     if (opts.expectedFence !== undefined) {\n       const preCheck = this._fences.check(workerId, { fence: opts.expectedFence });\n       if (!preCheck.ok) return { ok: false, result: 'stale_fence', current: preCheck.current };\n     }\n \n+    const requestedEvent = this._log.append({\n+      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+      kind: 'control.follow_up_requested', actor: opts.actor ?? 'orchestrator',\n+      payload: { message, expectedFence: opts.expectedFence ?? null },\n+    });\n+    const requestedEvidence = this._coordMapEvent(requestedEvent);\n+    this._coordRecord('follow_up.requested', {\n+      taskId: task.id, workerId, expectedFence: opts.expectedFence ?? null, evidence: requestedEvidence,\n+    }, `driver.follow_up.requested:${task.id}:${requestedEvent.seq}`, opts.actor ?? 'orchestrator');\n+\n     // A native adapter can emit turn_started synchronously inside prompt(), before returning its\n     // Ack. Queue those events until admission commits; refusal leaves the prior terminal view.\n     const admission = { events: [] };\n     handle.turnAdmission = admission;\n     let ack;\n     try {\n       ack = await this._adapters[handle.vendor].prompt(workerId, message, 'turn');\n     } catch (err) {\n       if (handle.turnAdmission === admission) handle.turnAdmission = null;\n       if (admission.events.length > 0) this._rejectContradictoryAdmission(handle, admission, err);\n       return { ok: false, result: 'delivery_exception', reason: String(err?.message ?? err) };\n     }\n     // A crash/exit is intentionally processed immediately instead of queued. It wins over an Ack\n     // from the same call and can never be overwritten by reopening the prior terminal task.\n     if (handle.status !== 'idle') {\n       if (handle.turnAdmission === admission) handle.turnAdmission = null;\n       return { ok: false, result: 'worker_not_active' };\n     }\n     if (!ack || ack.ok !== true) {\n       if (handle.turnAdmission === admission) handle.turnAdmission = null;\n       if (admission.events.length > 0) this._rejectContradictoryAdmission(handle, admission, ack?.reason);\n       return { ok: false, result: ack?.reason ?? 'delivery_refused', reason: ack?.reason };\n     }\n \n     const stamp = this._fences.bumpTurn(workerId);\n-    const activeTask = this._createCoordinationRefinement(handle, task, 'follow_up');\n+    let activeTask;\n+    try {\n+      activeTask = this._createCoordinationRefinement(handle, task, 'follow_up');\n+    } catch (err) {\n+      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n+      handle.status = 'dead';\n+      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\n+      this._removeRuntimeScope(handle);\n+      this._removeTaskWorktree(task).catch(noop);\n+      throw err;\n+    }\n     activeTask.status = 'working';\n     activeTask.result = null;\n     activeTask.verdict = null;\n     handle.status = 'working';\n     handle.turnAdmission = null;\n     this._resetWatchdogTurn(handle);\n     this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n       kind: 'lifecycle.turn_started', actor: 'orchestrator',\n       modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n       payload: { followUp: true, message },\n     });\n     for (const event of admission.events) this._handleEvent(event);\n     return { ok: true, result: 'ok', emulated: ack.emulated === true };\n   }\n \n   _rejectContradictoryAdmission(handle, admission, reason) {\n     this._log.append({\n       worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n       kind: 'control.protocol_violation', actor: 'policy',\n       payload: {\n         op: 'follow_up_admission', reason: String(reason ?? 'adapter refused after emitting turn events'),\n         queuedKinds: admission.events.map((event) => event.kind), action: 'kill',\n       },\n     });\n     // The old result remains authoritative, but the session is no longer safe to reuse: its wire\n     // advanced despite refusing admission. Confirmed two-phase kill owns transport cleanup.\n     this._beginStop(handle, 'kill', undefined, 'policy').catch(noop);\n   }\n \n   // =========================================================================\n   // Command: interrupt() / kill() — two-phase stop (D9)\n   // =========================================================================\n \n   async interrupt(workerId, then, actor = 'orchestrator', opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     if (opts.expectedFence !== undefined) {\n       const check = this._fences.check(workerId, { fence: opts.expectedFence });\n       if (!check.ok) return { ok: false, result: 'stale_fence', current: check.current };\n     }\n     if (handle.status === 'dead' || handle.status === 'exited') {\n       return { ok: true, result: handle.status === 'dead' ? 'already_dead' : 'already_stopped' };\n     }\n     if (handle.status === 'orphaned') {\n       return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\n     }\n     return this._beginStop(handle, 'interrupt', then, actor);\n   }\n \n   async kill(workerId, actor = 'orchestrator', opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     if (opts.expectedFence !== undefined) {\n       const check = this._fences.check(workerId, { fence: opts.expectedFence });\n       if (!check.ok) return { ok: false, result: 'stale_fence', current: check.current };\n     }\n     if (handle.status === 'dead') return { ok: true, result: 'already_dead' };\n     if (handle.status === 'orphaned') {\n       return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\n     }\n     // CI3: a crashed/exited child cannot emit another kill.confirmed. Treat its authoritative\n     // terminal event as the confirmation, finish cleanup now, and never arm an unfulfillable wait.\n     if (handle.status === 'exited') {\n       handle.status = 'dead';\n       this._removeRuntimeScope(handle);\n       await this._removeTaskWorktree(this._tasks.get(handle.taskId));\n       return { ok: true, result: 'already_dead' };\n     }\n     return this._beginStop(handle, 'kill', undefined, actor);\n   }\n \n   _beginStop(handle, mode, then, actor) {\n     const existing = this._stopWaiters.get(handle.id);\n     if (existing) {\n       if (mode === 'kill' && existing.mode !== 'kill') {\n+        const harness = this._harnessOf(handle.vendor);\n+        const requested = this._log.append({ worker: handle.id, harness, turnEpoch: this._safeTurnEpoch(handle), kind: 'kill.requested', actor, payload: {} });\n+        const evidence = this._coordMapEvent(requested);\n+        this._coordRecord('control.stop_requested', { taskId: handle.taskId, workerId: handle.id, mode: 'kill', escalation: true, evidence }, `driver.stop_requested:${handle.taskId}:${requested.seq}`, actor);\n         existing.mode = 'kill';\n         existing.ackReady = false;\n         existing.confirmReceived = false;\n-        const harness = this._harnessOf(handle.vendor);\n-        this._log.append({ worker: handle.id, harness, turnEpoch: this._safeTurnEpoch(handle), kind: 'kill.requested', actor, payload: {} });\n         const call = Promise.resolve(this._adapters[handle.vendor].kill(handle.id));\n         this._wireAck(existing, call);\n       }\n       return new Promise((resolve) => existing.resolvers.push(resolve));\n     }\n \n+    this._fences.bumpHuman(handle.id);\n+    const harness = this._harnessOf(handle.vendor);\n+    const turnEpoch = this._safeTurnEpoch(handle);\n+    const reqKind = mode === 'kill' ? 'kill.requested' : 'control.interrupt_requested';\n+    const reqPayload = mode === 'kill' ? {} : { then: then ?? null, actor };\n+    const requested = this._log.append({ worker: handle.id, harness, turnEpoch, kind: reqKind, actor, payload: reqPayload });\n+    const evidence = this._coordMapEvent(requested);\n+    this._coordRecord('control.stop_requested', { taskId: handle.taskId, workerId: handle.id, mode, then: then ?? null, evidence }, `driver.stop_requested:${handle.taskId}:${requested.seq}`, actor);\n+\n     if (handle.status === 'blocked') {\n       if (handle.pendingApprovalId) {\n         this._resolveRecord(handle.pendingApprovalId, { decision: 'cancel' }, actor).catch(noop);\n       } else if (handle.pendingQuestionId) {\n         this._resolveRecord(handle.pendingQuestionId, { decision: 'cancel' }, actor).catch(noop);\n       }\n     }\n-\n-    this._fences.bumpHuman(handle.id);\n     if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) {\n       handle.spawnAbort.abort({ mode, actor });\n     }\n     handle.status = 'stopping';\n     this._clearWatchdog(handle);\n \n-    const harness = this._harnessOf(handle.vendor);\n-    const turnEpoch = this._safeTurnEpoch(handle);\n-    const reqKind = mode === 'kill' ? 'kill.requested' : 'control.interrupt_requested';\n-    const reqPayload = mode === 'kill' ? {} : { then: then ?? null, actor };\n-    this._log.append({ worker: handle.id, harness, turnEpoch, kind: reqKind, actor, payload: reqPayload });\n-\n     const waiter = {\n       mode,\n       workerId: handle.id,\n       emulated: false,\n       resolvers: [],\n       deadlineAt: this._now() + this._stopDeadlineMs,\n       ackReady: false,\n       confirmReceived: false,\n       finalized: false,\n       timerHandle: null,\n       then: mode === 'interrupt' ? then : undefined,\n     };\n     this._stopWaiters.set(handle.id, waiter);\n \n     // C4: a real, injectable, unref'd deadline timer — independent of tick()'s sweep,\n     // which remains as a redundant, harmless backup path.\n     waiter.timerHandle = this._setTimeout(() => this._forceStop(handle.id, waiter), this._stopDeadlineMs);\n     if (waiter.timerHandle && typeof waiter.timerHandle.unref === 'function') waiter.timerHandle.unref();\n \n     const call =\n       mode === 'kill'\n         ? Promise.resolve(this._adapters[handle.vendor].kill(handle.id))\n         : Promise.resolve(this._adapters[handle.vendor].interrupt(handle.id, then));\n     this._wireAck(waiter, call);\n \n     return new Promise((resolve) => waiter.resolvers.push(resolve));\n   }\n \n   _safeTurnEpoch(handle) {\n     try {\n       return this._fences.current(handle.id).turnEpoch;\n     } catch {\n       return 0;\n     }\n   }\n \n   _coordTransition(task, to, key, evidence = null, actor = 'policy') {\n     if (!this._coordination || !task) return null;\n     const durable = this._coordination.task(task.id);\n     if (!durable || durable.status === to) return durable;\n     const result = this._coordination.transitionTask(task.id, to, task.coordinationVersion ?? durable.version, { actor, key }, evidence);\n     task.coordinationVersion = result.task.version;\n     if (TERMINAL_TASK_STATUSES.has(to)) {\n       const handle = this._workers.get(task.assignee);\n       this._expireScratchClaims(handle, task, `task_${to}`);\n     }\n     return result.task;\n   }\n \n   _coordMap(event, key) {\n     if (!this._coordination || !event) return null;\n     return this._coordination.mapOperationalEvent(event, { actor: 'policy', key }).evidence;\n   }\n \n   _coordMapEvent(event) {\n     if (!event) return null;\n     return this._coordMap(event, `evidence:${event.worker}:${event.seq}`);\n   }\n \n   _coordRecord(kind, payload, key, actor = 'policy') {\n     if (!this._coordination) return null;\n     return this._coordination.recordDriver(kind, payload, { actor, key }).event;\n   }\n \n   _poisonCoordination(err) {\n     if (!this._fatalError) {\n       const fatal = new Error(`authoritative coordination mutation failed: ${err?.message ?? err}`, { cause: err });\n       fatal.name = 'CoordinationWriteIntegrityError';\n       fatal.code = 'coordination_write_unavailable';\n       this._fatalError = fatal;\n       for (const handle of this._workers.values()) {\n         if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) handle.spawnAbort.abort({ reason: 'coordination_write_unavailable' });\n       }\n     }\n     return this._fatalError;\n   }\n \n   _createCoordinationRefinement(handle, prior, relation) {\n     if (!this._coordination) return prior;\n     const id = `${prior.id}:refinement-${++this._refinementSeq}`;\n@@ -1661,416 +1713,472 @@ export class Coordinator {\n     }\n     if (event.kind === 'content.tool_call') {\n       const payload = event.payload ?? {};\n       const command = payload.command ?? payload.cmd ?? payload.item?.command ?? payload.rawInput?.command ?? payload.rawOutput?.command;\n       const exitCode = payload.exitCode ?? payload.item?.exitCode ?? payload.rawOutput?.exit_code;\n       const status = payload.status ?? payload.item?.status ?? (exitCode !== undefined ? 'completed' : null);\n       if (typeof command === 'string' && status === 'completed' && Number(exitCode) !== 0) {\n         const signature = `${command}::${Number(exitCode)}`;\n         handle.recentFailedActions.push(signature);\n         const threshold = this._watchdog.loopThreshold;\n         const tail = handle.recentFailedActions.slice(-threshold);\n         if (threshold > 0 && tail.length === threshold && tail.every((value) => value === signature) && !handle.watchdogActions.has('loop')) {\n           handle.watchdogActions.add('loop');\n           this._log.append({\n             worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n             kind: 'health.loop_suspected', actor: 'policy',\n             payload: { command, exitCode: Number(exitCode), count: threshold, action: this._watchdog.loopAction, mechanical: true },\n           });\n           this._applyWatchdogAction(handle, this._watchdog.loopAction);\n         }\n       }\n       return;\n     }\n     if (event.kind === 'content.file_edit') {\n       const payload = event.payload ?? {};\n       const rawPaths = [payload.path, ...(payload.paths ?? []), payload.item?.path,\n         ...((payload.item?.changes ?? []).map((change) => change.path)),\n         ...((payload.content ?? []).filter((item) => item?.type === 'diff').map((item) => item.path))].filter(Boolean);\n       const task = this._tasks.get(handle.taskId);\n       for (const rawPath of rawPaths) {\n         const path = this._relativeActionPath(handle, rawPath);\n         if (!path || pathInScope(task?.brief?.pathScope, path)) continue;\n         if (handle.watchdogActions.has('scope')) break;\n         handle.watchdogActions.add('scope');\n         this._log.append({\n           worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n           kind: 'health.scope_violation', actor: 'policy',\n           payload: { path, observedPath: rawPath, action: this._watchdog.scopeAction, mechanical: true },\n         });\n         this._applyWatchdogAction(handle, this._watchdog.scopeAction);\n       }\n     }\n   }\n \n   _wireAck(waiter, call) {\n     call\n       .then((ack) => {\n         waiter.emulated = !!(ack && ack.emulated === true);\n         waiter.ackReady = true;\n         if (ack?.ok === true && ack?.terminal === true) waiter.confirmReceived = true;\n         if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\n       })\n       .catch(() => {\n         waiter.ackReady = true;\n         if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\n       });\n   }\n \n   _onStopConfirmed(handle, confirmKind) {\n     const waiter = this._stopWaiters.get(handle.id);\n     if (!waiter) return;\n     if (confirmKind !== waiter.mode) return; // stale/mismatched confirmation — ignore\n     waiter.confirmReceived = true;\n     if (waiter.ackReady) this._finalizeStop(handle.id, waiter);\n   }\n \n   _finalizeStop(workerId, waiter) {\n     if (waiter.finalized) return;\n     waiter.finalized = true;\n     if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\n     const handle = this._workers.get(workerId);\n     const harness = handle ? this._harnessOf(handle.vendor) : '';\n     const kind = waiter.mode === 'kill' ? 'kill.confirmed' : 'control.interrupt_confirmed';\n     const ev = {\n       worker: workerId, harness, turnEpoch: handle ? this._safeTurnEpoch(handle) : 0, kind, actor: 'worker', payload: {},\n       modelRequested: handle?.modelRequested ?? null, modelResolved: handle?.modelResolved ?? null, modelObserved: handle?.modelObserved ?? null,\n     };\n     if (waiter.emulated) ev.emulated = true;\n     const stopEvent = this._log.append(ev);\n \n-    if (handle) {\n-      const task = this._tasks.get(handle.taskId);\n-      if (waiter.mode === 'kill') {\n-        if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n-          const evidence = this._coordMapEvent(stopEvent);\n-          this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n-        }\n-        handle.status = 'dead';\n-        this._removeRuntimeScope(handle);\n-        if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n-        this._removeTaskWorktree(task).catch(noop);\n-      } else {\n-        if (waiter.then !== undefined) {\n-          const stamp = this._fences.bumpTurn(handle.id);\n-          handle.status = 'working';\n-          if (task) {\n-            task.status = 'working';\n-            task.result = null;\n-            task.verdict = null;\n-          }\n-          this._log.append({\n-            worker: workerId, harness, turnEpoch: stamp.turnEpoch, kind: 'lifecycle.turn_started', actor: 'orchestrator',\n-            modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n-            payload: { followUp: true, afterInterrupt: true },\n-          });\n-          this._resetWatchdogTurn(handle);\n-        } else {\n+    try {\n+      if (handle) {\n+        const task = this._tasks.get(handle.taskId);\n+        if (waiter.mode === 'kill') {\n           if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n             const evidence = this._coordMapEvent(stopEvent);\n             this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n           }\n-          handle.status = 'idle';\n+          handle.status = 'dead';\n+          this._removeRuntimeScope(handle);\n           if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n+          this._removeTaskWorktree(task).catch(noop);\n+        } else {\n+          if (waiter.then !== undefined) {\n+            const stamp = this._fences.bumpTurn(handle.id);\n+            handle.status = 'working';\n+            if (task) {\n+              task.status = 'working';\n+              task.result = null;\n+              task.verdict = null;\n+            }\n+            this._log.append({\n+              worker: workerId, harness, turnEpoch: stamp.turnEpoch, kind: 'lifecycle.turn_started', actor: 'orchestrator',\n+              modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n+              payload: { followUp: true, afterInterrupt: true },\n+            });\n+            this._resetWatchdogTurn(handle);\n+          } else {\n+            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n+              const evidence = this._coordMapEvent(stopEvent);\n+              this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n+            }\n+            handle.status = 'idle';\n+            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n+          }\n         }\n       }\n+    } catch (err) {\n+      if (handle) {\n+        handle.status = 'dead';\n+        this._removeRuntimeScope(handle);\n+        this._removeTaskWorktree(this._tasks.get(handle.taskId)).catch(noop);\n+        Promise.resolve(this._adapters[handle.vendor]?.kill(handle.id)).catch(noop);\n+      }\n+      for (const resolve of waiter.resolvers) resolve({ ok: false, result: 'coordination_unavailable' });\n+      this._stopWaiters.delete(workerId);\n+      throw err;\n     }\n \n     const result = { ok: true, result: 'confirmed', emulated: waiter.emulated === true };\n     for (const resolve of waiter.resolvers) resolve(result);\n     this._stopWaiters.delete(workerId);\n     this._dispatchPass();\n   }\n \n   _forceStop(workerId, waiter) {\n     if (waiter.finalized) return;\n     waiter.finalized = true;\n     if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\n     const handle = this._workers.get(workerId);\n     const harness = handle ? this._harnessOf(handle.vendor) : '';\n     const forcedEvent = this._log.append({ worker: workerId, harness, turnEpoch: handle ? this._safeTurnEpoch(handle) : 0, kind: 'control.forced_stop', actor: 'policy', payload: {} });\n \n+    let coordinationFailure = null;\n+    if (handle) {\n+      const task = this._tasks.get(handle.taskId);\n+      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n+        try {\n+          const evidence = this._coordMapEvent(forcedEvent);\n+          this._coordTransition(task, 'failed', `task.failed:${task.id}:${forcedEvent.seq}`, evidence);\n+        } catch (err) {\n+          coordinationFailure = err;\n+        }\n+      }\n+    }\n+\n     if (handle && this._adapters[handle.vendor]) {\n       Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\n     }\n \n     if (handle) {\n       handle.status = 'dead';\n       this._removeRuntimeScope(handle);\n       const task = this._tasks.get(handle.taskId);\n-      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n-        const evidence = this._coordMapEvent(forcedEvent);\n-        this._coordTransition(task, 'failed', `task.failed:${task.id}:${forcedEvent.seq}`, evidence);\n-      }\n-      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\n+      if (!coordinationFailure && task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\n       this._removeTaskWorktree(task).catch(noop);\n     }\n \n-    const result = { ok: true, result: 'forced' };\n+    const result = coordinationFailure ? { ok: false, result: 'coordination_unavailable' } : { ok: true, result: 'forced' };\n     for (const resolve of waiter.resolvers) resolve(result);\n     this._stopWaiters.delete(workerId);\n   }\n \n   // =========================================================================\n   // Command: respond()\n   // =========================================================================\n \n   async respond(requestId, answer, actor = 'orchestrator') {\n     this.tick();\n     return this._resolveRecord(requestId, answer, actor);\n   }\n \n   async _resolveRecord(requestId, answer, actor) {\n     const record = this._pending.get(requestId);\n     if (!record) return { ok: false, result: 'not_found' };\n     if (record.state === 'resolving') {\n       // Wait for the reserved delivery. Echo its winner if it commits; retry fairly if the\n       // delivery rolls back to pending.\n       await record.resolvingDone;\n       if (record.state === 'resolved') {\n         return { ok: false, result: 'already_resolved', resolution: record.resolution };\n       }\n       return this._resolveRecord(requestId, answer, actor);\n     }\n     if (record.state !== 'pending') return { ok: false, result: 'already_resolved', resolution: record.resolution };\n \n     // CI2: reserve the single-consumer slot, but do not COMMIT resolution until the adapter\n     // accepts delivery. A failed/throwing wire operation rolls back to pending for retry.\n     record.state = 'resolving';\n     let releaseResolving;\n     record.resolvingDone = new Promise((resolve) => { releaseResolving = resolve; });\n     const finishResolving = () => {\n       releaseResolving();\n       delete record.resolvingDone;\n     };\n \n     const handle = this._workers.get(record.worker);\n \n     if (record.kind === 'publication') {\n       const decision = answer?.decision;\n       if (!['allow', 'deny'].includes(decision)) {\n         record.state = 'pending';\n         finishResolving();\n         return { ok: false, result: 'invalid_decision' };\n       }\n       const currentFence = handle ? this._fences.current(handle.id).fence : null;\n       const fenceValid = actor === 'policy' || answer?.fence === record.fenceAtAsk;\n       if (!handle || !fenceValid || currentFence !== record.fenceAtAsk) {\n-        record.state = 'resolved';\n-        record.consumer = actor;\n-        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n         if (handle) {\n-          this._log.append({\n+          const refusedEvent = this._log.append({\n             worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n             kind: 'publication.refused', actor: 'policy',\n             payload: { requestId, reason: 'stale_fence', remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n           });\n+          const evidence = this._coordMapEvent(refusedEvent);\n+          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\n         }\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n         finishResolving();\n         return { ok: false, result: 'stale_fence', current: currentFence };\n       }\n       if (decision === 'deny') {\n-        record.state = 'resolved';\n-        record.consumer = actor;\n-        record.resolution = { decision: 'deny' };\n-        this._log.append({\n+        const deniedEvent = this._log.append({\n           worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n           kind: 'publication.denied', actor,\n           payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n         });\n+        const evidence = this._coordMapEvent(deniedEvent);\n+        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = { decision: 'deny' };\n         finishResolving();\n         return { ok: true, result: 'denied' };\n       }\n       if (typeof this._publisher !== 'function') {\n         record.state = 'pending';\n         finishResolving();\n         return { ok: false, result: 'publication_unavailable' };\n       }\n+      let authorizedEvent;\n+      try {\n+        authorizedEvent = this._log.append({\n+          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+          kind: 'publication.authorized', actor,\n+          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha, fence: record.fenceAtAsk },\n+        });\n+        const evidence = this._coordMapEvent(authorizedEvent);\n+        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\n+      } catch (err) {\n+        record.state = 'pending';\n+        finishResolving();\n+        throw err;\n+      }\n       let published;\n       try {\n         published = await this._publisher(record.publication);\n       } catch (err) {\n         record.state = 'pending';\n         finishResolving();\n         throw new PublicationError(String(err?.message ?? err), 'publisher_failed');\n       }\n       record.state = 'resolved';\n       record.consumer = actor;\n       record.resolution = { decision: 'allow' };\n       const task = this._tasks.get(handle.taskId);\n       void published;\n       task.publication = Object.freeze({ requestId, ...record.publication, actor });\n-      const publicationEvent = this._log.append({\n-        worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n-        kind: 'publication.completed', actor, payload: task.publication,\n-      });\n-      const publicationEvidence = this._coordMapEvent(publicationEvent);\n-      this._coordination?.promoteKnowledgeNode({\n-        id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n-        body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n-        grounding: 'observed', informedBy: [`task:${task.id}`],\n-        evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n-      }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n-      this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n+      try {\n+        const publicationEvent = this._log.append({\n+          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+          kind: 'publication.completed', actor, payload: task.publication,\n+        });\n+        const publicationEvidence = this._coordMapEvent(publicationEvent);\n+        this._coordination?.promoteKnowledgeNode({\n+          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n+          body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n+          grounding: 'observed', informedBy: [`task:${task.id}`],\n+          evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n+        }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n+        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n+      } catch (err) {\n+        finishResolving();\n+        throw err;\n+      }\n       finishResolving();\n       return { ok: true, result: 'published', publication: task.publication };\n     }\n \n     const clearPending = () => {\n       if (!handle) return;\n       if (record.kind === 'question' && handle.pendingQuestionId === requestId) handle.pendingQuestionId = null;\n       if (record.kind === 'approval' && handle.pendingApprovalId === requestId) handle.pendingApprovalId = null;\n       if (handle.status === 'blocked') {\n         handle.status = 'working';\n         const task = this._tasks.get(handle.taskId);\n         if (task && task.status === 'input_required') task.status = 'working';\n       }\n     };\n \n     if (!handle) {\n       record.state = 'resolved';\n       record.consumer = actor;\n       record.resolution = answer;\n       clearPending();\n       finishResolving();\n       return { ok: true, result: 'applied' };\n     }\n \n     const harness = this._harnessOf(handle.vendor);\n     const currentTurnEpoch = this._safeTurnEpoch(handle);\n     const stale = record.turnEpochAtAsk !== currentTurnEpoch;\n \n     if (stale) {\n-      this._log.append({ worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'control.stale_rejected', actor, payload: { op: 'respond', requestId } });\n+      const staleEvent = this._log.append({ worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'control.stale_rejected', actor, payload: { op: 'respond', requestId } });\n+      const task = this._tasks.get(handle.taskId);\n+      if (task && this._coordination?.task(task.id)?.status === 'input_required') {\n+        const evidence = this._coordMapEvent(staleEvent);\n+        this._coordTransition(task, 'working', `task.working:${task.id}:${staleEvent.seq}`, { ...evidence, interaction: { requestId, disposition: 'stale_discarded' } }, actor);\n+      }\n       record.state = 'resolved';\n       record.consumer = actor;\n       record.resolution = answer;\n       clearPending();\n       finishResolving();\n       return { ok: true, result: 'applied', note: 'answer arrived after the asking turn ended; discarded per fencing' };\n     }\n \n     let ack;\n     try {\n       if (record.kind === 'question') {\n         ack = await this._adapters[handle.vendor].answer(handle.id, requestId, answer);\n       } else {\n         const decision = answer && answer.decision;\n         ack = await this._adapters[handle.vendor].approve(handle.id, requestId, decision, answer && answer.payload);\n       }\n     } catch (err) {\n       record.state = 'pending';\n       record.consumer = null;\n       record.resolution = null;\n       finishResolving();\n       throw err;\n     }\n \n     if (!ack || ack.ok !== true) {\n       record.state = 'pending';\n       record.consumer = null;\n       record.resolution = null;\n       finishResolving();\n       return { ok: false, result: 'delivery_refused', reason: ack?.reason ?? 'adapter did not affirm response delivery' };\n     }\n \n-    record.state = 'resolved';\n-    record.consumer = actor;\n-    record.resolution = answer;\n-\n     let resolvedEvent;\n     if (record.kind === 'question') {\n       const ev = { worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'question.answered', actor, payload: { requestId, answer } };\n       if (ack && ack.emulated === true) ev.emulated = true;\n       resolvedEvent = this._log.append(ev);\n     } else {\n       const decision = answer && answer.decision;\n       const ev = { worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'approval.resolved', actor, payload: { requestId, decision } };\n       if (ack && ack.emulated === true) ev.emulated = true;\n       resolvedEvent = this._log.append(ev);\n     }\n     const task = this._tasks.get(handle.taskId);\n     if (task && this._coordination?.task(task.id)?.status === 'input_required') {\n-      const evidence = this._coordMapEvent(resolvedEvent);\n-      this._coordTransition(task, 'working', `task.working:${task.id}:${resolvedEvent.seq}`, evidence, actor);\n+      try {\n+        const evidence = this._coordMapEvent(resolvedEvent);\n+        this._coordTransition(task, 'working', `task.working:${task.id}:${resolvedEvent.seq}`, { ...evidence, interaction: { requestId, disposition: 'delivered' } }, actor);\n+      } catch (err) {\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = answer;\n+        finishResolving();\n+        throw err;\n+      }\n     }\n \n+    record.state = 'resolved';\n+    record.consumer = actor;\n+    record.resolution = answer;\n     clearPending();\n     finishResolving();\n     return { ok: true, result: 'applied' };\n   }\n \n   // =========================================================================\n   // Command: result() / list()\n   // =========================================================================\n \n   async result(workerId) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     const attribution = {\n       vendor: handle.vendor,\n       modelRequested: handle.modelRequested ?? null,\n       modelResolved: handle.modelResolved ?? null,\n       modelObserved: handle.modelObserved ?? null,\n       modelMismatch: handle.modelMismatch ?? null,\n       sessionRequest: handle.sessionRequest ?? { mode: 'new' },\n       sessionRef: handle.sessionRef ?? null,\n       sessionContext: handle.sessionContext ?? null,\n       lineage: handle.lineage ?? null,\n       review: task?.review ?? null,\n       integration: task?.integration ?? null,\n       publication: task?.publication ?? null,\n       retainedResultRef: task?.retainedResultRef ?? null,\n     };\n     if (!task) return { ready: false, status: handle.status, ...attribution };\n     if (!TERMINAL_TASK_STATUSES.has(task.status)) return { ready: false, status: task.status, ...attribution };\n     return { ready: true, status: task.status, verdict: task.verdict, artifacts: task.result ? task.result.artifacts : undefined, ...attribution };\n   }\n \n   /** Pull-only causal recall. The coordination append is the authority boundary: if the read\n    * audit cannot be durably written, no recalled content is returned to the caller. */\n   recallKnowledge(query, reader = {}, opts = {}) {\n     this.tick();\n     if (!this._coordination) throw new Error('coordination store is required for knowledge recall');\n     const actor = opts.actor ?? 'orchestrator';\n     const key = opts.idempotencyKey;\n     if (typeof key !== 'string' || key.length === 0) throw new TypeError('knowledge recall requires idempotencyKey');\n     let taskId = reader.taskId ?? null;\n     const workerId = reader.workerId ?? reader.readerWorker ?? null;\n     if (workerId) {\n       const handle = this._getWorker(workerId);\n       if (taskId && taskId !== handle.taskId) throw new Error('knowledge reader task does not match worker ownership');\n       taskId = handle.taskId;\n     }\n     return this._coordination.readKnowledge(query, {\n       readerActor: actor,\n       readerWorker: workerId,\n       taskId,\n       runId: reader.runId ?? null,\n     }, { actor, key });\n   }\n \n   claimScratch(workerId, fields, opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     if (!task || !['working', 'input_required'].includes(task.status)) return { ok: false, result: 'task_not_active' };\n     if (opts.expectedFence === undefined) throw new TypeError('Scratch claim requires expectedFence');\n     const check = this._fences.check(workerId, { fence: opts.expectedFence });\n     if (!check.ok) return { ok: false, result: 'stale_fence', current: check.current };\n     if (typeof opts.idempotencyKey !== 'string' || opts.idempotencyKey.length === 0) throw new TypeError('Scratch claim requires idempotencyKey');\n     return this._coordination.claimScratch({\n       ...fields,\n       ownerWorker: workerId,\n       ownerTask: task.id,\n       fence: check.current.fence,\n     }, { actor: opts.actor ?? 'orchestrator', key: opts.idempotencyKey });\n   }\n \n   postScratchFact(workerId, fields, opts = {}) {\n     this.tick();\n     const handle = this._getWorker(workerId);\n     const task = this._tasks.get(handle.taskId);\n     if (!task || !['working', 'input_required'].includes(task.status)) return { ok: false, result: 'task_not_active' };\n     if (opts.expectedFence === undefined) throw new TypeError('Scratch fact requires expectedFence');\n     const check = this._fences.check(workerId, { fence: opts.expectedFence });\n@@ -2247,199 +2355,209 @@ export class Coordinator {\n           vendor: handle.vendor,\n           kind: payload?.threadId ? 'thread' : 'session',\n           id: nativeId,\n           persistence: this._adapters[handle.vendor]?.card()?.sessions?.resume === 'native' ? 'native' : 'process',\n           source: 'wire',\n         };\n         const refTask = this._tasks.get(handle.taskId);\n         if (refTask) refTask.sessionRef = handle.sessionRef;\n       }\n     }\n     const observedModel = payload?.modelObserved ?? payload?.modelId ?? payload?.model;\n     if (typeof observedModel === 'string' && observedModel.length > 0) {\n       handle.modelObserved = observedModel;\n       const task = this._tasks.get(handle.taskId);\n       if (task) task.modelObserved = observedModel;\n \n       const selection = this._adapters[handle.vendor]?.card()?.modelSelection;\n       const requestedAlias = selection?.acceptedAliases?.includes(handle.modelResolved);\n       if (handle.modelResolved && observedModel !== handle.modelResolved && !requestedAlias && !handle.modelMismatch) {\n         handle.modelMismatch = { requested: handle.modelResolved, observed: observedModel };\n         const mismatchTask = this._tasks.get(handle.taskId);\n         if (mismatchTask) {\n           mismatchTask.modelMismatch = handle.modelMismatch;\n         }\n         const mismatchEvent = this._log.append({\n           worker: workerId, harness, turnEpoch, kind: 'model.mismatch', actor: 'policy',\n           modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved, modelObserved: observedModel,\n           payload: { requested: handle.modelResolved, observed: observedModel, action: 'fail_and_kill' },\n         });\n         if (mismatchTask && !TERMINAL_TASK_STATUSES.has(mismatchTask.status)) {\n           const evidence = this._coordMapEvent(mismatchEvent);\n           this._coordTransition(mismatchTask, 'failed', `task.failed:${mismatchTask.id}:${mismatchEvent.seq}`, evidence);\n           mismatchTask.status = 'failed';\n         }\n         // Use the ordinary confirmed two-phase stop so process/worktree ownership remains live\n         // until the adapter proves the mismatched session is gone.\n         this._beginStop(handle, 'kill', undefined, 'policy').catch(noop);\n       }\n     }\n     const attribution = {\n       modelRequested: handle.modelRequested ?? null,\n       modelResolved: handle.modelResolved ?? null,\n       modelObserved: handle.modelObserved ?? null,\n     };\n     const appendAttributed = (partial) => this._log.append({ ...partial, ...attribution });\n \n     switch (kind) {\n       case 'resource.tokens':\n         this._recordUsage(handle, event);\n         break;\n       case 'lifecycle.turn_completed': {\n         // Adapters may wrap the WorkerResult as { result } (MockAdapter) or emit it directly\n         // (coordinator.test). Normalize so the logged claim and the gate both see the WorkerResult.\n         const wr = (payload && payload.result !== undefined && payload.status === undefined) ? payload.result : payload;\n         appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload: wr });\n         this._clearWatchdog(handle);\n         if (handle.status !== 'stopping' && handle.status !== 'dead') {\n           this._runTrustGate(handle, wr).catch(noop);\n         }\n         break;\n       }\n       case 'lifecycle.crashed':\n       case 'lifecycle.exited': {\n         const terminalEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n           const evidence = this._coordMapEvent(terminalEvent);\n           if (evidence) this._coordTransition(task, 'failed', `task.failed:${task.id}:${evidence.coordinationSeq}`, evidence);\n         }\n         if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\n         if (handle.status !== 'dead') handle.status = 'exited';\n         this._clearWatchdog(handle);\n         this._removeRuntimeScope(handle);\n         break;\n       }\n       case 'question.asked': {\n         const requestId = payload?.requestId;\n         const askedEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && TERMINAL_TASK_STATUSES.has(task.status)) break;\n-        this._pending.set(requestId, {\n+        const record = {\n           kind: 'question',\n           worker: workerId,\n           state: 'pending',\n           resolution: null,\n           consumer: null,\n           turnEpochAtAsk: this._safeTurnEpoch(handle),\n           deadlineAt: null,\n-        });\n+        };\n+        const evidence = this._coordMapEvent(askedEvent);\n         if (payload?.blocking !== false) {\n           if (task) {\n-            const evidence = this._coordMapEvent(askedEvent);\n-            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, evidence);\n+            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, { ...evidence, interaction: { kind: 'question', requestId, blocking: true } });\n           }\n+        } else {\n+          this._coordRecord('input.requested', { taskId: task?.id ?? null, workerId, kind: 'question', requestId, blocking: false, evidence }, `driver.input_requested:${handle.taskId}:${askedEvent.seq}`, actor ?? 'worker');\n+        }\n+        this._pending.set(requestId, record);\n+        if (payload?.blocking !== false) {\n           handle.status = 'blocked';\n           handle.pendingQuestionId = requestId;\n           if (task) task.status = 'input_required';\n         }\n         break;\n       }\n       case 'approval.requested': {\n         const requestId = payload?.requestId;\n         const askedEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && TERMINAL_TASK_STATUSES.has(task.status)) break;\n-        this._pending.set(requestId, {\n+        const record = {\n           kind: 'approval',\n           worker: workerId,\n           state: 'pending',\n           resolution: null,\n           consumer: null,\n           turnEpochAtAsk: this._safeTurnEpoch(handle),\n           deadlineAt: this._now() + this._approvalTimeoutMs,\n-        });\n+        };\n+        const evidence = this._coordMapEvent(askedEvent);\n         if (payload?.blocking !== false) {\n           if (task) {\n-            const evidence = this._coordMapEvent(askedEvent);\n-            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, evidence);\n+            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, { ...evidence, interaction: { kind: 'approval', requestId, blocking: true } });\n           }\n+        } else {\n+          this._coordRecord('input.requested', { taskId: task?.id ?? null, workerId, kind: 'approval', requestId, blocking: false, evidence }, `driver.input_requested:${handle.taskId}:${askedEvent.seq}`, actor ?? 'worker');\n+        }\n+        this._pending.set(requestId, record);\n+        if (payload?.blocking !== false) {\n           handle.status = 'blocked';\n           handle.pendingApprovalId = requestId;\n           if (task) task.status = 'input_required';\n         }\n         break;\n       }\n       case 'question.answered':\n       case 'approval.resolved': {\n         const resolvedEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && this._coordination?.task(task.id)?.status === 'input_required') {\n           const evidence = this._coordMapEvent(resolvedEvent);\n           this._coordTransition(task, 'working', `task.working:${task.id}:${resolvedEvent.seq}`, evidence, actor ?? 'worker');\n         }\n         break;\n       }\n       case 'control.interrupt_confirmed':\n         this._onStopConfirmed(handle, 'interrupt');\n         break;\n       case 'kill.confirmed':\n         this._onStopConfirmed(handle, 'kill');\n         break;\n       default:\n         appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n     }\n     this._observeWatchdogEvent(handle, event);\n   }\n \n   // =========================================================================\n   // Trust gate (D4/§3.6)\n   // =========================================================================\n \n   async _runTrustGate(handle, workerResult) {\n     const task = this._tasks.get(handle.taskId);\n     if (!task) return;\n     // SC13/SC14: a late terminal event from a stopped session cannot reopen a terminal task.\n     if (TERMINAL_TASK_STATUSES.has(task.status)) return;\n     task.status = 'verifying';\n     task.result = workerResult;\n     const harness = this._harnessOf(handle.vendor);\n \n     let verifyPath = null;\n     let baseVerifyPath = null;\n     let trustPhase = 'capture';\n     try {\n       // C5: thread the dispatching vendor through to captureCommit so the snapshot\n       // commit (when one is made) is genuinely attributed.\n       const captured = await this._worktrees.capture(handle.worktree ?? task.worktree, {\n         vendor: handle.vendor,\n         model: handle.modelObserved ?? handle.modelResolved,\n       });\n       const sha = captured && captured.sha;\n       const created = await this._worktrees.createVerifyWorktree(task.id, sha);\n       verifyPath = created && created.path;\n \n       const baseSha = task.sessionContext?.baseSha ?? null;\n       if (this._acceptOpts.requireRedGreen && baseSha && typeof this._worktrees.createBaseVerifyWorktree === 'function') {\n         const baseCreated = await this._worktrees.createBaseVerifyWorktree(task.id, baseSha);\n         baseVerifyPath = baseCreated?.path ?? null;\n       }\n       if (this._acceptOpts.requireCoverage && baseSha && sha && typeof this._worktrees.changedLines === 'function') {\n         task.changedLines = await this._worktrees.changedLines(baseSha, sha);\n       }\n \n       const verdict = await this._referee(task, workerResult, {\n         pinnedVerification: task.brief.verification,\n         sandbox: verifyPath,\n         baseSandbox: baseVerifyPath,\n       });\n \n       task.verdict = verdict;\n       // C1: referee.accept() (or an injected equivalent) is the SOLE done-gate.\n       const acceptOpts = { ...this._acceptOpts, expectExit: task.brief.verification.expectExit };\n       const accept = this._accept(verdict, acceptOpts);\n       const verifyEvent = this._log.append({\n         worker: handle.id,\n         harness,\n         turnEpoch: this._safeTurnEpoch(handle),\n         kind: 'verify.reverified',\n         actor: 'policy',\ndiff --git a/impl/test/phase11-acceptance-integration.test.mjs b/impl/test/phase11-acceptance-integration.test.mjs\nindex 3412edd..bc0f263 100644\n--- a/impl/test/phase11-acceptance-integration.test.mjs\n+++ b/impl/test/phase11-acceptance-integration.test.mjs\n@@ -162,250 +162,326 @@ test('AC3: required mutation rejects survivors and records their identities', as\n   const root = repo();\n   commitBase(root, { 'mutation.mjs': 'console.log(JSON.stringify({killed:1,total:2,survived:[\"m2\"]}))\\n' });\n   const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\n   const { coordinator } = createDriver({\n     repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3b-log-')), adapters: { mock: adapter },\n     requireMutation: true, watchdog: { stallMs: 0 },\n   });\n   const h = await coordinator.spawn('mock', brief({\n     command: 'test -f src/x.js', expectExit: 0, mutationCommand: 'node mutation.mjs',\n   }), { taskId: 'mutation-weak' });\n   await until(async () => (await coordinator.result(h.id)).ready);\n   const result = await coordinator.result(h.id);\n   assert.equal(result.status, 'failed');\n   assert.equal(result.verdict.mutationPassed, false);\n   assert.deepEqual(result.verdict.survivedMutants, ['m2']);\n });\n \n test('AC4: independent oracle receives immutable spec/git evidence and unlocks required integration', async () => {\n   const root = repo();\n   commitBase(root, { 'README.md': 'base\\n' });\n   const implementer = familyAdapter('family-a', {\n     outcome: 'completed', summary: 'untrusted implementer prose',\n     edits: [{ path: 'src/reviewed.txt', content: 'accepted\\n' }],\n   });\n   const oracle = familyAdapter('family-b', { outcome: 'completed', summary: 'independent oracle prose' });\n   const { coordinator, log } = createDriver({\n     repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac4-log-')),\n     adapters: { implementer, oracle }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\n   });\n   const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-parent' });\n   await until(async () => (await coordinator.result(parent.id)).ready);\n \n   await assert.rejects(\n     coordinator.integrate(parent.id),\n     (error) => error instanceof IntegrationError && error.code === 'independent_oracle_required',\n   );\n   const reviewer = await coordinator.spawnReview(parent.id, 'oracle', {\n     taskId: 'oracle-child', kind: 'oracle', verification: { command: 'true', expectExit: 0 },\n   });\n   await until(async () => (await coordinator.result(reviewer.id)).ready);\n   const reviewResult = await coordinator.result(reviewer.id);\n   assert.equal(reviewResult.status, 'completed');\n   assert.equal(reviewResult.review.independent, true);\n   assert.equal(reviewResult.review.implementerFamily, 'family-a');\n   assert.equal(reviewResult.review.reviewerFamily, 'family-b');\n   const spawned = log.read(reviewer.id).find((entry) => entry.kind === 'lifecycle.spawned');\n   assert.equal(spawned.payload.brief.reviewTarget.parentTaskId, 'review-parent');\n   assert.equal(spawned.payload.brief.reviewTarget.resultSha, reviewResult.review.resultSha);\n   assert.equal(spawned.payload.brief.reviewTarget.spec.goal, 'make the pinned check newly pass');\n   assert.equal(JSON.stringify(spawned.payload.brief).includes('untrusted implementer prose'), false);\n   assert.equal(log.read(parent.id).some((entry) => entry.kind === 'review.completed' && entry.payload.independent === true), true);\n \n   const integrated = await coordinator.integrate(parent.id);\n   assert.equal(integrated.ok, true);\n   assert.equal(existsSync(join(root, 'src/reviewed.txt')), true);\n });\n \n test('AC4: visible same-family fallback cannot satisfy a required independent oracle', async () => {\n   const root = repo();\n   commitBase(root);\n   const implementer = familyAdapter('shared-family', { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'ok\\n' }] });\n   const fallback = familyAdapter('shared-family', { outcome: 'completed' });\n   const { coordinator, log } = createDriver({\n     repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac4-fallback-log-')),\n     adapters: { implementer, fallback }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\n   });\n   const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/x.js', expectExit: 0 }), { taskId: 'same-family-parent' });\n   await until(async () => (await coordinator.result(parent.id)).ready);\n   const reviewer = await coordinator.spawnReview(parent.id, 'fallback', {\n     taskId: 'same-family-oracle', verification: { command: 'true', expectExit: 0 },\n   });\n   await until(async () => (await coordinator.result(reviewer.id)).ready);\n   assert.equal((await coordinator.result(reviewer.id)).review.independent, false);\n   assert.equal(log.read(parent.id).some((entry) => entry.kind === 'review.completed' && entry.payload.independent === false), true);\n   await assert.rejects(\n     coordinator.integrate(parent.id),\n     (error) => error instanceof IntegrationError && error.code === 'independent_oracle_required',\n   );\n });\n \n+test('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\n+  const root = repo();\n+  commitBase(root);\n+  const implementer = familyAdapter('family-a', { outcome: 'completed', edits: [{ path: 'src/reviewed.txt', content: 'review me\\n' }] });\n+  const reviewer = familyAdapter('family-b', { outcome: 'completed' });\n+  let reviewerSpawns = 0;\n+  const rawSpawn = reviewer.spawn.bind(reviewer);\n+  reviewer.spawn = async (...args) => { reviewerSpawns += 1; return rawSpawn(...args); };\n+  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\n+  const parent = await driver.coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-fault-parent' });\n+  await until(async () => (await driver.coordinator.result(parent.id)).ready);\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('\"review\"')) throw new Error('review task disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(driver.coordinator.spawnReview(parent.id, 'reviewer', {\n+    kind: 'review', taskId: 'review-fault-child', verification: { command: 'true', expectExit: 0 },\n+  }), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(reviewerSpawns, 0);\n+  assert.deepEqual(driver.coordination.snapshot().tasks.map((task) => [task.id, task.status]), [['review-fault-parent', 'completed']]);\n+});\n+\n test('AC5: ff-only integration reaps the worker/worktree/branch and records exact SHAs', async () => {\n   const root = repo();\n   commitBase(root, { 'README.md': 'base\\n' });\n   const beforeSha = git(['rev-parse', 'HEAD'], root);\n   const { coordinator, coordination, log, handle } = await completedTask(root);\n   const taskWorktree = coordinator.list().find((worker) => worker.id === handle.id)?.worktree;\n   assert.equal(typeof taskWorktree, 'string');\n \n   const response = await coordinator.integrate(handle.id, { strategy: 'ff-only', actor: 'test-orchestrator' });\n   assert.equal(response.ok, true);\n   assert.equal(response.result, 'integrated');\n   assert.equal(response.integration.beforeSha, beforeSha);\n   assert.equal(response.integration.afterSha, response.integration.resultSha);\n   assert.equal(git(['rev-parse', 'HEAD'], root), response.integration.resultSha);\n   assert.equal(existsSync(join(root, 'src/integrated.txt')), true);\n   assert.equal(coordinator.list().find((worker) => worker.id === handle.id)?.status, 'dead');\n   assert.equal(existsSync(taskWorktree), false);\n   assert.equal(git(['branch', '--list', 'baton/integrate-me'], root), '');\n   assert.equal(git(['for-each-ref', '--format=%(refname)', `refs/baton/results/${response.integration.resultSha}`], root), '');\n   assert.deepEqual((await coordinator.result(handle.id)).integration, response.integration);\n   const event = log.read(handle.id).find((entry) => entry.kind === 'integration.completed');\n   assert.equal(event.actor, 'test-orchestrator');\n   assert.equal(event.payload.afterSha, response.integration.afterSha);\n   assert.equal(coordination.snapshot().artifacts.some((artifact) => artifact.mediaType === 'application/vnd.baton.integration+json'), true);\n   assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.completed'), true);\n   assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:integrate:')), true);\n   assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'integration'), true);\n });\n \n test('AC5: a non-fast-forward main refuses without rewriting either tip', async () => {\n   const root = repo();\n   commitBase(root, { 'README.md': 'base\\n' });\n   const { coordinator, coordination, log, handle } = await completedTask(root, 'diverged-task');\n   writeFileSync(join(root, 'main-only.txt'), 'main advanced\\n');\n   git(['add', 'main-only.txt'], root);\n   git(['commit', '-q', '-m', 'advance main independently'], root);\n   const mainBefore = git(['rev-parse', 'HEAD'], root);\n   const taskSha = (await coordinator.result(handle.id)).integration?.resultSha\n     ?? log.read(handle.id).find((entry) => entry.kind === 'verify.reverified')?.payload?.capture?.sha;\n \n   await assert.rejects(\n     coordinator.integrate(handle.id),\n     (error) => error instanceof IntegrationError && error.code === 'non_fast_forward_or_dirty',\n   );\n   assert.equal(git(['rev-parse', 'HEAD'], root), mainBefore);\n   assert.notEqual(mainBefore, taskSha);\n   assert.equal(git(['show', '-s', '--format=%s', 'HEAD'], root), 'advance main independently');\n   assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   const refusedResult = await coordinator.result(handle.id);\n   assert.equal(refusedResult.integration, null);\n   assert.equal(refusedResult.retainedResultRef, `refs/baton/results/${taskSha}`);\n   assert.equal(git(['show-ref', '--verify', refusedResult.retainedResultRef], root).split(' ')[0], taskSha);\n   assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.refused'), true);\n });\n \n test('AC5: a dirty main refuses without touching the index or working tree', async () => {\n   const root = repo();\n   commitBase(root, { 'README.md': 'base\\n' });\n   const { coordinator, log, handle } = await completedTask(root, 'dirty-main-task');\n   writeFileSync(join(root, 'README.md'), 'uncommitted user edit\\n');\n   const headBefore = git(['rev-parse', 'HEAD'], root);\n   const statusBefore = git(['status', '--porcelain=v1'], root);\n \n   await assert.rejects(coordinator.integrate(handle.id), IntegrationError);\n   assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n   assert.equal(git(['status', '--porcelain=v1'], root), statusBefore);\n   assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   const retained = (await coordinator.result(handle.id)).retainedResultRef;\n   assert.equal(git(['show-ref', '--verify', retained], root).endsWith(` ${retained}`), true);\n });\n \n test('AC5: integration refuses an unaccepted captured result', async () => {\n   const root = repo();\n   commitBase(root);\n   const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\n   const { coordinator } = createDriver({\n     repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-reject-log-')), adapters: { mock: adapter },\n     watchdog: { stallMs: 0 },\n   });\n   const handle = await coordinator.spawn('mock', brief({ command: 'false', expectExit: 0 }), { taskId: 'rejected-result' });\n   await until(async () => (await coordinator.result(handle.id)).ready);\n   assert.equal((await coordinator.result(handle.id)).status, 'failed');\n   await assert.rejects(\n     coordinator.integrate(handle.id),\n     (error) => error instanceof IntegrationError && error.code === 'result_not_accepted',\n   );\n });\n \n+test('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\n+  const root = repo();\n+  commitBase(root);\n+  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\n+  const headBefore = git(['rev-parse', 'HEAD'], root);\n+  const workerBefore = coordinator._workers.get(handle.id);\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n+  assert.equal(workerBefore.status, 'idle');\n+  assert.equal(existsSync(workerBefore.worktree), true);\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\n+});\n+\n test('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\n   const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\n   const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   assert.equal(calls.length, 0);\n   assert.deepEqual(requested.target, {\n     remote: 'origin', ref: 'refs/heads/main', sha: (await coordinator.result(handle.id)).integration.afterSha,\n   });\n   const event = log.read(handle.id).at(-1);\n   assert.equal(event.kind, 'publication.requested');\n   assert.equal(JSON.stringify(event).includes('credential'), false);\n \n   const [a, b] = await Promise.all([\n     coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n     coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'other-user'),\n   ]);\n   assert.equal(calls.length, 1);\n   assert.deepEqual(calls[0], requested.target);\n   assert.deepEqual(new Set([a.result, b.result]), new Set(['published', 'already_resolved']));\n   assert.equal((await coordinator.result(handle.id)).publication.sha, requested.target.sha);\n   assert.equal(log.read(handle.id).filter((entry) => entry.kind === 'publication.completed').length, 1);\n   assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'publication.completed'), true);\n   assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:publish:')), true);\n   assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'publication'), true);\n });\n \n+test('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\n+  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n+  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(\n+    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n+    (error) => error.code === 'coordination_write_unavailable',\n+  );\n+  assert.equal(calls.length, 0);\n+  assert.equal(coordinator._pending.get(requested.requestId).state, 'pending');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n+});\n+\n+test('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\n+  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n+  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"publication.completed\"')) throw new Error('publication completion disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(\n+    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n+    (error) => error.code === 'coordination_write_unavailable',\n+  );\n+  assert.equal(calls.length, 1);\n+  assert.equal(coordinator._pending.get(requested.requestId).state, 'resolved');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n+});\n+\n test('AC6: deny and timeout are fail-closed and never call the publisher', async () => {\n   let now = 1000;\n   const denied = await integratedPublicationTask({ now: () => now, approvalTimeoutMs: 10 });\n   const explicit = denied.coordinator.requestPublication(denied.handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   const deniedResult = await denied.coordinator.respond(explicit.requestId, { decision: 'deny', fence: explicit.fence }, 'test-user');\n   assert.equal(deniedResult.result, 'denied');\n   assert.equal(denied.calls.length, 0);\n \n   const timed = timedPublicationRequest(denied.coordinator, denied.handle.id);\n   now += 11;\n   denied.coordinator.list();\n   await until(() => denied.log.read(denied.handle.id).some((entry) => entry.kind === 'publication.denied' && entry.payload.requestId === timed.requestId));\n   assert.equal(denied.calls.length, 0);\n });\n \n function timedPublicationRequest(coordinator, workerId) {\n   return coordinator.requestPublication(workerId, { remote: 'origin', ref: 'refs/heads/main' });\n }\n \n test('AC6: a newer authority fence invalidates an older publication approval', async () => {\n   const { coordinator, handle, calls } = await integratedPublicationTask();\n   const oldRequest = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   const currentRequest = coordinator.requestPublication(handle.id, { remote: 'upstream', ref: 'refs/heads/reviewed' });\n   const stale = await coordinator.respond(oldRequest.requestId, { decision: 'allow', fence: oldRequest.fence }, 'test-user');\n   assert.equal(stale.result, 'stale_fence');\n   assert.equal(calls.length, 0);\n   await coordinator.respond(currentRequest.requestId, { decision: 'deny', fence: currentRequest.fence }, 'test-user');\n   assert.equal(calls.length, 0);\n });\n \n test('AC6: restart drops a pending approval and cannot publish it by replay', async () => {\n   const first = await integratedPublicationTask();\n   const request = first.coordinator.requestPublication(first.handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   const replayCalls = [];\n   const replay = createDriver({\n     repoRoot: first.root, logDir: first.logDir,\n     adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\n     publisher: async (target) => { replayCalls.push(target); }, watchdog: { stallMs: 0 },\n   });\n   const response = await replay.coordinator.respond(request.requestId, { decision: 'allow', fence: request.fence }, 'test-user');\n   assert.equal(response.result, 'not_found');\n   assert.equal(replayCalls.length, 0);\n });\n \n test('AC6: credential-bearing remotes and mismatched SHAs are rejected before logging', async () => {\n   const { coordinator, log, handle } = await integratedPublicationTask();\n   const before = log.read(handle.id).length;\n   assert.throws(\n     () => coordinator.requestPublication(handle.id, { remote: 'https://token@example.com/repo.git', ref: 'refs/heads/main' }),\n     (error) => error instanceof PublicationError && error.code === 'invalid_remote',\n   );\n   assert.throws(\n     () => coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main', sha: 'deadbeef' }),\n     (error) => error instanceof PublicationError && error.code === 'sha_mismatch',\n   );\n   assert.equal(log.read(handle.id).length, before);\n });\ndiff --git a/impl/test/phase11-coordination-store.test.mjs b/impl/test/phase11-coordination-store.test.mjs\nindex a696b25..0eaa0a2 100644\n--- a/impl/test/phase11-coordination-store.test.mjs\n+++ b/impl/test/phase11-coordination-store.test.mjs\n@@ -298,160 +298,238 @@ test('CK3/CK9: terminal task and accepted manifests commit in one append batch',\n   store.createTask(fields('atomic'), { actor: 'orchestrator', key: 'create-atomic' });\n   const claimed = store.claimTask('atomic', 'w-atomic', 1, { actor: 'orchestrator', key: 'claim-atomic' });\n   const verify = { worker: 'w-atomic', seq: 1, ts: '2026-01-01T00:00:00Z', kind: 'verify.reverified', actor: 'policy', payload: { accept: true } };\n   operational.set('w-atomic:1', verify);\n   const mapped = store.mapOperationalEvent(verify, { actor: 'policy', key: 'map-atomic' });\n   const manifests = [{ taskId: 'atomic', kind: 'verification', refs: { worker: 'w-atomic', workerSeq: 1 }, accepted: true, provenance: [mapped.evidence] }];\n   const before = store.events().length;\n   fail = true;\n   assert.throws(() => store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence), /batch disk full/);\n   assert.equal(store.events().length, before);\n   assert.equal(store.task('atomic').status, 'working');\n   assert.equal(store.snapshot().artifacts.length, 0);\n   fail = false;\n   const completed = store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence);\n   assert.equal(completed.task.status, 'completed');\n   assert.equal(completed.artifacts.length, 1);\n   assert.equal(new CoordinationStore(root, { operationalRead: (worker, seq) => operational.get(`${worker}:${seq}`) }).snapshot().artifacts.length, 1);\n });\n \n test('CK8/CK9: completed public task maps verification evidence, terminal state, and manifests', async () => {\n   const repo = dir();\n   execFileSync('git', ['init', '-q'], { cwd: repo });\n   execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n   execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n   writeFileSync(join(repo, 'README.md'), 'base\\n');\n   execFileSync('git', ['add', '.'], { cwd: repo });\n   execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\n   const driver = createDriver({\n     repoRoot: repo, logDir: dir(),\n     adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'done.txt', content: 'done\\n' }] } }) },\n     watchdog: { stallMs: 0 },\n   });\n   const brief = { goal: 'complete durably', constraints: [], pathScope: ['done.txt'], definitionOfDone: 'done', verification: { command: 'test -s done.txt', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n   const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'durable-complete' });\n   await until(async () => (await driver.coordinator.result(handle.id)).ready);\n   assert.equal(driver.coordination.task('durable-complete').status, 'completed');\n   const snapshot = driver.coordination.snapshot();\n   assert.equal(snapshot.evidence.some((item) => item.kind === 'verify.reverified'), true);\n   assert.deepEqual(snapshot.artifacts.map((artifact) => artifact.kind).sort(), ['commit', 'report', 'verification']);\n   assert.equal(snapshot.artifacts.filter((artifact) => artifact.kind !== 'report').every((artifact) => artifact.accepted === true && artifact.provenance.length === 1), true);\n   assert.equal(snapshot.artifacts.find((artifact) => artifact.kind === 'report').accepted, false, 'worker artifact claims stay explicitly untrusted');\n   assert.equal(snapshot.tasks[0].artifactIds.length, 3);\n   assert.equal(snapshot.knowledge.nodes.some((node) => node.type === 'Finding' && node.id.startsWith('outcome:durable-complete')), true);\n   assert.equal(driver.coordination.events().some((event) => event.kind === 'knowledge.promoted' && event.payload.promotion?.trigger === 'verified_task_outcome'), true);\n   const recalled = driver.coordinator.recallKnowledge({ types: ['Finding'] }, { workerId: handle.id, runId: 'run-durable' }, { actor: 'orchestrator', idempotencyKey: 'recall-durable-outcome' });\n   assert.equal(recalled.nodes.length, 1);\n   assert.match(recalled.frame, /UNTRUSTED_RECALLED_MEMORY/);\n   assert.equal(driver.coordination.traceKnowledge(recalled.nodes[0].id).edges.some((edge) => edge.type === 'ReadBy' && edge.to === 'task:durable-complete'), true);\n   const replay = new CoordinationStore(driver.coordination.root, {\n     operationalRead: (worker, seq) => driver.log.read(worker, seq).find((event) => event.seq === seq) ?? null,\n   });\n   assert.deepEqual(replay.snapshot(), driver.coordination.snapshot());\n });\n \n test('CK2/CK8: blocking input and resolution transition durably before terminal verification', async () => {\n   const repo = dir();\n   execFileSync('git', ['init', '-q'], { cwd: repo });\n   execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n   execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n   writeFileSync(join(repo, 'README.md'), 'base\\n');\n   execFileSync('git', ['add', '.'], { cwd: repo });\n   execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\n   const driver = createDriver({\n     repoRoot: repo, logDir: dir(),\n     adapters: { mock: new MockAdapter({ scenario: {\n       outcome: 'completed', edits: [{ path: 'asked.txt', content: 'asked\\n' }],\n       ask: { kind: 'question', question: 'continue?', blocking: true, afterEditIndex: 1 },\n     } }) }, watchdog: { stallMs: 0 },\n   });\n   const brief = { goal: 'input durably', constraints: [], pathScope: ['asked.txt'], definitionOfDone: 'asked', verification: { command: 'test -s asked.txt', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n   const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'durable-input' });\n   await until(() => driver.coordinator.list()[0]?.pendingQuestionId);\n   assert.equal(driver.coordination.task('durable-input').status, 'input_required');\n   const requestId = driver.coordinator.list()[0].pendingQuestionId;\n   await driver.coordinator.respond(requestId, { text: 'yes' }, 'human');\n   assert.equal(driver.coordination.events().some((event) => event.kind === 'task.transitioned' && event.payload.to === 'working'), true);\n   await until(async () => (await driver.coordinator.result(handle.id)).ready);\n   assert.equal(driver.coordination.task('durable-input').status, 'completed');\n });\n \n+test('CK8/CK9: input transition append failure poisons before pending/blocked state becomes visible', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'input crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'input-write-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.transitioned\"') && body.includes('\"input_required\"')) throw new Error('input transition disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  assert.throws(() => driver.coordinator._handleEvent({\n+    worker: handle.id, harness: 'mock@1.0.0', turnEpoch: driver.coordinator.list()[0].turnEpoch,\n+    kind: 'question.asked', actor: 'worker', payload: { requestId: 'q-crash', question: 'continue?', blocking: true },\n+  }), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(driver.coordination.task('input-write-failure').status, 'working');\n+  assert.equal(driver.coordinator._pending.size, 0);\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n+  await adapter.kill(handle.id);\n+});\n+\n+test('CK8/CK9: stop-intent append failure calls no adapter and leaves durable task working', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  let adapterKills = 0;\n+  const rawKill = adapter.kill.bind(adapter);\n+  adapter.kill = async (...args) => { adapterKills += 1; return rawKill(...args); };\n+  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'stop crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'stop-write-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"control.stop_requested\"')) throw new Error('stop intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(driver.coordinator.kill(handle.id, 'human'), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(adapterKills, 0);\n+  assert.equal(driver.coordination.task('stop-write-failure').status, 'working');\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n+  driver.coordination._appendFile = rawAppend;\n+  await rawKill(handle.id);\n+});\n+\n+test('CK8/CK9: cancellation completion failure resolves bounded, keeps stop intent, and restart closes task', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const logDir = dir();\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  const driver = createDriver({ repoRoot: repo, logDir, adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'cancel completion crash', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'cancel-completion-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.transitioned\"') && body.includes('\"cancelled\"')) throw new Error('cancel completion disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  const stopped = await driver.coordinator.kill(handle.id, 'human');\n+  assert.deepEqual(stopped, { ok: false, result: 'coordination_unavailable' });\n+  assert.equal(driver.coordination.task('cancel-completion-failure').status, 'working');\n+  assert.equal(driver.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'control.stop_requested'), true);\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'dead');\n+  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\n+  driver.coordination._appendFile = rawAppend;\n+  const replay = createDriver({ repoRoot: repo, logDir, coordination: driver.coordination, adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) }, watchdog: { stallMs: 0 } });\n+  assert.equal(replay.coordination.task('cancel-completion-failure').status, 'failed');\n+});\n+\n test('CK4: Scratch claims conservatively conflict, warn cross-tree, and expire only by event', () => {\n   const root = dir();\n   const store = new CoordinationStore(root);\n   const envA = { repoId: 'repo', treeSha: 'aaaa1111' };\n   const envB = { repoId: 'repo', treeSha: 'bbbb2222' };\n   const first = store.claimScratch({ resource: 'path:payments/**', ownerWorker: 'w1', ownerTask: 't1', intent: 'edit', envRef: envA, fence: 3, leaseDeadline: 'later' }, { actor: 'w1', key: 'claim-a' });\n   assert.equal(store.claimScratch({ resource: 'changed', envRef: envB }, { actor: 'w1', key: 'claim-a' }).result, 'idempotent');\n   const conflict = store.claimScratch({ resource: 'path:payments/stripe.js', ownerWorker: 'w2', ownerTask: 't2', intent: 'edit', envRef: envB, fence: 4, leaseDeadline: 'later' }, { actor: 'w2', key: 'claim-b' });\n   assert.equal(conflict.ok, false);\n   assert.equal(conflict.conflict.id, first.claim.id);\n   const globWitness = store.claimScratch({ resource: '*stripe.js', ownerWorker: 'w3', ownerTask: 't3', intent: 'edit', envRef: envB, fence: 5, leaseDeadline: 'later' }, { actor: 'w3', key: 'claim-glob-witness' });\n   assert.equal(globWitness.ok, false, 'glob/glob pair with witness path:payments/stripe.js must conservatively conflict');\n   const check = store.checkScratch('path:payments/stripe.js', envB);\n   assert.equal(check.clear, false);\n   assert.equal(check.claims[0].warning, 'observed on aaaa1111 — not your tree');\n   assert.equal(new CoordinationStore(root).checkScratch('path:payments/stripe.js', envB).clear, false, 'wall-clock replay never expires a lease');\n   store.expireScratchClaim(first.claim.id, 1, { actor: 'policy', key: 'expire-a' });\n   assert.equal(store.checkScratch('path:payments/stripe.js', envB).clear, true);\n   assert.equal(new CoordinationStore(root).checkScratch('path:payments/stripe.js', envB).clear, true);\n });\n \n test('CK4: Scratch facts are tree-scoped, grounded, immutable, and explicitly expired', () => {\n   const root = dir();\n   const store = new CoordinationStore(root);\n   const fact = store.postScratchFact({ namespace: 'tests', key: 'test:flaky', value: { seed: 42 }, grounding: 'observed', envRef: { repoId: 'repo', treeSha: 'cafe1234' }, evidence: [{ coordinationSeq: 1 }] }, { actor: 'w1', key: 'fact-a' });\n   const crossTree = store.checkScratch('test:flaky', { repoId: 'repo', treeSha: 'dead5678' });\n   assert.equal(crossTree.facts[0].warning, 'observed on cafe1234 — not your tree');\n   assert.throws(() => store.postScratchFact({ namespace: 'x', key: 'x', value: 1, grounding: 'asserted', envRef: { repoId: 'repo', treeSha: 'cafe1234' } }, { actor: 'w1', key: 'bad' }), (error) => error.code === 'invalid_grounding');\n   store.expireScratchFact(fact.fact.id, { actor: 'policy', key: 'expire-fact' });\n   assert.equal(store.checkScratch('test:flaky', { repoId: 'repo', treeSha: 'cafe1234' }).facts.length, 0);\n });\n \n test('CK5: causal evidence and temporal coherence are enforced', () => {\n   const store = new CoordinationStore(dir());\n   store.createTask(fields('a'), { actor: 'orchestrator', key: 'task-a' });\n   assert.throws(() => store.addKnowledgeNode({ type: 'Decision', id: 'D1', body: 'choose A', grounding: 'asserted' }, { actor: 'human', key: 'd1' }), (error) => error.code === 'causal_orphan');\n   assert.throws(() => store.addKnowledgeNode({ type: 'Finding', id: 'Ffuture', body: 'future', grounding: 'verified', evidence: [{ coordinationSeq: 99 }] }, { actor: 'policy', key: 'future' }), (error) => error.code === 'temporal_incoherence');\n   const finding = store.addKnowledgeNode({ type: 'Finding', id: 'F1', body: 'verified outcome', grounding: 'verified', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-01-01T00:00:00Z' }, { actor: 'policy', key: 'f1' });\n   const decision = store.addKnowledgeNode({ type: 'Decision', id: 'D1', body: 'choose A', grounding: 'asserted', informedBy: [finding.node.id], evidence: [{ coordinationSeq: finding.event.seq }], validFrom: '2026-01-02T00:00:00Z' }, { actor: 'human', key: 'd1-ok' });\n   assert.equal(store.traceKnowledge('D1').edges[0].type, 'Informed');\n });\n \n test('CK5/CK7: bitemporal query, supersession, logged reads, and contamination survive replay', () => {\n   const root = dir();\n   const store = new CoordinationStore(root);\n   store.createTask(fields('a'), { actor: 'orchestrator', key: 'task-a' });\n   store.addKnowledgeNode({ type: 'Finding', id: 'old', body: 'old belief', grounding: 'observed', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-01-01T00:00:00Z' }, { actor: 'policy', key: 'old' });\n   store.addKnowledgeNode({ type: 'Finding', id: 'new', body: 'new belief', grounding: 'verified', evidence: [{ coordinationSeq: 1 }], validFrom: '2026-02-01T00:00:00Z' }, { actor: 'policy', key: 'new' });\n   const read = store.readKnowledge({ types: ['Finding'], asOf: '2026-01-15T00:00:00Z' }, { readerActor: 'orchestrator', readerWorker: 'w1', taskId: 'a', runId: 'r1' }, { actor: 'orchestrator', key: 'read-old' });\n   assert.deepEqual(read.nodes.map((node) => node.id), ['old']);\n   assert.match(read.frame, /UNTRUSTED_RECALLED_MEMORY/);\n   const superseded = store.addKnowledgeEdge({ type: 'Supersedes', from: 'new', to: 'old', expectedValidityVersion: 1, validFrom: '2026-02-01T00:00:00Z' }, { actor: 'policy', key: 'supersede' });\n   assert.deepEqual(superseded.contamination.payload.affectedReadEvents, [read.event.seq]);\n   assert.deepEqual(store.queryKnowledge({ types: ['Finding'], asOf: '2026-01-15T00:00:00Z' }).map((node) => node.id), ['old']);\n   assert.deepEqual(store.queryKnowledge({ types: ['Finding'], asOf: '2026-02-15T00:00:00Z' }).map((node) => node.id), ['new']);\n   const invalidated = store.invalidateKnowledge('new', 1, 'refuted later', { actor: 'human', key: 'invalidate-new' });\n   assert.equal(invalidated.contamination.payload.affectedReadEvents.length, 0);\n   assert.equal(store.affectedReaders('old').length, 1);\n   const replay = new CoordinationStore(root);\n   assert.deepEqual(replay.snapshot(), store.snapshot());\n   assert.equal(replay.affectedReaders('old')[0].taskId, 'a');\n   assert.equal(replay.affectedReaders('old')[0].taskStatus, 'pending');\n   assert.equal(replay.traceKnowledge('old').edges.some((edge) => edge.type === 'ReadBy' && edge.to === 'task:a'), true);\n   const audit = replay.auditKnowledge();\n   assert.equal(audit.temporalCoherence.invalidEvidence, 0);\n   assert.equal(audit.recallUtility.reads, 1);\n   assert.equal(audit.contamination.affectedReads, 1);\n });\n \n test('CK7/CK9: invalidation and contamination commit atomically', () => {\n   const root = dir();\n   let fail = false;\n   const store = new CoordinationStore(root, { appendFile: (...args) => {\n     if (fail) throw new Error('invalidation disk full');\n     return appendFileSync(...args);\n   } });\n   store.createTask(fields('reader'), { actor: 'orchestrator', key: 'create-reader' });\n   store.addKnowledgeNode({ type: 'Finding', id: 'fragile', body: 'fragile belief', grounding: 'observed', evidence: [{ coordinationSeq: 1 }] }, { actor: 'policy', key: 'fragile' });\n   store.readKnowledge({ types: ['Finding'] }, { readerActor: 'orchestrator', taskId: 'reader', runId: 'r' }, { actor: 'orchestrator', key: 'read-fragile' });\n   const before = store.events().length;\ndiff --git a/impl/test/phase11-persistent-sessions.test.mjs b/impl/test/phase11-persistent-sessions.test.mjs\nindex 85e4f9a..5cba651 100644\n--- a/impl/test/phase11-persistent-sessions.test.mjs\n+++ b/impl/test/phase11-persistent-sessions.test.mjs\n@@ -72,160 +72,180 @@ function harness(ad, referee = async () => ({ reverified: true, observedExit: 0\n     referee: async (...args) => { verifyCalls.push(args); return referee(...args); },\n     route: () => 'session', approvalTimeoutMs: 1000, stopDeadlineMs: 100,\n   });\n   return { c, log, verifyCalls, coordination };\n }\n \n async function until(fn, timeoutMs = 2000) {\n   const deadline = Date.now() + timeoutMs;\n   while (Date.now() < deadline) {\n     const value = await fn();\n     if (value) return value;\n     await sleep(5);\n   }\n   throw new Error('condition not met');\n }\n \n function completed(summary = 'done') {\n   return { status: 'completed', summary, artifacts: { files: [] }, verification: { command: 'true', claimedExit: 0 } };\n }\n \n test('PS1/PS4: a verified worker accepts a public follow-up turn and verifies it independently', async () => {\n   const ad = adapter();\n   const { c, log, verifyCalls, coordination } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'follow-up' });\n   ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'session-1', pid: 123 }, 1);\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   assert.equal(verifyCalls.length, 1);\n \n   const ack = await c.send(h.id, 'second turn', 'turn');\n   assert.equal(ack.ok, true);\n   assert.equal(c.list()[0].status, 'working');\n   assert.equal((await c.result(h.id)).ready, false);\n   assert.deepEqual(ad.calls.prompt[0].slice(1), ['second turn', 'turn']);\n \n   ad.emit(h.id, 'lifecycle.turn_started', { sessionId: 'session-1' }, 2);\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('second'), 2);\n   await until(async () => (await c.result(h.id)).ready && verifyCalls.length === 2);\n   assert.equal((await c.result(h.id)).status, 'completed');\n   assert.equal(coordination.snapshot().tasks.length, 2);\n   assert.deepEqual(coordination.snapshot().tasks.map((task) => task.status), ['completed', 'completed']);\n   assert.equal(coordination.snapshot().tasks[1].refines, 'follow-up');\n \n   const replay = new Coordinator({\n     log, fences: new FenceTable(), adapters: { session: adapter() }, coordination,\n     worktrees: { create: async () => ({}), capture: async () => ({}), createVerifyWorktree: async () => ({}), removeVerifyWorktree: async () => {}, remove: async () => {}, reconcile: async () => {} },\n     referee: async () => ({}), route: () => 'session', approvalTimeoutMs: 1000, stopDeadlineMs: 100,\n   });\n   assert.equal(replay.list()[0].taskId, coordination.snapshot().tasks[1].id);\n   assert.equal((await replay.result(h.id)).status, 'completed');\n   assert.equal((await replay.result(h.id)).ready, true);\n });\n \n test('PS2: a refused follow-up restores the previous terminal result and logs no new turn', async () => {\n   const ad = adapter({ prompt: async () => ({ ok: false, reason: 'busy' }) });\n   const { c, log } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'follow-refused' });\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   const beforeStarts = log.read(h.id).filter((e) => e.kind === 'lifecycle.turn_started').length;\n   const ack = await c.send(h.id, 'try again', 'turn');\n   assert.equal(ack.ok, false);\n   assert.equal(ad.calls.prompt.length, 1, 'refusal must come from an attempted native follow-up, not the old idle guard');\n   assert.equal((await c.result(h.id)).status, 'completed');\n   assert.equal(c.list()[0].status, 'idle');\n   assert.equal(log.read(h.id).filter((e) => e.kind === 'lifecycle.turn_started').length, beforeStarts);\n });\n \n test('PS2: a follow-up exception is a refused Ack and preserves the prior result', async () => {\n   const ad = adapter({ prompt: async () => { throw new Error('wire write failed'); } });\n   const { c } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'follow-throws' });\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   const ack = await c.send(h.id, 'try again', 'turn');\n   assert.deepEqual(ack, { ok: false, result: 'delivery_exception', reason: 'wire write failed' });\n   assert.equal((await c.result(h.id)).status, 'completed');\n   assert.equal(c.list()[0].status, 'idle');\n });\n \n+test('CK8/CK9: follow-up refinement failure kills an already-advanced native session', async () => {\n+  const ad = adapter();\n+  const { c, coordination } = harness(ad);\n+  const h = await c.spawn('session', brief(), { taskId: 'follow-refinement-failure' });\n+  ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('follow-up refinement disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(c.send(h.id, 'advance native state', 'turn'), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(ad.calls.prompt.length, 1);\n+  assert.equal(ad.calls.kill.length, 1);\n+  assert.equal(c._workers.get(h.id).status, 'dead');\n+  assert.equal(coordination.snapshot().tasks.length, 1);\n+  assert.equal(coordination.snapshot().tasks[0].status, 'completed');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'follow_up.requested'), true);\n+});\n+\n test('PS2: emitted turn facts followed by refusal are a protocol violation and kill the ambiguous session', async () => {\n   let ad;\n   ad = adapter({\n     prompt: async (worker) => {\n       ad.emit(worker, 'lifecycle.turn_started', {}, 2);\n       return { ok: false, reason: 'contradictory refusal' };\n     },\n   });\n   const { c, log } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'follow-contradiction' });\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   const ack = await c.send(h.id, 'try again', 'turn');\n   assert.equal(ack.ok, false);\n   assert.ok(log.read(h.id).some((event) => event.kind === 'control.protocol_violation'));\n   assert.equal((await c.result(h.id)).status, 'completed');\n   assert.equal(c.list()[0].status, 'stopping');\n });\n \n test('PS3: interrupt-with-follow-up reopens coordinator state before the adapter follow-up terminal', async () => {\n   const ad = adapter();\n   const { c, verifyCalls } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'interrupt-follow' });\n   const stopping = c.interrupt(h.id, 'continue after interrupt', 'human');\n   ad.emit(h.id, 'control.interrupt_confirmed', {}, 1, 'orchestrator');\n   await stopping;\n   assert.equal(c.list()[0].status, 'working');\n   assert.equal((await c.result(h.id)).ready, false);\n   ad.emit(h.id, 'lifecycle.turn_started', {}, 2);\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('followed'), 2);\n   await until(async () => (await c.result(h.id)).ready);\n   assert.equal(verifyCalls.length, 1);\n   assert.equal((await c.result(h.id)).status, 'completed');\n });\n \n test('PS4: a late terminal from the prior wire epoch cannot overwrite the active follow-up', async () => {\n   const ad = adapter();\n   const { c, log, verifyCalls } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'stale-terminal' });\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   await c.send(h.id, 'next', 'turn');\n   ad.emit(h.id, 'lifecycle.turn_started', {}, 2);\n   ad.emit(h.id, 'lifecycle.turn_completed', completed('late first'), 1);\n   await sleep(20);\n   assert.equal(verifyCalls.length, 1);\n   assert.equal((await c.result(h.id)).ready, false);\n   assert.ok(log.read(h.id).some((e) => e.kind === 'control.stale_rejected' && e.payload?.op === 'terminal'));\n });\n \n test('PS5: wire sessionRef reaches handle/result and survives terminal replay', async () => {\n   const ad = adapter();\n   const { c, log, coordination } = harness(ad);\n   const h = await c.spawn('session', brief(), { taskId: 'session-ref' });\n   ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'native-session-7', pid: 123 }, 1);\n   ad.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   await until(async () => (await c.result(h.id)).ready);\n   const expected = { vendor: 'session', kind: 'session', id: 'native-session-7', persistence: 'native', source: 'wire' };\n   assert.deepEqual(c.list()[0].sessionRef, expected);\n   assert.deepEqual((await c.result(h.id)).sessionRef, expected);\n \n   const replay = new Coordinator({\n     log, coordination, fences: new FenceTable(), adapters: { session: adapter() },\n     worktrees: { create: async () => ({}), capture: async () => ({}), createVerifyWorktree: async () => ({}), removeVerifyWorktree: async () => {}, remove: async () => {}, reconcile: async () => {} },\n     referee: async () => ({}), route: () => 'session', approvalTimeoutMs: 1000, stopDeadlineMs: 100,\n   });\n   assert.deepEqual(replay.list()[0].sessionRef, expected);\n   assert.deepEqual(replay.list()[0].sessionRequest, { mode: 'new' });\n   assert.equal(replay.list()[0].status, 'orphaned');\n   assert.deepEqual(await replay.send(h.id, 'must not reach an unattached adapter', 'turn'), { ok: false, result: 'worker_not_active' });\n   assert.equal((await replay.result(h.id)).status, 'completed');\n });\n \n test('PS1-PS5: Claude, Codex, and Grok each run two public turns on one native session then kill', async (t) => {\n   const definitions = [\n     ['claude', () => new ClaudeSessionCli({ cmd: process.execPath, args: [FAKE_CLAUDE], killGraceMs: 20 })],\n     ['codex', () => new CodexAppServerCli({ cmd: process.execPath, args: [FAKE_CODEX, '--serve'], requestTimeoutMs: 500, versionProbe: () => 'fake' })],\n     ['grok', () => new GrokAcpCli({ cmd: process.execPath, args: [FAKE_GROK, '--serve'], requestTimeoutMs: 500, versionProbe: () => 'fake' })],\n   ];\n \n@@ -336,131 +356,189 @@ test('PS8: coordinator resume requires and reuses a validated worktree without c\n \n   await assert.rejects(\n     () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session' } }),\n     (err) => err instanceof SessionSelectionError && err.code === 'session_context_required',\n   );\n   assert.deepEqual(c.list(), []);\n   await assert.rejects(\n     () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session', context: { worktree: wt } } }),\n     (err) => err instanceof SessionSelectionError && err.code === 'session_context_required',\n   );\n   assert.deepEqual(c.list(), []);\n \n   const context = { worktree: wt, ownerTaskId: 'original-task' };\n   const h = await c.spawn('session', brief(), {\n     taskId: 'resumed-task', refines: 'original-task', session: { mode: 'resume', id: 'external-session', context },\n   });\n   await until(() => spawnOpts);\n   assert.equal(creates, 0);\n   assert.equal((await spawnOpts.worktreeReady).path, wt);\n   assert.deepEqual(h.lineage, { relation: 'resume', parentSessionId: 'external-session', parentTaskId: 'original-task' });\n   assert.equal(c.list()[0].sessionContext.worktree, wt);\n   assert.equal(c.list()[0].sessionContext.ownerTaskId, 'original-task');\n });\n \n test('PS8: fork gets a fresh worktree and a durable parent-session lineage edge', async () => {\n   const wt = mkdtempSync(join(tmpdir(), 'baton-ps-fork-wt-'));\n   const ad = adapter();\n   let spawnOpts;\n   ad.spawn = async (_worker, _brief, opts) => { spawnOpts = opts; return { ok: true }; };\n   let creates = 0;\n   const { c, log } = harness(ad, undefined, {\n     create: async () => { creates += 1; return { path: wt, branch: 'baton/forked-task', baseSha: 'base-1' }; },\n   });\n   const h = await c.spawn('session', brief(), {\n     taskId: 'forked-task', refines: 'parent-task', session: { mode: 'fork', id: 'parent-session', lastTurnId: 'parent-turn' },\n   });\n   await until(() => spawnOpts && c.list()[0].sessionContext);\n   assert.equal(creates, 1);\n   assert.equal((await spawnOpts.worktreeReady).path, wt);\n   assert.deepEqual(h.lineage, { relation: 'fork', parentSessionId: 'parent-session', parentTaskId: 'parent-task' });\n   assert.ok(log.read(h.id).some((event) => event.kind === 'worktree.ready' && event.payload?.worktree === wt));\n });\n \n test('PS7: replayed session is reattached only after bounded handshake proves the same native identity', async () => {\n   const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-wt-'));\n   const original = adapter();\n   const { c, log, coordination } = harness(original, undefined, {\n     create: async () => ({ path: wt, branch: 'baton/recover-task', baseSha: 'base-1' }),\n   });\n   const h = await c.spawn('session', brief(), { taskId: 'recover-task' });\n   await until(() => c.list()[0].sessionContext);\n   original.emit(h.id, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 111 }, 1);\n   original.emit(h.id, 'lifecycle.turn_completed', completed('before restart'), 1);\n   await until(async () => (await c.result(h.id)).ready);\n \n   const resumed = adapter();\n   resumed.spawn = async (worker) => {\n     resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 222 }, 1);\n     resumed.emit(worker, 'lifecycle.turn_started', { sessionId: 'recover-native' }, 1);\n     return { ok: true };\n   };\n   const replay = new Coordinator({\n     log, coordination, fences: new FenceTable(), adapters: { session: resumed }, repoRoot: null,\n     worktrees: {\n       create: async () => ({}), capture: async () => ({ sha: 'x' }), createVerifyWorktree: async () => ({ path: tmpdir() }),\n       removeVerifyWorktree: async () => {}, remove: async () => {}, reconcile: async () => {},\n       validateSessionContext: async (context) => ({ ok: context.worktree === wt }),\n     },\n     referee: async () => ({ reverified: true, observedExit: 0 }), route: () => 'session',\n     approvalTimeoutMs: 1000, stopDeadlineMs: 100, recoveryTimeoutMs: 100,\n   });\n   assert.equal(replay.list()[0].status, 'orphaned');\n   const recovered = await replay.recover(h.id);\n   assert.equal(recovered.ok, true);\n   assert.equal(recovered.result, 'attached');\n   assert.equal(replay.list()[0].status, 'working');\n   assert.equal(replay.list()[0].sessionRef.id, 'recover-native');\n   assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_attached'));\n });\n \n+test('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\n+  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\n+  const original = adapter();\n+  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n+  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\n+  await until(() => c.list()[0].sessionContext);\n+  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\n+  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+\n+  const resumed = adapter();\n+  let spawnCalls = 0;\n+  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\n+  const replay = new Coordinator({\n+    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n+    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n+    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n+  });\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(spawnCalls, 0);\n+  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n+});\n+\n+test('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\n+  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-refine-wt-'));\n+  const original = adapter();\n+  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n+  const h = await c.spawn('session', brief(), { taskId: 'recover-refine-task' });\n+  await until(() => c.list()[0].sessionContext);\n+  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\n+  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+\n+  const resumed = adapter();\n+  resumed.spawn = async (worker) => {\n+    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\n+    return { ok: true };\n+  };\n+  const replay = new Coordinator({\n+    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n+    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n+    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n+  });\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('recovery refinement disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n+  await until(() => resumed.calls.kill.length === 1);\n+  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n+});\n+\n test('PS7: a reattachment identity mismatch is refused and the untrusted transport is killed', async () => {\n   const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-bad-wt-'));\n   const original = adapter();\n   const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   const h = await c.spawn('session', brief(), { taskId: 'recover-bad-task' });\n   await until(() => c.list()[0].sessionContext);\n   original.emit(h.id, 'lifecycle.spawned', { sessionId: 'expected-native', pid: 111 }, 1);\n   original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   await until(async () => (await c.result(h.id)).ready);\n \n   const resumed = adapter();\n   resumed.spawn = async (worker) => {\n     resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'wrong-native', pid: 333 }, 1);\n     return { ok: true };\n   };\n   const replay = new Coordinator({\n     log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n     worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n     referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   });\n   const recovered = await replay.recover(h.id);\n   assert.equal(recovered.ok, false);\n   assert.equal(recovered.result, 'session_identity_mismatch');\n   assert.equal(replay.list()[0].status, 'orphaned');\n   assert.equal(resumed.calls.kill.length, 1);\n   assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_failed'));\n });\n \n test('PS7: a hung reattachment is bounded, remains orphaned, and invokes adapter cleanup', async () => {\n   const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-timeout-wt-'));\n   const original = adapter();\n   const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   const h = await c.spawn('session', brief(), { taskId: 'recover-timeout-task' });\n   await until(() => c.list()[0].sessionContext);\n   original.emit(h.id, 'lifecycle.spawned', { sessionId: 'timeout-native', pid: 111 }, 1);\n   original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   await until(async () => (await c.result(h.id)).ready);\n \n   const resumed = adapter();\n   resumed.spawn = async () => new Promise(() => {});\n   const replay = new Coordinator({\n     log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n     worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n     referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 20, stopDeadlineMs: 100,\n   });\n   const recovered = await replay.recover(h.id);\n   assert.equal(recovered.ok, false);\n   assert.equal(recovered.result, 'recovery_timeout');\n   assert.equal(replay.list()[0].status, 'orphaned');\n   assert.equal(resumed.calls.kill.length, 1);\n });\ndiff --git a/spec/phase11/coordination-knowledge.md b/spec/phase11/coordination-knowledge.md\nindex f6d50af..23ad20d 100644\n--- a/spec/phase11/coordination-knowledge.md\n+++ b/spec/phase11/coordination-knowledge.md\n@@ -1,148 +1,157 @@\n # Phase 11.4 — durable coordination and knowledge substrate\n \n This specification implements goal catalog A/G and the Scratch/Cairn portions of H without a\n homelab or `project-manager` runtime dependency. `project-manager` is prior-art inspiration for\n typed causal structure and audits only. Baton owns a deployment-neutral, single-host-first event\n store and deterministic projections.\n \n The first implementation vertical is deliberately one substrate, not four unrelated databases:\n task, artifact, Scratch, and knowledge mutations are typed coordination events. The append-only\n event file is truth; every query result is a replayable projection.\n \n ## CK1 — one durable coordination stream\n \n `CoordinationStore(root)` owns an append-only `events.jsonl` with gap-free global `seq`, hub-stamped\n ISO time, schema version, `kind`, authenticated/audited `actor`, idempotency key, and JSON payload.\n Only the hub writes. An append is durable before its projection mutates. Duplicate idempotency keys\n return the original event without a second mutation. Torn/malformed tails fail startup visibly;\n they are never silently reset or skipped. Replay produces byte-equivalent public projections.\n \n The store path is explicitly supplied by the assembly and never derived from a worker cwd. It has\n no network, PM, or homelab dependency.\n \n ### CK1a — relation to per-worker operational logs\n \n Existing `<worker>.jsonl` streams remain lossless high-volume operational telemetry with local\n per-worker sequence numbers. They are not a second task/KG truth. Every state-changing driver path\n first appends its typed coordination mutation; only then may the in-memory task/artifact/Scratch/KG\n projection change. Operational evidence is referenced as `{worker, seq, digest}` and becomes\n cross-worker comparable only through an `evidence.mapped` coordination event whose global sequence\n is the observation order. Knowledge temporal checks use the global coordination sequence, never a\n bare per-worker sequence.\n \n The two files cannot be transactionally committed together. Reconciliation therefore scans\n operational evidence references at startup: an operational event with no coordination mutation is\n telemetry only and cannot affect task/KG truth; a coordination event pointing to missing or\n digest-mismatched operational evidence fails integrity validation. Coordination append failure is\n fatal to the requested state mutation and leaves its projection unchanged. It may not use the\n legacy warning-and-drop behavior of the telemetry sink.\n \n Construction validates the complete coordination stream immediately: JSON parse, schema version,\n gap-free sequence, unique idempotency key, and final newline. A truncated/torn tail is an\n actionable startup error and is never auto-truncated. Schema migration is explicit versioned event\n translation into a new store, not in-place reinterpretation.\n \n ## CK2 — typed durable task DAG and claims\n \n A task record contains:\n \n ```text\n id, brief, status, deps[], assignee, version, refines, taskType,\n createdEvent, claimedEvent, terminalEvent, artifactIds[]\n ```\n \n Status is `pending | working | input_required | completed | failed | cancelled`. Terminal states\n are immutable. Allowed nonterminal transitions are explicit. Creation rejects missing deps and\n cycles. A task is ready iff it is pending, unassigned, and every dependency is completed.\n `claimTask(id, worker, expectedVersion, key)` is compare-and-swap: exactly one concurrent caller\n wins; stale, blocked, already-assigned, and terminal claims are typed refusals and do not append.\n Refinement creates a new task with `refines`; it never reopens a terminal task.\n \n Coordinator `spawn()` durably creates the task before returning a handle, including when the task\n will remain queued. It may preallocate a public pending worker handle as a local reservation, but\n the durable task `assignee` remains null until dispatch wins `claimTask`; reservation is not claim.\n Dispatch durably claims it. Input waits, recovery, cancellation, verification\n accept/reject, and integration/publication outcomes are reflected by typed events. Restart rebuilds\n queued tasks, dependency readiness, assignments, terminal state, and automatic identifiers before\n dispatching anything.\n \n+Every adapter- or outside-world-reaching action has a durable intent before the effect: stop,\n+persistent follow-up, recovery attach, review-task creation, local integration, publication\n+request, and publication authorization. If intent append fails, no adapter, worktree/Git merge, or\n+publisher is called. If an adapter has already confirmed stop/attach/turn advancement when a later\n+terminal/refinement append fails, the coordinator is poisoned, resolves any public waiter with a\n+typed coordination-unavailable result, kills or quarantines the ambiguous transport, and restart\n+closes the still-nonterminal durable task. Asynchronous adapter callbacks may not turn an already\n+recorded fatal coordination fault into an uncaught process exception.\n+\n `task.created` persists `brief`, `deps`, `refines`, `taskType`, requested vendor/model/session\n policy, and the reserved public handle ID. `task.claimed` persists assignee, resolved vendor/model,\n and expected/new versions. Refusal codes are `stale_version | already_assigned |\n deps_unsatisfied | terminal | cycle`; refusals append no event. Recovery terminalization refines or\n terminates the durable record and may never reopen a terminal predecessor.\n \n ## CK3 — immutable artifact manifest\n \n Artifacts are manifests, never copied bytes:\n \n ```text\n id, taskId, kind, refs, mediaType, digest, size?, provenance[], createdEvent\n ```\n \n Kinds include `commit | diff | verification | coverage | mutation | review | report |\n counterexample | representation | skill | bench-result`. `refs` are Git SHAs/refs or explicit\n content-addressed paths owned by Baton. Registration validates the task, snapshots caller-owned\n data, and is idempotent by manifest identity. `artifact.registered` names the immutable manifest;\n `artifact.superseded` links a correction without mutation. Failed/cancelled tasks may register\n counterexamples, logs, and reports, but an artifact marked `accepted` must cite the accepting\n verification/review event. Accepted capture, verdict, independent review, integration, and\n publication payloads carry manifest IDs through the public driver rather than leaving provenance\n split across `capturedSha` and worker prose.\n \n ## CK4 — Scratch is an event-derived operational projection\n \n Scratch supports the smallest worker-useful primitives:\n \n - append-only facts with namespace/key, immutable `envRef:{repoId,treeSha}`, `observed | derived`\n   grounding, evidence refs,\n   and optional explicit expiry event;\n - advisory claims with resource glob, owner worker/task, intent, `envRef`, supervisor fence,\n   version, and lease deadline;\n - point checks that return clear/held plus a mandatory cross-environment warning when the fact or\n   claim was observed on another tree; and\n - explicit `scratch.claim_expired` / `scratch.fact_expired` events.\n \n There is no reader-clock expiry, heartbeat verb, free-text fleet chat, mutable world-state blob, or\n general CAS document in the first vertical. Claim expiry is a hub-emitted event slaved to worker\n lease/terminal state, so replay at different wall times is identical. Path-resource conflicts use\n conservative glob intersection; uncertain overlap conflicts rather than false-negatives. Exclusive\n OS resources remain the sandbox/allocator's job, not an advisory Scratch claim.\n \n Worker-facing participation is eventually ambient through mediated tool results; until that tool\n path ships, the public store exposes deterministic point checks rather than a polling/watch loop.\n Live worktree paths are invalid environment references; callers snapshot `git write-tree` (or an\n equivalent content digest) before posting. A different `treeSha` always produces the exact warning\n frame `observed on <treeSha> — not your tree`.\n \n Normative path-claim overlap is conservative: normalize repository-relative POSIX paths; exact\n paths overlap on equality; a glob overlaps an exact path if the path matches; two globs overlap if\n either literal prefix before its first wildcard is a prefix of the other or an implementation can\n exhibit a matching witness. If disjointness cannot be proven, they conflict. This may false-positive\n but may not false-negative a known overlap.\n \n ## CK5 — self-contained typed bitemporal causal knowledge graph\n \n The graph is a materialized view over coordination events. It supports typed nodes:\n \n ```text\n Run, Task, Artifact, Phase, Experiment, Finding, Decision, Hypothesis, Principle,\n Constraint, Literature, Research, RouteStat, Skill, Counterexample, Representation,\n ScratchFact\n ```\n \n and typed directional edges:\n \n ```text\n Supports, Contradicts, Supersedes, Informed, ProducedBy, Contains, DependsOn,\n Refines, ReadBy, VerifiedBy, DerivedFrom, Affects, Cites, ObservedIn\n ```\n \n Every node and edge stores observation time (coordination seq/time), event time (referenced source\n seq/time), and valid time (`validFrom`, nullable `validTo`). Invalidated beliefs are never deleted.\n `Supersedes` and contradiction resolution append invalidation events with compare-and-swap on the\n current validity version.\n \n A Decision is rejected unless it has at least one `Informed` evidence reference. A verified\n Finding is rejected unless it has `ProducedBy`/`VerifiedBy` evidence. Temporal coherence rejects\n evidence whose source sequence/time is later than the node or edge event time. Worker/recalled text\n@@ -150,97 +159,102 @@ is always marked untrusted prose; grounding is hub-derived (`verified | observed\n asserted`) and never taken from a model confidence float.\n \n `queryKnowledge({asOf, observedAt, types, grounding})` applies both temporal dimensions.\n `traceKnowledge(id)` returns causal paths to immutable events/artifacts. Recall is pull-only,\n token-bounded at the caller, contradiction-aware, provenance-framed, and logged with `ReadBy`;\n nothing is automatically injected into a worker context.\n \n The durable read record is `knowledge.read{readerActor, readerWorker, runId, taskId, nodeIds[],\n query, asOf, observedAt, validityVersions}`. Its append must succeed before recalled content is\n returned. The graph projection creates `ReadBy` edges from every returned node to the reader task/\n run. An unlogged read is a failed operation, never a degraded success.\n \n ## CK6 — selective promotion and health\n \n Promotion is deterministic candidate generation, not an LLM in the control path:\n \n - accepted terminal task + verification/artifact → `knowledge.promoted{kind:'Finding'}`;\n - consequential human/orchestrator spawn, reroute, accept, integrate, publish, abort → Decision;\n - run boundary → Run scorecard;\n - repeated verified outcome → RouteStat/Playbook candidate;\n - cited observed Scratch fact → `knowledge.promotion_candidate{kind:'Finding'}`;\n - derived/formal Scratch fact remains quarantined until an independent oracle links it.\n \n The first vertical implements explicit node/edge writes plus named task/artifact auto-promotion\n hooks. Failed/unaccepted tasks never auto-promote a positive Finding; they may promote a\n Counterexample or failure Finding with explicit grounding.\n Later scorecard/recall promotion uses the same schema. `auditKnowledge()` reports causal\n completeness, temporal coherence, orphan nodes, contradiction resolution, invalid references,\n recall utility, and read-contamination blast radius separately—never one unexplained green bit.\n \n ## CK7 — contamination and read provenance\n \n Every knowledge or Scratch read that crosses into an orchestrator/worker context records reader,\n run/task, returned IDs, query/as-of boundary, and source validity versions through\n `knowledge.read`. If a node is later invalidated,\n `knowledge.contamination_record{nodeId, invalidationEvent, affectedReadEvents[]}` is appended and\n `affectedReaders(id)` projects every downstream task/run that consumed it, including current task\n status. A\n cross-tree Scratch read is labeled “observed on X — not your tree.” This is required before shared\n knowledge can influence routing or acceptance.\n \n ## CK8 — authority and public assembly\n \n The store is mandatory in `createDriver()` and returned as `{coordinator, story, router, log,\n coordination}`. The coordinator receives it in its constructor and invokes one non-optional\n state-changing integration point for spawn/create, dispatch/claim, input wait/resume, terminal,\n capture/verification, review, integration, and publication. A driver state mutation with no\n coordination event is a contract failure, not an optional sink failure. Direct mutation methods\n require an actor and\n idempotency key; future MCP/web surfaces map authenticated identity into that same actor field and\n do not bypass coordinator fences or approvals. Knowledge export/import is ordinary data I/O behind\n future approval gates, not a special PM/homelab integration.\n \n ## CK9 — zero-quota safety gate\n \n Temp-directory and temp-Git tests must prove before provider dogfooding:\n \n 1. duplicate append keys and duplicate task creates are idempotent;\n 2. queued/unassigned tasks and dependency readiness survive restart;\n 3. claim CAS has exactly one winner and terminal states cannot reopen;\n 4. cycles, stale versions, invalid transitions, and missing deps refuse without events;\n 5. artifact manifests are immutable and linked to accepted tasks;\n 6. Scratch conflicts, explicit lease expiry, replay determinism, and cross-tree warnings;\n 7. causal/temporal graph rejections, bitemporal as-of query, contradiction/supersession history,\n    and affected-reader tracing;\n 8. public-driver task creation, terminal verdict, captured commit, review, and integration appear\n    in the durable substrate; and\n 9. a full restart has identical task/artifact/Scratch/KG projections before any new dispatch.\n 10. an unsatisfied-dependency task crashes before dispatch and replays pending, unassigned, with\n     its exact `deps[]`, brief, requested vendor/model/session, and reserved handle identity;\n 11. a multi-task DAG replays with the same ready set and cannot dispatch a dependent early;\n 12. a truncated tail, sequence gap, duplicate key, missing operational evidence, and injected\n     append failure all fail visibly without projection mutation;\n 13. every public spawn/claim/input/terminal/capture/review/integration/publication state change has\n     a coordination event, and the test fails if coordinator state is nonempty while the substrate\n     stream is empty;\n 14. operational evidence mapping gives two worker-local events a comparable global observation\n     order without treating their local sequences as globally ordered; and\n 15. after a logged knowledge read, invalidating the node yields a nonempty contamination record\n     and `affectedReaders()` result; a forced read-log append failure returns no recalled content.\n+16. injected append failures cover create, claim, input wait/resume, stop intent, cancel terminal,\n+    persistent follow-up/recovery refinement, review creation, terminal artifact batch,\n+    integration intent, publication authorization, and knowledge/Scratch reads. Pre-effect\n+    failures call no adapter/Git/publisher; post-effect failures are bounded, poison authority,\n+    preserve the earlier intent, and replay to a non-success terminal state.\n \n After CK9 is green and committed, Baton may recursively run a real provider review against this\n spec and implementation. The recursive task must use isolated runtime credentials/budgets, exact\n model selection, a pinned verification command, independent trust-gate verification, and confirmed\n kill/process/worktree/branch cleanup.\n \n ## Explicitly later, never deleted\n \n - durable pending question/approval reconstruction and automatic live-session rejoin;\n - a SQLite/other query index (projection only), multi-process writer locking, and multi-machine\n   replication;\n - ambient Scratch injection in tool mediation, Bench action-cache execution, notify subscriptions,\n   and task promotion from contention;\n - automatic scorecards, bounded lexical/graph recall ranking, RouteStat feedback, Playbook/Skill\n   promotion, and import/export tooling;\n - authenticated MCP/HTTPS/WebSocket access (uses this substrate but ships in its own phase); and\n - graph-backed Atlas/CPG/representation nodes (schema is reserved here; producers ship later).\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'git show --unified=80 c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs spec/phase11/coordination-knowledge.md'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":16,"ts":"2026-07-11T20:06:25.675Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_g2HJCtp7pJ4bShnbWxlXhM8A","command":"/bin/zsh -lc \"sed -n '69,76p;212,248p' spec/phase11/coordination-knowledge.md && printf '\\\\n---DOC---\\\\n' && sed -n '1,220p' docs/26-full-system-goal.md && printf '\\\\n---ACC---\\\\n' && sed -n '1,220p' spec/phase11/acceptance-integration.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"73280","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"sed -n '1,220p' docs/26-full-system-goal.md","name":"26-full-system-goal.md","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/docs/26-full-system-goal.md"},{"type":"read","command":"sed -n '1,220p' spec/phase11/acceptance-integration.md","name":"acceptance-integration.md","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/acceptance-integration.md"}],"aggregatedOutput":"Every adapter- or outside-world-reaching action has a durable intent before the effect: stop,\npersistent follow-up, recovery attach, review-task creation, local integration, publication\nrequest, and publication authorization. If intent append fails, no adapter, worktree/Git merge, or\npublisher is called. If an adapter has already confirmed stop/attach/turn advancement when a later\nterminal/refinement append fails, the coordinator is poisoned, resolves any public waiter with a\ntyped coordination-unavailable result, kills or quarantines the ambiguous transport, and restart\ncloses the still-nonterminal durable task. Asynchronous adapter callbacks may not turn an already\nrecorded fatal coordination fault into an uncaught process exception.\n## CK9 — zero-quota safety gate\n\nTemp-directory and temp-Git tests must prove before provider dogfooding:\n\n1. duplicate append keys and duplicate task creates are idempotent;\n2. queued/unassigned tasks and dependency readiness survive restart;\n3. claim CAS has exactly one winner and terminal states cannot reopen;\n4. cycles, stale versions, invalid transitions, and missing deps refuse without events;\n5. artifact manifests are immutable and linked to accepted tasks;\n6. Scratch conflicts, explicit lease expiry, replay determinism, and cross-tree warnings;\n7. causal/temporal graph rejections, bitemporal as-of query, contradiction/supersession history,\n   and affected-reader tracing;\n8. public-driver task creation, terminal verdict, captured commit, review, and integration appear\n   in the durable substrate; and\n9. a full restart has identical task/artifact/Scratch/KG projections before any new dispatch.\n10. an unsatisfied-dependency task crashes before dispatch and replays pending, unassigned, with\n    its exact `deps[]`, brief, requested vendor/model/session, and reserved handle identity;\n11. a multi-task DAG replays with the same ready set and cannot dispatch a dependent early;\n12. a truncated tail, sequence gap, duplicate key, missing operational evidence, and injected\n    append failure all fail visibly without projection mutation;\n13. every public spawn/claim/input/terminal/capture/review/integration/publication state change has\n    a coordination event, and the test fails if coordinator state is nonempty while the substrate\n    stream is empty;\n14. operational evidence mapping gives two worker-local events a comparable global observation\n    order without treating their local sequences as globally ordered; and\n15. after a logged knowledge read, invalidating the node yields a nonempty contamination record\n    and `affectedReaders()` result; a forced read-log append failure returns no recalled content.\n16. injected append failures cover create, claim, input wait/resume, stop intent, cancel terminal,\n    persistent follow-up/recovery refinement, review creation, terminal artifact batch,\n    integration intent, publication authorization, and knowledge/Scratch reads. Pre-effect\n    failures call no adapter/Git/publisher; post-effect failures are bounded, poison authority,\n    preserve the earlier intent, and replay to a non-success terminal state.\n\nAfter CK9 is green and committed, Baton may recursively run a real provider review against this\nspec and implementation. The recursive task must use isolated runtime credentials/budgets, exact\nmodel selection, a pinned verification command, independent trust-gate verification, and confirmed\nkill/process/worktree/branch cleanup.\n\n---DOC---\n# 26 — Full-system goal: no planned capability disappears\n\nThis is the plan of record after the 2026-07-11 completeness audit. It supersedes any reading of\ndocs 22–25 that treats “phase 10 complete” as completion of Baton’s larger design. Phase 10 proved\na useful first-turn control spine. The active goal is the complete fleet system described across\n`SYSTEM.md`, `docs/`, `spec/`, and `reviews/`.\n\n“Later”, “fenced”, and “research bet” are sequencing/status labels, not deletion mechanisms. Every\nitem below remains visible until it is either shipped with evidence or explicitly retired by a\nrecorded, evidence-backed decision. A feature cannot vanish from the goal through a summary.\n\n## Scope correction\n\nThe shared knowledge design takes architectural inspiration from `project-manager`, but Baton has\n**no homelab integration or runtime dependency** in this goal. It owns a deployment-neutral typed\ncausal substrate and may expose ordinary import/export interfaces later. This avoids coupling the\nproduct to one machine or knowledge service.\n\n## Definition of complete\n\nBaton is complete when one orchestrator can choose harness **and model**, direct persistent\nmulti-vendor workers through a durable northbound surface, observe and govern them safely, accept\nand integrate only independently established work, share operational/coordinative/epistemic\nknowledge, and give workers the complete capability and representation planes below. Every claim\nmust be reachable through the public assembly, test-locked, adversarially reviewed, and live-proven\nwhere a real harness or operating-system boundary is involved.\n\nThe implementation loop for every increment is:\n\n1. verify current reality;\n2. write numbered contracts;\n3. capture a red test or measured failing probe;\n4. implement the smallest complete vertical;\n5. run narrow and full validation;\n6. adversarially review seams, not only modules;\n7. live-prove the real boundary; and\n8. update this catalog and the evidence ledger.\n\nRecursive dogfooding resumes only after the lifecycle, provenance, and credential gates relevant\nto the recursive run are green.\n\n## Complete capability catalog\n\n### A. Fleet control and reliability\n\n- Eight public commands: spawn, send, wait, respond, interrupt, result, list, kill.\n- Ordered delivery, human-over-policy fencing, single-consumer interactions, two-phase stop,\n  terminal monotonicity, bounded setup/turn/stop operations, and process/worktree reap.\n- Persistent multi-turn workers; interrupt-follow-up; resume, fork, rejoin, checkpoint, rewind, and\n  crash recovery without fabricating uniform semantics across vendors.\n- Restart-safe task/worker identity, pending interactions, fences, adapter/session references,\n  worktree ownership, story, budgets, and routing state.\n- Dependency DAG, refinement links, idempotent claims, artifact registry, path leases, and\n  deterministic ready-work selection.\n\n### B. Harness and model selection\n\nHarness/CLI and model are independent axes. The orchestrator can:\n\n- name an exact harness and exact model;\n- select a model while leaving harness routing automatic;\n- constrain allowed/denied models or families and express ordered preferences;\n- select reasoning/service tier where the harness exposes it;\n- inspect available/default/current model information and the provenance/freshness of that card;\n- see requested, resolved, observed, and rerouted model identities in handles, events, results,\n  verification, routing statistics, replay, scorecards, and commit attribution; and\n- fail visibly when a model cannot be honored—never silently fall back to a harness default.\n\nSelection must map to real controls: Claude/GLM `--model`, Codex thread/turn model overrides, Grok\n`--model`/ACP model state, and future adapters’ native mechanism.\n\n### C. Southbound harness depth\n\n- Claude: native session steering/interrupt, approvals/questions, hooks, context/usage\n  introspection, compaction events, constraint reinjection, model/permission reconfiguration,\n  resume/fork/rewind, config-home isolation, and live capability discovery.\n- Codex: app-server sessions, steer/interrupt, approvals/questions, thread inject/resume/fork,\n  goal pinning, structured outputs, review, compaction, usage/rate limits, broker/daemon topology,\n  model/service/reasoning selection, sandbox policy, and schema-driven feature detection.\n- Grok: ACP core plus model selection/state, session load/fork/rewind, auth, extensions, usage,\n  multi-client attach, home/MCP isolation, and explicit handling of unsupported ask-user behavior.\n- GLM: Claude-harness session parity, exact model mapping, non-refuser capability metadata,\n  concurrency/quota inputs, scoped credentials, and live proof when credentials are available.\n- Honest one-shot tier and future ACP adapters remain separately carded; no reduced tier may pose\n  as the session product.\n\n### D. Safety and governance\n\n- Real OS sandbox profiles, worktree confinement, network policy, scoped environment/credentials,\n  isolated harness homes, and approval-gated outside-world side effects.\n- Dry-run/approve-all/sample/autonomous trust ramp with emergency stop always available.\n- Wall, token, USD, rate-limit, account-seat, and quota-window budgets folded into authoritative\n  state with thresholds, hard stops, and degradation policies.\n- Deterministic watchdog actions for mechanical stall/loop/scope/churn cases; semantic failure\n  classifiers remain explicitly untrusted model judgments.\n- Correct provenance: hub facts, worker prose, external evidence, and derived claims never share a\n  trust label. Read edges support contamination/contradiction analysis.\n\n### E. Trust, review, and integration\n\n- Immutable validated delegation contract and exact definition of done.\n- Fresh-result verification plus base red→green, coverage-of-change, mutation strength,\n  impact-selected tests, property/fuzz/BMC/SMT/proof rungs, and reproducible counterexamples.\n- Independent oracle construction and cross-vendor semantic-diff review for risk-selected work.\n- Structured postmortems and failure attribution linked to source events.\n- Verified branch integration, textual then structured/semantic merge, conflict handling, effect\n  tripwire, review artifact, and explicit approval before push/deploy/other irreversible actions.\n\n### F. Routing, evaluation, and learning\n\n- Routing buckets keyed by harness, exact model, model family, task class, capability, policy, and\n  version; only verified outcomes learn; idempotent replay preserves them.\n- New-model exploration, decay, refusal feedback/reroute, operator pin/exclude/prefer controls,\n  quota awareness, and outcome/calibration telemetry.\n- Reproducible M0 control latency, M1 orchestration arms, and E2 cross-vendor decorrelation\n  evaluations, including null/negative outcomes and human-audit cost.\n\n### G. Three memory tempos plus Scratch\n\n- **Operational:** append-only event ledger, resumable cursors, replay, artifacts, and telemetry.\n- **Coordinative:** transactional task DAG, artifact manifest, claims/leases, refinements, and\n  ready-work queries.\n- **Epistemic:** durable typed causal knowledge with bounded recall.\n- **Scratch:** TTL/CAS/take/notify blackboard and memoized Bench for fleet-live facts and leases.\n\nThe epistemic layer borrows the strong ideas—not the deployment—from `project-manager`:\n\n- typed nodes: Run, Task, Artifact, Experiment, Finding, Decision, Hypothesis, Principle,\n  Constraint, Source/Literature, RouteStat, Skill, Counterexample, and Representation;\n- typed directional edges: Supports, Contradicts, Supersedes, Informed, ProducedBy, Contains,\n  DependsOn, Refines, ReadBy, VerifiedBy, and DerivedFrom;\n- every consequential decision has evidence edges to earlier immutable ledger events;\n- temporal coherence forbids an edge to evidence that did not yet exist;\n- bitemporal validity preserves what was believed then versus what is valid now;\n- selective promotion avoids turning every event into knowledge;\n- contradiction-gated, provenance-framed, token-bounded pull recall—never automatic shared-brain\n  injection; and\n- run scorecards expose graph and fleet health with metric breakdowns rather than one green bit.\n\n### H. Capability plane\n\nEvery capability uses one ACI envelope, capability cards, cost/provenance, resumable long work,\ncancellation, and hub re-verification.\n\n1. **Atlas:** shared lexical search, AST/CST structural search/rewrite, symbol graph and SCIP/LSP\n   navigation, optional semantic retrieval, base-plus-worktree overlays, staleness, and `code_seed`.\n2. **Vantage:** DAP observation plans, causal debugging results, record/replay assets, exclusive\n   debuggee leases, and reproducible postmortems.\n3. **Evidence Ladder:** tests through proof with honest language/tool ceilings, mutation scores,\n   trusted kernels, full dependency-closure hashes, and no “proven” claim over worker-weakened\n   specifications.\n4. **Scratch:** operational blackboard/coordination REPL and memoized experiment Bench.\n5. **Skill Forge and computer use:** verify, promote, version, revoke, and share portable skills;\n   distill flaky GUI trajectories into deterministic automation where possible.\n6. **Cartographer and Quartermaster:** shared repository orientation/call graphs plus gated\n   dependency/reuse/security evidence.\n7. **Cairn:** run scorecards, causal findings/decisions, route statistics, promotion, recall,\n   contradiction/supersession, and exportable body of knowledge.\n\n### I. Representation and computation ladder\n\nRepresentation is negotiated by task phase and cost, not reduced to raw text:\n\n- R0 text/tokens;\n- R1 CST/AST via tree-sitter/ast-grep/difftastic/GumTree-class edit scripts;\n- R2 symbol/call/dependency graphs via LSP, SCIP, stack graphs, or equivalent;\n- R3 code property graph: AST + CFG + PDG/dataflow/taint, including CPG deltas;\n- R4 compiler/intermediate representations for optimization and translation validation;\n- R5 behavioral fingerprints from differential/property/fuzz execution and effect signatures;\n- R6 structured and semantic diff/merge over syntax, symbols, control/data-flow, and behavior;\n- R7 e-graph/equality-saturation experiments where the domain is tractable; and\n- representation choreography: orient with a coarse graph, focus with local AST/CPG, finish with\n  a semantic delta and attestation overlay, retracting views no longer useful.\n\nThe first shipped vertical is AST/structural delta for review. CPG, semantic merge, behavioral\nfingerprints, IR, and e-graphs remain in the goal with explicit prototype/evaluation gates; a\nnegative result retires a rung through a recorded Decision, never through omission.\n\n### J. Context, operator, northbound, and runtime\n\n- Vendor-shaped briefs, addressed context, orientation references, constraint/DoD reinjection on\n  compaction, context usage governance, and retractable views.\n- MCP northbound fleet tools and tasks, a long-lived daemon, resumable waits/subscriptions, and an\n  operator text/TUI seat with narrative, provenance, takeover, approvals, budgets, and emergency\n  control.\n- An **authenticated web northbound** for the human user ↔ orchestrator direction. HTTPS command\n  requests and a resumable WebSocket/event stream expose the same coordinator authority—not a\n  parallel state machine—including spawn/harness/model selection, steer/nudge/turn, approval and\n  question response, interrupt/kill, goals/tasks, narrative, budgets, and emergency control.\n  Authentication binds a stable user/session identity; authorization scopes commands and repo/run\n  access; every command carries an idempotency key, fence expectation, origin, and audit actor.\n  TLS, secure session/token storage, expiry/revocation, origin and CSRF checks, replay protection,\n  rate limits, reconnect cursors, backpressure, and disconnect semantics are contract-tested.\n  Browser loss never cancels work implicitly, and the web tier cannot bypass approval, sandbox,\n  budget, or trust gates. A richer visual UI is optional; the secure control connection is not.\n  Numbered contracts and its adversarial gate live in\n  `spec/phase12/authenticated-web-northbound.md` (WN1–WN10).\n- Observability exports use OpenTelemetry GenAI conventions.\n- One-machine production first, with a reliable Go or Elixir/OTP core after the executable-spec\n  contracts stabilize. Remote/multi-machine execution remains catalogued but is not allowed to\n  distort the local correctness boundary.\n\n## Current pursuit order\n\n1. Repair P0 control integrity: immutable briefs, truthful responses, crash-safe idempotent kill,\n   provenance, story identity, replay identity, and complete cleanup.\n2. Ship harness-independent exact model selection and attribution.\n3. Make persistent sessions genuinely usable through the driver; add resume/fork/rejoin.\n4. Ship OS/credential isolation and budget/watchdog governance.\n5. Complete hardened acceptance and structured integration.\n6. Ship the task/artifact/Scratch substrate and deployment-neutral causal knowledge core.\n7. Ship Atlas + AST structural delta + repo map + scorecard as the first capability vertical.\n8. Expand the representation and capability ladders in measured increments.\n9. Add MCP plus authenticated HTTPS/WebSocket user↔orchestrator control, operator surfaces,\n   production runtime, and the registered evaluations.\n\nNo later step is permission to erase it from the goal.\n\n---ACC---\n# Phase 11.3 — acceptance ladder and integration lifecycle\n\n## AC1 — red then green is measured at pinned commits\n\nWhen required, Baton runs the exact pinned verification command in both a fresh base-SHA sandbox\nand a distinct fresh result-SHA sandbox. Acceptance requires base failure and result success. Both\nsandboxes are always reaped, including setup/referee failure paths.\n\n## AC2 — coverage is computed from the actual delta\n\nWhen required, Baton computes changed line numbers between the pinned base and captured result,\nruns the brief's coverage command in the result sandbox, and accepts only when every changed line\nis reported executed. Missing/malformed coverage is unknown and cannot satisfy a required gate.\n\n## AC3 — mutation strength is an explicit rung\n\nAn optional pinned mutation command runs only after the primary result check passes and returns\nJSON `{killed,total,survived[]}`. The verdict records strength and survivors. Required mutation\nacceptance demands a nonzero mutant population and no survivor; malformed/missing evidence is\nunknown, never success.\n\n## AC4 — independent oracle and review provenance\n\nOracle tests and risk-selected reviews are separate tasks with a different eligible vendor/model\nfamily from the implementer. Their briefs receive the immutable spec and captured diff/artifact\nreferences, not the implementer's prose answer. Oracle/review results pass their own trust gates\nand become explicit acceptance inputs; same-family fallback is visible and cannot satisfy a\nrequired independent gate.\n\n## AC5 — verified integration is explicit and local-first\n\n`integrate(worker)` is allowed only for an accepted captured SHA. Baton first stops/reaps the\nattached worker, then applies an explicit strategy. The first shipped strategy is `ff-only`: main\nmust be clean and still descend directly from the task base. Success records before/result/after\nSHAs and marks the task integrated. Before branch cleanup Baton pins the captured SHA under\n`refs/baton/results/<sha>`; success releases the pin because main retains it, while refusal keeps\nthe pin as durable evidence. Non-fast-forward state refuses without rewriting history.\n\n## AC6 — irreversible publication requires a separate approval\n\nPush/deploy is not implied by verification or local integration. A publication request is a\nsingle-consumer approval containing remote/ref/SHA; only an explicit allow response may invoke the\nside effect. Deny, timeout, replay, stale fence, or missing approval performs no push. Secret\nremote credentials never enter the event payload. The remote is a configured credential-free name,\nthe branch is a full `refs/heads/*` ref, and the SHA must equal the locally integrated result.\n\n## Safety gate\n\nTemp-repo tests prove base/result freshness, coverage, mutation parsing, sandbox cleanup,\nintegration success/refusal, and no-push-without-approval before any provider or remote probe.\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"sed -n '69,76p;212,248p' spec/phase11/coordination-knowledge.md && printf '\\\\n---DOC---\\\\n' && sed -n '1,220p' docs/26-full-system-goal.md && printf '\\\\n---ACC---\\\\n' && sed -n '1,220p' spec/phase11/acceptance-integration.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":17,"ts":"2026-07-11T20:06:25.687Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":23485,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":47628,"inputTokens":46048,"cachedInputTokens":26752,"outputTokens":1580,"reasoningOutputTokens":765},"last":{"totalTokens":23485,"inputTokens":23129,"cachedInputTokens":12160,"outputTokens":356,"reasoningOutputTokens":69},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":47628,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":18,"ts":"2026-07-11T20:06:25.688Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":19,"ts":"2026-07-11T20:06:25.688Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"The diff adds intent logging around recovery, integration, and publication, but I need the exact control flow around those paths and the new CK9 assertions before I can decide whether the guarantees actually hold."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":20,"ts":"2026-07-11T20:06:39.206Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_knW2FTksb9MIP6PuESJ1D6Em","command":"/bin/zsh -lc \"nl -ba impl/test/phase11-persistent-sessions.test.mjs | sed -n '430,560p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"64632","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/test/phase11-persistent-sessions.test.mjs | sed -n '430,560p'","name":"phase11-persistent-sessions.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-persistent-sessions.test.mjs"}],"aggregatedOutput":"   430\t  assert.equal(recovered.result, 'attached');\n   431\t  assert.equal(replay.list()[0].status, 'working');\n   432\t  assert.equal(replay.list()[0].sessionRef.id, 'recover-native');\n   433\t  assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_attached'));\n   434\t});\n   435\t\n   436\ttest('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\n   437\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\n   438\t  const original = adapter();\n   439\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   440\t  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\n   441\t  await until(() => c.list()[0].sessionContext);\n   442\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\n   443\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   444\t  await until(async () => (await c.result(h.id)).ready);\n   445\t\n   446\t  const resumed = adapter();\n   447\t  let spawnCalls = 0;\n   448\t  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\n   449\t  const replay = new Coordinator({\n   450\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   451\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   452\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   453\t  });\n   454\t  const rawAppend = coordination._appendFile;\n   455\t  coordination._appendFile = (file, body, encoding) => {\n   456\t    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\n   457\t    return rawAppend(file, body, encoding);\n   458\t  };\n   459\t  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n   460\t  assert.equal(spawnCalls, 0);\n   461\t  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n   462\t});\n   463\t\n   464\ttest('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\n   465\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-refine-wt-'));\n   466\t  const original = adapter();\n   467\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   468\t  const h = await c.spawn('session', brief(), { taskId: 'recover-refine-task' });\n   469\t  await until(() => c.list()[0].sessionContext);\n   470\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\n   471\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   472\t  await until(async () => (await c.result(h.id)).ready);\n   473\t\n   474\t  const resumed = adapter();\n   475\t  resumed.spawn = async (worker) => {\n   476\t    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\n   477\t    return { ok: true };\n   478\t  };\n   479\t  const replay = new Coordinator({\n   480\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   481\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   482\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   483\t  });\n   484\t  const rawAppend = coordination._appendFile;\n   485\t  coordination._appendFile = (file, body, encoding) => {\n   486\t    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('recovery refinement disk full');\n   487\t    return rawAppend(file, body, encoding);\n   488\t  };\n   489\t  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n   490\t  await until(() => resumed.calls.kill.length === 1);\n   491\t  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n   492\t});\n   493\t\n   494\ttest('PS7: a reattachment identity mismatch is refused and the untrusted transport is killed', async () => {\n   495\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-bad-wt-'));\n   496\t  const original = adapter();\n   497\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   498\t  const h = await c.spawn('session', brief(), { taskId: 'recover-bad-task' });\n   499\t  await until(() => c.list()[0].sessionContext);\n   500\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'expected-native', pid: 111 }, 1);\n   501\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   502\t  await until(async () => (await c.result(h.id)).ready);\n   503\t\n   504\t  const resumed = adapter();\n   505\t  resumed.spawn = async (worker) => {\n   506\t    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'wrong-native', pid: 333 }, 1);\n   507\t    return { ok: true };\n   508\t  };\n   509\t  const replay = new Coordinator({\n   510\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   511\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   512\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   513\t  });\n   514\t  const recovered = await replay.recover(h.id);\n   515\t  assert.equal(recovered.ok, false);\n   516\t  assert.equal(recovered.result, 'session_identity_mismatch');\n   517\t  assert.equal(replay.list()[0].status, 'orphaned');\n   518\t  assert.equal(resumed.calls.kill.length, 1);\n   519\t  assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_failed'));\n   520\t});\n   521\t\n   522\ttest('PS7: a hung reattachment is bounded, remains orphaned, and invokes adapter cleanup', async () => {\n   523\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-timeout-wt-'));\n   524\t  const original = adapter();\n   525\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   526\t  const h = await c.spawn('session', brief(), { taskId: 'recover-timeout-task' });\n   527\t  await until(() => c.list()[0].sessionContext);\n   528\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'timeout-native', pid: 111 }, 1);\n   529\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   530\t  await until(async () => (await c.result(h.id)).ready);\n   531\t\n   532\t  const resumed = adapter();\n   533\t  resumed.spawn = async () => new Promise(() => {});\n   534\t  const replay = new Coordinator({\n   535\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   536\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   537\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 20, stopDeadlineMs: 100,\n   538\t  });\n   539\t  const recovered = await replay.recover(h.id);\n   540\t  assert.equal(recovered.ok, false);\n   541\t  assert.equal(recovered.result, 'recovery_timeout');\n   542\t  assert.equal(replay.list()[0].status, 'orphaned');\n   543\t  assert.equal(resumed.calls.kill.length, 1);\n   544\t});\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/test/phase11-persistent-sessions.test.mjs | sed -n '430,560p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":21,"ts":"2026-07-11T20:06:39.254Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_04Cm5ck9fKf4Z3K5cTuyEewy","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '880,1010p;1030,1145p;1898,2015p;2838,3025p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"74736","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '880,1010p;1030,1145p;1898,2015p;2838,3025p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"   880\t\n   881\t  /** PS7: explicitly reattach a replayed native session. Recovery never trusts a stale PID;\n   882\t   * authority comes only from a fresh adapter handshake that reports the expected native ID. */\n   883\t  async recover(workerId, opts = {}) {\n   884\t    this.tick();\n   885\t    const handle = this._getWorker(workerId);\n   886\t    const task = this._tasks.get(handle.taskId);\n   887\t    if (handle.status !== 'orphaned') return { ok: false, result: 'worker_not_orphaned' };\n   888\t    if (!task || !handle.sessionRef || handle.sessionRef.persistence !== 'native') {\n   889\t      return { ok: false, result: 'session_not_resumable' };\n   890\t    }\n   891\t    const adapter = this._adapters[handle.vendor];\n   892\t    if (!adapter || !cardSupportsSession(adapter.card(), { mode: 'resume' })) {\n   893\t      return { ok: false, result: 'session_not_resumable' };\n   894\t    }\n   895\t    const rawContext = opts.context ?? handle.sessionContext;\n   896\t    const context = rawContext\n   897\t      ? normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context: rawContext }).context\n   898\t      : null;\n   899\t    if (!context) return { ok: false, result: 'session_context_required' };\n   900\t    try {\n   901\t      await this._validateSessionContext(context);\n   902\t    } catch (err) {\n   903\t      return { ok: false, result: err.code ?? 'session_context_mismatch', reason: err.message };\n   904\t    }\n   905\t\n   906\t    const timeoutMs = opts.timeoutMs ?? this._recoveryTimeoutMs;\n   907\t    const admission = { events: [] };\n   908\t    admission.spawned = new Promise((resolve) => { admission.resolveSpawned = resolve; });\n   909\t    handle.turnAdmission = admission;\n   910\t    const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\n   911\t    const recoveryRequested = this._log.append({\n   912\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n   913\t      kind: 'control.recovery_requested', actor: opts.actor ?? 'orchestrator',\n   914\t      payload: { sessionRef: handle.sessionRef, context },\n   915\t    });\n   916\t    const recoveryEvidence = this._coordMapEvent(recoveryRequested);\n   917\t    this._coordRecord('recovery.requested', {\n   918\t      taskId: task.id, workerId, sessionId: handle.sessionRef.id, context, evidence: recoveryEvidence,\n   919\t    }, `driver.recovery.requested:${task.id}:${recoveryRequested.seq}`, opts.actor ?? 'orchestrator');\n   920\t    const runtime = this._ensureRuntimeScope(handle);\n   921\t\n   922\t    let timerHandle;\n   923\t    let timedOut = false;\n   924\t    const timeout = new Promise((resolve) => {\n   925\t      timerHandle = this._setTimeout(() => { timedOut = true; resolve({ timeout: true }); }, timeoutMs);\n   926\t      if (timerHandle && typeof timerHandle.unref === 'function') timerHandle.unref();\n   927\t    });\n   928\t    const attempt = Promise.resolve(adapter.spawn(workerId, task.brief, {\n   929\t      worktree: context.worktree,\n   930\t      timeoutMs: task.brief?.budget?.wallMin ? task.brief.budget.wallMin * 60000 : undefined,\n   931\t      model: handle.modelResolved ?? undefined,\n   932\t      reasoningEffort: handle.modelPolicy?.reasoningEffort,\n   933\t      serviceTier: handle.modelPolicy?.serviceTier,\n   934\t      session,\n   935\t      env: runtime?.env,\n   936\t      replaceEnv: runtime?.replaceEnv === true,\n   937\t    })).then((ack) => ({ ack }), (error) => ({ error }));\n   938\t\n   939\t    let outcome = await Promise.race([attempt, timeout]);\n   940\t    if (outcome?.ack?.ok === true && !timedOut) {\n   941\t      outcome = await Promise.race([\n   942\t        admission.spawned.then((event) => ({ ack: outcome.ack, spawned: event })),\n   943\t        timeout,\n   944\t      ]);\n   945\t    }\n   946\t    if (timerHandle != null) this._clearTimeout(timerHandle);\n   947\t\n   948\t    const expectedId = handle.sessionRef.id;\n   949\t    const observedId = outcome?.spawned?.payload?.threadId ?? outcome?.spawned?.payload?.sessionId;\n   950\t    const failed = outcome?.timeout\n   951\t      ? { result: 'recovery_timeout', reason: `native reattachment exceeded ${timeoutMs}ms` }\n   952\t      : outcome?.error\n   953\t        ? { result: 'recovery_exception', reason: String(outcome.error?.message ?? outcome.error) }\n   954\t        : outcome?.ack?.ok !== true\n   955\t          ? { result: 'recovery_refused', reason: outcome?.ack?.reason ?? 'adapter refused recovery' }\n   956\t          : observedId !== expectedId\n   957\t            ? { result: 'session_identity_mismatch', reason: `expected ${expectedId}, observed ${observedId ?? '(none)'}` }\n   958\t            : null;\n   959\t\n   960\t    if (failed) {\n   961\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n   962\t      handle.status = 'orphaned';\n   963\t      this._log.append({\n   964\t        worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n   965\t        kind: 'control.recovery_failed', actor: 'policy', payload: { ...failed, action: 'kill_untrusted_transport' },\n   966\t      });\n   967\t      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n   968\t      return { ok: false, ...failed };\n   969\t    }\n   970\t\n   971\t    const stamp = this._fences.bumpTurn(workerId);\n   972\t    let activeTask;\n   973\t    try {\n   974\t      activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n   975\t    } catch (err) {\n   976\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n   977\t      handle.status = 'orphaned';\n   978\t      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n   979\t      throw err;\n   980\t    }\n   981\t    activeTask.status = 'working';\n   982\t    activeTask.result = null;\n   983\t    activeTask.verdict = null;\n   984\t    activeTask.sessionRequest = session;\n   985\t    activeTask.sessionContext = context;\n   986\t    handle.status = 'working';\n   987\t    handle.sessionRequest = session;\n   988\t    handle.sessionContext = context;\n   989\t    handle.turnAdmission = null;\n   990\t    this._resetWatchdogTurn(handle);\n   991\t    this._log.append({\n   992\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n   993\t      kind: 'control.recovery_attached', actor: 'orchestrator',\n   994\t      payload: { sessionRef: handle.sessionRef, context },\n   995\t    });\n   996\t    this._log.append({\n   997\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n   998\t      kind: 'lifecycle.turn_started', actor: 'orchestrator', payload: { recovery: true },\n   999\t      modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n  1000\t    });\n  1001\t    for (const event of admission.events) this._handleEvent(event);\n  1002\t    return { ok: true, result: 'attached', handle: this._publicHandle(handle) };\n  1003\t  }\n  1004\t\n  1005\t  /** AC5: explicitly integrate an accepted captured commit. This never pushes. */\n  1006\t  async integrate(workerId, opts = {}) {\n  1007\t    this.tick();\n  1008\t    const handle = this._getWorker(workerId);\n  1009\t    const task = this._tasks.get(handle.taskId);\n  1010\t    if (!task || task.status !== 'completed' || !task.capturedSha) {\n  1030\t    if (handle.status === 'working' || handle.status === 'blocked' || handle.status === 'stopping' || handle.status === 'pending') {\n  1031\t      throw new IntegrationError('worker must be idle, dead, exited, or orphaned before integration', 'worker_not_quiescent');\n  1032\t    }\n  1033\t\n  1034\t    this._coordRecord('integration.requested', {\n  1035\t      taskId: task.id, workerId, strategy, sha: task.capturedSha,\n  1036\t      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\n  1037\t    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\n  1038\t\n  1039\t    if (typeof this._worktrees.retainResult === 'function') {\n  1040\t      task.retainedResultRef = await this._worktrees.retainResult(task.capturedSha);\n  1041\t    }\n  1042\t\n  1043\t    if (handle.status === 'idle') {\n  1044\t      const stopped = await this.kill(workerId, opts.actor ?? 'orchestrator');\n  1045\t      if (!['confirmed', 'already_dead', 'already_stopped'].includes(stopped.result)) {\n  1046\t        throw new IntegrationError('worker could not be safely stopped before integration', 'worker_stop_failed');\n  1047\t      }\n  1048\t    } else if (handle.status === 'exited') {\n  1049\t      await this.kill(workerId, opts.actor ?? 'orchestrator');\n  1050\t    }\n  1051\t    await this._removeTaskWorktree(task);\n  1052\t\n  1053\t    let integrated;\n  1054\t    try {\n  1055\t      integrated = await this._worktrees.integrate(task.capturedSha, { strategy });\n  1056\t    } catch (err) {\n  1057\t      const refusedEvent = this._log.append({\n  1058\t        worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1059\t        kind: 'integration.refused', actor: 'policy',\n  1060\t        payload: { strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef, reason: String(err?.message ?? err) },\n  1061\t      });\n  1062\t      const refusedEvidence = this._coordMapEvent(refusedEvent);\n  1063\t      this._coordRecord('integration.refused', {\n  1064\t        taskId: task.id, strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef,\n  1065\t        reason: String(err?.message ?? err), evidence: refusedEvidence,\n  1066\t      }, `driver.integration.refused:${task.id}:${refusedEvent.seq}`, 'policy');\n  1067\t      throw new IntegrationError(String(err?.message ?? err), 'non_fast_forward_or_dirty');\n  1068\t    }\n  1069\t    if (task.retainedResultRef && typeof this._worktrees.releaseResult === 'function') {\n  1070\t      try { await this._worktrees.releaseResult(task.retainedResultRef); } catch { /* merged HEAD now retains the result */ }\n  1071\t      task.retainedResultRef = null;\n  1072\t    }\n  1073\t    task.integration = Object.freeze({ ...integrated, strategy, actor: opts.actor ?? 'orchestrator' });\n  1074\t    const integrationEvent = this._log.append({\n  1075\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1076\t      kind: 'integration.completed', actor: opts.actor ?? 'orchestrator', payload: task.integration,\n  1077\t    });\n  1078\t    const integrationEvidence = this._coordMapEvent(integrationEvent);\n  1079\t    if (this._coordination) {\n  1080\t      const acceptingEvidence = this._coordination.task(task.id).artifactIds\n  1081\t        .map((artifactId) => this._coordination.artifact(artifactId))\n  1082\t        .filter((artifact) => artifact?.accepted === true)\n  1083\t        .flatMap((artifact) => artifact.provenance ?? []);\n  1084\t      this._coordination.registerArtifact({\n  1085\t        taskId: task.id, kind: 'report', refs: { beforeSha: task.integration.beforeSha, resultSha: task.integration.resultSha, afterSha: task.integration.afterSha },\n  1086\t        mediaType: 'application/vnd.baton.integration+json', accepted: true, provenance: [integrationEvidence, ...acceptingEvidence],\n  1087\t      }, { actor: opts.actor ?? 'orchestrator', key: `artifact.integration:${task.id}:${task.integration.afterSha}` });\n  1088\t      this._coordination.promoteKnowledgeNode({\n  1089\t        id: `decision:integrate:${task.id}:${integrationEvent.seq}`, type: 'Decision',\n  1090\t        body: `Integrated task ${task.id} at ${task.integration.afterSha}`, grounding: 'observed',\n  1091\t        informedBy: [`task:${task.id}`],\n  1092\t        evidence: [{ coordinationSeq: integrationEvidence.coordinationSeq }],\n  1093\t      }, { kind: 'Decision', trigger: 'integration' }, { actor: opts.actor ?? 'orchestrator', key: `knowledge.integration:${task.id}:${integrationEvent.seq}` });\n  1094\t      this._coordRecord('integration.completed', { taskId: task.id, integration: task.integration, evidence: integrationEvidence }, `driver.integration:${task.id}:${integrationEvent.seq}`, opts.actor ?? 'orchestrator');\n  1095\t    }\n  1096\t    return { ok: true, result: 'integrated', integration: task.integration };\n  1097\t  }\n  1098\t\n  1099\t  /** AC6: create an approval-gated exact-SHA publication request. No side effect occurs here. */\n  1100\t  requestPublication(workerId, target = {}, actor = 'orchestrator') {\n  1101\t    this.tick();\n  1102\t    const handle = this._getWorker(workerId);\n  1103\t    const task = this._tasks.get(handle.taskId);\n  1104\t    if (!task?.integration?.afterSha) {\n  1105\t      throw new PublicationError('publication requires a locally integrated result', 'result_not_integrated');\n  1106\t    }\n  1107\t    const remote = target.remote;\n  1108\t    const ref = target.ref;\n  1109\t    const sha = target.sha ?? task.integration.afterSha;\n  1110\t    if (typeof remote !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9._-]*$/.test(remote)) {\n  1111\t      throw new PublicationError('remote must be a credential-free git remote name', 'invalid_remote');\n  1112\t    }\n  1113\t    if (typeof ref !== 'string' || !/^refs\\/heads\\/[A-Za-z0-9._\\/-]+$/.test(ref) || ref.includes('..')) {\n  1114\t      throw new PublicationError('ref must be a full, safe refs/heads/* name', 'invalid_ref');\n  1115\t    }\n  1116\t    if (sha !== task.integration.afterSha) {\n  1117\t      throw new PublicationError('publication SHA must equal the integrated result SHA', 'sha_mismatch');\n  1118\t    }\n  1119\t    const stamp = this._fences.bumpHuman(workerId);\n  1120\t    const requestId = `publication-${workerId}-${++this._publicationSeq}`;\n  1121\t    const publication = Object.freeze({ remote, ref, sha });\n  1122\t    const deadlineAt = this._now() + this._approvalTimeoutMs;\n  1123\t    const record = {\n  1124\t      kind: 'publication', worker: workerId, state: 'pending', resolution: null, consumer: null,\n  1125\t      turnEpochAtAsk: stamp.turnEpoch, fenceAtAsk: stamp.fence,\n  1126\t      deadlineAt, publication,\n  1127\t    };\n  1128\t    const requestedEvent = this._log.append({\n  1129\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1130\t      kind: 'publication.requested', actor,\n  1131\t      payload: { requestId, ...publication, fence: stamp.fence, deadlineAt },\n  1132\t    });\n  1133\t    const evidence = this._coordMapEvent(requestedEvent);\n  1134\t    this._coordRecord('publication.requested', {\n  1135\t      taskId: task.id, workerId, requestId, publication, fence: stamp.fence, deadlineAt, evidence,\n  1136\t    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\n  1137\t    this._pending.set(requestId, record);\n  1138\t    return { ok: true, requestId, fence: stamp.fence, target: publication };\n  1139\t  }\n  1140\t\n  1141\t  _allocWorkerId() {\n  1142\t    return `w-${++this._workerSeq}`;\n  1143\t  }\n  1144\t\n  1145\t  _assertNoCycle(taskId, deps) {\n  1898\t    if (record.state === 'resolving') {\n  1899\t      // Wait for the reserved delivery. Echo its winner if it commits; retry fairly if the\n  1900\t      // delivery rolls back to pending.\n  1901\t      await record.resolvingDone;\n  1902\t      if (record.state === 'resolved') {\n  1903\t        return { ok: false, result: 'already_resolved', resolution: record.resolution };\n  1904\t      }\n  1905\t      return this._resolveRecord(requestId, answer, actor);\n  1906\t    }\n  1907\t    if (record.state !== 'pending') return { ok: false, result: 'already_resolved', resolution: record.resolution };\n  1908\t\n  1909\t    // CI2: reserve the single-consumer slot, but do not COMMIT resolution until the adapter\n  1910\t    // accepts delivery. A failed/throwing wire operation rolls back to pending for retry.\n  1911\t    record.state = 'resolving';\n  1912\t    let releaseResolving;\n  1913\t    record.resolvingDone = new Promise((resolve) => { releaseResolving = resolve; });\n  1914\t    const finishResolving = () => {\n  1915\t      releaseResolving();\n  1916\t      delete record.resolvingDone;\n  1917\t    };\n  1918\t\n  1919\t    const handle = this._workers.get(record.worker);\n  1920\t\n  1921\t    if (record.kind === 'publication') {\n  1922\t      const decision = answer?.decision;\n  1923\t      if (!['allow', 'deny'].includes(decision)) {\n  1924\t        record.state = 'pending';\n  1925\t        finishResolving();\n  1926\t        return { ok: false, result: 'invalid_decision' };\n  1927\t      }\n  1928\t      const currentFence = handle ? this._fences.current(handle.id).fence : null;\n  1929\t      const fenceValid = actor === 'policy' || answer?.fence === record.fenceAtAsk;\n  1930\t      if (!handle || !fenceValid || currentFence !== record.fenceAtAsk) {\n  1931\t        if (handle) {\n  1932\t          const refusedEvent = this._log.append({\n  1933\t            worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1934\t            kind: 'publication.refused', actor: 'policy',\n  1935\t            payload: { requestId, reason: 'stale_fence', remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n  1936\t          });\n  1937\t          const evidence = this._coordMapEvent(refusedEvent);\n  1938\t          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\n  1939\t        }\n  1940\t        record.state = 'resolved';\n  1941\t        record.consumer = actor;\n  1942\t        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n  1943\t        finishResolving();\n  1944\t        return { ok: false, result: 'stale_fence', current: currentFence };\n  1945\t      }\n  1946\t      if (decision === 'deny') {\n  1947\t        const deniedEvent = this._log.append({\n  1948\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1949\t          kind: 'publication.denied', actor,\n  1950\t          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n  1951\t        });\n  1952\t        const evidence = this._coordMapEvent(deniedEvent);\n  1953\t        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\n  1954\t        record.state = 'resolved';\n  1955\t        record.consumer = actor;\n  1956\t        record.resolution = { decision: 'deny' };\n  1957\t        finishResolving();\n  1958\t        return { ok: true, result: 'denied' };\n  1959\t      }\n  1960\t      if (typeof this._publisher !== 'function') {\n  1961\t        record.state = 'pending';\n  1962\t        finishResolving();\n  1963\t        return { ok: false, result: 'publication_unavailable' };\n  1964\t      }\n  1965\t      let authorizedEvent;\n  1966\t      try {\n  1967\t        authorizedEvent = this._log.append({\n  1968\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1969\t          kind: 'publication.authorized', actor,\n  1970\t          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha, fence: record.fenceAtAsk },\n  1971\t        });\n  1972\t        const evidence = this._coordMapEvent(authorizedEvent);\n  1973\t        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\n  1974\t      } catch (err) {\n  1975\t        record.state = 'pending';\n  1976\t        finishResolving();\n  1977\t        throw err;\n  1978\t      }\n  1979\t      let published;\n  1980\t      try {\n  1981\t        published = await this._publisher(record.publication);\n  1982\t      } catch (err) {\n  1983\t        record.state = 'pending';\n  1984\t        finishResolving();\n  1985\t        throw new PublicationError(String(err?.message ?? err), 'publisher_failed');\n  1986\t      }\n  1987\t      record.state = 'resolved';\n  1988\t      record.consumer = actor;\n  1989\t      record.resolution = { decision: 'allow' };\n  1990\t      const task = this._tasks.get(handle.taskId);\n  1991\t      void published;\n  1992\t      task.publication = Object.freeze({ requestId, ...record.publication, actor });\n  1993\t      try {\n  1994\t        const publicationEvent = this._log.append({\n  1995\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1996\t          kind: 'publication.completed', actor, payload: task.publication,\n  1997\t        });\n  1998\t        const publicationEvidence = this._coordMapEvent(publicationEvent);\n  1999\t        this._coordination?.promoteKnowledgeNode({\n  2000\t          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n  2001\t          body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n  2002\t          grounding: 'observed', informedBy: [`task:${task.id}`],\n  2003\t          evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n  2004\t        }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n  2005\t        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n  2006\t      } catch (err) {\n  2007\t        finishResolving();\n  2008\t        throw err;\n  2009\t      }\n  2010\t      finishResolving();\n  2011\t      return { ok: true, result: 'published', publication: task.publication };\n  2012\t    }\n  2013\t\n  2014\t    const clearPending = () => {\n  2015\t      if (!handle) return;\n  2838\t\n  2839\t      // A persistent worker can own a chain of immutable refinement tasks. Only the first turn\n  2840\t      // carries lifecycle.spawned.taskId; later native turns deliberately reuse the same worker\n  2841\t      // and wire session. The coordination stream is authoritative for which refinement is\n  2842\t      // current after restart, so associate the replayed terminal/result state with the newest\n  2843\t      // durable task reserved for this worker instead of silently snapping back to turn one.\n  2844\t      const durableWorkerTasks = this._coordination?.snapshot().tasks\n  2845\t        .filter((task) => (task.reservedWorkerId ?? task.assignee) === workerId)\n  2846\t        .sort((a, b) => a.createdEvent - b.createdEvent) ?? [];\n  2847\t      const currentDurableTask = durableWorkerTasks.at(-1) ?? null;\n  2848\t      if (currentDurableTask) taskId = currentDurableTask.id;\n  2849\t\n  2850\t      // Operational completion without its authoritative coordination terminal batch is a crash\n  2851\t      // gap, never permission to infer success from telemetry. Fail the claimed task durably so a\n  2852\t      // restart cannot leave it working forever or fabricate its missing accepted manifests.\n  2853\t      if (currentDurableTask && !TERMINAL_TASK_STATUSES.has(currentDurableTask.status)\n  2854\t        && TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2855\t        recoveryTerminalized = true;\n  2856\t        const priorOperationalStatus = terminalStatus;\n  2857\t        terminalStatus = 'failed';\n  2858\t        const gapEvent = this._log.append({\n  2859\t          worker: workerId, harness: events.at(-1)?.harness ?? '', turnEpoch: maxTurnEpoch,\n  2860\t          kind: 'control.recovery_terminalized', actor: 'policy',\n  2861\t          payload: { reason: 'coordination_terminal_batch_missing', priorStatus: priorOperationalStatus },\n  2862\t        });\n  2863\t        const gapEvidence = this._coordMapEvent(gapEvent);\n  2864\t        const transitioned = this._coordination.transitionTask(currentDurableTask.id, 'failed', currentDurableTask.version, {\n  2865\t          actor: 'policy', key: `task.failed:${currentDurableTask.id}:coordination_gap:${gapEvent.seq}`,\n  2866\t        }, gapEvidence ?? { reason: 'coordination_terminal_batch_missing' });\n  2867\t        const seeded = this._tasks.get(currentDurableTask.id);\n  2868\t        if (seeded) seeded.coordinationVersion = transitioned.task.version;\n  2869\t      }\n  2870\t\n  2871\t      // CI6: replay cannot resurrect an adapter session. Until the persistent-session phase can\n  2872\t      // prove native reattachment, any nonterminal reconstructed task is durably failed and its\n  2873\t      // worker is marked orphaned (uncontrollable), never presented as working/input_required.\n  2874\t      if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2875\t        recoveryTerminalized = true;\n  2876\t        terminalStatus = 'failed';\n  2877\t        const recoveryEvent = this._log.append({\n  2878\t          worker: workerId,\n  2879\t          harness: events.at(-1)?.harness ?? '',\n  2880\t          turnEpoch: maxTurnEpoch,\n  2881\t          kind: 'control.recovery_terminalized',\n  2882\t          actor: 'policy',\n  2883\t          payload: { reason: 'session_not_reattached', priorStatus: events.at(-1)?.kind ?? 'unknown' },\n  2884\t        });\n  2885\t        const durable = taskId ? this._coordination?.task(taskId) : null;\n  2886\t        if (durable && !TERMINAL_TASK_STATUSES.has(durable.status)) {\n  2887\t          const evidence = this._coordMapEvent(recoveryEvent);\n  2888\t          const transitioned = this._coordination.transitionTask(taskId, 'failed', durable.version, {\n  2889\t            actor: 'policy', key: `task.failed:${taskId}:replay:${recoveryEvent?.seq ?? maxTurnEpoch}`,\n  2890\t          }, evidence ?? { reason: 'session_not_reattached' });\n  2891\t          const seeded = this._tasks.get(taskId);\n  2892\t          if (seeded) {\n  2893\t            seeded.coordinationVersion = transitioned.task.version;\n  2894\t            this._expireScratchClaims(this._workers.get(workerId), seeded, 'replay_failed');\n  2895\t          }\n  2896\t        }\n  2897\t      }\n  2898\t\n  2899\t      this._fences.register(workerId);\n  2900\t      while (this._fences.current(workerId).turnEpoch < maxTurnEpoch) this._fences.bumpTurn(workerId);\n  2901\t\n  2902\t      if (taskId) {\n  2903\t        const task = this._tasks.get(taskId) ?? {\n  2904\t          id: taskId,\n  2905\t          brief: brief ?? minimalBrief(),\n  2906\t          deps: [],\n  2907\t          status: 'pending',\n  2908\t          assignee: workerId,\n  2909\t          worktree: null,\n  2910\t          result: null,\n  2911\t          verdict: null,\n  2912\t          taskType: 'general',\n  2913\t          vendorRequested,\n  2914\t          modelRequested,\n  2915\t          modelResolved,\n  2916\t          modelObserved,\n  2917\t          modelPolicy,\n  2918\t          modelMismatch,\n  2919\t          sessionRequest,\n  2920\t          sessionRef,\n  2921\t          sessionContext,\n  2922\t          lineage,\n  2923\t          capturedSha,\n  2924\t          integration,\n  2925\t          retainedResultRef,\n  2926\t          publication,\n  2927\t          review,\n  2928\t        };\n  2929\t        const durable = this._coordination?.task(taskId);\n  2930\t        task.assignee = durable?.reservedWorkerId ?? workerId;\n  2931\t        task.deps = durable ? [...durable.deps] : task.deps;\n  2932\t        task.coordinationVersion = durable?.version ?? task.coordinationVersion ?? null;\n  2933\t        task.status = durable?.status ?? terminalStatus;\n  2934\t        task.result = lastResult ?? task.result;\n  2935\t        task.verdict = verdict ?? task.verdict;\n  2936\t        task.sessionRequest = sessionRequest;\n  2937\t        task.sessionRef = sessionRef;\n  2938\t        task.sessionContext = sessionContext;\n  2939\t        task.lineage = lineage;\n  2940\t        task.capturedSha = capturedSha;\n  2941\t        task.integration = integration;\n  2942\t        task.retainedResultRef = retainedResultRef;\n  2943\t        task.publication = publication;\n  2944\t        task.review = review;\n  2945\t        task.worktree = sessionContext?.worktree ?? task.worktree;\n  2946\t        this._tasks.set(taskId, task);\n  2947\t        if (!this._taskOrder.includes(taskId)) this._taskOrder.push(taskId);\n  2948\t      }\n  2949\t\n  2950\t      this._workers.set(workerId, {\n  2951\t        id: workerId,\n  2952\t        vendor: vendorResolved,\n  2953\t        modelRequested,\n  2954\t        modelResolved,\n  2955\t        modelObserved,\n  2956\t        modelPolicy,\n  2957\t        modelMismatch,\n  2958\t        sessionRequest,\n  2959\t        sessionRef,\n  2960\t        sessionContext,\n  2961\t        lineage,\n  2962\t        taskId,\n  2963\t        worktree: sessionContext?.worktree ?? null,\n  2964\t        // A durable native reference is not a live transport. Even a terminal task that was\n  2965\t        // reusable before restart must remain uncontrollable until PS7 proves reattachment.\n  2966\t        status: (recoveryTerminalized || sessionRef) ? 'orphaned' : this._deriveWorkerStatus(terminalStatus),\n  2967\t        pendingApprovalId: null,\n  2968\t        pendingQuestionId: null,\n  2969\t        budgetUsed,\n  2970\t        budgetThresholdsFired,\n  2971\t        usageCumulative,\n  2972\t        watchdogActions: new Set(),\n  2973\t        recentFailedActions: [],\n  2974\t        watchdogGeneration: 0,\n  2975\t        watchdogTimer: null,\n  2976\t        runtimeScope: null,\n  2977\t        runtimeLease: null,\n  2978\t        spawnAbort: null,\n  2979\t        createdAt: new Date(0).toISOString(),\n  2980\t      });\n  2981\t\n  2982\t      // CI6: replayed auto identifiers reserve their numeric slots. A subsequent allocation may\n  2983\t      // never collide with or overwrite reconstructed state.\n  2984\t      const workerMatch = /^w-(\\d+)$/.exec(workerId);\n  2985\t      if (workerMatch) this._workerSeq = Math.max(this._workerSeq, Number(workerMatch[1]));\n  2986\t      const taskMatch = /^task-(\\d+)$/.exec(taskId ?? '');\n  2987\t      if (taskMatch) this._taskSeq = Math.max(this._taskSeq, Number(taskMatch[1]));\n  2988\t    }\n  2989\t  }\n  2990\t\n  2991\t  _deriveWorkerStatus(taskStatus) {\n  2992\t    switch (taskStatus) {\n  2993\t      case 'completed':\n  2994\t      case 'failed':\n  2995\t      case 'cancelled':\n  2996\t        return 'idle';\n  2997\t      case 'input_required':\n  2998\t        return 'blocked';\n  2999\t      default:\n  3000\t        return 'working';\n  3001\t    }\n  3002\t  }\n  3003\t\n  3004\t  _terminalizeUnattachedCoordinationTasks() {\n  3005\t    if (!this._coordination) return;\n  3006\t    for (const durable of this._coordination.snapshot().tasks) {\n  3007\t      if (!['working', 'input_required'].includes(durable.status)) continue;\n  3008\t      const workerId = durable.assignee ?? durable.reservedWorkerId;\n  3009\t      const events = workerId ? this._log.read(workerId) : [];\n  3010\t      if (events.some((event) => event.kind === 'lifecycle.spawned')) continue;\n  3011\t      const recorded = this._coordRecord('recovery.claimed_without_spawn', { taskId: durable.id, workerId }, `driver.recovery:${durable.id}:claimed_without_spawn`);\n  3012\t      const transitioned = this._coordination.transitionTask(durable.id, 'failed', durable.version, {\n  3013\t        actor: 'policy', key: `task.failed:${durable.id}:claimed_without_spawn`,\n  3014\t      }, { coordinationSeq: recorded?.seq ?? null, reason: 'claimed_without_operational_spawn' });\n  3015\t      const task = this._tasks.get(durable.id);\n  3016\t      if (task) {\n  3017\t        task.status = 'failed'; task.coordinationVersion = transitioned.task.version;\n  3018\t        this._expireScratchClaims(this._workers.get(workerId), task, 'claimed_without_spawn');\n  3019\t      }\n  3020\t      const handle = workerId ? this._workers.get(workerId) : null;\n  3021\t      if (handle) handle.status = 'exited';\n  3022\t    }\n  3023\t  }\n  3024\t}\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '880,1010p;1030,1145p;1898,2015p;2838,3025p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":22,"ts":"2026-07-11T20:06:39.256Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_zaMpccq9IL5dqSMunUBEBTCt","command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '1,340p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"7961","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '1,340p'","name":"phase11-acceptance-integration.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs"}],"aggregatedOutput":"     1\timport { test } from 'node:test';\n     2\timport assert from 'node:assert/strict';\n     3\timport { execFileSync } from 'node:child_process';\n     4\timport { existsSync, mkdirSync, mkdtempSync, writeFileSync } from 'node:fs';\n     5\timport { tmpdir } from 'node:os';\n     6\timport { dirname, join } from 'node:path';\n     7\t\n     8\timport { createDriver, IntegrationError, PublicationError } from '../src/index.mjs';\n     9\timport { MockAdapter } from '../src/adapter.mjs';\n    10\t\n    11\tfunction git(args, cwd) { return execFileSync('git', args, { cwd, encoding: 'utf8' }).trim(); }\n    12\tfunction repo() {\n    13\t  const root = mkdtempSync(join(tmpdir(), 'baton-acceptance-'));\n    14\t  git(['init', '-q'], root);\n    15\t  git(['config', 'user.email', 'baton-test@example.com'], root);\n    16\t  git(['config', 'user.name', 'Baton Test'], root);\n    17\t  return root;\n    18\t}\n    19\tfunction commitBase(root, files = {}) {\n    20\t  for (const [path, content] of Object.entries(files)) {\n    21\t    const full = join(root, path);\n    22\t    mkdirSync(dirname(full), { recursive: true });\n    23\t    writeFileSync(full, content);\n    24\t  }\n    25\t  git(['add', '-A'], root);\n    26\t  git(['commit', '--allow-empty', '-q', '-m', 'base'], root);\n    27\t}\n    28\tfunction brief(verification) {\n    29\t  return {\n    30\t    goal: 'make the pinned check newly pass', constraints: [], pathScope: ['src/**'], definitionOfDone: 'check is red then green',\n    31\t    verification, budget: { tokens: 100000, usd: 5, wallMin: 5 },\n    32\t  };\n    33\t}\n    34\tasync function until(fn, timeoutMs = 5000) {\n    35\t  const deadline = Date.now() + timeoutMs;\n    36\t  while (Date.now() < deadline) {\n    37\t    const value = await fn();\n    38\t    if (value) return value;\n    39\t    await new Promise((resolve) => setTimeout(resolve, 10));\n    40\t  }\n    41\t  throw new Error('condition not met');\n    42\t}\n    43\t\n    44\tasync function completedTask(root, taskId = 'integrate-me') {\n    45\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/integrated.txt', content: 'accepted\\n' }] } });\n    46\t  const driver = createDriver({\n    47\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-log-')), adapters: { mock: adapter },\n    48\t    watchdog: { stallMs: 0 },\n    49\t  });\n    50\t  const handle = await driver.coordinator.spawn('mock', brief({ command: 'test -f src/integrated.txt', expectExit: 0 }), { taskId });\n    51\t  await until(async () => (await driver.coordinator.result(handle.id)).ready);\n    52\t  assert.equal((await driver.coordinator.result(handle.id)).status, 'completed');\n    53\t  return { ...driver, handle };\n    54\t}\n    55\t\n    56\tfunction familyAdapter(family, scenario) {\n    57\t  const adapter = new MockAdapter({ scenario });\n    58\t  const baseCard = adapter.card.bind(adapter);\n    59\t  adapter.card = () => ({\n    60\t    ...baseCard(),\n    61\t    modelSelection: {\n    62\t      mode: 'exact', configuredDefault: null, available: null, family,\n    63\t      acceptedPrefixes: [], acceptedAliases: [], reasoningEffort: null, serviceTier: null,\n    64\t    },\n    65\t  });\n    66\t  return adapter;\n    67\t}\n    68\t\n    69\tasync function integratedPublicationTask({ now, approvalTimeoutMs = 1000 } = {}) {\n    70\t  const root = repo();\n    71\t  commitBase(root);\n    72\t  const calls = [];\n    73\t  const logDir = mkdtempSync(join(tmpdir(), 'baton-ac6-log-'));\n    74\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/publish.txt', content: 'publish me\\n' }] } });\n    75\t  const driver = createDriver({\n    76\t    repoRoot: root, logDir, adapters: { mock: adapter }, now, approvalTimeoutMs,\n    77\t    publisher: async (target) => { calls.push(target); return { transport: 'test-publisher' }; },\n    78\t    watchdog: { stallMs: 0 },\n    79\t  });\n    80\t  const handle = await driver.coordinator.spawn('mock', brief({ command: 'test -f src/publish.txt', expectExit: 0 }), { taskId: 'publish-task' });\n    81\t  await until(async () => (await driver.coordinator.result(handle.id)).ready);\n    82\t  await driver.coordinator.integrate(handle.id);\n    83\t  return { ...driver, root, logDir, handle, calls };\n    84\t}\n    85\t\n    86\ttest('AC1: createDriver requireRedGreen proves base red and result green', async () => {\n    87\t  const root = repo();\n    88\t  commitBase(root);\n    89\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/new.txt', content: 'ok\\n' }] } });\n    90\t  const { coordinator, log } = createDriver({\n    91\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac1-log-')), adapters: { mock: adapter },\n    92\t    requireRedGreen: true, watchdog: { stallMs: 0 },\n    93\t  });\n    94\t  const h = await coordinator.spawn('mock', brief({ command: 'test -f src/new.txt', expectExit: 0 }), { taskId: 'red-green' });\n    95\t  await until(async () => (await coordinator.result(h.id)).ready);\n    96\t  const result = await coordinator.result(h.id);\n    97\t  assert.equal(result.status, 'completed');\n    98\t  assert.equal(result.verdict.redGreen, true);\n    99\t  assert.notEqual(result.verdict.baseExit, 0);\n   100\t  assert.equal(log.read(h.id).find((event) => event.kind === 'verify.reverified')?.payload?.accept, true);\n   101\t});\n   102\t\n   103\ttest('AC2: createDriver requireCoverage computes changed lines and accepts covered change', async () => {\n   104\t  const root = repo();\n   105\t  commitBase(root, {\n   106\t    'coverage.mjs': 'console.log(JSON.stringify({files:{\"src/x.js\":{executedLines:[1]}}}))\\n',\n   107\t  });\n   108\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\n   109\t  const { coordinator } = createDriver({\n   110\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2-log-')), adapters: { mock: adapter },\n   111\t    requireCoverage: true, watchdog: { stallMs: 0 },\n   112\t  });\n   113\t  const h = await coordinator.spawn('mock', brief({\n   114\t    command: 'test -f src/x.js', expectExit: 0, coverageCommand: 'node coverage.mjs',\n   115\t  }), { taskId: 'covered-change' });\n   116\t  await until(async () => (await coordinator.result(h.id)).ready);\n   117\t  const result = await coordinator.result(h.id);\n   118\t  assert.equal(result.status, 'completed');\n   119\t  assert.equal(result.verdict.coverageOfChange, true);\n   120\t  assert.deepEqual(result.verdict.uncoveredChangedLines, []);\n   121\t});\n   122\t\n   123\ttest('AC2: requireCoverage rejects a passing but uncovered change', async () => {\n   124\t  const root = repo();\n   125\t  commitBase(root, {\n   126\t    'coverage.mjs': 'console.log(JSON.stringify({files:{\"src/x.js\":{executedLines:[]}}}))\\n',\n   127\t  });\n   128\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\n   129\t  const { coordinator } = createDriver({\n   130\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2b-log-')), adapters: { mock: adapter },\n   131\t    requireCoverage: true, watchdog: { stallMs: 0 },\n   132\t  });\n   133\t  const h = await coordinator.spawn('mock', brief({\n   134\t    command: 'test -f src/x.js', expectExit: 0, coverageCommand: 'node coverage.mjs',\n   135\t  }), { taskId: 'uncovered-change' });\n   136\t  await until(async () => (await coordinator.result(h.id)).ready);\n   137\t  const result = await coordinator.result(h.id);\n   138\t  assert.equal(result.status, 'failed');\n   139\t  assert.equal(result.verdict.coverageOfChange, false);\n   140\t  assert.deepEqual(result.verdict.uncoveredChangedLines, ['src/x.js:1']);\n   141\t});\n   142\t\n   143\ttest('AC3: required mutation accepts a nonzero all-killed population', async () => {\n   144\t  const root = repo();\n   145\t  commitBase(root, { 'mutation.mjs': 'console.log(JSON.stringify({killed:2,total:2,survived:[]}))\\n' });\n   146\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\n   147\t  const { coordinator } = createDriver({\n   148\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3-log-')), adapters: { mock: adapter },\n   149\t    requireMutation: true, watchdog: { stallMs: 0 },\n   150\t  });\n   151\t  const h = await coordinator.spawn('mock', brief({\n   152\t    command: 'test -f src/x.js', expectExit: 0, mutationCommand: 'node mutation.mjs',\n   153\t  }), { taskId: 'mutation-strong' });\n   154\t  await until(async () => (await coordinator.result(h.id)).ready);\n   155\t  const result = await coordinator.result(h.id);\n   156\t  assert.equal(result.status, 'completed');\n   157\t  assert.equal(result.verdict.mutationPassed, true);\n   158\t  assert.equal(result.verdict.mutationStrength, 1);\n   159\t});\n   160\t\n   161\ttest('AC3: required mutation rejects survivors and records their identities', async () => {\n   162\t  const root = repo();\n   163\t  commitBase(root, { 'mutation.mjs': 'console.log(JSON.stringify({killed:1,total:2,survived:[\"m2\"]}))\\n' });\n   164\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\n   165\t  const { coordinator } = createDriver({\n   166\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3b-log-')), adapters: { mock: adapter },\n   167\t    requireMutation: true, watchdog: { stallMs: 0 },\n   168\t  });\n   169\t  const h = await coordinator.spawn('mock', brief({\n   170\t    command: 'test -f src/x.js', expectExit: 0, mutationCommand: 'node mutation.mjs',\n   171\t  }), { taskId: 'mutation-weak' });\n   172\t  await until(async () => (await coordinator.result(h.id)).ready);\n   173\t  const result = await coordinator.result(h.id);\n   174\t  assert.equal(result.status, 'failed');\n   175\t  assert.equal(result.verdict.mutationPassed, false);\n   176\t  assert.deepEqual(result.verdict.survivedMutants, ['m2']);\n   177\t});\n   178\t\n   179\ttest('AC4: independent oracle receives immutable spec/git evidence and unlocks required integration', async () => {\n   180\t  const root = repo();\n   181\t  commitBase(root, { 'README.md': 'base\\n' });\n   182\t  const implementer = familyAdapter('family-a', {\n   183\t    outcome: 'completed', summary: 'untrusted implementer prose',\n   184\t    edits: [{ path: 'src/reviewed.txt', content: 'accepted\\n' }],\n   185\t  });\n   186\t  const oracle = familyAdapter('family-b', { outcome: 'completed', summary: 'independent oracle prose' });\n   187\t  const { coordinator, log } = createDriver({\n   188\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac4-log-')),\n   189\t    adapters: { implementer, oracle }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\n   190\t  });\n   191\t  const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-parent' });\n   192\t  await until(async () => (await coordinator.result(parent.id)).ready);\n   193\t\n   194\t  await assert.rejects(\n   195\t    coordinator.integrate(parent.id),\n   196\t    (error) => error instanceof IntegrationError && error.code === 'independent_oracle_required',\n   197\t  );\n   198\t  const reviewer = await coordinator.spawnReview(parent.id, 'oracle', {\n   199\t    taskId: 'oracle-child', kind: 'oracle', verification: { command: 'true', expectExit: 0 },\n   200\t  });\n   201\t  await until(async () => (await coordinator.result(reviewer.id)).ready);\n   202\t  const reviewResult = await coordinator.result(reviewer.id);\n   203\t  assert.equal(reviewResult.status, 'completed');\n   204\t  assert.equal(reviewResult.review.independent, true);\n   205\t  assert.equal(reviewResult.review.implementerFamily, 'family-a');\n   206\t  assert.equal(reviewResult.review.reviewerFamily, 'family-b');\n   207\t  const spawned = log.read(reviewer.id).find((entry) => entry.kind === 'lifecycle.spawned');\n   208\t  assert.equal(spawned.payload.brief.reviewTarget.parentTaskId, 'review-parent');\n   209\t  assert.equal(spawned.payload.brief.reviewTarget.resultSha, reviewResult.review.resultSha);\n   210\t  assert.equal(spawned.payload.brief.reviewTarget.spec.goal, 'make the pinned check newly pass');\n   211\t  assert.equal(JSON.stringify(spawned.payload.brief).includes('untrusted implementer prose'), false);\n   212\t  assert.equal(log.read(parent.id).some((entry) => entry.kind === 'review.completed' && entry.payload.independent === true), true);\n   213\t\n   214\t  const integrated = await coordinator.integrate(parent.id);\n   215\t  assert.equal(integrated.ok, true);\n   216\t  assert.equal(existsSync(join(root, 'src/reviewed.txt')), true);\n   217\t});\n   218\t\n   219\ttest('AC4: visible same-family fallback cannot satisfy a required independent oracle', async () => {\n   220\t  const root = repo();\n   221\t  commitBase(root);\n   222\t  const implementer = familyAdapter('shared-family', { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'ok\\n' }] });\n   223\t  const fallback = familyAdapter('shared-family', { outcome: 'completed' });\n   224\t  const { coordinator, log } = createDriver({\n   225\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac4-fallback-log-')),\n   226\t    adapters: { implementer, fallback }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\n   227\t  });\n   228\t  const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/x.js', expectExit: 0 }), { taskId: 'same-family-parent' });\n   229\t  await until(async () => (await coordinator.result(parent.id)).ready);\n   230\t  const reviewer = await coordinator.spawnReview(parent.id, 'fallback', {\n   231\t    taskId: 'same-family-oracle', verification: { command: 'true', expectExit: 0 },\n   232\t  });\n   233\t  await until(async () => (await coordinator.result(reviewer.id)).ready);\n   234\t  assert.equal((await coordinator.result(reviewer.id)).review.independent, false);\n   235\t  assert.equal(log.read(parent.id).some((entry) => entry.kind === 'review.completed' && entry.payload.independent === false), true);\n   236\t  await assert.rejects(\n   237\t    coordinator.integrate(parent.id),\n   238\t    (error) => error instanceof IntegrationError && error.code === 'independent_oracle_required',\n   239\t  );\n   240\t});\n   241\t\n   242\ttest('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\n   243\t  const root = repo();\n   244\t  commitBase(root);\n   245\t  const implementer = familyAdapter('family-a', { outcome: 'completed', edits: [{ path: 'src/reviewed.txt', content: 'review me\\n' }] });\n   246\t  const reviewer = familyAdapter('family-b', { outcome: 'completed' });\n   247\t  let reviewerSpawns = 0;\n   248\t  const rawSpawn = reviewer.spawn.bind(reviewer);\n   249\t  reviewer.spawn = async (...args) => { reviewerSpawns += 1; return rawSpawn(...args); };\n   250\t  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\n   251\t  const parent = await driver.coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-fault-parent' });\n   252\t  await until(async () => (await driver.coordinator.result(parent.id)).ready);\n   253\t  const rawAppend = driver.coordination._appendFile;\n   254\t  driver.coordination._appendFile = (file, body, encoding) => {\n   255\t    if (body.includes('\"task.created\"') && body.includes('\"review\"')) throw new Error('review task disk full');\n   256\t    return rawAppend(file, body, encoding);\n   257\t  };\n   258\t  await assert.rejects(driver.coordinator.spawnReview(parent.id, 'reviewer', {\n   259\t    kind: 'review', taskId: 'review-fault-child', verification: { command: 'true', expectExit: 0 },\n   260\t  }), (error) => error.code === 'coordination_write_unavailable');\n   261\t  assert.equal(reviewerSpawns, 0);\n   262\t  assert.deepEqual(driver.coordination.snapshot().tasks.map((task) => [task.id, task.status]), [['review-fault-parent', 'completed']]);\n   263\t});\n   264\t\n   265\ttest('AC5: ff-only integration reaps the worker/worktree/branch and records exact SHAs', async () => {\n   266\t  const root = repo();\n   267\t  commitBase(root, { 'README.md': 'base\\n' });\n   268\t  const beforeSha = git(['rev-parse', 'HEAD'], root);\n   269\t  const { coordinator, coordination, log, handle } = await completedTask(root);\n   270\t  const taskWorktree = coordinator.list().find((worker) => worker.id === handle.id)?.worktree;\n   271\t  assert.equal(typeof taskWorktree, 'string');\n   272\t\n   273\t  const response = await coordinator.integrate(handle.id, { strategy: 'ff-only', actor: 'test-orchestrator' });\n   274\t  assert.equal(response.ok, true);\n   275\t  assert.equal(response.result, 'integrated');\n   276\t  assert.equal(response.integration.beforeSha, beforeSha);\n   277\t  assert.equal(response.integration.afterSha, response.integration.resultSha);\n   278\t  assert.equal(git(['rev-parse', 'HEAD'], root), response.integration.resultSha);\n   279\t  assert.equal(existsSync(join(root, 'src/integrated.txt')), true);\n   280\t  assert.equal(coordinator.list().find((worker) => worker.id === handle.id)?.status, 'dead');\n   281\t  assert.equal(existsSync(taskWorktree), false);\n   282\t  assert.equal(git(['branch', '--list', 'baton/integrate-me'], root), '');\n   283\t  assert.equal(git(['for-each-ref', '--format=%(refname)', `refs/baton/results/${response.integration.resultSha}`], root), '');\n   284\t  assert.deepEqual((await coordinator.result(handle.id)).integration, response.integration);\n   285\t  const event = log.read(handle.id).find((entry) => entry.kind === 'integration.completed');\n   286\t  assert.equal(event.actor, 'test-orchestrator');\n   287\t  assert.equal(event.payload.afterSha, response.integration.afterSha);\n   288\t  assert.equal(coordination.snapshot().artifacts.some((artifact) => artifact.mediaType === 'application/vnd.baton.integration+json'), true);\n   289\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.completed'), true);\n   290\t  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:integrate:')), true);\n   291\t  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'integration'), true);\n   292\t});\n   293\t\n   294\ttest('AC5: a non-fast-forward main refuses without rewriting either tip', async () => {\n   295\t  const root = repo();\n   296\t  commitBase(root, { 'README.md': 'base\\n' });\n   297\t  const { coordinator, coordination, log, handle } = await completedTask(root, 'diverged-task');\n   298\t  writeFileSync(join(root, 'main-only.txt'), 'main advanced\\n');\n   299\t  git(['add', 'main-only.txt'], root);\n   300\t  git(['commit', '-q', '-m', 'advance main independently'], root);\n   301\t  const mainBefore = git(['rev-parse', 'HEAD'], root);\n   302\t  const taskSha = (await coordinator.result(handle.id)).integration?.resultSha\n   303\t    ?? log.read(handle.id).find((entry) => entry.kind === 'verify.reverified')?.payload?.capture?.sha;\n   304\t\n   305\t  await assert.rejects(\n   306\t    coordinator.integrate(handle.id),\n   307\t    (error) => error instanceof IntegrationError && error.code === 'non_fast_forward_or_dirty',\n   308\t  );\n   309\t  assert.equal(git(['rev-parse', 'HEAD'], root), mainBefore);\n   310\t  assert.notEqual(mainBefore, taskSha);\n   311\t  assert.equal(git(['show', '-s', '--format=%s', 'HEAD'], root), 'advance main independently');\n   312\t  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   313\t  const refusedResult = await coordinator.result(handle.id);\n   314\t  assert.equal(refusedResult.integration, null);\n   315\t  assert.equal(refusedResult.retainedResultRef, `refs/baton/results/${taskSha}`);\n   316\t  assert.equal(git(['show-ref', '--verify', refusedResult.retainedResultRef], root).split(' ')[0], taskSha);\n   317\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.refused'), true);\n   318\t});\n   319\t\n   320\ttest('AC5: a dirty main refuses without touching the index or working tree', async () => {\n   321\t  const root = repo();\n   322\t  commitBase(root, { 'README.md': 'base\\n' });\n   323\t  const { coordinator, log, handle } = await completedTask(root, 'dirty-main-task');\n   324\t  writeFileSync(join(root, 'README.md'), 'uncommitted user edit\\n');\n   325\t  const headBefore = git(['rev-parse', 'HEAD'], root);\n   326\t  const statusBefore = git(['status', '--porcelain=v1'], root);\n   327\t\n   328\t  await assert.rejects(coordinator.integrate(handle.id), IntegrationError);\n   329\t  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n   330\t  assert.equal(git(['status', '--porcelain=v1'], root), statusBefore);\n   331\t  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   332\t  const retained = (await coordinator.result(handle.id)).retainedResultRef;\n   333\t  assert.equal(git(['show-ref', '--verify', retained], root).endsWith(` ${retained}`), true);\n   334\t});\n   335\t\n   336\ttest('AC5: integration refuses an unaccepted captured result', async () => {\n   337\t  const root = repo();\n   338\t  commitBase(root);\n   339\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\n   340\t  const { coordinator } = createDriver({\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '1,340p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":23,"ts":"2026-07-11T20:06:39.272Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":37346,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":84974,"inputTokens":82691,"cachedInputTokens":49664,"outputTokens":2283,"reasoningOutputTokens":1131},"last":{"totalTokens":37346,"inputTokens":36643,"cachedInputTokens":22912,"outputTokens":703,"reasoningOutputTokens":366},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":84974,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":24,"ts":"2026-07-11T20:06:39.274Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":25,"ts":"2026-07-11T20:06:39.274Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_eHlrB1hb8v71u7lsXRnayvsF","command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '336,520p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"14654","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '336,520p'","name":"phase11-acceptance-integration.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs"}],"aggregatedOutput":"   336\ttest('AC5: integration refuses an unaccepted captured result', async () => {\n   337\t  const root = repo();\n   338\t  commitBase(root);\n   339\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\n   340\t  const { coordinator } = createDriver({\n   341\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-reject-log-')), adapters: { mock: adapter },\n   342\t    watchdog: { stallMs: 0 },\n   343\t  });\n   344\t  const handle = await coordinator.spawn('mock', brief({ command: 'false', expectExit: 0 }), { taskId: 'rejected-result' });\n   345\t  await until(async () => (await coordinator.result(handle.id)).ready);\n   346\t  assert.equal((await coordinator.result(handle.id)).status, 'failed');\n   347\t  await assert.rejects(\n   348\t    coordinator.integrate(handle.id),\n   349\t    (error) => error instanceof IntegrationError && error.code === 'result_not_accepted',\n   350\t  );\n   351\t});\n   352\t\n   353\ttest('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\n   354\t  const root = repo();\n   355\t  commitBase(root);\n   356\t  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\n   357\t  const headBefore = git(['rev-parse', 'HEAD'], root);\n   358\t  const workerBefore = coordinator._workers.get(handle.id);\n   359\t  const rawAppend = coordination._appendFile;\n   360\t  coordination._appendFile = (file, body, encoding) => {\n   361\t    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\n   362\t    return rawAppend(file, body, encoding);\n   363\t  };\n   364\t  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\n   365\t  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n   366\t  assert.equal(workerBefore.status, 'idle');\n   367\t  assert.equal(existsSync(workerBefore.worktree), true);\n   368\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\n   369\t});\n   370\t\n   371\ttest('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\n   372\t  const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\n   373\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   374\t  assert.equal(calls.length, 0);\n   375\t  assert.deepEqual(requested.target, {\n   376\t    remote: 'origin', ref: 'refs/heads/main', sha: (await coordinator.result(handle.id)).integration.afterSha,\n   377\t  });\n   378\t  const event = log.read(handle.id).at(-1);\n   379\t  assert.equal(event.kind, 'publication.requested');\n   380\t  assert.equal(JSON.stringify(event).includes('credential'), false);\n   381\t\n   382\t  const [a, b] = await Promise.all([\n   383\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   384\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'other-user'),\n   385\t  ]);\n   386\t  assert.equal(calls.length, 1);\n   387\t  assert.deepEqual(calls[0], requested.target);\n   388\t  assert.deepEqual(new Set([a.result, b.result]), new Set(['published', 'already_resolved']));\n   389\t  assert.equal((await coordinator.result(handle.id)).publication.sha, requested.target.sha);\n   390\t  assert.equal(log.read(handle.id).filter((entry) => entry.kind === 'publication.completed').length, 1);\n   391\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'publication.completed'), true);\n   392\t  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:publish:')), true);\n   393\t  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'publication'), true);\n   394\t});\n   395\t\n   396\ttest('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\n   397\t  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n   398\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   399\t  const rawAppend = coordination._appendFile;\n   400\t  coordination._appendFile = (file, body, encoding) => {\n   401\t    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\n   402\t    return rawAppend(file, body, encoding);\n   403\t  };\n   404\t  await assert.rejects(\n   405\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   406\t    (error) => error.code === 'coordination_write_unavailable',\n   407\t  );\n   408\t  assert.equal(calls.length, 0);\n   409\t  assert.equal(coordinator._pending.get(requested.requestId).state, 'pending');\n   410\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n   411\t});\n   412\t\n   413\ttest('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\n   414\t  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n   415\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   416\t  const rawAppend = coordination._appendFile;\n   417\t  coordination._appendFile = (file, body, encoding) => {\n   418\t    if (body.includes('\"kind\":\"publication.completed\"')) throw new Error('publication completion disk full');\n   419\t    return rawAppend(file, body, encoding);\n   420\t  };\n   421\t  await assert.rejects(\n   422\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   423\t    (error) => error.code === 'coordination_write_unavailable',\n   424\t  );\n   425\t  assert.equal(calls.length, 1);\n   426\t  assert.equal(coordinator._pending.get(requested.requestId).state, 'resolved');\n   427\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\n   428\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n   429\t});\n   430\t\n   431\ttest('AC6: deny and timeout are fail-closed and never call the publisher', async () => {\n   432\t  let now = 1000;\n   433\t  const denied = await integratedPublicationTask({ now: () => now, approvalTimeoutMs: 10 });\n   434\t  const explicit = denied.coordinator.requestPublication(denied.handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   435\t  const deniedResult = await denied.coordinator.respond(explicit.requestId, { decision: 'deny', fence: explicit.fence }, 'test-user');\n   436\t  assert.equal(deniedResult.result, 'denied');\n   437\t  assert.equal(denied.calls.length, 0);\n   438\t\n   439\t  const timed = timedPublicationRequest(denied.coordinator, denied.handle.id);\n   440\t  now += 11;\n   441\t  denied.coordinator.list();\n   442\t  await until(() => denied.log.read(denied.handle.id).some((entry) => entry.kind === 'publication.denied' && entry.payload.requestId === timed.requestId));\n   443\t  assert.equal(denied.calls.length, 0);\n   444\t});\n   445\t\n   446\tfunction timedPublicationRequest(coordinator, workerId) {\n   447\t  return coordinator.requestPublication(workerId, { remote: 'origin', ref: 'refs/heads/main' });\n   448\t}\n   449\t\n   450\ttest('AC6: a newer authority fence invalidates an older publication approval', async () => {\n   451\t  const { coordinator, handle, calls } = await integratedPublicationTask();\n   452\t  const oldRequest = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   453\t  const currentRequest = coordinator.requestPublication(handle.id, { remote: 'upstream', ref: 'refs/heads/reviewed' });\n   454\t  const stale = await coordinator.respond(oldRequest.requestId, { decision: 'allow', fence: oldRequest.fence }, 'test-user');\n   455\t  assert.equal(stale.result, 'stale_fence');\n   456\t  assert.equal(calls.length, 0);\n   457\t  await coordinator.respond(currentRequest.requestId, { decision: 'deny', fence: currentRequest.fence }, 'test-user');\n   458\t  assert.equal(calls.length, 0);\n   459\t});\n   460\t\n   461\ttest('AC6: restart drops a pending approval and cannot publish it by replay', async () => {\n   462\t  const first = await integratedPublicationTask();\n   463\t  const request = first.coordinator.requestPublication(first.handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   464\t  const replayCalls = [];\n   465\t  const replay = createDriver({\n   466\t    repoRoot: first.root, logDir: first.logDir,\n   467\t    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\n   468\t    publisher: async (target) => { replayCalls.push(target); }, watchdog: { stallMs: 0 },\n   469\t  });\n   470\t  const response = await replay.coordinator.respond(request.requestId, { decision: 'allow', fence: request.fence }, 'test-user');\n   471\t  assert.equal(response.result, 'not_found');\n   472\t  assert.equal(replayCalls.length, 0);\n   473\t});\n   474\t\n   475\ttest('AC6: credential-bearing remotes and mismatched SHAs are rejected before logging', async () => {\n   476\t  const { coordinator, log, handle } = await integratedPublicationTask();\n   477\t  const before = log.read(handle.id).length;\n   478\t  assert.throws(\n   479\t    () => coordinator.requestPublication(handle.id, { remote: 'https://token@example.com/repo.git', ref: 'refs/heads/main' }),\n   480\t    (error) => error instanceof PublicationError && error.code === 'invalid_remote',\n   481\t  );\n   482\t  assert.throws(\n   483\t    () => coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main', sha: 'deadbeef' }),\n   484\t    (error) => error instanceof PublicationError && error.code === 'sha_mismatch',\n   485\t  );\n   486\t  assert.equal(log.read(handle.id).length, before);\n   487\t});\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '336,520p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":26,"ts":"2026-07-11T20:06:42.770Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":54037,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":139011,"inputTokens":136587,"cachedInputTokens":85888,"outputTokens":2424,"reasoningOutputTokens":1181},"last":{"totalTokens":54037,"inputTokens":53896,"cachedInputTokens":36224,"outputTokens":141,"reasoningOutputTokens":50},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":139011,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":27,"ts":"2026-07-11T20:06:42.772Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":28,"ts":"2026-07-11T20:06:42.772Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"I have one likely seam: the publication path records authorization before invoking the publisher, but I haven’t yet seen any restart-time closure for the case where the publisher succeeds and the completion append fails. I’m checking whether replay or tests cover that ambiguity at all."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":29,"ts":"2026-07-11T20:06:59.354Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_EdD8sCmGta8o2KdAKrcoG65Z","command":"/bin/zsh -lc \"rg -n \\\"publication\\\\.(requested|authorized|completed|denied|refused)|recovery_terminalized|claimed_without_spawn|integration\\\\.requested|review\\\\.requested\\\" impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs impl/VALIDATION.md docs/handoff/evidence/phase11-coordination-foundation-2026-07-11.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"44557","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n \"publication\\\\.(requested|authorized|completed|denied|refused)|recovery_terminalized|claimed_without_spawn|integration\\\\.requested|review\\\\.requested\" impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs impl/VALIDATION.md docs/handoff/evidence/phase11-coordination-foundation-2026-07-11.md","query":"publication\\.(requested|authorized|completed|denied|refused)|recovery_terminalized|claimed_without_spawn|integration\\.requested|review\\.requested","path":"coordinator.mjs"}],"aggregatedOutput":"impl/test/phase11-coordination-store.test.mjs:245:  assert.equal(replay.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'recovery.claimed_without_spawn'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:361:    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:368:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:379:  assert.equal(event.kind, 'publication.requested');\nimpl/test/phase11-acceptance-integration.test.mjs:390:  assert.equal(log.read(handle.id).filter((entry) => entry.kind === 'publication.completed').length, 1);\nimpl/test/phase11-acceptance-integration.test.mjs:391:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'publication.completed'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:401:    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:410:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:418:    if (body.includes('\"kind\":\"publication.completed\"')) throw new Error('publication completion disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:427:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:428:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:442:  await until(() => denied.log.read(denied.handle.id).some((entry) => entry.kind === 'publication.denied' && entry.payload.requestId === timed.requestId));\nimpl/src/coordinator.mjs:846:      kind: 'review.requested', actor: opts.actor ?? 'orchestrator',\nimpl/src/coordinator.mjs:1034:    this._coordRecord('integration.requested', {\nimpl/src/coordinator.mjs:1037:    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:1130:      kind: 'publication.requested', actor,\nimpl/src/coordinator.mjs:1134:    this._coordRecord('publication.requested', {\nimpl/src/coordinator.mjs:1136:    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\nimpl/src/coordinator.mjs:1934:            kind: 'publication.refused', actor: 'policy',\nimpl/src/coordinator.mjs:1938:          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\nimpl/src/coordinator.mjs:1949:          kind: 'publication.denied', actor,\nimpl/src/coordinator.mjs:1953:        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\nimpl/src/coordinator.mjs:1969:          kind: 'publication.authorized', actor,\nimpl/src/coordinator.mjs:1973:        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\nimpl/src/coordinator.mjs:1996:          kind: 'publication.completed', actor, payload: task.publication,\nimpl/src/coordinator.mjs:2005:        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\nimpl/src/coordinator.mjs:2813:          case 'publication.completed':\nimpl/src/coordinator.mjs:2818:          case 'control.recovery_terminalized':\nimpl/src/coordinator.mjs:2860:          kind: 'control.recovery_terminalized', actor: 'policy',\nimpl/src/coordinator.mjs:2881:          kind: 'control.recovery_terminalized',\nimpl/src/coordinator.mjs:3011:      const recorded = this._coordRecord('recovery.claimed_without_spawn', { taskId: durable.id, workerId }, `driver.recovery:${durable.id}:claimed_without_spawn`);\nimpl/src/coordinator.mjs:3013:        actor: 'policy', key: `task.failed:${durable.id}:claimed_without_spawn`,\nimpl/src/coordinator.mjs:3018:        this._expireScratchClaims(this._workers.get(workerId), task, 'claimed_without_spawn');\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"rg -n \\\"publication\\\\.(requested|authorized|completed|denied|refused)|recovery_terminalized|claimed_without_spawn|integration\\\\.requested|review\\\\.requested\\\" impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-persistent-sessions.test.mjs impl/test/phase11-coordination-store.test.mjs impl/VALIDATION.md docs/handoff/evidence/phase11-coordination-foundation-2026-07-11.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":30,"ts":"2026-07-11T20:07:02.320Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":57729,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":196740,"inputTokens":193278,"cachedInputTokens":139520,"outputTokens":3462,"reasoningOutputTokens":1992},"last":{"totalTokens":57729,"inputTokens":56691,"cachedInputTokens":53632,"outputTokens":1038,"reasoningOutputTokens":811},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":196740,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":31,"ts":"2026-07-11T20:07:02.322Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":0.5,"hardStop":false,"action":"notify","used":{"tokens":196740,"usd":0},"limits":{"tokens":350000,"usd":3},"ratio":0.5621142857142857,"dimensions":{"tokens":0.5621142857142857,"usd":0}},"seq":32,"ts":"2026-07-11T20:07:02.322Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":33,"ts":"2026-07-11T20:07:02.322Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"control.steer","actor":"orchestrator","payload":{"message":"Budget steer: stop all further repository exploration now. Write reviews/dogfood/codex-ck9-crash-window-review.md immediately from the evidence already collected, with the four exact required headings, then run only the pinned verification command."},"seq":34,"ts":"2026-07-11T20:07:02.422Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_VeUyX7zbrOLeKJNSkRIa1kVf","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '2790,2825p;830,850p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"50606","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '2790,2825p;830,850p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"   830\t        parentTaskId: parent.id,\n   831\t        baseSha: review.baseSha,\n   832\t        resultSha: review.resultSha,\n   833\t        diffRange: review.baseSha ? `${review.baseSha}..${review.resultSha}` : null,\n   834\t      },\n   835\t    };\n   836\t    const child = await this.spawn(vendor, reviewBrief, {\n   837\t      taskId: opts.taskId,\n   838\t      model: opts.model,\n   839\t      modelPolicy: opts.modelPolicy,\n   840\t      taskType: kind,\n   841\t      refines: parent.id,\n   842\t      review,\n   843\t    });\n   844\t    this._log.append({\n   845\t      worker: workerId, harness: this._harnessOf(parentHandle.vendor), turnEpoch: this._safeTurnEpoch(parentHandle),\n   846\t      kind: 'review.requested', actor: opts.actor ?? 'orchestrator',\n   847\t      payload: { ...review, reviewerWorkerId: child.id, reviewerModelRequested: opts.model ?? null },\n   848\t    });\n   849\t    return child;\n   850\t  }\n  2790\t          case 'lifecycle.turn_started':\n  2791\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'working';\n  2792\t            break;\n  2793\t          case 'lifecycle.turn_completed':\n  2794\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2795\t              lastResult = e.payload;\n  2796\t              terminalStatus = 'verifying';\n  2797\t            }\n  2798\t            break;\n  2799\t          case 'verify.reverified':\n  2800\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2801\t              verdict = e.payload?.verdict ?? null;\n  2802\t              terminalStatus = e.payload?.accept ? 'completed' : 'failed';\n  2803\t              capturedSha = e.payload?.capture?.sha ?? capturedSha;\n  2804\t            }\n  2805\t            break;\n  2806\t          case 'integration.completed':\n  2807\t            integration = e.payload ?? integration;\n  2808\t            retainedResultRef = null;\n  2809\t            break;\n  2810\t          case 'integration.refused':\n  2811\t            retainedResultRef = e.payload?.retainedResultRef ?? retainedResultRef;\n  2812\t            break;\n  2813\t          case 'publication.completed':\n  2814\t            publication = e.payload ?? publication;\n  2815\t            break;\n  2816\t          case 'lifecycle.crashed':\n  2817\t          case 'control.forced_stop':\n  2818\t          case 'control.recovery_terminalized':\n  2819\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'failed';\n  2820\t            break;\n  2821\t          case 'kill.confirmed':\n  2822\t          case 'control.interrupt_confirmed':\n  2823\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'cancelled';\n  2824\t            break;\n  2825\t          case 'question.asked':\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '2790,2825p;830,850p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":35,"ts":"2026-07-11T20:07:05.609Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":59082,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":255822,"inputTokens":252234,"cachedInputTokens":195712,"outputTokens":3588,"reasoningOutputTokens":2027},"last":{"totalTokens":59082,"inputTokens":58956,"cachedInputTokens":56192,"outputTokens":126,"reasoningOutputTokens":35},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":255822,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":36,"ts":"2026-07-11T20:07:05.611Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":37,"ts":"2026-07-11T20:07:05.611Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"I have enough. I’m writing the gate review now and will only run the pinned verification command after the file is in place."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":38,"ts":"2026-07-11T20:07:53.727Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.file_edit","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"fileChange","id":"call_jFGHr3hQbwkrCVXiqt0Tf45b","changes":[{"path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/reviews/dogfood/codex-ck9-crash-window-review.md","kind":{"type":"add"},"diff":"## Verdict\nCK9 is not green. The changes in `c8a272e` materially improve pre-effect intent ordering for recovery, integration, and publication authorization, but they still leave one major deterministic crash-window gap: a successful publish followed by `publication.completed` append failure is not restart-closed. That violates the CK2/CK9 requirement in [spec/phase11/coordination-knowledge.md](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/coordination-knowledge.md:69) and [spec/phase11/coordination-knowledge.md](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/coordination-knowledge.md:239).\n\nThis is a deterministic gate finding, not a complaint about later product work. I am not counting durable replay of pending approvals against this commit, because `coordination-knowledge.md` explicitly leaves pending question/approval reconstruction for later. The problem is narrower: once publication side effects may already have happened, replay currently erases the ambiguity instead of closing it.\n\n## Crash-window matrix\n| Area | Evidence | Assessment |\n| --- | --- | --- |\n| Recovery attach intent ordering | `recover()` records `control.recovery_requested` and `driver.recorded/recovery.requested` before `adapter.spawn()` in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:911); pre-effect failure test in [impl/test/phase11-persistent-sessions.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-persistent-sessions.test.mjs:436). | No finding |\n| Bounded post-attach recovery ambiguity and adapter cleanup | Attached recovery refinement failure kills the transport and leaves the worker orphaned in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:971) and [impl/test/phase11-persistent-sessions.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-persistent-sessions.test.mjs:464). | No finding |\n| Restart closure for nonterminal recovered work | Replay fails non-reattached tasks and claimed-without-spawn tasks in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:2871) and [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:3004). | No finding |\n| Integration Git safety and pre-effect ordering | `integration.requested` is durably recorded before retain/kill/remove/integrate in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1034); failure test proves no Git movement in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:353). | No finding |\n| Publication authorization pre-effect ordering and single-consumer race | `_resolveRecord()` reserves the consumer slot and records `publication.authorized` before calling `_publisher` in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1909) and [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1965); tests cover no-publish-before-authorization and one-winner behavior in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:371) and authorization append failure in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:396). | No finding |\n| Publication post-effect ambiguity and restart closure | After `_publisher()` returns, the code marks the request resolved and mutates `task.publication` before `publication.completed` and `driver.recorded/publication.completed` succeed in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1987). Replay reconstructs publication only from `publication.completed` in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:2813). The only CK9 test for this gap stops before restart and merely asserts in-memory `state === 'resolved'` plus prior authorization in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:413). | Major finding |\n| Claim that CK9 is green | Because the publication side-effect gap is not replay-closed, the deterministic gate in [spec/phase11/coordination-knowledge.md](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/coordination-knowledge.md:241) is still unmet despite new green tests. | Major finding |\n\n## Remaining major findings\nMajor: publication side effects can become durable-intent-only ghosts after restart. In `_resolveRecord()`, a successful publish sets `record.state = 'resolved'`, `record.resolution = { decision: 'allow' }`, and `task.publication = ...` before `publication.completed` is appended and mapped to coordination in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1987). If that later append fails, the commit’s own test only proves bounded in-process poisoning, not replay closure, in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:413). On restart, replay restores publication state only from `publication.completed` events in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:2813), while a separate test confirms pending approvals are dropped across restart in [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:461). The result is not a later-feature gap; it is a CK9 gate violation because an external effect may already have happened, yet replay does not close the ambiguity to a durable non-success/ambiguous outcome and does not preserve operator-visible state beyond the prior `publication.authorized` intent.\n\n## Required next actions\n1. Add an explicit durable closure for the `publisher returned but publication completion append failed` window in [impl/src/coordinator.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs:1980), so replay surfaces a non-success or explicitly ambiguous publication state instead of silently reconstructing nothing.\n2. Add a temp-Git restart test beside [impl/test/phase11-acceptance-integration.test.mjs](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs:413) that forces `publication.completed` coordination failure, restarts the driver, and proves no second publish occurs while the ambiguity remains visible.\n3. Do not mark CK9 green in evidence or recursive-provider gating until that restart-closure path is implemented and rerun against the CK9 matrix in [spec/phase11/coordination-knowledge.md](/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/coordination-knowledge.md:212).\n"}],"status":"completed"},"paths":["/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/reviews/dogfood/codex-ck9-crash-window-review.md"]},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":39,"ts":"2026-07-11T20:08:32.459Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":64631,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":320453,"inputTokens":312111,"cachedInputTokens":254464,"outputTokens":8342,"reasoningOutputTokens":4602},"last":{"totalTokens":64631,"inputTokens":59877,"cachedInputTokens":58752,"outputTokens":4754,"reasoningOutputTokens":2575},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":320453,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":40,"ts":"2026-07-11T20:08:32.459Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":0.8,"hardStop":false,"action":"notify","used":{"tokens":320453,"usd":0},"limits":{"tokens":350000,"usd":3},"ratio":0.91558,"dimensions":{"tokens":0.91558,"usd":0}},"seq":41,"ts":"2026-07-11T20:08:32.460Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":67,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":42,"ts":"2026-07-11T20:08:32.460Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","text":"The review file is written. I’m running the exact pinned verification command now and stopping there."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":43,"ts":"2026-07-11T20:08:34.347Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","item":{"type":"commandExecution","id":"call_xCqIVb6QVPcQiagK3nmLku4d","command":"/bin/zsh -lc \"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Verdict$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Crash-window matrix$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Remaining major findings$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Required next actions$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"10607","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Verdict$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Crash-window matrix$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Remaining major findings$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Required next actions$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md"}],"aggregatedOutput":null,"exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Verdict$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Crash-window matrix$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Remaining major findings$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Required next actions$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":44,"ts":"2026-07-11T20:08:38.101Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":64933,"usd":0,"threadId":"019f52c9-4def-74a3-9049-210de58a13ee","turnId":"019f52c9-4e08-7442-8177-d8f4ccc5dcfb","tokenUsage":{"total":{"totalTokens":385386,"inputTokens":376794,"cachedInputTokens":314240,"outputTokens":8592,"reasoningOutputTokens":4618},"last":{"totalTokens":64933,"inputTokens":64683,"cachedInputTokens":59776,"outputTokens":250,"reasoningOutputTokens":16},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":385386,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":45,"ts":"2026-07-11T20:08:38.103Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":1,"hardStop":true,"action":"kill","used":{"tokens":385386,"usd":0},"limits":{"tokens":350000,"usd":3},"ratio":1.1011028571428572,"dimensions":{"tokens":1.1011028571428572,"usd":0}},"seq":46,"ts":"2026-07-11T20:08:38.103Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"kill.requested","actor":"policy","payload":{},"seq":47,"ts":"2026-07-11T20:08:38.103Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":68,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":35,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":48,"ts":"2026-07-11T20:08:38.108Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"kill.confirmed","actor":"worker","payload":{},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":49,"ts":"2026-07-11T20:08:38.114Z"}
