{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":1,"kind":"runtime.scope_created","actor":"policy","payload":{"root":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1","home":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/home","tmp":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/tmp","config":"/Users/wahargis/Development/Experiments/baton/.baton/runtime/w-1/config/codex","family":"codex","strippedEnvKeys":["ANTHROPIC_DEFAULT_HAIKU_MODEL","CLAUDE_CODE_MAX_OUTPUT_TOKENS","GEMINI_API_KEY","OPENROUTER_API_KEY","SECURITYSESSIONID","SSH_AUTH_SOCK"],"projectedEnvKeys":[],"projectedFiles":["auth.json"],"permissions":{"directories":"0700","credentialFiles":"0600"},"sandboxPolicy":"wire-workspaceWrite-network-deny","active":true},"seq":1,"ts":"2026-07-11T20:19:37.141Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":1,"kind":"lifecycle.spawned","actor":"orchestrator","payload":{"taskId":"codex-ck9-crash-window-review","brief":{"goal":"Adversarially review commit c8a272e's coordination crash-window changes against spec/phase11/coordination-knowledge.md and docs/26-full-system-goal.md. Inspect impl/src/coordinator.mjs plus phase11 coordination, persistent-session, and acceptance/integration tests. Write reviews/dogfood/codex-ck9-crash-window-review.md with exact headings \"## Verdict\", \"## Crash-window matrix\", \"## Remaining major findings\", and \"## Required next actions\". Try to falsify pre-effect intent ordering, bounded post-effect ambiguity, restart closure, single-consumer behavior, adapter/PID cleanup, integration Git safety, publication authorization, and claims that CK9 is green. Distinguish this deterministic gate from still-missing product features.","constraints":["Edit only reviews/dogfood/codex-ck9-crash-window-review.md.","Do not modify implementation, tests, specs, task state, or evidence files.","Do not commit, push, deploy, or use network tools.","Ground every finding in exact repository paths and classify critical, major, minor, or no finding.","Keep the review under 1800 words; do not accept green tests as sufficient evidence.","Use at most 18 repository-read/tool calls. Once enough evidence is available, stop exploring and write the review."],"pathScope":["reviews/dogfood/codex-ck9-crash-window-review.md"],"definitionOfDone":"The four exact headings exist and the review explicitly evaluates CK9 crash windows","verification":{"command":"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Verdict$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Crash-window matrix$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Remaining major findings$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Required next actions$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md","expectExit":0,"timeoutMs":10000},"budget":{"tokens":500000,"usd":3,"wallMin":6},"tools":[],"outputFormat":""},"vendorRequested":"codex","vendorResolved":"codex","modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelPolicy":{"allow":["gpt-5.4"],"allowFamilies":["openai"],"reasoningEffort":"high"},"sessionRequest":{"mode":"new"},"lineage":null,"review":null},"seq":2,"ts":"2026-07-11T20:19:37.152Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"lifecycle.turn_started","actor":"orchestrator","payload":{},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":null,"seq":3,"ts":"2026-07-11T20:19:37.153Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"worktree.ready","actor":"orchestrator","payload":{"worktree":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","repoRoot":"/Users/wahargis/Development/Experiments/baton","baseSha":"a6ed6728c65aed00456c747368017f82e1d7df37","branch":"baton/codex-ck9-crash-window-review","ownerTaskId":"codex-ck9-crash-window-review"},"seq":4,"ts":"2026-07-11T20:19:37.322Z"}
{"worker":"w-1","harness":"codex","turnEpoch":0,"kind":"lifecycle.spawned","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","pid":72336,"modelRequested":"gpt-5.4","modelObserved":"gpt-5.4","reasoningEffort":"high","serviceTier":null},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":5,"ts":"2026-07-11T20:19:37.932Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"lifecycle.turn_started","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":6,"ts":"2026-07-11T20:19:37.947Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"Reviewing commit `c8a272e` against the coordination specs first, then I’ll inspect `impl/src/coordinator.mjs` and the targeted tests to see whether CK9’s crash-window claims actually hold under restart and single-consumer conditions."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":7,"ts":"2026-07-11T20:19:45.979Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":10866,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":10866,"inputTokens":10475,"cachedInputTokens":1920,"outputTokens":391,"reasoningOutputTokens":239},"last":{"totalTokens":10866,"inputTokens":10475,"cachedInputTokens":1920,"outputTokens":391,"reasoningOutputTokens":239},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":10866,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":8,"ts":"2026-07-11T20:19:47.505Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":74,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":9,"ts":"2026-07-11T20:19:47.506Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"I’m pulling the spec language and the commit delta together first so the review can test the implementation against explicit obligations rather than against passing tests."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":10,"ts":"2026-07-11T20:19:56.552Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_TVOO7GWWEFjnbgKTmpn0iHfP","command":"/bin/zsh -lc \"sed -n '1,260p' docs/26-full-system-goal.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"20570","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"sed -n '1,260p' docs/26-full-system-goal.md","name":"26-full-system-goal.md","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/docs/26-full-system-goal.md"}],"aggregatedOutput":"# 26 — Full-system goal: no planned capability disappears\n\nThis is the plan of record after the 2026-07-11 completeness audit. It supersedes any reading of\ndocs 22–25 that treats “phase 10 complete” as completion of Baton’s larger design. Phase 10 proved\na useful first-turn control spine. The active goal is the complete fleet system described across\n`SYSTEM.md`, `docs/`, `spec/`, and `reviews/`.\n\n“Later”, “fenced”, and “research bet” are sequencing/status labels, not deletion mechanisms. Every\nitem below remains visible until it is either shipped with evidence or explicitly retired by a\nrecorded, evidence-backed decision. A feature cannot vanish from the goal through a summary.\n\n## Scope correction\n\nThe shared knowledge design takes architectural inspiration from `project-manager`, but Baton has\n**no homelab integration or runtime dependency** in this goal. It owns a deployment-neutral typed\ncausal substrate and may expose ordinary import/export interfaces later. This avoids coupling the\nproduct to one machine or knowledge service.\n\n## Definition of complete\n\nBaton is complete when one orchestrator can choose harness **and model**, direct persistent\nmulti-vendor workers through a durable northbound surface, observe and govern them safely, accept\nand integrate only independently established work, share operational/coordinative/epistemic\nknowledge, and give workers the complete capability and representation planes below. Every claim\nmust be reachable through the public assembly, test-locked, adversarially reviewed, and live-proven\nwhere a real harness or operating-system boundary is involved.\n\nThe implementation loop for every increment is:\n\n1. verify current reality;\n2. write numbered contracts;\n3. capture a red test or measured failing probe;\n4. implement the smallest complete vertical;\n5. run narrow and full validation;\n6. adversarially review seams, not only modules;\n7. live-prove the real boundary; and\n8. update this catalog and the evidence ledger.\n\nRecursive dogfooding resumes only after the lifecycle, provenance, and credential gates relevant\nto the recursive run are green.\n\n## Complete capability catalog\n\n### A. Fleet control and reliability\n\n- Eight public commands: spawn, send, wait, respond, interrupt, result, list, kill.\n- Ordered delivery, human-over-policy fencing, single-consumer interactions, two-phase stop,\n  terminal monotonicity, bounded setup/turn/stop operations, and process/worktree reap.\n- Persistent multi-turn workers; interrupt-follow-up; resume, fork, rejoin, checkpoint, rewind, and\n  crash recovery without fabricating uniform semantics across vendors.\n- Restart-safe task/worker identity, pending interactions, fences, adapter/session references,\n  worktree ownership, story, budgets, and routing state.\n- Dependency DAG, refinement links, idempotent claims, artifact registry, path leases, and\n  deterministic ready-work selection.\n\n### B. Harness and model selection\n\nHarness/CLI and model are independent axes. The orchestrator can:\n\n- name an exact harness and exact model;\n- select a model while leaving harness routing automatic;\n- constrain allowed/denied models or families and express ordered preferences;\n- select reasoning/service tier where the harness exposes it;\n- inspect available/default/current model information and the provenance/freshness of that card;\n- see requested, resolved, observed, and rerouted model identities in handles, events, results,\n  verification, routing statistics, replay, scorecards, and commit attribution; and\n- fail visibly when a model cannot be honored—never silently fall back to a harness default.\n\nSelection must map to real controls: Claude/GLM `--model`, Codex thread/turn model overrides, Grok\n`--model`/ACP model state, and future adapters’ native mechanism.\n\n### C. Southbound harness depth\n\n- Claude: native session steering/interrupt, approvals/questions, hooks, context/usage\n  introspection, compaction events, constraint reinjection, model/permission reconfiguration,\n  resume/fork/rewind, config-home isolation, and live capability discovery.\n- Codex: app-server sessions, steer/interrupt, approvals/questions, thread inject/resume/fork,\n  goal pinning, structured outputs, review, compaction, usage/rate limits, broker/daemon topology,\n  model/service/reasoning selection, sandbox policy, and schema-driven feature detection.\n- Grok: ACP core plus model selection/state, session load/fork/rewind, auth, extensions, usage,\n  multi-client attach, home/MCP isolation, and explicit handling of unsupported ask-user behavior.\n- GLM: Claude-harness session parity, exact model mapping, non-refuser capability metadata,\n  concurrency/quota inputs, scoped credentials, and live proof when credentials are available.\n- Honest one-shot tier and future ACP adapters remain separately carded; no reduced tier may pose\n  as the session product.\n\n### D. Safety and governance\n\n- Real OS sandbox profiles, worktree confinement, network policy, scoped environment/credentials,\n  isolated harness homes, and approval-gated outside-world side effects.\n- Dry-run/approve-all/sample/autonomous trust ramp with emergency stop always available.\n- Wall, token, USD, rate-limit, account-seat, and quota-window budgets folded into authoritative\n  state with thresholds, hard stops, and degradation policies.\n- Deterministic watchdog actions for mechanical stall/loop/scope/churn cases; semantic failure\n  classifiers remain explicitly untrusted model judgments.\n- Correct provenance: hub facts, worker prose, external evidence, and derived claims never share a\n  trust label. Read edges support contamination/contradiction analysis.\n\n### E. Trust, review, and integration\n\n- Immutable validated delegation contract and exact definition of done.\n- Fresh-result verification plus base red→green, coverage-of-change, mutation strength,\n  impact-selected tests, property/fuzz/BMC/SMT/proof rungs, and reproducible counterexamples.\n- Independent oracle construction and cross-vendor semantic-diff review for risk-selected work.\n- Structured postmortems and failure attribution linked to source events.\n- Verified branch integration, textual then structured/semantic merge, conflict handling, effect\n  tripwire, review artifact, and explicit approval before push/deploy/other irreversible actions.\n\n### F. Routing, evaluation, and learning\n\n- Routing buckets keyed by harness, exact model, model family, task class, capability, policy, and\n  version; only verified outcomes learn; idempotent replay preserves them.\n- New-model exploration, decay, refusal feedback/reroute, operator pin/exclude/prefer controls,\n  quota awareness, and outcome/calibration telemetry.\n- Reproducible M0 control latency, M1 orchestration arms, and E2 cross-vendor decorrelation\n  evaluations, including null/negative outcomes and human-audit cost.\n\n### G. Three memory tempos plus Scratch\n\n- **Operational:** append-only event ledger, resumable cursors, replay, artifacts, and telemetry.\n- **Coordinative:** transactional task DAG, artifact manifest, claims/leases, refinements, and\n  ready-work queries.\n- **Epistemic:** durable typed causal knowledge with bounded recall.\n- **Scratch:** TTL/CAS/take/notify blackboard and memoized Bench for fleet-live facts and leases.\n\nThe epistemic layer borrows the strong ideas—not the deployment—from `project-manager`:\n\n- typed nodes: Run, Task, Artifact, Experiment, Finding, Decision, Hypothesis, Principle,\n  Constraint, Source/Literature, RouteStat, Skill, Counterexample, and Representation;\n- typed directional edges: Supports, Contradicts, Supersedes, Informed, ProducedBy, Contains,\n  DependsOn, Refines, ReadBy, VerifiedBy, and DerivedFrom;\n- every consequential decision has evidence edges to earlier immutable ledger events;\n- temporal coherence forbids an edge to evidence that did not yet exist;\n- bitemporal validity preserves what was believed then versus what is valid now;\n- selective promotion avoids turning every event into knowledge;\n- contradiction-gated, provenance-framed, token-bounded pull recall—never automatic shared-brain\n  injection; and\n- run scorecards expose graph and fleet health with metric breakdowns rather than one green bit.\n\n### H. Capability plane\n\nEvery capability uses one ACI envelope, capability cards, cost/provenance, resumable long work,\ncancellation, and hub re-verification.\n\n1. **Atlas:** shared lexical search, AST/CST structural search/rewrite, symbol graph and SCIP/LSP\n   navigation, optional semantic retrieval, base-plus-worktree overlays, staleness, and `code_seed`.\n2. **Vantage:** DAP observation plans, causal debugging results, record/replay assets, exclusive\n   debuggee leases, and reproducible postmortems.\n3. **Evidence Ladder:** tests through proof with honest language/tool ceilings, mutation scores,\n   trusted kernels, full dependency-closure hashes, and no “proven” claim over worker-weakened\n   specifications.\n4. **Scratch:** operational blackboard/coordination REPL and memoized experiment Bench.\n5. **Skill Forge and computer use:** verify, promote, version, revoke, and share portable skills;\n   distill flaky GUI trajectories into deterministic automation where possible.\n6. **Cartographer and Quartermaster:** shared repository orientation/call graphs plus gated\n   dependency/reuse/security evidence.\n7. **Cairn:** run scorecards, causal findings/decisions, route statistics, promotion, recall,\n   contradiction/supersession, and exportable body of knowledge.\n\n### I. Representation and computation ladder\n\nRepresentation is negotiated by task phase and cost, not reduced to raw text:\n\n- R0 text/tokens;\n- R1 CST/AST via tree-sitter/ast-grep/difftastic/GumTree-class edit scripts;\n- R2 symbol/call/dependency graphs via LSP, SCIP, stack graphs, or equivalent;\n- R3 code property graph: AST + CFG + PDG/dataflow/taint, including CPG deltas;\n- R4 compiler/intermediate representations for optimization and translation validation;\n- R5 behavioral fingerprints from differential/property/fuzz execution and effect signatures;\n- R6 structured and semantic diff/merge over syntax, symbols, control/data-flow, and behavior;\n- R7 e-graph/equality-saturation experiments where the domain is tractable; and\n- representation choreography: orient with a coarse graph, focus with local AST/CPG, finish with\n  a semantic delta and attestation overlay, retracting views no longer useful.\n\nThe first shipped vertical is AST/structural delta for review. CPG, semantic merge, behavioral\nfingerprints, IR, and e-graphs remain in the goal with explicit prototype/evaluation gates; a\nnegative result retires a rung through a recorded Decision, never through omission.\n\n### J. Context, operator, northbound, and runtime\n\n- Vendor-shaped briefs, addressed context, orientation references, constraint/DoD reinjection on\n  compaction, context usage governance, and retractable views.\n- MCP northbound fleet tools and tasks, a long-lived daemon, resumable waits/subscriptions, and an\n  operator text/TUI seat with narrative, provenance, takeover, approvals, budgets, and emergency\n  control.\n- An **authenticated web northbound** for the human user ↔ orchestrator direction. HTTPS command\n  requests and a resumable WebSocket/event stream expose the same coordinator authority—not a\n  parallel state machine—including spawn/harness/model selection, steer/nudge/turn, approval and\n  question response, interrupt/kill, goals/tasks, narrative, budgets, and emergency control.\n  Authentication binds a stable user/session identity; authorization scopes commands and repo/run\n  access; every command carries an idempotency key, fence expectation, origin, and audit actor.\n  TLS, secure session/token storage, expiry/revocation, origin and CSRF checks, replay protection,\n  rate limits, reconnect cursors, backpressure, and disconnect semantics are contract-tested.\n  Browser loss never cancels work implicitly, and the web tier cannot bypass approval, sandbox,\n  budget, or trust gates. A richer visual UI is optional; the secure control connection is not.\n  Numbered contracts and its adversarial gate live in\n  `spec/phase12/authenticated-web-northbound.md` (WN1–WN10).\n- Observability exports use OpenTelemetry GenAI conventions.\n- One-machine production first, with a reliable Go or Elixir/OTP core after the executable-spec\n  contracts stabilize. Remote/multi-machine execution remains catalogued but is not allowed to\n  distort the local correctness boundary.\n\n## Current pursuit order\n\n1. Repair P0 control integrity: immutable briefs, truthful responses, crash-safe idempotent kill,\n   provenance, story identity, replay identity, and complete cleanup.\n2. Ship harness-independent exact model selection and attribution.\n3. Make persistent sessions genuinely usable through the driver; add resume/fork/rejoin.\n4. Ship OS/credential isolation and budget/watchdog governance.\n5. Complete hardened acceptance and structured integration.\n6. Ship the task/artifact/Scratch substrate and deployment-neutral causal knowledge core.\n7. Ship Atlas + AST structural delta + repo map + scorecard as the first capability vertical.\n8. Expand the representation and capability ladders in measured increments.\n9. Add MCP plus authenticated HTTPS/WebSocket user↔orchestrator control, operator surfaces,\n   production runtime, and the registered evaluations.\n\nNo later step is permission to erase it from the goal.\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"sed -n '1,260p' docs/26-full-system-goal.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":11,"ts":"2026-07-11T20:19:56.617Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_KYRfEde0HQ2pW3W6siKmEVfB","command":"/bin/zsh -lc \"sed -n '1,260p' spec/phase11/coordination-knowledge.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"50242","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"sed -n '1,260p' spec/phase11/coordination-knowledge.md","name":"coordination-knowledge.md","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/spec/phase11/coordination-knowledge.md"}],"aggregatedOutput":"# Phase 11.4 — durable coordination and knowledge substrate\n\nThis specification implements goal catalog A/G and the Scratch/Cairn portions of H without a\nhomelab or `project-manager` runtime dependency. `project-manager` is prior-art inspiration for\ntyped causal structure and audits only. Baton owns a deployment-neutral, single-host-first event\nstore and deterministic projections.\n\nThe first implementation vertical is deliberately one substrate, not four unrelated databases:\ntask, artifact, Scratch, and knowledge mutations are typed coordination events. The append-only\nevent file is truth; every query result is a replayable projection.\n\n## CK1 — one durable coordination stream\n\n`CoordinationStore(root)` owns an append-only `events.jsonl` with gap-free global `seq`, hub-stamped\nISO time, schema version, `kind`, authenticated/audited `actor`, idempotency key, and JSON payload.\nOnly the hub writes. An append is durable before its projection mutates. Duplicate idempotency keys\nreturn the original event without a second mutation. Torn/malformed tails fail startup visibly;\nthey are never silently reset or skipped. Replay produces byte-equivalent public projections.\n\nThe store path is explicitly supplied by the assembly and never derived from a worker cwd. It has\nno network, PM, or homelab dependency.\n\n### CK1a — relation to per-worker operational logs\n\nExisting `<worker>.jsonl` streams remain lossless high-volume operational telemetry with local\nper-worker sequence numbers. They are not a second task/KG truth. Every state-changing driver path\nfirst appends its typed coordination mutation; only then may the in-memory task/artifact/Scratch/KG\nprojection change. Operational evidence is referenced as `{worker, seq, digest}` and becomes\ncross-worker comparable only through an `evidence.mapped` coordination event whose global sequence\nis the observation order. Knowledge temporal checks use the global coordination sequence, never a\nbare per-worker sequence.\n\nThe two files cannot be transactionally committed together. Reconciliation therefore scans\noperational evidence references at startup: an operational event with no coordination mutation is\ntelemetry only and cannot affect task/KG truth; a coordination event pointing to missing or\ndigest-mismatched operational evidence fails integrity validation. Coordination append failure is\nfatal to the requested state mutation and leaves its projection unchanged. It may not use the\nlegacy warning-and-drop behavior of the telemetry sink.\n\nConstruction validates the complete coordination stream immediately: JSON parse, schema version,\ngap-free sequence, unique idempotency key, and final newline. A truncated/torn tail is an\nactionable startup error and is never auto-truncated. Schema migration is explicit versioned event\ntranslation into a new store, not in-place reinterpretation.\n\n## CK2 — typed durable task DAG and claims\n\nA task record contains:\n\n```text\nid, brief, status, deps[], assignee, version, refines, taskType,\ncreatedEvent, claimedEvent, terminalEvent, artifactIds[]\n```\n\nStatus is `pending | working | input_required | completed | failed | cancelled`. Terminal states\nare immutable. Allowed nonterminal transitions are explicit. Creation rejects missing deps and\ncycles. A task is ready iff it is pending, unassigned, and every dependency is completed.\n`claimTask(id, worker, expectedVersion, key)` is compare-and-swap: exactly one concurrent caller\nwins; stale, blocked, already-assigned, and terminal claims are typed refusals and do not append.\nRefinement creates a new task with `refines`; it never reopens a terminal task.\n\nCoordinator `spawn()` durably creates the task before returning a handle, including when the task\nwill remain queued. It may preallocate a public pending worker handle as a local reservation, but\nthe durable task `assignee` remains null until dispatch wins `claimTask`; reservation is not claim.\nDispatch durably claims it. Input waits, recovery, cancellation, verification\naccept/reject, and integration/publication outcomes are reflected by typed events. Restart rebuilds\nqueued tasks, dependency readiness, assignments, terminal state, and automatic identifiers before\ndispatching anything.\n\nEvery adapter- or outside-world-reaching action has a durable intent before the effect: stop,\npersistent follow-up, recovery attach, review-task creation, local integration, publication\nrequest, and publication authorization. If intent append fails, no adapter, worktree/Git merge, or\npublisher is called. If an adapter has already confirmed stop/attach/turn advancement when a later\nterminal/refinement append fails, the coordinator is poisoned, resolves any public waiter with a\ntyped coordination-unavailable result, kills or quarantines the ambiguous transport, and restart\ncloses the still-nonterminal durable task. Asynchronous adapter callbacks may not turn an already\nrecorded fatal coordination fault into an uncaught process exception.\n\n`task.created` persists `brief`, `deps`, `refines`, `taskType`, requested vendor/model/session\npolicy, and the reserved public handle ID. `task.claimed` persists assignee, resolved vendor/model,\nand expected/new versions. Refusal codes are `stale_version | already_assigned |\ndeps_unsatisfied | terminal | cycle`; refusals append no event. Recovery terminalization refines or\nterminates the durable record and may never reopen a terminal predecessor.\n\n## CK3 — immutable artifact manifest\n\nArtifacts are manifests, never copied bytes:\n\n```text\nid, taskId, kind, refs, mediaType, digest, size?, provenance[], createdEvent\n```\n\nKinds include `commit | diff | verification | coverage | mutation | review | report |\ncounterexample | representation | skill | bench-result`. `refs` are Git SHAs/refs or explicit\ncontent-addressed paths owned by Baton. Registration validates the task, snapshots caller-owned\ndata, and is idempotent by manifest identity. `artifact.registered` names the immutable manifest;\n`artifact.superseded` links a correction without mutation. Failed/cancelled tasks may register\ncounterexamples, logs, and reports, but an artifact marked `accepted` must cite the accepting\nverification/review event. Accepted capture, verdict, independent review, integration, and\npublication payloads carry manifest IDs through the public driver rather than leaving provenance\nsplit across `capturedSha` and worker prose.\n\n## CK4 — Scratch is an event-derived operational projection\n\nScratch supports the smallest worker-useful primitives:\n\n- append-only facts with namespace/key, immutable `envRef:{repoId,treeSha}`, `observed | derived`\n  grounding, evidence refs,\n  and optional explicit expiry event;\n- advisory claims with resource glob, owner worker/task, intent, `envRef`, supervisor fence,\n  version, and lease deadline;\n- point checks that return clear/held plus a mandatory cross-environment warning when the fact or\n  claim was observed on another tree; and\n- explicit `scratch.claim_expired` / `scratch.fact_expired` events.\n\nThere is no reader-clock expiry, heartbeat verb, free-text fleet chat, mutable world-state blob, or\ngeneral CAS document in the first vertical. Claim expiry is a hub-emitted event slaved to worker\nlease/terminal state, so replay at different wall times is identical. Path-resource conflicts use\nconservative glob intersection; uncertain overlap conflicts rather than false-negatives. Exclusive\nOS resources remain the sandbox/allocator's job, not an advisory Scratch claim.\n\nWorker-facing participation is eventually ambient through mediated tool results; until that tool\npath ships, the public store exposes deterministic point checks rather than a polling/watch loop.\nLive worktree paths are invalid environment references; callers snapshot `git write-tree` (or an\nequivalent content digest) before posting. A different `treeSha` always produces the exact warning\nframe `observed on <treeSha> — not your tree`.\n\nNormative path-claim overlap is conservative: normalize repository-relative POSIX paths; exact\npaths overlap on equality; a glob overlaps an exact path if the path matches; two globs overlap if\neither literal prefix before its first wildcard is a prefix of the other or an implementation can\nexhibit a matching witness. If disjointness cannot be proven, they conflict. This may false-positive\nbut may not false-negative a known overlap.\n\n## CK5 — self-contained typed bitemporal causal knowledge graph\n\nThe graph is a materialized view over coordination events. It supports typed nodes:\n\n```text\nRun, Task, Artifact, Phase, Experiment, Finding, Decision, Hypothesis, Principle,\nConstraint, Literature, Research, RouteStat, Skill, Counterexample, Representation,\nScratchFact\n```\n\nand typed directional edges:\n\n```text\nSupports, Contradicts, Supersedes, Informed, ProducedBy, Contains, DependsOn,\nRefines, ReadBy, VerifiedBy, DerivedFrom, Affects, Cites, ObservedIn\n```\n\nEvery node and edge stores observation time (coordination seq/time), event time (referenced source\nseq/time), and valid time (`validFrom`, nullable `validTo`). Invalidated beliefs are never deleted.\n`Supersedes` and contradiction resolution append invalidation events with compare-and-swap on the\ncurrent validity version.\n\nA Decision is rejected unless it has at least one `Informed` evidence reference. A verified\nFinding is rejected unless it has `ProducedBy`/`VerifiedBy` evidence. Temporal coherence rejects\nevidence whose source sequence/time is later than the node or edge event time. Worker/recalled text\nis always marked untrusted prose; grounding is hub-derived (`verified | observed | derived |\nasserted`) and never taken from a model confidence float.\n\n`queryKnowledge({asOf, observedAt, types, grounding})` applies both temporal dimensions.\n`traceKnowledge(id)` returns causal paths to immutable events/artifacts. Recall is pull-only,\ntoken-bounded at the caller, contradiction-aware, provenance-framed, and logged with `ReadBy`;\nnothing is automatically injected into a worker context.\n\nThe durable read record is `knowledge.read{readerActor, readerWorker, runId, taskId, nodeIds[],\nquery, asOf, observedAt, validityVersions}`. Its append must succeed before recalled content is\nreturned. The graph projection creates `ReadBy` edges from every returned node to the reader task/\nrun. An unlogged read is a failed operation, never a degraded success.\n\n## CK6 — selective promotion and health\n\nPromotion is deterministic candidate generation, not an LLM in the control path:\n\n- accepted terminal task + verification/artifact → `knowledge.promoted{kind:'Finding'}`;\n- consequential human/orchestrator spawn, reroute, accept, integrate, publish, abort → Decision;\n- run boundary → Run scorecard;\n- repeated verified outcome → RouteStat/Playbook candidate;\n- cited observed Scratch fact → `knowledge.promotion_candidate{kind:'Finding'}`;\n- derived/formal Scratch fact remains quarantined until an independent oracle links it.\n\nThe first vertical implements explicit node/edge writes plus named task/artifact auto-promotion\nhooks. Failed/unaccepted tasks never auto-promote a positive Finding; they may promote a\nCounterexample or failure Finding with explicit grounding.\nLater scorecard/recall promotion uses the same schema. `auditKnowledge()` reports causal\ncompleteness, temporal coherence, orphan nodes, contradiction resolution, invalid references,\nrecall utility, and read-contamination blast radius separately—never one unexplained green bit.\n\n## CK7 — contamination and read provenance\n\nEvery knowledge or Scratch read that crosses into an orchestrator/worker context records reader,\nrun/task, returned IDs, query/as-of boundary, and source validity versions through\n`knowledge.read`. If a node is later invalidated,\n`knowledge.contamination_record{nodeId, invalidationEvent, affectedReadEvents[]}` is appended and\n`affectedReaders(id)` projects every downstream task/run that consumed it, including current task\nstatus. A\ncross-tree Scratch read is labeled “observed on X — not your tree.” This is required before shared\nknowledge can influence routing or acceptance.\n\n## CK8 — authority and public assembly\n\nThe store is mandatory in `createDriver()` and returned as `{coordinator, story, router, log,\ncoordination}`. The coordinator receives it in its constructor and invokes one non-optional\nstate-changing integration point for spawn/create, dispatch/claim, input wait/resume, terminal,\ncapture/verification, review, integration, and publication. A driver state mutation with no\ncoordination event is a contract failure, not an optional sink failure. Direct mutation methods\nrequire an actor and\nidempotency key; future MCP/web surfaces map authenticated identity into that same actor field and\ndo not bypass coordinator fences or approvals. Knowledge export/import is ordinary data I/O behind\nfuture approval gates, not a special PM/homelab integration.\n\n## CK9 — zero-quota safety gate\n\nTemp-directory and temp-Git tests must prove before provider dogfooding:\n\n1. duplicate append keys and duplicate task creates are idempotent;\n2. queued/unassigned tasks and dependency readiness survive restart;\n3. claim CAS has exactly one winner and terminal states cannot reopen;\n4. cycles, stale versions, invalid transitions, and missing deps refuse without events;\n5. artifact manifests are immutable and linked to accepted tasks;\n6. Scratch conflicts, explicit lease expiry, replay determinism, and cross-tree warnings;\n7. causal/temporal graph rejections, bitemporal as-of query, contradiction/supersession history,\n   and affected-reader tracing;\n8. public-driver task creation, terminal verdict, captured commit, review, and integration appear\n   in the durable substrate; and\n9. a full restart has identical task/artifact/Scratch/KG projections before any new dispatch.\n10. an unsatisfied-dependency task crashes before dispatch and replays pending, unassigned, with\n    its exact `deps[]`, brief, requested vendor/model/session, and reserved handle identity;\n11. a multi-task DAG replays with the same ready set and cannot dispatch a dependent early;\n12. a truncated tail, sequence gap, duplicate key, missing operational evidence, and injected\n    append failure all fail visibly without projection mutation;\n13. every public spawn/claim/input/terminal/capture/review/integration/publication state change has\n    a coordination event, and the test fails if coordinator state is nonempty while the substrate\n    stream is empty;\n14. operational evidence mapping gives two worker-local events a comparable global observation\n    order without treating their local sequences as globally ordered; and\n15. after a logged knowledge read, invalidating the node yields a nonempty contamination record\n    and `affectedReaders()` result; a forced read-log append failure returns no recalled content.\n16. injected append failures cover create, claim, input wait/resume, stop intent, cancel terminal,\n    persistent follow-up/recovery refinement, review creation, terminal artifact batch,\n    integration intent, publication authorization, and knowledge/Scratch reads. Pre-effect\n    failures call no adapter/Git/publisher; post-effect failures are bounded, poison authority,\n    preserve the earlier intent, and replay to a non-success terminal state.\n\nAfter CK9 is green and committed, Baton may recursively run a real provider review against this\nspec and implementation. The recursive task must use isolated runtime credentials/budgets, exact\nmodel selection, a pinned verification command, independent trust-gate verification, and confirmed\nkill/process/worktree/branch cleanup.\n\n## Explicitly later, never deleted\n\n- durable pending question/approval reconstruction and automatic live-session rejoin;\n- a SQLite/other query index (projection only), multi-process writer locking, and multi-machine\n  replication;\n- ambient Scratch injection in tool mediation, Bench action-cache execution, notify subscriptions,\n  and task promotion from contention;\n- automatic scorecards, bounded lexical/graph recall ranking, RouteStat feedback, Playbook/Skill\n  promotion, and import/export tooling;\n- authenticated MCP/HTTPS/WebSocket access (uses this substrate but ships in its own phase); and\n- graph-backed Atlas/CPG/representation nodes (schema is reserved here; producers ship later).\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"sed -n '1,260p' spec/phase11/coordination-knowledge.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":12,"ts":"2026-07-11T20:19:56.618Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_mXXMBfpFNWgnBqpP7ReJvf7k","command":"/bin/zsh -lc 'git show --stat --oneline --decorate=short c8a272e'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"35792","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"git show --stat --oneline --decorate=short c8a272e"}],"aggregatedOutput":"c8a272e Close coordination effect crash windows\n .../phase11-coordination-foundation-2026-07-11.md  |  36 ++-\n impl/VALIDATION.md                                 |   2 +-\n impl/src/coordinator.mjs                           | 292 +++++++++++++++------\n impl/test/phase11-acceptance-integration.test.mjs  |  76 ++++++\n impl/test/phase11-coordination-store.test.mjs      |  78 ++++++\n impl/test/phase11-persistent-sessions.test.mjs     |  78 ++++++\n spec/phase11/coordination-knowledge.md             |  14 +\n 7 files changed, 479 insertions(+), 97 deletions(-)\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'git show --stat --oneline --decorate=short c8a272e'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":13,"ts":"2026-07-11T20:19:56.634Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":11328,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":22194,"inputTokens":21351,"cachedInputTokens":3840,"outputTokens":843,"reasoningOutputTokens":405},"last":{"totalTokens":11328,"inputTokens":10876,"cachedInputTokens":1920,"outputTokens":452,"reasoningOutputTokens":166},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":22194,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":14,"ts":"2026-07-11T20:19:56.635Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":74,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":15,"ts":"2026-07-11T20:19:56.636Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"The spec is explicit: CK9 is not “tests pass,” it is “pre-effect failures call nothing, post-effect failures poison and replay closed, and public-driver mutations remain substrate-authorized.” I’m checking the coordinator and tests directly against those points now."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":16,"ts":"2026-07-11T20:20:05.263Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_bLNCSaIl4P3v7dTtYOr8zNrb","command":"/bin/zsh -lc 'rg -n \"poison|publish|integration|intent|append failure|single-consumer|knowledge.read|persistent|recovery|stop intent|authorization|git|adapter|pid|cleanup|claimTask|claim|wait|resume|terminal|refine|follow-up|followup|post-effect|pre-effect\" impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"61677","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n 'poison|publish|integration|intent|append failure|single-consumer|knowledge.read|persistent|recovery|stop intent|authorization|git|adapter|pid|cleanup|claimTask|claim|wait|resume|terminal|refine|follow-up|followup|post-effect|pre-effect' impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs","query":"poison|publish|integration|intent|append failure|single-consumer|knowledge.read|persistent|recovery|stop intent|authorization|git|adapter|pid|cleanup|claimTask|claim|wait|resume|terminal|refine|follow-up|followup|post-effect|pre-effect","path":"coordinator.mjs"}],"aggregatedOutput":"impl/test/phase11-acceptance-integration.test.mjs:9:import { MockAdapter } from '../src/adapter.mjs';\nimpl/test/phase11-acceptance-integration.test.mjs:11:function git(args, cwd) { return execFileSync('git', args, { cwd, encoding: 'utf8' }).trim(); }\nimpl/test/phase11-acceptance-integration.test.mjs:14:  git(['init', '-q'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:15:  git(['config', 'user.email', 'baton-test@example.com'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:16:  git(['config', 'user.name', 'Baton Test'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:25:  git(['add', '-A'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:26:  git(['commit', '--allow-empty', '-q', '-m', 'base'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:37:    const value = await fn();\nimpl/test/phase11-acceptance-integration.test.mjs:39:    await new Promise((resolve) => setTimeout(resolve, 10));\nimpl/test/phase11-acceptance-integration.test.mjs:45:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/integrated.txt', content: 'accepted\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:47:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:50:  const handle = await driver.coordinator.spawn('mock', brief({ command: 'test -f src/integrated.txt', expectExit: 0 }), { taskId });\nimpl/test/phase11-acceptance-integration.test.mjs:51:  await until(async () => (await driver.coordinator.result(handle.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:52:  assert.equal((await driver.coordinator.result(handle.id)).status, 'completed');\nimpl/test/phase11-acceptance-integration.test.mjs:57:  const adapter = new MockAdapter({ scenario });\nimpl/test/phase11-acceptance-integration.test.mjs:58:  const baseCard = adapter.card.bind(adapter);\nimpl/test/phase11-acceptance-integration.test.mjs:59:  adapter.card = () => ({\nimpl/test/phase11-acceptance-integration.test.mjs:66:  return adapter;\nimpl/test/phase11-acceptance-integration.test.mjs:74:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/publish.txt', content: 'publish me\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:76:    repoRoot: root, logDir, adapters: { mock: adapter }, now, approvalTimeoutMs,\nimpl/test/phase11-acceptance-integration.test.mjs:77:    publisher: async (target) => { calls.push(target); return { transport: 'test-publisher' }; },\nimpl/test/phase11-acceptance-integration.test.mjs:80:  const handle = await driver.coordinator.spawn('mock', brief({ command: 'test -f src/publish.txt', expectExit: 0 }), { taskId: 'publish-task' });\nimpl/test/phase11-acceptance-integration.test.mjs:81:  await until(async () => (await driver.coordinator.result(handle.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:82:  await driver.coordinator.integrate(handle.id);\nimpl/test/phase11-acceptance-integration.test.mjs:89:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/new.txt', content: 'ok\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:91:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac1-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:94:  const h = await coordinator.spawn('mock', brief({ command: 'test -f src/new.txt', expectExit: 0 }), { taskId: 'red-green' });\nimpl/test/phase11-acceptance-integration.test.mjs:95:  await until(async () => (await coordinator.result(h.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:96:  const result = await coordinator.result(h.id);\nimpl/test/phase11-acceptance-integration.test.mjs:108:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:110:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:113:  const h = await coordinator.spawn('mock', brief({\nimpl/test/phase11-acceptance-integration.test.mjs:116:  await until(async () => (await coordinator.result(h.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:117:  const result = await coordinator.result(h.id);\nimpl/test/phase11-acceptance-integration.test.mjs:128:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:130:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac2b-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:133:  const h = await coordinator.spawn('mock', brief({\nimpl/test/phase11-acceptance-integration.test.mjs:136:  await until(async () => (await coordinator.result(h.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:137:  const result = await coordinator.result(h.id);\nimpl/test/phase11-acceptance-integration.test.mjs:146:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:148:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:151:  const h = await coordinator.spawn('mock', brief({\nimpl/test/phase11-acceptance-integration.test.mjs:154:  await until(async () => (await coordinator.result(h.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:155:  const result = await coordinator.result(h.id);\nimpl/test/phase11-acceptance-integration.test.mjs:164:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'src/x.js', content: 'export const x = 1;\\n' }] } });\nimpl/test/phase11-acceptance-integration.test.mjs:166:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac3b-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:169:  const h = await coordinator.spawn('mock', brief({\nimpl/test/phase11-acceptance-integration.test.mjs:172:  await until(async () => (await coordinator.result(h.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:173:  const result = await coordinator.result(h.id);\nimpl/test/phase11-acceptance-integration.test.mjs:179:test('AC4: independent oracle receives immutable spec/git evidence and unlocks required integration', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:189:    adapters: { implementer, oracle }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:191:  const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-parent' });\nimpl/test/phase11-acceptance-integration.test.mjs:192:  await until(async () => (await coordinator.result(parent.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:194:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:198:  const reviewer = await coordinator.spawnReview(parent.id, 'oracle', {\nimpl/test/phase11-acceptance-integration.test.mjs:201:  await until(async () => (await coordinator.result(reviewer.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:202:  const reviewResult = await coordinator.result(reviewer.id);\nimpl/test/phase11-acceptance-integration.test.mjs:214:  const integrated = await coordinator.integrate(parent.id);\nimpl/test/phase11-acceptance-integration.test.mjs:226:    adapters: { implementer, fallback }, requireIndependentOracle: true, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:228:  const parent = await coordinator.spawn('implementer', brief({ command: 'test -f src/x.js', expectExit: 0 }), { taskId: 'same-family-parent' });\nimpl/test/phase11-acceptance-integration.test.mjs:229:  await until(async () => (await coordinator.result(parent.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:230:  const reviewer = await coordinator.spawnReview(parent.id, 'fallback', {\nimpl/test/phase11-acceptance-integration.test.mjs:233:  await until(async () => (await coordinator.result(reviewer.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:234:  assert.equal((await coordinator.result(reviewer.id)).review.independent, false);\nimpl/test/phase11-acceptance-integration.test.mjs:236:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:242:test('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:250:  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-acceptance-integration.test.mjs:251:  const parent = await driver.coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-fault-parent' });\nimpl/test/phase11-acceptance-integration.test.mjs:252:  await until(async () => (await driver.coordinator.result(parent.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:258:  await assert.rejects(driver.coordinator.spawnReview(parent.id, 'reviewer', {\nimpl/test/phase11-acceptance-integration.test.mjs:265:test('AC5: ff-only integration reaps the worker/worktree/branch and records exact SHAs', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:268:  const beforeSha = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:269:  const { coordinator, coordination, log, handle } = await completedTask(root);\nimpl/test/phase11-acceptance-integration.test.mjs:273:  const response = await coordinator.integrate(handle.id, { strategy: 'ff-only', actor: 'test-orchestrator' });\nimpl/test/phase11-acceptance-integration.test.mjs:276:  assert.equal(response.integration.beforeSha, beforeSha);\nimpl/test/phase11-acceptance-integration.test.mjs:277:  assert.equal(response.integration.afterSha, response.integration.resultSha);\nimpl/test/phase11-acceptance-integration.test.mjs:278:  assert.equal(git(['rev-parse', 'HEAD'], root), response.integration.resultSha);\nimpl/test/phase11-acceptance-integration.test.mjs:282:  assert.equal(git(['branch', '--list', 'baton/integrate-me'], root), '');\nimpl/test/phase11-acceptance-integration.test.mjs:283:  assert.equal(git(['for-each-ref', '--format=%(refname)', `refs/baton/results/${response.integration.resultSha}`], root), '');\nimpl/test/phase11-acceptance-integration.test.mjs:284:  assert.deepEqual((await coordinator.result(handle.id)).integration, response.integration);\nimpl/test/phase11-acceptance-integration.test.mjs:285:  const event = log.read(handle.id).find((entry) => entry.kind === 'integration.completed');\nimpl/test/phase11-acceptance-integration.test.mjs:287:  assert.equal(event.payload.afterSha, response.integration.afterSha);\nimpl/test/phase11-acceptance-integration.test.mjs:288:  assert.equal(coordination.snapshot().artifacts.some((artifact) => artifact.mediaType === 'application/vnd.baton.integration+json'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:289:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.completed'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:291:  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'integration'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:297:  const { coordinator, coordination, log, handle } = await completedTask(root, 'diverged-task');\nimpl/test/phase11-acceptance-integration.test.mjs:299:  git(['add', 'main-only.txt'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:300:  git(['commit', '-q', '-m', 'advance main independently'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:301:  const mainBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:302:  const taskSha = (await coordinator.result(handle.id)).integration?.resultSha\nimpl/test/phase11-acceptance-integration.test.mjs:305:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:309:  assert.equal(git(['rev-parse', 'HEAD'], root), mainBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:311:  assert.equal(git(['show', '-s', '--format=%s', 'HEAD'], root), 'advance main independently');\nimpl/test/phase11-acceptance-integration.test.mjs:312:  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\nimpl/test/phase11-acceptance-integration.test.mjs:313:  const refusedResult = await coordinator.result(handle.id);\nimpl/test/phase11-acceptance-integration.test.mjs:314:  assert.equal(refusedResult.integration, null);\nimpl/test/phase11-acceptance-integration.test.mjs:316:  assert.equal(git(['show-ref', '--verify', refusedResult.retainedResultRef], root).split(' ')[0], taskSha);\nimpl/test/phase11-acceptance-integration.test.mjs:317:  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.refused'), true);\nimpl/test/phase11-acceptance-integration.test.mjs:323:  const { coordinator, log, handle } = await completedTask(root, 'dirty-main-task');\nimpl/test/phase11-acceptance-integration.test.mjs:325:  const headBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:326:  const statusBefore = git(['status', '--porcelain=v1'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:328:  await assert.rejects(coordinator.integrate(handle.id), IntegrationError);\nimpl/test/phase11-acceptance-integration.test.mjs:329:  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:330:  assert.equal(git(['status', '--porcelain=v1'], root), statusBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:331:  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\nimpl/test/phase11-acceptance-integration.test.mjs:332:  const retained = (await coordinator.result(handle.id)).retainedResultRef;\nimpl/test/phase11-acceptance-integration.test.mjs:333:  assert.equal(git(['show-ref', '--verify', retained], root).endsWith(` ${retained}`), true);\nimpl/test/phase11-acceptance-integration.test.mjs:336:test('AC5: integration refuses an unaccepted captured result', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:339:  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\nimpl/test/phase11-acceptance-integration.test.mjs:341:    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-reject-log-')), adapters: { mock: adapter },\nimpl/test/phase11-acceptance-integration.test.mjs:344:  const handle = await coordinator.spawn('mock', brief({ command: 'false', expectExit: 0 }), { taskId: 'rejected-result' });\nimpl/test/phase11-acceptance-integration.test.mjs:345:  await until(async () => (await coordinator.result(handle.id)).ready);\nimpl/test/phase11-acceptance-integration.test.mjs:346:  assert.equal((await coordinator.result(handle.id)).status, 'failed');\nimpl/test/phase11-acceptance-integration.test.mjs:347:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:353:test('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:356:  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\nimpl/test/phase11-acceptance-integration.test.mjs:357:  const headBefore = git(['rev-parse', 'HEAD'], root);\nimpl/test/phase11-acceptance-integration.test.mjs:361:    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:364:  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-acceptance-integration.test.mjs:365:  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\nimpl/test/phase11-acceptance-integration.test.mjs:368:  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\nimpl/test/phase11-acceptance-integration.test.mjs:371:test('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:372:  const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:376:    remote: 'origin', ref: 'refs/heads/main', sha: (await coordinator.result(handle.id)).integration.afterSha,\nimpl/test/phase11-acceptance-integration.test.mjs:382:  const [a, b] = await Promise.all([\nimpl/test/phase11-acceptance-integration.test.mjs:388:  assert.deepEqual(new Set([a.result, b.result]), new Set(['published', 'already_resolved']));\nimpl/test/phase11-acceptance-integration.test.mjs:389:  assert.equal((await coordinator.result(handle.id)).publication.sha, requested.target.sha);\nimpl/test/phase11-acceptance-integration.test.mjs:392:  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:publish:')), true);\nimpl/test/phase11-acceptance-integration.test.mjs:396:test('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:397:  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:401:    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\nimpl/test/phase11-acceptance-integration.test.mjs:404:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:413:test('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:414:  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:421:  await assert.rejects(\nimpl/test/phase11-acceptance-integration.test.mjs:431:test('AC6: deny and timeout are fail-closed and never call the publisher', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:433:  const denied = await integratedPublicationTask({ now: () => now, approvalTimeoutMs: 10 });\nimpl/test/phase11-acceptance-integration.test.mjs:435:  const deniedResult = await denied.coordinator.respond(explicit.requestId, { decision: 'deny', fence: explicit.fence }, 'test-user');\nimpl/test/phase11-acceptance-integration.test.mjs:442:  await until(() => denied.log.read(denied.handle.id).some((entry) => entry.kind === 'publication.denied' && entry.payload.requestId === timed.requestId));\nimpl/test/phase11-acceptance-integration.test.mjs:451:  const { coordinator, handle, calls } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:454:  const stale = await coordinator.respond(oldRequest.requestId, { decision: 'allow', fence: oldRequest.fence }, 'test-user');\nimpl/test/phase11-acceptance-integration.test.mjs:457:  await coordinator.respond(currentRequest.requestId, { decision: 'deny', fence: currentRequest.fence }, 'test-user');\nimpl/test/phase11-acceptance-integration.test.mjs:461:test('AC6: restart drops a pending approval and cannot publish it by replay', async () => {\nimpl/test/phase11-acceptance-integration.test.mjs:462:  const first = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:467:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-acceptance-integration.test.mjs:468:    publisher: async (target) => { replayCalls.push(target); }, watchdog: { stallMs: 0 },\nimpl/test/phase11-acceptance-integration.test.mjs:470:  const response = await replay.coordinator.respond(request.requestId, { decision: 'allow', fence: request.fence }, 'test-user');\nimpl/test/phase11-acceptance-integration.test.mjs:476:  const { coordinator, log, handle } = await integratedPublicationTask();\nimpl/test/phase11-acceptance-integration.test.mjs:479:    () => coordinator.requestPublication(handle.id, { remote: 'https://token@example.com/repo.git', ref: 'refs/heads/main' }),\nimpl/test/phase11-persistent-sessions.test.mjs:26:    goal: 'persistent task', constraints: [], pathScope: ['**'], definitionOfDone: 'true remains true',\nimpl/test/phase11-persistent-sessions.test.mjs:36:    sessions: { multiTurn: 'native', resume: 'native', fork: 'native' },\nimpl/test/phase11-persistent-sessions.test.mjs:40:function adapter(over = {}) {\nimpl/test/phase11-persistent-sessions.test.mjs:64:    log, fences: new FenceTable(), adapters: { session: ad }, coordination,\nimpl/test/phase11-persistent-sessions.test.mjs:81:    const value = await fn();\nimpl/test/phase11-persistent-sessions.test.mjs:83:    await sleep(5);\nimpl/test/phase11-persistent-sessions.test.mjs:89:  return { status: 'completed', summary, artifacts: { files: [] }, verification: { command: 'true', claimedExit: 0 } };\nimpl/test/phase11-persistent-sessions.test.mjs:92:test('PS1/PS4: a verified worker accepts a public follow-up turn and verifies it independently', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:93:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:95:  const h = await c.spawn('session', brief(), { taskId: 'follow-up' });\nimpl/test/phase11-persistent-sessions.test.mjs:96:  ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'session-1', pid: 123 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:98:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:101:  const ack = await c.send(h.id, 'second turn', 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:104:  assert.equal((await c.result(h.id)).ready, false);\nimpl/test/phase11-persistent-sessions.test.mjs:109:  await until(async () => (await c.result(h.id)).ready && verifyCalls.length === 2);\nimpl/test/phase11-persistent-sessions.test.mjs:110:  assert.equal((await c.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:113:  assert.equal(coordination.snapshot().tasks[1].refines, 'follow-up');\nimpl/test/phase11-persistent-sessions.test.mjs:116:    log, fences: new FenceTable(), adapters: { session: adapter() }, coordination,\nimpl/test/phase11-persistent-sessions.test.mjs:121:  assert.equal((await replay.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:122:  assert.equal((await replay.result(h.id)).ready, true);\nimpl/test/phase11-persistent-sessions.test.mjs:125:test('PS2: a refused follow-up restores the previous terminal result and logs no new turn', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:126:  const ad = adapter({ prompt: async () => ({ ok: false, reason: 'busy' }) });\nimpl/test/phase11-persistent-sessions.test.mjs:128:  const h = await c.spawn('session', brief(), { taskId: 'follow-refused' });\nimpl/test/phase11-persistent-sessions.test.mjs:130:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:132:  const ack = await c.send(h.id, 'try again', 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:134:  assert.equal(ad.calls.prompt.length, 1, 'refusal must come from an attempted native follow-up, not the old idle guard');\nimpl/test/phase11-persistent-sessions.test.mjs:135:  assert.equal((await c.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:140:test('PS2: a follow-up exception is a refused Ack and preserves the prior result', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:141:  const ad = adapter({ prompt: async () => { throw new Error('wire write failed'); } });\nimpl/test/phase11-persistent-sessions.test.mjs:143:  const h = await c.spawn('session', brief(), { taskId: 'follow-throws' });\nimpl/test/phase11-persistent-sessions.test.mjs:145:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:146:  const ack = await c.send(h.id, 'try again', 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:148:  assert.equal((await c.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:152:test('CK8/CK9: follow-up refinement failure kills an already-advanced native session', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:153:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:155:  const h = await c.spawn('session', brief(), { taskId: 'follow-refinement-failure' });\nimpl/test/phase11-persistent-sessions.test.mjs:157:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:160:    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('follow-up refinement disk full');\nimpl/test/phase11-persistent-sessions.test.mjs:163:  await assert.rejects(c.send(h.id, 'advance native state', 'turn'), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:174:  ad = adapter({\nimpl/test/phase11-persistent-sessions.test.mjs:181:  const h = await c.spawn('session', brief(), { taskId: 'follow-contradiction' });\nimpl/test/phase11-persistent-sessions.test.mjs:183:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:184:  const ack = await c.send(h.id, 'try again', 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:187:  assert.equal((await c.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:191:test('PS3: interrupt-with-follow-up reopens coordinator state before the adapter follow-up terminal', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:192:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:194:  const h = await c.spawn('session', brief(), { taskId: 'interrupt-follow' });\nimpl/test/phase11-persistent-sessions.test.mjs:197:  await stopping;\nimpl/test/phase11-persistent-sessions.test.mjs:199:  assert.equal((await c.result(h.id)).ready, false);\nimpl/test/phase11-persistent-sessions.test.mjs:202:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:204:  assert.equal((await c.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:207:test('PS4: a late terminal from the prior wire epoch cannot overwrite the active follow-up', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:208:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:210:  const h = await c.spawn('session', brief(), { taskId: 'stale-terminal' });\nimpl/test/phase11-persistent-sessions.test.mjs:212:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:213:  await c.send(h.id, 'next', 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:216:  await sleep(20);\nimpl/test/phase11-persistent-sessions.test.mjs:218:  assert.equal((await c.result(h.id)).ready, false);\nimpl/test/phase11-persistent-sessions.test.mjs:219:  assert.ok(log.read(h.id).some((e) => e.kind === 'control.stale_rejected' && e.payload?.op === 'terminal'));\nimpl/test/phase11-persistent-sessions.test.mjs:222:test('PS5: wire sessionRef reaches handle/result and survives terminal replay', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:223:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:225:  const h = await c.spawn('session', brief(), { taskId: 'session-ref' });\nimpl/test/phase11-persistent-sessions.test.mjs:226:  ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'native-session-7', pid: 123 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:228:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:231:  assert.deepEqual((await c.result(h.id)).sessionRef, expected);\nimpl/test/phase11-persistent-sessions.test.mjs:234:    log, coordination, fences: new FenceTable(), adapters: { session: adapter() },\nimpl/test/phase11-persistent-sessions.test.mjs:241:  assert.deepEqual(await replay.send(h.id, 'must not reach an unattached adapter', 'turn'), { ok: false, result: 'worker_not_active' });\nimpl/test/phase11-persistent-sessions.test.mjs:242:  assert.equal((await replay.result(h.id)).status, 'completed');\nimpl/test/phase11-persistent-sessions.test.mjs:259:      log, coordination, fences: new FenceTable(), adapters: { [name]: ad },\nimpl/test/phase11-persistent-sessions.test.mjs:270:    t.after(async () => { if (workerId) await c.kill(workerId, 'policy').catch(() => {}); });\nimpl/test/phase11-persistent-sessions.test.mjs:272:    const h = await c.spawn(name, brief(), { taskId: `two-turn-${name}` });\nimpl/test/phase11-persistent-sessions.test.mjs:274:    await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:276:    const pid = firstSpawn?.payload?.pid;\nimpl/test/phase11-persistent-sessions.test.mjs:278:    assert.ok(pid && sessionRef, `${name}: first turn must expose native PID/session identity`);\nimpl/test/phase11-persistent-sessions.test.mjs:280:    const follow = await c.send(h.id, `follow-up for ${name}`, 'turn');\nimpl/test/phase11-persistent-sessions.test.mjs:281:    assert.equal(follow.ok, true, `${name}: public follow-up accepted`);\nimpl/test/phase11-persistent-sessions.test.mjs:282:    await until(async () => (await c.result(h.id)).ready && log.read(h.id).filter((e) => e.kind === 'verify.reverified').length === 2);\nimpl/test/phase11-persistent-sessions.test.mjs:284:    const pids = new Set(log.read(h.id).filter((e) => e.kind === 'lifecycle.spawned' && e.actor === 'worker').map((e) => e.payload?.pid).filter(Boolean));\nimpl/test/phase11-persistent-sessions.test.mjs:285:    assert.deepEqual([...pids], [pid], `${name}: second turn must not respawn`);\nimpl/test/phase11-persistent-sessions.test.mjs:287:    const stopped = await c.kill(h.id, 'policy');\nimpl/test/phase11-persistent-sessions.test.mjs:293:test('PS6: unsupported resume/fork policy fails typed before worker allocation', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:294:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:295:  ad.card = () => ({ ...card(), sessions: { multiTurn: 'native', resume: 'native', fork: 'planned' } });\nimpl/test/phase11-persistent-sessions.test.mjs:297:  await assert.rejects(\nimpl/test/phase11-persistent-sessions.test.mjs:304:test('PS6: Claude maps per-task resume and fork to native session identities', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:305:  for (const mode of ['resume', 'fork']) {\nimpl/test/phase11-persistent-sessions.test.mjs:311:    const ack = await cli.spawn(worker, brief(), { worktree: tmpdir(), session: { mode, id: 'claude-parent' } });\nimpl/test/phase11-persistent-sessions.test.mjs:313:    const spawned = await until(() => events.find((e) => e.kind === 'lifecycle.spawned'));\nimpl/test/phase11-persistent-sessions.test.mjs:318:test('PS6: Codex maps resume and fork to thread/resume and thread/fork', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:319:  for (const mode of ['resume', 'fork']) {\nimpl/test/phase11-persistent-sessions.test.mjs:325:    const ack = await cli.spawn(worker, brief(), {\nimpl/test/phase11-persistent-sessions.test.mjs:330:    const spawned = await until(() => events.find((e) => e.kind === 'lifecycle.spawned'));\nimpl/test/phase11-persistent-sessions.test.mjs:335:test('PS6: Grok maps resume to ACP session/load', async (t) => {\nimpl/test/phase11-persistent-sessions.test.mjs:339:  t.after(() => cli.kill('grok-resume').catch(() => {}));\nimpl/test/phase11-persistent-sessions.test.mjs:340:  const ack = await cli.spawn('grok-resume', brief(), { worktree: tmpdir(), session: { mode: 'resume', id: 'grok-parent' } });\nimpl/test/phase11-persistent-sessions.test.mjs:342:  const spawned = await until(() => events.find((e) => e.kind === 'lifecycle.spawned'));\nimpl/test/phase11-persistent-sessions.test.mjs:346:test('PS8: coordinator resume requires and reuses a validated worktree without creating another', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:347:  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-resume-wt-'));\nimpl/test/phase11-persistent-sessions.test.mjs:348:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:353:    create: async () => { creates += 1; throw new Error('resume must not create'); },\nimpl/test/phase11-persistent-sessions.test.mjs:357:  await assert.rejects(\nimpl/test/phase11-persistent-sessions.test.mjs:358:    () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session' } }),\nimpl/test/phase11-persistent-sessions.test.mjs:362:  await assert.rejects(\nimpl/test/phase11-persistent-sessions.test.mjs:363:    () => c.spawn('session', brief(), { session: { mode: 'resume', id: 'external-session', context: { worktree: wt } } }),\nimpl/test/phase11-persistent-sessions.test.mjs:369:  const h = await c.spawn('session', brief(), {\nimpl/test/phase11-persistent-sessions.test.mjs:370:    taskId: 'resumed-task', refines: 'original-task', session: { mode: 'resume', id: 'external-session', context },\nimpl/test/phase11-persistent-sessions.test.mjs:372:  await until(() => spawnOpts);\nimpl/test/phase11-persistent-sessions.test.mjs:374:  assert.equal((await spawnOpts.worktreeReady).path, wt);\nimpl/test/phase11-persistent-sessions.test.mjs:375:  assert.deepEqual(h.lineage, { relation: 'resume', parentSessionId: 'external-session', parentTaskId: 'original-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:382:  const ad = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:389:  const h = await c.spawn('session', brief(), {\nimpl/test/phase11-persistent-sessions.test.mjs:390:    taskId: 'forked-task', refines: 'parent-task', session: { mode: 'fork', id: 'parent-session', lastTurnId: 'parent-turn' },\nimpl/test/phase11-persistent-sessions.test.mjs:392:  await until(() => spawnOpts && c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:394:  assert.equal((await spawnOpts.worktreeReady).path, wt);\nimpl/test/phase11-persistent-sessions.test.mjs:401:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:405:  const h = await c.spawn('session', brief(), { taskId: 'recover-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:406:  await until(() => c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:407:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:409:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:411:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:412:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:413:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'recover-native', pid: 222 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:414:    resumed.emit(worker, 'lifecycle.turn_started', { sessionId: 'recover-native' }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:418:    log, coordination, fences: new FenceTable(), adapters: { session: resumed }, repoRoot: null,\nimpl/test/phase11-persistent-sessions.test.mjs:425:    approvalTimeoutMs: 1000, stopDeadlineMs: 100, recoveryTimeoutMs: 100,\nimpl/test/phase11-persistent-sessions.test.mjs:428:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:433:  assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_attached'));\nimpl/test/phase11-persistent-sessions.test.mjs:436:test('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:437:  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\nimpl/test/phase11-persistent-sessions.test.mjs:438:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:440:  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:441:  await until(() => c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:442:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:444:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:446:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:448:  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\nimpl/test/phase11-persistent-sessions.test.mjs:450:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:452:    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\nimpl/test/phase11-persistent-sessions.test.mjs:456:    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\nimpl/test/phase11-persistent-sessions.test.mjs:459:  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:464:test('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:465:  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-refine-wt-'));\nimpl/test/phase11-persistent-sessions.test.mjs:466:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:468:  const h = await c.spawn('session', brief(), { taskId: 'recover-refine-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:469:  await until(() => c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:470:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:472:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:474:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:475:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:476:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:480:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:482:    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\nimpl/test/phase11-persistent-sessions.test.mjs:486:    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('recovery refinement disk full');\nimpl/test/phase11-persistent-sessions.test.mjs:489:  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-persistent-sessions.test.mjs:490:  await until(() => resumed.calls.kill.length === 1);\nimpl/test/phase11-persistent-sessions.test.mjs:496:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:498:  const h = await c.spawn('session', brief(), { taskId: 'recover-bad-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:499:  await until(() => c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:500:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'expected-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:502:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:504:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:505:  resumed.spawn = async (worker) => {\nimpl/test/phase11-persistent-sessions.test.mjs:506:    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'wrong-native', pid: 333 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:510:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:512:    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\nimpl/test/phase11-persistent-sessions.test.mjs:514:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:518:  assert.equal(resumed.calls.kill.length, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:519:  assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_failed'));\nimpl/test/phase11-persistent-sessions.test.mjs:522:test('PS7: a hung reattachment is bounded, remains orphaned, and invokes adapter cleanup', async () => {\nimpl/test/phase11-persistent-sessions.test.mjs:524:  const original = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:526:  const h = await c.spawn('session', brief(), { taskId: 'recover-timeout-task' });\nimpl/test/phase11-persistent-sessions.test.mjs:527:  await until(() => c.list()[0].sessionContext);\nimpl/test/phase11-persistent-sessions.test.mjs:528:  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'timeout-native', pid: 111 }, 1);\nimpl/test/phase11-persistent-sessions.test.mjs:530:  await until(async () => (await c.result(h.id)).ready);\nimpl/test/phase11-persistent-sessions.test.mjs:532:  const resumed = adapter();\nimpl/test/phase11-persistent-sessions.test.mjs:533:  resumed.spawn = async () => new Promise(() => {});\nimpl/test/phase11-persistent-sessions.test.mjs:535:    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\nimpl/test/phase11-persistent-sessions.test.mjs:537:    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 20, stopDeadlineMs: 100,\nimpl/test/phase11-persistent-sessions.test.mjs:539:  const recovered = await replay.recover(h.id);\nimpl/test/phase11-persistent-sessions.test.mjs:541:  assert.equal(recovered.result, 'recovery_timeout');\nimpl/test/phase11-persistent-sessions.test.mjs:543:  assert.equal(resumed.calls.kill.length, 1);\nimpl/test/phase11-coordination-store.test.mjs:15:const fields = (id, deps = []) => ({ id, brief: { goal: id }, deps, refines: null, taskType: 'test', reservedWorkerId: `w-${id}` });\nimpl/test/phase11-coordination-store.test.mjs:19:    const value = await fn();\nimpl/test/phase11-coordination-store.test.mjs:21:    await new Promise((resolve) => setTimeout(resolve, 10));\nimpl/test/phase11-coordination-store.test.mjs:40:test('CK1: append failure is fatal and leaves event/task projections unchanged', () => {\nimpl/test/phase11-coordination-store.test.mjs:43:  assert.deepEqual(store.snapshot(), { tasks: [], artifacts: [], evidence: [], scratch: { facts: [], claims: [], reads: [] }, knowledge: { nodes: [], edges: [], reads: [], contamination: [] }, lastSeq: 0 });\nimpl/test/phase11-coordination-store.test.mjs:46:test('CK8/CK9: public spawn poisons before publishing any handle when task creation cannot append', async () => {\nimpl/test/phase11-coordination-store.test.mjs:48:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:50:  const driver = createDriver({ repoRoot: repo, logDir: dir(), coordination, adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:51:  const brief = { goal: 'must not publish', constraints: [], pathScope: [], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:52:  await assert.rejects(driver.coordinator.spawn('mock', brief, { taskId: 'create-write-failure' }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:57:test('CK8/CK9: claim append failure poisons before adapter/worktree dispatch and replays pending', async () => {\nimpl/test/phase11-coordination-store.test.mjs:59:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:62:      if (body.includes('\"task.claimed\"')) throw new Error('claim disk full');\nimpl/test/phase11-coordination-store.test.mjs:66:  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\nimpl/test/phase11-coordination-store.test.mjs:69:    log: new Log(dir()), fences: new FenceTable(), adapters: { mock: adapter }, coordination,\nimpl/test/phase11-coordination-store.test.mjs:74:  await assert.rejects(coordinator.spawn('mock', brief, { taskId: 'claim-write-failure' }), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:75:  assert.equal(coordination.task('claim-write-failure').status, 'pending');\nimpl/test/phase11-coordination-store.test.mjs:76:  assert.equal(coordination.task('claim-write-failure').assignee, null);\nimpl/test/phase11-coordination-store.test.mjs:81:test('CK1/CK9: operational append failure poisons the coordinator and restart closes the claimed task', async () => {\nimpl/test/phase11-coordination-store.test.mjs:89:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-coordination-store.test.mjs:96:  const poisoned = make(failedLog);\nimpl/test/phase11-coordination-store.test.mjs:98:  await assert.rejects(poisoned.spawn('mock', brief, { taskId: 'append-failure' }), (error) => error.code === 'operational_log_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:99:  assert.equal(coordination.task('append-failure').status, 'working', 'the durable claim records the exact crash window');\nimpl/test/phase11-coordination-store.test.mjs:100:  assert.throws(() => poisoned.list(), (error) => error.code === 'operational_log_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:107:test('CK1/CK9: terminal artifact-batch failure poisons the driver and restarts as durable failed', async () => {\nimpl/test/phase11-coordination-store.test.mjs:109:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:110:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:111:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:113:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:114:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:116:  const make = (adapters) => createDriver({ repoRoot: repo, logDir, adapters, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:123:      throw new Error('terminal batch disk full');\nimpl/test/phase11-coordination-store.test.mjs:127:  const brief = { goal: 'atomic terminal', constraints: [], pathScope: ['atomic.txt'], definitionOfDone: 'atomic', verification: { command: 'test -s atomic.txt', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\nimpl/test/phase11-coordination-store.test.mjs:128:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'terminal-batch-failure' });\nimpl/test/phase11-coordination-store.test.mjs:129:  await until(() => driver.coordination.task('terminal-batch-failure')?.status === 'failed');\nimpl/test/phase11-coordination-store.test.mjs:135:  assert.equal(replay.coordination.task('terminal-batch-failure').status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:136:  assert.equal((await replay.coordinator.result(handle.id)).status, 'failed');\nimpl/test/phase11-coordination-store.test.mjs:168:test('CK2: claim CAS has one winner; stale/blocked refusals append nothing', () => {\nimpl/test/phase11-coordination-store.test.mjs:171:  const winner = store.claimTask('a', 'w1', 1, { actor: 'orchestrator', key: 'claim-a' });\nimpl/test/phase11-coordination-store.test.mjs:174:  assert.throws(() => store.claimTask('a', 'w2', 1, { actor: 'orchestrator', key: 'claim-b' }), (error) => error instanceof CoordinationRefusal && error.code === 'stale_version');\nimpl/test/phase11-coordination-store.test.mjs:178:test('CK2: terminal state is immutable across replay', () => {\nimpl/test/phase11-coordination-store.test.mjs:182:  store.claimTask('a', 'w1', 1, { actor: 'orchestrator', key: 'claim' });\nimpl/test/phase11-coordination-store.test.mjs:184:  assert.throws(() => store.transitionTask('a', 'working', 3, { actor: 'x', key: 'reopen' }), (error) => error.code === 'terminal');\nimpl/test/phase11-coordination-store.test.mjs:190:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:191:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:192:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:194:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:195:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:199:    adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 }, scenario: { outcome: 'completed' } }) },\nimpl/test/phase11-coordination-store.test.mjs:204:  const base = await first.coordinator.spawn('mock', brief, { taskId: 'base' });\nimpl/test/phase11-coordination-store.test.mjs:205:  const child = await first.coordinator.spawn('mock', brief, { taskId: 'child', deps: ['base'] });\nimpl/test/phase11-coordination-store.test.mjs:219:test('CK2/CK9: restart terminalizes a durable claim that crashed before operational spawn', () => {\nimpl/test/phase11-coordination-store.test.mjs:221:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:222:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:223:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:225:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:226:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:230:  const task = fields('claimed-before-spawn');\nimpl/test/phase11-coordination-store.test.mjs:232:  coordination.claimTask(task.id, task.reservedWorkerId, 1, { actor: 'orchestrator', key: 'claim-crash-task' });\nimpl/test/phase11-coordination-store.test.mjs:238:    adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) },\nimpl/test/phase11-coordination-store.test.mjs:244:  assert.equal(replay.log.workers().length, 0, 'recovery must not invent a lifecycle.spawned event');\nimpl/test/phase11-coordination-store.test.mjs:245:  assert.equal(replay.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'recovery.claimed_without_spawn'), true);\nimpl/test/phase11-coordination-store.test.mjs:265:  const claimed = store.claimTask('a', 'w-a', 1, { actor: 'orchestrator', key: 'claim-a-for-artifacts' });\nimpl/test/phase11-coordination-store.test.mjs:271:  store.transitionTask('a', 'completed', claimed.task.version, { actor: 'policy', key: 'complete-a-for-artifacts' }, mapped.evidence);\nimpl/test/phase11-coordination-store.test.mjs:287:test('CK3/CK9: terminal task and accepted manifests commit in one append batch', () => {\nimpl/test/phase11-coordination-store.test.mjs:299:  const claimed = store.claimTask('atomic', 'w-atomic', 1, { actor: 'orchestrator', key: 'claim-atomic' });\nimpl/test/phase11-coordination-store.test.mjs:306:  assert.throws(() => store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence), /batch disk full/);\nimpl/test/phase11-coordination-store.test.mjs:311:  const completed = store.transitionTaskWithArtifacts('atomic', 'completed', claimed.task.version, manifests, { actor: 'policy', key: 'atomic-terminal' }, mapped.evidence);\nimpl/test/phase11-coordination-store.test.mjs:317:test('CK8/CK9: completed public task maps verification evidence, terminal state, and manifests', async () => {\nimpl/test/phase11-coordination-store.test.mjs:319:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:320:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:321:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:323:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:324:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:327:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'done.txt', content: 'done\\n' }] } }) },\nimpl/test/phase11-coordination-store.test.mjs:331:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'durable-complete' });\nimpl/test/phase11-coordination-store.test.mjs:332:  await until(async () => (await driver.coordinator.result(handle.id)).ready);\nimpl/test/phase11-coordination-store.test.mjs:338:  assert.equal(snapshot.artifacts.find((artifact) => artifact.kind === 'report').accepted, false, 'worker artifact claims stay explicitly untrusted');\nimpl/test/phase11-coordination-store.test.mjs:352:test('CK2/CK8: blocking input and resolution transition durably before terminal verification', async () => {\nimpl/test/phase11-coordination-store.test.mjs:354:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:355:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:356:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:358:  execFileSync('git', ['add', '.'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:359:  execFileSync('git', ['commit', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:362:    adapters: { mock: new MockAdapter({ scenario: {\nimpl/test/phase11-coordination-store.test.mjs:368:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'durable-input' });\nimpl/test/phase11-coordination-store.test.mjs:369:  await until(() => driver.coordinator.list()[0]?.pendingQuestionId);\nimpl/test/phase11-coordination-store.test.mjs:372:  await driver.coordinator.respond(requestId, { text: 'yes' }, 'human');\nimpl/test/phase11-coordination-store.test.mjs:374:  await until(async () => (await driver.coordinator.result(handle.id)).ready);\nimpl/test/phase11-coordination-store.test.mjs:378:test('CK8/CK9: input transition append failure poisons before pending/blocked state becomes visible', async () => {\nimpl/test/phase11-coordination-store.test.mjs:380:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:381:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:382:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:383:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:384:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:385:  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:387:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'input-write-failure' });\nimpl/test/phase11-coordination-store.test.mjs:400:  await adapter.kill(handle.id);\nimpl/test/phase11-coordination-store.test.mjs:403:test('CK8/CK9: stop-intent append failure calls no adapter and leaves durable task working', async () => {\nimpl/test/phase11-coordination-store.test.mjs:405:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:406:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:407:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:408:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:409:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:410:  let adapterKills = 0;\nimpl/test/phase11-coordination-store.test.mjs:411:  const rawKill = adapter.kill.bind(adapter);\nimpl/test/phase11-coordination-store.test.mjs:412:  adapter.kill = async (...args) => { adapterKills += 1; return rawKill(...args); };\nimpl/test/phase11-coordination-store.test.mjs:413:  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:415:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'stop-write-failure' });\nimpl/test/phase11-coordination-store.test.mjs:418:    if (body.includes('\"kind\":\"control.stop_requested\"')) throw new Error('stop intent disk full');\nimpl/test/phase11-coordination-store.test.mjs:421:  await assert.rejects(driver.coordinator.kill(handle.id, 'human'), (error) => error.code === 'coordination_write_unavailable');\nimpl/test/phase11-coordination-store.test.mjs:422:  assert.equal(adapterKills, 0);\nimpl/test/phase11-coordination-store.test.mjs:426:  await rawKill(handle.id);\nimpl/test/phase11-coordination-store.test.mjs:429:test('CK8/CK9: cancellation completion failure resolves bounded, keeps stop intent, and restart closes task', async () => {\nimpl/test/phase11-coordination-store.test.mjs:431:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:432:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:433:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:434:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:436:  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\nimpl/test/phase11-coordination-store.test.mjs:437:  const driver = createDriver({ repoRoot: repo, logDir, adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:439:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'cancel-completion-failure' });\nimpl/test/phase11-coordination-store.test.mjs:445:  const stopped = await driver.coordinator.kill(handle.id, 'human');\nimpl/test/phase11-coordination-store.test.mjs:452:  const replay = createDriver({ repoRoot: repo, logDir, coordination: driver.coordination, adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) }, watchdog: { stallMs: 0 } });\nimpl/test/phase11-coordination-store.test.mjs:456:test('CK4: Scratch claims conservatively conflict, warn cross-tree, and expire only by event', () => {\nimpl/test/phase11-coordination-store.test.mjs:461:  const first = store.claimScratch({ resource: 'path:payments/**', ownerWorker: 'w1', ownerTask: 't1', intent: 'edit', envRef: envA, fence: 3, leaseDeadline: 'later' }, { actor: 'w1', key: 'claim-a' });\nimpl/test/phase11-coordination-store.test.mjs:462:  assert.equal(store.claimScratch({ resource: 'changed', envRef: envB }, { actor: 'w1', key: 'claim-a' }).result, 'idempotent');\nimpl/test/phase11-coordination-store.test.mjs:463:  const conflict = store.claimScratch({ resource: 'path:payments/stripe.js', ownerWorker: 'w2', ownerTask: 't2', intent: 'edit', envRef: envB, fence: 4, leaseDeadline: 'later' }, { actor: 'w2', key: 'claim-b' });\nimpl/test/phase11-coordination-store.test.mjs:465:  assert.equal(conflict.conflict.id, first.claim.id);\nimpl/test/phase11-coordination-store.test.mjs:466:  const globWitness = store.claimScratch({ resource: '*stripe.js', ownerWorker: 'w3', ownerTask: 't3', intent: 'edit', envRef: envB, fence: 5, leaseDeadline: 'later' }, { actor: 'w3', key: 'claim-glob-witness' });\nimpl/test/phase11-coordination-store.test.mjs:470:  assert.equal(check.claims[0].warning, 'observed on aaaa1111 — not your tree');\nimpl/test/phase11-coordination-store.test.mjs:472:  store.expireScratchClaim(first.claim.id, 1, { actor: 'policy', key: 'expire-a' });\nimpl/test/phase11-coordination-store.test.mjs:555:  execFileSync('git', ['init', '-q'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:556:  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:557:  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:558:  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\nimpl/test/phase11-coordination-store.test.mjs:561:    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } }) },\nimpl/test/phase11-coordination-store.test.mjs:565:  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'durable-cancel' });\nimpl/test/phase11-coordination-store.test.mjs:566:  await until(() => driver.coordinator.list()[0]?.status === 'working');\nimpl/test/phase11-coordination-store.test.mjs:569:  const lease = driver.coordinator.claimScratch(handle.id, {\nimpl/test/phase11-coordination-store.test.mjs:570:    resource: 'path:slow.txt', intent: 'edit', envRef, leaseDeadline: 'terminal',\nimpl/test/phase11-coordination-store.test.mjs:571:  }, { actor: 'orchestrator', expectedFence: active.fence, idempotencyKey: 'claim-slow-file' });\nimpl/test/phase11-coordination-store.test.mjs:572:  assert.equal(driver.coordinator.claimScratch(handle.id, { resource: 'path:other.txt', intent: 'edit', envRef }, { expectedFence: active.fence - 1, idempotencyKey: 'stale-claim' }).result, 'stale_fence');\nimpl/test/phase11-coordination-store.test.mjs:577:  const scratchRead = driver.coordinator.readScratch(handle.id, 'path:slow.txt', envRef, { idempotencyKey: 'read-slow-claim', runId: 'cancel-run' });\nimpl/test/phase11-coordination-store.test.mjs:579:  assert.equal(scratchRead.result.claims[0].ownerWorker, handle.id);\nimpl/test/phase11-coordination-store.test.mjs:581:  const stopped = await driver.coordinator.kill(handle.id, 'human');\nimpl/test/phase11-coordination-store.test.mjs:586:  assert.equal(driver.coordination.snapshot().scratch.claims.find((claim) => claim.id === lease.claim.id).active, false);\nimpl/src/coordinator.mjs:1:// coordinator.mjs — the main loop and the 8 commands (spawn/send/wait/respond/interrupt/\nimpl/src/coordinator.mjs:3:// reliably (fence-checked), enforces two-phase stop, single-consumer approvals, and the\nimpl/src/coordinator.mjs:54:  constructor(message, code = 'integration_refused') {\nimpl/src/coordinator.mjs:84:// SC13: cancellation is terminal too. No late spawn/delivery/turn continuation may revive it.\nimpl/src/coordinator.mjs:87:  'createTask', 'claimTask', 'transitionTask', 'transitionTaskWithArtifacts', 'mapOperationalEvent',\nimpl/src/coordinator.mjs:88:  'recordDriver', 'registerArtifact', 'supersedeArtifact', 'claimScratch', 'postScratchFact',\nimpl/src/coordinator.mjs:206:  if (!['new', 'resume', 'fork'].includes(mode)) {\nimpl/src/coordinator.mjs:260:    for (const method of ['snapshot', 'task', 'createTask', 'claimTask', 'transitionTask', 'transitionTaskWithArtifacts', 'mapOperationalEvent', 'recordDriver', 'registerArtifact', 'artifact', 'claimScratch', 'postScratchFact', 'readScratch', 'activeScratchClaims', 'expireScratchClaim', 'addKnowledgeNode', 'promoteKnowledgeNode', 'readKnowledge']) {\nimpl/src/coordinator.mjs:265:    this._adapters = opts.adapters;\nimpl/src/coordinator.mjs:278:            throw this._poisonCoordination(err);\nimpl/src/coordinator.mjs:290:    this._recoveryTimeoutMs = opts.recoveryTimeoutMs ?? 15000;\nimpl/src/coordinator.mjs:293:    this._budgetTerminalGraceMs = opts.budgetPolicy?.terminalGraceMs ?? 250;\nimpl/src/coordinator.mjs:301:    this._waitPollMs = opts.waitPollMs ?? 25;\nimpl/src/coordinator.mjs:306:    this._publisher = opts.publisher ?? null;\nimpl/src/coordinator.mjs:312:    // warning-and-drop. Once an append fails the coordinator is poisoned: every public command\nimpl/src/coordinator.mjs:354:    /** @type {Map<string, object>} workerId -> stop-waiter bookkeeping */\nimpl/src/coordinator.mjs:364:    this._refinementSeq = 0;\nimpl/src/coordinator.mjs:368:    for (const adapter of Object.values(this._adapters)) {\nimpl/src/coordinator.mjs:369:      adapter.onEvent((e) => {\nimpl/src/coordinator.mjs:372:          // failure has already poisoned this coordinator; do not let it become an uncaught\nimpl/src/coordinator.mjs:387:    this._terminalizeUnattachedCoordinationTasks();\nimpl/src/coordinator.mjs:407:      if (!vendor || !this._adapters[vendor]) continue;\nimpl/src/coordinator.mjs:408:      const card = this._adapters[vendor].card();\nimpl/src/coordinator.mjs:421:    for (const [workerId, waiter] of [...this._stopWaiters]) {\nimpl/src/coordinator.mjs:422:      if (!waiter.finalized && waiter.deadlineAt != null && now >= waiter.deadlineAt) {\nimpl/src/coordinator.mjs:423:        this._forceStop(workerId, waiter);\nimpl/src/coordinator.mjs:431:      if (!cardSupportsSession(this._adapters[vendor]?.card(), task.sessionRequest)) return null;\nimpl/src/coordinator.mjs:432:      const resolved = resolveCardModel(this._adapters[vendor]?.card(), task.modelRequested, task.modelPolicy, { explicit: true });\nimpl/src/coordinator.mjs:437:    for (const [name, ad] of Object.entries(this._adapters)) {\nimpl/src/coordinator.mjs:450:    for (const name of Object.keys(this._adapters)) inFlight[name] = this._inFlightCount(name);\nimpl/src/coordinator.mjs:452:    if (!chosen || !this._adapters[chosen] || !Object.hasOwn(resolvedModels, chosen)) return null;\nimpl/src/coordinator.mjs:465:    const card = this._adapters[vendor]?.card();\nimpl/src/coordinator.mjs:473:      const claim = this._coordination.claimTask(task.id, workerId, task.coordinationVersion, {\nimpl/src/coordinator.mjs:474:        actor: 'orchestrator', key: `task.claimed:${task.id}:${task.coordinationVersion}`,\nimpl/src/coordinator.mjs:476:      task.coordinationVersion = claim.task.version;\nimpl/src/coordinator.mjs:500:    // Create the worktree; the returned readiness promise is handed to the adapter so the\nimpl/src/coordinator.mjs:501:    // worker waits for its checkout to exist before touching disk. Status still flips to\nimpl/src/coordinator.mjs:502:    // 'working' and the adapter is invoked synchronously below (so a bare tick() dispatches\nimpl/src/coordinator.mjs:504:    const worktreeSource = task.sessionRequest?.mode === 'resume'\nimpl/src/coordinator.mjs:532:        // late worktree exists, reap again while the adapter's cancelled reservation prevents\nimpl/src/coordinator.mjs:536:            await this._removeTaskWorktree(task);\nimpl/src/coordinator.mjs:558:    // SC12: adapters receive an explicit cancellation signal in addition to their verb call.\nimpl/src/coordinator.mjs:559:    // Session adapters own the stronger pending-spawn reservation, while this signal makes the\nimpl/src/coordinator.mjs:565:    Promise.resolve(this._adapters[vendor].spawn(workerId, task.brief, {\nimpl/src/coordinator.mjs:598:   * terminal-transitions on it; payload phase:'spawn' says exactly what died and when. Skipped\nimpl/src/coordinator.mjs:599:   * if an adapter event already ended the worker (both paths racing is benign). */\nimpl/src/coordinator.mjs:601:    // SC13: a concurrent stop or earlier lifecycle terminal owns the outcome. Refusal is allowed\nimpl/src/coordinator.mjs:636:    if (vendor !== 'auto' && !this._adapters[vendor]) throw new UnknownVendorError(`unknown vendor \"${vendor}\"`);\nimpl/src/coordinator.mjs:637:    if (vendor !== 'auto' && !cardSupportsSession(this._adapters[vendor].card(), sessionRequest)) {\nimpl/src/coordinator.mjs:641:      const resolved = resolveCardModel(this._adapters[vendor].card(), opts.model, modelPolicy, { explicit: true });\nimpl/src/coordinator.mjs:646:      const anyCapable = Object.values(this._adapters).some((ad) => resolveCardModel(ad.card(), opts.model, modelPolicy, { explicit: false }).ok);\nimpl/src/coordinator.mjs:650:      const anySessionCapable = Object.values(this._adapters).some((ad) => cardSupportsSession(ad.card(), sessionRequest));\nimpl/src/coordinator.mjs:657:    if (sessionRequest.mode === 'resume') {\nimpl/src/coordinator.mjs:663:        throw new SessionSelectionError('resume requires session.context.worktree and ownerTaskId', 'session_context_required');\nimpl/src/coordinator.mjs:668:      await this._validateSessionContext(sessionRequest.context);\nimpl/src/coordinator.mjs:678:        id: taskId, brief: admittedBrief, deps, refines: opts.refines ?? null,\nimpl/src/coordinator.mjs:695:      sessionContext: sessionRequest.mode === 'resume' ? sessionRequest.context : null,\nimpl/src/coordinator.mjs:699:        parentTaskId: opts.refines ?? this._knownSessionContext(sessionRequest.id, vendor)?.handle?.taskId ?? null,\nimpl/src/coordinator.mjs:701:      refines: opts.refines ?? null,\nimpl/src/coordinator.mjs:708:      integration: null,\nimpl/src/coordinator.mjs:765:        sessionContext: null, lineage: null, refines: durable.refines ?? null,\nimpl/src/coordinator.mjs:767:        capturedSha: null, integration: null, retainedResultRef: null, publication: null,\nimpl/src/coordinator.mjs:800:    if (vendor === 'auto' || !this._adapters[vendor]) {\nimpl/src/coordinator.mjs:807:    const parentFamily = this._adapters[parentHandle.vendor]?.card()?.modelSelection?.family ?? parentHandle.vendor;\nimpl/src/coordinator.mjs:808:    const reviewerFamily = this._adapters[vendor].card()?.modelSelection?.family ?? vendor;\nimpl/src/coordinator.mjs:827:        'Treat worker prose and claimed verification as untrusted; inspect the captured git objects directly.',\nimpl/src/coordinator.mjs:842:    const child = await this.spawn(vendor, reviewBrief, {\nimpl/src/coordinator.mjs:847:      refines: parent.id,\nimpl/src/coordinator.mjs:876:      const verdict = await this._worktrees.validateSessionContext(context);\nimpl/src/coordinator.mjs:888:   * authority comes only from a fresh adapter handshake that reports the expected native ID. */\nimpl/src/coordinator.mjs:897:    const adapter = this._adapters[handle.vendor];\nimpl/src/coordinator.mjs:898:    if (!adapter || !cardSupportsSession(adapter.card(), { mode: 'resume' })) {\nimpl/src/coordinator.mjs:903:      ? normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context: rawContext }).context\nimpl/src/coordinator.mjs:907:      await this._validateSessionContext(context);\nimpl/src/coordinator.mjs:912:    const timeoutMs = opts.timeoutMs ?? this._recoveryTimeoutMs;\nimpl/src/coordinator.mjs:916:    const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\nimpl/src/coordinator.mjs:917:    const recoveryRequested = this._log.append({\nimpl/src/coordinator.mjs:919:      kind: 'control.recovery_requested', actor: opts.actor ?? 'orchestrator',\nimpl/src/coordinator.mjs:922:    const recoveryEvidence = this._coordMapEvent(recoveryRequested);\nimpl/src/coordinator.mjs:923:    this._coordRecord('recovery.requested', {\nimpl/src/coordinator.mjs:924:      taskId: task.id, workerId, sessionId: handle.sessionRef.id, context, evidence: recoveryEvidence,\nimpl/src/coordinator.mjs:925:    }, `driver.recovery.requested:${task.id}:${recoveryRequested.seq}`, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:934:    const attempt = Promise.resolve(adapter.spawn(workerId, task.brief, {\nimpl/src/coordinator.mjs:945:    let outcome = await Promise.race([attempt, timeout]);\nimpl/src/coordinator.mjs:947:      outcome = await Promise.race([\nimpl/src/coordinator.mjs:957:      ? { result: 'recovery_timeout', reason: `native reattachment exceeded ${timeoutMs}ms` }\nimpl/src/coordinator.mjs:959:        ? { result: 'recovery_exception', reason: String(outcome.error?.message ?? outcome.error) }\nimpl/src/coordinator.mjs:961:          ? { result: 'recovery_refused', reason: outcome?.ack?.reason ?? 'adapter refused recovery' }\nimpl/src/coordinator.mjs:971:        kind: 'control.recovery_failed', actor: 'policy', payload: { ...failed, action: 'kill_untrusted_transport' },\nimpl/src/coordinator.mjs:973:      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\nimpl/src/coordinator.mjs:980:      activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\nimpl/src/coordinator.mjs:984:      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\nimpl/src/coordinator.mjs:1001:      kind: 'control.recovery_attached', actor: 'orchestrator',\nimpl/src/coordinator.mjs:1006:      kind: 'lifecycle.turn_started', actor: 'orchestrator', payload: { recovery: true },\nimpl/src/coordinator.mjs:1019:      throw new IntegrationError('integration requires an accepted captured task result', 'result_not_accepted');\nimpl/src/coordinator.mjs:1028:        throw new IntegrationError('integration requires a completed independent oracle from a different model family', 'independent_oracle_required');\nimpl/src/coordinator.mjs:1033:      throw new IntegrationError(`unsupported integration strategy: ${strategy}`, 'unsupported_strategy');\nimpl/src/coordinator.mjs:1036:      throw new IntegrationError('worktree manager does not implement integration', 'integration_unavailable');\nimpl/src/coordinator.mjs:1039:      throw new IntegrationError('worker must be idle, dead, exited, or orphaned before integration', 'worker_not_quiescent');\nimpl/src/coordinator.mjs:1042:    this._coordRecord('integration.requested', {\nimpl/src/coordinator.mjs:1044:      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\nimpl/src/coordinator.mjs:1045:    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:1048:      task.retainedResultRef = await this._worktrees.retainResult(task.capturedSha);\nimpl/src/coordinator.mjs:1052:      const stopped = await this.kill(workerId, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:1054:        throw new IntegrationError('worker could not be safely stopped before integration', 'worker_stop_failed');\nimpl/src/coordinator.mjs:1057:      await this.kill(workerId, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:1059:    await this._removeTaskWorktree(task);\nimpl/src/coordinator.mjs:1063:      integrated = await this._worktrees.integrate(task.capturedSha, { strategy });\nimpl/src/coordinator.mjs:1067:        kind: 'integration.refused', actor: 'policy',\nimpl/src/coordinator.mjs:1071:      this._coordRecord('integration.refused', {\nimpl/src/coordinator.mjs:1074:      }, `driver.integration.refused:${task.id}:${refusedEvent.seq}`, 'policy');\nimpl/src/coordinator.mjs:1078:      try { await this._worktrees.releaseResult(task.retainedResultRef); } catch { /* merged HEAD now retains the result */ }\nimpl/src/coordinator.mjs:1081:    task.integration = Object.freeze({ ...integrated, strategy, actor: opts.actor ?? 'orchestrator' });\nimpl/src/coordinator.mjs:1082:    const integrationEvent = this._log.append({\nimpl/src/coordinator.mjs:1084:      kind: 'integration.completed', actor: opts.actor ?? 'orchestrator', payload: task.integration,\nimpl/src/coordinator.mjs:1086:    const integrationEvidence = this._coordMapEvent(integrationEvent);\nimpl/src/coordinator.mjs:1093:        taskId: task.id, kind: 'report', refs: { beforeSha: task.integration.beforeSha, resultSha: task.integration.resultSha, afterSha: task.integration.afterSha },\nimpl/src/coordinator.mjs:1094:        mediaType: 'application/vnd.baton.integration+json', accepted: true, provenance: [integrationEvidence, ...acceptingEvidence],\nimpl/src/coordinator.mjs:1095:      }, { actor: opts.actor ?? 'orchestrator', key: `artifact.integration:${task.id}:${task.integration.afterSha}` });\nimpl/src/coordinator.mjs:1097:        id: `decision:integrate:${task.id}:${integrationEvent.seq}`, type: 'Decision',\nimpl/src/coordinator.mjs:1098:        body: `Integrated task ${task.id} at ${task.integration.afterSha}`, grounding: 'observed',\nimpl/src/coordinator.mjs:1100:        evidence: [{ coordinationSeq: integrationEvidence.coordinationSeq }],\nimpl/src/coordinator.mjs:1101:      }, { kind: 'Decision', trigger: 'integration' }, { actor: opts.actor ?? 'orchestrator', key: `knowledge.integration:${task.id}:${integrationEvent.seq}` });\nimpl/src/coordinator.mjs:1102:      this._coordRecord('integration.completed', { taskId: task.id, integration: task.integration, evidence: integrationEvidence }, `driver.integration:${task.id}:${integrationEvent.seq}`, opts.actor ?? 'orchestrator');\nimpl/src/coordinator.mjs:1104:    return { ok: true, result: 'integrated', integration: task.integration };\nimpl/src/coordinator.mjs:1112:    if (!task?.integration?.afterSha) {\nimpl/src/coordinator.mjs:1117:    const sha = target.sha ?? task.integration.afterSha;\nimpl/src/coordinator.mjs:1119:      throw new PublicationError('remote must be a credential-free git remote name', 'invalid_remote');\nimpl/src/coordinator.mjs:1124:    if (sha !== task.integration.afterSha) {\nimpl/src/coordinator.mjs:1225:    // SC4a: per-worker delivery serialization — deliveries reach the adapter strictly in\nimpl/src/coordinator.mjs:1228:    // validated against may have changed while it waited. Ack boundedness is X3's existing\nimpl/src/coordinator.mjs:1240:    // cross a finalized stop, and a terminal task cannot be resurrected by a surviving session.\nimpl/src/coordinator.mjs:1242:    const card = this._adapters[handle.vendor]?.card();\nimpl/src/coordinator.mjs:1251:    if (!task || (TERMINAL_TASK_STATUSES.has(task.status) && !reusableFollowUp)) return { ok: false, result: 'task_terminal' };\nimpl/src/coordinator.mjs:1261:        const recoveryEvent = this._log.append({\nimpl/src/coordinator.mjs:1275:    const ack = await this._adapters[handle.vendor].prompt(workerId, message, mode);\nimpl/src/coordinator.mjs:1328:    // A native adapter can emit turn_started synchronously inside prompt(), before returning its\nimpl/src/coordinator.mjs:1329:    // Ack. Queue those events until admission commits; refusal leaves the prior terminal view.\nimpl/src/coordinator.mjs:1334:      ack = await this._adapters[handle.vendor].prompt(workerId, message, 'turn');\nimpl/src/coordinator.mjs:1340:    // A crash/exit is intentionally processed immediately instead of queued. It wins over an Ack\nimpl/src/coordinator.mjs:1341:    // from the same call and can never be overwritten by reopening the prior terminal task.\nimpl/src/coordinator.mjs:1359:      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\nimpl/src/coordinator.mjs:1387:        op: 'follow_up_admission', reason: String(reason ?? 'adapter refused after emitting turn events'),\nimpl/src/coordinator.mjs:1392:    // advanced despite refusing admission. Confirmed two-phase kill owns transport cleanup.\nimpl/src/coordinator.mjs:1411:      return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\nimpl/src/coordinator.mjs:1425:      return { ok: false, result: 'session_not_attached', reason: 'restart replay found no controllable adapter session' };\nimpl/src/coordinator.mjs:1428:    // terminal event as the confirmation, finish cleanup now, and never arm an unfulfillable wait.\nimpl/src/coordinator.mjs:1432:      await this._removeTaskWorktree(this._tasks.get(handle.taskId));\nimpl/src/coordinator.mjs:1449:        const call = Promise.resolve(this._adapters[handle.vendor].kill(handle.id));\nimpl/src/coordinator.mjs:1478:    const waiter = {\nimpl/src/coordinator.mjs:1490:    this._stopWaiters.set(handle.id, waiter);\nimpl/src/coordinator.mjs:1494:    waiter.timerHandle = this._setTimeout(() => this._forceStop(handle.id, waiter), this._stopDeadlineMs);\nimpl/src/coordinator.mjs:1495:    if (waiter.timerHandle && typeof waiter.timerHandle.unref === 'function') waiter.timerHandle.unref();\nimpl/src/coordinator.mjs:1499:        ? Promise.resolve(this._adapters[handle.vendor].kill(handle.id))\nimpl/src/coordinator.mjs:1500:        : Promise.resolve(this._adapters[handle.vendor].interrupt(handle.id, then));\nimpl/src/coordinator.mjs:1501:    this._wireAck(waiter, call);\nimpl/src/coordinator.mjs:1503:    return new Promise((resolve) => waiter.resolvers.push(resolve));\nimpl/src/coordinator.mjs:1542:  _poisonCoordination(err) {\nimpl/src/coordinator.mjs:1557:    const id = `${prior.id}:refinement-${++this._refinementSeq}`;\nimpl/src/coordinator.mjs:1559:      id, brief: prior.brief, deps: [], refines: prior.id, taskType: prior.taskType,\nimpl/src/coordinator.mjs:1564:    const claimed = this._coordination.claimTask(id, handle.id, created.task.version, {\nimpl/src/coordinator.mjs:1565:      actor: 'orchestrator', key: `task.claimed:${id}:${created.task.version}`,\nimpl/src/coordinator.mjs:1568:      ...prior, id, deps: [], refines: prior.id, status: 'working', result: null, verdict: null,\nimpl/src/coordinator.mjs:1569:      capturedSha: null, integration: null, retainedResultRef: null, publication: null, review: null,\nimpl/src/coordinator.mjs:1570:      coordinationVersion: claimed.task.version,\nimpl/src/coordinator.mjs:1581:    for (const claim of this._coordination.activeScratchClaims({ workerId, taskId: task.id })) {\nimpl/src/coordinator.mjs:1582:      this._coordination.expireScratchClaim(claim.id, claim.version, {\nimpl/src/coordinator.mjs:1583:        actor: 'policy', key: `scratch.claim_expired:${claim.id}:${claim.version}:${reason}`,\nimpl/src/coordinator.mjs:1591:    await Promise.resolve(this._worktrees.remove(ownerTaskId)).catch(noop);\nimpl/src/coordinator.mjs:1609:    try { this._runtimeScopes.remove(handle.id); } catch { /* best-effort cleanup continues */ }\nimpl/src/coordinator.mjs:1779:  _wireAck(waiter, call) {\nimpl/src/coordinator.mjs:1782:        waiter.emulated = !!(ack && ack.emulated === true);\nimpl/src/coordinator.mjs:1783:        waiter.ackReady = true;\nimpl/src/coordinator.mjs:1784:        if (ack?.ok === true && ack?.terminal === true) waiter.confirmReceived = true;\nimpl/src/coordinator.mjs:1785:        if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\nimpl/src/coordinator.mjs:1788:        waiter.ackReady = true;\nimpl/src/coordinator.mjs:1789:        if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\nimpl/src/coordinator.mjs:1794:    const waiter = this._stopWaiters.get(handle.id);\nimpl/src/coordinator.mjs:1795:    if (!waiter) return;\nimpl/src/coordinator.mjs:1796:    if (confirmKind !== waiter.mode) return; // stale/mismatched confirmation — ignore\nimpl/src/coordinator.mjs:1797:    waiter.confirmReceived = true;\nimpl/src/coordinator.mjs:1798:    if (waiter.ackReady) this._finalizeStop(handle.id, waiter);\nimpl/src/coordinator.mjs:1801:  _finalizeStop(workerId, waiter) {\nimpl/src/coordinator.mjs:1802:    if (waiter.finalized) return;\nimpl/src/coordinator.mjs:1803:    waiter.finalized = true;\nimpl/src/coordinator.mjs:1804:    if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\nimpl/src/coordinator.mjs:1807:    const kind = waiter.mode === 'kill' ? 'kill.confirmed' : 'control.interrupt_confirmed';\nimpl/src/coordinator.mjs:1812:    if (waiter.emulated) ev.emulated = true;\nimpl/src/coordinator.mjs:1818:        if (waiter.mode === 'kill') {\nimpl/src/coordinator.mjs:1828:          if (waiter.then !== undefined) {\nimpl/src/coordinator.mjs:1859:        Promise.resolve(this._adapters[handle.vendor]?.kill(handle.id)).catch(noop);\nimpl/src/coordinator.mjs:1861:      for (const resolve of waiter.resolvers) resolve({ ok: false, result: 'coordination_unavailable' });\nimpl/src/coordinator.mjs:1866:    const result = { ok: true, result: 'confirmed', emulated: waiter.emulated === true };\nimpl/src/coordinator.mjs:1867:    for (const resolve of waiter.resolvers) resolve(result);\nimpl/src/coordinator.mjs:1872:  _forceStop(workerId, waiter) {\nimpl/src/coordinator.mjs:1873:    if (waiter.finalized) return;\nimpl/src/coordinator.mjs:1874:    waiter.finalized = true;\nimpl/src/coordinator.mjs:1875:    if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\nimpl/src/coordinator.mjs:1893:    if (handle && this._adapters[handle.vendor]) {\nimpl/src/coordinator.mjs:1894:      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\nimpl/src/coordinator.mjs:1906:    for (const resolve of waiter.resolvers) resolve(result);\nimpl/src/coordinator.mjs:1925:      await record.resolvingDone;\nimpl/src/coordinator.mjs:1933:    // CI2: reserve the single-consumer slot, but do not COMMIT resolution until the adapter\nimpl/src/coordinator.mjs:1984:      if (typeof this._publisher !== 'function') {\nimpl/src/coordinator.mjs:2003:      let published;\nimpl/src/coordinator.mjs:2005:        published = await this._publisher(record.publication);\nimpl/src/coordinator.mjs:2009:        throw new PublicationError(String(err?.message ?? err), 'publisher_failed');\nimpl/src/coordinator.mjs:2015:      void published;\nimpl/src/coordinator.mjs:2024:          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\nimpl/src/coordinator.mjs:2035:      return { ok: true, result: 'published', publication: task.publication };\nimpl/src/coordinator.mjs:2080:        ack = await this._adapters[handle.vendor].answer(handle.id, requestId, answer);\nimpl/src/coordinator.mjs:2083:        ack = await this._adapters[handle.vendor].approve(handle.id, requestId, decision, answer && answer.payload);\nimpl/src/coordinator.mjs:2098:      return { ok: false, result: 'delivery_refused', reason: ack?.reason ?? 'adapter did not affirm response delivery' };\nimpl/src/coordinator.mjs:2153:      integration: task?.integration ?? null,\nimpl/src/coordinator.mjs:2174:      if (taskId && taskId !== handle.taskId) throw new Error('knowledge reader task does not match worker ownership');\nimpl/src/coordinator.mjs:2185:  claimScratch(workerId, fields, opts = {}) {\nimpl/src/coordinator.mjs:2190:    if (opts.expectedFence === undefined) throw new TypeError('Scratch claim requires expectedFence');\nimpl/src/coordinator.mjs:2193:    if (typeof opts.idempotencyKey !== 'string' || opts.idempotencyKey.length === 0) throw new TypeError('Scratch claim requires idempotencyKey');\nimpl/src/coordinator.mjs:2194:    return this._coordination.claimScratch({\nimpl/src/coordinator.mjs:2235:  // Command: wait()\nimpl/src/coordinator.mjs:2238:  async wait(timeoutMs = 25000) {\nimpl/src/coordinator.mjs:2245:    await this._sleep(0);\nimpl/src/coordinator.mjs:2252:      await this._sleep(Math.min(this._waitPollMs, remaining));\nimpl/src/coordinator.mjs:2365:          payload: { op: kind === 'lifecycle.turn_completed' ? 'terminal' : kind, attemptedTurnEpoch: normalizedEpoch, currentTurnEpoch: currentEpoch },\nimpl/src/coordinator.mjs:2389:          persistence: this._adapters[handle.vendor]?.card()?.sessions?.resume === 'native' ? 'native' : 'process',\nimpl/src/coordinator.mjs:2402:      const selection = this._adapters[handle.vendor]?.card()?.modelSelection;\nimpl/src/coordinator.mjs:2421:        // until the adapter proves the mismatched session is gone.\nimpl/src/coordinator.mjs:2438:        // (coordinator.test). Normalize so the logged claim and the gate both see the WorkerResult.\nimpl/src/coordinator.mjs:2449:        const terminalEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\nimpl/src/coordinator.mjs:2452:          const evidence = this._coordMapEvent(terminalEvent);\nimpl/src/coordinator.mjs:2550:    // SC13/SC14: a late terminal event from a stopped session cannot reopen a terminal task.\nimpl/src/coordinator.mjs:2562:      const captured = await this._worktrees.capture(handle.worktree ?? task.worktree, {\nimpl/src/coordinator.mjs:2567:      const created = await this._worktrees.createVerifyWorktree(task.id, sha);\nimpl/src/coordinator.mjs:2572:        const baseCreated = await this._worktrees.createBaseVerifyWorktree(task.id, baseSha);\nimpl/src/coordinator.mjs:2576:        task.changedLines = await this._worktrees.changedLines(baseSha, sha);\nimpl/src/coordinator.mjs:2579:      const verdict = await this._referee(task, workerResult, {\nimpl/src/coordinator.mjs:2618:          taskId: task.id, kind: 'commit', refs: { sha: captured.sha }, mediaType: 'application/vnd.git.commit',\nimpl/src/coordinator.mjs:2635:        const claimEvent = this._log.read(handle.id).filter((event) => event.kind === 'lifecycle.turn_completed').at(-1);\nimpl/src/coordinator.mjs:2636:        const claimEvidence = this._coordMapEvent(claimEvent);\nimpl/src/coordinator.mjs:2638:          taskId: task.id, kind: 'report', refs: { claimedArtifacts: workerResult.artifacts },\nimpl/src/coordinator.mjs:2639:          mediaType: 'application/vnd.baton.worker-artifact-claim+json', accepted: false,\nimpl/src/coordinator.mjs:2640:          provenance: claimEvidence ? [claimEvidence] : [], grounding: 'worker_prose',\nimpl/src/coordinator.mjs:2643:      const terminalStatus = accept ? 'completed' : 'failed';\nimpl/src/coordinator.mjs:2644:      trustPhase = 'terminal_batch';\nimpl/src/coordinator.mjs:2645:      const terminal = this._coordination.transitionTaskWithArtifacts(\nimpl/src/coordinator.mjs:2646:        task.id, terminalStatus, task.coordinationVersion,\nimpl/src/coordinator.mjs:2647:        manifests, { actor: 'policy', key: `task.${terminalStatus}:${task.id}:${verifyEvent.seq}` }, evidence,\nimpl/src/coordinator.mjs:2649:      task.coordinationVersion = terminal.task.version;\nimpl/src/coordinator.mjs:2650:      this._expireScratchClaims(handle, task, `task_${terminalStatus}`);\nimpl/src/coordinator.mjs:2651:      const artifactEvidence = terminal.artifacts.map((artifact) => ({ artifactId: artifact.id }));\nimpl/src/coordinator.mjs:2684:        const card = this._adapters[handle.vendor]?.card();\nimpl/src/coordinator.mjs:2710:          this._poisonCoordination(coordinationError);\nimpl/src/coordinator.mjs:2714:      if (['evidence_mapping', 'terminal_batch', 'promotion'].includes(trustPhase)) this._poisonCoordination(err);\nimpl/src/coordinator.mjs:2718:      if (verifyPath != null) await this._worktrees.removeVerifyWorktree(verifyPath);\nimpl/src/coordinator.mjs:2719:      if (baseVerifyPath != null) await this._worktrees.removeVerifyWorktree(baseVerifyPath);\nimpl/src/coordinator.mjs:2739:      let terminalStatus = 'working';\nimpl/src/coordinator.mjs:2742:      let recoveryTerminalized = false;\nimpl/src/coordinator.mjs:2755:      let integration = null;\nimpl/src/coordinator.mjs:2791:                  persistence: this._adapters[vendorResolved]?.card()?.sessions?.resume === 'native' ? 'native' : 'process',\nimpl/src/coordinator.mjs:2812:          case 'control.recovery_attached':\nimpl/src/coordinator.mjs:2813:            terminalStatus = 'working';\nimpl/src/coordinator.mjs:2818:              ? { mode: 'resume', id: sessionRef.id, ...(sessionContext ? { context: sessionContext } : {}) }\nimpl/src/coordinator.mjs:2822:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'working';\nimpl/src/coordinator.mjs:2825:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\nimpl/src/coordinator.mjs:2827:              terminalStatus = 'verifying';\nimpl/src/coordinator.mjs:2831:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\nimpl/src/coordinator.mjs:2833:              terminalStatus = e.payload?.accept ? 'completed' : 'failed';\nimpl/src/coordinator.mjs:2837:          case 'integration.completed':\nimpl/src/coordinator.mjs:2838:            integration = e.payload ?? integration;\nimpl/src/coordinator.mjs:2841:          case 'integration.refused':\nimpl/src/coordinator.mjs:2849:          case 'control.recovery_terminalized':\nimpl/src/coordinator.mjs:2850:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'failed';\nimpl/src/coordinator.mjs:2854:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'cancelled';\nimpl/src/coordinator.mjs:2858:            if (!TERMINAL_TASK_STATUSES.has(terminalStatus) && e.payload?.blocking !== false) terminalStatus = 'input_required';\nimpl/src/coordinator.mjs:2862:            if (terminalStatus === 'input_required') terminalStatus = 'working';\nimpl/src/coordinator.mjs:2870:      // A persistent worker can own a chain of immutable refinement tasks. Only the first turn\nimpl/src/coordinator.mjs:2872:      // and wire session. The coordination stream is authoritative for which refinement is\nimpl/src/coordinator.mjs:2873:      // current after restart, so associate the replayed terminal/result state with the newest\nimpl/src/coordinator.mjs:2881:      // Operational completion without its authoritative coordination terminal batch is a crash\nimpl/src/coordinator.mjs:2882:      // gap, never permission to infer success from telemetry. Fail the claimed task durably so a\nimpl/src/coordinator.mjs:2885:        && TERMINAL_TASK_STATUSES.has(terminalStatus)) {\nimpl/src/coordinator.mjs:2886:        recoveryTerminalized = true;\nimpl/src/coordinator.mjs:2887:        const priorOperationalStatus = terminalStatus;\nimpl/src/coordinator.mjs:2888:        terminalStatus = 'failed';\nimpl/src/coordinator.mjs:2891:          kind: 'control.recovery_terminalized', actor: 'policy',\nimpl/src/coordinator.mjs:2892:          payload: { reason: 'coordination_terminal_batch_missing', priorStatus: priorOperationalStatus },\nimpl/src/coordinator.mjs:2897:        }, gapEvidence ?? { reason: 'coordination_terminal_batch_missing' });\nimpl/src/coordinator.mjs:2902:      // CI6: replay cannot resurrect an adapter session. Until the persistent-session phase can\nimpl/src/coordinator.mjs:2903:      // prove native reattachment, any nonterminal reconstructed task is durably failed and its\nimpl/src/coordinator.mjs:2905:      if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\nimpl/src/coordinator.mjs:2906:        recoveryTerminalized = true;\nimpl/src/coordinator.mjs:2907:        terminalStatus = 'failed';\nimpl/src/coordinator.mjs:2908:        const recoveryEvent = this._log.append({\nimpl/src/coordinator.mjs:2912:          kind: 'control.recovery_terminalized',\nimpl/src/coordinator.mjs:2918:          const evidence = this._coordMapEvent(recoveryEvent);\nimpl/src/coordinator.mjs:2920:            actor: 'policy', key: `task.failed:${taskId}:replay:${recoveryEvent?.seq ?? maxTurnEpoch}`,\nimpl/src/coordinator.mjs:2955:          integration,\nimpl/src/coordinator.mjs:2964:        task.status = durable?.status ?? terminalStatus;\nimpl/src/coordinator.mjs:2972:        task.integration = integration;\nimpl/src/coordinator.mjs:2995:        // A durable native reference is not a live transport. Even a terminal task that was\nimpl/src/coordinator.mjs:2997:        status: (recoveryTerminalized || sessionRef) ? 'orphaned' : this._deriveWorkerStatus(terminalStatus),\nimpl/src/coordinator.mjs:3037:  _terminalizeUnattachedCoordinationTasks() {\nimpl/src/coordinator.mjs:3044:      const recorded = this._coordRecord('recovery.claimed_without_spawn', { taskId: durable.id, workerId }, `driver.recovery:${durable.id}:claimed_without_spawn`);\nimpl/src/coordinator.mjs:3046:        actor: 'policy', key: `task.failed:${durable.id}:claimed_without_spawn`,\nimpl/src/coordinator.mjs:3047:      }, { coordinationSeq: recorded?.seq ?? null, reason: 'claimed_without_operational_spawn' });\nimpl/src/coordinator.mjs:3051:        this._expireScratchClaims(this._workers.get(workerId), task, 'claimed_without_spawn');\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'rg -n \"poison|publish|integration|intent|append failure|single-consumer|knowledge.read|persistent|recovery|stop intent|authorization|git|adapter|pid|cleanup|claimTask|claim|wait|resume|terminal|refine|follow-up|followup|post-effect|pre-effect\" impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":17,"ts":"2026-07-11T20:20:05.330Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_5RR1lNxQJ65IKIvIM2GXxDXn","command":"/bin/zsh -lc 'git diff c8a272e''^ c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs spec/phase11/coordination-knowledge.md'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"99093","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"git diff c8a272e^ c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs spec/phase11/coordination-knowledge.md"}],"aggregatedOutput":"diff --git a/impl/src/coordinator.mjs b/impl/src/coordinator.mjs\nindex 5b26737..5eafbed 100644\n--- a/impl/src/coordinator.mjs\n+++ b/impl/src/coordinator.mjs\n@@ -365,7 +365,14 @@ export class Coordinator {\n     this._seedCoordinationTasks();\n \n     for (const adapter of Object.values(this._adapters)) {\n-      adapter.onEvent((e) => this._handleEvent(e));\n+      adapter.onEvent((e) => {\n+        try { this._handleEvent(e); } catch (err) {\n+          // Adapter callbacks are an asynchronous trust boundary. A fatal authoritative-write\n+          // failure has already poisoned this coordinator; do not let it become an uncaught\n+          // process exception. The next public command observes the fatal error.\n+          if (!this._fatalError) throw err;\n+        }\n+      });\n     }\n \n     // D10: reconcile first, then rebuild all state purely from the log.\n@@ -901,12 +908,16 @@ export class Coordinator {\n     admission.spawned = new Promise((resolve) => { admission.resolveSpawned = resolve; });\n     handle.turnAdmission = admission;\n     const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\n-    const runtime = this._ensureRuntimeScope(handle);\n-    this._log.append({\n+    const recoveryRequested = this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n       kind: 'control.recovery_requested', actor: opts.actor ?? 'orchestrator',\n       payload: { sessionRef: handle.sessionRef, context },\n     });\n+    const recoveryEvidence = this._coordMapEvent(recoveryRequested);\n+    this._coordRecord('recovery.requested', {\n+      taskId: task.id, workerId, sessionId: handle.sessionRef.id, context, evidence: recoveryEvidence,\n+    }, `driver.recovery.requested:${task.id}:${recoveryRequested.seq}`, opts.actor ?? 'orchestrator');\n+    const runtime = this._ensureRuntimeScope(handle);\n \n     let timerHandle;\n     let timedOut = false;\n@@ -958,7 +969,15 @@ export class Coordinator {\n     }\n \n     const stamp = this._fences.bumpTurn(workerId);\n-    const activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n+    let activeTask;\n+    try {\n+      activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n+    } catch (err) {\n+      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n+      handle.status = 'orphaned';\n+      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n+      throw err;\n+    }\n     activeTask.status = 'working';\n     activeTask.result = null;\n     activeTask.verdict = null;\n@@ -1012,6 +1031,11 @@ export class Coordinator {\n       throw new IntegrationError('worker must be idle, dead, exited, or orphaned before integration', 'worker_not_quiescent');\n     }\n \n+    this._coordRecord('integration.requested', {\n+      taskId: task.id, workerId, strategy, sha: task.capturedSha,\n+      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\n+    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\n+\n     if (typeof this._worktrees.retainResult === 'function') {\n       task.retainedResultRef = await this._worktrees.retainResult(task.capturedSha);\n     }\n@@ -1096,16 +1120,21 @@ export class Coordinator {\n     const requestId = `publication-${workerId}-${++this._publicationSeq}`;\n     const publication = Object.freeze({ remote, ref, sha });\n     const deadlineAt = this._now() + this._approvalTimeoutMs;\n-    this._pending.set(requestId, {\n+    const record = {\n       kind: 'publication', worker: workerId, state: 'pending', resolution: null, consumer: null,\n       turnEpochAtAsk: stamp.turnEpoch, fenceAtAsk: stamp.fence,\n       deadlineAt, publication,\n-    });\n-    this._log.append({\n+    };\n+    const requestedEvent = this._log.append({\n       worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n       kind: 'publication.requested', actor,\n       payload: { requestId, ...publication, fence: stamp.fence, deadlineAt },\n     });\n+    const evidence = this._coordMapEvent(requestedEvent);\n+    this._coordRecord('publication.requested', {\n+      taskId: task.id, workerId, requestId, publication, fence: stamp.fence, deadlineAt, evidence,\n+    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\n+    this._pending.set(requestId, record);\n     return { ok: true, requestId, fence: stamp.fence, target: publication };\n   }\n \n@@ -1278,6 +1307,16 @@ export class Coordinator {\n       if (!preCheck.ok) return { ok: false, result: 'stale_fence', current: preCheck.current };\n     }\n \n+    const requestedEvent = this._log.append({\n+      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+      kind: 'control.follow_up_requested', actor: opts.actor ?? 'orchestrator',\n+      payload: { message, expectedFence: opts.expectedFence ?? null },\n+    });\n+    const requestedEvidence = this._coordMapEvent(requestedEvent);\n+    this._coordRecord('follow_up.requested', {\n+      taskId: task.id, workerId, expectedFence: opts.expectedFence ?? null, evidence: requestedEvidence,\n+    }, `driver.follow_up.requested:${task.id}:${requestedEvent.seq}`, opts.actor ?? 'orchestrator');\n+\n     // A native adapter can emit turn_started synchronously inside prompt(), before returning its\n     // Ack. Queue those events until admission commits; refusal leaves the prior terminal view.\n     const admission = { events: [] };\n@@ -1303,7 +1342,17 @@ export class Coordinator {\n     }\n \n     const stamp = this._fences.bumpTurn(workerId);\n-    const activeTask = this._createCoordinationRefinement(handle, task, 'follow_up');\n+    let activeTask;\n+    try {\n+      activeTask = this._createCoordinationRefinement(handle, task, 'follow_up');\n+    } catch (err) {\n+      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n+      handle.status = 'dead';\n+      Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\n+      this._removeRuntimeScope(handle);\n+      this._removeTaskWorktree(task).catch(noop);\n+      throw err;\n+    }\n     activeTask.status = 'working';\n     activeTask.result = null;\n     activeTask.verdict = null;\n@@ -1380,17 +1429,28 @@ export class Coordinator {\n     const existing = this._stopWaiters.get(handle.id);\n     if (existing) {\n       if (mode === 'kill' && existing.mode !== 'kill') {\n+        const harness = this._harnessOf(handle.vendor);\n+        const requested = this._log.append({ worker: handle.id, harness, turnEpoch: this._safeTurnEpoch(handle), kind: 'kill.requested', actor, payload: {} });\n+        const evidence = this._coordMapEvent(requested);\n+        this._coordRecord('control.stop_requested', { taskId: handle.taskId, workerId: handle.id, mode: 'kill', escalation: true, evidence }, `driver.stop_requested:${handle.taskId}:${requested.seq}`, actor);\n         existing.mode = 'kill';\n         existing.ackReady = false;\n         existing.confirmReceived = false;\n-        const harness = this._harnessOf(handle.vendor);\n-        this._log.append({ worker: handle.id, harness, turnEpoch: this._safeTurnEpoch(handle), kind: 'kill.requested', actor, payload: {} });\n         const call = Promise.resolve(this._adapters[handle.vendor].kill(handle.id));\n         this._wireAck(existing, call);\n       }\n       return new Promise((resolve) => existing.resolvers.push(resolve));\n     }\n \n+    this._fences.bumpHuman(handle.id);\n+    const harness = this._harnessOf(handle.vendor);\n+    const turnEpoch = this._safeTurnEpoch(handle);\n+    const reqKind = mode === 'kill' ? 'kill.requested' : 'control.interrupt_requested';\n+    const reqPayload = mode === 'kill' ? {} : { then: then ?? null, actor };\n+    const requested = this._log.append({ worker: handle.id, harness, turnEpoch, kind: reqKind, actor, payload: reqPayload });\n+    const evidence = this._coordMapEvent(requested);\n+    this._coordRecord('control.stop_requested', { taskId: handle.taskId, workerId: handle.id, mode, then: then ?? null, evidence }, `driver.stop_requested:${handle.taskId}:${requested.seq}`, actor);\n+\n     if (handle.status === 'blocked') {\n       if (handle.pendingApprovalId) {\n         this._resolveRecord(handle.pendingApprovalId, { decision: 'cancel' }, actor).catch(noop);\n@@ -1398,20 +1458,12 @@ export class Coordinator {\n         this._resolveRecord(handle.pendingQuestionId, { decision: 'cancel' }, actor).catch(noop);\n       }\n     }\n-\n-    this._fences.bumpHuman(handle.id);\n     if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) {\n       handle.spawnAbort.abort({ mode, actor });\n     }\n     handle.status = 'stopping';\n     this._clearWatchdog(handle);\n \n-    const harness = this._harnessOf(handle.vendor);\n-    const turnEpoch = this._safeTurnEpoch(handle);\n-    const reqKind = mode === 'kill' ? 'kill.requested' : 'control.interrupt_requested';\n-    const reqPayload = mode === 'kill' ? {} : { then: then ?? null, actor };\n-    this._log.append({ worker: handle.id, harness, turnEpoch, kind: reqKind, actor, payload: reqPayload });\n-\n     const waiter = {\n       mode,\n       workerId: handle.id,\n@@ -1738,41 +1790,53 @@ export class Coordinator {\n     if (waiter.emulated) ev.emulated = true;\n     const stopEvent = this._log.append(ev);\n \n-    if (handle) {\n-      const task = this._tasks.get(handle.taskId);\n-      if (waiter.mode === 'kill') {\n-        if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n-          const evidence = this._coordMapEvent(stopEvent);\n-          this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n-        }\n-        handle.status = 'dead';\n-        this._removeRuntimeScope(handle);\n-        if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n-        this._removeTaskWorktree(task).catch(noop);\n-      } else {\n-        if (waiter.then !== undefined) {\n-          const stamp = this._fences.bumpTurn(handle.id);\n-          handle.status = 'working';\n-          if (task) {\n-            task.status = 'working';\n-            task.result = null;\n-            task.verdict = null;\n-          }\n-          this._log.append({\n-            worker: workerId, harness, turnEpoch: stamp.turnEpoch, kind: 'lifecycle.turn_started', actor: 'orchestrator',\n-            modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n-            payload: { followUp: true, afterInterrupt: true },\n-          });\n-          this._resetWatchdogTurn(handle);\n-        } else {\n+    try {\n+      if (handle) {\n+        const task = this._tasks.get(handle.taskId);\n+        if (waiter.mode === 'kill') {\n           if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n             const evidence = this._coordMapEvent(stopEvent);\n             this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n           }\n-          handle.status = 'idle';\n+          handle.status = 'dead';\n+          this._removeRuntimeScope(handle);\n           if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n+          this._removeTaskWorktree(task).catch(noop);\n+        } else {\n+          if (waiter.then !== undefined) {\n+            const stamp = this._fences.bumpTurn(handle.id);\n+            handle.status = 'working';\n+            if (task) {\n+              task.status = 'working';\n+              task.result = null;\n+              task.verdict = null;\n+            }\n+            this._log.append({\n+              worker: workerId, harness, turnEpoch: stamp.turnEpoch, kind: 'lifecycle.turn_started', actor: 'orchestrator',\n+              modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n+              payload: { followUp: true, afterInterrupt: true },\n+            });\n+            this._resetWatchdogTurn(handle);\n+          } else {\n+            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n+              const evidence = this._coordMapEvent(stopEvent);\n+              this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n+            }\n+            handle.status = 'idle';\n+            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n+          }\n         }\n       }\n+    } catch (err) {\n+      if (handle) {\n+        handle.status = 'dead';\n+        this._removeRuntimeScope(handle);\n+        this._removeTaskWorktree(this._tasks.get(handle.taskId)).catch(noop);\n+        Promise.resolve(this._adapters[handle.vendor]?.kill(handle.id)).catch(noop);\n+      }\n+      for (const resolve of waiter.resolvers) resolve({ ok: false, result: 'coordination_unavailable' });\n+      this._stopWaiters.delete(workerId);\n+      throw err;\n     }\n \n     const result = { ok: true, result: 'confirmed', emulated: waiter.emulated === true };\n@@ -1789,6 +1853,19 @@ export class Coordinator {\n     const harness = handle ? this._harnessOf(handle.vendor) : '';\n     const forcedEvent = this._log.append({ worker: workerId, harness, turnEpoch: handle ? this._safeTurnEpoch(handle) : 0, kind: 'control.forced_stop', actor: 'policy', payload: {} });\n \n+    let coordinationFailure = null;\n+    if (handle) {\n+      const task = this._tasks.get(handle.taskId);\n+      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n+        try {\n+          const evidence = this._coordMapEvent(forcedEvent);\n+          this._coordTransition(task, 'failed', `task.failed:${task.id}:${forcedEvent.seq}`, evidence);\n+        } catch (err) {\n+          coordinationFailure = err;\n+        }\n+      }\n+    }\n+\n     if (handle && this._adapters[handle.vendor]) {\n       Promise.resolve(this._adapters[handle.vendor].kill(workerId)).catch(noop);\n     }\n@@ -1797,15 +1874,11 @@ export class Coordinator {\n       handle.status = 'dead';\n       this._removeRuntimeScope(handle);\n       const task = this._tasks.get(handle.taskId);\n-      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n-        const evidence = this._coordMapEvent(forcedEvent);\n-        this._coordTransition(task, 'failed', `task.failed:${task.id}:${forcedEvent.seq}`, evidence);\n-      }\n-      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\n+      if (!coordinationFailure && task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'failed';\n       this._removeTaskWorktree(task).catch(noop);\n     }\n \n-    const result = { ok: true, result: 'forced' };\n+    const result = coordinationFailure ? { ok: false, result: 'coordination_unavailable' } : { ok: true, result: 'forced' };\n     for (const resolve of waiter.resolvers) resolve(result);\n     this._stopWaiters.delete(workerId);\n   }\n@@ -1855,28 +1928,32 @@ export class Coordinator {\n       const currentFence = handle ? this._fences.current(handle.id).fence : null;\n       const fenceValid = actor === 'policy' || answer?.fence === record.fenceAtAsk;\n       if (!handle || !fenceValid || currentFence !== record.fenceAtAsk) {\n-        record.state = 'resolved';\n-        record.consumer = actor;\n-        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n         if (handle) {\n-          this._log.append({\n+          const refusedEvent = this._log.append({\n             worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n             kind: 'publication.refused', actor: 'policy',\n             payload: { requestId, reason: 'stale_fence', remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n           });\n+          const evidence = this._coordMapEvent(refusedEvent);\n+          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\n         }\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n         finishResolving();\n         return { ok: false, result: 'stale_fence', current: currentFence };\n       }\n       if (decision === 'deny') {\n-        record.state = 'resolved';\n-        record.consumer = actor;\n-        record.resolution = { decision: 'deny' };\n-        this._log.append({\n+        const deniedEvent = this._log.append({\n           worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n           kind: 'publication.denied', actor,\n           payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n         });\n+        const evidence = this._coordMapEvent(deniedEvent);\n+        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = { decision: 'deny' };\n         finishResolving();\n         return { ok: true, result: 'denied' };\n       }\n@@ -1885,6 +1962,20 @@ export class Coordinator {\n         finishResolving();\n         return { ok: false, result: 'publication_unavailable' };\n       }\n+      let authorizedEvent;\n+      try {\n+        authorizedEvent = this._log.append({\n+          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+          kind: 'publication.authorized', actor,\n+          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha, fence: record.fenceAtAsk },\n+        });\n+        const evidence = this._coordMapEvent(authorizedEvent);\n+        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\n+      } catch (err) {\n+        record.state = 'pending';\n+        finishResolving();\n+        throw err;\n+      }\n       let published;\n       try {\n         published = await this._publisher(record.publication);\n@@ -1899,18 +1990,23 @@ export class Coordinator {\n       const task = this._tasks.get(handle.taskId);\n       void published;\n       task.publication = Object.freeze({ requestId, ...record.publication, actor });\n-      const publicationEvent = this._log.append({\n-        worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n-        kind: 'publication.completed', actor, payload: task.publication,\n-      });\n-      const publicationEvidence = this._coordMapEvent(publicationEvent);\n-      this._coordination?.promoteKnowledgeNode({\n-        id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n-        body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n-        grounding: 'observed', informedBy: [`task:${task.id}`],\n-        evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n-      }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n-      this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n+      try {\n+        const publicationEvent = this._log.append({\n+          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n+          kind: 'publication.completed', actor, payload: task.publication,\n+        });\n+        const publicationEvidence = this._coordMapEvent(publicationEvent);\n+        this._coordination?.promoteKnowledgeNode({\n+          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n+          body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n+          grounding: 'observed', informedBy: [`task:${task.id}`],\n+          evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n+        }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n+        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n+      } catch (err) {\n+        finishResolving();\n+        throw err;\n+      }\n       finishResolving();\n       return { ok: true, result: 'published', publication: task.publication };\n     }\n@@ -1940,7 +2036,12 @@ export class Coordinator {\n     const stale = record.turnEpochAtAsk !== currentTurnEpoch;\n \n     if (stale) {\n-      this._log.append({ worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'control.stale_rejected', actor, payload: { op: 'respond', requestId } });\n+      const staleEvent = this._log.append({ worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'control.stale_rejected', actor, payload: { op: 'respond', requestId } });\n+      const task = this._tasks.get(handle.taskId);\n+      if (task && this._coordination?.task(task.id)?.status === 'input_required') {\n+        const evidence = this._coordMapEvent(staleEvent);\n+        this._coordTransition(task, 'working', `task.working:${task.id}:${staleEvent.seq}`, { ...evidence, interaction: { requestId, disposition: 'stale_discarded' } }, actor);\n+      }\n       record.state = 'resolved';\n       record.consumer = actor;\n       record.resolution = answer;\n@@ -1973,10 +2074,6 @@ export class Coordinator {\n       return { ok: false, result: 'delivery_refused', reason: ack?.reason ?? 'adapter did not affirm response delivery' };\n     }\n \n-    record.state = 'resolved';\n-    record.consumer = actor;\n-    record.resolution = answer;\n-\n     let resolvedEvent;\n     if (record.kind === 'question') {\n       const ev = { worker: handle.id, harness, turnEpoch: currentTurnEpoch, kind: 'question.answered', actor, payload: { requestId, answer } };\n@@ -1990,10 +2087,21 @@ export class Coordinator {\n     }\n     const task = this._tasks.get(handle.taskId);\n     if (task && this._coordination?.task(task.id)?.status === 'input_required') {\n-      const evidence = this._coordMapEvent(resolvedEvent);\n-      this._coordTransition(task, 'working', `task.working:${task.id}:${resolvedEvent.seq}`, evidence, actor);\n+      try {\n+        const evidence = this._coordMapEvent(resolvedEvent);\n+        this._coordTransition(task, 'working', `task.working:${task.id}:${resolvedEvent.seq}`, { ...evidence, interaction: { requestId, disposition: 'delivered' } }, actor);\n+      } catch (err) {\n+        record.state = 'resolved';\n+        record.consumer = actor;\n+        record.resolution = answer;\n+        finishResolving();\n+        throw err;\n+      }\n     }\n \n+    record.state = 'resolved';\n+    record.consumer = actor;\n+    record.resolution = answer;\n     clearPending();\n     finishResolving();\n     return { ok: true, result: 'applied' };\n@@ -2324,7 +2432,7 @@ export class Coordinator {\n         const askedEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && TERMINAL_TASK_STATUSES.has(task.status)) break;\n-        this._pending.set(requestId, {\n+        const record = {\n           kind: 'question',\n           worker: workerId,\n           state: 'pending',\n@@ -2332,12 +2440,17 @@ export class Coordinator {\n           consumer: null,\n           turnEpochAtAsk: this._safeTurnEpoch(handle),\n           deadlineAt: null,\n-        });\n+        };\n+        const evidence = this._coordMapEvent(askedEvent);\n         if (payload?.blocking !== false) {\n           if (task) {\n-            const evidence = this._coordMapEvent(askedEvent);\n-            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, evidence);\n+            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, { ...evidence, interaction: { kind: 'question', requestId, blocking: true } });\n           }\n+        } else {\n+          this._coordRecord('input.requested', { taskId: task?.id ?? null, workerId, kind: 'question', requestId, blocking: false, evidence }, `driver.input_requested:${handle.taskId}:${askedEvent.seq}`, actor ?? 'worker');\n+        }\n+        this._pending.set(requestId, record);\n+        if (payload?.blocking !== false) {\n           handle.status = 'blocked';\n           handle.pendingQuestionId = requestId;\n           if (task) task.status = 'input_required';\n@@ -2349,7 +2462,7 @@ export class Coordinator {\n         const askedEvent = appendAttributed({ worker: workerId, harness, turnEpoch, kind, actor, payload });\n         const task = this._tasks.get(handle.taskId);\n         if (task && TERMINAL_TASK_STATUSES.has(task.status)) break;\n-        this._pending.set(requestId, {\n+        const record = {\n           kind: 'approval',\n           worker: workerId,\n           state: 'pending',\n@@ -2357,12 +2470,17 @@ export class Coordinator {\n           consumer: null,\n           turnEpochAtAsk: this._safeTurnEpoch(handle),\n           deadlineAt: this._now() + this._approvalTimeoutMs,\n-        });\n+        };\n+        const evidence = this._coordMapEvent(askedEvent);\n         if (payload?.blocking !== false) {\n           if (task) {\n-            const evidence = this._coordMapEvent(askedEvent);\n-            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, evidence);\n+            this._coordTransition(task, 'input_required', `task.input_required:${task.id}:${askedEvent.seq}`, { ...evidence, interaction: { kind: 'approval', requestId, blocking: true } });\n           }\n+        } else {\n+          this._coordRecord('input.requested', { taskId: task?.id ?? null, workerId, kind: 'approval', requestId, blocking: false, evidence }, `driver.input_requested:${handle.taskId}:${askedEvent.seq}`, actor ?? 'worker');\n+        }\n+        this._pending.set(requestId, record);\n+        if (payload?.blocking !== false) {\n           handle.status = 'blocked';\n           handle.pendingApprovalId = requestId;\n           if (task) task.status = 'input_required';\ndiff --git a/impl/test/phase11-acceptance-integration.test.mjs b/impl/test/phase11-acceptance-integration.test.mjs\nindex 3412edd..bc0f263 100644\n--- a/impl/test/phase11-acceptance-integration.test.mjs\n+++ b/impl/test/phase11-acceptance-integration.test.mjs\n@@ -239,6 +239,29 @@ test('AC4: visible same-family fallback cannot satisfy a required independent or\n   );\n });\n \n+test('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\n+  const root = repo();\n+  commitBase(root);\n+  const implementer = familyAdapter('family-a', { outcome: 'completed', edits: [{ path: 'src/reviewed.txt', content: 'review me\\n' }] });\n+  const reviewer = familyAdapter('family-b', { outcome: 'completed' });\n+  let reviewerSpawns = 0;\n+  const rawSpawn = reviewer.spawn.bind(reviewer);\n+  reviewer.spawn = async (...args) => { reviewerSpawns += 1; return rawSpawn(...args); };\n+  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\n+  const parent = await driver.coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-fault-parent' });\n+  await until(async () => (await driver.coordinator.result(parent.id)).ready);\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('\"review\"')) throw new Error('review task disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(driver.coordinator.spawnReview(parent.id, 'reviewer', {\n+    kind: 'review', taskId: 'review-fault-child', verification: { command: 'true', expectExit: 0 },\n+  }), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(reviewerSpawns, 0);\n+  assert.deepEqual(driver.coordination.snapshot().tasks.map((task) => [task.id, task.status]), [['review-fault-parent', 'completed']]);\n+});\n+\n test('AC5: ff-only integration reaps the worker/worktree/branch and records exact SHAs', async () => {\n   const root = repo();\n   commitBase(root, { 'README.md': 'base\\n' });\n@@ -327,6 +350,24 @@ test('AC5: integration refuses an unaccepted captured result', async () => {\n   );\n });\n \n+test('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\n+  const root = repo();\n+  commitBase(root);\n+  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\n+  const headBefore = git(['rev-parse', 'HEAD'], root);\n+  const workerBefore = coordinator._workers.get(handle.id);\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n+  assert.equal(workerBefore.status, 'idle');\n+  assert.equal(existsSync(workerBefore.worktree), true);\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\n+});\n+\n test('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\n   const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\n   const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n@@ -352,6 +393,41 @@ test('AC6: publication has no side effect before approval and allow publishes th\n   assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'publication'), true);\n });\n \n+test('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\n+  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n+  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(\n+    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n+    (error) => error.code === 'coordination_write_unavailable',\n+  );\n+  assert.equal(calls.length, 0);\n+  assert.equal(coordinator._pending.get(requested.requestId).state, 'pending');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n+});\n+\n+test('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\n+  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n+  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"publication.completed\"')) throw new Error('publication completion disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(\n+    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n+    (error) => error.code === 'coordination_write_unavailable',\n+  );\n+  assert.equal(calls.length, 1);\n+  assert.equal(coordinator._pending.get(requested.requestId).state, 'resolved');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n+});\n+\n test('AC6: deny and timeout are fail-closed and never call the publisher', async () => {\n   let now = 1000;\n   const denied = await integratedPublicationTask({ now: () => now, approvalTimeoutMs: 10 });\ndiff --git a/impl/test/phase11-coordination-store.test.mjs b/impl/test/phase11-coordination-store.test.mjs\nindex a696b25..0eaa0a2 100644\n--- a/impl/test/phase11-coordination-store.test.mjs\n+++ b/impl/test/phase11-coordination-store.test.mjs\n@@ -375,6 +375,84 @@ test('CK2/CK8: blocking input and resolution transition durably before terminal\n   assert.equal(driver.coordination.task('durable-input').status, 'completed');\n });\n \n+test('CK8/CK9: input transition append failure poisons before pending/blocked state becomes visible', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'input crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'input-write-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.transitioned\"') && body.includes('\"input_required\"')) throw new Error('input transition disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  assert.throws(() => driver.coordinator._handleEvent({\n+    worker: handle.id, harness: 'mock@1.0.0', turnEpoch: driver.coordinator.list()[0].turnEpoch,\n+    kind: 'question.asked', actor: 'worker', payload: { requestId: 'q-crash', question: 'continue?', blocking: true },\n+  }), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(driver.coordination.task('input-write-failure').status, 'working');\n+  assert.equal(driver.coordinator._pending.size, 0);\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n+  await adapter.kill(handle.id);\n+});\n+\n+test('CK8/CK9: stop-intent append failure calls no adapter and leaves durable task working', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  let adapterKills = 0;\n+  const rawKill = adapter.kill.bind(adapter);\n+  adapter.kill = async (...args) => { adapterKills += 1; return rawKill(...args); };\n+  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'stop crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'stop-write-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"control.stop_requested\"')) throw new Error('stop intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(driver.coordinator.kill(handle.id, 'human'), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(adapterKills, 0);\n+  assert.equal(driver.coordination.task('stop-write-failure').status, 'working');\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n+  driver.coordination._appendFile = rawAppend;\n+  await rawKill(handle.id);\n+});\n+\n+test('CK8/CK9: cancellation completion failure resolves bounded, keeps stop intent, and restart closes task', async () => {\n+  const repo = dir();\n+  execFileSync('git', ['init', '-q'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n+  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n+  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n+  const logDir = dir();\n+  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n+  const driver = createDriver({ repoRoot: repo, logDir, adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n+  const brief = { goal: 'cancel completion crash', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n+  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'cancel-completion-failure' });\n+  const rawAppend = driver.coordination._appendFile;\n+  driver.coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.transitioned\"') && body.includes('\"cancelled\"')) throw new Error('cancel completion disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  const stopped = await driver.coordinator.kill(handle.id, 'human');\n+  assert.deepEqual(stopped, { ok: false, result: 'coordination_unavailable' });\n+  assert.equal(driver.coordination.task('cancel-completion-failure').status, 'working');\n+  assert.equal(driver.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'control.stop_requested'), true);\n+  assert.equal(driver.coordinator._workers.get(handle.id).status, 'dead');\n+  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\n+  driver.coordination._appendFile = rawAppend;\n+  const replay = createDriver({ repoRoot: repo, logDir, coordination: driver.coordination, adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) }, watchdog: { stallMs: 0 } });\n+  assert.equal(replay.coordination.task('cancel-completion-failure').status, 'failed');\n+});\n+\n test('CK4: Scratch claims conservatively conflict, warn cross-tree, and expire only by event', () => {\n   const root = dir();\n   const store = new CoordinationStore(root);\ndiff --git a/impl/test/phase11-persistent-sessions.test.mjs b/impl/test/phase11-persistent-sessions.test.mjs\nindex 85e4f9a..5cba651 100644\n--- a/impl/test/phase11-persistent-sessions.test.mjs\n+++ b/impl/test/phase11-persistent-sessions.test.mjs\n@@ -149,6 +149,26 @@ test('PS2: a follow-up exception is a refused Ack and preserves the prior result\n   assert.equal(c.list()[0].status, 'idle');\n });\n \n+test('CK8/CK9: follow-up refinement failure kills an already-advanced native session', async () => {\n+  const ad = adapter();\n+  const { c, coordination } = harness(ad);\n+  const h = await c.spawn('session', brief(), { taskId: 'follow-refinement-failure' });\n+  ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('follow-up refinement disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(c.send(h.id, 'advance native state', 'turn'), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(ad.calls.prompt.length, 1);\n+  assert.equal(ad.calls.kill.length, 1);\n+  assert.equal(c._workers.get(h.id).status, 'dead');\n+  assert.equal(coordination.snapshot().tasks.length, 1);\n+  assert.equal(coordination.snapshot().tasks[0].status, 'completed');\n+  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'follow_up.requested'), true);\n+});\n+\n test('PS2: emitted turn facts followed by refusal are a protocol violation and kill the ambiguous session', async () => {\n   let ad;\n   ad = adapter({\n@@ -413,6 +433,64 @@ test('PS7: replayed session is reattached only after bounded handshake proves th\n   assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_attached'));\n });\n \n+test('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\n+  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\n+  const original = adapter();\n+  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n+  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\n+  await until(() => c.list()[0].sessionContext);\n+  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\n+  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+\n+  const resumed = adapter();\n+  let spawnCalls = 0;\n+  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\n+  const replay = new Coordinator({\n+    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n+    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n+    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n+  });\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n+  assert.equal(spawnCalls, 0);\n+  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n+});\n+\n+test('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\n+  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-refine-wt-'));\n+  const original = adapter();\n+  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n+  const h = await c.spawn('session', brief(), { taskId: 'recover-refine-task' });\n+  await until(() => c.list()[0].sessionContext);\n+  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\n+  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n+  await until(async () => (await c.result(h.id)).ready);\n+\n+  const resumed = adapter();\n+  resumed.spawn = async (worker) => {\n+    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\n+    return { ok: true };\n+  };\n+  const replay = new Coordinator({\n+    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n+    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n+    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n+  });\n+  const rawAppend = coordination._appendFile;\n+  coordination._appendFile = (file, body, encoding) => {\n+    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('recovery refinement disk full');\n+    return rawAppend(file, body, encoding);\n+  };\n+  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n+  await until(() => resumed.calls.kill.length === 1);\n+  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n+});\n+\n test('PS7: a reattachment identity mismatch is refused and the untrusted transport is killed', async () => {\n   const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-bad-wt-'));\n   const original = adapter();\ndiff --git a/spec/phase11/coordination-knowledge.md b/spec/phase11/coordination-knowledge.md\nindex f6d50af..23ad20d 100644\n--- a/spec/phase11/coordination-knowledge.md\n+++ b/spec/phase11/coordination-knowledge.md\n@@ -66,6 +66,15 @@ accept/reject, and integration/publication outcomes are reflected by typed event\n queued tasks, dependency readiness, assignments, terminal state, and automatic identifiers before\n dispatching anything.\n \n+Every adapter- or outside-world-reaching action has a durable intent before the effect: stop,\n+persistent follow-up, recovery attach, review-task creation, local integration, publication\n+request, and publication authorization. If intent append fails, no adapter, worktree/Git merge, or\n+publisher is called. If an adapter has already confirmed stop/attach/turn advancement when a later\n+terminal/refinement append fails, the coordinator is poisoned, resolves any public waiter with a\n+typed coordination-unavailable result, kills or quarantines the ambiguous transport, and restart\n+closes the still-nonterminal durable task. Asynchronous adapter callbacks may not turn an already\n+recorded fatal coordination fault into an uncaught process exception.\n+\n `task.created` persists `brief`, `deps`, `refines`, `taskType`, requested vendor/model/session\n policy, and the reserved public handle ID. `task.claimed` persists assignee, resolved vendor/model,\n and expected/new versions. Refusal codes are `stale_version | already_assigned |\n@@ -227,6 +236,11 @@ Temp-directory and temp-Git tests must prove before provider dogfooding:\n     order without treating their local sequences as globally ordered; and\n 15. after a logged knowledge read, invalidating the node yields a nonempty contamination record\n     and `affectedReaders()` result; a forced read-log append failure returns no recalled content.\n+16. injected append failures cover create, claim, input wait/resume, stop intent, cancel terminal,\n+    persistent follow-up/recovery refinement, review creation, terminal artifact batch,\n+    integration intent, publication authorization, and knowledge/Scratch reads. Pre-effect\n+    failures call no adapter/Git/publisher; post-effect failures are bounded, poison authority,\n+    preserve the earlier intent, and replay to a non-success terminal state.\n \n After CK9 is green and committed, Baton may recursively run a real provider review against this\n spec and implementation. The recursive task must use isolated runtime credentials/budgets, exact\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'git diff c8a272e''^ c8a272e -- impl/src/coordinator.mjs impl/test/phase11-acceptance-integration.test.mjs impl/test/phase11-coordination-store.test.mjs impl/test/phase11-persistent-sessions.test.mjs spec/phase11/coordination-knowledge.md'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":18,"ts":"2026-07-11T20:20:05.335Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":18130,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":40324,"inputTokens":39061,"cachedInputTokens":14464,"outputTokens":1263,"reasoningOutputTokens":451},"last":{"totalTokens":18130,"inputTokens":17710,"cachedInputTokens":10624,"outputTokens":420,"reasoningOutputTokens":46},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":40324,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":19,"ts":"2026-07-11T20:20:05.336Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":20,"ts":"2026-07-11T20:20:05.337Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"I have enough surface coverage now to stop broad searching. I’m reading the critical coordinator paths with line numbers to see whether the new tests actually cover the failure modes the spec cares about, or only the happy-path shape around them."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":21,"ts":"2026-07-11T20:20:22.181Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_zrGwlX6UrePIvH0Pporg7ySX","command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '242,430p;460,520p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"68595","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '242,430p;460,520p'","name":"phase11-acceptance-integration.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-acceptance-integration.test.mjs"}],"aggregatedOutput":"   242\ttest('CK8/CK9: review task creation failure reaches no reviewer adapter and preserves parent evidence', async () => {\n   243\t  const root = repo();\n   244\t  commitBase(root);\n   245\t  const implementer = familyAdapter('family-a', { outcome: 'completed', edits: [{ path: 'src/reviewed.txt', content: 'review me\\n' }] });\n   246\t  const reviewer = familyAdapter('family-b', { outcome: 'completed' });\n   247\t  let reviewerSpawns = 0;\n   248\t  const rawSpawn = reviewer.spawn.bind(reviewer);\n   249\t  reviewer.spawn = async (...args) => { reviewerSpawns += 1; return rawSpawn(...args); };\n   250\t  const driver = createDriver({ repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-review-fault-log-')), adapters: { implementer, reviewer }, watchdog: { stallMs: 0 } });\n   251\t  const parent = await driver.coordinator.spawn('implementer', brief({ command: 'test -f src/reviewed.txt', expectExit: 0 }), { taskId: 'review-fault-parent' });\n   252\t  await until(async () => (await driver.coordinator.result(parent.id)).ready);\n   253\t  const rawAppend = driver.coordination._appendFile;\n   254\t  driver.coordination._appendFile = (file, body, encoding) => {\n   255\t    if (body.includes('\"task.created\"') && body.includes('\"review\"')) throw new Error('review task disk full');\n   256\t    return rawAppend(file, body, encoding);\n   257\t  };\n   258\t  await assert.rejects(driver.coordinator.spawnReview(parent.id, 'reviewer', {\n   259\t    kind: 'review', taskId: 'review-fault-child', verification: { command: 'true', expectExit: 0 },\n   260\t  }), (error) => error.code === 'coordination_write_unavailable');\n   261\t  assert.equal(reviewerSpawns, 0);\n   262\t  assert.deepEqual(driver.coordination.snapshot().tasks.map((task) => [task.id, task.status]), [['review-fault-parent', 'completed']]);\n   263\t});\n   264\t\n   265\ttest('AC5: ff-only integration reaps the worker/worktree/branch and records exact SHAs', async () => {\n   266\t  const root = repo();\n   267\t  commitBase(root, { 'README.md': 'base\\n' });\n   268\t  const beforeSha = git(['rev-parse', 'HEAD'], root);\n   269\t  const { coordinator, coordination, log, handle } = await completedTask(root);\n   270\t  const taskWorktree = coordinator.list().find((worker) => worker.id === handle.id)?.worktree;\n   271\t  assert.equal(typeof taskWorktree, 'string');\n   272\t\n   273\t  const response = await coordinator.integrate(handle.id, { strategy: 'ff-only', actor: 'test-orchestrator' });\n   274\t  assert.equal(response.ok, true);\n   275\t  assert.equal(response.result, 'integrated');\n   276\t  assert.equal(response.integration.beforeSha, beforeSha);\n   277\t  assert.equal(response.integration.afterSha, response.integration.resultSha);\n   278\t  assert.equal(git(['rev-parse', 'HEAD'], root), response.integration.resultSha);\n   279\t  assert.equal(existsSync(join(root, 'src/integrated.txt')), true);\n   280\t  assert.equal(coordinator.list().find((worker) => worker.id === handle.id)?.status, 'dead');\n   281\t  assert.equal(existsSync(taskWorktree), false);\n   282\t  assert.equal(git(['branch', '--list', 'baton/integrate-me'], root), '');\n   283\t  assert.equal(git(['for-each-ref', '--format=%(refname)', `refs/baton/results/${response.integration.resultSha}`], root), '');\n   284\t  assert.deepEqual((await coordinator.result(handle.id)).integration, response.integration);\n   285\t  const event = log.read(handle.id).find((entry) => entry.kind === 'integration.completed');\n   286\t  assert.equal(event.actor, 'test-orchestrator');\n   287\t  assert.equal(event.payload.afterSha, response.integration.afterSha);\n   288\t  assert.equal(coordination.snapshot().artifacts.some((artifact) => artifact.mediaType === 'application/vnd.baton.integration+json'), true);\n   289\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.completed'), true);\n   290\t  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:integrate:')), true);\n   291\t  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'integration'), true);\n   292\t});\n   293\t\n   294\ttest('AC5: a non-fast-forward main refuses without rewriting either tip', async () => {\n   295\t  const root = repo();\n   296\t  commitBase(root, { 'README.md': 'base\\n' });\n   297\t  const { coordinator, coordination, log, handle } = await completedTask(root, 'diverged-task');\n   298\t  writeFileSync(join(root, 'main-only.txt'), 'main advanced\\n');\n   299\t  git(['add', 'main-only.txt'], root);\n   300\t  git(['commit', '-q', '-m', 'advance main independently'], root);\n   301\t  const mainBefore = git(['rev-parse', 'HEAD'], root);\n   302\t  const taskSha = (await coordinator.result(handle.id)).integration?.resultSha\n   303\t    ?? log.read(handle.id).find((entry) => entry.kind === 'verify.reverified')?.payload?.capture?.sha;\n   304\t\n   305\t  await assert.rejects(\n   306\t    coordinator.integrate(handle.id),\n   307\t    (error) => error instanceof IntegrationError && error.code === 'non_fast_forward_or_dirty',\n   308\t  );\n   309\t  assert.equal(git(['rev-parse', 'HEAD'], root), mainBefore);\n   310\t  assert.notEqual(mainBefore, taskSha);\n   311\t  assert.equal(git(['show', '-s', '--format=%s', 'HEAD'], root), 'advance main independently');\n   312\t  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   313\t  const refusedResult = await coordinator.result(handle.id);\n   314\t  assert.equal(refusedResult.integration, null);\n   315\t  assert.equal(refusedResult.retainedResultRef, `refs/baton/results/${taskSha}`);\n   316\t  assert.equal(git(['show-ref', '--verify', refusedResult.retainedResultRef], root).split(' ')[0], taskSha);\n   317\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'integration.refused'), true);\n   318\t});\n   319\t\n   320\ttest('AC5: a dirty main refuses without touching the index or working tree', async () => {\n   321\t  const root = repo();\n   322\t  commitBase(root, { 'README.md': 'base\\n' });\n   323\t  const { coordinator, log, handle } = await completedTask(root, 'dirty-main-task');\n   324\t  writeFileSync(join(root, 'README.md'), 'uncommitted user edit\\n');\n   325\t  const headBefore = git(['rev-parse', 'HEAD'], root);\n   326\t  const statusBefore = git(['status', '--porcelain=v1'], root);\n   327\t\n   328\t  await assert.rejects(coordinator.integrate(handle.id), IntegrationError);\n   329\t  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n   330\t  assert.equal(git(['status', '--porcelain=v1'], root), statusBefore);\n   331\t  assert.equal(log.read(handle.id).at(-1).kind, 'integration.refused');\n   332\t  const retained = (await coordinator.result(handle.id)).retainedResultRef;\n   333\t  assert.equal(git(['show-ref', '--verify', retained], root).endsWith(` ${retained}`), true);\n   334\t});\n   335\t\n   336\ttest('AC5: integration refuses an unaccepted captured result', async () => {\n   337\t  const root = repo();\n   338\t  commitBase(root);\n   339\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed' } });\n   340\t  const { coordinator } = createDriver({\n   341\t    repoRoot: root, logDir: mkdtempSync(join(tmpdir(), 'baton-ac5-reject-log-')), adapters: { mock: adapter },\n   342\t    watchdog: { stallMs: 0 },\n   343\t  });\n   344\t  const handle = await coordinator.spawn('mock', brief({ command: 'false', expectExit: 0 }), { taskId: 'rejected-result' });\n   345\t  await until(async () => (await coordinator.result(handle.id)).ready);\n   346\t  assert.equal((await coordinator.result(handle.id)).status, 'failed');\n   347\t  await assert.rejects(\n   348\t    coordinator.integrate(handle.id),\n   349\t    (error) => error instanceof IntegrationError && error.code === 'result_not_accepted',\n   350\t  );\n   351\t});\n   352\t\n   353\ttest('CK8/CK9: integration intent append failure leaves Git and worker ownership untouched', async () => {\n   354\t  const root = repo();\n   355\t  commitBase(root);\n   356\t  const { coordinator, coordination, handle } = await completedTask(root, 'integration-intent-failure');\n   357\t  const headBefore = git(['rev-parse', 'HEAD'], root);\n   358\t  const workerBefore = coordinator._workers.get(handle.id);\n   359\t  const rawAppend = coordination._appendFile;\n   360\t  coordination._appendFile = (file, body, encoding) => {\n   361\t    if (body.includes('\"kind\":\"integration.requested\"')) throw new Error('integration intent disk full');\n   362\t    return rawAppend(file, body, encoding);\n   363\t  };\n   364\t  await assert.rejects(coordinator.integrate(handle.id), (error) => error.code === 'coordination_write_unavailable');\n   365\t  assert.equal(git(['rev-parse', 'HEAD'], root), headBefore);\n   366\t  assert.equal(workerBefore.status, 'idle');\n   367\t  assert.equal(existsSync(workerBefore.worktree), true);\n   368\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'integration.requested'), false);\n   369\t});\n   370\t\n   371\ttest('AC6: publication has no side effect before approval and allow publishes the exact integrated SHA once', async () => {\n   372\t  const { coordinator, coordination, log, handle, calls } = await integratedPublicationTask();\n   373\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   374\t  assert.equal(calls.length, 0);\n   375\t  assert.deepEqual(requested.target, {\n   376\t    remote: 'origin', ref: 'refs/heads/main', sha: (await coordinator.result(handle.id)).integration.afterSha,\n   377\t  });\n   378\t  const event = log.read(handle.id).at(-1);\n   379\t  assert.equal(event.kind, 'publication.requested');\n   380\t  assert.equal(JSON.stringify(event).includes('credential'), false);\n   381\t\n   382\t  const [a, b] = await Promise.all([\n   383\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   384\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'other-user'),\n   385\t  ]);\n   386\t  assert.equal(calls.length, 1);\n   387\t  assert.deepEqual(calls[0], requested.target);\n   388\t  assert.deepEqual(new Set([a.result, b.result]), new Set(['published', 'already_resolved']));\n   389\t  assert.equal((await coordinator.result(handle.id)).publication.sha, requested.target.sha);\n   390\t  assert.equal(log.read(handle.id).filter((entry) => entry.kind === 'publication.completed').length, 1);\n   391\t  assert.equal(coordination.events().some((entry) => entry.kind === 'driver.recorded' && entry.payload.kind === 'publication.completed'), true);\n   392\t  assert.equal(coordination.queryKnowledge({ types: ['Decision'] }).some((node) => node.id.startsWith('decision:publish:')), true);\n   393\t  assert.equal(coordination.events().some((entry) => entry.kind === 'knowledge.promoted' && entry.payload.promotion?.trigger === 'publication'), true);\n   394\t});\n   395\t\n   396\ttest('CK8/CK9: publication authorization append failure invokes no publisher', async () => {\n   397\t  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n   398\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   399\t  const rawAppend = coordination._appendFile;\n   400\t  coordination._appendFile = (file, body, encoding) => {\n   401\t    if (body.includes('\"kind\":\"publication.authorized\"')) throw new Error('publication authorization disk full');\n   402\t    return rawAppend(file, body, encoding);\n   403\t  };\n   404\t  await assert.rejects(\n   405\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   406\t    (error) => error.code === 'coordination_write_unavailable',\n   407\t  );\n   408\t  assert.equal(calls.length, 0);\n   409\t  assert.equal(coordinator._pending.get(requested.requestId).state, 'pending');\n   410\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n   411\t});\n   412\t\n   413\ttest('CK8/CK9: post-publish completion failure is bounded and preserves prior authorization', async () => {\n   414\t  const { coordinator, coordination, handle, calls } = await integratedPublicationTask();\n   415\t  const requested = coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main' }, 'test-user');\n   416\t  const rawAppend = coordination._appendFile;\n   417\t  coordination._appendFile = (file, body, encoding) => {\n   418\t    if (body.includes('\"kind\":\"publication.completed\"')) throw new Error('publication completion disk full');\n   419\t    return rawAppend(file, body, encoding);\n   420\t  };\n   421\t  await assert.rejects(\n   422\t    coordinator.respond(requested.requestId, { decision: 'allow', fence: requested.fence }, 'test-user'),\n   423\t    (error) => error.code === 'coordination_write_unavailable',\n   424\t  );\n   425\t  assert.equal(calls.length, 1);\n   426\t  assert.equal(coordinator._pending.get(requested.requestId).state, 'resolved');\n   427\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.authorized'), true);\n   428\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'publication.completed'), false);\n   429\t});\n   430\t\n   460\t\n   461\ttest('AC6: restart drops a pending approval and cannot publish it by replay', async () => {\n   462\t  const first = await integratedPublicationTask();\n   463\t  const request = first.coordinator.requestPublication(first.handle.id, { remote: 'origin', ref: 'refs/heads/main' });\n   464\t  const replayCalls = [];\n   465\t  const replay = createDriver({\n   466\t    repoRoot: first.root, logDir: first.logDir,\n   467\t    adapters: { mock: new MockAdapter({ scenario: { outcome: 'completed' } }) },\n   468\t    publisher: async (target) => { replayCalls.push(target); }, watchdog: { stallMs: 0 },\n   469\t  });\n   470\t  const response = await replay.coordinator.respond(request.requestId, { decision: 'allow', fence: request.fence }, 'test-user');\n   471\t  assert.equal(response.result, 'not_found');\n   472\t  assert.equal(replayCalls.length, 0);\n   473\t});\n   474\t\n   475\ttest('AC6: credential-bearing remotes and mismatched SHAs are rejected before logging', async () => {\n   476\t  const { coordinator, log, handle } = await integratedPublicationTask();\n   477\t  const before = log.read(handle.id).length;\n   478\t  assert.throws(\n   479\t    () => coordinator.requestPublication(handle.id, { remote: 'https://token@example.com/repo.git', ref: 'refs/heads/main' }),\n   480\t    (error) => error instanceof PublicationError && error.code === 'invalid_remote',\n   481\t  );\n   482\t  assert.throws(\n   483\t    () => coordinator.requestPublication(handle.id, { remote: 'origin', ref: 'refs/heads/main', sha: 'deadbeef' }),\n   484\t    (error) => error instanceof PublicationError && error.code === 'sha_mismatch',\n   485\t  );\n   486\t  assert.equal(log.read(handle.id).length, before);\n   487\t});\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/test/phase11-acceptance-integration.test.mjs | sed -n '242,430p;460,520p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":22,"ts":"2026-07-11T20:20:22.239Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_5WdQDfrvNwvYWLQ9DIrqP4Lu","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '1528,1568p;1790,1888p;1928,2045p;2878,3055p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"25456","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '1528,1568p;1790,1888p;1928,2045p;2878,3055p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"  1528\t    if (!this._coordination || !event) return null;\n  1529\t    return this._coordination.mapOperationalEvent(event, { actor: 'policy', key }).evidence;\n  1530\t  }\n  1531\t\n  1532\t  _coordMapEvent(event) {\n  1533\t    if (!event) return null;\n  1534\t    return this._coordMap(event, `evidence:${event.worker}:${event.seq}`);\n  1535\t  }\n  1536\t\n  1537\t  _coordRecord(kind, payload, key, actor = 'policy') {\n  1538\t    if (!this._coordination) return null;\n  1539\t    return this._coordination.recordDriver(kind, payload, { actor, key }).event;\n  1540\t  }\n  1541\t\n  1542\t  _poisonCoordination(err) {\n  1543\t    if (!this._fatalError) {\n  1544\t      const fatal = new Error(`authoritative coordination mutation failed: ${err?.message ?? err}`, { cause: err });\n  1545\t      fatal.name = 'CoordinationWriteIntegrityError';\n  1546\t      fatal.code = 'coordination_write_unavailable';\n  1547\t      this._fatalError = fatal;\n  1548\t      for (const handle of this._workers.values()) {\n  1549\t        if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) handle.spawnAbort.abort({ reason: 'coordination_write_unavailable' });\n  1550\t      }\n  1551\t    }\n  1552\t    return this._fatalError;\n  1553\t  }\n  1554\t\n  1555\t  _createCoordinationRefinement(handle, prior, relation) {\n  1556\t    if (!this._coordination) return prior;\n  1557\t    const id = `${prior.id}:refinement-${++this._refinementSeq}`;\n  1558\t    const created = this._coordination.createTask({\n  1559\t      id, brief: prior.brief, deps: [], refines: prior.id, taskType: prior.taskType,\n  1560\t      reservedWorkerId: handle.id, vendorRequested: handle.vendor,\n  1561\t      modelRequested: handle.modelRequested, modelPolicy: handle.modelPolicy,\n  1562\t      sessionRequest: handle.sessionRequest, relation,\n  1563\t    }, { actor: 'orchestrator', key: `task.created:${id}` });\n  1564\t    const claimed = this._coordination.claimTask(id, handle.id, created.task.version, {\n  1565\t      actor: 'orchestrator', key: `task.claimed:${id}:${created.task.version}`,\n  1566\t    });\n  1567\t    const next = {\n  1568\t      ...prior, id, deps: [], refines: prior.id, status: 'working', result: null, verdict: null,\n  1790\t      });\n  1791\t  }\n  1792\t\n  1793\t  _onStopConfirmed(handle, confirmKind) {\n  1794\t    const waiter = this._stopWaiters.get(handle.id);\n  1795\t    if (!waiter) return;\n  1796\t    if (confirmKind !== waiter.mode) return; // stale/mismatched confirmation — ignore\n  1797\t    waiter.confirmReceived = true;\n  1798\t    if (waiter.ackReady) this._finalizeStop(handle.id, waiter);\n  1799\t  }\n  1800\t\n  1801\t  _finalizeStop(workerId, waiter) {\n  1802\t    if (waiter.finalized) return;\n  1803\t    waiter.finalized = true;\n  1804\t    if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\n  1805\t    const handle = this._workers.get(workerId);\n  1806\t    const harness = handle ? this._harnessOf(handle.vendor) : '';\n  1807\t    const kind = waiter.mode === 'kill' ? 'kill.confirmed' : 'control.interrupt_confirmed';\n  1808\t    const ev = {\n  1809\t      worker: workerId, harness, turnEpoch: handle ? this._safeTurnEpoch(handle) : 0, kind, actor: 'worker', payload: {},\n  1810\t      modelRequested: handle?.modelRequested ?? null, modelResolved: handle?.modelResolved ?? null, modelObserved: handle?.modelObserved ?? null,\n  1811\t    };\n  1812\t    if (waiter.emulated) ev.emulated = true;\n  1813\t    const stopEvent = this._log.append(ev);\n  1814\t\n  1815\t    try {\n  1816\t      if (handle) {\n  1817\t        const task = this._tasks.get(handle.taskId);\n  1818\t        if (waiter.mode === 'kill') {\n  1819\t          if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n  1820\t            const evidence = this._coordMapEvent(stopEvent);\n  1821\t            this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n  1822\t          }\n  1823\t          handle.status = 'dead';\n  1824\t          this._removeRuntimeScope(handle);\n  1825\t          if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n  1826\t          this._removeTaskWorktree(task).catch(noop);\n  1827\t        } else {\n  1828\t          if (waiter.then !== undefined) {\n  1829\t            const stamp = this._fences.bumpTurn(handle.id);\n  1830\t            handle.status = 'working';\n  1831\t            handle.turnTerminalObserved = false;\n  1832\t            this._clearBudgetStop(handle);\n  1833\t            if (task) {\n  1834\t              task.status = 'working';\n  1835\t              task.result = null;\n  1836\t              task.verdict = null;\n  1837\t            }\n  1838\t            this._log.append({\n  1839\t              worker: workerId, harness, turnEpoch: stamp.turnEpoch, kind: 'lifecycle.turn_started', actor: 'orchestrator',\n  1840\t              modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n  1841\t              payload: { followUp: true, afterInterrupt: true },\n  1842\t            });\n  1843\t            this._resetWatchdogTurn(handle);\n  1844\t          } else {\n  1845\t            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n  1846\t              const evidence = this._coordMapEvent(stopEvent);\n  1847\t              this._coordTransition(task, 'cancelled', `task.cancelled:${task.id}:${stopEvent.seq}`, evidence);\n  1848\t            }\n  1849\t            handle.status = 'idle';\n  1850\t            if (task && !TERMINAL_TASK_STATUSES.has(task.status)) task.status = 'cancelled';\n  1851\t          }\n  1852\t        }\n  1853\t      }\n  1854\t    } catch (err) {\n  1855\t      if (handle) {\n  1856\t        handle.status = 'dead';\n  1857\t        this._removeRuntimeScope(handle);\n  1858\t        this._removeTaskWorktree(this._tasks.get(handle.taskId)).catch(noop);\n  1859\t        Promise.resolve(this._adapters[handle.vendor]?.kill(handle.id)).catch(noop);\n  1860\t      }\n  1861\t      for (const resolve of waiter.resolvers) resolve({ ok: false, result: 'coordination_unavailable' });\n  1862\t      this._stopWaiters.delete(workerId);\n  1863\t      throw err;\n  1864\t    }\n  1865\t\n  1866\t    const result = { ok: true, result: 'confirmed', emulated: waiter.emulated === true };\n  1867\t    for (const resolve of waiter.resolvers) resolve(result);\n  1868\t    this._stopWaiters.delete(workerId);\n  1869\t    this._dispatchPass();\n  1870\t  }\n  1871\t\n  1872\t  _forceStop(workerId, waiter) {\n  1873\t    if (waiter.finalized) return;\n  1874\t    waiter.finalized = true;\n  1875\t    if (waiter.timerHandle != null) this._clearTimeout(waiter.timerHandle);\n  1876\t    const handle = this._workers.get(workerId);\n  1877\t    const harness = handle ? this._harnessOf(handle.vendor) : '';\n  1878\t    const forcedEvent = this._log.append({ worker: workerId, harness, turnEpoch: handle ? this._safeTurnEpoch(handle) : 0, kind: 'control.forced_stop', actor: 'policy', payload: {} });\n  1879\t\n  1880\t    let coordinationFailure = null;\n  1881\t    if (handle) {\n  1882\t      const task = this._tasks.get(handle.taskId);\n  1883\t      if (task && !TERMINAL_TASK_STATUSES.has(task.status)) {\n  1884\t        try {\n  1885\t          const evidence = this._coordMapEvent(forcedEvent);\n  1886\t          this._coordTransition(task, 'failed', `task.failed:${task.id}:${forcedEvent.seq}`, evidence);\n  1887\t        } catch (err) {\n  1888\t          coordinationFailure = err;\n  1928\t      }\n  1929\t      return this._resolveRecord(requestId, answer, actor);\n  1930\t    }\n  1931\t    if (record.state !== 'pending') return { ok: false, result: 'already_resolved', resolution: record.resolution };\n  1932\t\n  1933\t    // CI2: reserve the single-consumer slot, but do not COMMIT resolution until the adapter\n  1934\t    // accepts delivery. A failed/throwing wire operation rolls back to pending for retry.\n  1935\t    record.state = 'resolving';\n  1936\t    let releaseResolving;\n  1937\t    record.resolvingDone = new Promise((resolve) => { releaseResolving = resolve; });\n  1938\t    const finishResolving = () => {\n  1939\t      releaseResolving();\n  1940\t      delete record.resolvingDone;\n  1941\t    };\n  1942\t\n  1943\t    const handle = this._workers.get(record.worker);\n  1944\t\n  1945\t    if (record.kind === 'publication') {\n  1946\t      const decision = answer?.decision;\n  1947\t      if (!['allow', 'deny'].includes(decision)) {\n  1948\t        record.state = 'pending';\n  1949\t        finishResolving();\n  1950\t        return { ok: false, result: 'invalid_decision' };\n  1951\t      }\n  1952\t      const currentFence = handle ? this._fences.current(handle.id).fence : null;\n  1953\t      const fenceValid = actor === 'policy' || answer?.fence === record.fenceAtAsk;\n  1954\t      if (!handle || !fenceValid || currentFence !== record.fenceAtAsk) {\n  1955\t        if (handle) {\n  1956\t          const refusedEvent = this._log.append({\n  1957\t            worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1958\t            kind: 'publication.refused', actor: 'policy',\n  1959\t            payload: { requestId, reason: 'stale_fence', remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n  1960\t          });\n  1961\t          const evidence = this._coordMapEvent(refusedEvent);\n  1962\t          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\n  1963\t        }\n  1964\t        record.state = 'resolved';\n  1965\t        record.consumer = actor;\n  1966\t        record.resolution = { decision: 'deny', reason: 'stale_fence' };\n  1967\t        finishResolving();\n  1968\t        return { ok: false, result: 'stale_fence', current: currentFence };\n  1969\t      }\n  1970\t      if (decision === 'deny') {\n  1971\t        const deniedEvent = this._log.append({\n  1972\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1973\t          kind: 'publication.denied', actor,\n  1974\t          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha },\n  1975\t        });\n  1976\t        const evidence = this._coordMapEvent(deniedEvent);\n  1977\t        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\n  1978\t        record.state = 'resolved';\n  1979\t        record.consumer = actor;\n  1980\t        record.resolution = { decision: 'deny' };\n  1981\t        finishResolving();\n  1982\t        return { ok: true, result: 'denied' };\n  1983\t      }\n  1984\t      if (typeof this._publisher !== 'function') {\n  1985\t        record.state = 'pending';\n  1986\t        finishResolving();\n  1987\t        return { ok: false, result: 'publication_unavailable' };\n  1988\t      }\n  1989\t      let authorizedEvent;\n  1990\t      try {\n  1991\t        authorizedEvent = this._log.append({\n  1992\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1993\t          kind: 'publication.authorized', actor,\n  1994\t          payload: { requestId, remote: record.publication.remote, ref: record.publication.ref, sha: record.publication.sha, fence: record.fenceAtAsk },\n  1995\t        });\n  1996\t        const evidence = this._coordMapEvent(authorizedEvent);\n  1997\t        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\n  1998\t      } catch (err) {\n  1999\t        record.state = 'pending';\n  2000\t        finishResolving();\n  2001\t        throw err;\n  2002\t      }\n  2003\t      let published;\n  2004\t      try {\n  2005\t        published = await this._publisher(record.publication);\n  2006\t      } catch (err) {\n  2007\t        record.state = 'pending';\n  2008\t        finishResolving();\n  2009\t        throw new PublicationError(String(err?.message ?? err), 'publisher_failed');\n  2010\t      }\n  2011\t      record.state = 'resolved';\n  2012\t      record.consumer = actor;\n  2013\t      record.resolution = { decision: 'allow' };\n  2014\t      const task = this._tasks.get(handle.taskId);\n  2015\t      void published;\n  2016\t      task.publication = Object.freeze({ requestId, ...record.publication, actor });\n  2017\t      try {\n  2018\t        const publicationEvent = this._log.append({\n  2019\t          worker: handle.id, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  2020\t          kind: 'publication.completed', actor, payload: task.publication,\n  2021\t        });\n  2022\t        const publicationEvidence = this._coordMapEvent(publicationEvent);\n  2023\t        this._coordination?.promoteKnowledgeNode({\n  2024\t          id: `decision:publish:${task.id}:${publicationEvent.seq}`, type: 'Decision',\n  2025\t          body: `Published task ${task.id} to ${task.publication.remote}/${task.publication.ref}`,\n  2026\t          grounding: 'observed', informedBy: [`task:${task.id}`],\n  2027\t          evidence: [{ coordinationSeq: publicationEvidence.coordinationSeq }],\n  2028\t        }, { kind: 'Decision', trigger: 'publication' }, { actor, key: `knowledge.publication:${task.id}:${publicationEvent.seq}` });\n  2029\t        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n  2030\t      } catch (err) {\n  2031\t        finishResolving();\n  2032\t        throw err;\n  2033\t      }\n  2034\t      finishResolving();\n  2035\t      return { ok: true, result: 'published', publication: task.publication };\n  2036\t    }\n  2037\t\n  2038\t    const clearPending = () => {\n  2039\t      if (!handle) return;\n  2040\t      if (record.kind === 'question' && handle.pendingQuestionId === requestId) handle.pendingQuestionId = null;\n  2041\t      if (record.kind === 'approval' && handle.pendingApprovalId === requestId) handle.pendingApprovalId = null;\n  2042\t      if (handle.status === 'blocked') {\n  2043\t        handle.status = 'working';\n  2044\t        const task = this._tasks.get(handle.taskId);\n  2045\t        if (task && task.status === 'input_required') task.status = 'working';\n  2878\t      const currentDurableTask = durableWorkerTasks.at(-1) ?? null;\n  2879\t      if (currentDurableTask) taskId = currentDurableTask.id;\n  2880\t\n  2881\t      // Operational completion without its authoritative coordination terminal batch is a crash\n  2882\t      // gap, never permission to infer success from telemetry. Fail the claimed task durably so a\n  2883\t      // restart cannot leave it working forever or fabricate its missing accepted manifests.\n  2884\t      if (currentDurableTask && !TERMINAL_TASK_STATUSES.has(currentDurableTask.status)\n  2885\t        && TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2886\t        recoveryTerminalized = true;\n  2887\t        const priorOperationalStatus = terminalStatus;\n  2888\t        terminalStatus = 'failed';\n  2889\t        const gapEvent = this._log.append({\n  2890\t          worker: workerId, harness: events.at(-1)?.harness ?? '', turnEpoch: maxTurnEpoch,\n  2891\t          kind: 'control.recovery_terminalized', actor: 'policy',\n  2892\t          payload: { reason: 'coordination_terminal_batch_missing', priorStatus: priorOperationalStatus },\n  2893\t        });\n  2894\t        const gapEvidence = this._coordMapEvent(gapEvent);\n  2895\t        const transitioned = this._coordination.transitionTask(currentDurableTask.id, 'failed', currentDurableTask.version, {\n  2896\t          actor: 'policy', key: `task.failed:${currentDurableTask.id}:coordination_gap:${gapEvent.seq}`,\n  2897\t        }, gapEvidence ?? { reason: 'coordination_terminal_batch_missing' });\n  2898\t        const seeded = this._tasks.get(currentDurableTask.id);\n  2899\t        if (seeded) seeded.coordinationVersion = transitioned.task.version;\n  2900\t      }\n  2901\t\n  2902\t      // CI6: replay cannot resurrect an adapter session. Until the persistent-session phase can\n  2903\t      // prove native reattachment, any nonterminal reconstructed task is durably failed and its\n  2904\t      // worker is marked orphaned (uncontrollable), never presented as working/input_required.\n  2905\t      if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2906\t        recoveryTerminalized = true;\n  2907\t        terminalStatus = 'failed';\n  2908\t        const recoveryEvent = this._log.append({\n  2909\t          worker: workerId,\n  2910\t          harness: events.at(-1)?.harness ?? '',\n  2911\t          turnEpoch: maxTurnEpoch,\n  2912\t          kind: 'control.recovery_terminalized',\n  2913\t          actor: 'policy',\n  2914\t          payload: { reason: 'session_not_reattached', priorStatus: events.at(-1)?.kind ?? 'unknown' },\n  2915\t        });\n  2916\t        const durable = taskId ? this._coordination?.task(taskId) : null;\n  2917\t        if (durable && !TERMINAL_TASK_STATUSES.has(durable.status)) {\n  2918\t          const evidence = this._coordMapEvent(recoveryEvent);\n  2919\t          const transitioned = this._coordination.transitionTask(taskId, 'failed', durable.version, {\n  2920\t            actor: 'policy', key: `task.failed:${taskId}:replay:${recoveryEvent?.seq ?? maxTurnEpoch}`,\n  2921\t          }, evidence ?? { reason: 'session_not_reattached' });\n  2922\t          const seeded = this._tasks.get(taskId);\n  2923\t          if (seeded) {\n  2924\t            seeded.coordinationVersion = transitioned.task.version;\n  2925\t            this._expireScratchClaims(this._workers.get(workerId), seeded, 'replay_failed');\n  2926\t          }\n  2927\t        }\n  2928\t      }\n  2929\t\n  2930\t      this._fences.register(workerId);\n  2931\t      while (this._fences.current(workerId).turnEpoch < maxTurnEpoch) this._fences.bumpTurn(workerId);\n  2932\t\n  2933\t      if (taskId) {\n  2934\t        const task = this._tasks.get(taskId) ?? {\n  2935\t          id: taskId,\n  2936\t          brief: brief ?? minimalBrief(),\n  2937\t          deps: [],\n  2938\t          status: 'pending',\n  2939\t          assignee: workerId,\n  2940\t          worktree: null,\n  2941\t          result: null,\n  2942\t          verdict: null,\n  2943\t          taskType: 'general',\n  2944\t          vendorRequested,\n  2945\t          modelRequested,\n  2946\t          modelResolved,\n  2947\t          modelObserved,\n  2948\t          modelPolicy,\n  2949\t          modelMismatch,\n  2950\t          sessionRequest,\n  2951\t          sessionRef,\n  2952\t          sessionContext,\n  2953\t          lineage,\n  2954\t          capturedSha,\n  2955\t          integration,\n  2956\t          retainedResultRef,\n  2957\t          publication,\n  2958\t          review,\n  2959\t        };\n  2960\t        const durable = this._coordination?.task(taskId);\n  2961\t        task.assignee = durable?.reservedWorkerId ?? workerId;\n  2962\t        task.deps = durable ? [...durable.deps] : task.deps;\n  2963\t        task.coordinationVersion = durable?.version ?? task.coordinationVersion ?? null;\n  2964\t        task.status = durable?.status ?? terminalStatus;\n  2965\t        task.result = lastResult ?? task.result;\n  2966\t        task.verdict = verdict ?? task.verdict;\n  2967\t        task.sessionRequest = sessionRequest;\n  2968\t        task.sessionRef = sessionRef;\n  2969\t        task.sessionContext = sessionContext;\n  2970\t        task.lineage = lineage;\n  2971\t        task.capturedSha = capturedSha;\n  2972\t        task.integration = integration;\n  2973\t        task.retainedResultRef = retainedResultRef;\n  2974\t        task.publication = publication;\n  2975\t        task.review = review;\n  2976\t        task.worktree = sessionContext?.worktree ?? task.worktree;\n  2977\t        this._tasks.set(taskId, task);\n  2978\t        if (!this._taskOrder.includes(taskId)) this._taskOrder.push(taskId);\n  2979\t      }\n  2980\t\n  2981\t      this._workers.set(workerId, {\n  2982\t        id: workerId,\n  2983\t        vendor: vendorResolved,\n  2984\t        modelRequested,\n  2985\t        modelResolved,\n  2986\t        modelObserved,\n  2987\t        modelPolicy,\n  2988\t        modelMismatch,\n  2989\t        sessionRequest,\n  2990\t        sessionRef,\n  2991\t        sessionContext,\n  2992\t        lineage,\n  2993\t        taskId,\n  2994\t        worktree: sessionContext?.worktree ?? null,\n  2995\t        // A durable native reference is not a live transport. Even a terminal task that was\n  2996\t        // reusable before restart must remain uncontrollable until PS7 proves reattachment.\n  2997\t        status: (recoveryTerminalized || sessionRef) ? 'orphaned' : this._deriveWorkerStatus(terminalStatus),\n  2998\t        pendingApprovalId: null,\n  2999\t        pendingQuestionId: null,\n  3000\t        budgetUsed,\n  3001\t        budgetThresholdsFired,\n  3002\t        usageCumulative,\n  3003\t        budgetStopTimer: null,\n  3004\t        turnTerminalObserved: false,\n  3005\t        watchdogActions: new Set(),\n  3006\t        recentFailedActions: [],\n  3007\t        watchdogGeneration: 0,\n  3008\t        watchdogTimer: null,\n  3009\t        runtimeScope: null,\n  3010\t        runtimeLease: null,\n  3011\t        spawnAbort: null,\n  3012\t        createdAt: new Date(0).toISOString(),\n  3013\t      });\n  3014\t\n  3015\t      // CI6: replayed auto identifiers reserve their numeric slots. A subsequent allocation may\n  3016\t      // never collide with or overwrite reconstructed state.\n  3017\t      const workerMatch = /^w-(\\d+)$/.exec(workerId);\n  3018\t      if (workerMatch) this._workerSeq = Math.max(this._workerSeq, Number(workerMatch[1]));\n  3019\t      const taskMatch = /^task-(\\d+)$/.exec(taskId ?? '');\n  3020\t      if (taskMatch) this._taskSeq = Math.max(this._taskSeq, Number(taskMatch[1]));\n  3021\t    }\n  3022\t  }\n  3023\t\n  3024\t  _deriveWorkerStatus(taskStatus) {\n  3025\t    switch (taskStatus) {\n  3026\t      case 'completed':\n  3027\t      case 'failed':\n  3028\t      case 'cancelled':\n  3029\t        return 'idle';\n  3030\t      case 'input_required':\n  3031\t        return 'blocked';\n  3032\t      default:\n  3033\t        return 'working';\n  3034\t    }\n  3035\t  }\n  3036\t\n  3037\t  _terminalizeUnattachedCoordinationTasks() {\n  3038\t    if (!this._coordination) return;\n  3039\t    for (const durable of this._coordination.snapshot().tasks) {\n  3040\t      if (!['working', 'input_required'].includes(durable.status)) continue;\n  3041\t      const workerId = durable.assignee ?? durable.reservedWorkerId;\n  3042\t      const events = workerId ? this._log.read(workerId) : [];\n  3043\t      if (events.some((event) => event.kind === 'lifecycle.spawned')) continue;\n  3044\t      const recorded = this._coordRecord('recovery.claimed_without_spawn', { taskId: durable.id, workerId }, `driver.recovery:${durable.id}:claimed_without_spawn`);\n  3045\t      const transitioned = this._coordination.transitionTask(durable.id, 'failed', durable.version, {\n  3046\t        actor: 'policy', key: `task.failed:${durable.id}:claimed_without_spawn`,\n  3047\t      }, { coordinationSeq: recorded?.seq ?? null, reason: 'claimed_without_operational_spawn' });\n  3048\t      const task = this._tasks.get(durable.id);\n  3049\t      if (task) {\n  3050\t        task.status = 'failed'; task.coordinationVersion = transitioned.task.version;\n  3051\t        this._expireScratchClaims(this._workers.get(workerId), task, 'claimed_without_spawn');\n  3052\t      }\n  3053\t      const handle = workerId ? this._workers.get(workerId) : null;\n  3054\t      if (handle) handle.status = 'exited';\n  3055\t    }\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '1528,1568p;1790,1888p;1928,2045p;2878,3055p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":23,"ts":"2026-07-11T20:20:22.240Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_hLMt0caeElDT4z87jaEdQeLJ","command":"/bin/zsh -lc \"nl -ba impl/test/phase11-coordination-store.test.mjs | sed -n '375,470p' && nl -ba impl/test/phase11-persistent-sessions.test.mjs | sed -n '149,235p;433,505p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"15537","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/test/phase11-coordination-store.test.mjs","name":"phase11-coordination-store.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-coordination-store.test.mjs"},{"type":"read","command":"nl -ba impl/test/phase11-persistent-sessions.test.mjs","name":"phase11-persistent-sessions.test.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/test/phase11-persistent-sessions.test.mjs"}],"aggregatedOutput":"   375\t  assert.equal(driver.coordination.task('durable-input').status, 'completed');\n   376\t});\n   377\t\n   378\ttest('CK8/CK9: input transition append failure poisons before pending/blocked state becomes visible', async () => {\n   379\t  const repo = dir();\n   380\t  execFileSync('git', ['init', '-q'], { cwd: repo });\n   381\t  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n   382\t  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n   383\t  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n   384\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n   385\t  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n   386\t  const brief = { goal: 'input crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n   387\t  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'input-write-failure' });\n   388\t  const rawAppend = driver.coordination._appendFile;\n   389\t  driver.coordination._appendFile = (file, body, encoding) => {\n   390\t    if (body.includes('\"task.transitioned\"') && body.includes('\"input_required\"')) throw new Error('input transition disk full');\n   391\t    return rawAppend(file, body, encoding);\n   392\t  };\n   393\t  assert.throws(() => driver.coordinator._handleEvent({\n   394\t    worker: handle.id, harness: 'mock@1.0.0', turnEpoch: driver.coordinator.list()[0].turnEpoch,\n   395\t    kind: 'question.asked', actor: 'worker', payload: { requestId: 'q-crash', question: 'continue?', blocking: true },\n   396\t  }), (error) => error.code === 'coordination_write_unavailable');\n   397\t  assert.equal(driver.coordination.task('input-write-failure').status, 'working');\n   398\t  assert.equal(driver.coordinator._pending.size, 0);\n   399\t  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n   400\t  await adapter.kill(handle.id);\n   401\t});\n   402\t\n   403\ttest('CK8/CK9: stop-intent append failure calls no adapter and leaves durable task working', async () => {\n   404\t  const repo = dir();\n   405\t  execFileSync('git', ['init', '-q'], { cwd: repo });\n   406\t  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n   407\t  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n   408\t  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n   409\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n   410\t  let adapterKills = 0;\n   411\t  const rawKill = adapter.kill.bind(adapter);\n   412\t  adapter.kill = async (...args) => { adapterKills += 1; return rawKill(...args); };\n   413\t  const driver = createDriver({ repoRoot: repo, logDir: dir(), adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n   414\t  const brief = { goal: 'stop crash window', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n   415\t  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'stop-write-failure' });\n   416\t  const rawAppend = driver.coordination._appendFile;\n   417\t  driver.coordination._appendFile = (file, body, encoding) => {\n   418\t    if (body.includes('\"kind\":\"control.stop_requested\"')) throw new Error('stop intent disk full');\n   419\t    return rawAppend(file, body, encoding);\n   420\t  };\n   421\t  await assert.rejects(driver.coordinator.kill(handle.id, 'human'), (error) => error.code === 'coordination_write_unavailable');\n   422\t  assert.equal(adapterKills, 0);\n   423\t  assert.equal(driver.coordination.task('stop-write-failure').status, 'working');\n   424\t  assert.equal(driver.coordinator._workers.get(handle.id).status, 'working');\n   425\t  driver.coordination._appendFile = rawAppend;\n   426\t  await rawKill(handle.id);\n   427\t});\n   428\t\n   429\ttest('CK8/CK9: cancellation completion failure resolves bounded, keeps stop intent, and restart closes task', async () => {\n   430\t  const repo = dir();\n   431\t  execFileSync('git', ['init', '-q'], { cwd: repo });\n   432\t  execFileSync('git', ['config', 'user.email', 'baton-test@example.com'], { cwd: repo });\n   433\t  execFileSync('git', ['config', 'user.name', 'Baton Test'], { cwd: repo });\n   434\t  execFileSync('git', ['commit', '--allow-empty', '-q', '-m', 'base'], { cwd: repo });\n   435\t  const logDir = dir();\n   436\t  const adapter = new MockAdapter({ scenario: { outcome: 'completed', edits: [{ path: 'slow.txt', content: 'x', delayMs: 5000 }] } });\n   437\t  const driver = createDriver({ repoRoot: repo, logDir, adapters: { mock: adapter }, watchdog: { stallMs: 0 } });\n   438\t  const brief = { goal: 'cancel completion crash', constraints: [], pathScope: ['slow.txt'], definitionOfDone: 'none', verification: { command: 'true', expectExit: 0 }, budget: { tokens: 1000, usd: 1, wallMin: 1 } };\n   439\t  const handle = await driver.coordinator.spawn('mock', brief, { taskId: 'cancel-completion-failure' });\n   440\t  const rawAppend = driver.coordination._appendFile;\n   441\t  driver.coordination._appendFile = (file, body, encoding) => {\n   442\t    if (body.includes('\"task.transitioned\"') && body.includes('\"cancelled\"')) throw new Error('cancel completion disk full');\n   443\t    return rawAppend(file, body, encoding);\n   444\t  };\n   445\t  const stopped = await driver.coordinator.kill(handle.id, 'human');\n   446\t  assert.deepEqual(stopped, { ok: false, result: 'coordination_unavailable' });\n   447\t  assert.equal(driver.coordination.task('cancel-completion-failure').status, 'working');\n   448\t  assert.equal(driver.coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'control.stop_requested'), true);\n   449\t  assert.equal(driver.coordinator._workers.get(handle.id).status, 'dead');\n   450\t  assert.throws(() => driver.coordinator.list(), (error) => error.code === 'coordination_write_unavailable');\n   451\t  driver.coordination._appendFile = rawAppend;\n   452\t  const replay = createDriver({ repoRoot: repo, logDir, coordination: driver.coordination, adapters: { mock: new MockAdapter({ card: { concurrencyCeiling: 0 } }) }, watchdog: { stallMs: 0 } });\n   453\t  assert.equal(replay.coordination.task('cancel-completion-failure').status, 'failed');\n   454\t});\n   455\t\n   456\ttest('CK4: Scratch claims conservatively conflict, warn cross-tree, and expire only by event', () => {\n   457\t  const root = dir();\n   458\t  const store = new CoordinationStore(root);\n   459\t  const envA = { repoId: 'repo', treeSha: 'aaaa1111' };\n   460\t  const envB = { repoId: 'repo', treeSha: 'bbbb2222' };\n   461\t  const first = store.claimScratch({ resource: 'path:payments/**', ownerWorker: 'w1', ownerTask: 't1', intent: 'edit', envRef: envA, fence: 3, leaseDeadline: 'later' }, { actor: 'w1', key: 'claim-a' });\n   462\t  assert.equal(store.claimScratch({ resource: 'changed', envRef: envB }, { actor: 'w1', key: 'claim-a' }).result, 'idempotent');\n   463\t  const conflict = store.claimScratch({ resource: 'path:payments/stripe.js', ownerWorker: 'w2', ownerTask: 't2', intent: 'edit', envRef: envB, fence: 4, leaseDeadline: 'later' }, { actor: 'w2', key: 'claim-b' });\n   464\t  assert.equal(conflict.ok, false);\n   465\t  assert.equal(conflict.conflict.id, first.claim.id);\n   466\t  const globWitness = store.claimScratch({ resource: '*stripe.js', ownerWorker: 'w3', ownerTask: 't3', intent: 'edit', envRef: envB, fence: 5, leaseDeadline: 'later' }, { actor: 'w3', key: 'claim-glob-witness' });\n   467\t  assert.equal(globWitness.ok, false, 'glob/glob pair with witness path:payments/stripe.js must conservatively conflict');\n   468\t  const check = store.checkScratch('path:payments/stripe.js', envB);\n   469\t  assert.equal(check.clear, false);\n   470\t  assert.equal(check.claims[0].warning, 'observed on aaaa1111 — not your tree');\n   149\t  assert.equal(c.list()[0].status, 'idle');\n   150\t});\n   151\t\n   152\ttest('CK8/CK9: follow-up refinement failure kills an already-advanced native session', async () => {\n   153\t  const ad = adapter();\n   154\t  const { c, coordination } = harness(ad);\n   155\t  const h = await c.spawn('session', brief(), { taskId: 'follow-refinement-failure' });\n   156\t  ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   157\t  await until(async () => (await c.result(h.id)).ready);\n   158\t  const rawAppend = coordination._appendFile;\n   159\t  coordination._appendFile = (file, body, encoding) => {\n   160\t    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('follow-up refinement disk full');\n   161\t    return rawAppend(file, body, encoding);\n   162\t  };\n   163\t  await assert.rejects(c.send(h.id, 'advance native state', 'turn'), (error) => error.code === 'coordination_write_unavailable');\n   164\t  assert.equal(ad.calls.prompt.length, 1);\n   165\t  assert.equal(ad.calls.kill.length, 1);\n   166\t  assert.equal(c._workers.get(h.id).status, 'dead');\n   167\t  assert.equal(coordination.snapshot().tasks.length, 1);\n   168\t  assert.equal(coordination.snapshot().tasks[0].status, 'completed');\n   169\t  assert.equal(coordination.events().some((event) => event.kind === 'driver.recorded' && event.payload.kind === 'follow_up.requested'), true);\n   170\t});\n   171\t\n   172\ttest('PS2: emitted turn facts followed by refusal are a protocol violation and kill the ambiguous session', async () => {\n   173\t  let ad;\n   174\t  ad = adapter({\n   175\t    prompt: async (worker) => {\n   176\t      ad.emit(worker, 'lifecycle.turn_started', {}, 2);\n   177\t      return { ok: false, reason: 'contradictory refusal' };\n   178\t    },\n   179\t  });\n   180\t  const { c, log } = harness(ad);\n   181\t  const h = await c.spawn('session', brief(), { taskId: 'follow-contradiction' });\n   182\t  ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   183\t  await until(async () => (await c.result(h.id)).ready);\n   184\t  const ack = await c.send(h.id, 'try again', 'turn');\n   185\t  assert.equal(ack.ok, false);\n   186\t  assert.ok(log.read(h.id).some((event) => event.kind === 'control.protocol_violation'));\n   187\t  assert.equal((await c.result(h.id)).status, 'completed');\n   188\t  assert.equal(c.list()[0].status, 'stopping');\n   189\t});\n   190\t\n   191\ttest('PS3: interrupt-with-follow-up reopens coordinator state before the adapter follow-up terminal', async () => {\n   192\t  const ad = adapter();\n   193\t  const { c, verifyCalls } = harness(ad);\n   194\t  const h = await c.spawn('session', brief(), { taskId: 'interrupt-follow' });\n   195\t  const stopping = c.interrupt(h.id, 'continue after interrupt', 'human');\n   196\t  ad.emit(h.id, 'control.interrupt_confirmed', {}, 1, 'orchestrator');\n   197\t  await stopping;\n   198\t  assert.equal(c.list()[0].status, 'working');\n   199\t  assert.equal((await c.result(h.id)).ready, false);\n   200\t  ad.emit(h.id, 'lifecycle.turn_started', {}, 2);\n   201\t  ad.emit(h.id, 'lifecycle.turn_completed', completed('followed'), 2);\n   202\t  await until(async () => (await c.result(h.id)).ready);\n   203\t  assert.equal(verifyCalls.length, 1);\n   204\t  assert.equal((await c.result(h.id)).status, 'completed');\n   205\t});\n   206\t\n   207\ttest('PS4: a late terminal from the prior wire epoch cannot overwrite the active follow-up', async () => {\n   208\t  const ad = adapter();\n   209\t  const { c, log, verifyCalls } = harness(ad);\n   210\t  const h = await c.spawn('session', brief(), { taskId: 'stale-terminal' });\n   211\t  ad.emit(h.id, 'lifecycle.turn_completed', completed('first'), 1);\n   212\t  await until(async () => (await c.result(h.id)).ready);\n   213\t  await c.send(h.id, 'next', 'turn');\n   214\t  ad.emit(h.id, 'lifecycle.turn_started', {}, 2);\n   215\t  ad.emit(h.id, 'lifecycle.turn_completed', completed('late first'), 1);\n   216\t  await sleep(20);\n   217\t  assert.equal(verifyCalls.length, 1);\n   218\t  assert.equal((await c.result(h.id)).ready, false);\n   219\t  assert.ok(log.read(h.id).some((e) => e.kind === 'control.stale_rejected' && e.payload?.op === 'terminal'));\n   220\t});\n   221\t\n   222\ttest('PS5: wire sessionRef reaches handle/result and survives terminal replay', async () => {\n   223\t  const ad = adapter();\n   224\t  const { c, log, coordination } = harness(ad);\n   225\t  const h = await c.spawn('session', brief(), { taskId: 'session-ref' });\n   226\t  ad.emit(h.id, 'lifecycle.spawned', { sessionId: 'native-session-7', pid: 123 }, 1);\n   227\t  ad.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   228\t  await until(async () => (await c.result(h.id)).ready);\n   229\t  const expected = { vendor: 'session', kind: 'session', id: 'native-session-7', persistence: 'native', source: 'wire' };\n   230\t  assert.deepEqual(c.list()[0].sessionRef, expected);\n   231\t  assert.deepEqual((await c.result(h.id)).sessionRef, expected);\n   232\t\n   233\t  const replay = new Coordinator({\n   234\t    log, coordination, fences: new FenceTable(), adapters: { session: adapter() },\n   235\t    worktrees: { create: async () => ({}), capture: async () => ({}), createVerifyWorktree: async () => ({}), removeVerifyWorktree: async () => {}, remove: async () => {}, reconcile: async () => {} },\n   433\t  assert.ok(log.read(h.id).some((event) => event.kind === 'control.recovery_attached'));\n   434\t});\n   435\t\n   436\ttest('CK8/CK9: recovery intent append failure reaches no native adapter', async () => {\n   437\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-intent-wt-'));\n   438\t  const original = adapter();\n   439\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   440\t  const h = await c.spawn('session', brief(), { taskId: 'recover-intent-task' });\n   441\t  await until(() => c.list()[0].sessionContext);\n   442\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'intent-native', pid: 111 }, 1);\n   443\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   444\t  await until(async () => (await c.result(h.id)).ready);\n   445\t\n   446\t  const resumed = adapter();\n   447\t  let spawnCalls = 0;\n   448\t  resumed.spawn = async () => { spawnCalls += 1; return { ok: true }; };\n   449\t  const replay = new Coordinator({\n   450\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   451\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   452\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   453\t  });\n   454\t  const rawAppend = coordination._appendFile;\n   455\t  coordination._appendFile = (file, body, encoding) => {\n   456\t    if (body.includes('\"kind\":\"recovery.requested\"')) throw new Error('recovery intent disk full');\n   457\t    return rawAppend(file, body, encoding);\n   458\t  };\n   459\t  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n   460\t  assert.equal(spawnCalls, 0);\n   461\t  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n   462\t});\n   463\t\n   464\ttest('CK8/CK9: recovery refinement failure kills a native transport that already attached', async () => {\n   465\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-refine-wt-'));\n   466\t  const original = adapter();\n   467\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   468\t  const h = await c.spawn('session', brief(), { taskId: 'recover-refine-task' });\n   469\t  await until(() => c.list()[0].sessionContext);\n   470\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 111 }, 1);\n   471\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   472\t  await until(async () => (await c.result(h.id)).ready);\n   473\t\n   474\t  const resumed = adapter();\n   475\t  resumed.spawn = async (worker) => {\n   476\t    resumed.emit(worker, 'lifecycle.spawned', { sessionId: 'refine-native', pid: 222 }, 1);\n   477\t    return { ok: true };\n   478\t  };\n   479\t  const replay = new Coordinator({\n   480\t    log, coordination, fences: new FenceTable(), adapters: { session: resumed },\n   481\t    worktrees: { validateSessionContext: async () => ({ ok: true }), remove: async () => {}, reconcile: async () => {} },\n   482\t    referee: async () => ({}), route: () => 'session', recoveryTimeoutMs: 100, stopDeadlineMs: 100,\n   483\t  });\n   484\t  const rawAppend = coordination._appendFile;\n   485\t  coordination._appendFile = (file, body, encoding) => {\n   486\t    if (body.includes('\"task.created\"') && body.includes('refinement-')) throw new Error('recovery refinement disk full');\n   487\t    return rawAppend(file, body, encoding);\n   488\t  };\n   489\t  await assert.rejects(replay.recover(h.id), (error) => error.code === 'coordination_write_unavailable');\n   490\t  await until(() => resumed.calls.kill.length === 1);\n   491\t  assert.equal(replay._workers.get(h.id).status, 'orphaned');\n   492\t});\n   493\t\n   494\ttest('PS7: a reattachment identity mismatch is refused and the untrusted transport is killed', async () => {\n   495\t  const wt = mkdtempSync(join(tmpdir(), 'baton-ps-recover-bad-wt-'));\n   496\t  const original = adapter();\n   497\t  const { c, log, coordination } = harness(original, undefined, { create: async () => ({ path: wt }) });\n   498\t  const h = await c.spawn('session', brief(), { taskId: 'recover-bad-task' });\n   499\t  await until(() => c.list()[0].sessionContext);\n   500\t  original.emit(h.id, 'lifecycle.spawned', { sessionId: 'expected-native', pid: 111 }, 1);\n   501\t  original.emit(h.id, 'lifecycle.turn_completed', completed(), 1);\n   502\t  await until(async () => (await c.result(h.id)).ready);\n   503\t\n   504\t  const resumed = adapter();\n   505\t  resumed.spawn = async (worker) => {\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/test/phase11-coordination-store.test.mjs | sed -n '375,470p' && nl -ba impl/test/phase11-persistent-sessions.test.mjs | sed -n '149,235p;433,505p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":24,"ts":"2026-07-11T20:20:22.285Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":39559,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":79883,"inputTokens":77747,"cachedInputTokens":31744,"outputTokens":2136,"reasoningOutputTokens":944},"last":{"totalTokens":39559,"inputTokens":38686,"cachedInputTokens":17280,"outputTokens":873,"reasoningOutputTokens":493},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":79883,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":25,"ts":"2026-07-11T20:20:22.287Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":26,"ts":"2026-07-11T20:20:22.287Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_RbsHlIPBKZly0Z038fgfdFGu","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '996,1110p;1120,1145p;1238,1358p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"86842","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '996,1110p;1120,1145p;1238,1358p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"   996\t    handle.sessionContext = context;\n   997\t    handle.turnAdmission = null;\n   998\t    this._resetWatchdogTurn(handle);\n   999\t    this._log.append({\n  1000\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1001\t      kind: 'control.recovery_attached', actor: 'orchestrator',\n  1002\t      payload: { sessionRef: handle.sessionRef, context },\n  1003\t    });\n  1004\t    this._log.append({\n  1005\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1006\t      kind: 'lifecycle.turn_started', actor: 'orchestrator', payload: { recovery: true },\n  1007\t      modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n  1008\t    });\n  1009\t    for (const event of admission.events) this._handleEvent(event);\n  1010\t    return { ok: true, result: 'attached', handle: this._publicHandle(handle) };\n  1011\t  }\n  1012\t\n  1013\t  /** AC5: explicitly integrate an accepted captured commit. This never pushes. */\n  1014\t  async integrate(workerId, opts = {}) {\n  1015\t    this.tick();\n  1016\t    const handle = this._getWorker(workerId);\n  1017\t    const task = this._tasks.get(handle.taskId);\n  1018\t    if (!task || task.status !== 'completed' || !task.capturedSha) {\n  1019\t      throw new IntegrationError('integration requires an accepted captured task result', 'result_not_accepted');\n  1020\t    }\n  1021\t    if (this._requireIndependentOracle) {\n  1022\t      const oracle = [...this._tasks.values()].find((candidate) =>\n  1023\t        candidate.review?.parentTaskId === task.id\n  1024\t        && candidate.review.kind === 'oracle'\n  1025\t        && candidate.review.independent === true\n  1026\t        && candidate.status === 'completed');\n  1027\t      if (!oracle) {\n  1028\t        throw new IntegrationError('integration requires a completed independent oracle from a different model family', 'independent_oracle_required');\n  1029\t      }\n  1030\t    }\n  1031\t    const strategy = opts.strategy ?? 'ff-only';\n  1032\t    if (strategy !== 'ff-only') {\n  1033\t      throw new IntegrationError(`unsupported integration strategy: ${strategy}`, 'unsupported_strategy');\n  1034\t    }\n  1035\t    if (!this._worktrees || typeof this._worktrees.integrate !== 'function') {\n  1036\t      throw new IntegrationError('worktree manager does not implement integration', 'integration_unavailable');\n  1037\t    }\n  1038\t    if (handle.status === 'working' || handle.status === 'blocked' || handle.status === 'stopping' || handle.status === 'pending') {\n  1039\t      throw new IntegrationError('worker must be idle, dead, exited, or orphaned before integration', 'worker_not_quiescent');\n  1040\t    }\n  1041\t\n  1042\t    this._coordRecord('integration.requested', {\n  1043\t      taskId: task.id, workerId, strategy, sha: task.capturedSha,\n  1044\t      actor: opts.actor ?? 'orchestrator', effect: 'local_git_merge',\n  1045\t    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\n  1046\t\n  1047\t    if (typeof this._worktrees.retainResult === 'function') {\n  1048\t      task.retainedResultRef = await this._worktrees.retainResult(task.capturedSha);\n  1049\t    }\n  1050\t\n  1051\t    if (handle.status === 'idle') {\n  1052\t      const stopped = await this.kill(workerId, opts.actor ?? 'orchestrator');\n  1053\t      if (!['confirmed', 'already_dead', 'already_stopped'].includes(stopped.result)) {\n  1054\t        throw new IntegrationError('worker could not be safely stopped before integration', 'worker_stop_failed');\n  1055\t      }\n  1056\t    } else if (handle.status === 'exited') {\n  1057\t      await this.kill(workerId, opts.actor ?? 'orchestrator');\n  1058\t    }\n  1059\t    await this._removeTaskWorktree(task);\n  1060\t\n  1061\t    let integrated;\n  1062\t    try {\n  1063\t      integrated = await this._worktrees.integrate(task.capturedSha, { strategy });\n  1064\t    } catch (err) {\n  1065\t      const refusedEvent = this._log.append({\n  1066\t        worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1067\t        kind: 'integration.refused', actor: 'policy',\n  1068\t        payload: { strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef, reason: String(err?.message ?? err) },\n  1069\t      });\n  1070\t      const refusedEvidence = this._coordMapEvent(refusedEvent);\n  1071\t      this._coordRecord('integration.refused', {\n  1072\t        taskId: task.id, strategy, sha: task.capturedSha, retainedResultRef: task.retainedResultRef,\n  1073\t        reason: String(err?.message ?? err), evidence: refusedEvidence,\n  1074\t      }, `driver.integration.refused:${task.id}:${refusedEvent.seq}`, 'policy');\n  1075\t      throw new IntegrationError(String(err?.message ?? err), 'non_fast_forward_or_dirty');\n  1076\t    }\n  1077\t    if (task.retainedResultRef && typeof this._worktrees.releaseResult === 'function') {\n  1078\t      try { await this._worktrees.releaseResult(task.retainedResultRef); } catch { /* merged HEAD now retains the result */ }\n  1079\t      task.retainedResultRef = null;\n  1080\t    }\n  1081\t    task.integration = Object.freeze({ ...integrated, strategy, actor: opts.actor ?? 'orchestrator' });\n  1082\t    const integrationEvent = this._log.append({\n  1083\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1084\t      kind: 'integration.completed', actor: opts.actor ?? 'orchestrator', payload: task.integration,\n  1085\t    });\n  1086\t    const integrationEvidence = this._coordMapEvent(integrationEvent);\n  1087\t    if (this._coordination) {\n  1088\t      const acceptingEvidence = this._coordination.task(task.id).artifactIds\n  1089\t        .map((artifactId) => this._coordination.artifact(artifactId))\n  1090\t        .filter((artifact) => artifact?.accepted === true)\n  1091\t        .flatMap((artifact) => artifact.provenance ?? []);\n  1092\t      this._coordination.registerArtifact({\n  1093\t        taskId: task.id, kind: 'report', refs: { beforeSha: task.integration.beforeSha, resultSha: task.integration.resultSha, afterSha: task.integration.afterSha },\n  1094\t        mediaType: 'application/vnd.baton.integration+json', accepted: true, provenance: [integrationEvidence, ...acceptingEvidence],\n  1095\t      }, { actor: opts.actor ?? 'orchestrator', key: `artifact.integration:${task.id}:${task.integration.afterSha}` });\n  1096\t      this._coordination.promoteKnowledgeNode({\n  1097\t        id: `decision:integrate:${task.id}:${integrationEvent.seq}`, type: 'Decision',\n  1098\t        body: `Integrated task ${task.id} at ${task.integration.afterSha}`, grounding: 'observed',\n  1099\t        informedBy: [`task:${task.id}`],\n  1100\t        evidence: [{ coordinationSeq: integrationEvidence.coordinationSeq }],\n  1101\t      }, { kind: 'Decision', trigger: 'integration' }, { actor: opts.actor ?? 'orchestrator', key: `knowledge.integration:${task.id}:${integrationEvent.seq}` });\n  1102\t      this._coordRecord('integration.completed', { taskId: task.id, integration: task.integration, evidence: integrationEvidence }, `driver.integration:${task.id}:${integrationEvent.seq}`, opts.actor ?? 'orchestrator');\n  1103\t    }\n  1104\t    return { ok: true, result: 'integrated', integration: task.integration };\n  1105\t  }\n  1106\t\n  1107\t  /** AC6: create an approval-gated exact-SHA publication request. No side effect occurs here. */\n  1108\t  requestPublication(workerId, target = {}, actor = 'orchestrator') {\n  1109\t    this.tick();\n  1110\t    const handle = this._getWorker(workerId);\n  1120\t    }\n  1121\t    if (typeof ref !== 'string' || !/^refs\\/heads\\/[A-Za-z0-9._\\/-]+$/.test(ref) || ref.includes('..')) {\n  1122\t      throw new PublicationError('ref must be a full, safe refs/heads/* name', 'invalid_ref');\n  1123\t    }\n  1124\t    if (sha !== task.integration.afterSha) {\n  1125\t      throw new PublicationError('publication SHA must equal the integrated result SHA', 'sha_mismatch');\n  1126\t    }\n  1127\t    const stamp = this._fences.bumpHuman(workerId);\n  1128\t    const requestId = `publication-${workerId}-${++this._publicationSeq}`;\n  1129\t    const publication = Object.freeze({ remote, ref, sha });\n  1130\t    const deadlineAt = this._now() + this._approvalTimeoutMs;\n  1131\t    const record = {\n  1132\t      kind: 'publication', worker: workerId, state: 'pending', resolution: null, consumer: null,\n  1133\t      turnEpochAtAsk: stamp.turnEpoch, fenceAtAsk: stamp.fence,\n  1134\t      deadlineAt, publication,\n  1135\t    };\n  1136\t    const requestedEvent = this._log.append({\n  1137\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1138\t      kind: 'publication.requested', actor,\n  1139\t      payload: { requestId, ...publication, fence: stamp.fence, deadlineAt },\n  1140\t    });\n  1141\t    const evidence = this._coordMapEvent(requestedEvent);\n  1142\t    this._coordRecord('publication.requested', {\n  1143\t      taskId: task.id, workerId, requestId, publication, fence: stamp.fence, deadlineAt, evidence,\n  1144\t    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\n  1145\t    this._pending.set(requestId, record);\n  1238\t    const task = this._tasks.get(handle.taskId);\n  1239\t    // SC14: delivery-slot acquisition is the authority boundary. A queued continuation cannot\n  1240\t    // cross a finalized stop, and a terminal task cannot be resurrected by a surviving session.\n  1241\t    if (handle.status === 'stopping') return { ok: false, result: 'worker_stopping' };\n  1242\t    const card = this._adapters[handle.vendor]?.card();\n  1243\t    const reusableFollowUp = mode === 'turn'\n  1244\t      && handle.status === 'idle'\n  1245\t      && task && TERMINAL_TASK_STATUSES.has(task.status)\n  1246\t      && ['native', 'emulated'].includes(card?.sessions?.multiTurn);\n  1247\t    if (handle.status === 'idle' && !reusableFollowUp) return { ok: false, result: 'worker_not_active' };\n  1248\t    if (handle.status === 'dead' || handle.status === 'exited' || handle.status === 'orphaned' || handle.status === 'pending') {\n  1249\t      return { ok: false, result: 'worker_not_active' };\n  1250\t    }\n  1251\t    if (!task || (TERMINAL_TASK_STATUSES.has(task.status) && !reusableFollowUp)) return { ok: false, result: 'task_terminal' };\n  1252\t\n  1253\t    if (reusableFollowUp) return this._deliverFollowUp(handle, task, message, opts);\n  1254\t\n  1255\t    // C3: pre-check against an externally-supplied fence, BEFORE any delivery attempt —\n  1256\t    // re-evaluated HERE at delivery-slot acquisition, not at send() entry (SC4b).\n  1257\t    if (opts.expectedFence !== undefined) {\n  1258\t      const preCheck = this._fences.check(workerId, { fence: opts.expectedFence });\n  1259\t      if (!preCheck.ok) {\n  1260\t        const harness = this._harnessOf(handle.vendor);\n  1261\t        const recoveryEvent = this._log.append({\n  1262\t          worker: workerId,\n  1263\t          harness,\n  1264\t          turnEpoch: this._fences.current(workerId).turnEpoch,\n  1265\t          kind: 'control.stale_rejected',\n  1266\t          actor: 'orchestrator',\n  1267\t          payload: { op: 'send', mode, attempted: opts.expectedFence, current: preCheck.current, phase: 'pre_delivery' },\n  1268\t        });\n  1269\t        return { ok: false, result: 'stale_fence', current: preCheck.current };\n  1270\t      }\n  1271\t    }\n  1272\t\n  1273\t    const stamp = this._fences.issue(workerId);\n  1274\t    const harness = this._harnessOf(handle.vendor);\n  1275\t    const ack = await this._adapters[handle.vendor].prompt(workerId, message, mode);\n  1276\t    const check = this._fences.check(workerId, stamp);\n  1277\t    const currentTurnEpoch = this._fences.current(workerId).turnEpoch;\n  1278\t\n  1279\t    if (!check.ok) {\n  1280\t      this._log.append({\n  1281\t        worker: workerId,\n  1282\t        harness,\n  1283\t        turnEpoch: currentTurnEpoch,\n  1284\t        kind: 'control.stale_rejected',\n  1285\t        actor: 'orchestrator',\n  1286\t        payload: { op: 'send', mode, attempted: stamp, current: check.current, phase: 'post_delivery' },\n  1287\t      });\n  1288\t      // C3: delivery already happened despite the staleness — say so, loudly.\n  1289\t      this._log.append({\n  1290\t        worker: workerId,\n  1291\t        harness,\n  1292\t        turnEpoch: currentTurnEpoch,\n  1293\t        kind: 'control.delivery_amended',\n  1294\t        actor: 'policy',\n  1295\t        payload: { op: 'send', mode, message, deliveredDespiteStale: true, attempted: stamp, current: check.current },\n  1296\t      });\n  1297\t      return { ok: false, result: 'stale_fence', current: check.current };\n  1298\t    }\n  1299\t\n  1300\t    if (ack && ack.ok === false) {\n  1301\t      return { ok: false, result: ack.reason ?? 'delivery_refused', reason: ack.reason };\n  1302\t    }\n  1303\t\n  1304\t    const kind = mode === 'nudge' ? 'control.nudge' : mode === 'steer' ? 'control.steer' : 'control.send';\n  1305\t    const ev = { worker: workerId, harness, turnEpoch: currentTurnEpoch, kind, actor: 'orchestrator', payload: { message } };\n  1306\t    if (ack && ack.emulated === true) ev.emulated = true;\n  1307\t    this._log.append(ev);\n  1308\t    return { ok: true, result: 'ok', emulated: ack && ack.emulated === true };\n  1309\t  }\n  1310\t\n  1311\t  async _deliverFollowUp(handle, task, message, opts) {\n  1312\t    const workerId = handle.id;\n  1313\t    if (opts.expectedFence !== undefined) {\n  1314\t      const preCheck = this._fences.check(workerId, { fence: opts.expectedFence });\n  1315\t      if (!preCheck.ok) return { ok: false, result: 'stale_fence', current: preCheck.current };\n  1316\t    }\n  1317\t\n  1318\t    const requestedEvent = this._log.append({\n  1319\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n  1320\t      kind: 'control.follow_up_requested', actor: opts.actor ?? 'orchestrator',\n  1321\t      payload: { message, expectedFence: opts.expectedFence ?? null },\n  1322\t    });\n  1323\t    const requestedEvidence = this._coordMapEvent(requestedEvent);\n  1324\t    this._coordRecord('follow_up.requested', {\n  1325\t      taskId: task.id, workerId, expectedFence: opts.expectedFence ?? null, evidence: requestedEvidence,\n  1326\t    }, `driver.follow_up.requested:${task.id}:${requestedEvent.seq}`, opts.actor ?? 'orchestrator');\n  1327\t\n  1328\t    // A native adapter can emit turn_started synchronously inside prompt(), before returning its\n  1329\t    // Ack. Queue those events until admission commits; refusal leaves the prior terminal view.\n  1330\t    const admission = { events: [] };\n  1331\t    handle.turnAdmission = admission;\n  1332\t    let ack;\n  1333\t    try {\n  1334\t      ack = await this._adapters[handle.vendor].prompt(workerId, message, 'turn');\n  1335\t    } catch (err) {\n  1336\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n  1337\t      if (admission.events.length > 0) this._rejectContradictoryAdmission(handle, admission, err);\n  1338\t      return { ok: false, result: 'delivery_exception', reason: String(err?.message ?? err) };\n  1339\t    }\n  1340\t    // A crash/exit is intentionally processed immediately instead of queued. It wins over an Ack\n  1341\t    // from the same call and can never be overwritten by reopening the prior terminal task.\n  1342\t    if (handle.status !== 'idle') {\n  1343\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n  1344\t      return { ok: false, result: 'worker_not_active' };\n  1345\t    }\n  1346\t    if (!ack || ack.ok !== true) {\n  1347\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n  1348\t      if (admission.events.length > 0) this._rejectContradictoryAdmission(handle, admission, ack?.reason);\n  1349\t      return { ok: false, result: ack?.reason ?? 'delivery_refused', reason: ack?.reason };\n  1350\t    }\n  1351\t\n  1352\t    const stamp = this._fences.bumpTurn(workerId);\n  1353\t    let activeTask;\n  1354\t    try {\n  1355\t      activeTask = this._createCoordinationRefinement(handle, task, 'follow_up');\n  1356\t    } catch (err) {\n  1357\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n  1358\t      handle.status = 'dead';\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '996,1110p;1120,1145p;1238,1358p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":27,"ts":"2026-07-11T20:20:29.062Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_zGUoKYCr2cwxSiG2X1dgYyvH","command":"/bin/zsh -lc \"rg -n \\\"_poisonCoordination\\\\(|coordination_write_unavailable|_finalizeStop\\\\(|publication.completed|integration.requested|integration.completed|requestPublication\\\\(|_resolveRecord\\\\(|recover\\\\(\\\" impl/src/coordinator.mjs\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"39128","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n \"_poisonCoordination\\\\(|coordination_write_unavailable|_finalizeStop\\\\(|publication.completed|integration.requested|integration.completed|requestPublication\\\\(|_resolveRecord\\\\(|recover\\\\(\" impl/src/coordinator.mjs","query":"_poisonCoordination\\(|coordination_write_unavailable|_finalizeStop\\(|publication.completed|integration.requested|integration.completed|requestPublication\\(|_resolveRecord\\(|recover\\(","path":"coordinator.mjs"}],"aggregatedOutput":"278:            throw this._poisonCoordination(err);\n418:        this._resolveRecord(requestId, { decision: 'deny' }, 'policy').catch(noop);\n889:  async recover(workerId, opts = {}) {\n1042:    this._coordRecord('integration.requested', {\n1045:    }, `driver.integration.requested:${task.id}:${task.capturedSha}`, opts.actor ?? 'orchestrator');\n1084:      kind: 'integration.completed', actor: opts.actor ?? 'orchestrator', payload: task.integration,\n1102:      this._coordRecord('integration.completed', { taskId: task.id, integration: task.integration, evidence: integrationEvidence }, `driver.integration:${task.id}:${integrationEvent.seq}`, opts.actor ?? 'orchestrator');\n1108:  requestPublication(workerId, target = {}, actor = 'orchestrator') {\n1466:        this._resolveRecord(handle.pendingApprovalId, { decision: 'cancel' }, actor).catch(noop);\n1468:        this._resolveRecord(handle.pendingQuestionId, { decision: 'cancel' }, actor).catch(noop);\n1542:  _poisonCoordination(err) {\n1546:      fatal.code = 'coordination_write_unavailable';\n1549:        if (handle.spawnAbort && !handle.spawnAbort.signal.aborted) handle.spawnAbort.abort({ reason: 'coordination_write_unavailable' });\n1785:        if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\n1789:        if (waiter.confirmReceived) this._finalizeStop(waiter.workerId, waiter);\n1798:    if (waiter.ackReady) this._finalizeStop(handle.id, waiter);\n1801:  _finalizeStop(workerId, waiter) {\n1916:    return this._resolveRecord(requestId, answer, actor);\n1919:  async _resolveRecord(requestId, answer, actor) {\n1929:      return this._resolveRecord(requestId, answer, actor);\n2020:          kind: 'publication.completed', actor, payload: task.publication,\n2029:        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n2710:          this._poisonCoordination(coordinationError);\n2714:      if (['evidence_mapping', 'terminal_batch', 'promotion'].includes(trustPhase)) this._poisonCoordination(err);\n2837:          case 'integration.completed':\n2844:          case 'publication.completed':\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"rg -n \\\"_poisonCoordination\\\\(|coordination_write_unavailable|_finalizeStop\\\\(|publication.completed|integration.requested|integration.completed|requestPublication\\\\(|_resolveRecord\\\\(|recover\\\\(\\\" impl/src/coordinator.mjs\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":28,"ts":"2026-07-11T20:20:29.097Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":55474,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":135357,"inputTokens":132905,"cachedInputTokens":70016,"outputTokens":2452,"reasoningOutputTokens":1035},"last":{"totalTokens":55474,"inputTokens":55158,"cachedInputTokens":38272,"outputTokens":316,"reasoningOutputTokens":91},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":135357,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":29,"ts":"2026-07-11T20:20:29.098Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":30,"ts":"2026-07-11T20:20:29.098Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"The remaining question is whether these new paths actually poison coordinator authority after an already-performed side effect, or whether they only throw once and leave the process live enough to double-run the effect. That is the most important seam left."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":31,"ts":"2026-07-11T20:20:41.517Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_fFDZo4fLRLfdOwAgKkHfM6CV","command":"/bin/zsh -lc \"rg -n \\\"_fatalError|tick\\\\(|throw this\\\\._fatalError|if \\\\(this\\\\._fatalError\\\\)\\\" impl/src/coordinator.mjs\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"7737","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n \"_fatalError|tick\\\\(|throw this\\\\._fatalError|if \\\\(this\\\\._fatalError\\\\)\" impl/src/coordinator.mjs","query":"_fatalError|tick\\(|throw this\\._fatalError|if \\(this\\._fatalError\\)","path":"coordinator.mjs"}],"aggregatedOutput":"319:      this._fatalError = null;\n326:          if (!this._fatalError) {\n330:            this._fatalError = fatal;\n337:          throw this._fatalError;\n374:          if (!this._fatalError) throw err;\n391:  // tick() — dispatch + deadline sweep. Called implicitly by every public command.\n394:  tick() {\n395:    if (this._fatalError) throw this._fatalError;\n502:    // 'working' and the adapter is invoked synchronously below (so a bare tick() dispatches\n626:    this.tick();\n749:    this.tick();\n794:    this.tick();\n890:    this.tick();\n1015:    this.tick();\n1109:    this.tick();\n1223:    this.tick();\n1401:    this.tick();\n1417:    this.tick();\n1492:    // C4: a real, injectable, unref'd deadline timer — independent of tick()'s sweep,\n1543:    if (!this._fatalError) {\n1547:      this._fatalError = fatal;\n1552:    return this._fatalError;\n1915:    this.tick();\n2139:    this.tick();\n2165:    this.tick();\n2186:    this.tick();\n2203:    this.tick();\n2220:    this.tick();\n2230:    this.tick();\n2239:    this.tick();\n2246:    this.tick();\n2253:      this.tick();\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"rg -n \\\"_fatalError|tick\\\\(|throw this\\\\._fatalError|if \\\\(this\\\\._fatalError\\\\)\\\" impl/src/coordinator.mjs\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":32,"ts":"2026-07-11T20:20:41.567Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_DM0LvwkDcPsVULYhOAqa1cZg","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '240,320p;360,430p;880,1010p;2688,2722p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"91557","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '240,320p;360,430p;880,1010p;2688,2722p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"   240\t    ...(context ? { context } : {}),\n   241\t  });\n   242\t}\n   243\t\n   244\tfunction cardSupportsSession(card, request) {\n   245\t  if (!request || request.mode === 'new') return true;\n   246\t  return card?.sessions?.[request.mode] === 'native' || card?.sessions?.[request.mode] === 'emulated';\n   247\t}\n   248\t\n   249\t/** C1: the default done-gate, behavior-preserving-by-construction for every caller that\n   250\t * doesn't override it — exactly today's inline check, moved into an injectable, named\n   251\t * function. `acceptOpts.expectExit` carries the per-task expected exit code. */\n   252\tfunction defaultAccept(verdict, acceptOpts) {\n   253\t  return !!(verdict && verdict.reverified === true && verdict.observedExit === acceptOpts.expectExit);\n   254\t}\n   255\t\n   256\texport class Coordinator {\n   257\t  /** @param {object} opts */\n   258\t  constructor(opts) {\n   259\t    if (!opts?.coordination) throw new TypeError('Coordinator requires a durable coordination store');\n   260\t    for (const method of ['snapshot', 'task', 'createTask', 'claimTask', 'transitionTask', 'transitionTaskWithArtifacts', 'mapOperationalEvent', 'recordDriver', 'registerArtifact', 'artifact', 'claimScratch', 'postScratchFact', 'readScratch', 'activeScratchClaims', 'expireScratchClaim', 'addKnowledgeNode', 'promoteKnowledgeNode', 'readKnowledge']) {\n   261\t      if (typeof opts.coordination[method] !== 'function') throw new TypeError(`Coordinator coordination store is missing ${method}()`);\n   262\t    }\n   263\t    this._log = opts.log;\n   264\t    this._fences = opts.fences;\n   265\t    this._adapters = opts.adapters;\n   266\t    this._worktrees = opts.worktrees;\n   267\t    this._runtimeScopes = opts.runtimeScopes ?? null;\n   268\t    const rawCoordination = opts.coordination;\n   269\t    this._coordination = new Proxy(rawCoordination, {\n   270\t      get: (target, property, receiver) => {\n   271\t        const value = Reflect.get(target, property, receiver);\n   272\t        if (typeof value !== 'function') return value;\n   273\t        const bound = value.bind(target);\n   274\t        if (!COORDINATION_MUTATORS.has(property)) return bound;\n   275\t        return (...args) => {\n   276\t          try { return bound(...args); } catch (err) {\n   277\t            if (err?.name === 'CoordinationRefusal' || err instanceof TypeError) throw err;\n   278\t            throw this._poisonCoordination(err);\n   279\t          }\n   280\t        };\n   281\t      },\n   282\t    });\n   283\t    this._referee = opts.referee;\n   284\t    this._route = opts.route;\n   285\t    this._story = opts.story ?? null;\n   286\t    this._repoRoot = opts.repoRoot ?? null;\n   287\t    this._now = opts.now || Date.now;\n   288\t    this._approvalTimeoutMs = opts.approvalTimeoutMs ?? 60000;\n   289\t    this._stopDeadlineMs = opts.stopDeadlineMs ?? 15000;\n   290\t    this._recoveryTimeoutMs = opts.recoveryTimeoutMs ?? 15000;\n   291\t    this._budgetThresholds = Object.freeze([...(opts.budgetPolicy?.thresholds ?? [0.5, 0.8, 1])].sort((a, b) => a - b));\n   292\t    this._budgetHardStopAt = opts.budgetPolicy?.hardStopAt ?? 1;\n   293\t    this._budgetTerminalGraceMs = opts.budgetPolicy?.terminalGraceMs ?? 250;\n   294\t    this._watchdog = Object.freeze({\n   295\t      stallMs: opts.watchdog?.stallMs ?? 120000,\n   296\t      loopThreshold: opts.watchdog?.loopThreshold ?? 3,\n   297\t      scopeAction: opts.watchdog?.scopeAction ?? 'kill',\n   298\t      loopAction: opts.watchdog?.loopAction ?? 'interrupt',\n   299\t      stallAction: opts.watchdog?.stallAction ?? 'interrupt',\n   300\t    });\n   301\t    this._waitPollMs = opts.waitPollMs ?? 25;\n   302\t    // C1: the sole done-gate, and the driver-level policy passed to every accept() call.\n   303\t    this._accept = opts.accept ?? defaultAccept;\n   304\t    this._acceptOpts = opts.acceptOpts ?? {};\n   305\t    this._requireIndependentOracle = opts.requireIndependentOracle ?? false;\n   306\t    this._publisher = opts.publisher ?? null;\n   307\t    // C4: injectable timer primitives for a real, unref'd stop-deadline timer.\n   308\t    this._setTimeout = opts.setTimeout ?? globalThis.setTimeout;\n   309\t    this._clearTimeout = opts.clearTimeout ?? globalThis.clearTimeout;\n   310\t\n   311\t    // D8/CK1: feed the optional story sink, but never turn an authoritative-log failure into a\n   312\t    // warning-and-drop. Once an append fails the coordinator is poisoned: every public command\n   313\t    // fails closed until process restart/replay, and any not-yet-entered spawn is aborted. A\n   314\t    // caller may tear storage down only after it has quiesced the coordinator; racing teardown\n   315\t    // is an integrity failure, not a benign sink failure.\n   316\t    {\n   317\t      const rawAppend = this._log.append.bind(this._log);\n   318\t      this._appendFailures = 0;\n   319\t      this._fatalError = null;\n   320\t      this._log.append = (partial) => {\n   360\t\n   361\t    this._workerSeq = 0;\n   362\t    this._taskSeq = 0;\n   363\t    this._publicationSeq = 0;\n   364\t    this._refinementSeq = 0;\n   365\t\n   366\t    this._seedCoordinationTasks();\n   367\t\n   368\t    for (const adapter of Object.values(this._adapters)) {\n   369\t      adapter.onEvent((e) => {\n   370\t        try { this._handleEvent(e); } catch (err) {\n   371\t          // Adapter callbacks are an asynchronous trust boundary. A fatal authoritative-write\n   372\t          // failure has already poisoned this coordinator; do not let it become an uncaught\n   373\t          // process exception. The next public command observes the fatal error.\n   374\t          if (!this._fatalError) throw err;\n   375\t        }\n   376\t      });\n   377\t    }\n   378\t\n   379\t    // D10: reconcile first, then rebuild all state purely from the log.\n   380\t    if (this._worktrees && typeof this._worktrees.reconcile === 'function') {\n   381\t      Promise.resolve(this._worktrees.reconcile()).catch(noop);\n   382\t    }\n   383\t    if (this._runtimeScopes && typeof this._runtimeScopes.reconcile === 'function') {\n   384\t      Promise.resolve(this._runtimeScopes.reconcile([])).catch(noop);\n   385\t    }\n   386\t    this._replay();\n   387\t    this._terminalizeUnattachedCoordinationTasks();\n   388\t  }\n   389\t\n   390\t  // =========================================================================\n   391\t  // tick() — dispatch + deadline sweep. Called implicitly by every public command.\n   392\t  // =========================================================================\n   393\t\n   394\t  tick() {\n   395\t    if (this._fatalError) throw this._fatalError;\n   396\t    this._sweepDeadlines();\n   397\t    this._dispatchPass();\n   398\t  }\n   399\t\n   400\t  _dispatchPass() {\n   401\t    for (const taskId of this._taskOrder) {\n   402\t      const task = this._tasks.get(taskId);\n   403\t      if (!task || task.status !== 'pending') continue;\n   404\t      if (task.deps.some((d) => this._tasks.get(d)?.status !== 'completed')) continue;\n   405\t      const selection = this._resolveVendor(task);\n   406\t      const vendor = selection?.vendor;\n   407\t      if (!vendor || !this._adapters[vendor]) continue;\n   408\t      const card = this._adapters[vendor].card();\n   409\t      if (this._inFlightCount(vendor) >= card.concurrencyCeiling) continue;\n   410\t      this._dispatch(task, vendor, selection.model);\n   411\t    }\n   412\t  }\n   413\t\n   414\t  _sweepDeadlines() {\n   415\t    const now = this._now();\n   416\t    for (const [requestId, record] of [...this._pending]) {\n   417\t      if ((record.kind === 'approval' || record.kind === 'publication') && record.state === 'pending' && record.deadlineAt != null && now >= record.deadlineAt) {\n   418\t        this._resolveRecord(requestId, { decision: 'deny' }, 'policy').catch(noop);\n   419\t      }\n   420\t    }\n   421\t    for (const [workerId, waiter] of [...this._stopWaiters]) {\n   422\t      if (!waiter.finalized && waiter.deadlineAt != null && now >= waiter.deadlineAt) {\n   423\t        this._forceStop(workerId, waiter);\n   424\t      }\n   425\t    }\n   426\t  }\n   427\t\n   428\t  _resolveVendor(task) {\n   429\t    if (task.vendorRequested !== 'auto') {\n   430\t      const vendor = task.vendorRequested;\n   880\t      return;\n   881\t    }\n   882\t    if (!existsSync(context.worktree)) {\n   883\t      throw new SessionSelectionError(`session worktree does not exist: ${context.worktree}`, 'session_context_missing');\n   884\t    }\n   885\t  }\n   886\t\n   887\t  /** PS7: explicitly reattach a replayed native session. Recovery never trusts a stale PID;\n   888\t   * authority comes only from a fresh adapter handshake that reports the expected native ID. */\n   889\t  async recover(workerId, opts = {}) {\n   890\t    this.tick();\n   891\t    const handle = this._getWorker(workerId);\n   892\t    const task = this._tasks.get(handle.taskId);\n   893\t    if (handle.status !== 'orphaned') return { ok: false, result: 'worker_not_orphaned' };\n   894\t    if (!task || !handle.sessionRef || handle.sessionRef.persistence !== 'native') {\n   895\t      return { ok: false, result: 'session_not_resumable' };\n   896\t    }\n   897\t    const adapter = this._adapters[handle.vendor];\n   898\t    if (!adapter || !cardSupportsSession(adapter.card(), { mode: 'resume' })) {\n   899\t      return { ok: false, result: 'session_not_resumable' };\n   900\t    }\n   901\t    const rawContext = opts.context ?? handle.sessionContext;\n   902\t    const context = rawContext\n   903\t      ? normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context: rawContext }).context\n   904\t      : null;\n   905\t    if (!context) return { ok: false, result: 'session_context_required' };\n   906\t    try {\n   907\t      await this._validateSessionContext(context);\n   908\t    } catch (err) {\n   909\t      return { ok: false, result: err.code ?? 'session_context_mismatch', reason: err.message };\n   910\t    }\n   911\t\n   912\t    const timeoutMs = opts.timeoutMs ?? this._recoveryTimeoutMs;\n   913\t    const admission = { events: [] };\n   914\t    admission.spawned = new Promise((resolve) => { admission.resolveSpawned = resolve; });\n   915\t    handle.turnAdmission = admission;\n   916\t    const session = normalizeSessionRequest({ mode: 'resume', id: handle.sessionRef.id, context });\n   917\t    const recoveryRequested = this._log.append({\n   918\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n   919\t      kind: 'control.recovery_requested', actor: opts.actor ?? 'orchestrator',\n   920\t      payload: { sessionRef: handle.sessionRef, context },\n   921\t    });\n   922\t    const recoveryEvidence = this._coordMapEvent(recoveryRequested);\n   923\t    this._coordRecord('recovery.requested', {\n   924\t      taskId: task.id, workerId, sessionId: handle.sessionRef.id, context, evidence: recoveryEvidence,\n   925\t    }, `driver.recovery.requested:${task.id}:${recoveryRequested.seq}`, opts.actor ?? 'orchestrator');\n   926\t    const runtime = this._ensureRuntimeScope(handle);\n   927\t\n   928\t    let timerHandle;\n   929\t    let timedOut = false;\n   930\t    const timeout = new Promise((resolve) => {\n   931\t      timerHandle = this._setTimeout(() => { timedOut = true; resolve({ timeout: true }); }, timeoutMs);\n   932\t      if (timerHandle && typeof timerHandle.unref === 'function') timerHandle.unref();\n   933\t    });\n   934\t    const attempt = Promise.resolve(adapter.spawn(workerId, task.brief, {\n   935\t      worktree: context.worktree,\n   936\t      timeoutMs: task.brief?.budget?.wallMin ? task.brief.budget.wallMin * 60000 : undefined,\n   937\t      model: handle.modelResolved ?? undefined,\n   938\t      reasoningEffort: handle.modelPolicy?.reasoningEffort,\n   939\t      serviceTier: handle.modelPolicy?.serviceTier,\n   940\t      session,\n   941\t      env: runtime?.env,\n   942\t      replaceEnv: runtime?.replaceEnv === true,\n   943\t    })).then((ack) => ({ ack }), (error) => ({ error }));\n   944\t\n   945\t    let outcome = await Promise.race([attempt, timeout]);\n   946\t    if (outcome?.ack?.ok === true && !timedOut) {\n   947\t      outcome = await Promise.race([\n   948\t        admission.spawned.then((event) => ({ ack: outcome.ack, spawned: event })),\n   949\t        timeout,\n   950\t      ]);\n   951\t    }\n   952\t    if (timerHandle != null) this._clearTimeout(timerHandle);\n   953\t\n   954\t    const expectedId = handle.sessionRef.id;\n   955\t    const observedId = outcome?.spawned?.payload?.threadId ?? outcome?.spawned?.payload?.sessionId;\n   956\t    const failed = outcome?.timeout\n   957\t      ? { result: 'recovery_timeout', reason: `native reattachment exceeded ${timeoutMs}ms` }\n   958\t      : outcome?.error\n   959\t        ? { result: 'recovery_exception', reason: String(outcome.error?.message ?? outcome.error) }\n   960\t        : outcome?.ack?.ok !== true\n   961\t          ? { result: 'recovery_refused', reason: outcome?.ack?.reason ?? 'adapter refused recovery' }\n   962\t          : observedId !== expectedId\n   963\t            ? { result: 'session_identity_mismatch', reason: `expected ${expectedId}, observed ${observedId ?? '(none)'}` }\n   964\t            : null;\n   965\t\n   966\t    if (failed) {\n   967\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n   968\t      handle.status = 'orphaned';\n   969\t      this._log.append({\n   970\t        worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: this._safeTurnEpoch(handle),\n   971\t        kind: 'control.recovery_failed', actor: 'policy', payload: { ...failed, action: 'kill_untrusted_transport' },\n   972\t      });\n   973\t      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n   974\t      return { ok: false, ...failed };\n   975\t    }\n   976\t\n   977\t    const stamp = this._fences.bumpTurn(workerId);\n   978\t    let activeTask;\n   979\t    try {\n   980\t      activeTask = this._createCoordinationRefinement(handle, task, 'recovery');\n   981\t    } catch (err) {\n   982\t      if (handle.turnAdmission === admission) handle.turnAdmission = null;\n   983\t      handle.status = 'orphaned';\n   984\t      Promise.resolve(adapter.kill(workerId)).catch(noop).finally(() => this._removeRuntimeScope(handle));\n   985\t      throw err;\n   986\t    }\n   987\t    activeTask.status = 'working';\n   988\t    activeTask.result = null;\n   989\t    activeTask.verdict = null;\n   990\t    activeTask.sessionRequest = session;\n   991\t    activeTask.sessionContext = context;\n   992\t    handle.status = 'working';\n   993\t    handle.turnTerminalObserved = false;\n   994\t    this._clearBudgetStop(handle);\n   995\t    handle.sessionRequest = session;\n   996\t    handle.sessionContext = context;\n   997\t    handle.turnAdmission = null;\n   998\t    this._resetWatchdogTurn(handle);\n   999\t    this._log.append({\n  1000\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1001\t      kind: 'control.recovery_attached', actor: 'orchestrator',\n  1002\t      payload: { sessionRef: handle.sessionRef, context },\n  1003\t    });\n  1004\t    this._log.append({\n  1005\t      worker: workerId, harness: this._harnessOf(handle.vendor), turnEpoch: stamp.turnEpoch,\n  1006\t      kind: 'lifecycle.turn_started', actor: 'orchestrator', payload: { recovery: true },\n  1007\t      modelRequested: handle.modelRequested ?? null, modelResolved: handle.modelResolved ?? null, modelObserved: handle.modelObserved ?? null,\n  1008\t    });\n  1009\t    for (const event of admission.events) this._handleEvent(event);\n  1010\t    return { ok: true, result: 'attached', handle: this._publicHandle(handle) };\n  2688\t          // never let a broken router affect coordinator correctness\n  2689\t        }\n  2690\t      }\n  2691\t    } catch (err) {\n  2692\t      const errorEvent = this._log.append({\n  2693\t        worker: handle.id,\n  2694\t        harness,\n  2695\t        turnEpoch: this._safeTurnEpoch(handle),\n  2696\t        kind: 'error',\n  2697\t        actor: 'policy',\n  2698\t        payload: { message: String((err && err.message) || err), phase: 'trust_gate', trustPhase },\n  2699\t      });\n  2700\t      let durable = this._coordination.task(task.id);\n  2701\t      if (durable && !TERMINAL_TASK_STATUSES.has(durable.status)) {\n  2702\t        try {\n  2703\t          const evidence = this._coordMapEvent(errorEvent);\n  2704\t          const transitioned = this._coordination.transitionTask(task.id, 'failed', durable.version, {\n  2705\t            actor: 'policy', key: `task.failed:${task.id}:trust_gate:${errorEvent.seq}`,\n  2706\t          }, evidence ?? { reason: 'trust_gate_exception', trustPhase });\n  2707\t          task.coordinationVersion = transitioned.task.version;\n  2708\t          durable = transitioned.task;\n  2709\t        } catch (coordinationError) {\n  2710\t          this._poisonCoordination(coordinationError);\n  2711\t          durable = this._coordination.task(task.id);\n  2712\t        }\n  2713\t      }\n  2714\t      if (['evidence_mapping', 'terminal_batch', 'promotion'].includes(trustPhase)) this._poisonCoordination(err);\n  2715\t      task.status = durable?.status ?? 'failed';\n  2716\t      if (task.status !== 'completed') task.verdict = null;\n  2717\t    } finally {\n  2718\t      if (verifyPath != null) await this._worktrees.removeVerifyWorktree(verifyPath);\n  2719\t      if (baseVerifyPath != null) await this._worktrees.removeVerifyWorktree(baseVerifyPath);\n  2720\t    }\n  2721\t\n  2722\t    handle.status = 'idle';\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '240,320p;360,430p;880,1010p;2688,2722p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":33,"ts":"2026-07-11T20:20:41.611Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":61209,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":196566,"inputTokens":193482,"cachedInputTokens":124672,"outputTokens":3084,"reasoningOutputTokens":1402},"last":{"totalTokens":61209,"inputTokens":60577,"cachedInputTokens":54656,"outputTokens":632,"reasoningOutputTokens":367},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":196566,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":34,"ts":"2026-07-11T20:20:41.612Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":35,"ts":"2026-07-11T20:20:41.612Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"I’ve found one likely gap already: the coordinator records `publication.authorized`, but replay only reconstructs `publication.completed`. If the publisher runs and completion write fails, restart appears to forget the publish-side ambiguity entirely instead of closing it in state. Checking that path precisely now."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":36,"ts":"2026-07-11T20:20:56.422Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_bWFBQ2Qc3aAtZ2Ckq4KNMVK7","command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '2828,2850p'\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"39355","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"read","command":"nl -ba impl/src/coordinator.mjs | sed -n '2828,2850p'","name":"coordinator.mjs","path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/impl/src/coordinator.mjs"}],"aggregatedOutput":"  2828\t            }\n  2829\t            break;\n  2830\t          case 'verify.reverified':\n  2831\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) {\n  2832\t              verdict = e.payload?.verdict ?? null;\n  2833\t              terminalStatus = e.payload?.accept ? 'completed' : 'failed';\n  2834\t              capturedSha = e.payload?.capture?.sha ?? capturedSha;\n  2835\t            }\n  2836\t            break;\n  2837\t          case 'integration.completed':\n  2838\t            integration = e.payload ?? integration;\n  2839\t            retainedResultRef = null;\n  2840\t            break;\n  2841\t          case 'integration.refused':\n  2842\t            retainedResultRef = e.payload?.retainedResultRef ?? retainedResultRef;\n  2843\t            break;\n  2844\t          case 'publication.completed':\n  2845\t            publication = e.payload ?? publication;\n  2846\t            break;\n  2847\t          case 'lifecycle.crashed':\n  2848\t          case 'control.forced_stop':\n  2849\t          case 'control.recovery_terminalized':\n  2850\t            if (!TERMINAL_TASK_STATUSES.has(terminalStatus)) terminalStatus = 'failed';\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"nl -ba impl/src/coordinator.mjs | sed -n '2828,2850p'\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":37,"ts":"2026-07-11T20:20:56.498Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_3J6qpbGttyiVSSeL7UMAsIBI","command":"/bin/zsh -lc \"rg -n \\\"publication\\\\.authorized|publication\\\\.requested|publication\\\\.denied|publication\\\\.refused|publication\\\\.completed\\\" impl/src/coordinator.mjs\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"94096","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"search","command":"rg -n \"publication\\\\.authorized|publication\\\\.requested|publication\\\\.denied|publication\\\\.refused|publication\\\\.completed\" impl/src/coordinator.mjs","query":"publication\\.authorized|publication\\.requested|publication\\.denied|publication\\.refused|publication\\.completed","path":"coordinator.mjs"}],"aggregatedOutput":"1138:      kind: 'publication.requested', actor,\n1142:    this._coordRecord('publication.requested', {\n1144:    }, `driver.publication.requested:${task.id}:${requestId}`, actor);\n1958:            kind: 'publication.refused', actor: 'policy',\n1962:          this._coordRecord('publication.refused', { taskId: handle.taskId, requestId, reason: 'stale_fence', publication: record.publication, evidence }, `driver.publication.refused:${handle.taskId}:${requestId}`, 'policy');\n1973:          kind: 'publication.denied', actor,\n1977:        this._coordRecord('publication.denied', { taskId: handle.taskId, requestId, publication: record.publication, evidence }, `driver.publication.denied:${handle.taskId}:${requestId}`, actor);\n1993:          kind: 'publication.authorized', actor,\n1997:        this._coordRecord('publication.authorized', { taskId: handle.taskId, requestId, publication: record.publication, fence: record.fenceAtAsk, evidence }, `driver.publication.authorized:${handle.taskId}:${requestId}`, actor);\n2020:          kind: 'publication.completed', actor, payload: task.publication,\n2029:        this._coordRecord('publication.completed', { taskId: task.id, publication: task.publication, evidence: publicationEvidence }, `driver.publication:${task.id}:${publicationEvent.seq}`, actor);\n2844:          case 'publication.completed':\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"rg -n \\\"publication\\\\.authorized|publication\\\\.requested|publication\\\\.denied|publication\\\\.refused|publication\\\\.completed\\\" impl/src/coordinator.mjs\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":38,"ts":"2026-07-11T20:20:56.506Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":66947,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":263513,"inputTokens":259683,"cachedInputTokens":184960,"outputTokens":3830,"reasoningOutputTokens":1892},"last":{"totalTokens":66947,"inputTokens":66201,"cachedInputTokens":60288,"outputTokens":746,"reasoningOutputTokens":490},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":263513,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":39,"ts":"2026-07-11T20:20:56.508Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":0.5,"hardStop":false,"action":"notify","used":{"tokens":263513,"usd":0},"limits":{"tokens":500000,"usd":3},"ratio":0.527026,"dimensions":{"tokens":0.527026,"usd":0}},"seq":40,"ts":"2026-07-11T20:20:56.508Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":41,"ts":"2026-07-11T20:20:56.508Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"control.steer","actor":"orchestrator","payload":{"message":"Budget steer: stop all further repository exploration now. Write reviews/dogfood/codex-ck9-crash-window-review.md immediately from the evidence already collected, with the four exact required headings, then run only the pinned verification command."},"seq":42,"ts":"2026-07-11T20:20:56.572Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"I’ve isolated the reviewable gaps. Before writing, I’m checking whether the target review file already exists so I can update it cleanly without touching anything else."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":43,"ts":"2026-07-11T20:21:59.972Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_xKLJCfeXJQoGNn1ABH3ShSxS","command":"/bin/zsh -lc 'test -f reviews/dogfood/codex-ck9-crash-window-review.md && echo exists || echo missing'","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"8349","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"test -f reviews/dogfood/codex-ck9-crash-window-review.md && echo exists || echo missing"}],"aggregatedOutput":"missing\n","exitCode":0,"durationMs":0},"command":"/bin/zsh -lc 'test -f reviews/dogfood/codex-ck9-crash-window-review.md && echo exists || echo missing'","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":44,"ts":"2026-07-11T20:22:01.515Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":71103,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":334616,"inputTokens":327317,"cachedInputTokens":250880,"outputTokens":7299,"reasoningOutputTokens":5232},"last":{"totalTokens":71103,"inputTokens":67634,"cachedInputTokens":65920,"outputTokens":3469,"reasoningOutputTokens":3340},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":334616,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":45,"ts":"2026-07-11T20:22:01.517Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":75,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":46,"ts":"2026-07-11T20:22:01.518Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"Writing the review now. I’m keeping it to the deterministic CK9 gate, with explicit “no finding” rows where the collected evidence is sufficient, and separating that from later product features the spec still leaves open."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":47,"ts":"2026-07-11T20:22:28.456Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.file_edit","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"fileChange","id":"call_WLV6RYRwAz2FYDmaExkJf3TR","changes":[{"path":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/reviews/dogfood/codex-ck9-crash-window-review.md","kind":{"type":"add"},"diff":"## Verdict\nCK9 is not green. The new crash-window work closes several pre-effect and restart cases, but the collected evidence still leaves one critical and one major deterministic gap against `spec/phase11/coordination-knowledge.md` CK2/CK9 and the stricter “every claim must be test-locked and adversarially reviewed” bar in `docs/26-full-system-goal.md`.\n\nThis review does not count still-later product features as CK9 blockers. In particular, the existing restart behavior that drops pending publication approvals is fail-closed and aligns with the spec’s explicitly-later reconstruction work rather than with this gate.\n\n## Crash-window matrix\n| Area | Classification | Evidence |\n| --- | --- | --- |\n| Stop intent is durable before adapter stop/kill | No finding | `impl/test/phase11-coordination-store.test.mjs:403-425` proves injected `control.stop_requested` append failure calls no adapter and leaves the durable task working. |\n| Blocking input append failure does not expose blocked/pending state | No finding | `impl/test/phase11-coordination-store.test.mjs:378-400` keeps task state `working`, leaves `_pending` empty, and exposes no blocked worker after the write failure. |\n| Follow-up and recovery intent ordering for persistent sessions | No finding | `impl/src/coordinator.mjs:917-985` and `impl/src/coordinator.mjs:1318-1358`, plus `impl/test/phase11-persistent-sessions.test.mjs:152-169` and `:436-491`, show intent-before-effect and transport kill/quarantine when refinement fails after native advancement/attachment. |\n| Restart closure after stop/post-effect cancellation failure | No finding | `impl/src/coordinator.mjs:2881-2927` and `impl/test/phase11-coordination-store.test.mjs:429-453` close the nonterminal durable task to `failed` on replay after a bounded cancellation ambiguity. |\n| Publication request authorization is single-consumer and pre-effect safe | No finding | `impl/src/coordinator.mjs:1933-2035` and `impl/test/phase11-acceptance-integration.test.mjs:371-410` show one allow wins, a second responder does not republish, and authorization append failure calls no publisher. |\n| Publication post-effect ambiguity is durably closed on restart | Critical finding | `impl/src/coordinator.mjs:1991-2029` performs authorization then publisher side effect before durable completion, but replay only rebuilds `publication.completed` at `impl/src/coordinator.mjs:2844-2845`. `impl/test/phase11-acceptance-integration.test.mjs:413-428` checks only the in-process throw, not restart closure. |\n| Integration pre-effect Git safety | No finding | `impl/src/coordinator.mjs:1013-1076` records `integration.requested` before merge and `impl/test/phase11-acceptance-integration.test.mjs:294-369` covers dirty/non-fast-forward refusal and intent-append failure without rewriting `HEAD`. |\n| Integration post-effect durable-substrate closure | Major finding | `impl/src/coordinator.mjs:1081-1102` logs integration success and mutates `task.integration` before artifact/knowledge/driver substrate writes, while replay trusts `integration.completed` at `impl/src/coordinator.mjs:2837-2840`. No collected test injects this post-merge path. |\n| Pending approval reconstruction after restart | No finding for CK9 | `impl/test/phase11-acceptance-integration.test.mjs:461-473` is fail-closed. That missing reconstruction is still a later product feature, not this deterministic gate. |\n\n## Remaining major findings\n- Critical: A successful publish can be hidden by restart instead of being closed as bounded ambiguity. In `impl/src/coordinator.mjs:1991-2029`, `publication.authorized` is durably recorded and the publisher is invoked before `publication.completed` is durably recorded. Replay only restores `publication.completed` (`impl/src/coordinator.mjs:2844-2845`); it has no closure path for “authorized/publisher-called but completion write failed.” The only CK9 test for this path, `impl/test/phase11-acceptance-integration.test.mjs:413-428`, stops before replay. That means a real external side effect can occur and a restart can collapse back to an ordinary completed task with no surfaced publication ambiguity.\n- Major: A successful local integration can replay as clean success without the full durable public-substrate evidence CK8 requires. `impl/src/coordinator.mjs:1081-1102` sets `task.integration`, writes `integration.completed` to the operational log, and only then appends the integration artifact, knowledge promotion, and durable driver record. Replay trusts the operational success marker at `impl/src/coordinator.mjs:2837-2840`. If any post-merge coordination append fails, the coordinator poisons in-process, but restart can still reconstruct `integration` from telemetry without proving the corresponding durable substrate event/artifact exists. The collected acceptance coverage only exercises pre-effect intent failure (`impl/test/phase11-acceptance-integration.test.mjs:353-369`), so CK9’s required post-effect replay closure is still unproven.\n\n## Required next actions\n- Add a deterministic temp-Git publication test that lets the publisher succeed, forces the durable `publication.completed` write to fail, restarts the driver, and asserts three things: no second publish is possible, the earlier authorization remains discoverable, and replay surfaces an explicit non-success/ambiguous publication outcome rather than silently dropping back to plain `completed`.\n- Add a deterministic temp-Git integration test that lets the merge succeed, then forces one of the post-merge coordination writes in `impl/src/coordinator.mjs:1087-1102` to fail. After restart, assert Baton does not present a clean integrated result unless the corresponding durable substrate evidence exists.\n- Do not mark CK9 green from the current suite. The pre-effect cases are materially better, but the remaining publish/integration post-effect replay gaps are still inside this deterministic gate. Keep broader pending-interaction reconstruction tracked as later product scope, not as justification to wave through these two failures.\n"}],"status":"completed"},"paths":["/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review/reviews/dogfood/codex-ck9-crash-window-review.md"]},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":48,"ts":"2026-07-11T20:22:53.672Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":74019,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":408635,"inputTokens":398518,"cachedInputTokens":318336,"outputTokens":10117,"reasoningOutputTokens":6605},"last":{"totalTokens":74019,"inputTokens":71201,"cachedInputTokens":67456,"outputTokens":2818,"reasoningOutputTokens":1373},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":408635,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":49,"ts":"2026-07-11T20:22:53.674Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":0.8,"hardStop":false,"action":"notify","used":{"tokens":408635,"usd":0},"limits":{"tokens":500000,"usd":3},"ratio":0.81727,"dimensions":{"tokens":0.81727,"usd":0}},"seq":50,"ts":"2026-07-11T20:22:53.674Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":76,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":51,"ts":"2026-07-11T20:22:53.674Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.message","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","text":"The review file is written. I’m running only the pinned verification command now."},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":52,"ts":"2026-07-11T20:22:55.360Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"kind":"content.tool_call","actor":"worker","payload":{"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","item":{"type":"commandExecution","id":"call_eqgp6FG0MnkZ0cuMOIHFrWDf","command":"/bin/zsh -lc \"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Verdict$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Crash-window matrix$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Remaining major findings$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Required next actions$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md\"","cwd":"/Users/wahargis/Development/Experiments/baton/.baton/wt/codex-ck9-crash-window-review","processId":"54351","source":"unifiedExecStartup","status":"completed","commandActions":[{"type":"unknown","command":"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Verdict$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Crash-window matrix$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Remaining major findings$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '^## Required next actions$' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md"}],"aggregatedOutput":null,"exitCode":0,"durationMs":0},"command":"/bin/zsh -lc \"test -s reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Verdict$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Crash-window matrix$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Remaining major findings$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q '\"'^## Required next actions$'\"' reviews/dogfood/codex-ck9-crash-window-review.md && grep -q 'CK9' reviews/dogfood/codex-ck9-crash-window-review.md\"","exitCode":0,"status":"completed"},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":53,"ts":"2026-07-11T20:22:59.093Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":74347,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":482982,"inputTokens":472618,"cachedInputTokens":389376,"outputTokens":10364,"reasoningOutputTokens":6621},"last":{"totalTokens":74347,"inputTokens":74100,"cachedInputTokens":71040,"outputTokens":247,"reasoningOutputTokens":16},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":482982,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":54,"ts":"2026-07-11T20:22:59.095Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":76,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":55,"ts":"2026-07-11T20:22:59.095Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"control.steer","actor":"orchestrator","payload":{"message":"Completion steer: the pinned artifact verification has passed. Do not inspect or edit anything else. Emit your final response immediately so Baton can run its independent trust gate."},"seq":56,"ts":"2026-07-11T20:22:59.171Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"tokenUsage","accounting":"delta","tokens":74493,"usd":0,"threadId":"019f52d5-e72b-7db2-be2a-d5cf321606f9","turnId":"019f52d5-e754-79f3-9341-d684d1220160","tokenUsage":{"total":{"totalTokens":557475,"inputTokens":547011,"cachedInputTokens":462976,"outputTokens":10464,"reasoningOutputTokens":6631},"last":{"totalTokens":74493,"inputTokens":74393,"cachedInputTokens":73600,"outputTokens":100,"reasoningOutputTokens":10},"modelContextWindow":258400},"wireAccounting":"cumulative","wireTokens":557475,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":57,"ts":"2026-07-11T20:23:01.963Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"resource.budget_threshold","actor":"policy","payload":{"threshold":1,"hardStop":true,"action":"kill","used":{"tokens":557475,"usd":0},"limits":{"tokens":500000,"usd":3},"ratio":1.11495,"dimensions":{"tokens":1.11495,"usd":0}},"seq":58,"ts":"2026-07-11T20:23:01.963Z"}
{"worker":"w-1","harness":"codex","turnEpoch":1,"actor":"worker","kind":"resource.tokens","payload":{"source":"rateLimit","rateLimits":{"limitId":"codex","limitName":null,"primary":{"usedPercent":76,"windowDurationMins":300,"resetsAt":1783804277},"secondary":{"usedPercent":36,"windowDurationMins":10080,"resetsAt":1784355072},"credits":null,"individualLimit":null,"planType":"pro","rateLimitReachedType":null},"tokens":0,"usd":0,"accounting":"delta","wireAccounting":"delta","wireTokens":0,"wireUsd":0},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":59,"ts":"2026-07-11T20:23:01.963Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"kill.requested","actor":"policy","payload":{},"seq":60,"ts":"2026-07-11T20:23:03.960Z"}
{"worker":"w-1","harness":"codex@codex-cli 0.144.0","turnEpoch":2,"kind":"kill.confirmed","actor":"worker","payload":{},"modelRequested":"gpt-5.4","modelResolved":"gpt-5.4","modelObserved":"gpt-5.4","seq":61,"ts":"2026-07-11T20:23:03.980Z"}
