# Cold restart recovery, 2026-09-23

## Situation

The host froze on 2026-09-22 at 20:44 UTC while the deployment ran 32 worker seats and a full
`npm test --prefix impl` (issue #561). The resident restarted on master `65c913f0` at about 02:30
UTC on 2026-09-23. Every seat of this swarm was dead at restart. The uncommitted snapshots named
below are preserved on `origin` under `refs/baton/preserve/`, the predecessor ref below exists
only in this checkout, and the contribution ledger retains every recorded row.

## Resumed seats

`bend2-orchestrator5` took the lead seat at restart. Two seats carry the unlanded work:

| Successor | Route | Run | Carries |
|---|---|---|---|
| `bend2-laws-lead6` | omp / deepseek-flash / high | `run-6912465f9a0f620a9570ca91a02e9e2e` | The approved law encoding increment: `laws.bend`, `laws-trace.md`, `laws-check.py`, the three model examples, and the `laws-proposed.md` and `laws-design-notes.md` repairs, preserved at `refs/baton/preserve/uncommitted/ws-947333eea5e0c5fbc297483e14bd9519/20260923T022150Z`. |
| `bend2-arch-lead4` | omp / deepseek-flash / high | `run-0991c36655a8ee2dde2e40bbdfc6eee5` | The architecture corrections and the rebased plan, then the pure-decision/replay and native-effect prototypes and the composed durable-admission-to-canonical-publication proof, preserved at `refs/baton/preserve/uncommitted/ws-8ddb241b1545328062dacc12b9b5807e/20260923T022150Z`. |

One attempt to keep a predecessor session through `resumeFrom` refused
`session_context_mismatch` against the predecessor checkpoint, and two fresh seats on the codex
route (`bend2-laws-lead5`, `bend2-arch-lead3`) died `provider_crashed` within seconds of
admission. The replacement seats run on the mandate's omp worker route, and the host budget holds
the lead plus two seats while issue #561 is open.

## Retained work

| Ref | Content | Owner |
|---|---|---|
| `refs/baton/preserve/uncommitted/ws-947333eea5e0c5fbc297483e14bd9519/20260923T022150Z` | The law encoding increment | `bend2-laws-lead6` |
| `refs/baton/preserve/uncommitted/ws-8ddb241b1545328062dacc12b9b5807e/20260923T022150Z` | The architecture corrections and the rebased plan and target | `bend2-arch-lead4` |
| `refs/baton/preserve/uncommitted/ws-42660416c8ff54323d8111d9c5925fa6/20260923T022150Z` | The language capability examples (`lang-cap-dropped-child.*`, `lang-cap-durability*`) | `bend2-orchestrator5`; recovered, re-run at the pin, and published as an accepted contribution. |
| `refs/baton/preserve/bend2-laws-lead4/predecessor` (`881be83b`, local only) | The earlier law draft including `examples/laws-check.evidence.md` | `bend2-laws-lead6` |

Every dead seat's holder was released through `swarm.holder_released`, and five releases reported
an assignment row they cleared: `bend2-arch-lead2`, `bend2-laws-lead4`, `bend2-reviewer2`,
`bend2-laws-lead5`, and `bend2-arch-lead3`. The reviewer's six review records are captured in
`contribution-20dc89e0651fa4d7c041383599183261` and accepted. The law increment, the architecture
corrections, the recovery records, and the language examples each carry an accepted contribution;
their landing waits on the publication declaration below.

## Landed in this recovery

- `docs/bend2/reviews/codex/`: the Codex architecture review with its delivery message, manifest,
  Base surface checks, and the dropped-child probe, copied byte for byte and verified against
  `codex-review-manifest.json`. The directory README states the verdict, the seven required
  corrections, and every file hash.
- The root's approved-scope and recovery records carried onto `bend2-rewrite` byte for byte:
  `README.md`, `docs/bend2/MANDATE.md`, `docs/bend2/README.md`, `docs/bend2/authorization.md`,
  `docs/bend2/recovery-2026-09-22.md`, and `docs/bend2/reviews/codex-final-law-review-r9.1.md`.
- [`go-no-go.md`](go-no-go.md) rebased on the approved 16 laws and the architecture review: the
  law basis is the approved set with [`laws-trace.md`](laws-trace.md)'s checked scope, and the
  prerequisites an eventual Go rests on name the durability, process, JSON, HTTP/TLS,
  cancellation and supervision, and cryptography families.

Commit `b69c1f82` carries the first two items and commit `ad8e1c28` carries the third.

## Landing state

A real `swarm.integrate` on `bend2-rewrite` refuses `integrate_publish_undeclared`: the serving
deployment declares no `advanced.integration.publishRemote`, so a landing has nowhere to publish
(the #558 behavior on master `65c913f0`). The same contribution's dry run prepared its squash
`093660fba8f12c3cd742ca679c72306c4d4ac3c6` over six `docs/bend2/reviews/` paths with the gate
selection skipped (`no_affected_tests`) and no conflicts. Landings resume when the deployment
declares the shared remote and reloads the resident.

`git ls-remote origin refs/heads/bend2-rewrite` reported
`cc1a18766c5284b556d0e8622e3436980761407c` at the time of writing, matching the local target.
A completed landing needs a fresh observation of that ref.
