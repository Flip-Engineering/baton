# Bend2 proof examples index

Every example pairs a `.bend` program (header: `# CLAIM:`) with a `.evidence.md` recording the
host, the pinned reference, the toolchain, every command with its verbatim output, and a verdict.
The convention is fixed by `README.md` in this directory. This index is named `index.md` so no
test selects it: a changed `README.md` draws a wide gate set at landing (issue #546), and
`README.md` itself is owned by the orchestrator.

Rows marked "landing with the language review" arrive on the same branch as
[`../language-review.md`](../language-review.md) (cherry-picked from the lane branch
`baton/ws-78f229391cd49ec10cf1bb072bdd1a55`, commits `18065ce9` and `70f0078f`, unchanged).
The `lang-cap-*` programs share one evidence file, `lang-cap-probes.evidence.md`, which records
the probe battery, the checker facts the probes had to learn, and the match against the
independent reviewer's probe table (Codex review r3); the table's last column names each row's
probe letter.

| Example | Claim (one line) | Files | Verdict |
|---|---|---|---|
| lang-core-types | The checker enforces affine use and Data-only copying, verifies termination by shrinking arguments, and refuses computed scrutinees, mutual recursion and reusable closures — each with a precise message. | `lang-core-types.bend`, `lang-core-types.evidence.md` | Claim holds at the pin; four refusals and one positive run recorded. |
| lang-core-errors | Fallible effects answer the handle beside a `Result`, `IO.try` unwraps or exits (code 2), the `Fail` branch carries a U32 code and String message, and a `do` block binds only plain annotated binders. | `lang-core-errors.bend`, `lang-core-errors.evidence.md` | Claim holds at the pin; EBADF with a live handle, `IO.try` exit, and the destructuring refusal recorded. |
| lang-core-effects | A foreign effect is a def whose body names a `.c` and a `.js` import; the same program runs interpreted, as a native executable (C host) and as emitted JavaScript under Node. | `lang-core-effects.bend`, `lang-core-effects.c`, `lang-core-effects.js`, `lang-core-effects.evidence.md` | Claim holds at the pin; all three lanes print `Core.double(21) = 42`. |
| lang-core-imports | A module is a file imported by relative path under a local alias; an open law is proven in another file as `def M.name`; a Hub import fetches hash-pinned content at check time, so a checked-in module is the offline (vendored) form. | `lang-core-imports.bend`, `lang-core-imports-lib.bend`, `lang-core-imports.evidence.md` | Claim holds at the pin; open-law TODO refusal, cross-file proof, missing-file refusal, and the Hub-fetch refusal naming its URL recorded. |
| lang-host-concurrency | The parallel call is two calls in one statement; the native runtime spreads it over the cores given to `--threads` (median 1.736 s at 1 thread, 0.330 s at 10, identical values); `IO.fork`/`IO.join` run computations concurrently on one event loop; the JavaScript target runs sequentially. | `lang-host-concurrency.bend`, `lang-host-concurrency.evidence.md` | Claim holds at the pin; 5-run scaling table and the `bun:ffi` boundary recorded. |
| lang-host-gpu | `!` marks a parallel call for the GPU: the native build emits a Metal `.gpu` companion, `--gpu off` runs the CPU, and a binary without its companion recompiles the GPU program; both paths print the same value. | `lang-host-gpu.bend`, `lang-host-gpu.evidence.md` | Claim holds at the pin; `file(1)` output for the companion and both run paths recorded. |
| lang-host-interop | Base ships filesystem, TCP, UDP and environment effects (one program runs all four on three lanes) and ships no process spawn and no JSON (`bend base exec`/`Json` refuse). | `lang-host-interop.bend`, `lang-host-interop.evidence.md` | Claim holds at the pin; six-line run output, the full `bend base` listing, and the two absent-surface refusals recorded. |
| lang-host-foreign | A host effect Base does not ship is a def with a `.c`/`.js` pair: a process-spawn effect type-checks and runs on all lanes, answers whole stdout and the exit status, blocks the event loop for the command's life, and carries no handle, streaming, signals or kill. | `lang-host-foreign.bend`, `lang-host-foreign-exec.c`, `lang-host-foreign-exec.js`, `lang-host-foreign.evidence.md` | Claim holds at the pin; both branches (success, `exit 3`) on interpreter and native lanes recorded. |
| lang-host-tooling | The toolchain is one command (check, run, native, JavaScript, page bundle, publish); the `#|` convention is `gates/test.ts`'s judge; `test`, `debug`, `repl` and `fmt` do not exist; the one companion tool is a formatting-only LSP. | `lang-host-tooling.bend`, `lang-host-tooling.evidence.md` | Claim holds at the pin; the command surface, the byte-identical `#|` diff, and the upstream test inventory recorded. |
| c-only-spawn | An effect whose only host half is a C file builds a native binary that runs with no JavaScript in its runtime; the interpreter lane refuses the same file. | `c-only-spawn.bend`, `c-only-spawn-exec.c`, `c-only-spawn.evidence.md` | Claim holds at the pin. Landed on `bend2-rewrite` (contribution `dfa52a1f38c9108a4bbdfc331e73e519`). |
| toolchain-sanity | The pinned toolchain checks, runs and builds on this host. | `toolchain-sanity.bend`, `toolchain-sanity.evidence.md` | Owned by bend2-orchestrator2; landed on `bend2-rewrite`. |
| lang-cap-forge | An exported affine constructor is forgeable from an importing module: `L.Lease{999}` checks and `release` answers the forged field, so naming a type establishes no provenance. | `lang-cap-lease.bend`, `lang-cap-forge.bend`, `lang-cap-probes.evidence.md` (probe a) | Reproduced at the pin (reviewer probe table row 1). |
| lang-cap-drop | An affine value can be dropped without its release path; affinity enforces at-most-once use, and the duplicate control is refused with "consumed more than once". | `lang-cap-drop.bend`, `lang-cap-probes.evidence.md` (probe b) | Reproduced at the pin (reviewer probe table rows 2–3). |
| lang-cap-history | A pure fold returning a fixed-shape result keeps nothing: `add_review([1,2], 3)` answers `[3]` and checks. | `lang-cap-history.bend`, `lang-cap-probes.evidence.md` (probe c) | Reproduced at the pin (reviewer probe table row 4). |
| lang-cap-history-law | A stated law separates preservation from shape: the concrete equality rejects the discarding fold (expected `[3]`, observed `[1, 2, 3]`) and accepts the appending one; the law that binds takes the quantified form. | `lang-cap-history-law.bend`, `lang-cap-probes.evidence.md` (probe c) | Reproduced at the pin (reviewer probe table rows 7–8). |
| lang-cap-receipt | A receipt-shaped result establishes no durable write: the program sleeps, acknowledges `Receipt{1}`, and the named path never exists. | `lang-cap-receipt.bend`, `lang-cap-probes.evidence.md` (probe d) | Reproduced at the pin (reviewer probe table row 6). |
| lang-cap-second-predicate | A type does not force every reader through one function: two defs both return `Custody`, the second builds `Custody{0}`. | `lang-cap-second-predicate.bend`, `lang-cap-probes.evidence.md` (probe e) | Reproduced at the pin (reviewer probe table row 5). |
| lang-cap-dropped-child | Dropping the `Chan` returned by `IO.fork` does not cancel or join the computation: the parent returns first, the child continues after its sleep, and the process exits only after the child completes. | `lang-cap-dropped-child.bend`, `lang-cap-dropped-child.evidence.md` | Reproduced at the pin on the interpreter and native lanes (executable 1,127,848 bytes) by the recovery seat, 2026-09-23. |
| lang-cap-durability | Base writes and closes a file and its Bend-visible results carry no durability receipt; the checker refuses `File.sync`, `File.rename` and `File.stat` because those names do not exist at the pin. | `lang-cap-durability.bend`, `lang-cap-durability-sync.bend`, `lang-cap-durability-stat.bend`, `lang-cap-durability-rename.bend`, `lang-cap-durability.evidence.md` | Reproduced at the pin (write/close control, three refusals, executable 1,128,216 bytes) by the recovery seat, 2026-09-23. |
| arch-replay-stop | One frozen stop trace decides the same required behaviour in a pure Bend2 model and in the branch's recorded-row fold, and the two causes a refusal does not separate are the gap. | `arch-replay-stop.bend`, `arch-replay-stop.mjs`, `arch-replay-stop.evidence.md` and the companion `.cases`, `.expect`, `.ledger.jsonl`, `.reference.txt`, `.prototype.txt` | Reproduced at the pin by two seats; 3 cases, 2 disagreements (ARCH-CLOSE-11). |
| arch-replay-basis | A lease trace replayed under the issuing basis, a changed basis and no basis: the resident refuses what the doctor reports clean. | `arch-replay-basis.bend`, `arch-replay-basis.mjs`, `arch-replay-basis.evidence.md` and the same companions | Reproduced at the pin; 4 cases, 2 disagreements. |
| arch-replay-cursor | A restart preserves the cursor and owed delivery, and a checkpoint under another authority is stale, not corrupt. | `arch-replay-cursor.bend`, `arch-replay-cursor.mjs`, `arch-replay-cursor.evidence.md` and the same companions | Reproduced at the pin; 4 cases, 0 disagreements. |
| arch-effect-publication | Capability validation, durable acceptance, guarded publication and settlement without a repeat, with the primitives the pin lacks named. | `arch-effect-publication.bend`, `arch-effect-publication.evidence.md` | Reproduced at the pin; five modes, including dispatch_count unchanged across a fresh recovery process. |
| arch-publish-compose | The composed acceptance-to-publication path across four processes over one fixture tree. | `arch-publish-compose.sh`, `arch-publish-compose.evidence.md` | Reproduced at the pin; four stages, exit 0. |
| controlled-remote | The publication half of a landing against a controlled remote: a local-only completion, a wrong destination, the declared destination, and a lost response reconciled by observation. | `controlled-remote.sh`, `controlled-remote.evidence.md` | Reproduced at the pin (git 2.50.1); two runs byte-identical, exit 0. |
| arch-publish-content | Completion evidence from the published content: the declared destination's own object store is read at the commit its ref names, hashed, and compared with the prepared squash's listing. | `arch-publish-content.sh`, `arch-publish-content.evidence.md` | Reproduced at the pin; two runs identical, and a destination naming another commit reports no_evidence. |
| arch-publish-bind | The acceptance record bound to the published commit: one record line names the operation, the commit and the destination, and completion requires the record's commit to be the observed ref. | `arch-publish-bind.sh`, `arch-publish-bind.evidence.md` | Reproduced at the pin; binding_is_string_equality=true, digest_bound_record=none. |
| arch-publish-contention | Two attempts publish to one declared ref: the blind attempt is refused non-fast-forward, the later attempt reads, rebases and publishes, and the earlier claim re-reads as superseded with its commit preserved. | `arch-publish-contention.sh`, `arch-publish-contention.evidence.md` | Reproduced at the pin; two runs identical, supersession_is_distinct_from_loss=true. |
| arch-publish-cas | The local compare-and-swap: a landing holding a stale head is refused with cas=refused_target_moved and moves nothing, then re-reads and retries, and the moved target publishes to a declared ref that agrees with it. | `arch-publish-cas.sh`, `arch-publish-cas.evidence.md` | Reproduced at the pin; two runs identical, local_cas_and_destination_agree=true. |
