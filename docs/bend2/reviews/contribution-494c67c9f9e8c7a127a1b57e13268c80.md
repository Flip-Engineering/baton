# Review: contribution-494c67c9f9e8c7a127a1b57e13268c80

| | |
|---|---|
| Author | bend2-laws-lead3 |
| Captured at | `98048fbd` on top of branch head `60901316` (the merge of the #556 recovery lineage with revision 8); revises `docs/bend2/laws-proposed.md` to revision 9 and corrects `docs/bend2/laws-design-notes.md` |
| Decision | accept |
| Review row | `swarm.contribution_reviewed` seq see below, 2026-09-22 |
| Reviewer | bend2-reviewer, independent seat |
| Hold note | reviewed under the authorized check; this file is intentionally left **uncommitted** until the root resumes |

## What was run and what it answered

Diffed `60901316..98048fbd` (56 insertions, 61 deletions, both files — exactly the stated items)
and counted the final text:

- **Composition: 16 independent entries.** 16 M-headings (the M-9 heading is gone), 16
  `Forbidden:` statements, 16 `Binding reason:` statements, 16 status labels (5 extracted,
  8 partly enforced, 3 proposed, enumerated per entry). M-3a is relabeled
  `[partly enforced; two clauses are proposed — the evidence-staleness prohibition and the
  artifact-scoped dirty-tree requirement]`, identifying both proposed clauses.
- **M-9 is a traceability pointer only.** Its seven mentions are the section header ("M-9 is
  absorbed into M-8"), the absorption sentence in M-8 ("whose identifier is retained for
  traceability"), the absorbed-entries note (AB-04 named as its evidence row), the reference
  closure, and the operator-decision section — none is a second prohibition. AB-04 is routed to
  M-8 in the design notes, and the waiting / serializable-transaction /
  compatible-subdivision examples are preserved in M-8 as explanations of the coordination
  freedom.
- **Design-notes rows corrected in place, history retained**: LEDG-15 → M-3a (the
  stale-evidence prohibition; the gate-purity mechanism stays in the notes); AB-04 → M-8
  (absorbed with M-9); DEV-6 superseded-deferred; DEV-7 superseded-excluded.
- **The document ends with "Presented for the operator's decision"**: the Codex r8 clearance,
  the per-entry operator decision, the deferred and excluded entries, the absorbed clauses, the
  proof-mechanics attachment on approval, `laws.bend` still empty, and the lane holding under
  the pause.
- **Wording rule**: the document says "incorporated into the candidate set as proposed — they
  are not adopted until the operator"; the only other "adopted" occurrence is a quoted
  historical verdict string. The operator-facing prose is plain technical English.

## Decision

accept — the diff is exactly the stated reduction, the composition is 16 independent entries,
M-9 is traceability only, and the operator-facing section obeys the vocabulary rule. Ready to
land and present to the operator.
